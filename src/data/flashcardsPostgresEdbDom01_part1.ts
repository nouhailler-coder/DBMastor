import { FlashcardItem } from '../types';

export const postgresEdbDom01Part1Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 1 : MODÈLE MULTI-PROCESSUS, POSTMASTER & CYCLE DE VIE D'UNE CONNEXION (Cartes 1 à 10)
  // Processus maître, fork(), backend process, limitation mémoire, thread vs process
  // =========================================================================
  {
    id: 'fc-pgedb-dom01-001',
    cardNumber: 1,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Modèle de processus PostgreSQL vs Threads',
    difficulty: 'easy',
    tags: ['Architecture', 'Postmaster', 'Multi-Process', 'Fork'],
    front: {
      question: 'Quel est le modèle d\'exécution fondamental de PostgreSQL pour traiter les requêtes clientes : multi-thread ou multi-processus ?',
      hint: 'Chaque connexion cliente est prise en charge par un processus système dédié créé par un fork du processus maître.',
    },
    back: {
      answer: 'PostgreSQL utilise un modèle multi-processus (client/server process-based) et non un modèle multi-thread.\nChaque nouvelle connexion cliente est allouée à un processus backend dédié ("postgres backend process"), créé par le processus maître ("postmaster") via l\'appel système fork().',
      explanation: 'Ce modèle offre une isolation mémoire étanche : si un backend plante ou rencontre un segfault, il ne corrompt pas la mémoire privée des autres backends, garantissant une résilience maximale.',
      examTrap: 'Ne confondez pas avec MySQL InnoDB ou Oracle qui utilisent massivement des threads légers. Sous PostgreSQL, chaque connexion est un vrai processus Unix avec son propre PID.',
      ruleRef: 'PostgreSQL 16 Core Documentation - Chapter 53: PostgreSQL Internals: Architecture Overview',
    },
  },
  {
    id: 'fc-pgedb-dom01-002',
    cardNumber: 2,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Rôle du processus Postmaster (postgres)',
    difficulty: 'easy',
    tags: ['Postmaster', 'Daemon', 'Port 5432', 'Supervision'],
    front: {
      question: 'Quel est le rôle principal du processus superviseur "Postmaster" (exécutable `postgres`) ?',
      hint: 'Il écoute sur le port réseau, alloue la mémoire partagée et supervise l\'ensemble des processus enfants.',
    },
    back: {
      answer: 'Le processus Postmaster est le démon superviseur parent de l\'instance. Il est responsable de :\n1. Initialiser la mémoire partagée (Shared Memory) et les sémaphores au démarrage.\n2. Démarrer les processus d\'arrière-plan auxiliaires (Checkpointer, BgWriter, WalWriter, Autovacuum Launcher).\n3. Écouter sur le socket Unix et le port TCP (défaut 5432) pour accueillir les connexions clientes et exécuter un `fork()` pour créer un backend dédié.\n4. Redémarrer l\'instance en cas de crash d\'un backend enfant.',
      explanation: 'Historiquement nommé "postmaster", l\'exécutable moderne s\'appelle "postgres", mais la terminologie "postmaster" reste le terme officiel pour désigner le processus père (PID racine).',
      ruleRef: 'EDB Certified Associate Exam Topic: Server Architecture & Postmaster responsibilities',
    },
  },
  {
    id: 'fc-pgedb-dom01-003',
    cardNumber: 3,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Fichier postmaster.pid et son contenu',
    difficulty: 'medium',
    tags: ['postmaster.pid', 'PGDATA', 'Cluster status', 'Socket'],
    front: {
      question: 'À quoi sert le fichier `postmaster.pid` situé à la racine du répertoire `$PGDATA` et que contient-il ?',
      codeSnippet: '$ cat $PGDATA/postmaster.pid\n14238\n/var/lib/postgresql/16/main\n1709884800\n5432\n/var/run/postgresql\n*\n5432001  184320',
      hint: 'Il empêche le lancement simultané de deux instances sur le même cluster de données.',
    },
    back: {
      answer: 'Le fichier `postmaster.pid` est un verrou (lockfile) attestant qu\'un serveur PostgreSQL est actif sur ce répertoire `$PGDATA`. Il contient sur des lignes distinctes :\n- Ligne 1 : Le PID du processus maître Postmaster.\n- Ligne 2 : Le chemin absolu de `$PGDATA`.\n- Ligne 3 : L\'horodatage de démarrage (epoch Unix).\n- Ligne 4 : Le numéro de port TCP configuré (ex: 5432).\n- Ligne 5 : Le répertoire du socket Unix.\n- Ligne 6 : L\'adresse d\'écoute réseau (listen_addresses).\n- Ligne 7 : Les identifiants de segments de mémoire partagée.',
      explanation: 'Si le serveur plante brutalement, `postmaster.pid` peut rester sur disque. À l\'initialisation, `pg_ctl` ou `postgres` vérifie si le PID mentionné existe encore sur l\'OS avant de démarrer.',
      examTrap: 'Ne supprimez JAMAIS manuellement `postmaster.pid` tant que vous n\'avez pas vérifié avec `ps -ef | grep postgres` qu\'aucun processus orphelin n\'accède au répertoire !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3: Starting the Database Server',
    },
  },
  {
    id: 'fc-pgedb-dom01-004',
    cardNumber: 4,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Cycle de vie d\'un Backend Process (Connexion cliente)',
    difficulty: 'medium',
    tags: ['Backend', 'Connexion', 'Cycle de vie', 'pg_stat_activity'],
    front: {
      question: 'Décrivez les étapes du cycle de vie d\'un backend PostgreSQL depuis la requête de connexion d\'un client jusqu\'à sa déconnexion.',
      hint: 'Écoute Postmaster -> Authentification pg_hba.conf -> Fork backend -> Exécution des requêtes -> Terminaison.',
    },
    back: {
      answer: '1. Le client initie une connexion TCP/IP sur le port 5432.\n2. Le Postmaster reçoit la demande et valide l\'adresse dans `pg_hba.conf`.\n3. Postmaster effectue un `fork()` pour créer un nouveau processus `postgres` backend.\n4. Le backend négocie l\'authentification (ex: SCRAM-SHA-256) et initialise ses tampons locaux.\n5. Le backend traite les requêtes SQL (Parse, Rewrite, Plan, Execute) en accédant aux tampons partagés.\n6. À la fermeture de session (ou `DISCONNECT`), le backend libère sa mémoire privée et se termine (`exit 0`).',
      explanation: 'Chaque backend consomme des descripteurs de fichiers, de la mémoire locale et un slot de connexion parmi la limite fixée par `max_connections`.',
      ruleRef: 'EDB Associate 16: Client Connection Handling & Backend Lifecycle',
    },
  },
  {
    id: 'fc-pgedb-dom01-005',
    cardNumber: 5,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Paramètre max_connections et coût mémoire',
    difficulty: 'medium',
    tags: ['max_connections', 'Dimensionnement', 'RAM', 'Connection Pooler'],
    front: {
      question: 'Pourquoi est-il déconseillé de régler `max_connections` à une valeur très élevée (ex: 2000) sans utiliser un pooler comme PgBouncer ?',
      hint: 'Chaque connexion consomme de la mémoire système dédiée et engendre de la contention sur les verrous internes de l\'ordonnanceur.',
    },
    back: {
      answer: '1. Consommation mémoire : Chaque backend alloue de 5 à 10 Mo de mémoire de base + les allocations dynamiques de `work_mem` par requête. 2000 connexions actives risquent de saturer la RAM et déclencher l\'OOM Killer Linux.\n2. Contention CPU & verrous : Un trop grand nombre de processus concurrents s\'affrontent pour les verrous légers (LWLocks) sur les tampons partagés et le catalogue système.\n3. Changements de contexte : Le noyau OS passe plus de temps en context switching qu\'en calcul effectif.',
      explanation: 'La recommandation standard est de régler `max_connections` entre 100 et 300 et d\'intercaler un pooler de connexions léger (PgBouncer ou pgpool-II) en mode transaction ou session.',
      examTrap: 'Une valeur de max_connections trop haute ne résout pas les goulots d\'étranglement ; elle aggrave la dégradation des performances sous forte charge.',
      ruleRef: 'EDB PostgreSQL Tuning Guide - Connection Scaling and Resource Limits',
    },
  },
  {
    id: 'fc-pgedb-dom01-006',
    cardNumber: 6,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Consulter les backends actifs via pg_stat_activity',
    difficulty: 'easy',
    tags: ['pg_stat_activity', 'psql', 'Monitoring', 'PID'],
    front: {
      question: 'Quelle vue système permet d\'identifier tous les processus backends connectés, leur état (`active`, `idle`), leur PID et la requête SQL en cours ?',
      hint: 'Vue statistique essentielle commençant par pg_stat_...',
    },
    back: {
      answer: 'La vue système `pg_stat_activity`.\nElle fournit une ligne par processus serveur (backends clients et processus d\'arrière-plan).',
      codeSnippet: 'SELECT pid, usename, datname, client_addr, state, wait_event_type, wait_event, query\nFROM pg_stat_activity\nWHERE backend_type = \'client backend\';',
      explanation: 'La colonne `state` prend les valeurs : `active` (exécute une requête), `idle` (en attente d\'une commande client), `idle in transaction` (transaction ouverte non validée), ou `idle in transaction (aborted)`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 28: The Cumulative Statistics System',
    },
  },
  {
    id: 'fc-pgedb-dom01-007',
    cardNumber: 7,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Terminaison contrôlée d\'un backend : pg_cancel_backend vs pg_terminate_backend',
    difficulty: 'medium',
    tags: ['pg_cancel_backend', 'pg_terminate_backend', 'SIGINT', 'SIGTERM'],
    front: {
      question: 'Quelle est la différence fondamentale entre `pg_cancel_backend(pid)` et `pg_terminate_backend(pid)` ?',
      hint: 'L\'un interrompt uniquement la requête SQL en cours, l\'autre ferme violemment la connexion réseau du client.',
    },
    back: {
      answer: '- `pg_cancel_backend(pid)` : Envoie un signal `SIGINT` au processus. Il annule la requête SQL en cours d\'exécution mais maintient la connexion cliente active et ouverte.\n- `pg_terminate_backend(pid)` : Envoie un signal `SIGTERM` au processus. Il tue immédiatement le backend et déconnecte définitivement la session cliente.',
      explanation: 'On tente toujours d\'abord un `pg_cancel_backend()`. Si le backend est bloqué sur un verrou ou un appel système non interruptible, on utilise `pg_terminate_backend()`.',
      examTrap: 'Ne jamais utiliser la commande shell Linux `kill -9 <pid>` sur un backend PostgreSQL ! Cela provoque un crash généralisé du Postmaster qui redémarre toute l\'instance pour restaurer la mémoire partagée.',
      ruleRef: 'PostgreSQL 16 System Administration Functions - Server Signaling Functions',
    },
  },
  {
    id: 'fc-pgedb-dom01-008',
    cardNumber: 8,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Conséquence d\'un signal SIGKILL (kill -9) sur un backend',
    difficulty: 'hard',
    tags: ['SIGKILL', 'Crash Recovery', 'Shared Memory', 'Panique'],
    front: {
      question: 'Que se passe-t-il au niveau de l\'instance entière si l\'administrateur système exécute `kill -9 <pid>` sur un processus backend client ?',
      hint: 'Postmaster détecte une sortie anormale, déconnecte tous les clients et déclenche un recovery complet.',
    },
    back: {
      answer: 'Le processus Postmaster détecte immédiatement qu\'un processus enfant est mort sans nettoyer ses verrous et structures dans la mémoire partagée.\nPar sécurité et pour prévenir toute corruption mémoire, Postmaster :\n1. Envoie un signal SIGQUIT à TOUS les autres backends (déconnexion immédiate de tous les utilisateurs).\n2. Réinitialise entièrement les segments de mémoire partagée.\n3. Lance une procédure de reprise après crash ("crash recovery") en rejouant les WAL depuis le dernier point de contrôle.',
      explanation: 'L\'utilisation de `kill -9` provoque ainsi une interruption de service globale non planifiée pour l\'ensemble du cluster.',
      ruleRef: 'EnterpriseDB Postgres Administration - Handling Unresponsive Backends',
    },
  },
  {
    id: 'fc-pgedb-dom01-009',
    cardNumber: 9,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Identification des types de processus dans pg_stat_activity',
    difficulty: 'medium',
    tags: ['backend_type', 'pg_stat_activity', 'Background Workers'],
    front: {
      question: 'Dans la colonne `backend_type` de la vue `pg_stat_activity`, quels sont les différents types de processus que l\'on peut observer ?',
      hint: 'Il n\'y a pas que les connexions clientes ; les processus d\'arrière-plan natifs y figurent également.',
    },
    back: {
      answer: 'On y trouve notamment :\n- `client backend` : Connexion applicative utilisateur.\n- `background writer` : Processus BgWriter.\n- `checkpointer` : Processus de création des checkpoints.\n- `walwriter` : Processus d\'écriture du WAL.\n- `autovacuum launcher` & `autovacuum worker` : Sous-système autovacuum.\n- `archiver` : Processus d\'archivage des segments WAL.\n- `walsender` & `walreceiver` : Processus de réplication physique en streaming.\n- `logical replication launcher` / `apply worker` : Réplication logique.',
      explanation: 'Depuis les versions récentes de PostgreSQL, tous les processus internes du moteur sont exposés dans cette même vue pour simplifier le monitoring unifié.',
      ruleRef: 'PostgreSQL 16 Cumulative Statistics - pg_stat_activity.backend_type',
    },
  },
  {
    id: 'fc-pgedb-dom01-010',
    cardNumber: 10,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Gestion des workers parallèles (Parallel Query Execution)',
    difficulty: 'hard',
    tags: ['Parallel Workers', 'max_parallel_workers', 'max_worker_processes'],
    front: {
      question: 'Quels processus sont instanciés lorsqu\'une requête analytique volumineuse déclenche une exécution parallèle sous PostgreSQL ?',
      hint: 'Le backend leader demande au background worker pool d\'allouer des "parallel worker processes".',
    },
    back: {
      answer: 'Le backend client agit comme "Leader Process". Il demande au Postmaster d\'allouer des processus "Parallel Workers" (processus enfants du type `parallel worker`).\nCes workers exécutent des portions de scan (Parallel Seq Scan) ou de jointure en parallèle, échangent des blocs de données via des files de mémoire partagée (Dynamic Shared Memory - DSM), et renvoient leurs résultats partiels au Leader qui les consolide (Gather).',
      explanation: 'Les paramètres clés gouvernant ce comportement sont `max_worker_processes`, `max_parallel_workers` (limite globale) et `max_parallel_workers_per_gather` (limite par requête, défaut 2).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 15: Parallel Query',
    },
  },

  // =========================================================================
  // SECTION 2 : ARCHITECTURE DE LA MÉMOIRE PARTAGÉE (SHARED MEMORY) (Cartes 11 à 20)
  // shared_buffers, dimensionnement, clock sweep, dirty buffers, buffer tag
  // =========================================================================
  {
    id: 'fc-pgedb-dom01-011',
    cardNumber: 11,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Rôle et architecture de shared_buffers',
    difficulty: 'easy',
    tags: ['shared_buffers', 'Shared Memory', 'Cache', '8KB Pages'],
    front: {
      question: 'Quelle est la fonction essentielle du paramètre `shared_buffers` dans l\'architecture mémoire de PostgreSQL ?',
      hint: 'C\'est le cache en mémoire vive principal partagé par tous les processus pour stocker les pages de données de 8 Ko.',
    },
    back: {
      answer: '`shared_buffers` détermine la quantité de mémoire vive allouée par PostgreSQL pour héberger son cache de pages de données partagé (Buffer Pool).\nTous les processus backends et processus d\'arrière-plan accèdent à cette zone commune pour lire ou modifier des blocs de tables et d\'index (d\'une taille unitaire de 8 Ko par défaut).',
      explanation: 'Si une page demandée est déjà dans `shared_buffers` (cache hit), la lecture est quasi instantanée en RAM sans aucun I/O disque physique.',
      ruleRef: 'EnterpriseDB Postgres Architecture - Shared Memory Structures',
    },
  },
  {
    id: 'fc-pgedb-dom01-012',
    cardNumber: 12,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Règle de dimensionnement recommandée de shared_buffers',
    difficulty: 'medium',
    tags: ['shared_buffers', 'RAM', 'Page Cache Linux', 'Recommandation'],
    front: {
      question: 'Quelle est la règle empirique standard de dimensionnement de `shared_buffers` sur un serveur dédié et pourquoi ne pas lui allouer 80% de la RAM ?',
      hint: 'Entre 25% et 40% de la mémoire totale. PostgreSQL s\'appuie massivement sur le cache de pages du noyau de l\'OS (Linux Page Cache).',
    },
    back: {
      answer: 'La recommandation standard est d\'allouer entre 25% et 40% de la RAM totale du serveur à `shared_buffers` (souvent plafonné à 16-32 Go).\nOn n\'alloue JAMAIS 80% car PostgreSQL s\'appuie sur une architecture de "double buffering" :\n1. `shared_buffers` pour le cache applicatif interne.\n2. Le cache de fichiers du système d\'exploitation (Linux OS Page Cache) qui utilise toute la RAM restante pour accélérer les écritures et lectures physiques sans double copie.',
      explanation: 'De plus, les processus clients ont besoin de RAM disponible pour `work_mem` et `maintenance_work_mem`. Un `shared_buffers` démesuré entraîne des risques de swap et dégrade les performances.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20.4: Resource Consumption - Memory',
    },
  },
  {
    id: 'fc-pgedb-dom01-013',
    cardNumber: 13,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Taille standard d\'une page (Block Size) sous PostgreSQL',
    difficulty: 'easy',
    tags: ['Page Size', 'Block Size', '8KB', 'BLCKSZ'],
    front: {
      question: 'Quelle est la taille par défaut d\'un bloc (page) de données dans `shared_buffers` et sur le système de fichiers sous PostgreSQL ?',
      hint: 'Une puissance de 2 de 8 kilo-octets.',
    },
    back: {
      answer: 'La taille standard est de 8 Ko (8192 octets).\nCette valeur est fixée lors de la compilation (`BLCKSZ = 8192`) et s\'applique à la fois aux tampons en mémoire et aux blocs stockés dans les fichiers de données sur disque.',
      explanation: 'Toute lecture ou écriture de table ou d\'index s\'effectue toujours par blocs atomiques complets de 8 Ko, même si la ligne recherchée ne mesure que 50 octets.',
      ruleRef: 'EDB Certified Associate Exam: Storage Internals and Page Layout',
    },
  },
  {
    id: 'fc-pgedb-dom01-014',
    cardNumber: 14,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Algorithme de remplacement Clock Sweep (Clock Algorithm)',
    difficulty: 'hard',
    tags: ['Clock Sweep', 'Eviction', 'LRU', 'Usage Count'],
    front: {
      question: 'Quel algorithme PostgreSQL utilise-t-il pour expulser des pages de `shared_buffers` lorsqu\'un processus a besoin d\'un tampon libre ?',
      hint: 'Ce n\'est pas un LRU pur mais une variante circulaire avec une aiguille d\'horloge et un compteur d\'usage (usage_count).',
    },
    back: {
      answer: 'PostgreSQL utilise l\'algorithme d\'aiguille d\'horloge ("Clock Sweep Algorithm").\nChaque descripteur de tampon possède un compteur `usage_count` (valeur de 0 à 5).\n1. L\'aiguille balaie les tampons circulairement.\n2. Si `usage_count > 0`, l\'aiguille décrémente le compteur de 1 et avance au tampon suivant.\n3. Si `usage_count == 0` et que le tampon n\'est pas verrouillé (ni "sale"), il est sélectionné pour être écrasé/réalloué.\n4. Lorsqu\'une page est lue par un backend, son `usage_count` est incrémenté (max 5).',
      explanation: 'Cet algorithme est extrêmement économe en CPU par rapport à une liste chaînée LRU classique qui exigerait de verrouiller la tête de liste à chaque consultation.',
      ruleRef: 'PostgreSQL 16 Source Code Documentation - src/backend/storage/buffer/freelist.c',
    },
  },
  {
    id: 'fc-pgedb-dom01-015',
    cardNumber: 15,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Notion de Dirty Buffer (Tampon modifié / sale)',
    difficulty: 'medium',
    tags: ['Dirty Buffer', 'shared_buffers', 'Écriture', 'Commit'],
    front: {
      question: 'Qu\'appelle-t-on un "Dirty Buffer" (tampon sale) dans `shared_buffers` et qui a le droit de le vider sur disque ?',
      hint: 'Un bloc modifié en mémoire qui ne correspond plus à la version présente sur le disque.',
    },
    back: {
      answer: 'Un "Dirty Buffer" est une page de 8 Ko dans `shared_buffers` qui a été modifiée par une opération DML (INSERT, UPDATE, DELETE) mais dont les modifications n\'ont pas encore été écrites sur le fichier de données physique du disque.\nLes processus habilités à l\'écrire sur disque sont :\n1. Le Checkpointer (lors des checkpoints programmés).\n2. Le Background Writer (en tâche de fond continue).\n3. Un Backend client en secours (si aucun tampon propre n\'est disponible lors d\'une lecture).',
      explanation: 'Bien que le fichier de données ne soit pas à jour, la transaction est déjà sécurisée grâce au WAL qui a été écrit sur disque lors du COMMIT.',
      ruleRef: 'EDB Associate 16: Checkpointer and Background Writer Interaction',
    },
  },
  {
    id: 'fc-pgedb-dom01-016',
    cardNumber: 16,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Structure d\'un Buffer Tag (Identification d\'une page)',
    difficulty: 'hard',
    tags: ['Buffer Tag', 'RelFileNode', 'ForkNumber', 'BlockNumber'],
    front: {
      question: 'Quels sont les trois éléments essentiels qui composent un "Buffer Tag" permettant d\'identifier de manière unique une page dans `shared_buffers` ?',
      hint: 'L\'identifiant de la table/fichier, le type de fichier (main, fsm, vm) et le numéro séquentiel du bloc.',
    },
    back: {
      answer: 'Un Buffer Tag se compose de :\n1. `rnode` (RelFileLocator) : Spécifie le tablespace OID, la base de données OID et le numéro de fichier de la relation.\n2. `forkNum` (Fork Number) : Indique le type de segment (0 = `main` pour les données, 1 = `fsm` pour la Free Space Map, 2 = `vm` pour la Visibility Map, 3 = `init`).\n3. `blockNum` (Block Number) : L\'index ordinal du bloc de 8 Ko au sein du fichier (0, 1, 2, ... N).',
      explanation: 'Une table de hachage en mémoire partagée associe chaque Buffer Tag au descripteur de slot correspondant dans `shared_buffers`.',
      ruleRef: 'PostgreSQL 16 Storage Internals - struct BufferTag in buf_internals.h',
    },
  },
  {
    id: 'fc-pgedb-dom01-017',
    cardNumber: 17,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Extension pg_buffercache pour inspecter shared_buffers',
    difficulty: 'medium',
    tags: ['pg_buffercache', 'Inspection', 'Cache Hit', 'Dirty Pages'],
    front: {
      question: 'Quelle extension officielle PostgreSQL permet d\'analyser en temps réel le contenu exact de `shared_buffers` (tables en cache, pages sales, etc.) ?',
      hint: 'Extension contrib fournissant la vue pg_buffercache.',
    },
    back: {
      answer: 'L\'extension `pg_buffercache`.\nElle installe la vue `pg_buffercache` qui expose une ligne par tampon de 8 Ko de `shared_buffers`.',
      codeSnippet: 'CREATE EXTENSION pg_buffercache;\n\nSELECT c.relname, count(*) AS buffers,\n       round(count(*) * 8.0 / 1024, 2) AS cached_mb,\n       round(100.0 * count(*) / current_setting(\'shared_buffers\')::integer, 2) AS pct_shared_buffers\nFROM pg_buffercache b\nJOIN pg_class c ON b.relfilenode = pg_relation_filenode(c.oid)\nWHERE b.reldatabase = (SELECT oid FROM pg_database WHERE datname = current_database())\nGROUP BY c.relname\nORDER BY buffers DESC LIMIT 10;',
      explanation: 'Elle permet de voir quelles tables occupent la majorité de la RAM et quelles pages sont marquées `isdirty = true`.',
      ruleRef: 'PostgreSQL 16 Contrib Modules - pg_buffercache',
    },
  },
  {
    id: 'fc-pgedb-dom01-018',
    cardNumber: 18,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Cache Hit Ratio et calcul d\'efficacité',
    difficulty: 'easy',
    tags: ['Cache Hit Ratio', 'pg_statio_user_tables', 'Performance'],
    front: {
      question: 'Comment calcule-t-on le "Buffer Cache Hit Ratio" sous PostgreSQL et quelle valeur minimale vise-t-on en production OLTP ?',
      hint: 'Rapport entre les blocs lus en mémoire (heap_blks_hit) et le total des blocs accédés (hit + read). On vise > 99%.',
    },
    back: {
      answer: 'Formule :\n`Hit Ratio = (heap_blks_hit) / (heap_blks_hit + heap_blks_read) * 100`\nEn environnement transactionnel (OLTP), on vise un ratio supérieur à 99% (au moins 95%).',
      codeSnippet: 'SELECT sum(heap_blks_hit) * 100.0 / nullif(sum(heap_blks_hit + heap_blks_read), 0) AS cache_hit_ratio\nFROM pg_statio_user_tables;',
      explanation: 'Un ratio inférieur à 90% indique que PostgreSQL effectue trop de lectures physiques sur disque (I/O goulot), suggérant soit un `shared_buffers` sous-dimensionné, soit un manque d\'index appropriés provoquant des Seq Scans intempestifs.',
      ruleRef: 'EnterpriseDB Performance Tuning - Buffer Cache Hit Ratio Monitoring',
    },
  },
  {
    id: 'fc-pgedb-dom01-019',
    cardNumber: 19,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Impact du rechargement de configuration sur shared_buffers',
    difficulty: 'medium',
    tags: ['shared_buffers', 'pg_reload_conf', 'Redémarrage', 'postmaster'],
    front: {
      question: 'Peut-on modifier la valeur de `shared_buffers` à chaud via `SELECT pg_reload_conf();` sans redémarrer le serveur ?',
      hint: 'Non, la mémoire partagée est allouée une seule fois au boot de l\'instance par le Postmaster.',
    },
    back: {
      answer: 'NON, c\'est impossible.\n`shared_buffers` est un paramètre de contexte `postmaster`. Il ne peut être modifié que dans le fichier `postgresql.conf` (ou via `ALTER SYSTEM`) et NÉCESSITE un redémarrage complet du service PostgreSQL (`pg_ctl restart`).',
      explanation: 'La vue `pg_settings` indique `context = \'postmaster\'` pour `shared_buffers`. Une tentative de rechargement à chaud générera un avertissement dans les logs et la nouvelle valeur sera ignorée jusqu\'au prochain restart.',
      examTrap: 'Ne confondez pas avec work_mem qui est modifiable dynamiquement par session (USER) sans redémarrage.',
      ruleRef: 'PostgreSQL 16 Documentation - View pg_settings and Configuration Contexts',
    },
  },
  {
    id: 'fc-pgedb-dom01-020',
    cardNumber: 20,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Verrous internes sur la mémoire partagée (LWLocks)',
    difficulty: 'hard',
    tags: ['LWLock', 'BufferMapping', 'Contention', 'Spinlocks'],
    front: {
      question: 'Quel type de verrou interne (primitif) PostgreSQL utilise-t-il pour synchroniser les accès concurrents aux tampons de `shared_buffers` ?',
      hint: 'Ce ne sont pas les verrous SQL (heavyweight locks) de niveau table/ligne, mais des LightWeight Locks (LWLocks).',
    },
    back: {
      answer: 'PostgreSQL utilise des "LightWeight Locks" (LWLocks), notamment :\n- `BufferMappingLock` : Protège la table de hachage associant les tags de pages aux index de buffers.\n- `BufferContentLock` : Protège le contenu physique de chaque page de 8 Ko (mode partagé en lecture, exclusif lors d\'une écriture/modification).\n- `BufferIO` : Synchronise les I/O disque physiques en cours sur un tampon.',
      explanation: 'Contrairement aux verrous SQL lourds stockés dans la Lock Table et gérant les deadlocks, les LWLocks sont des verrous légers et ultra-rapides gérés en mémoire partagée sans détection automatique de blocage mutuel.',
      ruleRef: 'PostgreSQL 16 Internals - Lock Manager and LightWeight Locks (LWLocks)',
    },
  },

  // =========================================================================
  // SECTION 3 : MÉMOIRE LOCALE PAR SESSION ET PAR OPÉRATION (WORK_MEM) (Cartes 21 à 30)
  // work_mem, hash_mem_multiplier, tris, hachages, déversement sur disque, OOM
  // =========================================================================
  {
    id: 'fc-pgedb-dom01-021',
    cardNumber: 21,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Définition et périmètre d\'allocation de work_mem',
    difficulty: 'easy',
    tags: ['work_mem', 'Mémoire locale', 'ORDER BY', 'Tri'],
    front: {
      question: 'Quelle est la définition exacte de `work_mem` et à quel niveau est-elle allouée : par serveur, par connexion ou par opération ?',
      hint: 'Attention au piège classique de certification : elle n\'est ni par serveur ni simplement par connexion !',
    },
    back: {
      answer: '`work_mem` spécifie la quantité de mémoire privée allouée PAR OPÉRATION élémentaire de tri (ORDER BY, DISTINCT) ou de table de hachage (Hash Join, Hash Aggregate).\nElle n\'est PAS partagée entre les processus et n\'est PAS globale à une session. Une même requête complexe peut allouer plusieurs fois la valeur de `work_mem` simultanément.',
      explanation: 'Valeur par défaut standard : 4 Mo (4MB).',
      examTrap: 'Piège d\'examen récurrent : "work_mem est la mémoire allouée par session cliente" -> FAUX ! C\'est alloué par nœud d\'exécution nécessitant un espace de travail temporaire en mémoire.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20.4: work_mem definition',
    },
  },
  {
    id: 'fc-pgedb-dom01-022',
    cardNumber: 22,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Calcul d\'exposition mémoire avec work_mem',
    difficulty: 'medium',
    tags: ['work_mem', 'OOM Killer', 'Multiplication', 'Calcul'],
    front: {
      question: 'Si une requête contient 2 tris (ORDER BY) et 2 jointures par hachage (Hash Join), et que `work_mem = 64MB`, combien de mémoire vive ce backend peut-il allouer au maximum ?',
      hint: 'Multipliez le nombre d\'opérations simultanées actives par la valeur unitaire de work_mem.',
    },
    back: {
      answer: 'Ce backend peut allouer jusqu\'à :\n`4 opérations * 64 Mo = 256 Mo` pour cette seule requête !\nSi 100 clients exécutent cette même requête en parallèle, la consommation théorique atteint `25,6 Go` de RAM.',
      explanation: 'C\'est la raison pour laquelle une augmentation imprudente de `work_mem` au niveau global dans `postgresql.conf` peut rapidement provoquer un épuisement total de la mémoire RAM du serveur.',
      ruleRef: 'EDB Associate 16: Memory Sizing and Avoiding Out-of-Memory Conditions',
    },
  },
  {
    id: 'fc-pgedb-dom01-023',
    cardNumber: 23,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Symptôme d\'un work_mem insuffisant : Spilling to Disk (External Sort)',
    difficulty: 'medium',
    tags: ['EXPLAIN ANALYZE', 'External Sort', 'Disk Spill', 'work_mem'],
    front: {
      question: 'Dans un rapport `EXPLAIN (ANALYZE, BUFFERS)`, quelle mention indique formellement que `work_mem` est insuffisant pour contenir un tri en mémoire vive ?',
      hint: 'Cherchez la ligne "Sort Method" : "quicksort" en mémoire vs "external sort/merge" sur disque.',
    },
    back: {
      answer: 'La mention :\n`Sort Method: external merge  Disk: xxxxkB` (ou `external sort`).\nElle prouve que le volume de données dépassait la capacité allouée par `work_mem`. PostgreSQL a dû déverser les données intermédiaires dans des fichiers temporaires sur disque (`pgsql_tmp`), dégradant fortement les temps d\'exécution.',
      codeSnippet: '-- Exemple dans EXPLAIN ANALYZE :\n-- Sort Method: quicksort  Memory: 2548kB   => Optimal (en RAM)\n-- Sort Method: external merge  Disk: 18450kB  => work_mem insuffisant',
      explanation: 'Augmenter `work_mem` permet de basculer en `Sort Method: quicksort` entièrement en mémoire RAM.',
      ruleRef: 'PostgreSQL 16 Documentation - Using EXPLAIN ANALYZE with Sorting Operations',
    },
  },
  {
    id: 'fc-pgedb-dom01-024',
    cardNumber: 24,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Ajustement dynamique de work_mem par session ou transaction',
    difficulty: 'easy',
    tags: ['SET work_mem', 'Session Level', 'Transaction Level', 'Bonnes pratiques'],
    front: {
      question: 'Quelle est la bonne pratique pour exécuter un script de reporting lourd sans augmenter `work_mem` pour tous les utilisateurs du serveur ?',
      hint: 'Modifier la variable localement au début de la session ou dans la transaction avec la commande SET.',
    },
    back: {
      answer: 'Utiliser la commande `SET work_mem` au niveau de la session spécifique ou `SET LOCAL work_mem` au sein d\'une transaction unique.',
      codeSnippet: 'BEGIN;\n-- Allouer temporairement 256 Mo pour cette transaction complexe uniquement\nSET LOCAL work_mem = \'256MB\';\n\nSELECT customer_id, count(*), sum(amount)\nFROM sales\nGROUP BY customer_id\nORDER BY sum(amount) DESC;\n\nCOMMIT;\n-- work_mem revient automatiquement à sa valeur par défaut après le COMMIT',
      explanation: 'Cela évite de modifier `postgresql.conf` et protège le serveur contre les connexions transactionnelles légères qui ne consommeront ainsi que la valeur de base (ex: 4 Mo).',
      ruleRef: 'PostgreSQL 16 System Administration - Configuration Parameters at Runtime',
    },
  },
  {
    id: 'fc-pgedb-dom01-025',
    cardNumber: 25,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Rôle du paramètre hash_mem_multiplier',
    difficulty: 'hard',
    tags: ['hash_mem_multiplier', 'PostgreSQL 13+', 'Hash Join', 'work_mem'],
    front: {
      question: 'Introduit dans PostgreSQL 13 et validé dans PostgreSQL 16, quel est le rôle du paramètre `hash_mem_multiplier` ?',
      hint: 'Il permet aux opérations basées sur des tables de hachage de consommer plus de mémoire que les simples tris.',
    },
    back: {
      answer: '`hash_mem_multiplier` détermine le facteur multiplicateur de `work_mem` alloué spécifiquement aux opérations de hachage (Hash Join, Hash Aggregate, Hash set operations).\nMémoire allouée = `work_mem * hash_mem_multiplier`.\nLa valeur par défaut est `1.0` (ou `2.0` selon certaines configurations).',
      explanation: 'Les opérations de hachage souffrent beaucoup plus du déversement sur disque que les tris (algorithme d\'external hash partitioning complexe). Ce paramètre leur accorde un budget supérieur sans pénaliser les tris.',
      ruleRef: 'PostgreSQL 16 Documentation - Resource Consumption: hash_mem_multiplier',
    },
  },
  {
    id: 'fc-pgedb-dom01-026',
    cardNumber: 26,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Surveillance des fichiers temporaires créés sur disque',
    difficulty: 'medium',
    tags: ['log_temp_files', 'Fichiers temporaires', 'Spill', 'I/O'],
    front: {
      question: 'Quel paramètre de configuration permet d\'enregistrer dans les logs PostgreSQL la création de tout fichier temporaire sur disque dépassant une certaine taille ?',
      hint: 'log_temp_files = 0 enregistre tous les fichiers temporaires dès leur suppression.',
    },
    back: {
      answer: 'Le paramètre `log_temp_files`.\n- Régler `log_temp_files = 0` logue TOUS les fichiers temporaires créés (tri, hachage, curseurs holdables) lors de leur suppression.\n- Régler `log_temp_files = \'10MB\'` ne logue que les fichiers temporaires dont la taille atteint ou dépasse 10 Mo.',
      explanation: 'C\'est l\'outil de détection numéro 1 pour repérer les requêtes SQL nécessitant une augmentation ponctuelle de `work_mem` ou une réécriture.',
      ruleRef: 'PostgreSQL 16 Documentation - Error Reporting and Logging: What to Log',
    },
  },
  {
    id: 'fc-pgedb-dom01-027',
    cardNumber: 27,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Risque de l\'Out Of Memory (OOM) Killer sous Linux',
    difficulty: 'hard',
    tags: ['OOM Killer', 'Linux', 'overcommit_memory', 'Crash'],
    front: {
      question: 'Pourquoi le mécanisme "OOM Killer" de Linux est-il particulièrement dangereux pour une instance PostgreSQL et comment le configurer ?',
      hint: 'Linux suralloue la mémoire par défaut ; si la RAM sature, l\'OS tue le processus le plus consommateur, souvent le Postmaster.',
    },
    back: {
      answer: 'Par défaut, Linux pratique l\'optimisme mémoire (overcommit). Quand la mémoire physique et le swap sont saturés, le noyau Linux invoque l\'OOM Killer qui choisit un processus et le termine brutalement avec un `SIGKILL`.\nSi le Postmaster ou un de ses backends est tué de la sorte, toute l\'instance crash immédiatement.\nPrévention recommandées :\n1. Régler dans `/etc/sysctl.conf` : `vm.overcommit_memory = 2` et `vm.overcommit_ratio = 80` (interdit la surallocation).\n2. Configurer `oom_score_adj = -1000` pour le processus Postmaster afin que l\'OS ne le cible jamais en priorité.',
      explanation: 'C\'est un prérequis d\'administration système critique pour les environnements de production PostgreSQL d\'entreprise (EDB).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.4.3: Managing Kernel Resources - Linux Memory Overcommit',
    },
  },
  {
    id: 'fc-pgedb-dom01-028',
    cardNumber: 28,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Opérations concernées par work_mem vs non concernées',
    difficulty: 'medium',
    tags: ['work_mem', 'SELECT', 'DISTINCT', 'Opérations'],
    front: {
      question: 'Parmi les opérations suivantes, lesquelles utilisent `work_mem` : ORDER BY, CREATE INDEX, Hash Join, VACUUM, DISTINCT ?',
      hint: 'Les opérations DDL et de maintenance n\'utilisent PAS work_mem.',
    },
    back: {
      answer: '- Utilisent `work_mem` : `ORDER BY`, `Hash Join`, `DISTINCT` (et `Bitmap Index Scan`, `Merge Join`).\n- N\'utilisent PAS `work_mem` : `CREATE INDEX` et `VACUUM` (ils utilisent `maintenance_work_mem` et `autovacuum_work_mem`).',
      explanation: '`work_mem` est réservée exclusivement aux opérations de requêtage DML/SELECT au sein du plan d\'exécution.',
      examTrap: 'Ne répondez jamais que CREATE INDEX utilise work_mem. La création et reconstruction d\'index s\'appuie sur maintenance_work_mem.',
      ruleRef: 'EDB Certified Associate Exam Topic: Memory Allocation by Statement Type',
    },
  },
  {
    id: 'fc-pgedb-dom01-029',
    cardNumber: 29,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Impact de work_mem sur Bitmap Heap Scan',
    difficulty: 'medium',
    tags: ['Bitmap Heap Scan', 'Bitmap Index Scan', 'Lossy Bitmap', 'work_mem'],
    front: {
      question: 'Que se passe-t-il lors d\'un `Bitmap Index Scan` si le volume de TID (identifiants de lignes) dépasse l\'espace disponible dans `work_mem` ?',
      hint: 'Le bitmap devient "lossy" (avec perte) et PostgreSQL doit re-vérifier chaque ligne de la page.',
    },
    back: {
      answer: 'Le bitmap de pointeurs de lignes en mémoire devient "lossy" (avec perte).\nAu lieu d\'enregistrer les pointeurs précis des lignes individuelles (exact bitmap), PostgreSQL ne mémorise plus que l\'adresse de la page entière de 8 Ko.\nConséquence : Lors de l\'étape suivante `Bitmap Heap Scan`, PostgreSQL doit lire tous les tuples de la page et ré-évaluer le prédicat (`Recheck Cond`), ce qui augmente l\'utilisation CPU.',
      explanation: 'Dans EXPLAIN ANALYZE, on observe alors `Rows Removed by Index Recheck` et un passage en `lossy pages`. Augmenter légèrement `work_mem` rétablit un bitmap exact.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 14: Populating a Database and Index Scans',
    },
  },
  {
    id: 'fc-pgedb-dom01-030',
    cardNumber: 30,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Affichage de la valeur courante de work_mem',
    difficulty: 'easy',
    tags: ['SHOW', 'current_setting', 'psql', 'work_mem'],
    front: {
      question: 'Quelles commandes SQL ou métacommandes psql permettent de connaître la valeur courante de `work_mem` pour la session active ?',
      hint: 'SHOW work_mem ou la fonction current_setting().',
    },
    back: {
      answer: '1. Commande SQL standard : `SHOW work_mem;`\n2. Fonction système : `SELECT current_setting(\'work_mem\');`\n3. Consultation du catalogue : `SELECT setting, unit FROM pg_settings WHERE name = \'work_mem\';`',
      explanation: 'La valeur affichée est celle qui s\'applique à la session en cours, prenant en compte d\'éventuels `SET work_mem` locaux.',
      ruleRef: 'PostgreSQL 16 Documentation - System Administration Functions',
    },
  },

  // =========================================================================
  // SECTION 4 : MÉMOIRE DE MAINTENANCE ET TAMPONS TEMPORAIRES (Cartes 31 à 40)
  // maintenance_work_mem, autovacuum_work_mem, temp_buffers, VACUUM, CREATE INDEX
  // =========================================================================
  {
    id: 'fc-pgedb-dom01-031',
    cardNumber: 31,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Rôle de maintenance_work_mem',
    difficulty: 'easy',
    tags: ['maintenance_work_mem', 'VACUUM', 'CREATE INDEX', 'ALTER TABLE'],
    front: {
      question: 'Quelles opérations majeures de base de données tirent parti du paramètre `maintenance_work_mem` ?',
      hint: 'Opérations d\'administration : création d\'index, vacuum manuel, ajout de clés étrangères, etc.',
    },
    back: {
      answer: '`maintenance_work_mem` alloue la mémoire de travail pour les opérations de maintenance lourdes :\n1. `CREATE INDEX` et `REINDEX` (tri initial des clés d\'index B-Tree).\n2. `VACUUM` (stockage temporaire de la liste des Dead Tuples collectés).\n3. `ALTER TABLE ADD FOREIGN KEY` (validation des contraintes d\'intégrité référentielle).\n4. `CLUSTER` (réorganisation physique d\'une table selon un index).',
      explanation: 'Puisque ces opérations sont rares et généralement exécutées par un seul administrateur à la fois, `maintenance_work_mem` peut être dimensionné beaucoup plus généreusement que `work_mem` (ex: 1 à 4 Go).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20.4: maintenance_work_mem',
    },
  },
  {
    id: 'fc-pgedb-dom01-032',
    cardNumber: 32,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Différence entre maintenance_work_mem et autovacuum_work_mem',
    difficulty: 'medium',
    tags: ['autovacuum_work_mem', 'maintenance_work_mem', 'Worker count'],
    front: {
      question: 'Quelle est la relation entre `maintenance_work_mem` et `autovacuum_work_mem` ?',
      hint: 'Si autovacuum_work_mem vaut -1, il hérite de la valeur de maintenance_work_mem.',
    },
    back: {
      answer: '- Si `autovacuum_work_mem = -1` (valeur par défaut), chaque worker d\'autovacuum utilise la valeur définie par `maintenance_work_mem`.\n- Si `autovacuum_work_mem` est explicitement configuré (ex: `256MB`), les workers d\'autovacuum utilisent ce montant dédié, laissant `maintenance_work_mem` disponible pour les DDL manuels administrateur.',
      explanation: 'Attention au dimensionnement : si `maintenance_work_mem = 2GB` et `autovacuum_max_workers = 5`, jusqu\'à 5 x 2 Go = 10 Go de RAM pourraient être alloués si `autovacuum_work_mem` reste à `-1`. Spécifier une valeur dédiée protège contre ce risque.',
      examTrap: 'Ne laissez pas autovacuum_work_mem à -1 si vous augmentez considérablement maintenance_work_mem pour un REINDEX ponctuel.',
      ruleRef: 'EnterpriseDB Postgres Tuning - Autovacuum Memory Configuration',
    },
  },
  {
    id: 'fc-pgedb-dom01-033',
    cardNumber: 33,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Fonctionnement de VACUUM avec la mémoire de maintenance',
    difficulty: 'hard',
    tags: ['VACUUM', 'Dead Tuples', 'maintenance_work_mem', 'TID array'],
    front: {
      question: 'Comment le processus `VACUUM` utilise-t-il la mémoire allouée par `maintenance_work_mem` lors du nettoyage d\'une table volumineuse ?',
      hint: 'Il stocke une liste de pointeurs de lignes mortes (TIDs de 6 octets) jusqu\'à saturation de la mémoire avant de scanner les index.',
    },
    back: {
      answer: 'Lors de la phase 1 (scan de la table), `VACUUM` charge dans `maintenance_work_mem` les identifiants d\'éléments morts (TIDs - ItemPointers de 6 octets).\n- Si la mémoire allouée suffit pour tous les dead tuples, il parcourt tous les index en un seul passage (phase 2) pour retirer les pointeurs correspondants.\n- Si `maintenance_work_mem` sature (environ 178 millions de TIDs par Go), VACUUM doit interrompre le scan de table, nettoyer les index, vider sa mémoire, puis reprendre le scan de table (multiples passes d\'index très coûteuses en I/O).',
      explanation: 'Allouer au moins 1 Go à `autovacuum_work_mem` sur les tables massives évite les passes d\'index multiples répétées.',
      ruleRef: 'PostgreSQL 16 Internals - Vacuum Processing and TID Storage',
    },
  },
  {
    id: 'fc-pgedb-dom01-034',
    cardNumber: 34,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Rôle de temp_buffers pour les tables temporaires',
    difficulty: 'medium',
    tags: ['temp_buffers', 'Tables temporaires', 'Session', 'CREATE TEMP TABLE'],
    front: {
      question: 'Quelle est la fonction du paramètre `temp_buffers` et en quoi diffère-t-il de `shared_buffers` ?',
      hint: 'Il sert exclusivement aux tables temporaires créées au sein de la session et n\'est pas partagé entre les utilisateurs.',
    },
    back: {
      answer: '`temp_buffers` définit le cache mémoire privé utilisé par chaque session pour stocker les pages de ses tables temporaires locales (`CREATE TEMP TABLE`).\nDifférences clés avec `shared_buffers` :\n1. Local à la session : aucun autre backend ne peut y accéder.\n2. Aucun verrouillage concurrent (pas de LWLocks nécessaires).\n3. Aucune écriture de WAL générée pour les tables temporaires (sauf pour les métadonnées de catalogue), ce qui rend les accès extrêmement véloces.',
      explanation: 'Valeur par défaut : 8 Mo (8MB). Il peut être ajusté par session via `SET temp_buffers` avant le premier accès à une table temporaire.',
      examTrap: 'Une fois qu\'une table temporaire a été accédée dans la session, tenter d\'exécuter SET temp_buffers provoquera une erreur.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20.4: temp_buffers',
    },
  },
  {
    id: 'fc-pgedb-dom01-035',
    cardNumber: 35,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Plafond maximum effectif de maintenance_work_mem pour VACUUM',
    difficulty: 'hard',
    tags: ['VACUUM', '1GB limit', 'Historique', 'PostgreSQL 16'],
    front: {
      question: 'Historiquement, quelle était la limite mémoire maximale qu\'un seul worker VACUUM pouvait utiliser dans `maintenance_work_mem`, et comment cela a-t-il évolué ?',
      hint: 'La structure interne était historiquement plafonnée à 1 Go (1GB) pour le tableau de TIDs.',
    },
    back: {
      answer: 'Jusqu\'à PostgreSQL 16, le stockage interne des TIDs dans VACUUM utilisait un tableau plat de structures `ItemPointerData`, dont la taille maximale était limitée par l\'allocateur à 1 Go (1024 Mo).\nAllouer plus de 1 Go à `autovacuum_work_mem` ne bénéficiait donc pas au VACUUM classique (bien que bénéfique à `CREATE INDEX`).\nPostgreSQL 17 a levé cette limite en introduisant les Radix Trees pour les TIDs.',
      explanation: 'Pour l\'examen EDB Associate 16, retenez qu\'allouer 1 Go à autovacuum_work_mem est le plafond optimal recommandé pour le nettoyage des tables volumineuses.',
      ruleRef: 'PostgreSQL 16 Release Notes and Vacuum Implementation Limits',
    },
  },
  {
    id: 'fc-pgedb-dom01-036',
    cardNumber: 36,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Accélération de CREATE INDEX avec max_parallel_maintenance_workers',
    difficulty: 'medium',
    tags: ['CREATE INDEX', 'Parallélisme', 'max_parallel_maintenance_workers'],
    front: {
      question: 'Quel paramètre contrôle le nombre de processus workers pouvant assister un `CREATE INDEX` en parallèle ?',
      hint: 'max_parallel_maintenance_workers (défaut 2).',
    },
    back: {
      answer: 'Le paramètre `max_parallel_maintenance_workers` (valeur par défaut = 2).\nIl autorise le processus de création d\'index B-Tree à spawner des workers auxiliaires pour scanner la table et trier les clés en parallèle.',
      explanation: 'Chaque worker parallèle reçoit une fraction de la mémoire définie par `maintenance_work_mem`. Cette fonctionnalité accélère drastiquement la création d\'index sur les tables de plusieurs millions de lignes.',
      ruleRef: 'PostgreSQL 16 Documentation - Parallel Index Builds',
    },
  },
  {
    id: 'fc-pgedb-dom01-037',
    cardNumber: 37,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Localisation des fichiers temporaires (pgsql_tmp)',
    difficulty: 'medium',
    tags: ['pgsql_tmp', 'Tablespace', 'Disque', 'Fichiers temporaires'],
    front: {
      question: 'Où sont écrits physiquement les fichiers temporaires de tri/hachage lorsque `work_mem` est dépassé, et peut-on changer leur emplacement ?',
      hint: 'Dans un sous-répertoire pgsql_tmp à l\'intérieur de $PGDATA/base/ ou via le paramètre temp_tablespaces.',
    },
    back: {
      answer: 'Par défaut, ils sont créés dans `$PGDATA/base/pgsql_tmp/` ou dans le répertoire du tablespace de la base de données active.\nOn peut changer leur emplacement en utilisant le paramètre `temp_tablespaces` :\n`ALTER SYSTEM SET temp_tablespaces = \'fast_nvme_tablespace\';`',
      explanation: 'Placer les fichiers temporaires sur un disque SSD NVMe distinct et très rapide évite que des gros tris saturés ne ralentissent les I/O du tablespace principal où résident les tables.',
      ruleRef: 'PostgreSQL 16 Administration - Managing Temporary Files with temp_tablespaces',
    },
  },
  {
    id: 'fc-pgedb-dom01-038',
    cardNumber: 38,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Limitation de l\'espace disque temporaire avec temp_file_limit',
    difficulty: 'medium',
    tags: ['temp_file_limit', 'Protection disque', 'Espace disque', 'Crash'],
    front: {
      question: 'Quel paramètre permet d\'empêcher une requête malveillante ou un tri incontrôlé de saturer l\'intégralité du disque dur avec des fichiers temporaires ?',
      hint: 'temp_file_limit (exprimé en Ko).',
    },
    back: {
      answer: 'Le paramètre `temp_file_limit`.\nIl définit la taille maximale cumulée de fichiers temporaires qu\'un processus backend est autorisé à créer (en Ko).\nSi une session dépasse cette limite, sa requête est immédiatement interrompue avec l\'erreur `ERROR: temporary file size exceeds temp_file_limit`.',
      explanation: 'La valeur `-1` (défaut) désactive la limite. Sur les serveurs partagés, il est fortement recommandé de fixer une limite (ex: `20GB`).',
      ruleRef: 'PostgreSQL 16 Documentation - Resource Consumption: Disk Resource Limits',
    },
  },
  {
    id: 'fc-pgedb-dom01-039',
    cardNumber: 39,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Cycles de vie et nettoyage des fichiers temporaires',
    difficulty: 'easy',
    tags: ['pgsql_tmp', 'Nettoyage', 'Déconnexion', 'Crash'],
    front: {
      question: 'Quand les fichiers temporaires créés dans `pgsql_tmp` sont-ils supprimés ?',
      hint: 'Dès la fin de la requête ou de la transaction, ou au démarrage du serveur en cas de crash brutal.',
    },
    back: {
      answer: '1. En fonctionnement normal : dès que le nœud d\'exécution du tri/hachage se termine ou à la clôture de la transaction/session.\n2. En cas de crash brutal de l\'instance (`SIGKILL` ou coupure de courant) : le processus Postmaster parcourt et supprime automatiquement tous les fichiers orphelins dans les dossiers `pgsql_tmp` lors de sa séquence d\'initialisation.',
      explanation: 'Il n\'est donc jamais nécessaire de créer un script cron externe pour purger `pgsql_tmp`.',
      ruleRef: 'EDB Associate 16: Temporary File Management and Crash Recovery',
    },
  },
  {
    id: 'fc-pgedb-dom01-040',
    cardNumber: 40,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Synthèse des 4 paramètres mémoire essentiels de PostgreSQL',
    difficulty: 'easy',
    tags: ['Synthèse', 'shared_buffers', 'work_mem', 'maintenance_work_mem', 'temp_buffers'],
    front: {
      question: 'Résumez en une phrase le rôle distinct des 4 structures mémoire de base : `shared_buffers`, `work_mem`, `maintenance_work_mem`, et `temp_buffers`.',
      hint: 'Cache partagé global, mémoire locale de tri/requête, mémoire locale DDL/VACUUM, et cache local des tables temporaires.',
    },
    back: {
      answer: '1. `shared_buffers` : Cache global partagé en RAM pour les pages de données de 8 Ko de toutes les tables et index.\n2. `work_mem` : Mémoire locale privée allouée par opération de tri ou de hachage lors de l\'exécution d\'un SELECT/DML.\n3. `maintenance_work_mem` : Mémoire locale allouée pour les tâches d\'administration lourdes (VACUUM, CREATE INDEX, ALTER TABLE).\n4. `temp_buffers` : Cache mémoire local privé dédié aux blocs des tables temporaires de la session.',
      explanation: 'Comprendre ces 4 distinctions est le socle absolu pour réussir le Domaine 1 de la certification EDB Associate 16.',
      ruleRef: 'EDB Certified Associate Exam Study Guide - Domain 1 Architecture Checklist',
    },
  },

  // =========================================================================
  // SECTION 5 : LE JOURNAL DE TRANSACTIONS (WAL) & WAL BUFFERS (Cartes 41 à 50)
  // WAL, wal_buffers, LSN, fsync, synchronous_commit, Crash Recovery, ACID
  // =========================================================================
  {
    id: 'fc-pgedb-dom01-041',
    cardNumber: 41,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Principe du Write-Ahead Logging (WAL)',
    difficulty: 'easy',
    tags: ['WAL', 'Write-Ahead Logging', 'ACID', 'Durabilité'],
    front: {
      question: 'Quel est le principe fondamental du "Write-Ahead Logging" (WAL) et quelle lettre du standard ACID garantit-il en premier lieu ?',
      hint: 'Les changements doivent être écrits et synchronisés dans le journal séquentiel AVANT que les pages de données ne soient écrites sur disque.',
    },
    back: {
      answer: 'Le principe fondamental du WAL : "Toute modification d\'une page de données doit être enregistrée de façon durable dans un journal séquentiel avant que la page modifiée (dirty buffer) ne soit autorisée à être écrite sur le fichier de données".\nIl garantit en premier lieu la **Durabilité (D)** (ainsi que l\'Atomicité) du modèle ACID.',
      explanation: 'Écrire de façon séquentielle dans le WAL est infiniment plus rapide que des écritures aléatoires sur les fichiers de tables éparpillés sur le disque. Si le serveur crashe, le rejeu du WAL reconstruit l\'état exact de la base.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 30: Reliability and the Write-Ahead Log',
    },
  },
  {
    id: 'fc-pgedb-dom01-042',
    cardNumber: 42,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Rôle et dimensionnement de wal_buffers',
    difficulty: 'medium',
    tags: ['wal_buffers', 'Shared Memory', '16MB', 'Auto-tuning'],
    front: {
      question: 'À quoi sert le paramètre `wal_buffers` et quelle est sa règle de configuration automatique par défaut ?',
      hint: 'Tampon en mémoire partagée hébergeant les enregistrements WAL avant écriture sur disque. Par défaut, réglé sur -1.',
    },
    back: {
      answer: '`wal_buffers` spécifie la quantité de mémoire partagée utilisée pour mettre en tampon les enregistrements WAL avant qu\'ils ne soient écrits sur les fichiers disque dans `pg_wal`.\n- Si `wal_buffers = -1` (défaut), PostgreSQL calcule automatiquement sa taille comme environ 1/32 de `shared_buffers` (avec un minimum de 64 Ko et un plafond standard de 16 Mo).\n- Pour les charges d\'écritures intensives (bulk loads), on le fixe manuellement à 16 Mo ou 64 Mo.',
      explanation: 'Un segment WAL complet mesurant 16 Mo par défaut, 16 Mo de `wal_buffers` permet de temporiser un segment entier en mémoire.',
      ruleRef: 'PostgreSQL 16 Documentation - Write Ahead Log: wal_buffers',
    },
  },
  {
    id: 'fc-pgedb-dom01-043',
    cardNumber: 43,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Log Sequence Number (LSN)',
    difficulty: 'medium',
    tags: ['LSN', 'pg_lsn', '64-bit', 'XLOG'],
    front: {
      question: 'Qu\'est-ce qu\'un LSN (Log Sequence Number) sous PostgreSQL et quel est son format ?',
      hint: 'Un entier 64 bits affiché en hexadécimal séparé par un slash (ex: 0/16B3748), représentant une position absolue dans le flux WAL.',
    },
    back: {
      answer: 'Un LSN (Log Sequence Number) est un entier 64 bits unique représentant la position byte par byte dans le flux continu des journaux de transactions (WAL).\nFormat d\'affichage : Deux nombres hexadécimaux séparés par une barre oblique (ex: `1/A24F80`).\n- La première partie indique le numéro logique de fichier d\'historique.\n- La seconde partie indique l\'offset précis en octets au sein de ce fichier.',
      explanation: 'Chaque page de données dans PostgreSQL stocke dans son en-tête le LSN de la dernière modification qu\'elle a reçue (`pd_lsn`). Cela permet à PostgreSQL de savoir si un bloc a déjà appliqué une modification lors du recovery.',
      ruleRef: 'PostgreSQL 16 Data Types - The pg_lsn Type',
    },
  },
  {
    id: 'fc-pgedb-dom01-044',
    cardNumber: 44,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Comparaison de LSN avec pg_current_wal_lsn()',
    difficulty: 'medium',
    tags: ['pg_current_wal_lsn', 'pg_wal_lsn_diff', 'psql'],
    front: {
      question: 'Comment calculer la quantité exacte d\'octets de WAL générés entre deux instants à l\'aide des fonctions PostgreSQL ?',
      hint: 'Utiliser pg_current_wal_lsn() et pg_wal_lsn_diff().',
    },
    back: {
      answer: 'On capture le LSN initial avec `pg_current_wal_lsn()`, puis après l\'opération, on calcule la différence numérique avec `pg_wal_lsn_diff()` :',
      codeSnippet: 'SELECT pg_wal_lsn_diff(\n  pg_current_wal_lsn(),\n  \'0/16B3000\'::pg_lsn\n) AS wal_bytes_generated;',
      explanation: 'La fonction `pg_wal_lsn_diff` soustrait deux valeurs de type `pg_lsn` et renvoie directement le résultat en nombre d\'octets (numeric).',
      ruleRef: 'PostgreSQL 16 Administration Functions - WAL Control Functions',
    },
  },
  {
    id: 'fc-pgedb-dom01-045',
    cardNumber: 45,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Paramètre fsync et intégrité des données',
    difficulty: 'easy',
    tags: ['fsync', 'postgresql.conf', 'Crash', 'Danger'],
    front: {
      question: 'Que fait le paramètre `fsync = on` et quelles sont les conséquences catastrophiques de le désactiver (`fsync = off`) en production ?',
      hint: 'Il force le système d\'exploitation à vider physiquement les écritures sur le support de stockage matériel.',
    },
    back: {
      answer: '- Avec `fsync = on` (défaut obligatoire) : PostgreSQL force le système d\'exploitation à synchroniser physiquement chaque écriture WAL sur le support disque via l\'appel système `fsync()` avant de confirmer le succès d\'un COMMIT au client.\n- Avec `fsync = off` : Le serveur écrit dans le cache disque sans forcer l\'écriture physique. Les performances grimpent spectaculairement, mais en cas de coupure de courant ou de kernel panic, la base de données subit une **corruption irréversible** et le redémarrage crash recovery échouera.',
      explanation: 'Règle d\'or EDB : `fsync = off` n\'est toléré QUE lors d\'un chargement initial massif d\'un cluster de test qu\'on peut entièrement jeter en cas d\'échec.',
      ruleRef: 'PostgreSQL 16 Documentation - Write Ahead Log: fsync parameter',
    },
  },
  {
    id: 'fc-pgedb-dom01-046',
    cardNumber: 46,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Le paramètre synchronous_commit',
    difficulty: 'medium',
    tags: ['synchronous_commit', 'Asynchronous Commit', 'Performance', 'RPO'],
    front: {
      question: 'Quelles sont les valeurs possibles de `synchronous_commit` et que signifie `synchronous_commit = off` ?',
      hint: 'Mode commit asynchrone : la transaction valide immédiatement sans attendre l\'écriture physique du WAL sur disque.',
    },
    back: {
      answer: 'Valeurs possibles : `on` (défaut), `off`, `local`, `remote_write`, `remote_apply`.\n- `synchronous_commit = off` (Commit Asynchrone) : Le client reçoit la confirmation de son COMMIT immédiatement après l\'écriture dans `wal_buffers`, sans attendre que le WAL soit physiquement flushé sur disque via `fsync`.\n- Tolérance : En cas de coupure matérielle, quelques transactions récentes (jusqu\'à 3x `wal_writer_delay`, soit ~60 ms) peuvent être perdues, MAIS **la base reste parfaitement cohérente** (aucune corruption).',
      explanation: 'C\'est une excellente optimisation pour les applications de télémétrie ou de logging où un débit très élevé l\'emporte sur une perte minime de quelques millisecondes de données.',
      examTrap: 'synchronous_commit = off ne corrompt PAS la base (contrairement à fsync = off). Il sacrifie uniquement un petit delta de RPO.',
      ruleRef: 'EDB Certified Associate: Asynchronous Commit vs fsync',
    },
  },
  {
    id: 'fc-pgedb-dom01-047',
    cardNumber: 47,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Nommage et structure des fichiers de segments WAL dans pg_wal',
    difficulty: 'hard',
    tags: ['pg_wal', 'Segment', '16MB', 'Nommage hexadécimal'],
    front: {
      question: 'Quelle est la taille par défaut d\'un fichier de segment WAL sous PostgreSQL et comment est construit son nom de 24 caractères hexadécimaux ?',
      codeSnippet: '$ ls $PGDATA/pg_wal\n000000010000000000000001  000000010000000000000002  archive_status',
      hint: 'Taille standard 16 Mo. Le nom contient : Timeline (8 car.), Log logical number (8 car.) et Segment number (8 car.).',
    },
    back: {
      answer: 'Taille par défaut : 16 Mo (16MB).\nLe nom d\'un fichier WAL comprend exactement 24 caractères hexadécimaux découpés en 3 blocs de 8 chiffres :\n1. Caractères 1 à 8 : **Timeline ID** (ex: `00000001` pour la timeline de production initiale).\n2. Caractères 9 à 16 : **Numéro logique de fichier WAL** (Log Id).\n3. Caractères 17 à 24 : **Numéro de segment** séquentiel (ex: `00000001`, `00000002`... jusqu\'à `000000FF`).',
      explanation: 'Depuis PostgreSQL 11, la taille des segments peut être modifiée lors de l\'`initdb` avec `--wal-segsize` (ex: 64 Mo).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 30.5: WAL Internals',
    },
  },
  {
    id: 'fc-pgedb-dom01-048',
    cardNumber: 48,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Mécanisme du Full Page Writes (FPW)',
    difficulty: 'hard',
    tags: ['full_page_writes', 'Torn Pages', 'Checkpoint', 'Crash'],
    front: {
      question: 'Pourquoi le paramètre `full_page_writes = on` est-il indispensable pour se prémunir contre les "Torn Pages" (pages déchirées) ?',
      hint: 'En cas de crash de l\'OS pendant l\'écriture d\'une page de 8 Ko (écrite par blocs OS de 4 Ko), la première modification après checkpoint écrit l\'intégralité de la page dans le WAL.',
    },
    back: {
      answer: 'Les blocs PostgreSQL mesurent 8 Ko alors que les écritures disque au niveau système de fichiers sont souvent de 4 Ko ou 512 octets.\nSi une coupure de courant survient au milieu de l\'écriture d\'un bloc de 8 Ko, le bloc est laissé dans un état bâtard ("torn page" ou demi-page corrompue).\nAvec `full_page_writes = on` :\nLors du premier changement sur une page survenant après un checkpoint, PostgreSQL enregistre l\'IMAGE COMPLÈTE de la page de 8 Ko dans le WAL. Lors du crash recovery, il remplace la page déchirée par l\'image saine du WAL.',
      explanation: 'Désactiver `full_page_writes` réduit le volume de WAL mais rend la récupération après crash matériel totalement incertaine.',
      ruleRef: 'PostgreSQL 16 Reliability - full_page_writes and Torn Page Protection',
    },
  },
  {
    id: 'fc-pgedb-dom01-049',
    cardNumber: 49,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Outil de débogage et lecture des WAL : pg_waldump',
    difficulty: 'medium',
    tags: ['pg_waldump', 'Debug', 'Inspection WAL', 'CLI'],
    front: {
      question: 'Quel utilitaire en ligne de commande fourni avec PostgreSQL permet de décoder et lire le contenu binaire d\'un segment de WAL ?',
      hint: 'pg_waldump (anciennement pg_xlogdump).',
    },
    back: {
      answer: 'L\'utilitaire `pg_waldump`.\nIl lit les fichiers de segments WAL bruts et affiche les enregistrements en texte clair (horodatage, type d\'opération Heap/Btree, transaction ID, LSN, longueur).',
      codeSnippet: '$ pg_waldump -p $PGDATA/pg_wal 000000010000000000000001\n-- Affiche les enregistrements : rmgr: Heap, len: 74, xid: 842, type: INSERT ...',
      explanation: 'Très utile pour analyser la cause d\'un pic soudain de génération de WAL ou pour auditer les transactions juste avant un incident.',
      ruleRef: 'PostgreSQL 16 Client Applications - pg_waldump',
    },
  },
  {
    id: 'fc-pgedb-dom01-050',
    cardNumber: 50,
    domainId: 'pg-edb-dom-01',
    domainCode: 'DOM-01',
    domainTitle: 'Architecture interne du serveur & Processus d\'arrière-plan',
    subtopic: 'Forcer la bascule d\'un segment WAL avec pg_switch_wal()',
    difficulty: 'easy',
    tags: ['pg_switch_wal', 'Archivage', 'WAL segment', 'psql'],
    front: {
      question: 'Quelle fonction SQL permet de fermer immédiatement le segment WAL en cours et d\'en ouvrir un nouveau, facilitant ainsi son archivage ?',
      hint: 'pg_switch_wal() (qui remplace l\'ancienne fonction pg_switch_xlog()).',
    },
    back: {
      answer: 'La fonction `SELECT pg_switch_wal();`.\nElle force PostgreSQL à clore le fichier WAL actuel (même s\'il n\'a pas atteint les 16 Mo), à le marquer prêt pour l\'archivage (fichier `.ready`), et à basculer les écritures suivantes sur le segment numéro N+1.',
      explanation: 'Nécessite les privilèges superuser ou le rôle prédéfini `pg_checkpoint` / `pg_write_server_files`.',
      ruleRef: 'PostgreSQL 16 Administration Functions - WAL Control Functions: pg_switch_wal',
    },
  },
];
