import { FlashcardItem } from '../types';

export const postgresEdbDom05Part2Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 6 : WRITE-AHEAD LOGGING (WAL) & ARCHIVAGE CONTINU (Cartes 51 à 60)
  // wal_level, segments 16Mo, archive_mode, archive_command, archive_library
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-051',
    cardNumber: 51,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Rôle fondamental du Write-Ahead Logging (WAL)',
    difficulty: 'easy',
    tags: ['WAL', 'Architecture', 'ACID', 'Durability', 'Crash Recovery'],
    front: {
      question: 'Quel est le rôle fondamental du mécanisme Write-Ahead Logging (WAL) dans PostgreSQL et quel principe ACID garantit-il ?',
      hint: 'Écrire les intentions dans un journal séquentiel avant d\'écrire les pages de données.',
    },
    back: {
      answer: 'Le principe du **Write-Ahead Logging (WAL)** stipule qu\'**aucune modification de bloc de données ne peut être écrite sur le disque dans les fichiers de table tant que l\'enregistrement WAL correspondant décrivant cette modification n\'a pas été persisté sur disque (fsync)**.\n\nIl garantit le principe de **Durabilité (le \'D\' d\'ACID)** et l\'Atomicité en permettant de rejouer les modifications committées en cas de crash intempestif (*Redo Log*).',
      explanation: 'Écrire séquentiellement dans les journaux WAL est infiniment plus rapide que d\'écrire de manière aléatoire dans des dizaines de blocs de données dispersés.',
      examTrap: 'Les WAL ne servent pas seulement au crash-recovery local : ils constituent la matière première indispensable pour l\'archivage continu, la réplication en streaming et la restauration PITR.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 30: Reliability and the Write-Ahead Log',
    },
  },
  {
    id: 'fc-pgedb-dom05-052',
    cardNumber: 52,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Niveaux de détail des WAL : paramètre `wal_level`',
    difficulty: 'medium',
    tags: ['wal_level', 'Configuration', 'Replication', 'PITR'],
    front: {
      question: 'Quelles sont les 3 valeurs possibles pour le paramètre `wal_level` et quel niveau est exigé au minimum pour la réplication physique et l\'archivage continu ?',
      codeSnippet: '# postgresql.conf\nwal_level = replica',
      hint: 'minimal, replica, logical.',
    },
    back: {
      answer: 'Les 3 valeurs de `wal_level` sont :\n\n1. **`minimal`** : Ne consigne que le strict nécessaire pour récupérer d\'un crash local (désactive le logging de certaines opérations de masse comme `CREATE TABLE AS` ou `COPY`). Ni archivage ni réplication possibles.\n2. **`replica`** (Défaut) : Écrit suffisamment d\'informations pour supporter **l\'archivage continu des WAL, la restauration PITR et la réplication physique en streaming** (y compris avec standby en lecture `hot_standby`).\n3. **`logical`** : Ajoute les informations de décodage logique nécessaires à la réplication logique entre tables et aux flux CDC (Change Data Capture).',
      explanation: 'Pour la réplication physique et le PITR, `wal_level = replica` est le niveau requis.',
      examTrap: 'Changer `wal_level` nécessite obligatoirement un redémarrage complet du serveur PostgreSQL (`restart`) ; un simple reload de configuration ne suffit pas.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20: Write-Ahead Log Settings (wal_level)',
    },
  },
  {
    id: 'fc-pgedb-dom05-053',
    cardNumber: 53,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Structure physique et convention de nommage des segments WAL',
    difficulty: 'hard',
    tags: ['WAL', 'Segments', 'pg_wal', 'Naming', 'LSN'],
    front: {
      question: 'Quelle est la taille par défaut d\'un fichier segment WAL dans `$PGDATA/pg_wal/` et comment est composé son nom hexadécimal à 24 caractères ?',
      codeSnippet: '0000000100000002000000FF',
      hint: '16 Mo par segment, divisé en 3 parties de 8 caractères hexadécimaux.',
    },
    back: {
      answer: 'Un fichier WAL mesure **16 Mo par défaut** (configurable à l\'initdb via `--wal-segsize`).\n\nSon nom de 24 caractères hexadécimaux (96 bits) se décompose en 3 blocs de 8 caractères :\n1. **8 premiers caractères (ex: `00000001`)** : La **Timeline ID** (numéro de ligne temporelle, commence à 1).\n2. **8 caractères du milieu (ex: `00000002`)** : La partie haute (Log ID) du LSN.\n3. **8 derniers caractères (ex: `000000FF`)** : Le numéro de segment WAL dans ce Log ID (de 00 à FF pour des segments de 16 Mo).',
      explanation: 'La structure à 24 caractères permet de classer naturellement les fichiers par ordre chronologique strict dans le système de fichiers.',
      examTrap: 'Ne supprimez jamais manuellement de fichiers dans `$PGDATA/pg_wal/` avec `rm` : cela détruira la séquence transactionnelle et empêchera PostgreSQL de redémarrer.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 30: WAL Internals and File Layout',
    },
  },
  {
    id: 'fc-pgedb-dom05-054',
    cardNumber: 54,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Activation de l\'archivage continu : `archive_mode` et `archive_command`',
    difficulty: 'medium',
    tags: ['archive_mode', 'archive_command', 'PITR', 'postgresql.conf'],
    front: {
      question: 'Quels paramètres devez-vous configurer dans `postgresql.conf` pour activer l\'archivage continu des journaux WAL vers un stockage sécurisé ?',
      codeSnippet: '# Configuration requise:\narchive_mode = on\narchive_command = \'test ! -f /mnt/nfs/wal_archive/%f && cp %p /mnt/nfs/wal_archive/%f\'',
      hint: 'archive_mode, archive_command, et les macros %p et %f.',
    },
    back: {
      answer: 'Deux paramètres principaux sont indispensables :\n\n1. **`archive_mode = on`** : Active le processus d\'arrière-plan `archiver` (options : `off`, `on`, ou `always` pour archiver aussi depuis un standby).\n2. **`archive_command = \'...\'`** : Commande système shell exécutée chaque fois qu\'un fichier WAL de 16 Mo est clos et prêt à être sauvegardé.',
      explanation: 'Pour que l\'archivage prenne effet, `wal_level` doit également être au moins à `replica`. Passer `archive_mode` de `off` à `on` nécessite un redémarrage.',
      examTrap: 'La commande `archive_command` doit obligatoirement renvoyer un code de retour de succès (`exit 0`). Si la commande renvoie une valeur différente de 0, PostgreSQL considère que l\'archivage a échoué et retentera indéfiniment.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20: Continuous Archiving Settings',
    },
  },
  {
    id: 'fc-pgedb-dom05-055',
    cardNumber: 55,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Variables de substitution `%p` et `%f` dans `archive_command`',
    difficulty: 'easy',
    tags: ['archive_command', 'Macros', 'Shell', 'Configuration'],
    front: {
      question: 'Que représentent exactement les variables de substitution `%p` et `%f` dans la chaîne de configuration de `archive_command` ?',
      codeSnippet: 'archive_command = \'rsync -a %p backup_server:/var/wal_archive/%f\'',
      hint: '%p pour path complet du fichier source, %f pour filename seul.',
    },
    back: {
      answer: '- **`%p`** : Est remplacé par le **chemin d\'accès relatif ou absolu complet** du fichier WAL source à archiver (ex: `pg_wal/00000001000000010000002A`).\n- **`%f`** : Est remplacé par le **nom du fichier uniquement** (le nom à 24 caractères sans le répertoire, ex: `00000001000000010000002A`).',
      explanation: 'On utilise `%p` comme source de la copie et `%f` pour nommer le fichier dans le répertoire d\'archivage cible.',
      examTrap: 'Il est crucial de ne jamais écraser un fichier existant dans l\'archive avec un contenu différent : une bonne `archive_command` vérifie l\'absence préalable du fichier cible (`test ! -f ...`).',
      ruleRef: 'PostgreSQL 16 Documentation - archive_command substitutions',
    },
  },
  {
    id: 'fc-pgedb-dom05-056',
    cardNumber: 56,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Nouveauté PostgreSQL 16 : modules partagés `archive_library`',
    difficulty: 'hard',
    tags: ['archive_library', 'Postgres 16', 'Performance', 'Shared Library'],
    front: {
      question: 'Qu\'est-ce que le paramètre `archive_library` introduit dans les versions récentes et présent dans PostgreSQL 16, et quel est son avantage sur `archive_command` ?',
      codeSnippet: 'archive_mode = on\narchive_library = \'basic_archive\'',
      hint: 'Éviter de lancer un processus shell OS (fork/exec) pour chaque fichier WAL.',
    },
    back: {
      answer: '`archive_library` permet de charger une **bibliothèque C partagée dynamique (`.so` / `.dll`)** directement dans le processus `archiver` de PostgreSQL pour gérer l\'archivage des WAL.\n\n**Avantages majeurs** :\n1. **Performance** : Élimine le coût processeur du fork/exec répété d\'un sous-shell système (`/bin/sh`) à chaque segment de 16 Mo.\n2. **Fiabilité et contrôle** : Offre une gestion d\'erreur native, des retries programmables et des intégrations directes avec le stockage Cloud (AWS S3, Azure Blob, GCP Storage).',
      explanation: 'PostgreSQL inclut le module modèle `basic_archive` dans ses extensions `contrib`.',
      examTrap: 'Si `archive_library` est configuré, `archive_command` est ignoré. PostgreSQL émettra un avertissement si les deux sont définis.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20: archive_library',
    },
  },
  {
    id: 'fc-pgedb-dom05-057',
    cardNumber: 57,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Conséquence fatale d\'un échec répété de `archive_command`',
    difficulty: 'medium',
    tags: ['archive_command', 'Disk Full', 'pg_wal', 'Alerting', 'Incident'],
    front: {
      question: 'Que se passe-t-il sur le serveur PostgreSQL si la commande définie dans `archive_command` échoue en continu (ex: partage NFS cible saturé ou déconnecté) ?',
      hint: 'PostgreSQL ne supprime JAMAIS un WAL qui n\'a pas encore été archivé avec succès...',
    },
    back: {
      answer: 'PostgreSQL applique une politique de sécurité stricte : **aucun fichier WAL n\'est supprimé ou recyclé tant que son archivage n\'a pas réussi (retour 0 de `archive_command`)**.\n\nConséquence en chaîne :\n1. Les segments WAL s\'accumulent indéfiniment dans le répertoire local `$PGDATA/pg_wal/`.\n2. L\'espace disque du système de fichiers contenant `pg_wal` se remplit progressivement jusqu\'à saturation complète (100% plein).\n3. À saturation du disque, **PostgreSQL s\'arrête net (PANIC / crash)** et refuse toute nouvelle écriture ou transaction applicative.',
      explanation: 'La surveillance de `pg_stat_archiver` et de l\'espace disque de `pg_wal` est l\'une des alertes de monitoring les plus critiques pour un DBA PostgreSQL.',
      examTrap: 'Le paramètre `max_wal_size` ne purge PAS les WAL non archivés : la préservation de l\'intégrité de l\'archive WAL passe toujours avant la limite de `max_wal_size`.',
      ruleRef: 'PostgreSQL 16 Documentation - Continuous Archiving Warnings',
    },
  },
  {
    id: 'fc-pgedb-dom05-058',
    cardNumber: 58,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Garantie de RPO avec `archive_timeout`',
    difficulty: 'medium',
    tags: ['archive_timeout', 'RPO', 'WAL Archiving', 'Data Loss'],
    front: {
      question: 'À quoi sert le paramètre `archive_timeout` et dans quel cas est-il indispensable pour respecter les objectifs de perte de données (RPO) ?',
      codeSnippet: '# postgresql.conf\narchive_timeout = 900  # 15 minutes',
      hint: 'Forcer la fermeture et l\'archivage d\'un WAL même s\'il n\'a pas atteint les 16 Mo.',
    },
    back: {
      answer: 'Par défaut, un fichier WAL n\'est archivé que lorsqu\'il est entièrement rempli (16 Mo). Sur une base de données avec peu d\'activité d\'écriture nocturne ou le week-end, le remplissage de 16 Mo peut prendre des heures ou des jours.\n\n`archive_timeout` force PostgreSQL à **basculer périodiquement vers un nouveau fichier WAL** si le fichier en cours a été ouvert depuis plus longtemps que la durée spécifiée (ex: 15 minutes), déclenchant son archivage immédiat.',
      explanation: 'Cela garantit que l\'archive déportée ne prend jamais plus de retard que la valeur de `archive_timeout`, plafonnant ainsi le RPO en cas de désastre matériel.',
      examTrap: 'Ne réglez pas `archive_timeout` à une valeur trop basse (ex: 10 secondes), car cela générerait un grand nombre de fichiers de 16 Mo quasi-vides, gaspillant de la bande passante et du stockage.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20: archive_timeout',
    },
  },
  {
    id: 'fc-pgedb-dom05-059',
    cardNumber: 59,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Bascule manuelle de segment WAL avec `pg_switch_wal()`',
    difficulty: 'easy',
    tags: ['pg_switch_wal', 'WAL', 'SQL Functions', 'Archiving'],
    front: {
      question: 'Quelle fonction SQL permet à un administrateur de forcer immédiatement la clôture du segment WAL en cours et le passage au suivant ?',
      hint: 'Fonction pour switcher de WAL.',
    },
    back: {
      answer: 'La fonction SQL est **`SELECT pg_switch_wal();`**.\n\nElle ferme le fichier WAL actif, l\'envoie immédiatement au processus `archiver` pour traitement par `archive_command`, et ouvre le segment WAL suivant.',
      explanation: 'Elle renvoie la position LSN de fin du fichier commuté.',
      examTrap: 'Dans les anciennes versions (antérieures à PostgreSQL 10), cette fonction s\'appelait `pg_switch_xlog()`. Dans PostgreSQL 16, c\'est obligatoirement `pg_switch_wal()`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 9: Backup Control Functions',
    },
  },
  {
    id: 'fc-pgedb-dom05-060',
    cardNumber: 60,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Surveillance du processus d\'archivage avec `pg_stat_archiver`',
    difficulty: 'medium',
    tags: ['pg_stat_archiver', 'Monitoring', 'Troubleshooting', 'Metrics'],
    front: {
      question: 'Quelle vue système permet de diagnostiquer la santé de l\'archivage WAL et quelles colonnes clés alertent sur un blocage ?',
      codeSnippet: 'SELECT archived_count, last_archived_wal, failed_count, last_failed_wal, last_failed_time \nFROM pg_stat_archiver;',
      hint: 'failed_count, last_failed_wal, et last_failed_time.',
    },
    back: {
      answer: 'La vue système est **`pg_stat_archiver`**.\n\nColonnes d\'alerte fondamentales :\n- **`failed_count`** : Nombre total de tentatives d\'archivage ayant échoué. Si ce compteur s\'incrémente continuellement, l\'archivage est en panne.\n- **`last_failed_wal`** : Nom du segment WAL sur lequel l\'échec survient.\n- **`last_failed_time`** : Horodatage précis de la dernière erreur.\n- **`archived_count`** : Nombre de segments archivés avec succès.',
      explanation: 'Cette vue fournit un diagnostic immédiat pour savoir si `archive_command` rencontre des soucis de droits, de disque plein ou de réseau.',
      examTrap: 'Si `archived_count` ne progresse plus et que `last_failed_wal` correspond au plus ancien fichier WAL en attente dans `pg_wal`, toute la chaîne d\'archivage est bloquée.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 28: pg_stat_archiver view',
    },
  },

  // =========================================================================
  // SECTION 7 : RESTAURATION POINT-IN-TIME RECOVERY (PITR) & REPLAY (Cartes 61 à 70)
  // recovery.signal, restore_command, cibles (time, xid, lsn, name), timelines
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-061',
    cardNumber: 61,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Séquencement des 4 étapes majeures d\'un PITR',
    difficulty: 'medium',
    tags: ['PITR', 'Procedure', 'Restore', 'Recovery', 'WAL'],
    front: {
      question: 'Quelles sont les 4 étapes chronologiques indispensables pour réaliser une restauration Point-in-Time Recovery (PITR) après une erreur humaine (ex: DROP TABLE à 14h15) ?',
      hint: 'Arrêt, restauration des fichiers physiques, configuration du signal et restore_command, démarrage et replay.',
    },
    back: {
      answer: 'Les 4 étapes chronologiques sont :\n\n1. **Arrêter l\'instance** et sécuriser/sauvegarder les derniers WAL non archivés présents dans `pg_wal`.\n2. **Nettoyer et restaurer la sauvegarde physique de base** (`pg_basebackup` ou tar) dans `$PGDATA` vierge.\n3. **Configurer la cible** : Créer le fichier vide **`recovery.signal`** dans `$PGDATA`, et spécifier `restore_command` ainsi que `recovery_target_time = "14:14:00"` dans `postgresql.conf`.\n4. **Démarrer PostgreSQL** : Le serveur passe en mode Recovery, extrait les WAL via `restore_command`, rejoue les transactions jusqu\'à la cible exacte, puis s\'arrête ou ouvre la base.',
      explanation: 'Le PITR permet de voyager dans le temps pour remonter la base à la seconde précédant l\'incident.',
      examTrap: 'Ne jamais décompresser une sauvegarde physique de base par-dessus un `$PGDATA` existant non vidé : il faut toujours partir d\'un répertoire complètement propre pour éviter les fichiers résiduels corrompus.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Point-in-Time Recovery (PITR)',
    },
  },
  {
    id: 'fc-pgedb-dom05-062',
    cardNumber: 62,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Rôle du fichier déclencheur `recovery.signal` (PostgreSQL 12 à 16)',
    difficulty: 'easy',
    tags: ['recovery.signal', 'Signals', 'PITR', 'Postgres 16'],
    front: {
      question: 'Quel est le rôle du fichier `recovery.signal` dans les versions modernes de PostgreSQL (dont PostgreSQL 16) et quelle est sa différence avec `standby.signal` ?',
      codeSnippet: 'touch $PGDATA/recovery.signal',
      hint: 'L\'un est pour une restauration ciblée qui s\'arrête, l\'autre pour un réplica permanent en streaming.',
    },
    back: {
      answer: '- **`recovery.signal`** : Fichier marqueur vide qui indique à PostgreSQL au démarrage d\'entrer en **mode de restauration ciblée (Archive Recovery / PITR)**. Il rejoue les journaux jusqu\'à la cible configurée puis promut la base en lecture/écriture.\n- **`standby.signal`** : Indique à PostgreSQL d\'entrer en **mode Standby permanent** (réplica passif qui reste connecté au primaire et attend des WAL indéfiniment).',
      explanation: 'Dès que la restauration PITR atteint sa cible et que le serveur s\'ouvre en production, PostgreSQL supprime automatiquement le fichier `recovery.signal`.',
      examTrap: 'Si les deux fichiers `recovery.signal` et `standby.signal` sont présents simultanément dans `$PGDATA`, le mode Standby prend la priorité.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Recovery Target Settings',
    },
  },
  {
    id: 'fc-pgedb-dom05-063',
    cardNumber: 63,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Paramètre de rapatriement `restore_command`',
    difficulty: 'medium',
    tags: ['restore_command', 'PITR', 'Archive', 'Shell'],
    front: {
      question: 'Quel est le rôle du paramètre `restore_command` lors d\'un PITR et quelle syntaxe shell standard utilise-t-il ?',
      codeSnippet: 'restore_command = \'cp /mnt/nfs/wal_archive/%f %p\'',
      hint: 'L\'inverse symétrique d\'archive_command : ramener les WAL depuis l\'archive.',
    },
    back: {
      answer: '`restore_command` est la commande shell exécutée par PostgreSQL pour **récupérer un segment WAL archivé et le copier dans `$PGDATA/pg_wal/`** pendant la phase de recovery.\n\nSyntaxe des macros :\n- **`%f`** : Nom du fichier WAL recherché par le moteur (ex: `00000001000000010000002A`).\n- **`%p`** : Chemin de destination sur le cluster local où le fichier doit être écrit.',
      explanation: 'Quand PostgreSQL atteint la fin des WAL disponibles, `restore_command` renvoie un code d\'erreur non-zéro, ce qui signale normalement à PostgreSQL que le dernier fichier a été atteint.',
      examTrap: 'Ne faites pas échouer brutalement le serveur si un fichier manque : PostgreSQL teste régulièrement si le WAL suivant existe en appelant `restore_command`. Un code de retour non-zéro est normal et attendu en fin de recovery.',
      ruleRef: 'PostgreSQL 16 Documentation - restore_command details',
    },
  },
  {
    id: 'fc-pgedb-dom05-064',
    cardNumber: 64,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Cible temporelle avec `recovery_target_time`',
    difficulty: 'easy',
    tags: ['recovery_target_time', 'PITR', 'Timestamp', 'Timezone'],
    front: {
      question: 'Comment définir une cible de restauration temporelle exacte à l\'aide du paramètre `recovery_target_time` ?',
      codeSnippet: '# postgresql.conf\nrecovery_target_time = \'2026-09-15 14:14:30.000 UTC\'',
      hint: 'Chaîne d\'horodatage avec fuseau horaire explicite.',
    },
    back: {
      answer: 'On configure **`recovery_target_time`** avec un horodatage textuel compatible `timestamp with time zone` :\n```ini\nrecovery_target_time = \'2026-09-15 14:14:30 UTC\'\n```\nPostgreSQL rejouera les WAL jusqu\'à la dernière transaction validée (`commit`) avant ou à cet instant précis.',
      explanation: 'Il est fortement conseillé de préciser le fuseau horaire (ex: `UTC` ou `Europe/Paris`) pour éviter toute ambigüité liée aux réglages de locale du système d\'exploitation.',
      examTrap: 'Si `recovery_target_time` est configuré à une date antérieure à l\'instant où la sauvegarde de base physique s\'est terminée (indiqué dans `backup_label`), PostgreSQL échouera en indiquant qu\'il ne peut pas remonter si loin dans le passé.',
      ruleRef: 'PostgreSQL 16 Documentation - recovery_target_time',
    },
  },
  {
    id: 'fc-pgedb-dom05-065',
    cardNumber: 65,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Cible par identifiant de transaction : `recovery_target_xid`',
    difficulty: 'medium',
    tags: ['recovery_target_xid', 'PITR', 'Transactions', 'Forensics'],
    front: {
      question: 'Dans quelle situation utilise-t-on `recovery_target_xid` plutôt que `recovery_target_time` et comment déterminer cet identifiant ?',
      hint: 'Quand on a repéré dans les logs l\'ID précis de la transaction destructrice.',
    },
    back: {
      answer: 'On utilise **`recovery_target_xid`** lorsqu\'on a identifié dans les logs PostgreSQL ou via un outil d\'audit de WAL (`pg_waldump`) le **numéro de transaction (XID)** exact ayant causé l\'incident (ex: un script automatique erroné).\n\nEn couplant `recovery_target_xid = "458921"` avec **`recovery_target_inclusive = false`**, PostgreSQL rejoue toutes les transactions jusqu\'à celle-ci en **excluant strictement la transaction incriminée**.',
      explanation: 'C\'est la méthode la plus chirurgicale possible pour annuler une erreur humaine sans perdre les transactions légitimes survenues quelques millisecondes plus tôt.',
      examTrap: 'Par défaut, `recovery_target_inclusive` vaut `true`. Si vous oubliez de le régler sur `false`, la transaction fautive spécifiée dans `recovery_target_xid` sera elle aussi rejouée et committée !',
      ruleRef: 'PostgreSQL 16 Documentation - recovery_target_xid and recovery_target_inclusive',
    },
  },
  {
    id: 'fc-pgedb-dom05-066',
    cardNumber: 66,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Cible par position LSN : `recovery_target_lsn`',
    difficulty: 'hard',
    tags: ['recovery_target_lsn', 'LSN', 'PITR', 'Replication'],
    front: {
      question: 'Qu\'est-ce que `recovery_target_lsn` et pourquoi est-ce la cible la plus précise mathématiquement ?',
      codeSnippet: 'recovery_target_lsn = \'1/4A23F0B8\'',
      hint: 'Log Sequence Number, adresse octet 64 bits dans le flux WAL.',
    },
    back: {
      answer: 'Le **Log Sequence Number (LSN)** est un entier 64 bits représentant l\'adresse exacte en octets dans le journal de transactions WAL.\n\n`recovery_target_lsn` demande à PostgreSQL d\'appliquer le recovery jusqu\'à ce point d\'octet précis dans le WAL. Contrairement au temps qui dépend des horloges de serveurs sujettes au drift temporel (NTP), le LSN est un ordre séquentiel monotone absolu et déterministe.',
      explanation: 'C\'est la méthode utilisée en interne par les mécanismes de failover et d\'orchestration haute disponibilité pour aligner précisément deux réplicas.',
      examTrap: 'Le LSN doit être au-delà du point de cohérence minimale indiqué dans le fichier `backup_label`.',
      ruleRef: 'PostgreSQL 16 Documentation - recovery_target_lsn',
    },
  },
  {
    id: 'fc-pgedb-dom05-067',
    cardNumber: 67,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Points de restauration nommés : `pg_create_restore_point`',
    difficulty: 'medium',
    tags: ['pg_create_restore_point', 'Named Restore Point', 'PITR', 'DevOps'],
    front: {
      question: 'Comment poser un point de repère textuel nommé dans le flux WAL avant une opération de maintenance applicative risquée, et comment cibler ce point lors d\'un PITR ?',
      hint: 'Fonction pg_create_restore_point et paramètre recovery_target_name.',
    },
    back: {
      answer: '1. **Création du point de repère** (avant l\'opération) :\n```sql\nSELECT pg_create_restore_point(\'avant_migration_v2\');\n```\nCette commande écrit un enregistrement spécial nommé dans les WAL.\n\n2. **Restauration ciblée** (en cas d\'échec de la migration) :\nConfigurer dans `postgresql.conf` :\n```ini\nrecovery_target_name = \'avant_migration_v2\'\n```',
      explanation: 'C\'est une pratique d\'excellence avant toute mise en production risquée : cela évite d\'avoir à noter ou chercher des heures précises ou des XIDs.',
      examTrap: 'La fonction `pg_create_restore_point()` requiert les privilèges superutilisateur ou le rôle prédéfini `pg_checkpoint` / `pg_write_all_data`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_create_restore_point() and recovery_target_name',
    },
  },
  {
    id: 'fc-pgedb-dom05-068',
    cardNumber: 68,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Action en fin de recovery : `recovery_target_action`',
    difficulty: 'medium',
    tags: ['recovery_target_action', 'pause', 'promote', 'shutdown', 'PITR'],
    front: {
      question: 'Quelles sont les 3 valeurs possibles pour `recovery_target_action` et pourquoi la valeur `pause` est-elle la plus sûre en production ?',
      codeSnippet: 'recovery_target_action = \'pause\' # (Défaut)',
      hint: 'pause, promote, shutdown.',
    },
    back: {
      answer: 'Les 3 valeurs sont :\n\n1. **`pause`** (Défaut) : Met le serveur en pause dès que la cible est atteinte. La base reste en lecture seule (`Read-Only`), permettant au DBA de se connecter pour **vérifier l\'état des données** (`SELECT`) avant de confirmer la reprise.\n2. **`promote`** : Promeut immédiatement le serveur en lecture/écriture (`Read-Write`), crée une nouvelle timeline et accepte les connexions clientes.\n3. **`shutdown`** : Arrête immédiatement le serveur PostgreSQL dès que la cible de restauration est atteinte.',
      explanation: 'Avec `pause`, si le DBA s\'aperçoit qu\'il a visé 5 minutes trop tôt, il peut modifier `recovery_target_time` et reprendre le replay avec `SELECT pg_wal_replay_resume();` sans tout recommencer.',
      examTrap: 'Si le serveur est en pause après PITR, pour le promouvoir définitivement en production, il faut exécuter `SELECT pg_wal_replay_resume();`.',
      ruleRef: 'PostgreSQL 16 Documentation - recovery_target_action',
    },
  },
  {
    id: 'fc-pgedb-dom05-069',
    cardNumber: 69,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Notion de Timelines et fichiers d\'historique `.history`',
    difficulty: 'hard',
    tags: ['Timelines', 'History File', 'Branching', 'PITR Internals'],
    front: {
      question: 'Qu\'est-ce qu\'une Timeline dans PostgreSQL, pourquoi une nouvelle timeline est-elle créée après chaque promotion/PITR, et à quoi sert le fichier `.history` ?',
      hint: 'Résolution du problème du paradoxe temporel en cas de restauration d\'un point passé.',
    },
    back: {
      answer: 'Une **Timeline** est un identifiant de branche temporelle (entier incrémental, ex: Timeline 1, 2, 3...) qui identifie de manière unique la lignée d\'un cluster.\n\n- **Pourquoi créer une timeline ?** Si on restaure un état passé à 14h00 alors que la base avait déjà fonctionné jusqu\'à 15h00, recommencer à écrire générerait de nouveaux WAL avec les mêmes noms que les anciens : la nouvelle timeline empêche d\'écraser les anciens WAL.\n- **Fichier `.history`** : Fichier texte (ex: `00000002.history`) qui enregistre l\'arbre généalogique des timelines (indique à quel LSN exact la timeline 2 a bifurqué depuis la timeline 1).',
      explanation: 'Grâce aux fichiers d\'historique, PostgreSQL sait naviguer entre différentes branches de restauration en cascade.',
      examTrap: 'Si vous perdez les fichiers `.history` dans votre archive WAL, PostgreSQL ne saura pas comment relier les segments des différentes timelines.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Timelines',
    },
  },
  {
    id: 'fc-pgedb-dom05-070',
    cardNumber: 70,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Suivre la dernière ligne temporelle avec `recovery_target_timeline = "latest"`',
    difficulty: 'medium',
    tags: ['recovery_target_timeline', 'latest', 'Standby', 'PITR'],
    front: {
      question: 'Pourquoi est-il recommandé de configurer `recovery_target_timeline = "latest"` sur les serveurs réplicas (Standby) et lors d\'un PITR ?',
      codeSnippet: 'recovery_target_timeline = \'latest\'',
      hint: 'Suivre automatiquement la timeline la plus récente trouvée dans l\'archive WAL.',
    },
    back: {
      answer: 'Par défaut (`recovery_target_timeline = "current"`), PostgreSQL ne rejoue que les WAL appartenant à la timeline courante de la sauvegarde de base.\n\nEn configurant **`recovery_target_timeline = "latest"`** :\nPostgreSQL inspecte l\'archive WAL, découvre les fichiers d\'historique `.history` et bascule automatiquement sur la branche de timeline la plus récente disponible.\n\nC\'est indispensable pour un Standby afin qu\'il suive automatiquement son primaire si celui-ci a fait l\'objet d\'un failover ou d\'un basculement de timeline.',
      explanation: 'Dans PostgreSQL 12+, `latest` est devenu le comportement par défaut pour les réplicas Standby.',
      examTrap: 'Si vous forcez un numéro de timeline numérique (ex: `recovery_target_timeline = 1`), le moteur s\'arrêtera à la fin de la timeline 1 et refusera de rejouer les segments de la timeline 2.',
      ruleRef: 'PostgreSQL 16 Documentation - recovery_target_timeline',
    },
  },

  // =========================================================================
  // SECTION 8 : RÉPLICATION PHYSIQUE EN STREAMING (STREAMING REPLICATION) (Cartes 71 à 80)
  // Architecture walsender/walreceiver, hot_standby, retards, conflits, promotion
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-071',
    cardNumber: 71,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Architecture de la Réplication Physique en Streaming',
    difficulty: 'easy',
    tags: ['Streaming Replication', 'Architecture', 'Primary', 'Standby'],
    front: {
      question: 'Comment fonctionne la réplication physique en streaming de PostgreSQL entre le serveur primaire et le standby ?',
      hint: 'Transmission continue des enregistrements WAL au fil de l\'eau sur le réseau sans attendre un fichier de 16 Mo.',
    },
    back: {
      answer: 'La **Réplication Physique en Streaming** transmet les enregistrements WAL directement sur une connexion réseau TCP en flux continu, octet par octet, dès qu\'ils sont générés par le primaire.\n\nLe réplica secondaire (Standby) reçoit ce flux et rejoue immédiatement les modifications dans ses propres fichiers de données, restant ainsi synchronisé avec un décalage de quelques millisecondes seulement (faible latence).',
      explanation: 'Elle ne nécessite pas d\'attendre qu\'un fichier WAL entier de 16 Mo soit complété et archivé, réduisant le RPO à une valeur quasi-nulle.',
      examTrap: 'La réplication physique reproduit l\'intégralité du cluster à l\'identique (bit à bit) : il est impossible d\'exclure une table ou une base spécifique en réplication physique.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Streaming Replication',
    },
  },
  {
    id: 'fc-pgedb-dom05-072',
    cardNumber: 72,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Processus d\'arrière-plan : `walsender`, `walreceiver` et `startup`',
    difficulty: 'medium',
    tags: ['Processes', 'walsender', 'walreceiver', 'startup', 'Internals'],
    front: {
      question: 'Quels sont les rôles respectifs des processus `walsender`, `walreceiver` et `startup` dans la chaîne de réplication streaming ?',
      hint: 'Un processus émetteur sur le primaire, deux processus récepteurs et applicateurs sur le standby.',
    },
    back: {
      answer: '1. **`walsender`** (Sur le Primaire) : Processus ouvrier dédié qui lit les WAL en mémoire partagée ou sur disque et les envoie sur le socket réseau au réplica.\n2. **`walreceiver`** (Sur le Standby) : Processus client qui se connecte au primaire, reçoit le flux réseau des WAL et les écrit dans le `pg_wal` local du standby.\n3. **`startup`** (Sur le Standby) : Processus historique de recovery qui lit les données écrites par `walreceiver` et rejoue (*redo/apply*) les modifications dans les blocs de tables et d\'index.',
      explanation: 'Si le réplica prend du retard, c\'est souvent le processus `startup` qui est ralenti par des requêtes en lecture concurrentes ou des I/O disques saturées.',
      examTrap: 'Sur le primaire, chaque standby connecté possède son propre processus `walsender`. Le nombre maximum de standbys simultanés est limité par le paramètre `max_wal_senders`.',
      ruleRef: 'PostgreSQL 16 Documentation - Streaming Replication Internals',
    },
  },
  {
    id: 'fc-pgedb-dom05-073',
    cardNumber: 73,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Serveur réplica en lecture seule avec `hot_standby = on`',
    difficulty: 'easy',
    tags: ['hot_standby', 'Read-Only', 'Replication', 'Offloading'],
    front: {
      question: 'Quel paramètre permet à un serveur secondaire (Standby) d\'accepter des requêtes en lecture (`SELECT`) pendant qu\'il rejoue en continu les WAL du primaire ?',
      codeSnippet: '# postgresql.conf sur le standby\nhot_standby = on',
      hint: 'hot_standby (activé par défaut).',
    },
    back: {
      answer: 'Le paramètre est **`hot_standby = on`** (activé par défaut dans PostgreSQL).\n\nIl permet d\'ouvrir le serveur secondaire aux requêtes de consultation (`SELECT`, génération de rapports analytiques, sauvegardes `pg_dump`), déchargeant ainsi le serveur primaire de la charge de lecture.',
      explanation: 'Toute tentative d\'écriture (`INSERT`, `UPDATE`, `CREATE TABLE`) sur un serveur `hot_standby` est immédiatement rejetée avec l\'erreur "ERROR: cannot execute ... in a read-only transaction".',
      examTrap: 'Si `hot_standby = off`, le serveur secondaire rejoue les WAL mais refuse toute connexion cliente non-superutilisateur.',
      ruleRef: 'PostgreSQL 16 Documentation - hot_standby parameter',
    },
  },
  {
    id: 'fc-pgedb-dom05-074',
    cardNumber: 74,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Configuration amont avec `primary_conninfo`',
    difficulty: 'medium',
    tags: ['primary_conninfo', 'Configuration', 'Standby', 'libpq'],
    front: {
      question: 'Où et comment configure-t-on la chaîne de connexion permettant au réplica de localiser et s\'authentifier auprès de son primaire ?',
      codeSnippet: 'primary_conninfo = \'host=10.0.0.1 port=5432 user=repuser password=Secret sslmode=require\'',
      hint: 'Dans postgresql.conf ou postgresql.auto.conf sur le serveur standby.',
    },
    back: {
      answer: 'On configure le paramètre **`primary_conninfo`** dans le fichier `postgresql.conf` ou `postgresql.auto.conf` du **serveur Standby**.\n\nC\'est une chaîne de connexion standard au format libpq contenant l\'hôte primaire (`host`), le port (`port`), l\'utilisateur de réplication (`user`), les options SSL (`sslmode`), et éventuellement le mot de passe (ou géré via `.pgpass`).',
      explanation: 'Depuis PostgreSQL 12+, ce paramètre peut être modifié et rechargé à chaud avec `pg_reload_conf()` sans nécessiter de redémarrage du standby.',
      examTrap: 'Ne configurez pas `primary_conninfo` sur le serveur primaire : il n\'a de sens que sur un nœud secondaire possédant un fichier `standby.signal`.',
      ruleRef: 'PostgreSQL 16 Documentation - primary_conninfo setting',
    },
  },
  {
    id: 'fc-pgedb-dom05-075',
    cardNumber: 75,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Conflits de réplication en lecture (`max_standby_streaming_delay`)',
    difficulty: 'hard',
    tags: ['Replication Conflicts', 'max_standby_streaming_delay', 'Vacuum', 'Locks'],
    front: {
      question: 'Qu\'est-ce qu\'un conflit de réplication sur un serveur `hot_standby` et quel paramètre évite d\'annuler immédiatement les requêtes de lecture en cours ?',
      hint: 'Le processus startup veut appliquer un nettoyage de tuple supprimé ou un verrou exclusif qu\'une lecture consulte.',
    },
    back: {
      answer: 'Un **conflit de réplication** survient lorsqu\'une requête en lecture sur le standby consulte des lignes ou détient un verrou sur une table alors que le flux WAL reçu ordonne de supprimer ces lignes (ex: `VACUUM` survenu sur le primaire) ou d\'acquérir un verrou exclusif (`ACCESS EXCLUSIVE` suite à un DDL).\n\nLe paramètre **`max_standby_streaming_delay`** (30s par défaut) définit le temps maximal que le processus de replay attend avant **d\'annuler brutalement la requête de lecture cliente** (`canceling statement due to conflict with recovery`).',
      explanation: 'Si on règle la valeur à `-1`, le replay attendra indéfiniment la fin de la requête de lecture, mais le standby accumulera un retard de réplication grandissant.',
      examTrap: 'Pour éviter que le VACUUM du primaire ne supprime des tuples encore nécessaires aux lectures du standby, on peut activer `hot_standby_feedback = on`.',
      ruleRef: 'PostgreSQL 16 Documentation - Handling Replication Conflicts',
    },
  },
  {
    id: 'fc-pgedb-dom05-076',
    cardNumber: 76,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Surveillance de la réplication sur le primaire : `pg_stat_replication`',
    difficulty: 'medium',
    tags: ['pg_stat_replication', 'Monitoring', 'LSN', 'Standby Status'],
    front: {
      question: 'Quelle vue système sur le serveur primaire permet de visualiser l\'état de tous les réplicas connectés et quelles sont les 4 étapes de progression LSN affichées ?',
      codeSnippet: 'SELECT client_addr, state, sync_state, sent_lsn, write_lsn, flush_lsn, replay_lsn\nFROM pg_stat_replication;',
      hint: 'sent_lsn, write_lsn, flush_lsn, replay_lsn.',
    },
    back: {
      answer: 'La vue système est **`pg_stat_replication`**.\n\nLes 4 étapes de progression LSN sont :\n1. **`sent_lsn`** : Point LSN envoyé sur le réseau par le `walsender` du primaire.\n2. **`write_lsn`** : Point LSN reçu et écrit dans les tampons OS du standby par `walreceiver`.\n3. **`flush_lsn`** : Point LSN persisté physiquement sur le disque du standby (`fsync`).\n4. **`replay_lsn`** : Point LSN effectivement réappliqué et visible dans les données par le processus `startup`.',
      explanation: 'La différence entre ces LSN permet de savoir précisément si un retard provient du réseau, du sous-système disque ou de la vitesse d\'application CPU.',
      examTrap: 'Si `sent_lsn` est loin devant `replay_lsn`, le standby a du lag applicatif (souvent causé par des requêtes de lecture longues ou des I/O lentes sur le standby).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 28: pg_stat_replication',
    },
  },
  {
    id: 'fc-pgedb-dom05-077',
    cardNumber: 77,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Calcul du retard de réplication (Replication Lag) avec `pg_wal_lsn_diff()`',
    difficulty: 'medium',
    tags: ['Replication Lag', 'pg_wal_lsn_diff', 'SQL Query', 'Monitoring'],
    front: {
      question: 'Quelle fonction SQL permet de calculer avec exactitude le retard de réplication en octets entre le primaire et un standby ?',
      codeSnippet: 'SELECT client_addr, application_name,\n  pg_wal_lsn_diff(pg_current_wal_lsn(), replay_lsn) AS lag_bytes\nFROM pg_stat_replication;',
      hint: 'Fonction de différence entre deux valeurs LSN.',
    },
    back: {
      answer: 'On utilise la fonction **`pg_wal_lsn_diff(lsn1, lsn2)`**.\n\nElle soustrait deux positions LSN et renvoie le décalage exact **en nombre d\'octets** (bytes) sous forme de valeur `numeric`.\n\nExemple de diagnostic :\n```sql\nSELECT application_name,\n  pg_size_pretty(pg_wal_lsn_diff(sent_lsn, replay_lsn)) AS replay_lag\nFROM pg_stat_replication;\n```',
      explanation: 'Combinée avec `pg_size_pretty()`, elle permet d\'afficher un résultat immédiatement lisible (ex: `128 MB`, `4 kB`).',
      examTrap: 'Ne comparez pas des LSN par de simples soustractions arithmétiques `-` en SQL : les LSN sont des types de données hexadécimaux spécifiques (`pg_lsn`).',
      ruleRef: 'PostgreSQL 16 Documentation - WAL Information Functions (pg_wal_lsn_diff)',
    },
  },
  {
    id: 'fc-pgedb-dom05-078',
    cardNumber: 78,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Surveillance locale sur le standby : `pg_stat_wal_receiver`',
    difficulty: 'medium',
    tags: ['pg_stat_wal_receiver', 'Standby', 'Monitoring', 'walreceiver'],
    front: {
      question: 'Quelle vue système interrogée directement SUR LE STANDBY permet de vérifier qu\'il est activement connecté à son primaire et reçoit le flux ?',
      codeSnippet: 'SELECT status, sender_host, sender_port, latest_end_lsn, received_tli \nFROM pg_stat_wal_receiver;',
      hint: 'Vue spécifique au processus walreceiver.',
    },
    back: {
      answer: 'La vue système est **`pg_stat_wal_receiver`**.\n\nElle renvoie une seule ligne si le processus `walreceiver` est actif, avec des informations vitales :\n- **`status`** : Doit indiquer `streaming`.\n- **`sender_host` / `sender_port`** : Adresse du serveur primaire amont.\n- **`latest_end_lsn`** : Dernier LSN reçu.\n- **`last_msg_receipt_time`** : Horodatage du dernier message keepalive reçu du primaire.',
      explanation: 'Si cette requête renvoie 0 ligne sur le standby, cela signifie que la réplication en streaming est interrompue (problème réseau, mot de passe incorrect, ou standby en échec).',
      examTrap: 'Sur le serveur primaire, interroger `pg_stat_wal_receiver` renvoie toujours 0 ligne car aucun walreceiver ne tourne sur un primaire.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 28: pg_stat_wal_receiver',
    },
  },
  {
    id: 'fc-pgedb-dom05-079',
    cardNumber: 79,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Réplication en cascade (Cascading Replication)',
    difficulty: 'hard',
    tags: ['Cascading Replication', 'Scale-out', 'WAN', 'Architecture'],
    front: {
      question: 'Qu\'est-ce que la réplication en cascade (*Cascading Standby*) et quel problème d\'architecture permet-elle de résoudre ?',
      hint: 'Un serveur Standby qui sert lui-même d\'émetteur walsender vers d\'autres Standbys.',
    },
    back: {
      answer: 'La **Réplication en cascade** permet à un serveur Standby de se comporter comme une source de réplication en relayant le flux WAL qu\'il reçoit vers d\'autres Standbys en aval :\n`Primaire -> Standby 1 (Relais) -> Standby 2, 3, 4`.\n\nElle résout deux problèmes majeurs :\n1. **Bande passante réseau inter-sites (WAN)** : Pour répliquer vers un datacenter distant avec 5 réplicas, un seul flux traverse la liaison WAN vers le Standby relais, qui distribue ensuite localement aux 4 autres.\n2. **Soulagement CPU/réseau du Primaire** : Évite au primaire de maintenir des dizaines de connexions `walsender` concurrentes.',
      explanation: 'Le Standby relais doit lui aussi avoir `max_wal_senders` configuré pour pouvoir émettre vers les standbys en aval.',
      examTrap: 'Un standby en cascade est toujours asynchrone par rapport à ses enfants : il ne peut pas servir de réplication synchrone garantissant la persistance sur le primaire original.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Cascading Replication',
    },
  },
  {
    id: 'fc-pgedb-dom05-080',
    cardNumber: 80,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Promotion du Standby en Primaire : commande et fonction SQL',
    difficulty: 'easy',
    tags: ['Promotion', 'pg_ctl promote', 'pg_promote', 'Failover'],
    front: {
      question: 'Quelles sont les deux méthodes officielles pour promouvoir un serveur Standby afin qu\'il devienne le nouveau serveur Primaire en lecture/écriture ?',
      hint: 'Une commande CLI côté système d\'exploitation et une fonction SQL moderne exécutable à distance.',
    },
    back: {
      answer: 'Les 2 méthodes officielles sont :\n\n1. **En ligne de commande OS** via l\'utilitaire `pg_ctl` :\n```bash\npg_ctl promote -D /var/lib/postgresql/16/main\n```\n2. **En SQL** via la fonction d\'administration intégrée :\n```sql\nSELECT pg_promote(wait => true, wait_seconds => 60);\n```',
      explanation: 'Lors de la promotion, le standby finit de rejouer les WAL déjà reçus, supprime son fichier `standby.signal`, incrémente sa Timeline (ex: passage de timeline 1 à 2), et passe immédiatement en mode lecture/écriture.',
      examTrap: 'L\'ancienne méthode consistant à créer un fichier trigger (`trigger_file`) est désormais dépréciée au profit de `pg_ctl promote` et `pg_promote()`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_ctl promote and pg_promote() function',
    },
  },

  // =========================================================================
  // SECTION 9 : SLOTS DE RÉPLICATION & RÉPLICATION SYNCHRONE (Cartes 81 à 90)
  // Replication slots, max_slot_wal_keep_size, synchronous_standby_names, synchronous_commit
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-081',
    cardNumber: 81,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Rôle fondamental des Slots de Réplication Physiques (Replication Slots)',
    difficulty: 'medium',
    tags: ['Replication Slots', 'WAL Retention', 'High Availability', 'Architecture'],
    front: {
      question: 'Quel problème critique les Slots de Réplication Physiques (Replication Slots) résolvent-ils dans une architecture primaire-secondaire ?',
      hint: 'Éviter que le primaire ne supprime ou recycle des fichiers WAL dont le réplica a encore besoin.',
    },
    back: {
      answer: 'Sans slot de réplication, si un réplica est arrêté ou déconnecté pour maintenance pendant que le primaire subit une forte activité d\'écritures, le primaire recycle/supprime ses anciens fichiers WAL pour ne pas saturer son propre disque. À sa reconnexion, le réplica constate qu\'un segment WAL manque et casse sa réplication.\n\nUn **Slot de Réplication Physique garantit que le primaire conservera TOUS les segments WAL sur son disque tant que le standby associé n\'a pas confirmé les avoir reçus et appliqués**.',
      explanation: 'Le slot mémorise la position LSN minimale requise par le réplica.',
      examTrap: 'Si le standby reste arrêté trop longtemps, la garantie du slot peut devenir une arme à double tranchant en saturant le disque du primaire.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Replication Slots',
    },
  },
  {
    id: 'fc-pgedb-dom05-082',
    cardNumber: 82,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Création et configuration d\'un Slot de Réplication Physique',
    difficulty: 'medium',
    tags: ['pg_create_physical_replication_slot', 'primary_slot_name', 'Configuration'],
    front: {
      question: 'Comment créer un slot de réplication physique sur le primaire et comment configurer le réplica pour qu\'il utilise ce slot ?',
      hint: 'Fonction pg_create_physical_replication_slot sur le primaire et paramètre primary_slot_name sur le standby.',
    },
    back: {
      answer: '1. **Sur le serveur Primaire** : Créer le slot de réplication avec la fonction SQL :\n```sql\nSELECT pg_create_physical_replication_slot(\'standby_node1_slot\');\n```\n2. **Sur le serveur Standby** : Renseigner le paramètre **`primary_slot_name`** dans son `postgresql.conf` (ou `postgresql.auto.conf`) :\n```ini\nprimary_slot_name = \'standby_node1_slot\'\n```',
      explanation: 'Lorsque le `walreceiver` du standby se connecte, il s\'associe à ce slot et transmet continuellement son `flush_lsn`, permettant au primaire de libérer les WAL plus anciens.',
      examTrap: 'Le nom du slot de réplication ne doit contenir que des lettres minuscules, des chiffres et des tirets bas `_` (pas de tiret `-`).',
      ruleRef: 'PostgreSQL 16 Documentation - primary_slot_name setting',
    },
  },
  {
    id: 'fc-pgedb-dom05-083',
    cardNumber: 83,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Le danger mortel des Replication Slots inactifs',
    difficulty: 'hard',
    tags: ['Replication Slots', 'Disk Full', 'Production Incident', 'pg_wal'],
    front: {
      question: 'Quel incident grave de production survient si un serveur Standby utilisant un slot de réplication est détruit, arrêté ou oublié pendant plusieurs jours sans supprimer son slot ?',
      hint: 'Le primaire continue de stocker tous les WAL sans jamais pouvoir en recycler un seul.',
    },
    back: {
      answer: 'Le serveur primaire continue d\'accumuler tous les journaux WAL générés dans son répertoire `$PGDATA/pg_wal/`, sans **JAMAIS pouvoir en recycler ni purger aucun** (puisqu\'il attend désespérément le standby).\n\nConséquence inévitable :\nLe disque dur du serveur primaire se remplit à 100%, provoquant un **PANIC / arrêt brutal du moteur de base de données en production**, empêchant toute transaction.',
      explanation: 'Les slots orphelins sont l\'une des causes numéro 1 d\'arrêt inopiné d\'instances de production mal monitorées.',
      examTrap: 'La vue système `pg_replication_slots` permet d\'identifier les slots inactifs (`active = false`) et leur volume de rétention de WAL.',
      ruleRef: 'PostgreSQL 16 Documentation - Managing Replication Slots Safely',
    },
  },
  {
    id: 'fc-pgedb-dom05-084',
    cardNumber: 84,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Protection anti-saturation : paramètre `max_slot_wal_keep_size`',
    difficulty: 'medium',
    tags: ['max_slot_wal_keep_size', 'Disk Protection', 'Safeguard', 'Postgres 16'],
    front: {
      question: 'Quel paramètre de sécurité majeur permet de plafonner l\'espace disque maximal que les Replication Slots ont le droit de retenir dans `pg_wal` sur le primaire ?',
      codeSnippet: '# postgresql.conf\nmax_slot_wal_keep_size = 50GB',
      hint: 'max_slot_wal_keep_size (ex: 50GB ou 100GB).',
    },
    back: {
      answer: 'Le paramètre est **`max_slot_wal_keep_size`**.\n\nIl définit la quantité maximale de WAL que les slots de réplication peuvent retenir sur le primaire (ex: `50GB`). Si un standby déconnecté dépasse ce seuil de retard :\nPostgreSQL choisit de **protéger le primaire** : il supprime les WAL les plus anciens pour éviter la saturation du disque local, et marque le slot du standby en statut **`lost`** (invalide).',
      explanation: 'Le standby défaillant ne pourra plus se resynchroniser directement en streaming et devra être reconstruit via `pg_basebackup` ou `pg_rewind`, mais la production principale aura été sauvée du crash disque.',
      examTrap: 'Par défaut, `max_slot_wal_keep_size = -1`, ce qui signifie **rétention illimitée** ! Il est fortement recommandé en production de lui attribuer une valeur finie correspondant à l\'espace disque disponible.',
      ruleRef: 'PostgreSQL 16 Documentation - max_slot_wal_keep_size setting',
    },
  },
  {
    id: 'fc-pgedb-dom05-085',
    cardNumber: 85,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Suppression d\'un slot obsolète avec `pg_drop_replication_slot()`',
    difficulty: 'easy',
    tags: ['pg_drop_replication_slot', 'Maintenance', 'SQL Functions', 'Disk Space'],
    front: {
      question: 'Comment supprimer un slot de réplication physique orphelin qui retient des dizaines de gigaoctets de WAL sur le primaire ?',
      codeSnippet: 'SELECT pg_drop_replication_slot(\'ancien_standby_slot\');',
      hint: 'Fonction SQL pg_drop_replication_slot.',
    },
    back: {
      answer: 'En exécutant la fonction SQL suivante en tant que superutilisateur sur le primaire :\n```sql\nSELECT pg_drop_replication_slot(\'ancien_standby_slot\');\n```\nDès la suppression du slot, au prochain checkpoint, PostgreSQL libère et recycle immédiatement tous les segments WAL qui étaient retenus exclusivement par ce slot.',
      explanation: 'La vue `pg_replication_slots` permet de vérifier au préalable la liste de tous les slots existants.',
      examTrap: 'On ne peut pas supprimer un slot qui est actuellement actif (`active = true`). Il faut d\'abord arrêter le processus réplica connecté avant de drop le slot.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_drop_replication_slot() reference',
    },
  },
  {
    id: 'fc-pgedb-dom05-086',
    cardNumber: 86,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Réplication Asynchrone vs Réplication Synchrone',
    difficulty: 'easy',
    tags: ['Replication', 'Asynchronous', 'Synchronous', 'RPO', 'Performance'],
    front: {
      question: 'Quelle est la différence fondamentale entre la réplication asynchrone (mode par défaut) et la réplication synchrone en termes de commit et de risque de perte de données ?',
      hint: 'Attendre la confirmation du standby avant de rendre la main au client applicatif.',
    },
    back: {
      answer: '- **Réplication Asynchrone** (Défaut) : Dès que la transaction est écrite dans le WAL local du primaire (`fsync`), le `COMMIT` est confirmé au client sans attendre le standby. Débit maximal, mais risque de perte de quelques transactions récentes (RPO > 0) si le primaire brûle subitement.\n- **Réplication Synchrone** : Le `COMMIT` sur le primaire **attend obligatoirement qu\'au moins un standby désigné confirme avoir reçu et persisté le WAL** avant de confirmer le succès à l\'application. Garantie de zéro perte de données (RPO = 0), au prix d\'une latence de commit dépendante du réseau.',
      explanation: 'La réplication synchrone protège contre la perte de données financière ou bancaire au détriment de la vitesse de validation des transactions.',
      examTrap: 'La réplication synchrone augmente la latence de chaque transaction en écriture mais n\'impacte pas les requêtes en lecture seule `SELECT`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Synchronous Replication',
    },
  },
  {
    id: 'fc-pgedb-dom05-087',
    cardNumber: 87,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Configuration de la réplication synchrone avec `synchronous_standby_names`',
    difficulty: 'medium',
    tags: ['synchronous_standby_names', 'Configuration', 'Primary', 'application_name'],
    front: {
      question: 'Quel paramètre dans `postgresql.conf` sur le serveur primaire active la réplication synchrone et à quoi fait référence le nom spécifié ?',
      codeSnippet: '# postgresql.conf sur le primaire\nsynchronous_standby_names = \'standby1,standby2\'',
      hint: 'synchronous_standby_names et le paramètre application_name du standby.',
    },
    back: {
      answer: 'Le paramètre est **`synchronous_standby_names`**.\n\nLe nom spécifié fait référence à la chaîne **`application_name`** déclarée par le standby dans sa propre directive `primary_conninfo` (ex: `primary_conninfo = \'... application_name=standby1\'`).',
      explanation: 'Si la valeur de `synchronous_standby_names` est vide (`\'\'`), la réplication fonctionne en mode asynchrone.',
      examTrap: 'Si le nom renseigné dans `synchronous_standby_names` ne correspond à aucun `application_name` de standby connecté, toutes les écritures sur le primaire seront immédiatement bloquées en attente !',
      ruleRef: 'PostgreSQL 16 Documentation - synchronous_standby_names',
    },
  },
  {
    id: 'fc-pgedb-dom05-088',
    cardNumber: 88,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Syntaxes avancées de sélection synchrone : `FIRST` vs `ANY`',
    difficulty: 'hard',
    tags: ['synchronous_standby_names', 'FIRST', 'ANY', 'Quorum', 'High Availability'],
    front: {
      question: 'Quelle est la différence entre les syntaxes `FIRST num (...)` (priorité ordonnée) et `ANY num (...)` (quorum) dans `synchronous_standby_names` ?',
      codeSnippet: '# Option A:\nsynchronous_standby_names = \'FIRST 2 (node1, node2, node3)\'\n# Option B:\nsynchronous_standby_names = \'ANY 2 (node1, node2, node3)\'',
      hint: 'Ordre de priorité stricte des réplicas vs vote à la majorité (n\'importe lesquels parmi la liste).',
    },
    back: {
      answer: '- **`FIRST num (...)` (Priorité ordonnée)** : Les transactions attendent les `num` premiers réplicas connectés **dans l\'ordre strict de la liste**. Dans l\'exemple A, `node1` et `node2` sont prioritaires. `node3` est un réplica de secours passif et ne devient synchrone que si `node1` ou `node2` tombe en panne.\n- **`ANY num (...)` (Quorum)** : Les transactions attendent la confirmation de **n\'importe quels `num` réplicas parmi la liste**. Le commit est validé dès que les 2 plus rapides ont répondu, indépendamment de leur position dans la liste.',
      explanation: 'Le mode `ANY` (quorum-based) est idéal dans le Cloud ou les architectures distribuées car il élimine les ralentissements causés par un nœud unique temporairement congestionné.',
      examTrap: 'Si vous écrivez simplement `synchronous_standby_names = \'node1, node2\'` sans mot-clé, PostgreSQL applique implicitement `FIRST 1 (node1, node2)`.',
      ruleRef: 'PostgreSQL 16 Documentation - Syntax of synchronous_standby_names',
    },
  },
  {
    id: 'fc-pgedb-dom05-089',
    cardNumber: 89,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Les 5 niveaux de synchronisation de `synchronous_commit`',
    difficulty: 'hard',
    tags: ['synchronous_commit', 'remote_apply', 'remote_write', 'on', 'local', 'off'],
    front: {
      question: 'Quels sont les 5 niveaux progressifs du paramètre `synchronous_commit` et que garantit le niveau le plus élevé `remote_apply` ?',
      hint: 'off, local, remote_write, on, remote_apply.',
    },
    back: {
      answer: 'Les 5 niveaux ordonnés du moins strict au plus strict sont :\n\n1. **`off`** : Le commit n\'attend même pas le fsync sur le primaire (asynchrone local).\n2. **`local`** : Attend le fsync sur le disque du primaire uniquement.\n3. **`remote_write`** : Attend que le standby ait reçu et écrit le WAL dans son OS (sans fsync sur le standby).\n4. **`on`** (Défaut) : Attend que le standby ait persisté le WAL sur son disque physique (`fsync` / `flush_lsn`).\n5. **`remote_apply`** : Attend que le standby ait **complètement rejoué et appliqué la transaction dans ses pages de données**. Garantit la cohérence en lecture immédiate (*Read-Your-Writes consistency*) sur le standby.',
      explanation: 'Avec `remote_apply`, un client qui insère sur le primaire puis interroge immédiatement le standby verra à coup sûr sa modification sans aucun délai de réplication visible.',
      examTrap: 'Ce paramètre peut être modifié au niveau de chaque session ou transaction (`SET LOCAL synchronous_commit = off;`) pour accélérer des chargements par lots non critiques.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 20: synchronous_commit',
    },
  },
  {
    id: 'fc-pgedb-dom05-090',
    cardNumber: 90,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Panne de standby en réplication synchrone et blocage des écritures',
    difficulty: 'medium',
    tags: ['Synchronous Replication', 'Failure Scenario', 'Hang', 'High Availability'],
    front: {
      question: 'Que se passe-t-il sur le serveur primaire en réplication synchrone (`synchronous_standby_names = "node1"`) si `node1` crash ou subit une coupure réseau ?',
      hint: 'Les requêtes en lecture fonctionnent toujours, mais que deviennent les COMMIT des transactions en écriture ?',
    },
    back: {
      answer: 'Si le seul standby synchrone requis tombe en panne :\n\n1. **Les requêtes en lecture seule (`SELECT`)** continuent de s\'exécuter normalement sur le primaire sans aucun blocage.\n2. **Toute transaction modifiant des données (`INSERT`, `UPDATE`, `DELETE`)** s\'exécute jusqu\'au `COMMIT`, puis **se fige indéfiniment** en attente de confirmation réseau (`wait_event = \'SyncRep\'`).\n3. L\'application cliente se retrouve complètement bloquée en écriture.',
      explanation: 'C\'est le compromis fondamental de la réplication synchrone : elle privilégie la cohérence absolue des données au détriment de la disponibilité en cas de panne unique.',
      examTrap: 'Pour débloquer la situation sans réparer le réplica, le DBA doit temporairement désactiver la réplication synchrone sur le primaire avec `synchronous_standby_names = \'\'` suivi de `pg_reload_conf()`.',
      ruleRef: 'PostgreSQL 16 Documentation - Caveats of Synchronous Replication',
    },
  },

  // =========================================================================
  // SECTION 10 : NOUVEAUTÉS PG16, OUTILS ENTERPRISE EDB & HA (Cartes 91 à 100)
  // Décodage logique sur standby (PG16), pg_rewind, BART, EFM, PGD/BDR
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-091',
    cardNumber: 91,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Nouveauté majeure PostgreSQL 16 : Décodage logique sur les Standbys',
    difficulty: 'hard',
    tags: ['Postgres 16', 'Logical Replication', 'Standby', 'CDC', 'Debezium'],
    front: {
      question: 'Quelle fonctionnalité révolutionnaire concernant la réplication logique a été introduite dans PostgreSQL 16 sur les serveurs secondaires (Standby) ?',
      hint: 'Créer des slots de réplication logique et exporter des flux de données depuis un standby en lecture.',
    },
    back: {
      answer: 'PostgreSQL 16 permet de **créer des slots de réplication logique et d\'exécuter le décodage logique directement sur un serveur Standby physique** (*Logical decoding on Standbys*).\n\nDans toutes les versions précédentes (PG 15 et antérieures), le décodage logique était strictement réservé au serveur Primaire.',
      explanation: 'Cela permet de brancher des outils de Change Data Capture (CDC comme Debezium, Kafka) ou des flux de réplication logique analytiques sur un réplica secondaire, déchargeant ainsi totalement le serveur primaire de ce surcoût CPU.',
      examTrap: 'Sur le primaire, le paramètre `wal_level` doit être configuré à `logical` pour que le flux WAL contienne les informations nécessaires répliquées vers le standby.',
      ruleRef: 'PostgreSQL 16 Documentation - Release Notes: Logical Decoding on Standbys',
    },
  },
  {
    id: 'fc-pgedb-dom05-092',
    cardNumber: 92,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Rôles prédéfinis de sécurité pour la sauvegarde : `pg_read_all_data`',
    difficulty: 'medium',
    tags: ['Security', 'pg_read_all_data', 'Privileges', 'Least Privilege'],
    front: {
      question: 'Quel rôle système prédéfini permet à un compte de sauvegarde dédié d\'exécuter `pg_dump` sur toutes les tables sans nécessiter les privilèges superutilisateur (`SUPERUSER`) ?',
      codeSnippet: 'GRANT pg_read_all_data TO backup_user;',
      hint: 'Rôle système en lecture globale introduit dans les versions modernes.',
    },
    back: {
      answer: 'Le rôle système prédéfini est **`pg_read_all_data`**.\n\nIl accorde automatiquement les droits de lecture (`SELECT`) sur toutes les tables, vues et schémas du cluster, ainsi que sur toutes les futures tables créées ultérieurement, sans avoir à être `SUPERUSER`.',
      explanation: 'Respecte le principe de sécurité du moindre privilège (*Least Privilege*), évitant qu\'un compte de sauvegarde automatisé ne possède les droits destructeurs de superutilisateur.',
      examTrap: 'Bien que `pg_read_all_data` suffise pour `pg_dump`, pour exécuter `pg_dumpall -g` (sauvegarder les rôles et tablespaces), les droits `SUPERUSER` restent indispensables.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22: Predefined Roles (pg_read_all_data)',
    },
  },
  {
    id: 'fc-pgedb-dom05-093',
    cardNumber: 93,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Resynchronisation rapide après failover : utilitaire `pg_rewind`',
    difficulty: 'hard',
    tags: ['pg_rewind', 'Failover', 'High Availability', 'Resynchronization'],
    front: {
      question: 'Quel est le rôle de l\'utilitaire `pg_rewind` et quel énorme gain de temps apporte-t-il après un basculement (Failover) ?',
      codeSnippet: 'pg_rewind --target-pgdata=$PGDATA --source-server=\'host=new_primary dbname=postgres user=postgres\'',
      hint: 'Réaligner un ancien primaire ayant divergé pour le transformer en standby sans tout recopier.',
    },
    back: {
      answer: 'Après un failover, l\'ancien serveur primaire possède des écritures committées localement qui n\'ont jamais été transmises au nouveau primaire : leurs timelines ont divergé.\n\n`pg_rewind` compare les deux clusters à partir du point de divergence, **annule et remplace uniquement les quelques blocs de données ayant divergé**, et réaligne l\'ancien primaire sur la timeline du nouveau primaire en quelques secondes ou minutes.',
      explanation: 'Sans `pg_rewind`, le DBA serait contraint d\'exécuter un `pg_basebackup` complet de plusieurs téraoctets sur le réseau pour reconstruire le réplica.',
      examTrap: '`pg_rewind` nécessite que `wal_log_hints = on` ou que les checksums soient activés (`data_checksums = on`) lors de l\'initdb du cluster.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_rewind',
    },
  },
  {
    id: 'fc-pgedb-dom05-094',
    cardNumber: 94,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Prérequis indispensables pour l\'utilisation de `pg_rewind`',
    difficulty: 'medium',
    tags: ['pg_rewind', 'wal_log_hints', 'data_checksums', 'Configuration'],
    front: {
      question: 'Quels sont les deux prérequis de configuration mutuellement suffisants pour pouvoir utiliser `pg_rewind` ?',
      hint: 'Garder la trace des blocs modifiés même lors des écritures partielles : soit un paramètre dans postgresql.conf, soit une option d\'initdb.',
    },
    back: {
      answer: 'Pour que `pg_rewind` fonctionne, le serveur primaire doit avoir activé soit :\n1. **`wal_log_hints = on`** dans son `postgresql.conf` (nécessite un redémarrage pour activation).\nOU\n2. Les sommes de contrôle de données **`data_checksums = on`** (activées à la création du cluster avec `initdb -k` ou via `pg_checksums`).',
      explanation: '`pg_rewind` a besoin d\'inspecter les pages enregistrées dans les WAL pour savoir quels blocs ont été modifiés entre le point de bifurcation et l\'arrêt de l\'instance.',
      examTrap: 'Si aucun de ces deux réglages n\'est activé, `pg_rewind` refusera de s\'exécuter avec l\'erreur "target server needs to use either data checksums, or wal_log_hints".',
      ruleRef: 'PostgreSQL 16 Documentation - pg_rewind Prerequisites',
    },
  },
  {
    id: 'fc-pgedb-dom05-095',
    cardNumber: 95,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Outil EnterpriseDB : EDB Backup and Recovery Tool (BART)',
    difficulty: 'medium',
    tags: ['EDB', 'BART', 'Enterprise Tools', 'Block-Level Incremental'],
    front: {
      question: 'Qu\'est-ce que l\'outil propriétaire EnterpriseDB **BART (Backup and Recovery Tool)** et quelles fonctionnalités avancées apporte-t-il ?',
      hint: 'Gestion centralisée des catalogues de sauvegarde, sauvegardes incrémentales par blocs et politiques de rétention.',
    },
    back: {
      answer: '**EDB BART** est un outil de niveau entreprise développé par EnterpriseDB pour la gestion centralisée des sauvegardes physiques de parcs de serveurs PostgreSQL et EDB Postgres Advanced Server (EPAS).\n\nFonctionnalités majeures :\n1. **Sauvegardes physiques incrémentales par blocs (Block-level Incremental Backups)** : Ne sauvegarde que les blocs modifiés depuis le dernier backup plein.\n2. **Politiques de rétention automatisées (Retention Policies)** : Suppression programmée basée sur l\'âge ou le nombre de sauvegardes (redondance).\n3. **Catalogue centralisé et audit d\'intégrité des WAL**.',
      explanation: 'BART réduit drastiquement les fenêtres de sauvegarde nocturnes et l\'espace disque consommé sur les volumétries de plusieurs téraoctets.',
      examTrap: 'Dans les environnements cloud et conteneurisés modernes, EnterpriseDB recommande également des solutions comme `Barman` ou `CloudNativePG`.',
      ruleRef: 'EDB Backup and Recovery Tool (BART) Documentation',
    },
  },
  {
    id: 'fc-pgedb-dom05-096',
    cardNumber: 96,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Haute Disponibilité EnterpriseDB : EDB Failover Manager (EFM)',
    difficulty: 'medium',
    tags: ['EDB', 'EFM', 'Failover Manager', 'High Availability', 'Quorum'],
    front: {
      question: 'Quel est le rôle de **EDB Failover Manager (EFM)** dans une architecture PostgreSQL de haute disponibilité ?',
      hint: 'Surveillance par agents, détection de panne par quorum, promotion automatique et isolation (fencing).',
    },
    back: {
      answer: '**EDB Failover Manager (EFM)** est une solution logicielle en cluster qui surveille en continu la santé des nœuds primaire et standbys pour automatiser le basculement sans intervention humaine.\n\nMissions principales :\n1. **Détection rapide des pannes** du primaire via un réseau d\'agents légers et d\'un nœud témoin (*Witness*).\n2. **Élection et promotion automatique** du standby le plus à jour en tant que nouveau primaire.\n3. **Prévention du Split-Brain et Fencing** : Isole l\'ancien primaire pour s\'assurer qu\'il ne continue pas à accepter des écritures concurrentes.\n4. **Reconfiguration automatique des VIP** (adresses IP virtuelles).',
      explanation: 'EFM utilise un algorithme de quorum pour éviter les faux basculements en cas de micro-coupure réseau temporaire.',
      examTrap: 'EFM requiert au minimum 3 nœuds (ex: Primaire, Standby, Témoin/Witness) pour garantir la majorité du quorum et éviter le redouté split-brain.',
      ruleRef: 'EDB Postgres Failover Manager (EFM) Guide',
    },
  },
  {
    id: 'fc-pgedb-dom05-097',
    cardNumber: 97,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Architecture Multi-Maître EDB : Postgres Distributed (PGD / BDR)',
    difficulty: 'hard',
    tags: ['EDB', 'PGD', 'BDR', 'Multi-Master', 'Active-Active'],
    front: {
      question: 'Qu\'est-ce que la technologie **EDB Postgres Distributed (PGD / anciennement BDR)** et en quoi se distingue-t-elle de la réplication streaming standard ?',
      hint: 'Réplication multi-maître active-active multirégionale avec résolution de conflits.',
    },
    back: {
      answer: '**EDB Postgres Distributed (PGD / BDR)** est une solution d\'entreprise de **réplication logique multi-maître active-active** multi-régions.\n\nDifférences avec la réplication streaming standard :\n- En réplication streaming, un seul nœud est en écriture (actif/passif).\n- Avec PGD/BDR, **tous les nœuds du cluster peuvent accepter des écritures simultanées en lecture/écriture** (`INSERT`, `UPDATE`), situés sur différents continents ou datacenters.\n- Il intègre des moteurs sophistiqués de détection et résolution automatique de conflits de données (ex: *Last-Update-Wins* ou colonnes de résolution personnalisées) et des mécanismes de consensus Raft.',
      explanation: 'PGD permet d\'atteindre une disponibilité "cinq 9" (99,999%) avec un temps d\'indisponibilité quasi nul lors des maintenances de version majeure.',
      examTrap: 'BDR/PGD est une technologie propriétaire EnterpriseDB sous licence d\'entreprise, et non un composant de PostgreSQL communautaire.',
      ruleRef: 'EDB Postgres Distributed (PGD) Overview',
    },
  },
  {
    id: 'fc-pgedb-dom05-098',
    cardNumber: 98,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Gestion des sommes de contrôle de blocs : `pg_checksums`',
    difficulty: 'medium',
    tags: ['Checksums', 'Corruption', 'pg_checksums', 'Data Integrity'],
    front: {
      question: 'À quoi servent les sommes de contrôle de blocs de données (Data Checksums) et comment les activer sur un cluster existant sans recréer la base ?',
      hint: 'Détection des corruptions physiques silencieuses sur le disque (bit rot) via l\'utilitaire offline pg_checksums.',
    },
    back: {
      answer: 'Les **Data Checksums** calculent une empreinte de hachage sur chaque bloc de données de 8 Ko lors de l\'écriture disque et la vérifient à chaque lecture pour détecter immédiatement les corruptions matérielles silencieuses (*Bit rot* ou panne de contrôleur).\n\nPour les activer sur un cluster existant :\n1. Arrêter impérativement le cluster (`pg_ctl stop`).\n2. Exécuter l\'utilitaire en ligne de commande :\n```bash\npg_checksums -e -D /var/lib/postgresql/16/main\n```\n3. Redémarrer le cluster.',
      explanation: 'Si un bloc est corrompu, PostgreSQL rejette la lecture avec l\'erreur `invalid page in block ...; checksum verification failed`, évitant la propagation de données fausses dans les sauvegardes.',
      examTrap: '`pg_checksums` ne peut être exécuté qu\'à froid : tenter de l\'exécuter pendant que le serveur tourne provoquera une erreur immédiate.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_checksums',
    },
  },
  {
    id: 'fc-pgedb-dom05-099',
    cardNumber: 99,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Validation et test périodique des sauvegardes (Disaster Recovery Testing)',
    difficulty: 'medium',
    tags: ['Disaster Recovery', 'Best Practices', 'Testing', 'RTO', 'RPO'],
    front: {
      question: 'Quelle est la règle d\'or professionnelle concernant la fiabilité d\'un plan de sauvegarde PostgreSQL en entreprise ?',
      hint: '"Une sauvegarde non testée n\'est pas une sauvegarde, c\'est un vœu pieux."',
    },
    back: {
      answer: '**"Une sauvegarde dont la restauration n\'a jamais été testée avec succès n\'est pas une sauvegarde valide."**\n\nPratiques industrielles indispensables :\n1. Automatiser régulièrement des **restaurations tests sur un environnement isolé** (serveur de test/staging) avec vérification de cohérence applicative.\n2. Mesurer régulièrement le **RTO (Recovery Time Objective)** réel : combien d\'heures prend le rejeu des WAL sur une base de 5 To ?\n3. Surveiller l\'espace de stockage des archives WAL avec des seuils d\'alerte proactifs à 70% et 85% d\'occupation.',
      explanation: 'Des incidents fréquents surviennent lorsqu\'une archive WAL manque ou qu\'un mot de passe de chiffrement a été perdu, ce qu\'un test de restauration périodique détecte immédiatement.',
      examTrap: 'Se fier uniquement au code retour 0 du script de dump sans vérifier l\'intégrité des fichiers ou la complétude des WAL mène aux catastrophes lors des sinistres majeurs.',
      ruleRef: 'EnterpriseDB Backup Strategy Best Practices',
    },
  },
  {
    id: 'fc-pgedb-dom05-100',
    cardNumber: 100,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Matrice de décision de sauvegarde : Logique vs Physique vs Streaming',
    difficulty: 'medium',
    tags: ['Summary', 'Decision Matrix', 'pg_dump', 'pg_basebackup', 'Certification'],
    front: {
      question: 'Résumez dans une matrice synthétique quel outil PostgreSQL choisir selon l\'objectif recherché : migration de version majeure, RPO=0, reprise sur incident en quelques minutes, et sauvegarde sélective.',
      hint: 'pg_dump/pg_restore vs pg_basebackup/PITR vs Streaming Replication.',
    },
    back: {
      answer: 'Matrice de décision officielle EDB / PostgreSQL :\n\n- **Sauvegarde sélective (une table / un schéma) ou archivage historique** : `pg_dump` (format `-Fc` custom).\n- **Migration vers une version majeure supérieure (ex: PG 15 -> PG 16)** : `pg_dump / pg_restore` ou `pg_upgrade`.\n- **Protection contre les sinistres matériels et restauration PITR à la seconde** : `pg_basebackup` + Archivage continu des WAL (`archive_command` / `archive_library`).\n- **Haute disponibilité, basculement en quelques secondes et RPO proche de zéro** : `Streaming Replication` (physique) avec Replication Slots + EDB Failover Manager (EFM).',
      explanation: 'Une politique d\'entreprise mature combine ces trois couches de manière complémentaire.',
      examTrap: 'Une réplication streaming ne remplace JAMAIS une sauvegarde : si un développeur tape par erreur `DROP TABLE clients;`, la commande est répliquée en temps réel sur tous les Standbys en quelques millisecondes ! Seul le PITR permet de récupérer la table.',
      ruleRef: 'EDB PostgreSQL Associate Study Guide - Domain 5 Synthesis',
    },
  },
];
