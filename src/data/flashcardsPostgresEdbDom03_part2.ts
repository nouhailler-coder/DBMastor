import { FlashcardItem } from '../types';

export const postgresEdbDom03Part2Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 6 : PRIVILÈGES SQL SUR LES OBJETS & COMMANDES GRANT / REVOKE (Cartes 51 à 60)
  // SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER, USAGE, \dp, ACL
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-051',
    cardNumber: 51,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Privilèges SQL sur les tables et syntaxe `GRANT`',
    difficulty: 'easy',
    tags: ['GRANT', 'Privileges', 'SELECT', 'INSERT', 'UPDATE', 'DELETE', 'TRUNCATE'],
    front: {
      question: 'Quels sont les différents privilèges SQL applicables directement sur les tables d\'une base de données PostgreSQL ?',
      codeSnippet: 'GRANT SELECT, INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER ON TABLE clients TO app_user;',
      hint: 'Il existe 7 privilèges majeurs sur les tables : 4 DML standards + 3 opérations structurelles/contrôles.',
    },
    back: {
      answer: 'Les privilèges applicables aux tables sont :\n1. `SELECT` : Permet la lecture des données (`SELECT`, y compris pour `COPY TO`).\n2. `INSERT` : Permet l\'insertion de nouvelles lignes (`INSERT`, `COPY FROM`).\n3. `UPDATE` : Permet la mise à jour des colonnes existantes.\n4. `DELETE` : Permet la suppression de lignes.\n5. `TRUNCATE` : Permet de vider rapidement l\'intégralité de la table.\n6. `REFERENCES` : Permet de créer une contrainte de clé étrangère référençant cette table (nécessite de vérifier les valeurs de la clé primaire).\n7. `TRIGGER` : Permet de créer des triggers sur la table.',
      explanation: 'Pour accorder tous ces privilèges en bloc, on utilise le raccourci `GRANT ALL PRIVILEGES ON TABLE nom TO role;`.',
      examTrap: 'Le privilège `TRUNCATE` n\'est PAS inclus dans `DELETE`. Un utilisateur ayant `GRANT DELETE` ne pourra pas exécuter `TRUNCATE TABLE` !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Table Privileges)',
    },
  },
  {
    id: 'fc-pgedb-dom03-052',
    cardNumber: 52,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Privilèges granulaires au niveau des colonnes',
    difficulty: 'medium',
    tags: ['Column Privileges', 'Granular Security', 'SELECT (columns)', 'UPDATE (columns)'],
    front: {
      question: 'Comment accorder un droit d\'accès uniquement sur certaines colonnes spécifiques d\'une table sans exposer les colonnes sensibles ?',
      codeSnippet: 'GRANT SELECT (id, nom, ville) ON TABLE clients TO stagiaire;\nGRANT UPDATE (telephone, email) ON TABLE clients TO support_user;',
      hint: 'On spécifie la liste des colonnes autorisées entre parenthèses juste après le privilège ciblé.',
    },
    back: {
      answer: 'PostgreSQL supporte nativement les privilèges au niveau colonne (Column-Level Security) pour `SELECT`, `INSERT`, `UPDATE` et `REFERENCES` :\n\nSyntaxe :\n```sql\nGRANT SELECT (colonne1, colonne2) ON TABLE ma_table TO mon_role;\n```\nEffets :\n- Le rôle peut exécuter `SELECT id, nom, ville FROM clients;` avec succès.\n- S\'il tente `SELECT salaire FROM clients;` ou `SELECT * FROM clients;`, PostgreSQL renvoie immédiatement "permission denied for table clients".\n- Permet de masquer facilement des données hautement confidentielles (RIB, mots de passe, numéros de sécurité sociale) sans recourir à des vues.',
      explanation: 'Notez que `DELETE` et `TRUNCATE` ne supportent pas de spécification au niveau colonne car ils affectent la ligne ou la relation entière.',
      examTrap: 'Exécuter `SELECT *` échoue dès lors que l\'utilisateur n\'a pas les droits sur l\'intégralité des colonnes de la table.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.1: Database Roles & Column Privileges',
    },
  },
  {
    id: 'fc-pgedb-dom03-053',
    cardNumber: 53,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Privilèges sur les schémas : `USAGE` vs `CREATE`',
    difficulty: 'medium',
    tags: ['USAGE', 'CREATE', 'Schema Security', 'Permissions Hierarchy'],
    front: {
      question: 'Quelle est la différence fondamentale entre les privilèges `USAGE` et `CREATE` accordés sur un schéma PostgreSQL ?',
      codeSnippet: 'GRANT USAGE ON SCHEMA rh TO rh_user;\nGRANT CREATE ON SCHEMA rh TO rh_admin;',
      hint: 'L\'un permet de traverser le schéma pour accéder aux tables qu\'il contient, l\'autre permet d\'y créer de nouveaux objets.',
    },
    back: {
      answer: '1. `USAGE` sur un schéma :\n- Autorise l\'utilisateur à "parcourir" le schéma et à accéder aux objets qu\'il contient (tables, vues, fonctions, types), sous réserve qu\'il dispose également des privilèges sur ces objets individuels.\n- Sans `USAGE` sur le schéma, même si vous avez `GRANT ALL` sur une table `rh.salaires`, toute requête déclenchera : "permission denied for schema rh" !\n\n2. `CREATE` sur un schéma :\n- Autorise l\'utilisateur à créer de nouveaux objets au sein de ce schéma (`CREATE TABLE`, `CREATE VIEW`, `CREATE SEQUENCE`, etc.).',
      explanation: 'Le privilège `USAGE` sur un schéma est le prérequis incontournable pour que n\'importe quel droit sur une table puisse s\'exercer.',
      examTrap: 'Avoir `USAGE` sur un schéma ne confère aucun droit de lecture automatique sur les tables qu\'il contient : les deux niveaux de permissions sont strictement complémentaires.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Schema)',
    },
  },
  {
    id: 'fc-pgedb-dom03-054',
    cardNumber: 54,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Privilèges sur les séquences : `USAGE`, `SELECT`, `UPDATE`',
    difficulty: 'medium',
    tags: ['Sequences', 'USAGE', 'nextval', 'currval', 'setval'],
    front: {
      question: 'Quels privilèges sont requis pour qu\'un utilisateur puisse consommer les valeurs d\'une séquence via `nextval()` ou réinitialiser sa valeur avec `setval()` ?',
      codeSnippet: 'GRANT USAGE ON SEQUENCE commande_id_seq TO app_user;\nGRANT UPDATE ON SEQUENCE commande_id_seq TO dba_user;',
      hint: 'nextval() exige le privilège USAGE (ou SELECT), tandis que setval() exige UPDATE.',
    },
    back: {
      answer: 'Sur les objets de type séquence (`SEQUENCE`) :\n1. `USAGE` : Permet d\'invoquer les fonctions de consommation `nextval()` et `currval()`. C\'est le privilège indispensable pour qu\'un utilisateur puisse insérer des lignes dans une table utilisant une clé auto-incrémentée (`SERIAL` ou `GENERATED ALWAYS AS IDENTITY`).\n2. `SELECT` : Permet de lire la valeur courante et les métadonnées de la séquence avec `currval()` ou `SELECT * FROM ma_sequence;` sans incrémenter le compteur.\n3. `UPDATE` : Permet de modifier la valeur de la séquence avec la fonction `setval()` ou de modifier ses propriétés via `ALTER SEQUENCE`.',
      explanation: 'Si un utilisateur reçoit un droit `INSERT` sur une table mais n\'a pas `USAGE` sur la séquence associée à l\'ID, son insertion échouera lors de l\'évaluation du DEFAULT.',
      examTrap: 'Oublier d\'accorder `USAGE` sur la séquence liée à une clé primaire génère une erreur "permission denied for sequence ...".',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Sequence Privileges)',
    },
  },
  {
    id: 'fc-pgedb-dom03-055',
    cardNumber: 55,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Privilèges sur les bases de données : `CONNECT`, `CREATE`, `TEMPORARY`',
    difficulty: 'medium',
    tags: ['CONNECT', 'CREATE DATABASE', 'TEMPORARY', 'Database Level Privileges'],
    front: {
      question: 'Quels sont les privilèges applicables au niveau de l\'objet base de données (`DATABASE`) et que permet `TEMPORARY` ?',
      codeSnippet: 'GRANT CONNECT, TEMPORARY ON DATABASE prod_db TO dev_user;\nGRANT CREATE ON DATABASE dev_db TO dev_lead;',
      hint: 'CONNECT autorise l\'établissement de session, TEMPORARY autorise la création de tables temporaires.',
    },
    back: {
      answer: 'Les trois privilèges applicables au niveau d\'une base de données sont :\n1. `CONNECT` : Autorise le rôle à ouvrir une session dans la base. Par défaut, ce privilège est accordé au pseudo-rôle `PUBLIC`.\n2. `CREATE` : Autorise le rôle à créer de nouveaux schémas (`CREATE SCHEMA`) ou des souscriptions à l\'intérieur de cette base.\n3. `TEMPORARY` (ou `TEMP`) : Autorise la création de tables temporaires (`CREATE TEMPORARY TABLE`) au cours de la session.\n\nPourquoi `TEMPORARY` est crucial :\nLes tables temporaires sont stockées dans un schéma système éphémère (`pg_temp_nn`). Sans ce privilège, les requêtes utilisant des tables de travail temporaires ou des clauses CTE matérialisées sur disque peuvent être refusées.',
      explanation: 'Pour isoler une base sensible : `REVOKE CONNECT ON DATABASE db_name FROM PUBLIC;` puis `GRANT CONNECT ON DATABASE db_name TO authorized_group;`.',
      examTrap: 'Avoir `CONNECT` sur une base ne donne aucun droit automatique sur les tables : cela ouvre simplement la porte d\'entrée de la base.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Database Privileges)',
    },
  },
  {
    id: 'fc-pgedb-dom03-056',
    cardNumber: 56,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Privilège d\'exécution de code : `EXECUTE` sur les fonctions',
    difficulty: 'easy',
    tags: ['EXECUTE', 'Functions', 'Procedures', 'Code Privileges'],
    front: {
      question: 'Quel privilège contrôle la capacité d\'un utilisateur à appeler une fonction ou une procédure stockée SQL / PL/pgSQL ?',
      codeSnippet: 'GRANT EXECUTE ON FUNCTION calculer_tva(numeric) TO comptable;\nREVOKE EXECUTE ON ALL FUNCTIONS IN SCHEMA public FROM PUBLIC;',
      hint: 'C\'est le mot-clé EXECUTE.',
    },
    back: {
      answer: 'Le privilège est **`EXECUTE`**.\n\nFonctionnement :\n- Il autorise l\'appel de la fonction dans une clause `SELECT`, un trigger, ou via l\'instruction `CALL` pour une procédure stockée.\n\nAttention historique fondamentale :\n- Par défaut dans PostgreSQL, lors de la création d\'une fonction dans le schéma `public`, le privilège `EXECUTE` est automatiquement accordé au pseudo-rôle `PUBLIC` !\n- Pour sécuriser une fonction sensible, il faut impérativement révoquer ce droit : `REVOKE EXECUTE ON FUNCTION ma_fonction(...) FROM PUBLIC;` puis l\'accorder nominativement aux seuls rôles autorisés.',
      explanation: 'Combiné avec des fonctions en `SECURITY DEFINER`, `EXECUTE` permet de donner un accès contrôlé à des opérations sans exposer les tables brutes sous-jacentes.',
      examTrap: 'Lorsque vous référencez une fonction dans un `GRANT/REVOKE`, vous devez spécifier la signature exacte de ses arguments (ex: `calculer_tva(numeric)`).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Function Privileges)',
    },
  },
  {
    id: 'fc-pgedb-dom03-057',
    cardNumber: 57,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Délégation de privilèges sur les objets avec `WITH GRANT OPTION`',
    difficulty: 'medium',
    tags: ['WITH GRANT OPTION', 'Delegation', 'Object Security'],
    front: {
      question: 'À quoi sert la clause `WITH GRANT OPTION` lors de l\'octroi d\'un privilège sur une table ou un objet ?',
      codeSnippet: 'GRANT SELECT, UPDATE ON TABLE contrats TO lead_juridique WITH GRANT OPTION;',
      hint: 'Elle autorise le bénéficiaire à transmettre ce privilège à son tour à d\'autres utilisateurs.',
    },
    back: {
      answer: 'La clause `WITH GRANT OPTION` autorise l\'utilisateur bénéficiaire (`lead_juridique`) à :\n1. Transmettre à son tour les privilèges reçus (`SELECT`, `UPDATE`) à n\'importe quel autre rôle du cluster (`GRANT SELECT ON contrats TO juriste_junior;`).\n2. Révoquer les privilèges qu\'il a lui-même accordés.\n\nDifférence de terminologie essentielle :\n- `WITH GRANT OPTION` s\'utilise pour les privilèges sur les OBJETS de données (tables, vues, schémas, fonctions).\n- `WITH ADMIN OPTION` s\'utilise pour la gestion d\'appartenance aux RÔLES (`GRANT groupe TO role`).',
      explanation: 'Si le propriétaire révoque le droit avec l\'option CASCADE, tous les sous-privilèges accordés par délégation sont automatiquement révoqués.',
      examTrap: 'Vous ne pouvez pas accorder `WITH GRANT OPTION` au pseudo-rôle `PUBLIC`.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (WITH GRANT OPTION)',
    },
  },
  {
    id: 'fc-pgedb-dom03-058',
    cardNumber: 58,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Révocation en cascade : `CASCADE` vs `RESTRICT`',
    difficulty: 'hard',
    tags: ['REVOKE', 'CASCADE', 'RESTRICT', 'Privilege Revocation'],
    front: {
      question: 'Que se passe-t-il lorsque vous tentez de révoquer un privilège détenu `WITH GRANT OPTION` avec `RESTRICT` (défaut) vs `CASCADE` ?',
      codeSnippet: 'REVOKE SELECT ON contrats FROM lead_juridique RESTRICT; -- Échoue si lead_juridique a fait des GRANT !\nREVOKE SELECT ON contrats FROM lead_juridique CASCADE;',
      hint: 'CASCADE révoque récursivement tous les droits en chaîne accordés par cette personne.',
    },
    back: {
      answer: '1. `RESTRICT` (comportement par défaut du standard SQL) :\n- Si l\'utilisateur ciblé a lui-même transmis ce privilège à d\'autres rôles via `WITH GRANT OPTION`, la commande REVOKE est immédiatement REFUSÉE avec une erreur pour éviter de casser la chaîne de dépendances à votre insu.\n\n2. `CASCADE` :\n- Révoque le privilège chez l\'utilisateur ciblé ET révoque récursivement tous les privilèges accordés en aval par cet utilisateur (et par ceux à qui il les avait accordés) !\n\nUtilisation typique : Départ d\'un collaborateur ou d\'un sous-administrateur nécessitant de nettoyer l\'ensemble de son arbre de délégation.',
      explanation: 'La clause `CASCADE` garantit qu\'aucun privilège "orphelin" ne subsiste suite à la déchéance du délégataire.',
      examTrap: 'Une révocation avec `CASCADE` peut couper les accès d\'applications tierces si elles avaient reçu leurs privilèges du compte révoqué !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: REVOKE (CASCADE/RESTRICT)',
    },
  },
  {
    id: 'fc-pgedb-dom03-059',
    cardNumber: 59,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Inspection des privilèges dans psql : `\\dp` et `\\z`',
    difficulty: 'easy',
    tags: ['\\dp', '\\z', 'psql', 'ACL Inspection'],
    front: {
      question: 'Quelles méta-commandes `psql` permettent d\'afficher la matrice des privilèges d\'accès (ACL) accordés sur les tables et vues ?',
      codeSnippet: '\\dp clients\n\\z public.*',
      hint: 'Deux méta-commandes strictement synonymes dans psql : \\dp et \\z.',
    },
    back: {
      answer: 'Les méta-commandes `\\dp` (Display Privileges) et `\\z` (synonyme historique) affichent la liste des tables, vues et séquences avec :\n- Le schéma (`Schema`)\n- Le nom de la relation (`Name`)\n- Le type (`Type` : table, view, sequence)\n- Les droits d\'accès (`Access privileges`)\n- Les privilèges sur les colonnes (`Column privileges`)\n- Les politiques de sécurité de lignes (`Policies`)\n\nPermet au DBA d\'inspecter instantanément en interactif quels rôles disposent de quels droits sur un objet.',
      explanation: 'Vous pouvez restreindre la recherche par motif (ex: `\\dp rh.*`).',
      examTrap: 'Si la colonne `Access privileges` est vide, cela ne signifie pas que personne n\'a accès : cela signifie que les privilèges par défaut du propriétaire et de `PUBLIC` s\'appliquent.',
      ruleRef: 'PostgreSQL 16 Documentation - PostgreSQL Client Applications: psql',
    },
  },
  {
    id: 'fc-pgedb-dom03-060',
    cardNumber: 60,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Décodage de la syntaxe des ACL PostgreSQL (`arwdDxt/grantor`)',
    difficulty: 'hard',
    tags: ['ACL', 'Access Control Lists', 'Privilege Codes', 'arwdDxt'],
    front: {
      question: 'Comment déchiffrer la chaîne de droits ACL suivante affichée par `\\dp` : `app_user=arwdDxt/postgres` ?',
      codeSnippet: 'Access privileges: {postgres=arwdDxt/postgres,app_user=arwd/postgres}',
      hint: 'Chaque lettre correspond à un privilège précis (a: append/insert, r: read/select, w: write/update, d: delete, D: truncate...).',
    },
    back: {
      answer: 'Format d\'une entrée ACL : `role=privilèges/grantor`\n\nDécodage de chaque lettre de privilège :\n- `r` = `SELECT` (read)\n- `a` = `INSERT` (append)\n- `w` = `UPDATE` (write)\n- `d` = `DELETE`\n- `D` = `TRUNCATE` (grand D)\n- `x` = `REFERENCES`\n- `t` = `TRIGGER`\n- `X` = `EXECUTE` (fonctions)\n- `U` = `USAGE` (schémas, séquences, types)\n- `C` = `CREATE` (schémas, tablespaces, bases)\n- `c` = `CONNECT` (bases de données)\n- `T` = `TEMPORARY`\n- `*` = `WITH GRANT OPTION` (ex: `r*` = SELECT avec droit de déléguer)\n\n`/postgres` indique le rôle qui a accordé ce privilège.',
      explanation: 'Ainsi, `app_user=arwd/postgres` signifie que `postgres` a accordé `SELECT`, `INSERT`, `UPDATE` et `DELETE` sur cette table à `app_user`.',
      examTrap: 'Attention à la casse : petit `d` signifie `DELETE`, grand `D` signifie `TRUNCATE` ! Petit `c` signifie `CONNECT`, grand `C` signifie `CREATE`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 5.7: Privileges (Access Control Lists)',
    },
  },

  // =========================================================================
  // SECTION 7 : PRIVILÈGES PAR DÉFAUT (ALTER DEFAULT PRIVILEGES) (Cartes 61 à 70)
  // FOR ROLE, IN SCHEMA, TABLES, SEQUENCES, ROUTINES, TYPES, \ddp
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-061',
    cardNumber: 61,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Le problème fondamental des tables nouvellement créées',
    difficulty: 'medium',
    tags: ['Default Privileges', 'Ownership', 'Security Model', 'Architecture'],
    front: {
      question: 'Pourquoi, après avoir exécuté `GRANT SELECT ON ALL TABLES IN SCHEMA app TO analyste;`, l\'analyste ne peut-il pas lire une nouvelle table créée le lendemain dans ce schéma ?',
      hint: 'GRANT ne s\'applique qu\'aux objets existant au moment exact de l\'exécution de la commande.',
    },
    back: {
      answer: 'Dans le modèle relationnel PostgreSQL, les commandes `GRANT` classiques ont un effet purement ponctuel et rétroactif :\n- `GRANT ... ON ALL TABLES` modifie l\'ACL de toutes les tables qui existent physiquement AU MOMENT de l\'instruction.\n- Toute nouvelle table créée ultérieurement par un développeur appartiendra à ce créateur, et aucun privilège ne sera accordé à l\'analyste par défaut.\n\nPour que les futurs objets créés reçoivent automatiquement des permissions sans intervention humaine manuelle, il faut obligatoirement configurer des **privilèges par défaut** via la commande `ALTER DEFAULT PRIVILEGES`.',
      explanation: 'C\'est l\'une des causes d\'incident les plus récurrentes en production lors des déploiements applicatifs.',
      examTrap: 'Ne confondez pas `GRANT ON ALL TABLES` (objets passés) et `ALTER DEFAULT PRIVILEGES` (objets futurs).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DEFAULT PRIVILEGES',
    },
  },
  {
    id: 'fc-pgedb-dom03-062',
    cardNumber: 62,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Syntaxe de base de `ALTER DEFAULT PRIVILEGES`',
    difficulty: 'medium',
    tags: ['ALTER DEFAULT PRIVILEGES', 'Syntax', 'Automation', 'RBAC'],
    front: {
      question: 'Quelle est la syntaxe pour que chaque nouvelle table créée dans le schéma `public` soit automatiquement accessible en lecture au rôle `readonly_group` ?',
      codeSnippet: 'ALTER DEFAULT PRIVILEGES IN SCHEMA public\nGRANT SELECT ON TABLES TO readonly_group;',
      hint: 'Instruction ALTER DEFAULT PRIVILEGES suivie de IN SCHEMA et de la clause GRANT habituelle.',
    },
    back: {
      answer: 'La syntaxe est :\n```sql\nALTER DEFAULT PRIVILEGES IN SCHEMA public\nGRANT SELECT ON TABLES TO readonly_group;\n```\nEffets :\n- Chaque fois que le rôle courant crée une nouvelle table ou vue dans le schéma `public`, PostgreSQL ajoute automatiquement `GRANT SELECT ... TO readonly_group` sur l\'objet créé.',
      explanation: 'Cette directive garantit la pérennité des droits sans nécessiter de cron de réassignation.',
      examTrap: 'Cette règle ne s\'applique QUE si les nouvelles tables sont créées par l\'utilisateur qui a lancé la commande (sauf si la clause `FOR ROLE` est spécifiée).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DEFAULT PRIVILEGES',
    },
  },
  {
    id: 'fc-pgedb-dom03-063',
    cardNumber: 63,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Le piège capital de la clause `FOR ROLE`',
    difficulty: 'hard',
    tags: ['FOR ROLE', 'ALTER DEFAULT PRIVILEGES', 'Critical Trap', 'Exam Must-Know'],
    front: {
      question: 'Pourquoi la commande `ALTER DEFAULT PRIVILEGES GRANT SELECT ON TABLES TO analyste;` lancée par le superutilisateur `postgres` ne s\'applique-t-elle pas lorsque l\'utilisateur `dev_user` crée une table ?',
      hint: 'Par défaut, ALTER DEFAULT PRIVILEGES ne s\'applique qu\'aux objets créés par l\'utilisateur courant.',
    },
    back: {
      answer: 'Sans clause `FOR ROLE`, la commande n\'affecte que les objets créés par le rôle qui exécute l\'instruction (ici `postgres`). Si `dev_user` crée une table, la règle ne se déclenche pas !\n\nPour que la règle s\'applique aux objets créés par `dev_user`, il faut impérativement spécifier `FOR ROLE` :\n```sql\nALTER DEFAULT PRIVILEGES FOR ROLE dev_user IN SCHEMA public\nGRANT SELECT ON TABLES TO analyste;\n```\nCondition préalable : Le rôle exécutant la commande doit être superutilisateur ou membre du rôle désigné dans `FOR ROLE`.',
      explanation: 'C\'est le piège le plus difficile de la certification EDB sur la gestion des droits.',
      examTrap: 'Retenez bien : Les privilèges par défaut sont attachés au CRÉATEUR des objets (`FOR ROLE`), pas seulement au schéma !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DEFAULT PRIVILEGES',
    },
  },
  {
    id: 'fc-pgedb-dom03-064',
    cardNumber: 64,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Scope par schéma (`IN SCHEMA`) vs Scope global par base',
    difficulty: 'medium',
    tags: ['IN SCHEMA', 'Scope', 'Database Level', 'Partitioning'],
    front: {
      question: 'Que se passe-t-il si vous omettez la clause `IN SCHEMA <nom>` dans une commande `ALTER DEFAULT PRIVILEGES` ?',
      codeSnippet: 'ALTER DEFAULT PRIVILEGES FOR ROLE app_deployer\nGRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO app_user;',
      hint: 'La règle s\'applique alors à tous les schémas de la base de données courante.',
    },
    back: {
      answer: 'Si la clause `IN SCHEMA` est omise :\n- La règle par défaut s\'applique de manière globale à TOUS les schémas existants et futurs de la base de données courante pour les objets créés par ce rôle.\n- Si `app_deployer` crée une table dans `schema1`, `schema2` ou `public`, `app_user` recevra automatiquement les privilèges demandés sur chacune d\'elles.\n\nPratique pour un profil d\'application étendu sur plusieurs schémas.',
      explanation: 'Attention toutefois : `app_user` devra toujours disposer du privilège `USAGE` sur ces schémas respectifs pour pouvoir interroger les tables !',
      examTrap: '`ALTER DEFAULT PRIVILEGES` reste cantonné à la base de données courante : elle n\'affecte pas les autres bases du cluster.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DEFAULT PRIVILEGES',
    },
  },
  {
    id: 'fc-pgedb-dom03-065',
    cardNumber: 65,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Types d\'objets supportés par `ALTER DEFAULT PRIVILEGES`',
    difficulty: 'medium',
    tags: ['TABLES', 'SEQUENCES', 'ROUTINES', 'TYPES', 'SCHEMAS'],
    front: {
      question: 'Quels types d\'objets de base de données peuvent être ciblés par la commande `ALTER DEFAULT PRIVILEGES` ?',
      codeSnippet: 'ALTER DEFAULT PRIVILEGES ... GRANT ... ON [QUEL TYPE ?];',
      hint: 'Il existe 5 familles majeures d\'objets : tables, séquences, fonctions/procédures, types et schémas.',
    },
    back: {
      answer: '`ALTER DEFAULT PRIVILEGES` supporte 5 types d\'objets cibles :\n1. `TABLES` : Concerne les tables, les vues et les tables étrangères (`foreign tables`).\n2. `SEQUENCES` : Concerne les séquences génératrices d\'identifiants.\n3. `FUNCTIONS` / `ROUTINES` : Concerne les fonctions SQL/PL et procédures stockées.\n4. `TYPES` : Concerne les types composites, énumérations (`ENUM`) et domaines.\n5. `SCHEMAS` : Concerne les futurs schémas créés (autorise `USAGE` ou `CREATE` sur les nouveaux schémas).',
      explanation: 'Le mot-clé `ROUTINES` est recommandé car il englobe à la fois les fonctions et les procédures.',
      examTrap: 'On ne peut pas définir de privilèges par défaut sur des index ou des triggers (qui héritent directement de la table mère).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DEFAULT PRIVILEGES',
    },
  },
  {
    id: 'fc-pgedb-dom03-066',
    cardNumber: 66,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Le piège de la non-rétroactivité',
    difficulty: 'easy',
    tags: ['Non-retroactive', 'Best Practice', 'Migration', 'Exam Trap'],
    front: {
      question: 'Si vous configurez `ALTER DEFAULT PRIVILEGES` sur une base existante contenant déjà 50 tables, que devez-vous obligatoirement faire pour les 50 tables déjà présentes ?',
      hint: 'ALTER DEFAULT PRIVILEGES n\'a aucun impact sur le passé.',
    },
    back: {
      answer: 'Vous devez IMPÉRATIVEMENT exécuter la commande `GRANT` classique pour traiter les objets existants !\n\nProcédure standard en deux étapes :\n1. Traiter le stock existant :\n   ```sql\n   GRANT SELECT ON ALL TABLES IN SCHEMA public TO mon_role;\n   GRANT USAGE ON ALL SEQUENCES IN SCHEMA public TO mon_role;\n   ```\n2. Traiter le flux futur :\n   ```sql\n   ALTER DEFAULT PRIVILEGES IN SCHEMA public\n   GRANT SELECT ON TABLES TO mon_role;\n   ALTER DEFAULT PRIVILEGES IN SCHEMA public\n   GRANT USAGE ON SEQUENCES TO mon_role;\n   ```',
      explanation: 'Omettre l\'une des deux étapes crée un décalage : soit les anciennes tables sont inaccessibles, soit les futures le seront.',
      examTrap: '`ALTER DEFAULT PRIVILEGES` ne modifie JAMAIS aucune table déjà existante dans le catalogue.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DEFAULT PRIVILEGES',
    },
  },
  {
    id: 'fc-pgedb-dom03-067',
    cardNumber: 67,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Inspection des privilèges par défaut avec `\\ddp`',
    difficulty: 'easy',
    tags: ['\\ddp', 'psql', 'Audit', 'pg_default_acl'],
    front: {
      question: 'Quelle méta-commande `psql` permet de lister l\'ensemble des privilèges par défaut configurés dans la base courante ?',
      codeSnippet: '\\ddp',
      hint: 'Méta-commande commençant par antislash ddp (Display Default Privileges).',
    },
    back: {
      answer: 'La commande est **`\\ddp`** (Display Default Privileges).\n\nInformations affichées :\n- `Owner` : Le rôle qui crée les objets (cible du `FOR ROLE`).\n- `Schema` : Le schéma ciblé (ou vide si global à la base).\n- `Type` : Le type d\'objet (`table`, `sequence`, `function`, etc.).\n- `Access privileges` : Les permissions accordées automatiquement lors de la création.\n\nEn coulisse, psql interroge la table catalogue `pg_default_acl`.',
      explanation: 'Indispensable pour auditer la configuration avant de livrer un environnement en production.',
      examTrap: 'Si `\\ddp` ne renvoie aucune ligne, cela signifie qu\'aucun privilège par défaut n\'a été personnalisé dans la base.',
      ruleRef: 'PostgreSQL 16 Documentation - PostgreSQL Client Applications: psql',
    },
  },
  {
    id: 'fc-pgedb-dom03-068',
    cardNumber: 68,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Révocation des privilèges par défaut',
    difficulty: 'medium',
    tags: ['REVOKE DEFAULT PRIVILEGES', 'Security Clean-up', 'ALTER DEFAULT PRIVILEGES'],
    front: {
      question: 'Comment annuler une règle de privilèges par défaut précédemment configurée ?',
      codeSnippet: 'ALTER DEFAULT PRIVILEGES IN SCHEMA public\nREVOKE SELECT ON TABLES FROM readonly_group;',
      hint: 'On réutilise la syntaxe ALTER DEFAULT PRIVILEGES en remplaçant la clause GRANT par REVOKE.',
    },
    back: {
      answer: 'Pour supprimer une règle de privilèges par défaut, on remplace le mot-clé `GRANT` par `REVOKE` :\n```sql\nALTER DEFAULT PRIVILEGES FOR ROLE dev_user IN SCHEMA public\nREVOKE SELECT ON TABLES FROM readonly_group;\n```\nEffets :\n- Les futures tables créées par `dev_user` n\'accorderont plus automatiquement le droit `SELECT` à `readonly_group`.\n- Attention : Cela ne retire PAS le droit `SELECT` sur les tables qui ont déjà été créées pendant la période où la règle était active ! Pour les tables déjà créées, il faut exécuter un `REVOKE SELECT ON ALL TABLES ...`.',
      explanation: 'Tout comme pour la création, la révocation par défaut ne rétroagit pas sur le passé.',
      examTrap: 'Penser que `REVOKE` dans `ALTER DEFAULT PRIVILEGES` va nettoyer les droits sur les tables existantes est une erreur classique.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DEFAULT PRIVILEGES',
    },
  },
  {
    id: 'fc-pgedb-dom03-069',
    cardNumber: 69,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Pattern d\'architecture recommandé : Rôle Propriétaire vs Rôle Applicatif',
    difficulty: 'hard',
    tags: ['Security Architecture', 'app_owner vs app_user', 'Best Practice', 'CIS Benchmark'],
    front: {
      question: 'Quel modèle d\'architecture de rôles est recommandé par EnterpriseDB et les benchmarks CIS pour séparer la création DDL des accès DML en production ?',
      hint: 'Séparer un rôle propriétaire NOLOGIN qui possède les tables et un rôle applicatif LOGIN qui consomme les données.',
    },
    back: {
      answer: 'Le pattern recommandé repose sur une stricte dissociation :\n\n1. **Rôle Propriétaire DDL** (`app_owner`) :\n   - Attribut `NOLOGIN`.\n   - Possède le schéma et toutes les tables (`ALTER SCHEMA app OWNER TO app_owner;`).\n   - Déployé par les pipelines de migration (Flyway, Liquibase, Sqitch) via un compte de déploiement temporaire faisant `SET ROLE app_owner`.\n\n2. **Rôle Applicatif DML** (`app_user`) :\n   - Attribut `LOGIN` avec mot de passe fort SCRAM.\n   - N\'est propriétaire d\'aucun objet (ne peut exécuter aucun `DROP TABLE` ni `ALTER TABLE`).\n   - Bénéficie uniquement de `USAGE` sur le schéma, et de `SELECT, INSERT, UPDATE, DELETE` configurés via `ALTER DEFAULT PRIVILEGES FOR ROLE app_owner`.\n\n3. **Rôle Lecture Seule / BI** (`app_readonly`) :\n   - Reçoit uniquement `SELECT` via les privilèges par défaut.',
      explanation: 'Ce modèle empêche une vulnérabilité SQL injection dans l\'application de pouvoir altérer ou supprimer la structure de la base.',
      examTrap: 'Faire tourner l\'application web avec le rôle propriétaire de la base est une grave violation des règles d\'architecture sécurisée.',
      ruleRef: 'EnterpriseDB Security Best Practices & CIS PostgreSQL Benchmark Guide',
    },
  },
  {
    id: 'fc-pgedb-dom03-070',
    cardNumber: 70,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Catalogue sous-jacent : `pg_default_acl`',
    difficulty: 'hard',
    tags: ['pg_default_acl', 'Catalog', 'Internal Architecture'],
    front: {
      question: 'Quelle table du catalogue système PostgreSQL enregistre les règles de privilèges par défaut ?',
      codeSnippet: 'SELECT defaclrole::regrole, defaclnamespace::regnamespace, defaclobjtype, defaclacl\nFROM pg_default_acl;',
      hint: 'Table pg_default_acl reliant le rôle créateur, l\'espace de nom et le type d\'objet.',
    },
    back: {
      answer: 'La table système est **`pg_default_acl`**.\n\nColonnes structurelles :\n- `defaclrole` : OID du rôle pour lequel la règle s\'applique (`FOR ROLE`).\n- `defaclnamespace` : OID du schéma (`IN SCHEMA`), ou 0 si la règle est globale à la base.\n- `defaclobjtype` : Code caractère représentant le type d\'objet :\n  - `r` = relation (table/vue)\n  - `S` = séquence\n  - `f` = fonction\n  - `T` = type\n  - `n` = schéma (namespace)\n- `defaclacl` : Tableau des droits d\'accès (ACL) par défaut injectés à la création.',
      explanation: 'Cette table est locale à chaque base de données.',
      examTrap: 'Si un schéma est supprimé avec `DROP SCHEMA ... CASCADE`, les entrées associées dans `pg_default_acl` sont également nettoyées.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Catalogs - pg_default_acl',
    },
  },

  // =========================================================================
  // SECTION 8 : SÉCURITÉ AU NIVEAU DES LIGNES (ROW-LEVEL SECURITY - RLS) (Cartes 71 à 80)
  // ENABLE/FORCE RLS, CREATE POLICY, USING vs WITH CHECK, PERMISSIVE vs RESTRICTIVE
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-071',
    cardNumber: 71,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Qu\'est-ce que le Row-Level Security (RLS) ?',
    difficulty: 'easy',
    tags: ['RLS', 'Row-Level Security', 'Multi-tenant', 'Data Isolation'],
    front: {
      question: 'Quel est le principe de la sécurité au niveau des lignes (Row-Level Security - RLS) et quel problème d\'architecture résout-elle ?',
      hint: 'Elle permet de filtrer dynamiquement et invisiblement les lignes d\'une table selon l\'utilisateur qui exécute la requête.',
    },
    back: {
      answer: 'Le Row-Level Security (RLS) est un mécanisme de sécurité granulaire au niveau du moteur SQL :\n- Il applique des filtres de sécurité invisibles et incontournables lors de l\'exécution des requêtes SQL (`SELECT`, `INSERT`, `UPDATE`, `DELETE`).\n- Un utilisateur n\'a accès qu\'aux lignes de la table qui satisfont l\'expression booléenne de la politique de sécurité qui lui est appliquée.\n\nProblème résolu :\n- Dans les applications multi-locataires (Multi-tenant SaaS) partageant la même base de données, RLS garantit qu\'un client A ne pourra JAMAIS voir ou modifier les données du client B, même si le code applicatif oublie d\'ajouter `WHERE tenant_id = ...` dans ses requêtes !',
      explanation: 'Le filtrage est effectué au plus profond du moteur d\'exécution, éliminant tout risque de fuite de données par omission logicielle.',
      examTrap: 'Par défaut sur une table PostgreSQL, RLS est DÉSACTIVÉ. Il doit être explicitement activé table par table.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 5.8: Row Security Policies',
    },
  },
  {
    id: 'fc-pgedb-dom03-072',
    cardNumber: 72,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Activation de RLS : `ENABLE` vs `FORCE ROW LEVEL SECURITY`',
    difficulty: 'medium',
    tags: ['ENABLE RLS', 'FORCE RLS', 'Owner Bypass', 'Table Security'],
    front: {
      question: 'Quelle est la différence capitale entre `ENABLE ROW LEVEL SECURITY` et `FORCE ROW LEVEL SECURITY` concernant le propriétaire de la table ?',
      codeSnippet: 'ALTER TABLE documents ENABLE ROW LEVEL SECURITY;\nALTER TABLE documents FORCE ROW LEVEL SECURITY;',
      hint: 'Par défaut avec ENABLE, le propriétaire de la table ignore totalement les filtres RLS. FORCE lui impose le filtre.',
    },
    back: {
      answer: '1. `ENABLE ROW LEVEL SECURITY` :\n- Active le moteur de politiques sur la table pour les utilisateurs ordinaires.\n- MAIS le propriétaire de la table (`owner`) et les `SUPERUSER` contournent totalement RLS et continuent de voir l\'intégralité des lignes !\n\n2. `FORCE ROW LEVEL SECURITY` :\n- Force l\'application stricte des politiques RLS MÊME pour le rôle propriétaire de la table (`table owner`).\n- Seuls les rôles superutilisateurs ou les rôles dotés de l\'attribut `BYPASSRLS` peuvent encore contourner les politiques.',
      explanation: '`FORCE ROW LEVEL SECURITY` est indispensable si l\'application se connecte avec le même compte qui a créé les tables.',
      examTrap: 'Si vous testez vos politiques RLS en étant connecté avec le compte qui a créé la table et sans `FORCE`, vous aurez l\'impression erronée que RLS ne fonctionne pas car vous verrez toutes les lignes !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER TABLE (Row Level Security)',
    },
  },
  {
    id: 'fc-pgedb-dom03-073',
    cardNumber: 73,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Création d\'une politique de sécurité : `CREATE POLICY`',
    difficulty: 'medium',
    tags: ['CREATE POLICY', 'Syntax', 'Row-Level Security', 'RLS Policies'],
    front: {
      question: 'Quelle est la syntaxe pour créer une politique RLS permettant à chaque utilisateur de ne voir que ses propres documents ?',
      codeSnippet: 'CREATE POLICY user_docs_policy ON documents\n  FOR SELECT\n  TO app_users\n  USING (proprietaire_id = current_user);',
      hint: 'Instruction CREATE POLICY ciblant la table, la commande, le rôle et l\'expression USING.',
    },
    back: {
      answer: 'La syntaxe standard est :\n```sql\nCREATE POLICY nom_politique ON nom_table\n  [FOR { ALL | SELECT | INSERT | UPDATE | DELETE }]\n  [TO { nom_role | PUBLIC }]\n  [USING ( expression_booleenne_using )]\n  [WITH CHECK ( expression_booleenne_with_check )];\n```\nDans l\'exemple :\n- Tout membre de `app_users` exécutant `SELECT * FROM documents;` ne verra que les lignes où `proprietaire_id = current_user`.\n- Les autres lignes sont silencieusement filtrées comme si elles n\'existaient pas.',
      explanation: 'Si aucune commande n\'est précisée (`FOR ALL`), la politique s\'applique à toutes les opérations DML.',
      examTrap: 'Si RLS est activé sur une table (`ENABLE RLS`) mais qu\'aucune politique n\'a été créée, le comportement par défaut est le refus total : aucune ligne n\'est visible pour les utilisateurs non-privilégiés (Default Deny) !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: CREATE POLICY',
    },
  },
  {
    id: 'fc-pgedb-dom03-074',
    cardNumber: 74,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Clause `USING` vs Clause `WITH CHECK`',
    difficulty: 'hard',
    tags: ['USING', 'WITH CHECK', 'RLS Clauses', 'INSERT / UPDATE Validation'],
    front: {
      question: 'Quelle est la différence fonctionnelle entre la clause `USING` et la clause `WITH CHECK` dans une commande `CREATE POLICY` ?',
      codeSnippet: 'CREATE POLICY policy_compta ON factures\n  FOR UPDATE\n  USING (agence_id = current_setting(\'app.current_agence\'))\n  WITH CHECK (agence_id = current_setting(\'app.current_agence\') AND montant > 0);',
      hint: 'USING détermine quelles lignes existantes peuvent être lues/modifiées ; WITH CHECK valide les nouvelles lignes créées ou mises à jour.',
    },
    back: {
      answer: '1. Clause `USING` (Filtre de lecture / sélection) :\n- Évaluée sur les lignes EXISTANTES de la table.\n- Détermine si une ligne est visible pour `SELECT`, ou éligible pour être ciblée par `UPDATE` ou `DELETE`.\n- Si la condition est fausse, la ligne est simplement ignorée.\n\n2. Clause `WITH CHECK` (Validation d\'écriture) :\n- Évaluée sur les NOUVELLES lignes insérées (`INSERT`) ou sur le nouveau contenu produit après modification (`UPDATE`).\n- Empêche un utilisateur d\'insérer une ligne qui violerait la politique ou de transférer une ligne vers une autre agence.\n- Si la condition est fausse lors d\'un `INSERT` ou `UPDATE`, PostgreSQL lève une ERREUR explicite "new row violates row-level security policy" !',
      explanation: 'Si `WITH CHECK` n\'est pas spécifiée, la clause `USING` est automatiquement utilisée pour les deux vérifications.',
      examTrap: 'Une politique `FOR INSERT` ne peut comporter qu\'une clause `WITH CHECK` (pas de `USING`, car la ligne n\'existe pas encore dans la table).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 5.8: Row Security Policies',
    },
  },
  {
    id: 'fc-pgedb-dom03-075',
    cardNumber: 75,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Combinaison de politiques : `AS PERMISSIVE` vs `AS RESTRICTIVE`',
    difficulty: 'hard',
    tags: ['AS PERMISSIVE', 'AS RESTRICTIVE', 'Policy Combination', 'Boolean Logic'],
    front: {
      question: 'Comment PostgreSQL combine-t-il plusieurs politiques RLS définies sur la même table avec `AS PERMISSIVE` (défaut) vs `AS RESTRICTIVE` ?',
      codeSnippet: 'CREATE POLICY p1 ON clients AS PERMISSIVE FOR SELECT USING (region = \'Europe\');\nCREATE POLICY p2 ON clients AS PERMISSIVE FOR SELECT USING (vip = true);\nCREATE POLICY p3 ON clients AS RESTRICTIVE FOR SELECT USING (actif = true);',
      hint: 'Les politiques permissives sont combinées par des OR logiques, tandis que les restrictives sont combinées par des AND logiques.',
    },
    back: {
      answer: 'Formule d\'évaluation globale de PostgreSQL :\n```text\n(Permissive_1 OR Permissive_2 OR ...) AND (Restrictive_1 AND Restrictive_2 AND ...)\n```\n\n1. `AS PERMISSIVE` (comportement par défaut) :\n- Les politiques permissives s\'additionnent par l\'opérateur logique **OU (OR)**.\n- Il suffit que la ligne satisfasse AU MOINS UNE des politiques permissives pour être acceptée.\n\n2. `AS RESTRICTIVE` :\n- Les politiques restrictives agissent comme des verrous obligatoires combinés par **ET (AND)**.\n- La ligne DOIT obligatoirement satisfaire TOUTES les politiques restrictives, en plus de satisfaire les politiques permissives.',
      explanation: 'Dans l\'exemple ci-contre, un client sera visible s\'il est actif (`actif = true`) ET qu\'il se trouve en Europe OU qu\'il est VIP.',
      examTrap: 'Si aucune politique permissive n\'est satisfaite, la ligne est rejetée, même si toutes les politiques restrictives sont valides.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 5.8: Row Security Policies',
    },
  },
  {
    id: 'fc-pgedb-dom03-076',
    cardNumber: 76,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Utilisation de variables de session pour le multi-tenant (`current_setting`)',
    difficulty: 'medium',
    tags: ['Multi-tenant', 'current_setting', 'Session Variables', 'SaaS Architecture'],
    front: {
      question: 'Comment implémenter une politique RLS multi-tenant sans créer un utilisateur PostgreSQL distinct pour chaque client de l\'application ?',
      codeSnippet: 'CREATE POLICY tenant_isolation_policy ON comptes\n  FOR ALL\n  USING (tenant_id = NULLIF(current_setting(\'app.current_tenant_id\', true), \'\')::uuid);',
      hint: 'L\'application web injecte l\'ID du tenant dans la session via SET LOCAL avant d\'exécuter ses requêtes.',
    },
    back: {
      answer: 'On s\'appuie sur les variables de configuration de session avec `current_setting()` :\n\n1. L\'application web utilise un pool de connexions unique avec un utilisateur générique `app_user`.\n2. Au début de chaque transaction HTTP/requête, l\'application positionne l\'ID du client courant :\n   ```sql\n   SET LOCAL app.current_tenant_id = \'a4c1...-uuid\';\n   ```\n3. La politique RLS évalue dynamiquement cette valeur :\n   ```sql\n   USING (tenant_id = current_setting(\'app.current_tenant_id\', true)::uuid)\n   ```\n4. À la fin de la transaction (`COMMIT`), la variable est automatiquement réinitialisée, évitant toute fuite entre requêtes d\'un même pool.',
      explanation: 'Le second argument `true` dans `current_setting(\'cle\', true)` évite de lever une erreur fatale si la variable n\'a pas encore été initialisée (renvoie NULL).',
      examTrap: 'Toujours utiliser `SET LOCAL` (portée transactionnelle) et non `SET` global pour ne pas contaminer la connexion du pool lors des requêtes suivantes.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 5.8: Row Security Policies',
    },
  },
  {
    id: 'fc-pgedb-dom03-077',
    cardNumber: 77,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Audit des politiques RLS avec la vue `pg_policies`',
    difficulty: 'easy',
    tags: ['pg_policies', 'Catalog', 'Audit', 'Inspection'],
    front: {
      question: 'Quelle vue système permet de consulter l\'intégralité des politiques de sécurité au niveau des lignes actives sur la base ?',
      codeSnippet: 'SELECT schemaname, tablename, policyname, permissive, roles, cmd, qual, with_check\nFROM pg_policies;',
      hint: 'Vue système pg_policies.',
    },
    back: {
      answer: 'La vue système est **`pg_policies`**.\n\nColonnes détaillées :\n- `schemaname` : Nom du schéma.\n- `tablename` : Nom de la table protégée.\n- `policyname` : Nom de la politique.\n- `permissive` : `PERMISSIVE` ou `RESTRICTIVE`.\n- `roles` : Tableau des rôles auxquels elle s\'applique (ex: `{public}`, `{app_user}`).\n- `cmd` : Commande ciblée (`SELECT`, `INSERT`, `UPDATE`, `DELETE` ou `ALL`).\n- `qual` : Expression textuelle de la clause `USING`.\n- `with_check` : Expression textuelle de la clause `WITH CHECK`.',
      explanation: 'Dans `psql`, on peut également voir si RLS est actif sur une table via `\\d+ nom_table` (rechercher la mention "Row Level Security: enabled").',
      examTrap: '`pg_policies` liste les politiques créées, mais n\'indique pas si RLS a été activé sur la table (`rowsecurity = true` dans `pg_class`).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Views - pg_policies',
    },
  },
  {
    id: 'fc-pgedb-dom03-078',
    cardNumber: 78,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Contournement et impact sur les performances des politiques RLS',
    difficulty: 'hard',
    tags: ['Performance', 'Indexation', 'Subqueries', 'RLS Optimization'],
    front: {
      question: 'Quel est l\'impact des politiques RLS sur le plan d\'exécution (`EXPLAIN`) et les performances des requêtes ?',
      codeSnippet: 'EXPLAIN ANALYZE SELECT * FROM factures;',
      hint: 'Le moteur réécrit la requête en injectant l\'expression RLS directement comme un filtre WHERE obligatoire.',
    },
    back: {
      answer: 'Impact sur le planificateur et les performances :\n1. **Réécriture de requête** : Le parseur injecte automatiquement l\'expression booléenne de la politique dans l\'arbre de la requête, agissant exactement comme une condition `WHERE` additionnelle.\n2. **Utilisation des index** : Si l\'expression RLS filtre sur `tenant_id` ou `owner_id`, un index sur cette colonne est indispensable, sinon PostgreSQL fera un Sequential Scan complet de la table à chaque requête !\n3. **Sous-requêtes coûteuses** : Si la politique RLS contient une sous-requête corrélée (`USING (id IN (SELECT ...))`), cette sous-requête peut être évaluée pour chaque ligne candidate, dégradant fortement les performances.\n4. Recommandation : Préférer des fonctions `STABLE` légères et indexer systématiquement les colonnes testées dans les clauses `USING`.',
      explanation: 'L\'inspection avec `EXPLAIN` montre explicitement le filtre de sécurité sous la mention "Filter: ...".',
      examTrap: 'Oublier d\'indexer la colonne utilisée dans la condition RLS divise les débits de requêtes par 10 ou 100 sur les tables volumineuses.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 5.8: Row Security Policies',
    },
  },
  {
    id: 'fc-pgedb-dom03-079',
    cardNumber: 79,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Modification et suppression de politiques RLS',
    difficulty: 'easy',
    tags: ['ALTER POLICY', 'DROP POLICY', 'Maintenance'],
    front: {
      question: 'Quelles sont les commandes SQL pour modifier l\'expression d\'une politique RLS existante ou la supprimer complètement ?',
      codeSnippet: 'ALTER POLICY user_docs_policy ON documents USING (...);\nDROP POLICY user_docs_policy ON documents;',
      hint: 'ALTER POLICY et DROP POLICY en précisant le nom de la politique et de la table.',
    },
    back: {
      answer: '1. Modifier une politique existante :\n```sql\nALTER POLICY nom_politique ON nom_table\n  [TO { nom_role | PUBLIC }]\n  [USING ( nouvelle_expression_using )]\n  [WITH CHECK ( nouvelle_expression_with_check )];\n```\n2. Renommer une politique :\n```sql\nALTER POLICY ancien_nom ON nom_table RENAME TO nouveau_nom;\n```\n3. Supprimer définitivement une politique :\n```sql\nDROP POLICY nom_politique ON nom_table;\n```',
      explanation: 'La modification prend effet instantanément pour toutes les transactions futures.',
      examTrap: 'Si vous supprimez la dernière politique d\'une table dont RLS est toujours actif (`ENABLE RLS`), la table bascule en mode verrouillé ("Default Deny") : plus aucun utilisateur standard ne peut lire de lignes !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER POLICY / DROP POLICY',
    },
  },
  {
    id: 'fc-pgedb-dom03-080',
    cardNumber: 80,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Désactivation complète de RLS sur une table',
    difficulty: 'easy',
    tags: ['DISABLE RLS', 'ALTER TABLE', 'Security Toggle'],
    front: {
      question: 'Comment désactiver la sécurité au niveau des lignes sur une table sans supprimer les politiques précédemment définies ?',
      codeSnippet: 'ALTER TABLE documents DISABLE ROW LEVEL SECURITY;',
      hint: 'Instruction ALTER TABLE nom_table DISABLE ROW LEVEL SECURITY.',
    },
    back: {
      answer: 'La commande est :\n```sql\nALTER TABLE nom_table DISABLE ROW LEVEL SECURITY;\n```\nEffets :\n- Le moteur PostgreSQL cesse d\'appliquer les politiques RLS sur cette table.\n- Les droits d\'accès reviennent au modèle de permissions standard `GRANT/REVOKE` au niveau de la table entière.\n- Les définitions des politiques (`CREATE POLICY`) sont conservées intactes dans le catalogue système : elles pourront être réactivées à tout moment via `ENABLE ROW LEVEL SECURITY`.',
      explanation: 'Utile lors d\'opérations de migration lourde ou de dépannage temporaire.',
      examTrap: 'Désactiver RLS expose immédiatement l\'intégralité des lignes de la table à tous les rôles disposant du privilège `SELECT`.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER TABLE (Row Level Security)',
    },
  },

  // =========================================================================
  // SECTION 9 : SÉCURITÉ DES FONCTIONS, VUES & EXTENSIONS (Cartes 81 à 90)
  // SECURITY DEFINER vs INVOKER, search_path hijack, security_invoker views (PG 15+), CREATE EXTENSION
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-081',
    cardNumber: 81,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: '`SECURITY DEFINER` vs `SECURITY INVOKER`',
    difficulty: 'medium',
    tags: ['SECURITY DEFINER', 'SECURITY INVOKER', 'Functions', 'Privilege Escalation'],
    front: {
      question: 'Quelle est la différence fondamentale entre une fonction déclarée `SECURITY INVOKER` (défaut) et une fonction déclarée `SECURITY DEFINER` ?',
      codeSnippet: 'CREATE FUNCTION verifier_compte() RETURNS void AS ... SECURITY INVOKER;\nCREATE FUNCTION debiter_compte() RETURNS void AS ... SECURITY DEFINER;',
      hint: 'L\'une s\'exécute avec les privilèges de celui qui appelle la fonction, l\'autre avec ceux de son créateur/propriétaire.',
    },
    back: {
      answer: '1. `SECURITY INVOKER` (comportement par défaut) :\n- La fonction s\'exécute avec les privilèges de l\'utilisateur qui APPELLE la fonction.\n- Si l\'appelant n\'a pas de droits sur une table sous-jacente lue dans la fonction, la fonction échoue avec "permission denied".\n\n2. `SECURITY DEFINER` (équivalent du bit `setuid` Unix) :\n- La fonction s\'exécute avec les privilèges du rôle PROPRIÉTAIRE (`owner`) de la fonction.\n- Permet à un utilisateur ordinaire d\'exécuter une tâche contrôlée nécessitant des privilèges élevés (ex: modifier une table de grand livre) sans lui accorder d\'accès direct à la table.',
      explanation: 'Les fonctions `SECURITY DEFINER` sont très puissantes mais présentent des risques de sécurité majeurs si elles ne sont pas codées avec rigueur.',
      examTrap: 'Une fonction `SECURITY DEFINER` appartenant à `postgres` (superuser) donne virtuellement les pouvoirs superutilisateur lors de son exécution.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 38.6: Function Security and Privileges',
    },
  },
  {
    id: 'fc-pgedb-dom03-082',
    cardNumber: 82,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Vulnérabilité de détournement de `search_path` (Search Path Hijacking)',
    difficulty: 'hard',
    tags: ['search_path', 'Hijacking', 'Trojan Horse', 'Security Vulnerability'],
    front: {
      question: 'Quelle faille de sécurité critique menace une fonction `SECURITY DEFINER` si la variable `search_path` n\'est pas explicitement verrouillée ?',
      codeSnippet: '-- Vulnérable à l\'attaque "Trojan Horse" :\nCREATE FUNCTION reset_password(u text, p text) RETURNS void AS $$\n  UPDATE users SET pass = p WHERE login = u;\n$$ LANGUAGE sql SECURITY DEFINER;',
      hint: 'Un attaquant peut créer une table ou fonction malveillante nommée users ou update dans un schéma temporaire public.',
    },
    back: {
      answer: 'L\'attaque par détournement de chemin de recherche (Search Path Hijacking) :\n- Lorsqu\'une fonction `SECURITY DEFINER` s\'exécute, elle hérite du `search_path` de la session de l\'utilisateur appelant.\n- Si l\'attaquant a le droit de créer des objets dans un schéma prioritaire (comme `public` ou le schéma temporaire `pg_temp`), il peut créer une fausse table `users` ou une fausse fonction surchargée contenant du code malveillant.\n- Lorsque la fonction superutilisateur tourne, elle résout l\'objet malveillant de l\'attaquant au lieu de l\'objet légitime, exécutant du code non autorisé avec les privilèges du propriétaire (élévation totale de privilèges !).',
      explanation: 'C\'est l\'une des vulnérabilités les plus célèbres de l\'écosystème PostgreSQL (ayant motivé les CVE et durcissements récents).',
      examTrap: 'Ne JAMAIS écrire une fonction `SECURITY DEFINER` sans fixer explicitement son `search_path`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 38.6: Writing SECURITY DEFINER Functions Safely',
    },
  },
  {
    id: 'fc-pgedb-dom03-083',
    cardNumber: 83,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Sécurisation obligatoire d\'une fonction `SECURITY DEFINER`',
    difficulty: 'hard',
    tags: ['SET search_path', 'Hardening', 'SECURITY DEFINER', 'Best Practice'],
    front: {
      question: 'Quelle clause obligatoire doit systématiquement accompagner la déclaration de toute fonction `SECURITY DEFINER` pour neutraliser le risque de détournement de schéma ?',
      codeSnippet: 'CREATE FUNCTION safe_operation()\nRETURNS void AS $$\n...\n$$ LANGUAGE plpgsql SECURITY DEFINER\nSET search_path = pg_catalog, pg_temp; -- Clause salvatrice !',
      hint: 'La clause SET search_path force un chemin de recherche sain et immuable lors de l\'exécution.',
    },
    back: {
      answer: 'La meilleure pratique officielle imposée est d\'ajouter la clause :\n```sql\nSET search_path = pg_catalog, pg_temp\n```\n(ou en ciblant explicitement les schémas applicatifs sécurisés : `SET search_path = app_schema, pg_catalog`).\n\nEffets salvateurs :\n1. Au moment de l\'appel de la fonction, PostgreSQL force temporairement la valeur de `search_path` à cette liste sûre, ignorant totalement le `search_path` de la session de l\'appelant.\n2. Élimine tout risque d\'injection de tables fantômes ou d\'opérateurs pirates.\n3. À la sortie de la fonction, le `search_path` de l\'appelant est fidèlement restitué.',
      explanation: 'Il est également fortement recommandé de préfixer tous les noms de tables par leur schéma dans le corps de la fonction (ex: `app_schema.utilisateurs`).',
      examTrap: 'Oublier `pg_temp` ou le placer en tête de liste permettrait encore l\'exploitation de fonctions temporaires.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 38.6: Writing SECURITY DEFINER Functions Safely',
    },
  },
  {
    id: 'fc-pgedb-dom03-084',
    cardNumber: 84,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Modèle de sécurité des vues standard',
    difficulty: 'medium',
    tags: ['Views', 'Security Definer Semantics', 'View Permissions', 'Ownership'],
    front: {
      question: 'Comment PostgreSQL résout-il les droits d\'accès aux tables sous-jacentes lors de l\'interrogation d\'une vue standard (`CREATE VIEW`) ?',
      codeSnippet: 'CREATE VIEW v_salaires_publics AS SELECT id, nom, poste FROM salaires;\nGRANT SELECT ON v_salaires_publics TO stagiaire;\n-- Le stagiaire n\'a AUCUN droit direct sur la table salaires. La requête fonctionne-t-elle ?',
      hint: 'Les vues standard agissent historiquement comme des fonctions SECURITY DEFINER vis-à-vis des tables de base.',
    },
    back: {
      answer: 'OUI, la requête fonctionne parfaitement !\n\nModèle des vues standard :\n1. L\'utilisateur (`stagiaire`) doit posséder le privilège `SELECT` sur la VUE (`v_salaires_publics`).\n2. Les vérifications de permissions sur les tables physiques sous-jacentes (`salaires`) sont effectuées avec les privilèges du **PROPRIÉTAIRE DE LA VUE**, et NON PAS ceux de l\'appelant.\n3. Tant que le propriétaire de la vue a le droit d\'accéder à la table `salaires`, le `stagiaire` peut interroger la vue sans avoir besoin du moindre droit sur la table sous-jacente.\n\nPermet de créer des vues filtrées sécurisées masquant des colonnes confidentielles.',
      explanation: 'Ce mécanisme fonctionne comme un masque de sécurité parfait depuis les débuts du SQL relationnel.',
      examTrap: 'Si le propriétaire de la vue perd ses droits sur la table sous-jacente, la vue devient instantanément inaccessible pour tout le monde.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 39.4: Rules and Privileges in Views',
    },
  },
  {
    id: 'fc-pgedb-dom03-085',
    cardNumber: 85,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Option de vue `security_invoker = true` (PostgreSQL 15+)',
    difficulty: 'hard',
    tags: ['security_invoker', 'PostgreSQL 15+', 'RLS in Views', 'View Options'],
    front: {
      question: 'Quelle option majeure pour les vues a été introduite dans PostgreSQL 15 pour forcer le respect des droits de l\'appelant et des politiques RLS ?',
      codeSnippet: 'CREATE VIEW v_mes_commandes\nWITH (security_invoker = true)\nAS SELECT * FROM commandes;',
      hint: 'security_invoker oblige la vue à vérifier les permissions et les règles RLS de l\'utilisateur qui lance la requête.',
    },
    back: {
      answer: 'L\'option est **`security_invoker = true`** (PostgreSQL 15+) :\n\nComportement :\n- Contrairement aux vues classiques, les accès aux tables sous-jacentes sont évalués avec les privilèges et le contexte de l\'utilisateur qui APPELLE la vue (`invoker`), et non plus du propriétaire de la vue.\n- **Impact crucial sur RLS** : Si la table `commandes` est protégée par Row-Level Security (RLS), les politiques RLS de l\'utilisateur appelant s\'appliquent désormais à travers la vue !\n- Avec les vues standard classiques (sans cette option), RLS évaluait le propriétaire de la vue, ce qui court-circuitait souvent le filtrage multi-tenant attendu.',
      explanation: 'Pour modifier une vue existante : `ALTER VIEW nom_vue SET (security_invoker = true);`.',
      examTrap: 'Avec `security_invoker = true`, l\'utilisateur appelant DOIT avoir des privilèges directs (`SELECT`) sur la table sous-jacente, sinon la requête échouera.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: CREATE VIEW (security_invoker)',
    },
  },
  {
    id: 'fc-pgedb-dom03-086',
    cardNumber: 86,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Sécurité des extensions : restriction de `CREATE EXTENSION`',
    difficulty: 'medium',
    tags: ['CREATE EXTENSION', 'pg_extension', 'Trusted Extensions', 'Superuser'],
    front: {
      question: 'Pourquoi la commande `CREATE EXTENSION` est-elle historiquement restreinte aux rôles `SUPERUSER` et qu\'est-ce qu\'une extension de confiance ("trusted") ?',
      codeSnippet: 'CREATE EXTENSION pgcrypto;\nCREATE EXTENSION pg_stat_statements;',
      hint: 'Une extension peut charger des bibliothèques dynamiques en C (.so) qui s\'exécutent au niveau du noyau du serveur.',
    },
    back: {
      answer: 'Risque des extensions :\n- De nombreuses extensions compilent des bibliothèques C natives (`.so`/`.dll`) qui s\'exécutent directement dans l\'espace mémoire du processus serveur PostgreSQL avec les privilèges OS de `postgres`.\n- Une extension non contrôlée pourrait compromettre l\'intégrité de l\'hôte, d\'où la restriction stricte à `SUPERUSER`.\n\nExtensions de confiance (**Trusted Extensions**) :\n- Depuis PostgreSQL 13, certaines extensions reconnues comme sans risque (ne chargeant pas de code C arbitraire et isolées dans leur schéma) sont marquées `trusted = true` dans leur fichier de contrôle `.control` (ex: `pgcrypto`, `uuid-ossp`, `citext`, `hstore`).\n- Un utilisateur ordinaire ayant le droit `CREATE` sur la base peut installer ces extensions de confiance sans être superutilisateur.',
      explanation: 'La liste des extensions installées se consulte dans la vue `pg_extension`.',
      examTrap: 'Les extensions comme `pg_stat_statements` qui exigent une pré-charge dans `shared_preload_libraries` ne peuvent jamais être installées sans superutilisateur.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 38.17: Packaging Related Objects into an Extension',
    },
  },
  {
    id: 'fc-pgedb-dom03-087',
    cardNumber: 87,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Langages de procédures de confiance (`trusted`) vs non sécurisés (`untrusted`)',
    difficulty: 'hard',
    tags: ['Procedural Languages', 'PL/pgSQL', 'Untrusted Languages', 'plpythonu'],
    front: {
      question: 'Quelle est la différence de sécurité entre un langage procédural qualifié de `TRUSTED` (comme `plpgsql`) et un langage `UNTRUSTED` (comme `plperlu` ou `plpython3u`) ?',
      codeSnippet: 'CREATE FUNCTION script_os() RETURNS text AS $$\n  import os\n  return os.popen("whoami").read()\n$$ LANGUAGE plpython3u;',
      hint: 'Le u dans le nom du langage signifie untrusted : il donne un accès complet au système de fichiers et au réseau de la machine hôte.',
    },
    back: {
      answer: '1. Langages `TRUSTED` (ex: `plpgsql`, `sql`, `pltcl`) :\n- Le moteur garantit un bac à sable (sandbox) strict.\n- Le code du langage ne peut pas accéder au système de fichiers local du serveur, ni ouvrir de sockets réseau arbitraires, ni exécuter d\'appels système OS.\n- N\'importe quel utilisateur disposant du droit `USAGE` sur le langage peut créer des fonctions dans ce langage.\n\n2. Langages `UNTRUSTED` (suffixés par `u`, ex: `plpython3u`, `plperlu`, `C`) :\n- N\'imposent aucune restriction : le script peut exécuter n\'importe quelle commande OS, lire `/etc/passwd`, ouvrir des connexions réseau ou effacer des fichiers avec les privilèges du compte OS `postgres`.\n- Seul un `SUPERUSER` est autorisé à créer des fonctions écrites dans un langage `untrusted`.',
      explanation: 'Le suffixe `u` signifie explicitement "untrusted" (non approuvé / non confiné).',
      examTrap: 'Accorder le droit d\'utiliser un langage non sécurisé à un utilisateur équivaut immédiatement à lui donner les droits superutilisateur et root.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 41: Procedural Languages',
    },
  },
  {
    id: 'fc-pgedb-dom03-088',
    cardNumber: 88,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Durcissement du schéma d\'installation des extensions',
    difficulty: 'medium',
    tags: ['CREATE EXTENSION CASCADE', 'Schema Hardening', 'Security Best Practice'],
    front: {
      question: 'Pourquoi est-il fortement recommandé de spécifier la clause `SCHEMA` lors de l\'installation d\'une extension avec `CREATE EXTENSION` ?',
      codeSnippet: 'CREATE EXTENSION pgcrypto SCHEMA extensions;',
      hint: 'Pour éviter de polluer le schéma public ou un schéma applicatif et compartimenter les privilèges d\'exécution.',
    },
    back: {
      answer: 'Spécifier `SCHEMA nom_schema` (ex: `extensions`) lors de l\'installation :\n1. **Compartimentation** : Regroupe l\'ensemble des fonctions, types et tables de l\'extension dans un schéma dédié plutôt que de les déverser pêle-mêle dans le schéma `public`.\n2. **Contrôle d\'accès granulaire** : Permet d\'accorder ou de révoquer l\'accès aux fonctionnalités de l\'extension de manière ciblée (`GRANT USAGE ON SCHEMA extensions TO ...`).\n3. **Prévention des conflits** : Évite les collisions de noms entre les fonctions de l\'extension et les tables/fonctions applicatives métiers.\n4. **Sauvegardes facilitées** : Simplifie l\'exclusion ou l\'inclusion lors des exports logiques (`pg_dump`).',
      explanation: 'Pour que les utilisateurs accèdent facilement aux fonctions de l\'extension, on ajoute simplement ce schéma dans leur `search_path`.',
      examTrap: 'Certaines extensions plus anciennes ne sont pas "relocatable" (déplaçables) et exigent impérativement d\'être créées dans un schéma déterminé par leur auteur.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: CREATE EXTENSION',
    },
  },
  {
    id: 'fc-pgedb-dom03-089',
    cardNumber: 89,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Inspection du code et des attributs de sécurité des fonctions (`pg_proc`)',
    difficulty: 'easy',
    tags: ['pg_proc', 'Inspection', 'Security Audit', 'prosecdef'],
    front: {
      question: 'Quelle colonne de la table catalogue `pg_proc` indique si une fonction est configurée en `SECURITY DEFINER` ?',
      codeSnippet: 'SELECT proname, prosecdef, prosrc\nFROM pg_proc\nWHERE prosecdef = true;',
      hint: 'La colonne prosecdef (Procedure Security Definer) est un booléen.',
    },
    back: {
      answer: 'La colonne est **`prosecdef`** dans la table système `pg_proc` :\n- `true` : La fonction s\'exécute avec les privilèges du propriétaire (`SECURITY DEFINER`).\n- `false` : La fonction s\'exécute avec les privilèges de l\'appelant (`SECURITY INVOKER`).\n\nRequête d\'audit de sécurité indispensable :\nElle permet aux DBA de lister toutes les fonctions à privilèges élevés de la base pour s\'assurer qu\'elles possèdent bien une directive `search_path` sécurisée et qu\'elles n\'appartiennent pas indûment à `postgres`.',
      explanation: 'Dans psql, la commande `\\df+ nom_fonction` affiche également la mention `Security: definer` ou `Security: invoker`.',
      examTrap: 'Une fonction `prosecdef = true` appartenant à un utilisateur supprimé ou orphelin peut bloquer son exécution.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Catalogs - pg_proc',
    },
  },
  {
    id: 'fc-pgedb-dom03-090',
    cardNumber: 90,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Révocation préventive de l\'exécution sur les fonctions sensibles',
    difficulty: 'medium',
    tags: ['REVOKE EXECUTE', 'PUBLIC', 'Security Hardening'],
    front: {
      question: 'Pourquoi et comment restreindre l\'accès à une fonction de calcul financier critique dès sa création ?',
      codeSnippet: 'CREATE FUNCTION clore_exercice_comptable() ...;\n-- Quelle commande de sécurité exécuter immédiatement après ?',
      hint: 'Par défaut, PUBLIC reçoit le privilège EXECUTE sur les nouvelles fonctions dans le schéma public.',
    },
    back: {
      answer: 'Procédure de durcissement obligatoire :\n1. Révoquer immédiatement le droit implicite accordé à tout le monde :\n   ```sql\n   REVOKE EXECUTE ON FUNCTION clore_exercice_comptable() FROM PUBLIC;\n   ```\n2. Accorder le privilège d\'exécution de manière nominative au seul rôle métier légitime :\n   ```sql\n   GRANT EXECUTE ON FUNCTION clore_exercice_comptable() TO directeur_financier;\n   ```\n\nSans cette première ligne de révocation, n\'importe quel utilisateur connecté à la base pourrait invoquer la fonction directement !',
      explanation: 'Pour automatiser ce durcissement sur tout un schéma : `ALTER DEFAULT PRIVILEGES REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC;`.',
      examTrap: 'Croire qu\'une fonction n\'est accessible qu\'à son créateur par défaut est une illusion : le standard PostgreSQL accorde historiquement `EXECUTE` à `PUBLIC`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 5.7: Privileges (Functions)',
    },
  },

  // =========================================================================
  // SECTION 10 : AUDIT DE SÉCURITÉ, PG_STAT_ACTIVITY, DÉCONNEXION & CHIFFREMENT (Cartes 91 à 100)
  // pg_stat_activity, pg_terminate_backend, log_connections, pgaudit, SSL/TLS, pgcrypto, schema public PG15+
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-091',
    cardNumber: 91,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Surveillance des sessions et sécurité avec `pg_stat_activity`',
    difficulty: 'easy',
    tags: ['pg_stat_activity', 'Monitoring', 'Active Sessions', 'Troubleshooting'],
    front: {
      question: 'Quelles colonnes de sécurité clés de la vue `pg_stat_activity` permettent d\'auditer qui est actuellement connecté au serveur et d\'où ?',
      codeSnippet: 'SELECT pid, usename, datname, client_addr, client_port, ssl, backend_start, state, query\nFROM pg_stat_activity\nWHERE state != \'idle\';',
      hint: 'Colonnes renseignant l\'utilisateur (usename), l\'IP cliente (client_addr), l\'utilisation de SSL (ssl) et la requête en cours (query).',
    },
    back: {
      answer: 'La vue système fondamentale est **`pg_stat_activity`** :\n- `pid` : L\'identifiant du processus serveur dédié à la session.\n- `usename` : Le nom du rôle PostgreSQL connecté.\n- `datname` : Le nom de la base de données courante.\n- `client_addr` : L\'adresse IP source de la machine cliente (NULL si connexion locale par socket Unix).\n- `client_port` : Le port TCP client émetteur.\n- `ssl` : Booléen (`true`/`false`) indiquant si la connexion transite dans un tunnel TLS chiffré.\n- `backend_start` : Date et heure d\'ouverture de la session.\n- `query` : Texte de la dernière requête SQL exécutée.',
      explanation: 'Un utilisateur ordinaire ne voit en clair que ses propres requêtes ; pour voir les requêtes des autres, il doit posséder le rôle `pg_read_all_stats` ou être superutilisateur.',
      examTrap: 'Si `client_addr` est NULL, cela n\'indique pas un bug : cela signifie simplement que la connexion a été ouverte localement via socket Unix.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 28.2: The Dynamic Statistics Views - pg_stat_activity',
    },
  },
  {
    id: 'fc-pgedb-dom03-092',
    cardNumber: 92,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Interruption de requêtes vs expulsion de sessions (`cancel` vs `terminate`)',
    difficulty: 'medium',
    tags: ['pg_cancel_backend', 'pg_terminate_backend', 'Kill Session', 'SIGINT vs SIGTERM'],
    front: {
      question: 'Quelle est la différence fondamentale entre les fonctions d\'administration `pg_cancel_backend(pid)` et `pg_terminate_backend(pid)` ?',
      codeSnippet: 'SELECT pg_cancel_backend(4567);\nSELECT pg_terminate_backend(4567);',
      hint: 'L\'une interrompt la requête en cours en maintenant la session ouverte ; l\'autre déconnecte et tue physiquement la session.',
    },
    back: {
      answer: '1. `pg_cancel_backend(pid)` (signal SIGINT) :\n- Interrompt et annule UNIQUEMENT la requête SQL en cours d\'exécution dans la session ciblée.\n- La connexion cliente reste ouverte et active, prête à recevoir une nouvelle requête du client.\n- Action non destructive pour la session.\n\n2. `pg_terminate_backend(pid)` (signal SIGTERM) :\n- Déconnecte brutalement et détruit le processus serveur associé à la session cliente.\n- La transaction en cours est immédiatement annulée (`ROLLBACK`) et la socket réseau est fermée côté serveur.\n- Le client reçoit l\'erreur "FATAL: terminating connection due to administrator command".',
      explanation: 'Indispensable pour libérer des verrous exclusifs bloquants ou expulser une session compromise.',
      examTrap: 'Il est impossible de tuer le backend d\'un `SUPERUSER` sans disposer soi-même des privilèges superutilisateur.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 9.27: Server Signaling Functions',
    },
  },
  {
    id: 'fc-pgedb-dom03-093',
    cardNumber: 93,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Journalisation des connexions et déconnexions pour l\'audit',
    difficulty: 'easy',
    tags: ['log_connections', 'log_disconnections', 'Auditing', 'Logging'],
    front: {
      question: 'Quels paramètres de `postgresql.conf` permettent de consigner dans les fichiers de logs chaque ouverture et fermeture de session avec sa durée ?',
      codeSnippet: 'log_connections = on\nlog_disconnections = on\nlog_duration = on',
      hint: 'Paramètres booléens log_connections et log_disconnections.',
    },
    back: {
      answer: 'Dans `postgresql.conf` :\n1. `log_connections = on` :\n   - Écrit une ligne de log détaillée dès qu\'une connexion cliente est négociée avec succès (affiche l\'utilisateur, la base, l\'IP source, le port et la méthode d\'authentification utilisée).\n2. `log_disconnections = on` :\n   - Écrit une ligne de log lors de la fermeture de la session en indiquant la durée totale pendant laquelle la session est restée active (`session time`).\n3. `log_duration = on` : Logue le temps d\'exécution de chaque instruction SQL.\n\nRequis par tous les standards de conformité (PCI-DSS, HIPAA, SOX) pour la traçabilité des accès.',
      explanation: 'Ces paramètres sont modifiables à chaud avec rechargement `SIGHUP`.',
      examTrap: 'Sur des applications utilisant des connexions courtes sans pool de connexions (ex: PHP classique ouvrant 500 connexions/sec), activer `log_connections` peut faire exploser la taille des logs disque.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8: Error Reporting and Logging',
    },
  },
  {
    id: 'fc-pgedb-dom03-094',
    cardNumber: 94,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Extension d\'audit certifiée : `pgaudit`',
    difficulty: 'hard',
    tags: ['pgaudit', 'Enterprise Security', 'Compliance', 'Audit Trail'],
    front: {
      question: 'Qu\'est-ce que l\'extension `pgaudit` (PostgreSQL Audit Extension) et quel avantage apporte-t-elle par rapport au simple paramètre natif `log_statement = \'all\'` ?',
      codeSnippet: 'shared_preload_libraries = \'pgaudit\'\npgaudit.log = \'write, ddl, role\'',
      hint: 'Elle fournit un formatage d\'audit conforme aux exigences réglementaires gouvernementales et financières avec filtrage par classes d\'opérations.',
    },
    back: {
      answer: '`pgaudit` est l\'extension d\'audit de référence en entreprise et dans le cloud :\n\nAvantages décisifs par rapport à `log_statement` :\n1. **Formatage structuré et standardisé** : Génère des entrées d\'audit normalisées conformes aux normes gouvernementales américaines (DoD STIG, HIPAA, PCI-DSS).\n2. **Filtrage par classes sémantiques** : Permet de cibler finement ce qui doit être audité via `pgaudit.log` :\n   - `READ` (`SELECT`)\n   - `WRITE` (`INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`)\n   - `FUNCTION` (appels de fonctions)\n   - `ROLE` (`GRANT`, `REVOKE`, `CREATE ROLE`)\n   - `DDL` (`CREATE`, `ALTER`, `DROP`)\n   - `MISC` (`DISCARD`, `VACUUM`, `CHECKPOINT`)\n3. **Audit par objet ou par rôle** : Possibilité d\'auditer une seule table critique sans saturer les logs avec le reste de la base.',
      explanation: 'Exige d\'être chargée au démarrage du serveur via `shared_preload_libraries = \'pgaudit\'`.',
      examTrap: 'Si `pgaudit` est mentionné dans l\'examen EDB, il fait référence à la solution d\'audit de niveau entreprise certifiée.',
      ruleRef: 'PostgreSQL Audit Extension (pgaudit) Documentation & EnterpriseDB Security Guidelines',
    },
  },
  {
    id: 'fc-pgedb-dom03-095',
    cardNumber: 95,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Chiffrement en transit avec SSL/TLS côté serveur',
    difficulty: 'medium',
    tags: ['SSL/TLS', 'ssl = on', 'Certificates', 'In-Transit Encryption'],
    front: {
      question: 'Quels sont les paramètres minimaux indispensables dans `postgresql.conf` pour activer le chiffrement SSL/TLS des flux réseau ?',
      codeSnippet: 'ssl = on\nssl_cert_file = \'server.crt\'\nssl_key_file = \'server.key\'\nssl_ca_file = \'root.crt\'',
      hint: 'Activation de ssl et spécification du chemin vers le certificat public et la clé privée du serveur.',
    },
    back: {
      answer: 'Configuration minimale dans `postgresql.conf` :\n1. `ssl = on` : Active la couche SSL/TLS dans le binaire PostgreSQL.\n2. `ssl_cert_file = \'server.crt\'` : Chemin vers le certificat public x509 du serveur (au format PEM).\n3. `ssl_key_file = \'server.key\'` : Chemin vers la clé privée du serveur.\n4. `ssl_ca_file = \'root.crt\'` : Optionnel mais requis pour valider les certificats clients (mTLS).\n\nPermissions strictes du fichier clé sur Linux :\nLe fichier de clé privée (`server.key`) doit obligatoirement avoir des droits stricts (`chmod 0600 server.key` ou `0400`), sinon le serveur refusera catégoriquement de démarrer !',
      explanation: 'Combiné avec `hostssl` dans `pg_hba.conf`, cela garantit un chiffrement de bout en bout inattaquable.',
      examTrap: 'Si `server.key` est lisible par d\'autres utilisateurs (`permissions 0644`), PostgreSQL renvoie l\'erreur "private key file permissions are less strict than 0600" et s\'arrête.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.9.2: SSL Configuration',
    },
  },
  {
    id: 'fc-pgedb-dom03-096',
    cardNumber: 96,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Chiffrement au repos : EDB Transparent Data Encryption (TDE) vs pgcrypto',
    difficulty: 'hard',
    tags: ['TDE', 'Transparent Data Encryption', 'EPAS', 'pgcrypto', 'At-Rest Encryption'],
    front: {
      question: 'Quelle est la différence entre le Transparent Data Encryption (TDE) natif d\'EnterpriseDB (EPAS) et le chiffrement applicatif avec l\'extension `pgcrypto` ?',
      codeSnippet: '-- pgcrypto (niveau colonne SQL) :\nSELECT pgp_sym_encrypt(\'donnee_secrete\', \'cle_maitre\');\n\n-- TDE (niveau cluster matériel sous EPAS) :\ninitdb --data-encryption ...',
      hint: 'TDE chiffre automatiquement et de manière transparente tous les blocs disque sans modifier le code SQL applicatif.',
    },
    back: {
      answer: '1. **TDE (Transparent Data Encryption)** sous EDB Postgres Advanced Server (EPAS) :\n- Chiffre automatiquement l\'intégralité des fichiers de données, du WAL et des fichiers temporaires au niveau bloc lors de l\'écriture sur le stockage, et les déchiffre à la volée lors de la lecture en RAM.\n- Totalement transparent pour les développeurs : aucune modification du code SQL ou des requêtes applicatives.\n- Conforme aux exigences PCI-DSS sur le chiffrement des supports physiques en cas de vol de disques durs.\n\n2. **Extension `pgcrypto`** (PostgreSQL communautaire) :\n- Chiffrement au niveau colonne par appel de fonctions explicites (`pgp_sym_encrypt`, `pgp_pub_encrypt`).\n- Exige de réécrire les requêtes applicatives (`INSERT INTO ... pgp_sym_encrypt(...)`).\n- Empêche l\'utilisation standard des index B-tree sur les colonnes chiffrées.',
      explanation: 'TDE est l\'une des fonctionnalités phares d\'EDB Postgres pour les grandes banques et assurances.',
      examTrap: 'PostgreSQL communautaire natif v16 ne possède pas de TDE natif intégré au cœur du moteur ; TDE est fourni par EDB (EPAS) ou par des systèmes de fichiers chiffrés OS (LUKS).',
      ruleRef: 'EDB Postgres Advanced Server Documentation - Guide: Transparent Data Encryption (TDE)',
    },
  },
  {
    id: 'fc-pgedb-dom03-097',
    cardNumber: 97,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Fonctions de chiffrement avec l\'extension `pgcrypto`',
    difficulty: 'medium',
    tags: ['pgcrypto', 'crypt', 'gen_salt', 'pgp_sym_encrypt', 'Hashing'],
    front: {
      question: 'Quelles fonctions majeures fournit l\'extension `pgcrypto` pour le hachage sécurisé de mots de passe et le chiffrement symétrique de données ?',
      codeSnippet: 'CREATE EXTENSION pgcrypto;\n-- Hachage de mot de passe avec sel bcrypt :\nSELECT crypt(\'mon_mot_de_passe\', gen_salt(\'bf\', 8));\n-- Chiffrement symétrique AES :\nSELECT pgp_sym_encrypt(\'donnee confidentielle\', \'cle_secrete_aes\');',
      hint: 'crypt() et gen_salt() pour le hash à sens unique, pgp_sym_encrypt() et pgp_sym_decrypt() pour le chiffrement symétrique.',
    },
    back: {
      answer: 'Fonctions clés de l\'extension `pgcrypto` :\n1. `gen_salt(type [, rounds])` : Génère un sel cryptographique aléatoire fort. Le type `bf` utilise l\'algorithme moderne Blowfish (Bcrypt).\n2. `crypt(password, salt)` : Hache le mot de passe avec le sel fourni. Utilisé pour vérifier un mot de passe utilisateur sans jamais le stocker en clair :\n   ```sql\n   SELECT (password_hash = crypt(\'saisie_utilisateur\', password_hash)) AS valide FROM users;\n   ```\n3. `pgp_sym_encrypt(data, key)` : Chiffre une chaîne selon le standard OpenPGP avec un algorithme symétrique (AES-128/256 par défaut).\n4. `pgp_sym_decrypt(cipher_bytea, key)` : Déchiffre les données en clair à l\'aide de la clé.',
      explanation: 'L\'extension propose également le chiffrement asymétrique RSA / clé publique avec `pgp_pub_encrypt`.',
      examTrap: 'Si la clé symétrique passée à `pgp_sym_decrypt` est incorrecte, la fonction lève une erreur fatale.',
      ruleRef: 'PostgreSQL 16 Documentation - Appendix F: Additional Supplied Modules - pgcrypto',
    },
  },
  {
    id: 'fc-pgedb-dom03-098',
    cardNumber: 98,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Durcissement du schéma `public` dans PostgreSQL 15 & 16',
    difficulty: 'medium',
    tags: ['PostgreSQL 15+', 'public schema', 'Security Hardening', 'CVE-2018-1058'],
    front: {
      question: 'Quelle modification majeure de sécurité par défaut a été introduite dans PostgreSQL 15 concernant le schéma `public` et les droits de création des utilisateurs ordinaires ?',
      hint: 'Auparavant, n\'importe quel utilisateur pouvait créer des tables dans le schéma public, ce qui permettait des attaques par substitution.',
    },
    back: {
      answer: 'Depuis PostgreSQL 15 (et confirmé dans PG 16) :\n- Le privilège `CREATE` sur le schéma `public` a été **RÉVOQUÉ par défaut pour le pseudo-rôle `PUBLIC`** !\n- Dans les versions 14 et antérieures, tout rôle connecté pouvait immédiatement exécuter `CREATE TABLE public.test (...)` sans aucune restriction.\n\nConséquence en production :\n- Dans PostgreSQL 15+, si un développeur crée un utilisateur ordinaire et que cet utilisateur tente de créer une table dans `public`, la commande échoue avec "permission denied for schema public".\n- L\'administrateur doit désormais soit accorder explicitement le droit (`GRANT CREATE ON SCHEMA public TO mon_role;`), soit créer un schéma dédié propre à l\'application (`CREATE SCHEMA app AUTHORIZATION app_owner;`).',
      explanation: 'Cette mesure comble définitivement le vecteur d\'attaque CVE-2018-1058 sur les clusters partagés.',
      examTrap: 'C\'est l\'une des questions pièges les plus fréquentes lors de la migration vers PostgreSQL 15/16.',
      ruleRef: 'PostgreSQL 15 Release Notes & Chapter 5.9: Schemas (Public Schema Security)',
    },
  },
  {
    id: 'fc-pgedb-dom03-099',
    cardNumber: 99,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Protection contre les injections SQL (SQL Injection)',
    difficulty: 'easy',
    tags: ['SQL Injection', 'Prepared Statements', 'PREPARE', 'Application Security'],
    front: {
      question: 'Quel mécanisme de PostgreSQL et des pilotes applicatifs permet d\'immuniser les applications contre les attaques par injection SQL ?',
      codeSnippet: '-- Mauvais (concaténation de chaînes vulnérable) :\n-- "SELECT * FROM users WHERE login = \'" + userInput + "\';"\n\n-- Bon (Instruction préparée paramétrée) :\nPREPARE find_user(text) AS SELECT * FROM users WHERE login = $1;\nEXECUTE find_user(\'alice\');',
      hint: 'L\'utilisation de requêtes préparées avec des paramètres typés ($1, $2) sépare le code SQL des données en entrée.',
    },
    back: {
      answer: 'La protection absolue contre les injections SQL repose sur les **requêtes préparées et paramétrées** :\n\nFonctionnement :\n1. L\'instruction SQL est envoyée au serveur avec des espaces réservés paramétrés (`$1`, `$2`, etc.).\n2. Le serveur PostgreSQL analyse, vérifie la syntaxe et compile le plan d\'exécution AVANT de recevoir les données de l\'utilisateur.\n3. Les valeurs saisies par l\'utilisateur sont transmises dans un flux binaire séparé et traitées strictement comme des valeurs littérales de données, jamais comme du code exécutable.\n4. Même si la variable contient `\' OR \'1\'=\'1\' --`, cette chaîne est simplement comparée littéralement au champ, neutralisant 100% des injections SQL.',
      explanation: 'Toutes les bibliothèques modernes (JDBC PreparedStatement, psycopg3, pgx, node-postgres) exploitent ce protocole natif de PostgreSQL.',
      examTrap: 'Échapper manuellement les apostrophes avec des regex ou des fonctions maison dans l\'application reste faillible ; seules les requêtes paramétrées offrent une garantie totale.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: PREPARE / EXECUTE',
    },
  },
  {
    id: 'fc-pgedb-dom03-100',
    cardNumber: 100,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Checklist de durcissement global (Security Hardening Checklist)',
    difficulty: 'hard',
    tags: ['Hardening Checklist', 'CIS Benchmark', 'Security Summary', 'Exam Review'],
    front: {
      question: 'Quelles sont les 5 règles d\'or indispensables pour valider la conformité de sécurité d\'un cluster PostgreSQL en environnement d\'entreprise ?',
      hint: 'Mots de passe, restriction superutilisateur, chiffrement réseau, privilèges public, et principe du moindre privilège.',
    },
    back: {
      answer: 'Checklist de durcissement certifiée EnterpriseDB / CIS Benchmark :\n1. **Authentification moderne et stricte** : Forcer `password_encryption = \'scram-sha-256\'` et bannir `trust`, `password` et `md5` dans `pg_hba.conf`.\n2. **Bannir les accès distants Superuser** : Interdire au rôle `postgres` de se connecter depuis le réseau (`host all postgres ... reject`) ; restreindre son usage à la console locale (`local ... peer`).\n3. **Chiffrement réseau obligatoire** : Activer `ssl = on` avec certificats valides et utiliser des directives `hostssl` pour toutes les connexions applicatives distantes.\n4. **Verrouiller `PUBLIC` et le schéma `public`** : Révoquer `CONNECT ON DATABASE` et `CREATE ON SCHEMA` du pseudo-rôle `PUBLIC`.\n5. **Principe du moindre privilège** : Utiliser les rôles prédéfinis (`pg_read_all_data`, `pg_monitor`, `pg_maintain`) et séparer le propriétaire des schémas (`NOLOGIN`) des comptes applicatifs (`LOGIN`).',
      explanation: 'Cette synthèse résume les 100 flashcards du domaine de sécurité pour l\'examen EDB Certified Associate.',
      examTrap: 'Aucune mesure de sécurité isolée ne suffit : la sécurité PostgreSQL repose sur la défense en profondeur combinant le système OS, le réseau, `pg_hba.conf` et les ACL SQL.',
      ruleRef: 'EnterpriseDB Security Hardening Guide & CIS PostgreSQL 16 Benchmark v1.0.0',
    },
  },
];
