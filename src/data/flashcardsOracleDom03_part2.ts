import { FlashcardItem } from '../types';

export const oracleDom03Part2Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 5 : LA CLAUSE HAVING VS WHERE & RÈGLES DE FILTRAGE (51 à 65)
  // =========================================================================
  {
    id: 'fc-dom03-051',
    cardNumber: 51,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Rôle fondamental de HAVING',
    difficulty: 'easy',
    tags: ['HAVING', 'Filtrage de groupes', 'Rôle'],
    front: {
      question: 'Quel est le rôle précis de la clause HAVING dans une requête SQL et sur quoi ses conditions portent-elles ?',
      hint: 'Filtre-t-elle des lignes élémentaires ou des groupes déjà constitués ?',
    },
    back: {
      answer: 'La clause HAVING sert exclusivement à filtrer les GROUPES formés après agrégation, sur la base de critères portant sur les résultats des fonctions de groupe ou sur les colonnes de regroupement.',
      explanation: 'Alors que WHERE filtre chaque ligne individuellement avant la mise en groupe, HAVING intervient après le regroupement pour déterminer quels groupes entiers seront conservés dans le jeu de résultats.',
      codeSnippet: 'SELECT department_id, COUNT(*), AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING COUNT(*) >= 5; -- Ne garde que les départements d\'au moins 5 personnes',
      examTrap: 'Ne dites jamais que HAVING filtre les lignes : il filtre les groupes. Une condition éliminée par HAVING supprime tout le groupe associé.',
      ruleRef: 'Oracle SQL Reference - The HAVING Clause',
    },
  },
  {
    id: 'fc-dom03-052',
    cardNumber: 52,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'WHERE vs HAVING',
    difficulty: 'easy',
    tags: ['WHERE vs HAVING', 'Performance', 'Ordre d\'exécution'],
    front: {
      question: 'Quelles sont les deux différences majeures entre la clause WHERE et la clause HAVING ?',
      hint: 'Pensez au moment d\'évaluation et au droit d\'utiliser des fonctions d\'agrégation.',
    },
    back: {
      answer: '1) WHERE s\'exécute AVANT le groupement et n\'accepte JAMAIS de fonction de groupe. 2) HAVING s\'exécute APRÈS le groupement et accepte pleinement les fonctions de groupe.',
      explanation: 'WHERE filtre les lignes individuelles de la table source. HAVING filtre les lignes agrégées résultant du GROUP BY. Filtrer dans WHERE est toujours plus performant car cela réduit le volume de données à grouper en mémoire.',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nWHERE department_id IN (10, 20, 30) -- WHERE : filtre les lignes en amont\nGROUP BY department_id\nHAVING AVG(salary) > 5000;          -- HAVING : filtre les groupes calculés',
      examTrap: 'Ne mettez jamais un filtre de colonne individuelle dans HAVING s\'il peut être mis dans WHERE : c\'est sous-optimal et fréquemment critiqué dans les questions d\'architecture de l\'examen.',
      ruleRef: 'SQL Processing - WHERE vs HAVING Comparison',
    },
  },
  {
    id: 'fc-dom03-053',
    cardNumber: 53,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Position syntaxique de HAVING',
    difficulty: 'hard',
    tags: ['HAVING', 'Syntaxe', 'Position', 'Flexibilité Oracle'],
    front: {
      question: 'Sous Oracle SQL, la clause HAVING doit-elle OBLIGATOIREMENT être écrite APRÈS la clause GROUP BY, ou peut-elle être écrite AVANT ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nHAVING AVG(salary) > 6000\nGROUP BY department_id;',
      hint: 'Oracle est-il plus souple que la recommandation standard de style ?',
    },
    back: {
      answer: 'La clause HAVING peut légalement être placée AVANT ou APRÈS la clause GROUP BY sous Oracle SQL.',
      explanation: 'Bien que la bonne pratique universelle soit d\'écrire WHERE -> GROUP BY -> HAVING, l\'analyseur syntaxique d\'Oracle accepte sans aucune erreur HAVING avant GROUP BY (et même HAVING sans GROUP BY pour un groupe global).',
      codeSnippet: '-- Les deux syntaxes suivantes sont 100% valides sous Oracle :\n\n-- Ordre usuel :\nSELECT department_id, AVG(salary) FROM employees GROUP BY department_id HAVING AVG(salary) > 6000;\n\n-- Ordre inversé (valide !) :\nSELECT department_id, AVG(salary) FROM employees HAVING AVG(salary) > 6000 GROUP BY department_id;',
      examTrap: 'Question piège typique à l\'examen 1Z0-071 : un candidat voit HAVING avant GROUP BY et coche "Compilation Error". C\'est un piège ! La requête s\'exécute parfaitement.',
      ruleRef: 'Oracle SQL Parsing Rules - Placement of HAVING and GROUP BY',
    },
  },
  {
    id: 'fc-dom03-054',
    cardNumber: 54,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Agrégat dans HAVING absent du SELECT',
    difficulty: 'medium',
    tags: ['HAVING', 'SELECT', 'Agrégat non projeté'],
    front: {
      question: 'Une fonction d\'agrégation utilisée dans la clause HAVING (ex: HAVING MAX(salary) > 10000) doit-elle obligatoirement être sélectionnée dans la clause SELECT ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING MAX(salary) > 10000;',
      hint: 'Est-on obligé d\'afficher tout ce qu\'on évalue comme condition de filtre ?',
    },
    back: {
      answer: 'NON, absolument pas ! Les fonctions d\'agrégation de la clause HAVING n\'ont aucune obligation de figurer dans la clause SELECT.',
      explanation: 'Oracle calcule en mémoire les agrégats requis par la clause HAVING pour décider du sort de chaque groupe, qu\'ils soient ou non projetés dans la liste de résultat final.',
      codeSnippet: 'SELECT department_id\nFROM employees\nGROUP BY department_id\nHAVING COUNT(*) > 10 AND SUM(salary) > 50000;\n-- Affiche seulement department_id mais filtre sur COUNT(*) et SUM(salary) !',
      examTrap: 'L\'affirmation "Une fonction de groupe présente dans HAVING doit figurer dans le SELECT" est FAUSSE.',
      ruleRef: 'SQL Aggregate Processing - HAVING Filter Projection',
    },
  },
  {
    id: 'fc-dom03-055',
    cardNumber: 55,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Colonne simple dans HAVING',
    difficulty: 'hard',
    tags: ['HAVING', 'Colonne non agrégée', 'GROUP BY', 'Validité'],
    front: {
      question: 'Peut-on utiliser une colonne simple (non agrégée) dans la clause HAVING, comme dans : HAVING department_id = 50 ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING department_id = 50;',
      hint: 'La colonne est-elle présente dans la clause GROUP BY ?',
    },
    back: {
      answer: 'OUI, à la condition expresse que cette colonne figure dans la clause GROUP BY !',
      explanation: 'Si la colonne fait partie des critères de regroupement (GROUP BY), elle a une valeur unique et déterministe pour chaque groupe et peut donc être testée dans HAVING. Si elle n\'est pas dans le GROUP BY, cela lève ORA-00979.',
      codeSnippet: '-- VALIDE (department_id est dans GROUP BY) :\nSELECT department_id, COUNT(*) FROM employees GROUP BY department_id HAVING department_id = 50;\n\n-- MAIS DÉCONSEILLÉ : mieux vaut écrire WHERE department_id = 50 pour filtrer avant !',
      examTrap: 'Bien que valide sous Oracle, le test d\'une colonne simple sans agrégat dans HAVING est inefficace. À l\'examen, sachez cependant que la requête est syntaxiquement acceptée.',
      ruleRef: 'SQL Reference - Non-aggregate Columns in HAVING Clause',
    },
  },
  {
    id: 'fc-dom03-056',
    cardNumber: 56,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'HAVING sans clause GROUP BY',
    difficulty: 'hard',
    tags: ['HAVING', 'Sans GROUP BY', 'Groupe global', 'Cas particulier'],
    front: {
      question: 'Peut-on utiliser la clause HAVING dans une requête qui ne contient PAS de clause GROUP BY ?',
      codeSnippet: 'SELECT AVG(salary)\nFROM employees\nHAVING COUNT(*) > 50;',
      hint: 'Une table entière sans GROUP BY peut-elle être traitée comme un groupe unique filtrable ?',
    },
    back: {
      answer: 'OUI ! C\'est parfaitement légal sous Oracle SQL. La table entière constitue un groupe unique.',
      explanation: 'Si la condition dans HAVING (ex: COUNT(*) > 50) est vraie pour la table entière, le résultat de la fonction d\'agrégation (1 ligne) est retourné. Si la condition est fausse, aucune ligne n\'est retournée (0 ligne).',
      codeSnippet: 'SELECT AVG(salary), COUNT(*)\nFROM employees\nHAVING AVG(salary) > 5000;\n-- Si la moyenne générale des employés dépasse 5000, renvoie 1 ligne, sinon aucune ligne.',
      examTrap: 'Beaucoup de développeurs croient que HAVING exige impérativement GROUP BY. À l\'examen 1Z0-071, souvenez-vous que HAVING sans GROUP BY est tout à fait valide !',
      ruleRef: 'Oracle SQL Reference - HAVING Clause without GROUP BY',
    },
  },
  {
    id: 'fc-dom03-057',
    cardNumber: 57,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Alias dans HAVING',
    difficulty: 'medium',
    tags: ['Alias', 'HAVING', 'ORA-00904', 'Scope'],
    front: {
      question: 'Peut-on réutiliser un alias de colonne défini dans le SELECT à l\'intérieur de la clause HAVING sous Oracle ?',
      codeSnippet: 'SELECT department_id, AVG(salary) AS sal_moyen\nFROM employees\nGROUP BY department_id\nHAVING sal_moyen > 5000;',
      hint: 'La clause HAVING est-elle évaluée avant ou après la clause SELECT ?',
    },
    back: {
      answer: 'NON, c\'est interdit et produit ORA-00904: "SAL_MOYEN": invalid identifier.',
      explanation: 'La clause HAVING est évaluée AVANT la clause SELECT. Les alias créés dans le SELECT n\'existent donc pas encore lors de l\'exécution du filtre HAVING. Il faut réécrire la fonction complète : HAVING AVG(salary) > 5000.',
      codeSnippet: '-- ERREUR ORA-00904 :\nSELECT department_id, AVG(salary) AS sal_moyen FROM employees GROUP BY department_id HAVING sal_moyen > 5000;\n\n-- VALIDE :\nSELECT department_id, AVG(salary) AS sal_moyen FROM employees GROUP BY department_id HAVING AVG(salary) > 5000;',
      examTrap: 'Règle générale d\'or : les alias de colonnes du SELECT ne peuvent JAMAIS être utilisés dans WHERE, GROUP BY ou HAVING. Seul ORDER BY y a accès !',
      ruleRef: 'SQL Order of Execution - Scope of SELECT Aliases',
    },
  },
  {
    id: 'fc-dom03-058',
    cardNumber: 58,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Sous-requête dans la clause HAVING',
    difficulty: 'medium',
    tags: ['HAVING', 'Sous-requête', 'Comparaison', 'Agrégats'],
    front: {
      question: 'La clause HAVING peut-elle comparer le résultat d\'une fonction d\'agrégation à une sous-requête ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING AVG(salary) > (SELECT AVG(salary) FROM employees);',
      hint: 'Pensez à trouver les départements dont la moyenne est supérieure à la moyenne de toute l\'entreprise.',
    },
    back: {
      answer: 'Oui, c\'est une construction standard et très puissante en SQL.',
      explanation: 'Pour chaque groupe évalué par HAVING, sa moyenne AVG(salary) est comparée au résultat de la sous-requête scalaire retournant la moyenne globale de la table.',
      codeSnippet: 'SELECT department_id, ROUND(AVG(salary), 2) AS moyenne_dept\nFROM employees\nGROUP BY department_id\nHAVING AVG(salary) > (SELECT AVG(salary) FROM employees);',
      examTrap: 'Assurez-vous que la sous-requête renvoie une seule valeur (scalaire) si un opérateur de comparaison simple (>, =, <) est employé, sinon utilisez ALL/ANY.',
      ruleRef: 'Subqueries in the HAVING Clause',
    },
  },
  {
    id: 'fc-dom03-059',
    cardNumber: 59,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Opérateurs logiques AND/OR dans HAVING',
    difficulty: 'easy',
    tags: ['HAVING', 'AND', 'OR', 'Conditions multiples'],
    front: {
      question: 'Peut-on combiner plusieurs conditions dans une même clause HAVING en utilisant les opérateurs logiques AND, OR et NOT ?',
      codeSnippet: 'SELECT department_id, COUNT(*), MAX(salary)\nFROM employees\nGROUP BY department_id\nHAVING COUNT(*) > 5 AND (MAX(salary) > 10000 OR MIN(salary) < 3000);',
      hint: 'HAVING obéit-il aux mêmes règles booléennes que WHERE ?',
    },
    back: {
      answer: 'Oui ! La clause HAVING évalue des expressions booléennes composées exactement comme la clause WHERE.',
      explanation: 'Vous pouvez enchaîner autant de critères que nécessaire avec AND, OR, NOT, et parenthéser vos conditions pour définir l\'ordre d\'évaluation prioritaire.',
      codeSnippet: 'SELECT department_id, COUNT(*), AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING COUNT(*) >= 3 AND NOT (AVG(salary) < 4000);',
      examTrap: 'Attention à la priorité des opérateurs : AND prévaut sur OR, tout comme dans la clause WHERE.',
      ruleRef: 'Boolean Logic in HAVING Predicates',
    },
  },
  {
    id: 'fc-dom03-060',
    cardNumber: 60,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Condition HAVING toujours fausse',
    difficulty: 'medium',
    tags: ['HAVING', '1=2', 'Résultat vide', 'Lignes retournées'],
    front: {
      question: 'Que renvoie une requête avec GROUP BY et une clause HAVING dont la condition est toujours fausse (ex: HAVING 1 = 2) ?',
      codeSnippet: 'SELECT department_id, COUNT(*)\nFROM employees\nGROUP BY department_id\nHAVING 1 = 2;',
      hint: 'Combien de lignes le client reçoit-il ?',
    },
    back: {
      answer: 'Elle retourne 0 ligne ("no rows selected").',
      explanation: 'Tous les groupes formés par le GROUP BY sont testés contre la condition 1 = 2. Comme la condition s\'évalue à FALSE pour chacun d\'eux, tous les groupes sont éliminés.',
      codeSnippet: 'SELECT department_id, COUNT(*)\nFROM employees\nGROUP BY department_id\nHAVING 1 = 2;\n-- Résultat : Aucune ligne sélectionnée.',
      examTrap: 'Ne confondez pas avec COUNT(*) sans GROUP BY : ici, avec GROUP BY, si tous les groupes sont rejetés par HAVING, il n\'y a PAS de ligne contenant 0 ; il n\'y a AUCUNE ligne du tout !',
      ruleRef: 'Filter Behavior on Aggregated Groups',
    },
  },
  {
    id: 'fc-dom03-061',
    cardNumber: 61,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'HAVING avec fonction scalaire',
    difficulty: 'medium',
    tags: ['HAVING', 'Scalaire', 'LENGTH', 'ROUND', 'Agrégats'],
    front: {
      question: 'Peut-on envelopper une fonction d\'agrégation dans une fonction scalaire au sein de la clause HAVING (ex: HAVING ROUND(AVG(salary)) > 5000) ?',
      hint: 'Une fonction scalaire peut-elle prendre le résultat d\'un agrégat en paramètre ?',
    },
    back: {
      answer: 'Oui, c\'est parfaitement légal sous Oracle SQL.',
      explanation: 'Oracle calcule d\'abord l\'agrégat pour le groupe, puis applique la fonction scalaire (ROUND, TRUNC, LENGTH, etc.) sur cette valeur résultante avant d\'évaluer la condition de comparaison.',
      codeSnippet: 'SELECT department_id, ROUND(AVG(salary), 2)\nFROM employees\nGROUP BY department_id\nHAVING ROUND(AVG(salary), -3) >= 10000;',
      examTrap: 'C\'est totalement permis, à condition de ne pas violer la règle des colonnes simples non groupées.',
      ruleRef: 'SQL Language Reference - Functions in HAVING Expressions',
    },
  },
  {
    id: 'fc-dom03-062',
    cardNumber: 62,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'HAVING COUNT(DISTINCT)',
    difficulty: 'medium',
    tags: ['HAVING', 'COUNT', 'DISTINCT', 'Diversité'],
    front: {
      question: 'Comment filtrer les départements qui emploient des personnes exerçant au moins 3 métiers différents ?',
      codeSnippet: 'SELECT department_id\nFROM employees\nGROUP BY department_id\nHAVING ??? >= 3;',
      hint: 'Comment compter uniquement les métiers uniques dans chaque département ?',
    },
    back: {
      answer: 'En utilisant COUNT(DISTINCT job_id) >= 3 dans la clause HAVING.',
      explanation: 'La clause HAVING COUNT(DISTINCT job_id) >= 3 compte le nombre d\'intitulés de poste uniques au sein de chaque groupe de département et élimine ceux qui ont moins de 3 postes distincts.',
      codeSnippet: 'SELECT department_id, COUNT(DISTINCT job_id) AS nb_metiers\nFROM employees\nGROUP BY department_id\nHAVING COUNT(DISTINCT job_id) >= 3;',
      examTrap: 'COUNT(job_id) >= 3 compterait le nombre total de salariés ayant un métier non-null, ce qui ne vérifie pas qu\'ils exercent des métiers différents !',
      ruleRef: 'HAVING with COUNT DISTINCT Specifications',
    },
  },
  {
    id: 'fc-dom03-063',
    cardNumber: 63,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Clause HAVING avec BETWEEN',
    difficulty: 'easy',
    tags: ['HAVING', 'BETWEEN', 'Intervalles', 'Agrégats'],
    front: {
      question: 'L\'opérateur BETWEEN peut-il s\'appliquer à une fonction de groupe dans la clause HAVING ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING AVG(salary) BETWEEN 5000 AND 10000;',
      hint: 'BETWEEN est-il réservé aux colonnes simples ?',
    },
    back: {
      answer: 'Oui, tout à fait. L\'opérateur BETWEEN est parfaitement applicable aux agrégats dans la clause HAVING.',
      explanation: 'BETWEEN vérifie que le résultat agrégé du groupe se situe de manière inclusive dans l\'intervalle spécifié [borne_inf, borne_sup].',
      codeSnippet: 'SELECT department_id, COUNT(*)\nFROM employees\nGROUP BY department_id\nHAVING COUNT(*) BETWEEN 2 AND 5;',
      examTrap: 'Rappelez-vous toujours la règle de BETWEEN : la borne inférieure doit précéder la borne supérieure, sinon la condition s\'évalue à FALSE.',
      ruleRef: 'Operators in HAVING Clauses - Range Comparisons',
    },
  },
  {
    id: 'fc-dom03-064',
    cardNumber: 64,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'HAVING avec IN (liste d\'agrégats)',
    difficulty: 'medium',
    tags: ['HAVING', 'IN', 'Sous-requête', 'Agrégats'],
    front: {
      question: 'Peut-on utiliser l\'opérateur IN avec une fonction d\'agrégation dans HAVING pour la comparer à une liste de valeurs ou à une sous-requête ?',
      codeSnippet: 'SELECT department_id, MAX(salary)\nFROM employees\nGROUP BY department_id\nHAVING MAX(salary) IN (SELECT salary FROM special_salaries);',
      hint: 'Une fonction de groupe produit une valeur compatible avec les opérateurs d\'appartenance.',
    },
    back: {
      answer: 'Oui, les opérateurs IN et NOT IN s\'appliquent tout à fait aux résultats des fonctions d\'agrégation dans HAVING.',
      explanation: 'Oracle compare la valeur agrégée du groupe à chaque élément de la liste ou au résultat de la sous-requête.',
      codeSnippet: 'SELECT department_id, COUNT(*)\nFROM employees\nGROUP BY department_id\nHAVING COUNT(*) IN (1, 5, 10);',
      examTrap: 'Si la sous-requête avec NOT IN retourne ne serait-ce qu\'une seule valeur NULL, la condition globale s\'évalue à UNKNOWN et aucun groupe n\'est retourné !',
      ruleRef: 'Membership Operators in HAVING Clauses',
    },
  },
  {
    id: 'fc-dom03-065',
    cardNumber: 65,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Ordre logique complet de traitement',
    difficulty: 'medium',
    tags: ['Cycle de vie', 'Ordre logique', 'FROM', 'WHERE', 'SELECT'],
    front: {
      question: 'Quel est l\'ordre logique rigoureux de traitement des 6 clauses d\'une requête SQL exécutée par le moteur Oracle ?',
      hint: 'De FROM jusqu\'à ORDER BY.',
    },
    back: {
      answer: '1. FROM (+ JOIN) -> 2. WHERE -> 3. GROUP BY -> 4. HAVING -> 5. SELECT -> 6. ORDER BY.',
      explanation: 'Cet ordre logique explique pourquoi : 1) WHERE ne peut pas utiliser d\'agrégat (GROUP BY n\'a pas eu lieu). 2) GROUP BY et HAVING ne peuvent pas utiliser les alias du SELECT (SELECT n\'a pas encore été exécuté). 3) ORDER BY peut tout utiliser (il s\'exécute en dernier).',
      codeSnippet: '-- 1. FROM employees\n-- 2. WHERE salary > 2000\n-- 3. GROUP BY department_id\n-- 4. HAVING COUNT(*) > 2\n-- 5. SELECT department_id, AVG(salary) AS moy\n-- 6. ORDER BY moy DESC;',
      examTrap: 'Comprendre par cœur cet ordre logique permet de résoudre instantanément plus de 15 questions à l\'examen 1Z0-071.',
      ruleRef: 'Oracle SQL Statement Logical Execution Sequence',
    },
  },

  // =========================================================================
  // SECTION 6 : IMBRICATION DES FONCTIONS DE GROUPE & CONTRAINTES (66 à 75)
  // =========================================================================
  {
    id: 'fc-dom03-066',
    cardNumber: 66,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Imbrication maximale d\'agrégats',
    difficulty: 'medium',
    tags: ['Imbrication', 'Limite 2 niveaux', 'Règle Oracle', 'ORA-00935'],
    front: {
      question: 'Combien de niveaux d\'imbrication de fonctions de groupe Oracle autorise-t-il au maximum dans une même expression ?',
      codeSnippet: 'SELECT MAX(AVG(salary)) FROM employees GROUP BY department_id;',
      hint: 'Peut-on faire SUM(MAX(AVG(salary))) ?',
    },
    back: {
      answer: 'Deux (2) niveaux d\'imbrication au maximum.',
      explanation: 'Oracle autorise l\'imbrication d\'une fonction de groupe dans une autre (ex: MAX(AVG(salary))). Tenter d\'ajouter un troisième niveau (ex: SUM(MAX(AVG(salary)))) lève l\'erreur ORA-00935: group function is nested too deeply.',
      codeSnippet: '-- VALIDE (2 niveaux) :\nSELECT MAX(AVG(salary)) FROM employees GROUP BY department_id;\n\n-- ERREUR (3 niveaux d\'agrégats) : ORA-00935\nSELECT SUM(MAX(AVG(salary))) FROM employees GROUP BY department_id;',
      examTrap: 'Attention : une fonction scalaire comme ROUND(MAX(AVG(salary)), 2) est parfaitement VALIDE car ROUND est scalaire et ne compte pas comme un niveau de fonction de groupe.',
      ruleRef: 'Oracle SQL Reference - Nesting Group Functions Limit',
    },
  },
  {
    id: 'fc-dom03-067',
    cardNumber: 67,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Obligation de GROUP BY avec imbrication',
    difficulty: 'hard',
    tags: ['Imbrication', 'GROUP BY obligatoire', 'ORA-00978'],
    front: {
      question: 'L\'utilisation d\'une fonction de groupe imbriquée (comme MAX(AVG(salary))) exige-t-elle OBLIGATOIREMENT une clause GROUP BY ?',
      codeSnippet: 'SELECT MAX(AVG(salary)) FROM employees;',
      hint: 'Sur quoi le niveau intérieur (AVG) doit-il calculer ses groupes ?',
    },
    back: {
      answer: 'OUI, impérativement ! Omettre la clause GROUP BY lors d\'une imbrication d\'agrégats déclenche l\'erreur ORA-00978: nested group function without GROUP BY.',
      explanation: 'La fonction intérieure a besoin d\'une granularité de groupe pour produire un ensemble de sous-moyennes, sur lesquelles la fonction extérieure pourra ensuite extraire le maximum.',
      codeSnippet: '-- ERREUR (ORA-00978) :\nSELECT MAX(AVG(salary)) FROM employees;\n\n-- VALIDE :\nSELECT MAX(AVG(salary)) FROM employees GROUP BY department_id;',
      examTrap: 'Chaque fois que vous voyez deux fonctions de groupe imbriquées, vérifiez immédiatement la présence d\'une clause GROUP BY !',
      ruleRef: 'Oracle Error Messages - ORA-00978',
    },
  },
  {
    id: 'fc-dom03-068',
    cardNumber: 68,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Colonnes interdites avec fonctions imbriquées',
    difficulty: 'hard',
    tags: ['Imbrication', 'SELECT', 'ORA-00937', 'Interdiction stricte'],
    front: {
      question: 'Pourquoi la requête suivante échoue-t-elle sous Oracle SQL ?',
      codeSnippet: 'SELECT department_id, MAX(AVG(salary))\nFROM employees\nGROUP BY department_id;',
      hint: 'Combien de lignes MAX(AVG(salary)) produit-il ?',
    },
    back: {
      answer: 'Elle échoue avec ORA-00937 car dès lors qu\'une fonction de groupe est imbriquée, AUCUNE autre colonne ou expression (même présente dans le GROUP BY) ne peut figurer dans le SELECT.',
      explanation: 'AVG(salary) calcule la moyenne de chaque département (N lignes). Mais le MAX extérieur réduit ces N résultats à UNE SEULE valeur scalaire globale. On ne peut pas afficher les identifiants des départements à côté d\'une valeur scalaire unique sans sous-requête.',
      codeSnippet: '-- ERREUR ORA-00937 :\nSELECT department_id, MAX(AVG(salary)) FROM employees GROUP BY department_id;\n\n-- VALIDE :\nSELECT MAX(AVG(salary)) FROM employees GROUP BY department_id;',
      examTrap: 'C\'est l\'un des pièges les plus célèbres du 1Z0-071 : même si department_id est dans le GROUP BY, il est INTERDIT de le sélectionner avec MAX(AVG(...)) !',
      ruleRef: 'Single-Group Group Function Restriction on Nested Aggregates',
    },
  },
  {
    id: 'fc-dom03-069',
    cardNumber: 69,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Trouver la clé d\'un agrégat imbriqué',
    difficulty: 'hard',
    tags: ['Sous-requête', 'HAVING', 'Imbrication', 'Pattern examen'],
    front: {
      question: 'Comment afficher à la fois le department_id ET la valeur de sa moyenne pour le département qui possède la moyenne de salaire la plus élevée ?',
      hint: 'Utilisez la clause HAVING combinée à une sous-requête.',
    },
    back: {
      answer: 'En filtrant dans la clause HAVING sur la valeur maximale calculée par une sous-requête.',
      explanation: 'Puisqu\'on ne peut pas afficher department_id et MAX(AVG(salary)) ensemble directement, on sélectionne department_id et AVG(salary), et on filtre via HAVING AVG(salary) = (SELECT MAX(AVG(salary)) FROM employees GROUP BY department_id).',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING AVG(salary) = (\n  SELECT MAX(AVG(salary))\n  FROM employees\n  GROUP BY department_id\n);',
      examTrap: 'Ce motif de requête (query pattern) est un grand classique des questions de sélection à choix multiples.',
      ruleRef: 'SQL Query Patterns - Department with Highest Average',
    },
  },
  {
    id: 'fc-dom03-070',
    cardNumber: 70,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Imbrication avec plusieurs colonnes de GROUP BY',
    difficulty: 'medium',
    tags: ['Multi-colonnes', 'Imbrication', 'GROUP BY'],
    front: {
      question: 'Peut-on utiliser MAX(COUNT(*)) avec une clause GROUP BY comportant plusieurs colonnes (ex: GROUP BY department_id, job_id) ?',
      codeSnippet: 'SELECT MAX(COUNT(*))\nFROM employees\nGROUP BY department_id, job_id;',
      hint: 'Le nombre de colonnes dans GROUP BY influence-t-il le droit d\'imbriquer ?',
    },
    back: {
      answer: 'Oui, tout à fait. La fonction interne COUNT(*) est calculée pour chaque couple (dept, job), puis la fonction externe MAX en extrait le plus grand effectif.',
      explanation: 'La requête retourne le nombre maximal d\'employés partageant le même métier au sein d\'un même département dans toute l\'entreprise.',
      codeSnippet: 'SELECT MAX(COUNT(*)) AS plus_gros_effectif_metier\nFROM employees\nGROUP BY department_id, job_id;',
      examTrap: 'Rappel : là encore, interdiction formelle d\'ajouter department_id ou job_id dans la clause SELECT.',
      ruleRef: 'Nested Aggregates with Composite Group Keys',
    },
  },
  {
    id: 'fc-dom03-071',
    cardNumber: 71,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Imbrication dans la clause HAVING',
    difficulty: 'hard',
    tags: ['HAVING', 'Imbrication', 'ORA-00978', 'Restrictions'],
    front: {
      question: 'Peut-on placer directement une fonction de groupe imbriquée (comme MAX(AVG(salary))) dans la clause HAVING de la requête principale sans sous-requête ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nGROUP BY department_id\nHAVING AVG(salary) = MAX(AVG(salary));',
      hint: 'Une clause HAVING peut-elle évaluer une fonction imbriquée sur l\'ensemble des groupes à ce niveau ?',
    },
    back: {
      answer: 'NON ! Cela produit une erreur de compilation SQL sous Oracle.',
      explanation: 'La fonction extérieure MAX() nécessite de parcourir l\'ensemble des résultats agrégés de tous les groupes, ce qui n\'est pas permis en ligne dans la clause HAVING sans passer par une sous-requête.',
      codeSnippet: '-- INCORRECT :\nSELECT department_id FROM employees GROUP BY department_id HAVING AVG(salary) = MAX(AVG(salary));\n\n-- CORRECT (avec sous-requête) :\nSELECT department_id FROM employees GROUP BY department_id HAVING AVG(salary) = (SELECT MAX(AVG(salary)) FROM employees GROUP BY department_id);',
      examTrap: 'À l\'examen, toute tentative d\'écrire HAVING expr = MAX(AVG(...)) sans sous-requête est systématiquement une option invalide.',
      ruleRef: 'Oracle SQL Reference - Predicate Restrictions in HAVING',
    },
  },
  {
    id: 'fc-dom03-072',
    cardNumber: 72,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'MIN(MAX(col))',
    difficulty: 'medium',
    tags: ['MIN', 'MAX', 'Imbrication', 'Cas pratique'],
    front: {
      question: 'Que calcule l\'expression SELECT MIN(MAX(salary)) FROM employees GROUP BY department_id ?',
      hint: 'Décomposez de l\'intérieur vers l\'extérieur.',
    },
    back: {
      answer: 'Elle identifie d\'abord le salaire maximum de chaque département, puis extrait le plus petit parmi tous ces salaires maximaux.',
      explanation: 'MAX(salary) calcule le plafond salarial de chaque département. MIN() sélectionne ensuite le département dont le plafond salarial est le plus bas de l\'entreprise.',
      codeSnippet: 'SELECT MIN(MAX(salary)) AS salaire_max_le_plus_bas\nFROM employees\nGROUP BY department_id;',
      examTrap: 'Ce n\'est PAS équivalent au salaire minimum global MIN(salary) de la table !',
      ruleRef: 'Composite Nesting Semantics in SQL',
    },
  },
  {
    id: 'fc-dom03-073',
    cardNumber: 73,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Imbrication et clause ORDER BY',
    difficulty: 'medium',
    tags: ['ORDER BY', 'Imbrication', 'Nombre de lignes'],
    front: {
      question: 'Une clause ORDER BY est-elle utile ou sensée dans une requête qui sélectionne uniquement MAX(AVG(salary)) FROM employees GROUP BY department_id ?',
      hint: 'Combien de lignes de résultat une fonction imbriquée retourne-t-elle ?',
    },
    back: {
      answer: 'Non, elle n\'a aucun sens car la requête ne retourne qu\'une seule ligne de résultat.',
      explanation: 'Bien qu\'ajouter ORDER BY 1 ne provoque pas d\'erreur de syntaxe, trier un résultat composé d\'une unique valeur scalaire est totalement superflu.',
      codeSnippet: 'SELECT MAX(AVG(salary))\nFROM employees\nGROUP BY department_id;\n-- Retourne exactement 1 ligne de résultat.',
      examTrap: 'Si vous voyez ORDER BY dans une question avec agrégat imbriqué, la requête est syntaxiquement valide mais le tri est neutre.',
      ruleRef: 'Single-Row Result Set Processing',
    },
  },
  {
    id: 'fc-dom03-074',
    cardNumber: 74,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Imbrication de DISTINCT avec agrégats imbriqués',
    difficulty: 'hard',
    tags: ['DISTINCT', 'Imbrication', 'Syntaxe'],
    front: {
      question: 'L\'expression MAX(AVG(DISTINCT salary)) est-elle autorisée sous Oracle SQL ?',
      codeSnippet: 'SELECT MAX(AVG(DISTINCT salary))\nFROM employees\nGROUP BY department_id;',
      hint: 'DISTINCT peut-il être appliqué au niveau le plus interne ?',
    },
    back: {
      answer: 'Oui, c\'est légal. DISTINCT s\'applique à la fonction interne AVG.',
      explanation: 'Pour chaque département, AVG ne calcule la moyenne que sur les salaires uniques du département, et MAX extrait ensuite la plus haute de ces moyennes.',
      codeSnippet: 'SELECT MAX(AVG(DISTINCT salary))\nFROM employees\nGROUP BY department_id;',
      examTrap: 'En revanche, MAX(DISTINCT AVG(salary)) est redondant puisque MAX d\'un ensemble avec ou sans doublons donne la même valeur.',
      ruleRef: 'DISTINCT Keyword in Nested Aggregate Expressions',
    },
  },
  {
    id: 'fc-dom03-075',
    cardNumber: 75,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Imbrication de COUNT(*)',
    difficulty: 'medium',
    tags: ['COUNT(*)', 'AVG', 'Imbrication', 'Moyenne d\'effectif'],
    front: {
      question: 'Que calcule la requête : SELECT AVG(COUNT(*)) FROM employees GROUP BY department_id ?',
      hint: 'Calcule l\'effectif de chaque groupe, puis...',
    },
    back: {
      answer: 'Elle calcule l\'effectif moyen par département dans l\'entreprise (nombre total d\'employés divisé par le nombre de groupes de départements).',
      explanation: 'COUNT(*) compte les employés dans chaque département. AVG() fait ensuite la moyenne de ces effectifs départementaux.',
      codeSnippet: 'SELECT ROUND(AVG(COUNT(*)), 2) AS effectif_moyen_par_dept\nFROM employees\nGROUP BY department_id;',
      examTrap: 'C\'est une façon très élégante et fréquente en certification de calculer des ratios sans sous-requête complexe.',
      ruleRef: 'Practical Applications of Nested Group Functions',
    },
  },

  // =========================================================================
  // SECTION 7 : EXTENSIONS DE GROUPEMENT : ROLLUP, CUBE, GROUPING SETS (76 à 88)
  // =========================================================================
  {
    id: 'fc-dom03-076',
    cardNumber: 76,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Extension ROLLUP',
    difficulty: 'easy',
    tags: ['ROLLUP', 'Sous-totaux', 'Hiérarchie', 'Grand total'],
    front: {
      question: 'Quel est l\'objectif de l\'extension GROUP BY ROLLUP(a, b, c) et quel type d\'agrégats produit-elle ?',
      hint: 'Pensez à des sous-totaux progressifs et à un grand total.',
    },
    back: {
      answer: 'ROLLUP produit des sous-totaux hiérarchiques de la droite vers la gauche, ainsi qu\'un grand total général.',
      explanation: 'ROLLUP(a, b, c) génère les niveaux de regroupement suivants : 1) (a, b, c), 2) (a, b), 3) (a), et 4) () [le grand total pour toute la table]. Pour N arguments, ROLLUP produit exactement N + 1 niveaux de regroupement.',
      codeSnippet: 'SELECT department_id, job_id, SUM(salary)\nFROM employees\nGROUP BY ROLLUP(department_id, job_id);\n-- Produit : détails (dept, job), sous-totaux par dept, et grand total général.',
      examTrap: 'Formule d\'examen essentielle : pour N colonnes dans ROLLUP, il y a N + 1 regroupements générés.',
      ruleRef: 'Oracle SQL Reference - ROLLUP Extension',
    },
  },
  {
    id: 'fc-dom03-077',
    cardNumber: 77,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Ordre des colonnes dans ROLLUP',
    difficulty: 'medium',
    tags: ['ROLLUP', 'Sensible à l\'ordre', 'Hiérarchie'],
    front: {
      question: 'L\'ordre des colonnes spécifiées dans ROLLUP a-t-il une importance sur les sous-totaux générés ? ROLLUP(a, b) est-il équivalent à ROLLUP(b, a) ?',
      hint: 'La hiérarchie s\'effectue-t-elle de gauche à droite ou symétriquement ?',
    },
    back: {
      answer: 'OUI, l\'ordre est CRUCIAL ! ROLLUP(a, b) n\'est PAS équivalent à ROLLUP(b, a).',
      explanation: 'ROLLUP(a, b) produit : (a, b), (a), et (). ROLLUP(b, a) produit : (b, a), (b), et (). Le premier fournit des sous-totaux par "a", tandis que le second fournit des sous-totaux par "b".',
      codeSnippet: '-- Sous-totaux par département :\nSELECT department_id, job_id, SUM(salary) FROM employees GROUP BY ROLLUP(department_id, job_id);\n\n-- Sous-totaux par métier :\nSELECT department_id, job_id, SUM(salary) FROM employees GROUP BY ROLLUP(job_id, department_id);',
      examTrap: 'Contrairement à un GROUP BY standard où l\'ordre des colonnes n\'a pas d\'impact sur les agrégats, dans ROLLUP l\'ordre définit la hiérarchie des sous-totaux.',
      ruleRef: 'ROLLUP Column Ordering and Hierarchy',
    },
  },
  {
    id: 'fc-dom03-078',
    cardNumber: 78,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Extension CUBE',
    difficulty: 'medium',
    tags: ['CUBE', 'Toutes combinaisons', '2^N', 'Croisement'],
    front: {
      question: 'Combien de niveaux de regroupement distincts l\'extension GROUP BY CUBE(a, b, c) génère-t-elle pour 3 colonnes ?',
      hint: 'Quelle est la formule mathématique en fonction de N ?',
    },
    back: {
      answer: 'Elle génère 8 niveaux de regroupement (formule : 2^N, soit 2^3 = 8).',
      explanation: 'CUBE produit toutes les combinaisons possibles de regroupements pour les colonnes listées : (a, b, c), (a, b), (a, c), (b, c), (a), (b), (c), et () [grand total]. Idéal pour l\'analyse multidimensionnelle OLAP.',
      codeSnippet: 'SELECT department_id, job_id, SUM(salary)\nFROM employees\nGROUP BY CUBE(department_id, job_id);\n-- 2^2 = 4 regroupements : (dept, job), (dept), (job), ()',
      examTrap: 'Mémorisez la formule pour l\'examen : ROLLUP(N) = N + 1 groupes ; CUBE(N) = 2^N groupes.',
      ruleRef: 'Oracle SQL Reference - CUBE Extension',
    },
  },
  {
    id: 'fc-dom03-079',
    cardNumber: 79,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Ordre des colonnes dans CUBE',
    difficulty: 'medium',
    tags: ['CUBE', 'Insensible à l\'ordre', 'Symétrie'],
    front: {
      question: 'L\'ordre des colonnes dans CUBE(a, b) a-t-il une influence sur l\'ensemble des regroupements produits par rapport à CUBE(b, a) ?',
      hint: 'Génère-t-on toutes les combinaisons possibles dans les deux cas ?',
    },
    back: {
      answer: 'Non, les regroupements produits sont exactement les mêmes : (a, b), (a), (b), ().',
      explanation: 'Comme CUBE est non-directionnel et génère l\'intégralité des combinaisons de l\'espace multidimensionnel, inverser les arguments ne change pas les agrégats produits.',
      codeSnippet: 'SELECT department_id, job_id, SUM(salary) FROM employees GROUP BY CUBE(department_id, job_id);\n-- Produit exactement les mêmes agrégats que CUBE(job_id, department_id).',
      examTrap: 'ROLLUP est sensible à l\'ordre des colonnes, CUBE ne l\'est pas pour l\'ensemble des groupes générés.',
      ruleRef: 'CUBE Symmetry and Combinatorial Properties',
    },
  },
  {
    id: 'fc-dom03-080',
    cardNumber: 80,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Extension GROUPING SETS',
    difficulty: 'medium',
    tags: ['GROUPING SETS', 'Sur-mesure', 'Optimisation', 'UNION ALL'],
    front: {
      question: 'À quoi sert la clause GROUP BY GROUPING SETS et quel est son avantage par rapport à de multiples requêtes combinées avec UNION ALL ?',
      hint: 'Pensez au nombre de lectures de la table sur le disque.',
    },
    back: {
      answer: 'Elle permet de spécifier exactement et sélectivement les seuls groupements désirés, en effectuant un seul passage sur les données (au lieu de plusieurs scans avec UNION ALL).',
      explanation: 'GROUP BY GROUPING SETS ((a, b), (c)) ne calcule que les groupes (a, b) et les groupes (c), sans calculer le grand total ni les sous-totaux intermédiaires.',
      codeSnippet: 'SELECT department_id, job_id, manager_id, SUM(salary)\nFROM employees\nGROUP BY GROUPING SETS (\n  (department_id, job_id),\n  (manager_id),\n  ()\n);',
      examTrap: 'Chaque élément à l\'intérieur de GROUPING SETS représente un regroupement indépendant. Pour spécifier un regroupement multi-colonnes, il faut l\'entourer de parenthèses : (department_id, job_id).',
      ruleRef: 'Oracle SQL Reference - GROUPING SETS Expression',
    },
  },
  {
    id: 'fc-dom03-081',
    cardNumber: 81,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Grand total dans GROUPING SETS',
    difficulty: 'easy',
    tags: ['GROUPING SETS', 'Grand total', 'Parenthèses vides ()'],
    front: {
      question: 'Comment demande-t-on le grand total général (table entière) dans une clause GROUPING SETS ?',
      codeSnippet: 'SELECT department_id, SUM(salary)\nFROM employees\nGROUP BY GROUPING SETS (department_id, ???);',
      hint: 'Quelle syntaxe représente le groupe vide ?',
    },
    back: {
      answer: 'En ajoutant une paire de parenthèses vides : ().',
      explanation: 'La syntaxe () désigne un groupe sans aucune colonne de partitionnement, ce qui correspond au grand total pour l\'ensemble des lignes.',
      codeSnippet: 'SELECT department_id, SUM(salary)\nFROM employees\nGROUP BY GROUPING SETS (department_id, ());\n-- Produit les totaux par département PLUS le grand total général.',
      examTrap: 'Ne mettez pas NULL entre parenthèses : (NULL) créerait un regroupement sur la valeur littérale NULL, alors que () crée le grand total.',
      ruleRef: 'Syntax for Grand Total in GROUPING SETS',
    },
  },
  {
    id: 'fc-dom03-082',
    cardNumber: 82,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Combinaison de ROLLUP et GROUPING SETS',
    difficulty: 'hard',
    tags: ['GROUPING SETS', 'ROLLUP', 'Combinaison'],
    front: {
      question: 'Peut-on combiner ROLLUP ou CUBE à l\'intérieur d\'une clause GROUPING SETS ?',
      codeSnippet: 'SELECT department_id, job_id, manager_id, SUM(salary)\nFROM employees\nGROUP BY GROUPING SETS (ROLLUP(department_id, job_id), manager_id);',
      hint: 'GROUPING SETS accepte-t-il d\'autres spécifications de regroupement comme éléments ?',
    },
    back: {
      answer: 'Oui, parfaitement ! GROUPING SETS peut imbriquer des spécifications ROLLUP ou CUBE.',
      explanation: 'Dans cet exemple, Oracle génère les regroupements de ROLLUP(department_id, job_id) : (dept, job), (dept), () ; PLUS le regroupement par (manager_id).',
      codeSnippet: 'SELECT department_id, job_id, manager_id, SUM(salary)\nFROM employees\nGROUP BY GROUPING SETS (\n  ROLLUP(department_id, job_id),\n  manager_id\n);',
      examTrap: 'Cela évite les redondances tout en combinant des hiérarchies et des dimensions indépendantes.',
      ruleRef: 'Nesting Grouping Operations in SQL',
    },
  },
  {
    id: 'fc-dom03-083',
    cardNumber: 83,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'ROLLUP partiel (Composite Rollup)',
    difficulty: 'hard',
    tags: ['ROLLUP partiel', 'Colonnes fixes', 'Sous-totaux'],
    front: {
      question: 'Quels niveaux de regroupement produit la clause GROUP BY a, ROLLUP(b) ?',
      codeSnippet: 'SELECT a, b, SUM(x)\nFROM t\nGROUP BY a, ROLLUP(b);',
      hint: 'La colonne "a" est-elle toujours présente dans tous les regroupements ? Y aura-t-il un grand total sans "a" ?',
    },
    back: {
      answer: 'Elle produit uniquement (a, b) et (a). Il n\'y aura AUCUN grand total général () !',
      explanation: 'Puisque la colonne "a" se trouve en dehors du ROLLUP, elle est obligatoire et présente dans tous les sous-groupes. Les sous-groupes formés sont (a) combiné avec les niveaux de ROLLUP(b) [(b) et ()], ce qui donne : (a, b) et (a).',
      codeSnippet: 'SELECT department_id, job_id, SUM(salary)\nFROM employees\nGROUP BY department_id, ROLLUP(job_id);\n-- Donne les détails (dept, job) et les sous-totaux par dept. Pas de grand total d\'entreprise !',
      examTrap: 'Piège d\'examen très vicieux : dès qu\'une colonne est en dehors des parenthèses de ROLLUP, le grand total général de la table disparaît.',
      ruleRef: 'Partial ROLLUP Specifications',
    },
  },
  {
    id: 'fc-dom03-084',
    cardNumber: 84,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Colonnes composées dans ROLLUP (Composite Columns)',
    difficulty: 'hard',
    tags: ['Colonnes composées', 'ROLLUP', 'Parenthèses', 'Hiérarchie forcée'],
    front: {
      question: 'Dans la clause GROUP BY ROLLUP(a, (b, c)), combien de regroupements sont générés et quels sont-ils ?',
      hint: 'Les parenthèses (b, c) obligent-elles b et c à être traitées comme une seule entité indivisible ?',
    },
    back: {
      answer: 'Elle génère 3 regroupements : 1) (a, b, c), 2) (a), et 3) ().',
      explanation: 'Entourer des colonnes de parenthèses à l\'intérieur de ROLLUP forme une colonne composite. Le couple (b, c) est traité comme un bloc unitaire et ne sera jamais dissocié (il n\'y aura pas de sous-total sur (a, b) sans c).',
      codeSnippet: 'SELECT department_id, job_id, manager_id, SUM(salary)\nFROM employees\nGROUP BY ROLLUP(department_id, (job_id, manager_id));\n-- Produit : (dept, job, manager), (dept), ()',
      examTrap: 'Sans parenthèses, ROLLUP(a, b, c) aurait donné 4 regroupements. Avec (b, c), il n\'en donne que 3 !',
      ruleRef: 'Composite Columns in Grouping Functions',
    },
  },
  {
    id: 'fc-dom03-085',
    cardNumber: 85,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Valeurs NULL générées par les sous-totaux',
    difficulty: 'medium',
    tags: ['NULL', 'Sous-total', 'Indicateur', 'ROLLUP'],
    front: {
      question: 'Quelle valeur apparaît dans la colonne department_id pour la ligne représentant le grand total général généré par ROLLUP(department_id) ?',
      codeSnippet: 'SELECT department_id, SUM(salary)\nFROM employees\nGROUP BY ROLLUP(department_id);',
      hint: 'Comment Oracle matérialise-t-il l\'absence de valeur spécifique pour une dimension agrégée ?',
    },
    back: {
      answer: 'La valeur affichée est NULL.',
      explanation: 'Pour signifier qu\'une ligne représente une agrégation sur toutes les valeurs possibles d\'une colonne, Oracle insère la valeur NULL dans cette colonne.',
      codeSnippet: 'SELECT department_id, SUM(salary)\nFROM employees\nGROUP BY ROLLUP(department_id);\n-- La dernière ligne affichera : department_id = NULL | SUM(salary) = 691416',
      examTrap: 'Problème : comment savoir si ce NULL provient d\'un employé qui n\'a réellement pas de département, ou s\'il s\'agit de la ligne de sous-total ? (Réponse : grâce à la fonction GROUPING).',
      ruleRef: 'Representation of Super-aggregate NULL Values',
    },
  },
  {
    id: 'fc-dom03-086',
    cardNumber: 86,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Équivalence UNION ALL de ROLLUP',
    difficulty: 'hard',
    tags: ['ROLLUP', 'UNION ALL', 'Équivalence sémantique'],
    front: {
      question: 'La requête GROUP BY ROLLUP(a, b) est sémantiquement équivalente à l\'union de quelles requêtes avec UNION ALL ?',
      hint: 'Écrivez les 3 niveaux de groupement.',
    },
    back: {
      answer: '1) GROUP BY a, b UNION ALL 2) GROUP BY a (avec b as NULL) UNION ALL 3) agrégat sans GROUP BY (avec a et b as NULL).',
      explanation: 'ROLLUP est un raccourci syntaxique et d\'optimisation évitant d\'écrire trois requêtes distinctes assemblées par UNION ALL et scannant trois fois la table.',
      codeSnippet: '-- Équivalent manuel de ROLLUP(department_id, job_id) :\nSELECT department_id, job_id, SUM(salary) FROM employees GROUP BY department_id, job_id\nUNION ALL\nSELECT department_id, NULL, SUM(salary) FROM employees GROUP BY department_id\nUNION ALL\nSELECT NULL, NULL, SUM(salary) FROM employees;',
      examTrap: 'À l\'examen, on vous demande souvent d\'identifier la forme ROLLUP équivalente à un bloc complexe de UNION ALL.',
      ruleRef: 'SQL Transformations - ROLLUP to UNION ALL',
    },
  },
  {
    id: 'fc-dom03-087',
    cardNumber: 87,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Équivalence GROUPING SETS de ROLLUP(a, b)',
    difficulty: 'medium',
    tags: ['GROUPING SETS', 'ROLLUP', 'Réécriture'],
    front: {
      question: 'Comment réécrire GROUP BY ROLLUP(a, b) en utilisant strictement la syntaxe GROUP BY GROUPING SETS ?',
      hint: 'Listez les ensembles dans les parenthèses de GROUPING SETS.',
    },
    back: {
      answer: 'GROUP BY GROUPING SETS ((a, b), (a), ()).',
      explanation: 'Puisque ROLLUP(a, b) produit les niveaux (a, b), (a) et grand total (), spécifier ces trois mêmes ensembles dans GROUPING SETS produit exactement le même résultat.',
      codeSnippet: '-- Ces deux clauses produisent strictement le même résultat :\nGROUP BY ROLLUP(department_id, job_id)\nGROUP BY GROUPING SETS ((department_id, job_id), (department_id), ())',
      examTrap: 'Ne pas oublier les parenthèses autour de (a, b) dans GROUPING SETS, sinon "a, b" serait interprété comme deux regroupements individuels séparés : (a) et (b).',
      ruleRef: 'Equivalence between ROLLUP and GROUPING SETS',
    },
  },
  {
    id: 'fc-dom03-088',
    cardNumber: 88,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Équivalence GROUPING SETS de CUBE(a, b)',
    difficulty: 'medium',
    tags: ['GROUPING SETS', 'CUBE', 'Réécriture'],
    front: {
      question: 'Comment réécrire GROUP BY CUBE(a, b) en utilisant la syntaxe GROUP BY GROUPING SETS ?',
      hint: 'Il y a 4 combinaisons à expliciter.',
    },
    back: {
      answer: 'GROUP BY GROUPING SETS ((a, b), (a), (b), ()).',
      explanation: 'CUBE(a, b) produisant toutes les combinaisons ($2^2 = 4$), son écriture équivalente en GROUPING SETS détaille explicitement ces quatre ensembles.',
      codeSnippet: '-- Équivalence absolue :\nGROUP BY CUBE(department_id, job_id)\nGROUP BY GROUPING SETS ((department_id, job_id), (department_id), (job_id), ())',
      examTrap: 'C\'est une question de réécriture très fréquente dans l\'examen 1Z0-071.',
      ruleRef: 'Equivalence between CUBE and GROUPING SETS',
    },
  },

  // =========================================================================
  // SECTION 8 : FONCTIONS D'ASSISTANCE : GROUPING, GROUPING_ID, GROUP_ID (89 à 94)
  // =========================================================================
  {
    id: 'fc-dom03-089',
    cardNumber: 89,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonction GROUPING',
    difficulty: 'easy',
    tags: ['GROUPING', 'Valeurs 0 ou 1', 'Distinction NULL'],
    front: {
      question: 'Que renvoie la fonction GROUPING(colonne) et quelles sont ses deux seules valeurs de retour possibles ?',
      hint: 'Elle renvoie 1 si la ligne est un sous-total créé par ROLLUP/CUBE, et sinon ?',
    },
    back: {
      answer: 'Elle renvoie 1 si la colonne a été agrégée (valeur NULL générée pour un sous-total/grand total), et 0 si la valeur provient directement des données de la table.',
      explanation: 'GROUPING permet de différencier avec certitude un NULL naturel stocké dans la table d\'un NULL artificiel produit par une ligne de sous-total.',
      codeSnippet: 'SELECT \n  department_id,\n  GROUPING(department_id) AS est_sous_total,\n  SUM(salary)\nFROM employees\nGROUP BY ROLLUP(department_id);',
      examTrap: 'Mémorisez : 0 = donnée réelle de la ligne (groupe régulier) ; 1 = ligne d\'agrégation / sous-total calculé.',
      ruleRef: 'Oracle SQL Reference - GROUPING Function',
    },
  },
  {
    id: 'fc-dom03-090',
    cardNumber: 90,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'GROUPING avec CASE / DECODE pour étiqueter',
    difficulty: 'medium',
    tags: ['GROUPING', 'CASE', 'DECODE', 'Grand Total Label'],
    front: {
      question: 'Comment remplacer proprement la valeur NULL d\'une ligne de grand total par le texte \'GRAND TOTAL\' en utilisant GROUPING et CASE ?',
      hint: 'Testez si GROUPING vaut 1.',
    },
    back: {
      answer: 'CASE WHEN GROUPING(department_id) = 1 THEN \'GRAND TOTAL\' ELSE TO_CHAR(department_id) END.',
      explanation: 'Si GROUPING renvoie 1, on affiche le libellé \'GRAND TOTAL\'. Sinon, on affiche la valeur convertie de la colonne.',
      codeSnippet: 'SELECT \n  CASE \n    WHEN GROUPING(department_id) = 1 THEN \'GRAND TOTAL\'\n    ELSE TO_CHAR(department_id)\n  END AS departement,\n  SUM(salary) AS total_salaires\nFROM employees\nGROUP BY ROLLUP(department_id);',
      examTrap: 'Pourquoi ne pas utiliser NVL(TO_CHAR(department_id), \'GRAND TOTAL\') ? Parce que si un employé n\'a pas de département (NULL réel dans la table), NVL lui attribuerait à tort le label "GRAND TOTAL" ! Seul GROUPING évite cette anomalie.',
      ruleRef: 'Best Practices - Using GROUPING for Reporting Labels',
    },
  },
  {
    id: 'fc-dom03-091',
    cardNumber: 91,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonction GROUPING_ID',
    difficulty: 'hard',
    tags: ['GROUPING_ID', 'Vecteur binaire', 'Masque', 'Filtre de niveau'],
    front: {
      question: 'Comment la fonction GROUPING_ID(col1, col2) calcule-t-elle sa valeur numérique retournée ?',
      hint: 'Pensez à la concaténation binaire des bits de GROUPING(col1) et GROUPING(col2).',
    },
    back: {
      answer: 'Elle concatène les bits retournés par GROUPING pour chaque colonne et convertit ce nombre binaire en un entier décimal.',
      explanation: 'Pour GROUPING_ID(a, b) : si a=0 et b=0 -> binaire 00 -> 0. Si a=0 et b=1 -> binaire 01 -> 1. Si a=1 et b=0 -> binaire 10 -> 2. Si a=1 et b=1 (grand total) -> binaire 11 -> 3.',
      codeSnippet: 'SELECT \n  department_id, job_id, SUM(salary),\n  GROUPING(department_id) AS g_dept,\n  GROUPING(job_id)        AS g_job,\n  GROUPING_ID(department_id, job_id) AS gid\nFROM employees\nGROUP BY CUBE(department_id, job_id);',
      examTrap: 'GROUPING_ID est extrêmement utile dans la clause HAVING pour ne filtrer qu\'un niveau hiérarchique précis (ex: HAVING GROUPING_ID(dept, job) = 1).',
      ruleRef: 'Oracle SQL Reference - GROUPING_ID Function',
    },
  },
  {
    id: 'fc-dom03-092',
    cardNumber: 92,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Arguments de GROUPING vs GROUPING_ID',
    difficulty: 'easy',
    tags: ['Arguments', 'GROUPING', 'GROUPING_ID', 'Différence'],
    front: {
      question: 'Combien d\'arguments la fonction GROUPING accepte-t-elle par rapport à GROUPING_ID ?',
      hint: 'L\'une est unaire, l\'autre peut en prendre plusieurs.',
    },
    back: {
      answer: 'GROUPING n\'accepte qu\'un SEUL argument (une seule colonne), alors que GROUPING_ID accepte une ou plusieurs colonnes séparées par des virgules.',
      explanation: 'Écrire GROUPING(col1, col2) provoque ORA-00909: invalid number of arguments. Pour évaluer plusieurs colonnes d\'un coup, il faut utiliser GROUPING_ID(col1, col2).',
      codeSnippet: '-- ERREUR (ORA-00909) :\nSELECT GROUPING(department_id, job_id) FROM employees GROUP BY ROLLUP(department_id, job_id);\n\n-- VALIDE :\nSELECT GROUPING_ID(department_id, job_id) FROM employees GROUP BY ROLLUP(department_id, job_id);',
      examTrap: 'Erreur classique de syntaxe à l\'examen : repérez les appels à GROUPING avec 2 colonnes ou plus.',
      ruleRef: 'GROUPING vs GROUPING_ID Signature Specs',
    },
  },
  {
    id: 'fc-dom03-093',
    cardNumber: 93,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonction GROUP_ID',
    difficulty: 'hard',
    tags: ['GROUP_ID', 'Doublons de groupement', 'Déduplication'],
    front: {
      question: 'À quoi sert la fonction GROUP_ID() sous Oracle SQL et que permet de faire la condition HAVING GROUP_ID() = 0 ?',
      hint: 'Que se passe-t-il lorsque des extensions génèrent des regroupements identiques en double ?',
    },
    back: {
      answer: 'Elle sert à numéroter les regroupements en doublon (0 pour la première occurrence, 1 pour la deuxième, etc.). HAVING GROUP_ID() = 0 élimine tous les doublons de groupes.',
      explanation: 'Lorsque des combinaisons complexes de GROUP BY génèrent deux fois le même ensemble de regroupement, GROUP_ID() attribue 0 à l\'unique exemplaire original et > 0 aux répétitions.',
      codeSnippet: 'SELECT department_id, job_id, SUM(salary), GROUP_ID()\nFROM employees\nGROUP BY department_id, ROLLUP(department_id, job_id)\nHAVING GROUP_ID() = 0;',
      examTrap: 'GROUP_ID ne prend AUCUN argument (syntaxe : GROUP_ID()).',
      ruleRef: 'Oracle SQL Reference - GROUP_ID Function',
    },
  },
  {
    id: 'fc-dom03-094',
    cardNumber: 94,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'GROUPING dans la clause HAVING',
    difficulty: 'hard',
    tags: ['GROUPING', 'HAVING', 'Filtrer les totaux'],
    front: {
      question: 'Comment afficher UNIQUEMENT les lignes de sous-totaux et le grand total créés par ROLLUP, en excluant toutes les lignes de détail ?',
      hint: 'Quelle condition sur GROUPING ou GROUPING_ID peut-on poser dans HAVING ?',
    },
    back: {
      answer: 'En ajoutant HAVING GROUPING(job_id) = 1 (ou HAVING GROUPING_ID(dept, job) > 0).',
      explanation: 'Puisque GROUPING(job_id) vaut 0 pour les lignes de détail et 1 pour les lignes agrégées où job_id est cumulé, poser GROUPING(job_id) = 1 dans HAVING élimine toutes les lignes fines.',
      codeSnippet: 'SELECT department_id, job_id, SUM(salary)\nFROM employees\nGROUP BY ROLLUP(department_id, job_id)\nHAVING GROUPING(job_id) = 1;\n-- N\'affiche que les totaux par département et le grand total général !',
      examTrap: 'Très astucieux à l\'examen pour filtrer la granularité d\'un rapport sans sous-requête.',
      ruleRef: 'Filtering Aggregate Levels via HAVING and GROUPING',
    },
  },

  // =========================================================================
  // SECTION 9 : PIÈGES D'EXAMEN 1Z0-071 & CAS COMPLEXES COMBINÉS (95 à 100)
  // =========================================================================
  {
    id: 'fc-dom03-095',
    cardNumber: 95,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Dédoublement de lignes par Jointure faussant SUM/COUNT',
    difficulty: 'hard',
    tags: ['Jointure', 'Dédoublement de lignes', 'SUM faussé', 'Piège critique'],
    front: {
      question: 'Si vous faites un JOIN entre la table departments et la table employees, pourquoi SUM(d.budget) risque-t-il de produire une somme astronomique et fausse ?',
      hint: 'Combien de fois la ligne d\'un département est-elle répétée dans la jointure ?',
    },
    back: {
      answer: 'Parce que la ligne du département est multipliée par le nombre d\'employés qu\'il contient ! Son budget est donc additionné autant de fois qu\'il y a d\'employés.',
      explanation: 'Dans une relation 1 à N, faire une jointure avant d\'agréger sur la table parent multiplie les métriques du parent. La solution propre consiste soit à agréger la table enfant avant la jointure, soit à utiliser SUM(DISTINCT d.budget) si les budgets sont uniques.',
      codeSnippet: '-- MAUVAISE MÉTHODE (Budget multiplié) :\nSELECT d.department_name, SUM(d.budget), COUNT(e.employee_id)\nFROM departments d JOIN employees e ON d.department_id = e.department_id\nGROUP BY d.department_name;\n\n-- BONNE MÉTHODE (Pré-agrégation) :\nSELECT d.department_name, d.budget, emp.nb_staff\nFROM departments d\nJOIN (SELECT department_id, COUNT(*) AS nb_staff FROM employees GROUP BY department_id) emp\n  ON d.department_id = emp.department_id;',
      examTrap: 'Ce cas classique d\'examen teste si vous comprenez l\'effet démultiplicateur des jointures relationnelles sur les fonctions d\'agrégation.',
      ruleRef: 'Data Integrity and Pre-Aggregation in SQL Joins',
    },
  },
  {
    id: 'fc-dom03-096',
    cardNumber: 96,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT(DISTINCT) avec plusieurs colonnes concaténées et NULL',
    difficulty: 'hard',
    tags: ['COUNT', 'Concaténation', 'NULL', 'Piège subtil'],
    front: {
      question: 'Si col1 vaut \'A\' et col2 vaut NULL, que produit COUNT(col1 || col2) ? Est-ce que cette ligne est comptée ?',
      hint: 'Que donne \'A\' || NULL sous Oracle ? Est-ce une valeur NULL ou non-NULL ?',
    },
    back: {
      answer: 'Oui, la ligne EST comptabilisée car \'A\' || NULL produit \'A\' (non-NULL) sous Oracle SQL.',
      explanation: 'Rappelez-vous la règle d\'Oracle : la concaténation avec NULL traite NULL comme une chaîne de caractères vide \'\'. Donc \'A\' || NULL = \'A\'. Comme le résultat n\'est pas NULL, COUNT(\'A\') incrémente le compteur !',
      codeSnippet: 'SELECT COUNT(\'A\' || NULL) FROM dual;\n-- Résultat : 1 (alors que col2 était NULL !)',
      examTrap: 'Piège d\'examen d\'une extrême subtilité : concaténer deux colonnes pour contourner le manque de multi-colonnes dans COUNT(DISTINCT a || b) peut comptabiliser des lignes dont l\'une des colonnes était NULL !',
      ruleRef: 'Oracle SQL String Concatenation with NULL Values',
    },
  },
  {
    id: 'fc-dom03-097',
    cardNumber: 97,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'WHERE éliminant tout avant GROUP BY',
    difficulty: 'medium',
    tags: ['WHERE', 'GROUP BY', '0 groupe', 'Comportement'],
    front: {
      question: 'Si une clause WHERE élimine 100% des lignes de la table et qu\'une clause GROUP BY department_id est présente, combien de lignes la requête retourne-t-elle ?',
      codeSnippet: 'SELECT department_id, COUNT(*), SUM(salary)\nFROM employees\nWHERE 1 = 2\nGROUP BY department_id;',
      hint: 'Attention : il y a une clause GROUP BY !',
    },
    back: {
      answer: 'Elle retourne 0 ligne ("no rows selected").',
      explanation: 'C\'est la différence capitale : SANS GROUP BY, une requête d\'agrégation sur 0 ligne retourne 1 ligne (0, NULL). AVEC GROUP BY, aucun groupe ne peut être constitué, donc la requête retourne ZERO ligne !',
      codeSnippet: '-- AVEC GROUP BY sur ensemble vide -> 0 ligne retournée :\nSELECT department_id, COUNT(*) FROM employees WHERE 1 = 2 GROUP BY department_id;\n\n-- SANS GROUP BY sur ensemble vide -> 1 ligne retournée (valeur 0) :\nSELECT COUNT(*) FROM employees WHERE 1 = 2;',
      examTrap: 'Ce contraste (1 ligne sans GROUP BY vs 0 ligne avec GROUP BY) est l\'un des favoris absolus des examinateurs Oracle 1Z0-071.',
      ruleRef: 'Zero Rows Processing: With vs Without GROUP BY',
    },
  },
  {
    id: 'fc-dom03-098',
    cardNumber: 98,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'ORDER BY avec fonction de groupe non sélectionnée',
    difficulty: 'medium',
    tags: ['ORDER BY', 'Agrégats', 'Tri complexe'],
    front: {
      question: 'La requête suivante est-elle valide sous Oracle ?',
      codeSnippet: 'SELECT department_id\nFROM employees\nGROUP BY department_id\nORDER BY MIN(salary) ASC, MAX(hire_date) DESC;',
      hint: 'MIN(salary) et MAX(hire_date) sont-ils des agrégats valides pour les groupes formés ?',
    },
    back: {
      answer: 'OUI, 100% valide !',
      explanation: 'On peut trier les groupes sur n\'importe quelle fonction d\'agrégation valide calculée sur les lignes du groupe, même si celle-ci ne figure pas dans le SELECT.',
      codeSnippet: 'SELECT department_id\nFROM employees\nGROUP BY department_id\nORDER BY MIN(salary) ASC, MAX(hire_date) DESC;\n-- Trie les départements par leur salaire le plus bas, puis par la date d\'embauche la plus récente.',
      examTrap: 'Ne confondez pas avec ORDER BY salary (sans MIN/MAX), qui échouerait immédiatement avec ORA-00979 !',
      ruleRef: 'SQL Language Reference - ORDER BY Expressions with GROUP BY',
    },
  },
  {
    id: 'fc-dom03-099',
    cardNumber: 99,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT(DISTINCT NVL(col, val))',
    difficulty: 'hard',
    tags: ['COUNT', 'DISTINCT', 'NVL', 'Comptage de NULL'],
    front: {
      question: 'Comment forcer COUNT(DISTINCT commission_pct) à comptabiliser la présence de valeurs NULL comme une modalité distincte supplémentaire ?',
      hint: 'Quelle fonction permet de substituer une valeur sentinelle au NULL avant déduplication ?',
    },
    back: {
      answer: 'En écrivant COUNT(DISTINCT NVL(commission_pct, -1)) (ou toute autre valeur numérique impossible dans le jeu de données).',
      explanation: 'Puisque COUNT(DISTINCT col) élimine systématiquement les NULLs, remplacer NULL par une valeur sentinelle (ex: -1 ou 0 si absent) permet à cette modalité d\'être comptée parmi les valeurs distinctes.',
      codeSnippet: 'SELECT \n  COUNT(DISTINCT commission_pct)          AS sans_null,   -- Ex: 7 taux distincts\n  COUNT(DISTINCT NVL(commission_pct, -1)) AS avec_null    -- Ex: 8 taux distincts (inclut le cas \'sans commission\')\nFROM employees;',
      examTrap: 'À l\'examen, si la question exige de compter "toutes les valeurs uniques y compris les valeurs non renseignées", l\'emploi de NVL est impératif.',
      ruleRef: 'Techniques for Distinct Counting with NULL Inclusion',
    },
  },
  {
    id: 'fc-dom03-100',
    cardNumber: 100,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Synthèse Maître 1Z0-071 : Le Détecteur d\'Erreurs',
    difficulty: 'hard',
    tags: ['Synthèse', 'Checklist Examen', 'ORA-00934', 'ORA-00979', 'ORA-00937'],
    front: {
      question: 'Quelles sont les 4 règles réflexes d\'or à vérifier mentalement en 5 secondes face à toute question d\'agrégation du 1Z0-071 ?',
      hint: 'Pensez à WHERE, SELECT vs GROUP BY, alias, et imbrication.',
    },
    back: {
      answer: '1) Jamais d\'agrégat direct dans WHERE (-> ORA-00934). 2) Toute colonne non agrégée du SELECT doit être dans GROUP BY (-> ORA-00979). 3) Pas d\'alias ni de numéros de position dans GROUP BY/HAVING (-> ORA-00904). 4) Si agrégat imbriqué MAX(AVG()), aucune colonne dans le SELECT (-> ORA-00937) et GROUP BY obligatoire (-> ORA-00978).',
      explanation: 'Ces 4 règles couvrent à elles seules plus de 90% des causes d\'échec et de pièges sur le domaine DOM-03 de la certification Oracle Database SQL 1Z0-071.',
      codeSnippet: '-- La checklist mentale 1Z0-071 :\n-- [ ] Pas de SUM/AVG/COUNT dans WHERE ? (Sinon ORA-00934)\n-- [ ] Colonnes du SELECT présentes dans GROUP BY ? (Sinon ORA-00979)\n-- [ ] Pas d\'alias dans GROUP BY ou HAVING ? (Sinon ORA-00904)\n-- [ ] Pas de colonne individuelle avec MAX(AVG(...)) ? (Sinon ORA-00937)',
      examTrap: 'Gardez toujours cette checklist en tête avant de valider votre réponse le jour de l\'examen !',
      ruleRef: 'Oracle Database SQL 1Z0-071 Core Aggregation Mastery Checklist',
    },
  },
];
