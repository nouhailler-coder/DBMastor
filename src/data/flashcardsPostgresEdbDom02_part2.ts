import { FlashcardItem } from '../types';

export const postgresEdbDom02Part2Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 6 : MODIFICATION DYNAMIQUE DES PARAMÈTRES & COMMANDE `ALTER SYSTEM` (Cartes 51 à 60)
  // ALTER SYSTEM SET/RESET, pg_reload_conf(), ALTER DATABASE/ROLE, SET vs SET LOCAL, SHOW
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-051',
    cardNumber: 51,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Commande SQL `ALTER SYSTEM SET`',
    difficulty: 'easy',
    tags: ['ALTER SYSTEM', 'postgresql.auto.conf', 'Configuration', 'SQL Administration'],
    front: {
      question: 'Que réalise précisément la commande SQL `ALTER SYSTEM SET <paramètre> = <valeur>;` et dans quel fichier écrit-elle ?',
      codeSnippet: 'ALTER SYSTEM SET work_mem = \'128MB\';',
      hint: 'Elle modifie la configuration globale au niveau serveur en écrivant dans un fichier dédié sans toucher à postgresql.conf.',
    },
    back: {
      answer: 'La commande SQL `ALTER SYSTEM SET` écrit la modification directement dans le fichier `$PGDATA/postgresql.auto.conf`.\nElle permet à un superutilisateur d\'ajuster la configuration globale du cluster via une simple connexion SQL, sans nécessiter d\'accès SSH ou d\'éditeur de texte sur le système de fichiers hôte.\n\nAttention : La commande n\'applique PAS immédiatement la nouvelle valeur aux connexions actives : un rechargement de configuration (`SELECT pg_reload_conf();`) ou un redémarrage (selon le contexte du paramètre) reste indispensable.',
      explanation: 'Ce mécanisme est particulièrement utile dans les environnements cloud managés ou conteneurisés où l\'accès direct aux fichiers de l\'OS est interdit.',
      examTrap: '`ALTER SYSTEM` n\'édite JAMAIS `postgresql.conf`. Il écrit exclusivement dans `postgresql.auto.conf`.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER SYSTEM',
    },
  },
  {
    id: 'fc-pgedb-dom02-052',
    cardNumber: 52,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Suppression d\'un paramètre avec `ALTER SYSTEM RESET`',
    difficulty: 'medium',
    tags: ['ALTER SYSTEM RESET', 'postgresql.auto.conf', 'Revert', 'Defaults'],
    front: {
      question: 'Comment annuler une modification effectuée par `ALTER SYSTEM` et restaurer la valeur définie dans `postgresql.conf` ?',
      codeSnippet: 'ALTER SYSTEM RESET work_mem;\n-- ou\nALTER SYSTEM RESET ALL;',
      hint: 'La commande RESET retire la directive du fichier postgresql.auto.conf.',
    },
    back: {
      answer: 'Pour révoquer un paramètre géré par `ALTER SYSTEM` :\n- `ALTER SYSTEM RESET <paramètre>;` : Supprime la ligne correspondant à ce paramètre du fichier `postgresql.auto.conf`.\n- `ALTER SYSTEM RESET ALL;` : Vide complètement `postgresql.auto.conf` de toutes ses surcharges.\n\nAprès l\'exécution, un appel à `SELECT pg_reload_conf();` permet de réactiver la valeur originelle inscrite dans `postgresql.conf` (ou la valeur par défaut du système).',
      explanation: 'Contrairement à `ALTER SYSTEM SET param = DEFAULT` qui écrit littéralement `DEFAULT` dans le fichier, `ALTER SYSTEM RESET` supprime physiquement la directive du fichier.',
      examTrap: 'Oublier d\'exécuter `SELECT pg_reload_conf();` après un `ALTER SYSTEM RESET` laisse l\'ancienne valeur active en mémoire pour les sessions existantes.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER SYSTEM',
    },
  },
  {
    id: 'fc-pgedb-dom02-053',
    cardNumber: 53,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Rechargement à chaud avec `SELECT pg_reload_conf()`',
    difficulty: 'easy',
    tags: ['pg_reload_conf', 'SIGHUP', 'Reload', 'SQL Administration'],
    front: {
      question: 'À quoi sert la fonction SQL `SELECT pg_reload_conf();` et quel privilège est requis pour l\'exécuter ?',
      hint: 'Elle envoie un signal SIGHUP au Postmaster directement depuis une session SQL.',
    },
    back: {
      answer: 'La fonction `pg_reload_conf()` envoie un signal `SIGHUP` au processus serveur maître (Postmaster).\nEffets :\n1. Provoque la relecture immédiate de `postgresql.conf`, des fichiers inclus, de `postgresql.auto.conf` et de `pg_hba.conf`.\n2. Applique sans interruption les paramètres dont le contexte est `sighup` ou `user`.\n3. Renvoie un booléen `true` confirmant l\'envoi du signal.\n\nPrivilège : Réservée aux superutilisateurs ou aux rôles ayant reçu le rôle prédéfini `pg_signal_backend` / `pg_read_all_settings`.',
      explanation: 'Elle équivaut fidèlement à la commande système `pg_ctl reload` ou `systemctl reload postgresql`, mais s\'exécute au sein du protocole SQL.',
      examTrap: '`pg_reload_conf()` renvoie `true` pour signifier que le signal SIGHUP a bien été expédié, mais cela ne garantit pas que les nouveaux paramètres ne contiennent pas d\'erreur de syntaxe !',
      ruleRef: 'PostgreSQL 16 Documentation - System Administration Functions: Configuration Signalling',
    },
  },
  {
    id: 'fc-pgedb-dom02-054',
    cardNumber: 54,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Configuration par base de données avec `ALTER DATABASE SET`',
    difficulty: 'medium',
    tags: ['ALTER DATABASE', 'Database Level', 'Per-Database Configuration'],
    front: {
      question: 'Quel est l\'effet de la commande `ALTER DATABASE <nom> SET <paramètre> = <valeur>;` et quand prend-elle effet ?',
      codeSnippet: 'ALTER DATABASE compta_db SET search_path = compta, public;\nALTER DATABASE analytics_db SET work_mem = \'256MB\';',
      hint: 'Elle s\'applique automatiquement à toute nouvelle session se connectant à cette base de données.',
    },
    back: {
      answer: 'La commande `ALTER DATABASE ... SET` associe une valeur de configuration spécifique à une base de données particulière.\nComportement :\n1. Elle est enregistrée dans le catalogue système global (`pg_db_role_setting`).\n2. Elle ne modifie pas les sessions en cours.\n3. Elle prend effet automatiquement lors de chaque nouvelle connexion cliente établie sur cette base de données, écrasant les réglages globaux de `postgresql.conf`.\n4. Seuls les paramètres avec un contexte dynamique (`user`, `superuser`, `sighup`) peuvent être définis ainsi.',
      explanation: 'Très utile pour allouer davantage de `work_mem` à une base dédiée à la Business Intelligence (`analytics_db`) sans impacter les bases transactionnelles OLTP.',
      examTrap: 'Vous ne pouvez pas modifier un paramètre de contexte `postmaster` (comme `shared_buffers`) via `ALTER DATABASE`. Le moteur rejettera la commande avec une erreur.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER DATABASE',
    },
  },
  {
    id: 'fc-pgedb-dom02-055',
    cardNumber: 55,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Configuration par utilisateur / rôle avec `ALTER ROLE SET`',
    difficulty: 'medium',
    tags: ['ALTER ROLE', 'User Level', 'Per-User Configuration', 'statement_timeout'],
    front: {
      question: 'Pourquoi et comment configure-t-on des paramètres au niveau d\'un rôle utilisateur spécifique avec `ALTER ROLE ... SET` ?',
      codeSnippet: 'ALTER ROLE reporting_user SET statement_timeout = \'30min\';\nALTER ROLE app_web SET statement_timeout = \'5s\';',
      hint: 'Elle personnalise l\'environnement d\'exécution dès que cet utilisateur ouvre une session.',
    },
    back: {
      answer: 'La commande `ALTER ROLE <nom_role> SET <parametre> = <valeur>;` enregistre un réglage dans le catalogue `pg_db_role_setting` qui s\'appliquera automatiquement à toute session ouverte par ce rôle.\nCas d\'usage typiques :\n- Brider les requêtes de l\'application web avec un `statement_timeout = \'5s\'` pour éviter d\'engorger le pool.\n- Accorder un timeout généreux (`30min`) et un `work_mem` élevé (`512MB`) pour le compte batch ou reporting.\n- Définir un schéma par défaut avec `search_path`.',
      explanation: 'On peut même combiner rôle et base de données : `ALTER ROLE reporting_user IN DATABASE analytics_db SET work_mem = \'1GB\';`.',
      examTrap: 'Si le rôle utilise ensuite la commande `SET param = ...` dans sa session, la commande de session primera sur la valeur d\'`ALTER ROLE`.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: ALTER ROLE',
    },
  },
  {
    id: 'fc-pgedb-dom02-056',
    cardNumber: 56,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Surcharge au niveau session : `SET` vs `SET LOCAL`',
    difficulty: 'medium',
    tags: ['SET', 'SET LOCAL', 'Session', 'Transaction Scope', 'work_mem'],
    front: {
      question: 'Quelle est la différence fondamentale entre `SET <paramètre> = <valeur>;` et `SET LOCAL <paramètre> = <valeur>;` ?',
      codeSnippet: 'BEGIN;\n  SET LOCAL work_mem = \'512MB\';\n  -- Requête lourde\nCOMMIT;',
      hint: 'L\'un persiste pour toute la durée de la connexion cliente, l\'autre est strictement limité à la transaction en cours.',
    },
    back: {
      answer: '1. `SET <paramètre> = <valeur>;` :\n- Modifie le paramètre pour l\'ensemble de la session en cours.\n- La valeur persiste pour toutes les requêtes suivantes jusqu\'à la déconnexion ou jusqu\'à un prochain `SET` / `RESET`.\n\n2. `SET LOCAL <paramètre> = <valeur>;` :\n- Doit obligatoirement être exécuté à l\'intérieur d\'un bloc de transaction (`BEGIN ... COMMIT/ROLLBACK`).\n- La modification ne s\'applique QUE pour la durée de la transaction courante.\n- Dès que la transaction se termine (COMMIT ou ROLLBACK), le paramètre est automatiquement rétabli à sa valeur précédente.',
      explanation: '`SET LOCAL` est la recommandation d\'or pour temporairement booster la mémoire d\'une requête spécifique (`work_mem`) sans risquer de surconsommer la RAM lors des requêtes suivantes de la même session.',
      examTrap: 'Si vous exécutez `SET LOCAL` en dehors d\'un bloc de transaction (en mode autocommit), la commande n\'a aucun effet persistant et émet un avertissement (WARNING: SET LOCAL will only have effect in a transaction).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: SET',
    },
  },
  {
    id: 'fc-pgedb-dom02-057',
    cardNumber: 57,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Consultation des paramètres : `SHOW` et `current_setting()`',
    difficulty: 'easy',
    tags: ['SHOW', 'current_setting', 'Monitoring', 'Inspection'],
    front: {
      question: 'Comment consulter la valeur active d\'un paramètre de configuration en SQL avec `SHOW` et via la fonction `current_setting()` ?',
      codeSnippet: 'SHOW work_mem;\nSELECT current_setting(\'work_mem\');',
      hint: 'SHOW est une commande SQL autonome, tandis que current_setting() peut être intégrée dans des requêtes et expressions complexes.',
    },
    back: {
      answer: 'Deux méthodes existent pour inspecter un paramètre dans la session courante :\n1. Commande SQL autonome `SHOW <nom_paramètre>;` (ou `SHOW ALL;` pour lister l\'intégralité des paramètres).\n   - Exemple : `SHOW work_mem;` renvoie `64MB` sous forme de jeu d\'enregistrements textuel.\n2. Fonction SQL scalaire `current_setting(\'<nom_paramètre>\' [, missing_ok])` :\n   - Renvoie la valeur sous forme de `text`.\n   - Exemple : `SELECT current_setting(\'shared_buffers\');`\n   - Le second paramètre optionnel booléen (`missing_ok = true`) évite une erreur si le paramètre demandé n\'existe pas (ex: variable personnalisée non définie).',
      explanation: '`current_setting()` est très utile dans les triggers, procédures stockées ou requêtes de supervision pour brancher une logique conditionnelle.',
      examTrap: '`SHOW` ne peut pas être imbriqué dans une sous-requête ou une clause `WHERE` ; pour cela, il faut impérativement utiliser `current_setting()` ou interroger la vue `pg_settings`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 9.27: System Information Functions',
    },
  },
  {
    id: 'fc-pgedb-dom02-058',
    cardNumber: 58,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Réinitialisation des paramètres : `RESET` et `DISCARD ALL`',
    difficulty: 'medium',
    tags: ['RESET', 'DISCARD ALL', 'Session Cleanup', 'Connection Pooling'],
    front: {
      question: 'À quoi servent les commandes `RESET <paramètre>;` et `DISCARD ALL;` dans une session utilisateur ?',
      codeSnippet: 'RESET work_mem;\nDISCARD ALL;',
      hint: 'Elles rétablissent les valeurs par défaut et nettoient l\'état d\'une connexion réutilisée par un pooler.',
    },
    back: {
      answer: '1. `RESET <paramètre>;` (ou `SET <paramètre> TO DEFAULT;`) :\n- Réinitialise le paramètre spécifié à sa valeur configurée par défaut pour cette session (telle que définie par le rôle, la base ou le serveur).\n2. `DISCARD ALL;` :\n- Nettoie intégralement l\'état de la session courante. Elle équivaut à exécuter simultanément :\n  * `RESET ALL` (réinitialise tous les paramètres de configuration modifiés par `SET`).\n  * `DEALLOCATE ALL` (supprime toutes les requêtes préparées).\n  * `DISCARD PLANS` (vide le cache des plans d\'exécution).\n  * `DISCARD TEMP` (ferme et supprime toutes les tables temporaires de la session).\n  * `DISCARD SEQUENCES` (libère les numéros de séquences en cache).',
      explanation: 'La commande `DISCARD ALL` est systématiquement appelée par les gestionnaires de pools de connexions (PgBouncer en mode transaction) lors de la restitution d\'une connexion au pool pour éviter toute fuite d\'état entre utilisateurs.',
      examTrap: '`DISCARD ALL` ne peut pas être exécuté à l\'intérieur d\'un bloc de transaction actif (`BEGIN ... COMMIT`).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands: DISCARD',
    },
  },
  {
    id: 'fc-pgedb-dom02-059',
    cardNumber: 59,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Hiérarchie globale et ordre de préséance des configurations',
    difficulty: 'hard',
    tags: ['Precedence', 'Hierarchy', 'Architecture', 'Configuration', 'Exam Favorite'],
    front: {
      question: 'Quel est l\'ordre de priorité exact (du plus prioritaire au moins prioritaire) entre les différents niveaux de configuration de PostgreSQL ?',
      hint: 'Une directive plus locale ou éphémère l\'emporte toujours sur une directive globale plus large.',
    },
    back: {
      answer: 'L\'ordre de préséance rigoureux (du plus prioritaire au moins prioritaire) est :\n1. `SET LOCAL` (portée transaction courante uniquement).\n2. `SET` (portée session en cours).\n3. Options de ligne de commande passées au démarrage (`postgres -o "-c ..."`).\n4. `ALTER ROLE ... IN DATABASE ... SET` (combinaison spécifique utilisateur + base).\n5. `ALTER ROLE ... SET` (au niveau du rôle connecté).\n6. `ALTER DATABASE ... SET` (au niveau de la base connectée).\n7. `postgresql.auto.conf` (issu de `ALTER SYSTEM`).\n8. `postgresql.conf` (et directives `include` associées).\n9. Valeurs par défaut compilées en dur dans le binaire PostgreSQL.',
      explanation: 'Comprendre cette hiérarchie est indispensable pour diagnostiquer pourquoi un changement effectué dans `postgresql.conf` ne semble pas s\'appliquer à un utilisateur précis (souvent surchargé par un `ALTER ROLE`).',
      examTrap: 'Question classique d\'examen : Si un paramètre est présent à la fois dans `postgresql.conf` et dans `postgresql.auto.conf`, c\'est `postgresql.auto.conf` qui gagne !',
      ruleRef: 'EDB Certified Associate Study Guide - Chapter: Parameter Precedence Hierarchy',
    },
  },
  {
    id: 'fc-pgedb-dom02-060',
    cardNumber: 60,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Rôle de `pg_manage_configs` et privilèges non-superutilisateur',
    difficulty: 'medium',
    tags: ['Security', 'Privileges', 'pg_manage_configs', 'RBAC', 'PostgreSQL 16'],
    front: {
      question: 'Comment peut-on accorder à un utilisateur non-superutilisateur le droit d\'exécuter `ALTER SYSTEM` et de recharger la configuration sous PostgreSQL 16 ?',
      codeSnippet: 'GRANT pg_manage_configs TO admin_junior;\nGRANT pg_signal_backend TO admin_junior;',
      hint: 'PostgreSQL fournit des rôles système prédéfinis pour déléguer les privilèges d\'administration sans accorder les pleins pouvoirs de SUPERUSER.',
    },
    back: {
      answer: 'Depuis les versions récentes de PostgreSQL (notamment consolidé en PG 15/16), il n\'est plus obligatoire d\'accorder le statut risqué de `SUPERUSER` pour administrer la configuration :\n1. `pg_manage_configs` : Rôle prédéfini permettant d\'exécuter des commandes `ALTER SYSTEM` et de modifier les réglages du serveur.\n2. `pg_signal_backend` : Rôle prédéfini permettant d\'envoyer des signaux d\'annulation ou de rechargement via `SELECT pg_reload_conf();` et `SELECT pg_cancel_backend(pid);`.\n3. `pg_read_all_settings` : Permet de lire tous les paramètres de configuration même masqués.',
      explanation: 'Cette granularité respecte le principe de moindre privilège et renforce l\'audit de sécurité exigé par les normes ISO/PCI-DSS.',
      examTrap: 'Sans ces rôles ni les privilèges superutilisateur, exécuter `ALTER SYSTEM` renvoie immédiatement l\'erreur "permission denied".',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 22.5: Predefined Roles',
    },
  },

  // =========================================================================
  // SECTION 7 : CONTEXTES DE PARAMÈTRES DANS `pg_settings` & GESTION DES RECHARGEMENTS (Cartes 61 à 70)
  // Contextes (internal, postmaster, sighup, backend, superuser, user), pending_restart
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-061',
    cardNumber: 61,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Vue système `pg_settings` : colonnes fondamentales',
    difficulty: 'easy',
    tags: ['pg_settings', 'System Catalog', 'Metadata', 'Monitoring'],
    front: {
      question: 'Quelles informations essentielles sont fournies par la vue système `pg_settings` pour administrer les paramètres du serveur ?',
      codeSnippet: 'SELECT name, setting, unit, context, source, pending_restart\nFROM pg_settings\nWHERE name = \'shared_buffers\';',
      hint: 'Elle détaille la valeur active, son unité, quand et comment elle peut être modifiée, et sa provenance.',
    },
    back: {
      answer: 'La vue système `pg_settings` fournit la description complète de chaque paramètre du moteur :\n- `name` : Nom officiel du paramètre.\n- `setting` : Valeur actuelle active en mémoire.\n- `unit` : Unité de mesure (8kB, MB, ms, etc.).\n- `context` : Définit les conditions et privilèges requis pour modifier le paramètre (`postmaster`, `sighup`, `user`, etc.).\n- `source` : D\'où provient la valeur appliquée (`configuration file`, `default`, `override`, `session`).\n- `pending_restart` (booléen) : Vaut `true` si une modification a été faite dans le fichier mais nécessite un redémarrage pour s\'appliquer.',
      explanation: 'C\'est la vue de référence absolue interrogée par les DBA pour vérifier le comportement interne et planifier les interventions.',
      examTrap: 'La colonne `setting` affiche toujours la valeur brute numérique selon l\'unité indiquée par `unit` (ex: `131072` pour `shared_buffers` avec unité `8kB`, soit 1 Go).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Views - pg_settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-062',
    cardNumber: 62,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Contexte `internal`',
    difficulty: 'easy',
    tags: ['context', 'internal', 'Compilation', 'pg_settings', 'Architecture'],
    front: {
      question: 'Qu\'indique le contexte `internal` pour un paramètre dans la vue `pg_settings` ?',
      codeSnippet: 'SELECT name, setting, context FROM pg_settings WHERE context = \'internal\';',
      hint: 'Ces valeurs sont fixées en dur lors de la compilation du binaire ou de l\'initialisation du cluster et ne peuvent jamais être changées.',
    },
    back: {
      answer: 'Le contexte `internal` désigne des paramètres en lecture seule fixés lors de la compilation du binaire PostgreSQL ou figés à la création du cluster.\nCaractéristiques :\n- Impossible à modifier, ni via `postgresql.conf`, ni par `ALTER SYSTEM`, ni en session.\n- Exemples célèbres : `block_size` (8192 o), `wal_block_size` (8192 o), `wal_segment_size` (16MB), `server_version`, `integer_datetimes`.\n\nToute tentative d\'écriture sur un paramètre `internal` est rejetée avec une erreur.',
      explanation: 'Ces paramètres reflètent des constantes physiques indispensables à la stabilité binaire du moteur de stockage.',
      examTrap: 'Pour modifier `block_size` (ex: passer à 16 Ko ou 32 Ko), il faut obligatoirement recompiler PostgreSQL depuis les sources avec `./configure --with-blocksize=...` puis refaire un `initdb`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters - Contexts',
    },
  },
  {
    id: 'fc-pgedb-dom02-063',
    cardNumber: 63,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Contexte `postmaster`',
    difficulty: 'medium',
    tags: ['context', 'postmaster', 'Restart Required', 'pending_restart', 'RAM'],
    front: {
      question: 'Que signifie le contexte `postmaster` et comment s\'applique une modification d\'un tel paramètre ?',
      codeSnippet: 'SELECT name FROM pg_settings WHERE context = \'postmaster\';',
      hint: 'Ces paramètres dimensionnent les structures de mémoire partagée et nécessitent impérativement un arrêt/redémarrage.',
    },
    back: {
      answer: 'Le contexte `postmaster` signifie que le paramètre ne peut être appliqué QU\'AU DÉMARRAGE du serveur par le processus maître Postmaster.\nCaractéristiques :\n1. Toute modification apportée dans `postgresql.conf` ou via `ALTER SYSTEM` est enregistrée sur disque, mais n\'a aucun effet en mémoire tant que l\'instance n\'a pas été complètement arrêtée et redémarrée (`pg_ctl restart`).\n2. Un rechargement (`pg_ctl reload` ou `pg_reload_conf()`) est sans effet sur ces paramètres.\n3. Exemples clés : `shared_buffers`, `max_connections`, `archive_mode`, `listen_addresses`, `port`, `wal_level`, `huge_pages`.',
      explanation: 'Ces paramètres définissent la taille fixe des segments de mémoire partagée IPC et des structures système allouées une seule fois lors de la phase de boot du serveur.',
      examTrap: 'Tant que le serveur n\'a pas redémarré, la colonne `pending_restart` de la vue `pg_settings` passe à `true` pour ce paramètre.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters - Contexts',
    },
  },
  {
    id: 'fc-pgedb-dom02-064',
    cardNumber: 64,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Contexte `sighup`',
    difficulty: 'easy',
    tags: ['context', 'sighup', 'Reload', 'Dynamic', 'Zero Downtime'],
    front: {
      question: 'Quelles sont les propriétés d\'un paramètre de configuration possédant le contexte `sighup` ?',
      hint: 'Il s\'applique immédiatement à chaud sans aucune interruption de service dès réception du signal SIGHUP.',
    },
    back: {
      answer: 'Un paramètre de contexte `sighup` peut être modifié dynamiquement sans redémarrer le serveur PostgreSQL :\n1. Vous modifiez la valeur dans `postgresql.conf`, un fichier inclus ou via `ALTER SYSTEM`.\n2. Vous envoyez un signal SIGHUP (via `pg_ctl reload` ou `SELECT pg_reload_conf();`).\n3. Le serveur relit le fichier et applique immédiatement la nouvelle valeur à l\'ensemble du cluster et aux sessions existantes.\n4. Exemples : `work_mem` (valeur par défaut globale), `maintenance_work_mem`, `log_min_duration_statement`, `autovacuum`, `checkpoint_timeout`, `checkpoint_completion_target`.',
      explanation: 'La très grande majorité des paramètres de tuning fin de PostgreSQL sont de contexte `sighup`, permettant une optimisation en continu sans indisponibilité.',
      examTrap: 'Ne confondez pas `sighup` (applicable à chaud) et `postmaster` (redémarrage impératif). C\'est l\'une des questions les plus fréquentes à l\'examen EDB Associate.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters - Contexts',
    },
  },
  {
    id: 'fc-pgedb-dom02-065',
    cardNumber: 65,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Contexte `backend`',
    difficulty: 'medium',
    tags: ['context', 'backend', 'New Sessions', 'pg_settings', 'Connections'],
    front: {
      question: 'Comment se comporte un paramètre ayant le contexte `backend` lors d\'un rechargement de configuration ?',
      hint: 'Seules les nouvelles connexions futures recevront la nouvelle valeur ; les sessions existantes conservent l\'ancienne.',
    },
    back: {
      answer: 'Pour un paramètre de contexte `backend` :\n1. La modification s\'effectue dans les fichiers de configuration suivie d\'un rechargement (`SIGHUP`).\n2. Les sessions clientes déjà connectées au moment du rechargement CONSERVENT leur ancienne valeur jusqu\'à leur déconnexion.\n3. Seules les NOUVELLES connexions clientes établies après le rechargement recevront la nouvelle valeur.\n4. Exemple classique : `log_connections`, `log_disconnections`.',
      explanation: 'Ce comportement évite de perturber le cycle de vie interne de processus backends déjà initialisés sur des paramètres d\'infrastructure de connexion.',
      examTrap: 'Si vous activez `log_connections = on` et rechargez la conf, vous ne verrez pas d\'effet sur les sessions persistantes d\'un pooler de connexions tant que celles-ci ne sont pas renouvelées.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters - Contexts',
    },
  },
  {
    id: 'fc-pgedb-dom02-066',
    cardNumber: 66,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Contexte `superuser` vs Contexte `user`',
    difficulty: 'medium',
    tags: ['context', 'superuser', 'user', 'Privileges', 'Security'],
    front: {
      question: 'Quelle est la différence entre le contexte `superuser` et le contexte `user` lors de l\'utilisation de la commande `SET` en cours de session ?',
      codeSnippet: 'SET work_mem = \'64MB\';             -- Contexte: user\nSET session_replication_role = \'replica\'; -- Contexte: superuser',
      hint: 'L\'un peut être exécuté par n\'importe quel rôle connecté, l\'autre exige des privilèges de superutilisateur.',
    },
    back: {
      answer: '1. Contexte `user` :\n- Tout utilisateur peut modifier la valeur de ce paramètre pour sa propre session via `SET <paramètre> = <valeur>;` sans aucun privilège particulier.\n- Exemples : `work_mem`, `statement_timeout`, `search_path`, `client_min_messages`, `timezone`.\n\n2. Contexte `superuser` :\n- Seul un rôle possédant les privilèges de `SUPERUSER` peut modifier la valeur en cours de session via `SET`.\n- Si un utilisateur standard tente de le faire, PostgreSQL renvoie une erreur : "permission denied to set parameter".\n- Exemples : `session_replication_role`, `listen_addresses`, `log_min_messages`, `track_commit_timestamp`.',
      explanation: 'Les paramètres `superuser` touchent à l\'intégrité globale, à l\'audit de sécurité ou au contournement de contraintes (comme `session_replication_role` qui désactive les triggers).',
      examTrap: 'Un utilisateur standard ne peut jamais exécuter `SET session_replication_role = replica;` pour contourner des clés étrangères lors d\'un import.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters - Contexts',
    },
  },
  {
    id: 'fc-pgedb-dom02-067',
    cardNumber: 67,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Détection des paramètres en attente de redémarrage (`pending_restart`)',
    difficulty: 'medium',
    tags: ['pending_restart', 'pg_settings', 'Troubleshooting', 'Maintenance Window'],
    front: {
      question: 'Quelle requête SQL permet d\'identifier tous les paramètres modifiés dans la configuration mais qui attendent un redémarrage physique du serveur pour être appliqués ?',
      codeSnippet: 'SELECT name, setting, pending_restart\nFROM pg_settings\nWHERE pending_restart = true;',
      hint: 'La colonne booléenne pending_restart de pg_settings est conçue précisément pour cela.',
    },
    back: {
      answer: 'La requête est :\n```sql\nSELECT name, setting, pending_restart \nFROM pg_settings \nWHERE pending_restart = true;\n```\nElle liste tous les paramètres de contexte `postmaster` qui ont été édités dans `postgresql.conf` ou via `ALTER SYSTEM`, relus par un rechargement SIGHUP, mais qui ne sont pas encore effectifs en mémoire car l\'instance n\'a pas redémarré.',
      explanation: 'Cette requête est fondamentale pour les DBA avant de clore une fenêtre de maintenance, afin de s\'assurer qu\'aucun réglage critique (ex: modification de `shared_buffers`) n\'a été oublié.',
      examTrap: 'Si `pending_restart = true`, la valeur affichée dans la colonne `setting` est TOUJOURS l\'ancienne valeur active en mémoire, et non la nouvelle valeur inscrite dans le fichier !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Views - pg_settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-068',
    cardNumber: 68,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Origine des paramètres dans `pg_settings.source`',
    difficulty: 'medium',
    tags: ['source', 'pg_settings', 'Audit', 'Troubleshooting', 'Lineage'],
    front: {
      question: 'Quelles sont les valeurs courantes de la colonne `source` dans la vue `pg_settings` et que signifient-elles ?',
      hint: 'Elle indique si la valeur provient du fichier de configuration, de la session, de la ligne de commande ou des valeurs par défaut.',
    },
    back: {
      answer: 'La colonne `source` indique la provenance de la valeur courante du paramètre :\n- `default` : Valeur par défaut compilée en dur dans PostgreSQL (jamais modifiée).\n- `configuration file` : Lu depuis `postgresql.conf` ou un fichier inclus.\n- `override` : Lu depuis `postgresql.auto.conf`.\n- `command line` : Spécifié lors du démarrage du binaire via commutateur ou `pg_ctl -o`.\n- `database` : Défini via `ALTER DATABASE ... SET`.\n- `user` : Défini via `ALTER ROLE ... SET`.\n- `database user` : Défini via `ALTER ROLE ... IN DATABASE ... SET`.\n- `session` : Modifié temporairement dans la connexion courante par `SET`.\n- `environment variable` : Issu d\'une variable d\'environnement (ex: `PGPORT`).',
      explanation: 'Inspecter la colonne `source` permet de comprendre immédiatement pourquoi un paramètre ne prend pas la valeur attendue.',
      examTrap: 'Si un paramètre a pour source `session`, modifier le fichier `postgresql.conf` n\'aura aucun impact sur cette session en cours.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Views - pg_settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-069',
    cardNumber: 69,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Risque de blocage du serveur au redémarrage via `postgresql.auto.conf`',
    difficulty: 'hard',
    tags: ['Disaster Recovery', 'ALTER SYSTEM', 'postgresql.auto.conf', 'Crash', 'Troubleshooting'],
    front: {
      question: 'Que se passe-t-il si un administrateur configure par erreur une valeur aberrante pour un paramètre `postmaster` avec `ALTER SYSTEM` (ex: `shared_buffers = \'500GB\'` sur un serveur disposant de 16 Go de RAM) et redémarre le serveur ?',
      codeSnippet: 'ALTER SYSTEM SET shared_buffers = \'500GB\';\n-- Redémarrage du serveur...\n-- Le serveur plante et refuse de démarrer !',
      hint: 'Le serveur refuse de démarrer et comme l\'accès SQL est coupé, ALTER SYSTEM ne peut plus être exécuté.',
    },
    back: {
      answer: 'Conséquences et résolution :\n1. Au redémarrage, PostgreSQL tente d\'allouer 500 Go de mémoire partagée. L\'appel système échoue (mémoire insuffisante) et le Postmaster s\'arrête immédiatement avec une erreur FATALE.\n2. Comme l\'instance est arrêtée, il est IMPOSSIBLE de se connecter en SQL pour faire un `ALTER SYSTEM RESET`.\n3. Procédure de dépannage d\'urgence :\n   - Accéder au serveur hôte en ligne de commande (SSH).\n   - Éditer manuellement le fichier `$PGDATA/postgresql.auto.conf` avec `vi` ou `nano` pour supprimer la ligne fautive `shared_buffers = \'500GB\'` (ou supprimer temporairement le fichier).\n   - Relancer le serveur avec `pg_ctl start` ou `systemctl start postgresql-16`.',
      explanation: 'C\'est l\'unique cas d\'usage légitime où l\'édition manuelle d\'urgence de `postgresql.auto.conf` est requise et admise.',
      examTrap: 'Sur l\'examen, si une instance refuse de démarrer à cause d\'un réglage erroné dans `postgresql.auto.conf`, la réponse est d\'éditer manuellement ce fichier sur le disque au niveau OS.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.5: Alter System & Troubleshooting',
    },
  },
  {
    id: 'fc-pgedb-dom02-070',
    cardNumber: 70,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Fonctions de conversion et de validation : `pg_size_pretty()`',
    difficulty: 'easy',
    tags: ['pg_size_pretty', 'Monitoring', 'Units', 'Human Readable'],
    front: {
      question: 'À quoi sert la fonction utilitaire `pg_size_pretty()` dans les requêtes de supervision de la configuration et des tables ?',
      codeSnippet: 'SELECT name, pg_size_pretty(setting::bigint * 8192) AS buffer_size\nFROM pg_settings WHERE name = \'shared_buffers\';',
      hint: 'Elle convertit un nombre brut d\'octets en une chaîne lisible avec les unités appropriées (kB, MB, GB, TB).',
    },
    back: {
      answer: 'La fonction `pg_size_pretty(bigint)` ou `pg_size_pretty(numeric)` convertit une taille brute en octets (bytes) en une chaîne de caractères lisible par un être humain avec l\'unité la plus pertinente (`bytes`, `kB`, `MB`, `GB`, `TB`).\nExemples :\n- `SELECT pg_size_pretty(1073741824::bigint);` renvoie `1024 MB` ou `1 GB`.\n- Elle est indispensable pour afficher la taille des relations (`pg_total_relation_size()`) ou pour convertir les valeurs en blocs de `pg_settings` en méga-octets ou giga-octets intelligibles.',
      explanation: 'PostgreSQL fournit également la fonction inverse `pg_size_bytes(text)` (ex: `pg_size_bytes(\'8GB\')` renvoie `8589934592`), très pratique pour les calculs arithmétiques dans les scripts SQL.',
      examTrap: 'Ne multipliez pas directement sans convertir le type : `setting::bigint` est nécessaire car la colonne `setting` de `pg_settings` est de type `text`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 9.27: System Information Functions',
    },
  },

  // =========================================================================
  // SECTION 8 : RÉGLAGES RÉSEAUX, CONNEXIONS & ADRESSES D'ÉCOUTE (Cartes 71 à 80)
  // listen_addresses, port, max_connections, superuser_reserved_connections, SSL/TLS
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-071',
    cardNumber: 71,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Paramètre réseau fondamental : `listen_addresses`',
    difficulty: 'easy',
    tags: ['listen_addresses', 'Network', 'TCP/IP', 'localhost', 'Security'],
    front: {
      question: 'Quelle est la valeur par défaut de `listen_addresses` après une initialisation fraîche d\'un cluster, et que faut-il renseigner pour accepter les connexions depuis tout le réseau ?',
      codeSnippet: 'listen_addresses = \'*\'',
      hint: 'Par défaut, PostgreSQL n\'écoute que sur l\'interface de boucle locale (localhost).',
    },
    back: {
      answer: 'Par défaut, `listen_addresses = \'localhost\'`.\nConséquence : Le serveur n\'écoute que sur l\'interface locale de loopback (127.0.0.1 et ::1) et rejette toute tentative de connexion provenant d\'autres machines du réseau local ou d\'Internet.\n\nPour accepter les connexions distantes sur toutes les interfaces réseau configurées de la machine hôte, il faut définir :\n`listen_addresses = \'*\'`\n\nContexte : `postmaster` (nécessite un redémarrage complet de l\'instance).',
      explanation: 'On peut également restreindre l\'écoute à des adresses IP spécifiques de cartes réseau en les séparant par des virgules (ex: `listen_addresses = \'10.0.1.50,192.168.1.10\'`).',
      examTrap: 'Modifier `listen_addresses = \'*\'` ne suffit pas à autoriser un client : les règles de sécurité dans `pg_hba.conf` doivent également accorder l\'accès réseau au client !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3.1: Connection Settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-072',
    cardNumber: 72,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Port d\'écoute TCP (`port`)',
    difficulty: 'easy',
    tags: ['port', '5432', 'TCP', 'Network', 'Postmaster'],
    front: {
      question: 'Quel est le port TCP d\'écoute standard de PostgreSQL (`port`) et quelles sont les contraintes pour le modifier ?',
      codeSnippet: 'port = 5433',
      hint: 'Le port officiel enregistré IANA est 5432. Sa modification impose un redémarrage.',
    },
    back: {
      answer: 'Le port par défaut est le port TCP `5432`.\nContraintes de modification :\n1. Contexte : `postmaster` (impose d\'arrêter et redémarrer le serveur pour que le socket réseau se lie sur le nouveau port).\n2. Sous Linux, l\'utilisation d\'un port privilégié inférieur à 1024 (ex: port 80 ou 443) est interdite car PostgreSQL ne tourne pas sous le compte `root`.\n3. Tout changement de port impose d\'adapter les outils clients (`psql -p 5433`), la variable `PGPORT` et les chaînes de connexion applicatives.',
      explanation: 'Sur un serveur hébergeant plusieurs instances PostgreSQL simultanées, chaque instance doit impérativement avoir un port TCP unique (ex: 5432 pour le premier cluster, 5433 pour le second).',
      examTrap: 'Si le port configuré est déjà occupé par un autre processus ou une autre instance au démarrage, le serveur Postmaster échoue immédiatement avec une erreur "Address already in use".',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3.1: Connection Settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-073',
    cardNumber: 73,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Gestion du socket Unix (`unix_socket_directories`)',
    difficulty: 'medium',
    tags: ['unix_socket_directories', 'IPC', 'Local Connection', 'Performance'],
    front: {
      question: 'À quoi sert le paramètre `unix_socket_directories` et pourquoi les connexions locales via socket Unix sont-elles plus performantes que via TCP/IP ?',
      codeSnippet: 'unix_socket_directories = \'/var/run/postgresql, /tmp\'',
      hint: 'Il définit les dossiers où est créé le fichier de socket spécial .s.PGSQL.5432.',
    },
    back: {
      answer: '`unix_socket_directories` spécifie un ou plusieurs répertoires du système de fichiers où PostgreSQL crée son fichier de socket de domaine Unix (nommé `.s.PGSQL.<port>`).\nAvantages des sockets Unix pour les connexions locales (ex: `psql -h localhost` vs `psql` sans `-h`) :\n1. Zéro overhead de pile réseau TCP/IP (pas d\'encapsulation de paquets, pas de checksum TCP, pas de boucle de routage).\n2. Débit d\'échanges mémoire IPC plus rapide.\n3. Permet l\'authentification par compte système OS (`peer`), impossible via TCP/IP.',
      explanation: 'Par convention sous Debian/Ubuntu, le socket réside dans `/var/run/postgresql`, alors que sous RHEL/CentOS historique, il se trouve dans `/tmp`. Définir les deux chemins séparés par une virgule garantit une compatibilité totale des clients.',
      examTrap: 'Si vous supprimez par inadvertance le fichier temporaire `.s.PGSQL.5432` dans `/tmp`, les connexions locales sans `-h` échoueront immédiatement.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3.1: Connection Settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-074',
    cardNumber: 74,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Dimensionnement de `max_connections` et impacts collatéraux',
    difficulty: 'medium',
    tags: ['max_connections', 'Scaling', 'RAM', 'Connection Pooler', 'Postmaster'],
    front: {
      question: 'Quelle est la valeur par défaut de `max_connections`, pourquoi est-il dangereux de l\'augmenter à des valeurs extrêmes (ex: 2000), et quelle est la solution recommandée ?',
      hint: 'Chaque connexion alloue un processus lourd et consomme de la mémoire partagée et locale.',
    },
    back: {
      answer: 'Valeur par défaut : `100` connexions concurrentes (contexte `postmaster`).\nRisques d\'une valeur trop élevée (ex: 1000 ou 2000) :\n1. Chaque connexion est un processus OS dédié (`fork()`) consommant ~5 à 10 Mo de RAM de base, plus ses allocations dynamiques de `work_mem`.\n2. Risque majeur d\'Out-Of-Memory (OOM Killer) si de multiples requêtes consomment du `work_mem` simultanément.\n3. Contention sévère sur les verrous internes de mémoire partagée (`LWLocks` et `ProcArrayLock`) et dégradation exponentielle du CPU lors des changements de contexte.\n\nSolution recommandée : Maintenir `max_connections` entre 100 et 300 et installer un pooler de connexions externe léger comme PgBouncer.',
      explanation: 'PostgreSQL atteint son débit transactionnel optimal lorsque le nombre de requêtes actives simultanées est proche du nombre de cœurs CPU physiques disponibles.',
      examTrap: 'Augmenter `max_connections` augmente directement la taille de la mémoire partagée allouée au démarrage pour les tables de verrouillage, même si ces connexions ne sont pas utilisées.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3.1: Connection Settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-075',
    cardNumber: 75,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Réservation de secours avec `superuser_reserved_connections`',
    difficulty: 'medium',
    tags: ['superuser_reserved_connections', 'Superuser', 'Emergency', 'Saturation'],
    front: {
      question: 'À quoi sert le paramètre `superuser_reserved_connections` (défaut 3) lorsque le serveur atteint sa limite de connexions ?',
      codeSnippet: 'max_connections = 100\nsuperuser_reserved_connections = 5',
      hint: 'Il garantit que le DBA pourra toujours se connecter pour diagnostiquer la panne même si les applications ont saturé le pool.',
    },
    back: {
      answer: '`superuser_reserved_connections` réserve un quota de slots de connexion (par défaut 3) exclusivement aux comptes possédant le privilège `SUPERUSER`.\nFonctionnement :\n- Si `max_connections = 100` et `superuser_reserved_connections = 3`, les utilisateurs ordinaires ne peuvent occuper que 97 connexions simultanées.\n- Dès que 97 connexions sont atteintes, tout nouvel utilisateur standard reçoit l\'erreur : "FATAL: remaining connection slots are reserved for non-replication superuser connections".\n- Le DBA superutilisateur peut néanmoins toujours ouvrir une session pour inspecter `pg_stat_activity` et tuer les requêtes bloquantes.',
      explanation: 'C\'est une soupape de sécurité vitale pour éviter le verrouillage total d\'une instance en production par déni de service involontaire d\'une application.',
      examTrap: 'La valeur de `superuser_reserved_connections` doit obligatoirement être strictement inférieure à `max_connections`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3.1: Connection Settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-076',
    cardNumber: 76,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Chiffrement SSL/TLS : `ssl = on`, certificats et clés',
    difficulty: 'medium',
    tags: ['SSL', 'TLS', 'Security', 'Encryption', 'Certificates'],
    front: {
      question: 'Quels sont les trois paramètres de base à configurer dans `postgresql.conf` pour activer le chiffrement SSL/TLS des échanges réseau ?',
      codeSnippet: 'ssl = on\nssl_cert_file = \'server.crt\'\nssl_key_file = \'server.key\'\nssl_ca_file = \'root.crt\'',
      hint: 'L\'activation du booléen ssl et l\'indication des fichiers de certificat et de clé privée sont obligatoires.',
    },
    back: {
      answer: 'Pour activer le chiffrement SSL/TLS :\n1. `ssl = on` : Active la négociation du protocole TLS lors de la poignée de main initiale avec les clients.\n2. `ssl_cert_file = \'chemin/vers/server.crt\'` : Certificat public du serveur x509.\n3. `ssl_key_file = \'chemin/vers/server.key\'` : Clé privée correspondante.\n\nOptionnellement, `ssl_ca_file = \'root.crt\'` permet de vérifier les certificats des clients lors de l\'authentification mutuelle (mTLS / `cert`).',
      explanation: 'Sous PostgreSQL 16, l\'activation de `ssl = on` est de contexte `sighup` : on peut activer SSL ou recharger un certificat renouvelé sans redémarrer le serveur via `SELECT pg_reload_conf();` !',
      examTrap: 'Le fichier de clé privée (`server.key`) doit obligatoirement avoir des permissions OS strictes (0600 ou 0400) et appartenir à l\'utilisateur `postgres`. Sinon, le serveur refuse de l\'utiliser.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.9.4: SSL',
    },
  },
  {
    id: 'fc-pgedb-dom02-077',
    cardNumber: 77,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Rechargement à chaud des certificats SSL',
    difficulty: 'hard',
    tags: ['SSL', 'Certificates', 'Reload', 'SIGHUP', 'Maintenance'],
    front: {
      question: 'Est-il nécessaire d\'interrompre les connexions clientes ou de redémarrer le serveur PostgreSQL pour renouveler un certificat SSL expirant ?',
      hint: 'Les versions modernes de PostgreSQL permettent de recharger la clé et le certificat via un simple signal SIGHUP.',
    },
    back: {
      answer: 'NON, aucun redémarrage n\'est nécessaire !\nLes paramètres `ssl_cert_file`, `ssl_key_file`, `ssl_ca_file` et `ssl_crl_file` sont de contexte `sighup`.\n\nProcédure de renouvellement sans coupure (Zero Downtime) :\n1. Remplacer les fichiers de certificat et de clé sur le disque par les nouveaux certificats valides.\n2. Vérifier les permissions du fichier de clé (`chmod 600 server.key`).\n3. Exécuter `SELECT pg_reload_conf();` ou `pg_ctl reload`.\n4. Le Postmaster recharge les certificats en mémoire. Les nouvelles connexions négocient immédiatement le nouveau certificat sans impacter les sessions déjà connectées.',
      explanation: 'Cette souplesse élimine les fenêtres d\'interruption nocturne lors des renouvellements automatisés de certificats (ex: Let\'s Encrypt tous les 90 jours).',
      examTrap: 'Si le nouveau certificat et la clé privée ne correspondent pas mathématiquement, `pg_reload_conf()` émet une erreur dans le log et conserve l\'ancien certificat en mémoire sans planter le serveur.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.9.4: SSL',
    },
  },
  {
    id: 'fc-pgedb-dom02-078',
    cardNumber: 78,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Détection des connexions réseau orphelines (TCP Keepalives)',
    difficulty: 'medium',
    tags: ['TCP Keepalives', 'Ghost Connections', 'Network', 'Timeouts'],
    front: {
      question: 'Quels paramètres règlent les sondes TCP Keepalive dans `postgresql.conf` pour détecter et libérer les connexions clientes mortes brutalement (crash client ou coupure réseau) ?',
      codeSnippet: 'tcp_keepalives_idle = 60\ntcp_keepalives_interval = 10\ntcp_keepalives_count = 3',
      hint: 'Trois paramètres contrôlent le délai d\'inactivité avant la première sonde, l\'intervalle entre sondes et le nombre d\'échecs tolérés.',
    },
    back: {
      answer: 'Les paramètres de Keepalive TCP sont :\n1. `tcp_keepalives_idle` : Durée d\'inactivité (en secondes) sur la socket avant d\'envoyer la première sonde TCP Keepalive (ex: 60s).\n2. `tcp_keepalives_interval` : Intervalle (en secondes) entre deux sondes successives si le client ne répond pas (ex: 10s).\n3. `tcp_keepalives_count` : Nombre maximal de sondes consécutives sans réponse avant de déclarer la connexion morte et de libérer le backend (ex: 3 sondes).\n\nDans cet exemple, une connexion morte sera détectée et coupée proprement en 60 + (3 * 10) = 90 secondes.',
      explanation: 'Sans ce réglage, le backend PostgreSQL peut rester bloqué indéfiniment en attendant des données d\'un client éteint, conservant des verrous sur des tables et gaspillant un slot de `max_connections`.',
      examTrap: 'Par défaut, la valeur 0 utilise les réglages du noyau Linux de l\'OS, qui attend souvent 7200 secondes (2 heures !) avant de sonder une socket inactive.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3.1: Connection Settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-079',
    cardNumber: 79,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Gestion des bibliothèques préchargées : `shared_preload_libraries`',
    difficulty: 'hard',
    tags: ['shared_preload_libraries', 'Extensions', 'pg_stat_statements', 'Postmaster'],
    front: {
      question: 'Pourquoi certaines extensions comme `pg_stat_statements` ou `auto_explain` doivent-elles obligatoirement être déclarées dans `shared_preload_libraries` ?',
      codeSnippet: 'shared_preload_libraries = \'pg_stat_statements, auto_explain\'',
      hint: 'Elles s\'injectent dans les hooks internes du moteur et nécessitent d\'allouer de la mémoire partagée dès le boot du serveur.',
    },
    back: {
      answer: '`shared_preload_libraries` contient une liste de bibliothèques C partagées chargées par le Postmaster dès le démarrage de l\'instance.\nObligation pour certaines extensions :\n1. Elles ont besoin d\'allouer une zone de mémoire partagée dédiée (ex: le tableau des requêtes SQL de `pg_stat_statements`).\n2. Elles interceptent les hooks internes du planificateur ou de l\'exécuteur de requêtes dès l\'initialisation.\n3. Contexte : `postmaster` (nécessite obligatoirement un redémarrage complet de l\'instance pour prendre effet).',
      explanation: 'Si vous tentez d\'exécuter `CREATE EXTENSION pg_stat_statements;` sans avoir préalablement configuré `shared_preload_libraries` et redémarré le serveur, PostgreSQL lèvera l\'erreur : "pg_stat_statements must be loaded via shared_preload_libraries".',
      examTrap: 'Une faute d\'orthographe dans un nom de bibliothèque au sein de `shared_preload_libraries` empêchera totalement le serveur PostgreSQL de démarrer.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.7: Shared Library Preloading',
    },
  },
  {
    id: 'fc-pgedb-dom02-080',
    cardNumber: 80,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Utilisation de la mémoire Huge Pages sous Linux (`huge_pages`)',
    difficulty: 'hard',
    tags: ['huge_pages', 'Linux', 'Memory', 'TLB', 'Performance'],
    front: {
      question: 'Quel est l\'intérêt du paramètre `huge_pages` sous Linux et quelles sont ses valeurs possibles (`try`, `on`, `off`) ?',
      codeSnippet: 'huge_pages = try  # valeurs: off, on, try',
      hint: 'Il permet d\'utiliser des pages de mémoire de 2 Mo au lieu de 4 Ko pour réduire les défauts TLB sur de gros shared_buffers.',
    },
    back: {
      answer: '`huge_pages` contrôle l\'utilisation des pages de mémoire de grande taille (2 Mo sous Linux x86_64 par défaut au lieu des pages standard de 4 Ko) pour allouer le segment de `shared_buffers`.\nAvantages :\n- Réduit considérablement la taille de la table des pages du noyau Linux.\n- Évite les misses dans le cache TLB du processeur et améliore les performances de 5 à 15% sur les serveurs avec beaucoup de RAM (shared_buffers > 16 Go).\n\nValeurs :\n- `off` : Désactive les Huge Pages.\n- `try` (défaut moderne) : Tente d\'allouer en Huge Pages ; si le noyau n\'en a pas assez de disponibles, bascule silencieusement sur les pages standard de 4 Ko.\n- `on` : Exige les Huge Pages. Si l\'OS n\'a pas préalloué assez de Huge Pages (`/proc/sys/vm/nr_hugepages`), le serveur REFUSE de démarrer.',
      explanation: 'En production sur des serveurs dédiés, on configure le noyau Linux avec suffisamment de pages et on force `huge_pages = on` pour garantir qu\'aucune pagination lente n\'aura lieu.',
      examTrap: 'Contexte : `postmaster`. Toute modification de `huge_pages` exige un redémarrage complet de l\'instance.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.4.1: Managing Kernel Resources - Huge Pages',
    },
  },

  // =========================================================================
  // SECTION 9 : CONFIGURATION DES JOURNAUX D'ACTIVITÉ (LOGGING & AUDIT) (Cartes 81 à 90)
  // logging_collector, log_destination (csvlog, jsonlog), log_directory, rotation, log_line_prefix
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-081',
    cardNumber: 81,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Processus collecteur de journaux : `logging_collector`',
    difficulty: 'easy',
    tags: ['logging_collector', 'SysLogger', 'Logging', 'Postmaster', 'Audit'],
    front: {
      question: 'À quoi sert le paramètre `logging_collector` et quel processus d\'arrière-plan démarre-t-il lorsqu\'il est activé (`on`) ?',
      codeSnippet: 'logging_collector = on',
      hint: 'Il capture les sorties stderr du serveur et les redirige vers des fichiers journaux gérés avec rotation.',
    },
    back: {
      answer: '`logging_collector = on` active le processus d\'arrière-plan dédié `SysLogger` (ou `logger`).\nRôle :\n1. Il capture tous les messages émis sur la sortie d\'erreur standard (`stderr`) par l\'ensemble des processus PostgreSQL (backends, bgwriter, checkpointer, autovacuum).\n2. Il les écrit dans des fichiers de log rotatifs organisés sous le répertoire configuré (`log_directory`).\n3. Il gère la rotation automatique selon la taille ou le temps écoulé pour éviter la saturation du disque.\n\nContexte : `postmaster` (nécessite un redémarrage pour être activé/désactivé).',
      explanation: 'Si `logging_collector = off`, les messages du serveur sont simplement envoyés sur `stderr`, ce qui est géré soit par systemd (`journald`), soit par une redirection bash brute.',
      examTrap: 'Si `logging_collector = off`, les paramètres `log_directory`, `log_filename` et `log_rotation_age` n\'ont strictement aucun effet !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.1: Where to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-082',
    cardNumber: 82,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Destinations des traces : `log_destination` (stderr, csvlog, jsonlog)',
    difficulty: 'medium',
    tags: ['log_destination', 'csvlog', 'jsonlog', 'PostgreSQL 16', 'ELK'],
    front: {
      question: 'Quels sont les formats de logs supportés par `log_destination` et quelle nouveauté majeure a été introduite dans les versions récentes pour les outils comme Elasticsearch / Datadog ?',
      codeSnippet: 'log_destination = \'csvlog, jsonlog\'',
      hint: 'Le support du format structuré JSON natif a été introduit depuis PostgreSQL 15/16.',
    },
    back: {
      answer: '`log_destination` accepte une liste de formats séparés par des virgules :\n1. `stderr` : Format texte brut standard (défaut).\n2. `csvlog` : Format CSV structuré prêt à être chargé dans une table SQL (`CREATE TABLE postgres_log (...)`).\n3. `jsonlog` : Nouveauté majeure (PG 15+) écrivant chaque événement de log sous forme d\'un objet JSON valide d\'une seule ligne, idéal pour l\'ingestion directe dans la suite Elastic (ELK), Splunk ou Datadog.\n4. `syslog` : Redirection vers le démon syslog local de Linux.\n5. `eventlog` : Pour le journal des événements sous Windows.',
      explanation: 'Pour que `csvlog` ou `jsonlog` fonctionnent, le paramètre `logging_collector` doit impérativement être positionné sur `on`.',
      examTrap: 'Vous pouvez combiner plusieurs destinations (ex: `log_destination = \'stderr, jsonlog\'`), mais attention à l\'impact sur l\'espace disque qui sera doublé.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.1: Where to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-083',
    cardNumber: 83,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Emplacement et nommage des journaux (`log_directory`, `log_filename`)',
    difficulty: 'easy',
    tags: ['log_directory', 'log_filename', 'strftime', 'Logs'],
    front: {
      question: 'Comment configure-t-on l\'emplacement et le nom dynamique des fichiers de journaux avec `log_directory` et `log_filename` ?',
      codeSnippet: 'log_directory = \'log\'\nlog_filename = \'postgresql-%Y-%m-%d_%H%M%S.log\'',
      hint: 'Le nom de fichier accepte les jokers de formatage de date et d\'heure strftime (ex: %Y, %m, %d, %H).',
    },
    back: {
      answer: '1. `log_directory` : Chemin relatif (par rapport à `$PGDATA`) ou absolu du répertoire où sont stockés les fichiers journaux (ex: `log` ou `/var/log/postgresql`).\n2. `log_filename` : Motif de nommage du fichier supportant les spécificateurs de format temporel `strftime` standard :\n   - `%Y` : Année (4 chiffres)\n   - `%m` : Mois (01-12)\n   - `%d` : Jour du mois (01-31)\n   - `%H`, `%M`, `%S` : Heures, minutes, secondes.\n   - `%a` : Nom abrégé du jour (Mon, Tue...) très utile pour les rotations hebdomadaires.',
      explanation: 'Ces deux paramètres ont un contexte `sighup` : ils peuvent être modifiés à chaud sans redémarrer le serveur.',
      examTrap: 'Si `log_directory` est un chemin absolu vers une partition séparée, assurez-vous que l\'utilisateur système `postgres` possède bien les droits d\'écriture sur ce dossier.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.1: Where to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-084',
    cardNumber: 84,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Politique de rotation automatique (`log_rotation_age`, `log_rotation_size`)',
    difficulty: 'medium',
    tags: ['log_rotation_age', 'log_rotation_size', 'Log Rotation', 'Storage'],
    front: {
      question: 'Comment fonctionnent les paramètres `log_rotation_age` et `log_rotation_size` pour déclencher la fermeture et l\'ouverture d\'un nouveau fichier de log ?',
      codeSnippet: 'log_rotation_age = 1d\nlog_rotation_size = 100MB',
      hint: 'La rotation s\'opère dès que la première des deux conditions (temporelle ou volumétrique) est atteinte.',
    },
    back: {
      answer: 'PostgreSQL déclenche la rotation vers un nouveau fichier de log dès que l\'une des deux conditions est satisfaite :\n1. Condition temporelle (`log_rotation_age`) : Temps maximal avant fermeture du fichier courant (ex: `1d` pour une rotation quotidienne, `1h`, ou `0` pour désactiver la rotation par temps).\n2. Condition volumétrique (`log_rotation_size`) : Taille maximale que peut atteindre le fichier de log courant avant de basculer sur un nouveau fichier (ex: `100MB`, `1GB`, ou `0` pour désactiver la rotation par taille).',
      explanation: 'Combiner ces deux critères garantit que les fichiers restent faciles à manipuler avec `grep` ou `less` tout en évitant les fichiers uniques gigantesques de dizaines de gigas.',
      examTrap: 'Si `log_rotation_age = 0` et `log_rotation_size = 0`, PostgreSQL écrira indéfiniment dans le même fichier jusqu\'à saturation de la partition.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.1: Where to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-085',
    cardNumber: 85,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Écrasement cyclique avec `log_truncate_on_rotation`',
    difficulty: 'medium',
    tags: ['log_truncate_on_rotation', 'Log Retention', 'Disk Space', 'Purge'],
    front: {
      question: 'Dans quelle configuration utilise-t-on `log_truncate_on_rotation = on` et comment cela permet-il de créer une rétention tournante automatique sur 7 jours ?',
      codeSnippet: 'log_filename = \'postgresql-%a.log\'\nlog_rotation_age = 1d\nlog_truncate_on_rotation = on',
      hint: 'Si le fichier existe déjà lors de la rotation, il est tronqué et écrasé au lieu d\'ajouter les nouvelles lignes à la suite.',
    },
    back: {
      answer: '`log_truncate_on_rotation = on` indique au collecteur de logs que si un fichier portant le nom cible existe déjà au moment d\'une rotation temporelle, il doit être tronqué (vidé) et écrasé plutôt que de concaténer les logs à la fin (append).\n\nExemple de rétention sur 7 jours glissants :\n- `log_filename = \'postgresql-%a.log\'` (génère 7 fichiers : `postgresql-Mon.log`, `postgresql-Tue.log`, etc.).\n- `log_rotation_age = 1d`.\n- `log_truncate_on_rotation = on`.\nChaque lundi, le fichier `postgresql-Mon.log` de la semaine précédente est écrasé par les nouveaux logs du lundi en cours, garantissant que l\'espace disque reste strictement borné à 7 fichiers sans aucun script cron externe.',
      explanation: 'Si `log_truncate_on_rotation = off`, les logs du nouveau lundi seraient ajoutés à la fin du fichier du lundi précédent, accumulant les données indéfiniment.',
      examTrap: 'Attention : l\'écrasement ne se produit que lors d\'une rotation temporelle (`log_rotation_age`) et JAMAIS lors d\'une rotation déclenchée par la taille (`log_rotation_size`).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.1: Where to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-086',
    cardNumber: 86,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Niveaux de sévérité : `log_min_messages` et `client_min_messages`',
    difficulty: 'medium',
    tags: ['log_min_messages', 'client_min_messages', 'Severity', 'DEBUG', 'ERROR'],
    front: {
      question: 'Quelle est la hiérarchie des niveaux de sévérité des messages dans PostgreSQL et quelle est la différence entre `log_min_messages` et `client_min_messages` ?',
      hint: 'L\'un filtre ce qui est consigné sur disque dans le fichier de log du serveur, l\'autre ce qui est affiché à l\'écran du client psql.',
    },
    back: {
      answer: 'Hiérarchie croissante des niveaux de sévérité :\n`DEBUG5` < `DEBUG4` < `DEBUG3` < `DEBUG2` < `DEBUG1` < `INFO` < `NOTICE` < `WARNING` < `ERROR` < `LOG` < `FATAL` < `PANIC`.\n\nDifférence fondamentale :\n1. `log_min_messages` (défaut `WARNING`) : Filtre les messages écrits dans le fichier journal du serveur. Tout message d\'une sévérité égale ou supérieure est consigné sur disque.\n2. `client_min_messages` (défaut `NOTICE`) : Filtre les messages renvoyés en direct sur l\'écran du client applicatif ou du terminal `psql`.',
      explanation: 'En production, on évite absolument de passer `log_min_messages` en `DEBUG` sous peine de générer des gigaoctets de logs par minute et d\'effondrer les performances disque.',
      examTrap: 'Notez la place particulière de `LOG` : dans le serveur, `LOG` a une priorité située entre `ERROR` et `FATAL`, garantissant qu\'un événement système est bien consigné sans être considéré comme une erreur par le client.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.2: When to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-087',
    cardNumber: 87,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Surveillance des requêtes lentes avec `log_min_duration_statement`',
    difficulty: 'easy',
    tags: ['log_min_duration_statement', 'Slow Queries', 'Performance', 'Troubleshooting'],
    front: {
      question: 'Comment configure-t-on `log_min_duration_statement` pour capturer dans les logs toutes les requêtes SQL dont l\'exécution dépasse 250 millisecondes ?',
      codeSnippet: 'log_min_duration_statement = 250  # en millisecondes',
      hint: 'La valeur s\'exprime en millisecondes. -1 désactive, 0 logue tout.',
    },
    back: {
      answer: 'Pour enregistrer les requêtes lentes dépassant 250 ms, on configure :\n`log_min_duration_statement = 250` (ou `\'250ms\'`).\n\nValeurs remarquables :\n- `-1` (défaut) : Désactive totalement la journalisation des requêtes basée sur la durée.\n- `0` : Journalise l\'intégralité des instructions SQL exécutées sur le serveur ainsi que leur durée (extrêmement verbeux, réservé au débogage ponctuel).\n- `> 0` : Journalise le texte SQL complet et la durée précise (en ms) de toute requête dépassant ce seuil.\n\nContexte : `sighup` (rechargeable à chaud via `SELECT pg_reload_conf();`).',
      explanation: 'C\'est le premier outil natif utilisé par les DBA pour identifier les requêtes consommatrices sans installer d\'extension tierce.',
      examTrap: 'Ne pas confondre avec `log_duration = on`, qui consigne la durée de chaque requête mais sans inclure le texte SQL si `log_statement = none`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.3: What to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-088',
    cardNumber: 88,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Formatage du préfixe des lignes de journal (`log_line_prefix`)',
    difficulty: 'medium',
    tags: ['log_line_prefix', 'Log Analysis', 'Audit', 'Prefix', 'Formatting'],
    front: {
      question: 'Quels sont les spécificateurs les plus importants à inclure dans `log_line_prefix` pour permettre un audit précis et l\'analyse avec `pgBadger` ?',
      codeSnippet: 'log_line_prefix = \'%m [%p] %q%u@%d from %h \'',
      hint: 'Ils permettent de tracer l\'horodatage précis, le PID, l\'utilisateur, la base de données et l\'IP cliente.',
    },
    back: {
      answer: 'Le paramètre `log_line_prefix` définit la chaîne préfixant chaque ligne écrite dans le log.\nSpécificateurs essentiels recommandés (notamment pour l\'analyseur `pgBadger`) :\n- `%m` : Horodatage précis avec millisecondes (ex: 2024-03-08 14:32:01.428 CET).\n- `%p` : PID du processus backend PostgreSQL ayant émis le message.\n- `%u` : Nom de l\'utilisateur/rôle connecté.\n- `%d` : Nom de la base de données ciblée.\n- `%h` : Adresse IP ou nom d\'hôte du client distant (ou socket).\n- `%a` : Nom de l\'application cliente (`application_name`).\n- `%q` : Ne rien afficher sauf si une session cliente est active (évite d\'afficher des champs vides pour les démons système comme autovacuum).',
      explanation: 'Un préfixe bien calibré est indispensable pour corréler un message d\'erreur avec une session dans `pg_stat_activity` ou un incident réseau.',
      examTrap: 'Si `log_line_prefix` est laissé vide ou par défaut sans `%m` et `%p`, il devient impossible de savoir quel utilisateur ou quel processus a provoqué une erreur ou un blocage.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.3: What to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-089',
    cardNumber: 89,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Traçabilité des connexions et déconnexions (`log_connections`)',
    difficulty: 'easy',
    tags: ['log_connections', 'log_disconnections', 'Audit', 'Security', 'Compliance'],
    front: {
      question: 'À quoi servent les paramètres `log_connections` et `log_disconnections` et quel est leur impact en environnement à fort trafic ?',
      codeSnippet: 'log_connections = on\nlog_disconnections = on',
      hint: 'Ils enregistrent chaque tentative de connexion réussie et chaque fermeture de session avec sa durée.',
    },
    back: {
      answer: '1. `log_connections = on` : Enregistre dans le log chaque tentative de connexion client réussie, avec le rôle, l\'adresse IP cliente et la base ciblée.\n2. `log_disconnections = on` : Enregistre chaque fin de session cliente, en précisant la durée totale de la connexion.\n\nImpact en production :\n- Très utile pour la conformité et la sécurité (audit des accès).\n- ATTENTION : Si l\'application n\'utilise pas de pooler de connexions et ouvre/ferme des centaines de connexions par seconde (anti-pattern PHP/CGI), cela génère une pollution massive des journaux et surcharge l\'I/O disque.',
      explanation: 'Ces paramètres ont un contexte `backend` : un rechargement ne s\'applique qu\'aux connexions établies ultérieurement.',
      examTrap: 'Ne confondez pas avec `log_duration` : `log_disconnections` mesure la durée de vie globale de la session, pas la durée des requêtes SQL.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.3: What to Log',
    },
  },
  {
    id: 'fc-pgedb-dom02-090',
    cardNumber: 90,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Journalisation des attentes de verrous : `log_lock_waits`',
    difficulty: 'medium',
    tags: ['log_lock_waits', 'deadlock_timeout', 'Locks', 'Concurrency', 'Troubleshooting'],
    front: {
      question: 'Comment fonctionne `log_lock_waits = on` et au bout de combien de temps consigne-t-il un message d\'avertissement dans le journal ?',
      codeSnippet: 'log_lock_waits = on\ndeadlock_timeout = 1s',
      hint: 'Il écrit dans le log dès qu\'une session attend l\'acquisition d\'un verrou pendant une durée supérieure à deadlock_timeout.',
    },
    back: {
      answer: 'Lorsque `log_lock_waits = on` :\n1. Si une transaction tente d\'acquérir un verrou (ex: `RowExclusiveLock` sur une ligne) déjà détenu par une autre session, elle attend.\n2. Si l\'attente dépasse la durée définie par `deadlock_timeout` (par défaut 1 seconde), PostgreSQL consigne immédiatement un message `LOG` dans le journal détaillant la requête bloquée, le PID bloqué et le PID bloquant.\n3. Dès que le verrou est enfin libéré et acquis, PostgreSQL enregistre un second message indiquant que l\'attente a pris fin et la durée totale du blocage.',
      explanation: 'C\'est l\'un des paramètres les plus puissants pour diagnostiquer les contentions applicatives et les interblocages sans perturber la production.',
      examTrap: 'L\'activation de `log_lock_waits` n\'augmente pas le risque de deadlock : elle informe simplement le DBA des blocages anormaux dépassant 1 seconde.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.8.3: What to Log',
    },
  },

  // =========================================================================
  // SECTION 10 : PARAMÈTRES SYSTÈME, VARIABLES CLIENTES & OUTILS DE DIAGNOSTIC (Cartes 91 à 100)
  // Timeouts, variables libpq, .pgpass, pg_checksums, pg_config, timezone
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-091',
    cardNumber: 91,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Paramètre de protection contre les requêtes infinies : `statement_timeout`',
    difficulty: 'easy',
    tags: ['statement_timeout', 'Timeouts', 'Protection', 'Resource Management'],
    front: {
      question: 'Quel est le rôle du paramètre `statement_timeout` et pourquoi est-il dangereux de le positionner globalement au niveau serveur à une valeur trop courte ?',
      codeSnippet: 'statement_timeout = \'30s\'',
      hint: 'Il interrompt automatiquement toute requête SQL dont l\'exécution dépasse la limite fixée.',
    },
    back: {
      answer: '`statement_timeout` définit le temps maximal (en millisecondes ou avec unité : `\'30s\'`, `\'5min\'`) qu\'une instruction SQL individuelle est autorisée à s\'exécuter.\nDès que ce délai est franchi, PostgreSQL annule immédiatement la requête et renvoie l\'erreur : "canceling statement due to statement timeout".\n\nRisque d\'une valeur globale trop stricte dans `postgresql.conf` :\nCela fera échouer les opérations administratives lourdes telles que les sauvegardes logiques (`pg_dump`), les créations d\'index volumineux (`CREATE INDEX`) ou les `VACUUM` manuels.',
      explanation: 'Bonne pratique : Laisser `statement_timeout = 0` (désactivé) dans `postgresql.conf`, et le surcharger pour les rôles applicatifs web via `ALTER ROLE app_user SET statement_timeout = \'10s\';`.',
      examTrap: 'La valeur 0 (valeur par défaut) désactive complètement le timeout, autorisant les requêtes à tourner indéfiniment.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.11: Client Connection Defaults',
    },
  },
  {
    id: 'fc-pgedb-dom02-092',
    cardNumber: 92,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Protection contre les blocages de DDL : `lock_timeout`',
    difficulty: 'medium',
    tags: ['lock_timeout', 'Locks', 'DDL', 'Migrations', 'Safety'],
    front: {
      question: 'À quoi sert `lock_timeout` et pourquoi est-il fortement recommandé de l\'utiliser lors des déploiements de migrations de schéma (`ALTER TABLE`) ?',
      codeSnippet: 'SET lock_timeout = \'5s\';\nALTER TABLE commandes ADD COLUMN status VARCHAR(20);',
      hint: 'Il limite le temps d\'attente pour obtenir un verrou sur une table avant d\'abandonner l\'opération.',
    },
    back: {
      answer: '`lock_timeout` interrompt l\'instruction SQL si celle-ci ne parvient pas à acquérir le verrou requis dans le temps imparti (ex: `5s`).\nPourquoi l\'utiliser en migration de schéma :\nUne commande `ALTER TABLE` demande un verrou exclusif (`AccessExclusiveLock`). Si une transaction concurrente lit la table, le DDL se met en attente.\nMais pire : toutes les requêtes SELECT suivantes sur cette table se retrouvent alors bloquées DERRIÈRE le DDL en attente, provoquant un bouchon général et la saturation de `max_connections`.\nAvec `lock_timeout = \'5s\'`, si le DDL ne peut pas verrouiller la table en 5 secondes, il échoue et se retire immédiatement, sauvant la production.',
      explanation: 'Contrairement à `statement_timeout` qui mesure le temps d\'exécution total, `lock_timeout` ne mesure que le temps passé en file d\'attente d\'acquisition du verrou.',
      examTrap: 'Si `statement_timeout` est plus court que `lock_timeout`, c\'est `statement_timeout` qui coupera la requête en premier.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.11: Client Connection Defaults',
    },
  },
  {
    id: 'fc-pgedb-dom02-093',
    cardNumber: 93,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Élimination des transactions fantômes : `idle_in_transaction_session_timeout`',
    difficulty: 'medium',
    tags: ['idle_in_transaction', 'Bloat', 'Wraparound', 'xmin horizon', 'Troubleshooting'],
    front: {
      question: 'Pourquoi une session en état `idle in transaction` est-elle désastreuse pour PostgreSQL et quel paramètre permet de la tuer automatiquement ?',
      codeSnippet: 'idle_in_transaction_session_timeout = \'10min\'',
      hint: 'Elle bloque l\'horizon xmin, empêche le VACUUM de nettoyer les tuples morts et provoque l\'explosion de la table (bloat).',
    },
    back: {
      answer: 'Une session "idle in transaction" a exécuté un `BEGIN` suivi d\'une commande (ex: `UPDATE` ou `SELECT`), mais le code applicatif a oublié de faire `COMMIT` ou `ROLLBACK`.\nConséquences désastreuses :\n1. Elle retient un ID de transaction (`xmin horizon`) figé dans le passé.\n2. Le processus `VACUUM` ne peut plus nettoyer aucun dead tuple généré après cet xmin sur l\'ensemble de la base !\n3. Cela entraîne un gonflement massif des tables et index (bloat) et mène au risque de TXID Wraparound.\n\nLe paramètre `idle_in_transaction_session_timeout` (ex: `10min` ou `60s`) détecte cet état d\'inactivité avec transaction ouverte et termine de force la session cliente fautive en effectuant un rollback propre.',
      explanation: 'PostgreSQL 14 a également introduit `idle_session_timeout` qui ferme les connexions simplement inactives (en dehors d\'une transaction).',
      examTrap: 'La valeur 0 (défaut) désactive le timeout, autorisant une session oubliée à bloquer le VACUUM pendant des semaines.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.11: Client Connection Defaults',
    },
  },
  {
    id: 'fc-pgedb-dom02-094',
    cardNumber: 94,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Variables d\'environnement clientes officielles de la bibliothèque libpq',
    difficulty: 'easy',
    tags: ['libpq', 'Environment Variables', 'PGHOST', 'PGPORT', 'psql'],
    front: {
      question: 'Quelles sont les variables d\'environnement clientes de la bibliothèque `libpq` utilisées par `psql`, `pg_dump` et les drivers applicatifs ?',
      codeSnippet: 'export PGHOST=192.168.1.50\nexport PGPORT=5432\nexport PGDATABASE=production\nexport PGUSER=app_owner',
      hint: 'Elles évitent de saisir les commutateurs de ligne de commande -h, -p, -d, -U lors de chaque appel.',
    },
    back: {
      answer: 'Les variables d\'environnement clientes majeures sont :\n- `PGHOST` : Adresse IP, nom d\'hôte ou répertoire du socket Unix (équivaut à `-h`).\n- `PGPORT` : Numéro de port TCP (équivaut à `-p`, défaut 5432).\n- `PGDATABASE` : Nom de la base de données cible (équivaut à `-d`).\n- `PGUSER` : Nom du rôle/utilisateur pour la connexion (équivaut à `-U`).\n- `PGPASSWORD` : Mot de passe de connexion (fortement déconseillé en production, préférer `~/.pgpass`).\n- `PGPASSFILE` : Chemin alternatif vers le fichier `.pgpass`.\n- `PGSERVICE` : Nom du profil de service défini dans `pg_service.conf`.',
      explanation: 'Ces variables sont reconnues par tous les outils natifs de PostgreSQL ainsi que par la plupart des connecteurs applicatifs (Python psycopg, PHP PDO, Node-pg, Go pgx).',
      examTrap: 'Ne définissez jamais `PGPASSWORD` dans les scripts shell ou fichiers bash_profile car la variable est visible en clair dans l\'environnement des processus (`ps e`).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 34.15: Environment Variables',
    },
  },
  {
    id: 'fc-pgedb-dom02-095',
    cardNumber: 95,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Fichier de mots de passe automatique `~/.pgpass`',
    difficulty: 'medium',
    tags: ['.pgpass', 'Security', 'Authentication', 'Automation', 'chmod 0600'],
    front: {
      question: 'Quel est le format exact d\'une ligne dans le fichier `~/.pgpass` et quelle est l\'exigence stricte sur ses permissions de fichier ?',
      codeSnippet: '# ~/.pgpass\nhostname:port:database:username:password',
      hint: 'Le fichier comporte 5 champs séparés par des deux-points et exige des permissions 0600.',
    },
    back: {
      answer: '1. Format d\'une entrée (5 champs séparés par des deux-points) :\n`hostname:port:database:username:password`\nLe caractère astérisque `*` peut être utilisé comme joker universel (ex: `*:5432:*:backup_user:MonMotDePasseSecret123`).\n\n2. Exigence de sécurité absolue sur les permissions :\nLe fichier doit impérativement avoir des droits octaux restreints à son propriétaire :\n`chmod 0600 ~/.pgpass`\nSi le fichier accorde des droits de lecture au groupe ou à d\'autres utilisateurs (ex: `0644`), `psql` et `libpq` IGNORENT TOTALEMENT le fichier et redemandent le mot de passe de manière interactive.',
      explanation: 'Ce mécanisme sécurisé évite d\'avoir à coder en dur des mots de passe dans les scripts de sauvegarde crontab ou d\'administration.',
      examTrap: 'Une question classique d\'examen présente un script de sauvegarde qui échoue en demandant un mot de passe malgré un `.pgpass` rempli : la cause est un `chmod` erroné supérieur à 0600.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 34.16: The Password File',
    },
  },
  {
    id: 'fc-pgedb-dom02-096',
    cardNumber: 96,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Fichier de services de connexion `pg_service.conf`',
    difficulty: 'medium',
    tags: ['pg_service.conf', 'Connection Profiles', 'TNSNAMES', 'libpq', 'Service'],
    front: {
      question: 'À quoi sert le fichier `pg_service.conf` et quelle est son utilité pour simplifier la configuration des applications et outils clients ?',
      codeSnippet: '# /etc/postgresql-common/pg_service.conf ou ~/.pg_service.conf\n[reporting_prod]\nhost=10.0.1.10\nport=5432\ndbname=bi_dw\nuser=analyste',
      hint: 'Il fonctionne comme le fichier tnsnames.ora d\'Oracle pour définir des alias de connexion nommés.',
    },
    back: {
      answer: 'Le fichier `pg_service.conf` permet de définir des profils de connexion nommés (services) regroupant tous les paramètres de connexion sous un alias entre crochets `[nom_service]`.\nUtilisation :\n- En ligne de commande : `psql service=reporting_prod`.\n- Dans une application : Utiliser la chaîne `service=reporting_prod` au lieu de coder en dur les adresses IP et ports.\n\nAvantages majeurs :\nEn cas de bascule vers un serveur réplica ou de modification d\'IP réseau, il suffit de modifier `pg_service.conf` en un lieu unique sans retoucher au code des applications clientes.',
      explanation: 'L\'emplacement par défaut est `~/.pg_service.conf` pour l\'utilisateur ou `/etc/pg_service.conf` au niveau système (ou défini par `PGSERVICEFILE`).',
      examTrap: 'Ne confondez pas `pg_service.conf` (profils de connexion clients) avec `pg_hba.conf` (règles de sécurité du serveur).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 34.17: The Connection Service File',
    },
  },
  {
    id: 'fc-pgedb-dom02-097',
    cardNumber: 97,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Gestion des fuseaux horaires (`timezone`, `log_timezone`)',
    difficulty: 'easy',
    tags: ['timezone', 'log_timezone', 'UTC', 'Time', 'Configuration'],
    front: {
      question: 'Quelle est la différence entre le paramètre `timezone` et le paramètre `log_timezone` dans `postgresql.conf` ?',
      codeSnippet: 'timezone = \'Europe/Paris\'\nlog_timezone = \'UTC\'',
      hint: 'L\'un gère l\'affichage et la conversion des données SQL des sessions, l\'autre l\'horodatage inscrit dans les fichiers logs.',
    },
    back: {
      answer: '1. `timezone` (défaut hérité de l\'OS ou UTC) :\n- Définit le fuseau horaire par défaut utilisé par le moteur SQL pour l\'affichage et la saisie des types temporels avec fuseau horaire (`TIMESTAMP WITH TIME ZONE`).\n- Contexte `user` : chaque session cliente peut redéfinir son propre fuseau via `SET timezone = \'America/New_York\';`.\n\n2. `log_timezone` (défaut hérité de l\'OS ou UTC) :\n- Définit le fuseau horaire utilisé pour consigner les horodatages des événements dans les fichiers de logs du serveur.\n- Contexte `sighup` : s\'applique transversalement à l\'ensemble du serveur.',
      explanation: 'En production, la recommandation d\'or des architectes de données est de configurer `log_timezone = \'UTC\'` pour faciliter la corrélation des incidents entre serveurs répartis sur plusieurs continents.',
      examTrap: 'PostgreSQL stocke toujours les valeurs `timestamptz` en interne converties en UTC : `timezone` ne contrôle que la conversion lors de l\'entrée et de la sortie.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.11.2: Locale and Formatting',
    },
  },
  {
    id: 'fc-pgedb-dom02-098',
    cardNumber: 98,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Outil de build et chemins système : `pg_config`',
    difficulty: 'medium',
    tags: ['pg_config', 'Compilation', 'Extensions', 'Paths', 'Devel'],
    front: {
      question: 'À quoi sert l\'utilitaire en ligne de commande `pg_config` et quelle est son importance lors de l\'installation d\'extensions externes ?',
      codeSnippet: '$ pg_config --bindir\n/usr/pgsql-16/bin\n$ pg_config --version\nPostgreSQL 16.2',
      hint: 'Il affiche les paramètres de configuration et les répertoires d\'installation de la version courante.',
    },
    back: {
      answer: '`pg_config` est un outil d\'information qui renvoie les chemins de compilation et répertoires d\'installation de la version PostgreSQL active :\n- `--bindir` : Emplacement des exécutables.\n- `--pkglibdir` : Emplacement des bibliothèques dynamiques partagées (.so / .dll).\n- `--sharedir` : Emplacement des fichiers de support et scripts SQL des extensions.\n- `--includedir-server` : En-têtes C pour développer des modules serveur.\n- `--configure` : Affiche les options exactes passées au script `./configure` lors du build.\n\nLors de la compilation ou de l\'installation d\'une extension tierce (comme PostGIS ou pgvector via `make`), le Makefile interroge automatiquement `pg_config` pour localiser la bonne version cible de PostgreSQL.',
      explanation: 'Si plusieurs versions de PostgreSQL cohabitent sur le même serveur, positionner le bon chemin de `pg_config` dans la variable `PATH` garantit que l\'extension sera compilée pour la bonne version.',
      examTrap: '`pg_config` est fourni avec le paquet client/devel et ne nécessite pas que le serveur de base de données soit en cours d\'exécution.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_config',
    },
  },
  {
    id: 'fc-pgedb-dom02-099',
    cardNumber: 99,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Activation et vérification des checksums à froid avec `pg_checksums`',
    difficulty: 'hard',
    tags: ['pg_checksums', 'Data Checksums', 'Offline', 'Integrity', 'Corruption'],
    front: {
      question: 'Comment utilise-t-on l\'utilitaire `pg_checksums` (introduit en PG 12) pour vérifier l\'intégrité des blocs ou activer les checksums sur un cluster existant ?',
      codeSnippet: '$ pg_ctl stop -m fast\n$ pg_checksums -e -D /var/lib/pgsql/16/data',
      hint: 'Cet outil doit impérativement être exécuté hors ligne, c\'est-à-dire avec le serveur complètement arrêté.',
    },
    back: {
      answer: 'L\'outil `pg_checksums` permet d\'activer, de désactiver ou de vérifier les sommes de contrôle sur un cluster de données :\n1. `-c` (ou `--check`) : Vérifie l\'intégrité de toutes les pages de données de toutes les tables du cluster et signale les blocs corrompus.\n2. `-e` (ou `--enable`) : Active le calcul des checksums sur un cluster initialisé sans l\'option `-k` (parcourt et met à jour l\'en-tête de chaque page existante sur disque).\n3. `-d` (ou `--disable`) : Désactive les checksums.\n\nCONDITION IMPÉRATIVE ABSOLUE :\nLe serveur PostgreSQL doit être TOTALEMENT ARRÊTÉ (`Database cluster state: shut down`). Toute tentative d\'exécuter `pg_checksums` sur un serveur actif est immédiatement rejetée pour éviter la corruption de données.',
      explanation: 'Avant PostgreSQL 12, si vous aviez oublié l\'option `-k` lors de `initdb`, la seule façon d\'activer les checksums était un dump/restore complet du cluster !',
      examTrap: 'Sur l\'examen, retenez qu\'on ne peut JAMAIS exécuter `pg_checksums` à chaud : l\'instance doit être arrêtée proprement.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_checksums',
    },
  },
  {
    id: 'fc-pgedb-dom02-100',
    cardNumber: 100,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Synthèse d\'architecture : Matrice de cycle de vie et d\'administration d\'une instance',
    difficulty: 'hard',
    tags: ['Architecture', 'Summary', 'initdb', 'pg_ctl', 'postgresql.conf', 'Certification'],
    front: {
      question: 'Quelles sont les 5 étapes séquentielles du cycle de vie complet d\'une instance PostgreSQL, de son installation à sa mise en production sécurisée ?',
      hint: 'De l\'installation des paquets jusqu\'à l\'audit dynamique des paramètres.',
    },
    back: {
      answer: 'Les 5 étapes fondamentales sont :\n1. Déploiement : Installation des paquets officiels PGDG/EDB (`postgresql16-server`, `contrib`) et création de l\'utilisateur système dédié `postgres`.\n2. Initialisation : Exécution d\'`initdb -D $PGDATA -E UTF8 --locale=C -k` (sélection de l\'encodage Unicode, locales stables et activation des checksums).\n3. Configuration de base : Réglage des directives fondamentales dans `postgresql.conf` (`listen_addresses`, `port`, `shared_buffers`, `logging_collector`, `include_dir`) et sécurisation du contrôle d\'accès dans `pg_hba.conf`.\n4. Démarrage et service : Lancement du serveur via le gestionnaire de services OS (`systemctl start postgresql-16` ou `pg_ctl start -D $PGDATA -l logfile -w`).\n5. Supervision et tuning continu : Gestion dynamique des surcharges (`ALTER SYSTEM`, `pg_reload_conf()`) et audit préventif via les vues système `pg_settings` et `pg_file_settings`.',
      explanation: 'Maîtriser ce cycle de vie et l\'interaction entre les fichiers disques et la mémoire est le cœur des compétences requises pour la certification PostgreSQL EDB Certified Associate.',
      examTrap: 'Veillez à toujours valider les droits de fichiers (`chmod 700 $PGDATA`) et la cohérence de `pg_hba.conf` avant d\'exposer le port réseau en écoute sur toutes les interfaces.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19: Server Configuration & EDB Certified Associate Blueprint',
    },
  },
];
