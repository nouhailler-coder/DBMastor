import { FlashcardItem } from '../types';

export const mysql908Dom04Part2Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 4 : SAUVEGARDE PHYSIQUE AVEC MYSQL ENTERPRISE BACKUP (MEB) (Cartes 51 à 70)
  // Architecture, mysqlbackup, phases, redo logs, LSN, incrémentale,
  // compression, chiffrement TDE, verrous BACKUP LOCK
  // =========================================================================
  {
    id: 'fc-mysql908-dom04-051',
    cardNumber: 51,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Principe fondamental de MySQL Enterprise Backup (MEB) vs Outils logiques',
    difficulty: 'medium',
    tags: ['MEB', 'mysqlbackup', 'Physical Backup', 'Architecture'],
    front: {
      question: 'Quel est le principe de fonctionnement de MySQL Enterprise Backup (MEB) et pourquoi surpasse-t-il les sauvegardes logiques en temps de restauration ?',
      hint: 'MEB copie directement les blocs de pages InnoDB (16 Ko) sur disque sans convertir les données en requêtes SQL texte.',
    },
    back: {
      answer: 'Fonctionnement et supériorité de MEB (`mysqlbackup`) :\n\n- **Copie au niveau bloc physique** :\n  - MEB ne génère pas de requêtes SQL (`INSERT`, `CREATE TABLE`). Il copie directement les blocs de données bruts de 16 Ko des tablespaces InnoDB (`.ibd`, `ibdata1`, tablespaces de logs).\n- **Temps de restauration quasi-instantané (RTO ultra-faible)** :\n  - Restaurer un dump logique de 2 To exige de réinsérer 500 millions de lignes, vérifier les contraintes et reconstruire tous les index B-Tree (des heures ou jours).\n  - Restaurer une sauvegarde MEB consiste simplement à recopier les fichiers sur disque à la vitesse brute du SSD/NVMe (quelques minutes), puis à appliquer les redo logs.',
      explanation: 'MEB est le composant d\'entreprise d\'Oracle dédié aux sauvegardes physiques à chaud sans interruption de service.',
      examTrap: 'MEB est un utilitaire propriétaire payant disponible avec l\'édition MySQL Enterprise (bien qu\'équivalent dans le monde open-source à Percona XtraBackup).',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - MySQL Enterprise Backup Overview',
    },
  },
  {
    id: 'fc-mysql908-dom04-052',
    cardNumber: 52,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Commandes et options essentielles de mysqlbackup',
    difficulty: 'medium',
    tags: ['mysqlbackup', 'CLI', 'Options', 'Commands'],
    front: {
      question: 'Quelles sont les commandes fondamentales de l\'utilitaire mysqlbackup pour les trois étapes clés : Sauvegarde, Préparation et Restauration ?',
      codeSnippet: `$ mysqlbackup --user=backup_user --backup-dir=/backups/full backup
$ mysqlbackup --backup-dir=/backups/full apply-log
$ mysqlbackup --backup-dir=/backups/full --datadir=/var/lib/mysql copy-back`,
      hint: 'backup (ou backup-and-apply-log), apply-log (préparation), et copy-back (restauration vers le datadir).',
    },
    back: {
      answer: 'Les 3 étapes fondamentales de `mysqlbackup` :\n\n1. **`backup`** (ou `backup-to-image`) :\n   - Se connecte au serveur en production, copie les fichiers tablespaces physiques et capture tous les redo logs générés pendant la copie.\n2. **`apply-log`** (Étape de préparation) :\n   - Opération hors ligne (serveur de sauvegarde) : applique les redo logs collectés pour rendre les tablespaces cohérents à un point précis dans le temps (Point of Consistency).\n3. **`copy-back`** (Restauration) :\n   - Le serveur MySQL étant arrêté, recopie l\'ensemble des fichiers préparés vers le répertoire de données (`datadir`) cible.',
      explanation: 'La combinaison backup-and-apply-log permet d\'enchaîner directement la phase 1 et la phase 2 si le disque de backup le supporte.',
      examTrap: 'Ne lancez jamais "copy-back" sur un backup dont le log n\'a pas été préparé ("apply-log"), sinon InnoDB démarrera en état de crash recovery non garanti.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - mysqlbackup Command Reference',
    },
  },
  {
    id: 'fc-mysql908-dom04-053',
    cardNumber: 53,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Architecture du processus de sauvegarde à chaud en deux phases dans MEB',
    difficulty: 'hard',
    tags: ['MEB', 'Redo Log', 'Fuzzy Backup', 'Hot Backup'],
    front: {
      question: 'Comment MEB garantit-il la cohérence transactionnelle sans verrouiller les écritures (DML) pendant la copie des fichiers de données ?',
      hint: 'Copie continue des redo logs pendant toute la durée de copie des tablespaces (concept de Fuzzy Checkpoint).',
    },
    back: {
      answer: 'Mécanisme de cohérence sans verrouillage de MEB :\n\n1. **Phase 1 : Enregistrement continu des Redo Logs** :\n   - Dès le démarrage, un thread MEB surveille et enregistre continuellement tous les redo logs générés par le moteur InnoDB dans un fichier temporaire `ibbackup_logfile`.\n2. **Phase 2 : Copie des blocs de données (Tablespaces)** :\n   - En parallèle, un autre thread copie les fichiers `.ibd`. Pendant cette copie qui peut durer 30 minutes, les transactions continuent d\'écrire et de modifier les pages (copie "floue" ou *fuzzy*).\n3. **Clôture de la sauvegarde** :\n   - MEB acquiert un verrou ultra-rapide de fin (`BACKUP LOCK`), note la position exacte du LSN final, et arrête l\'écoute des redo logs.\n4. **Cohérence ultérieure via `apply-log`** :\n   - Le rejeu ultérieur des redo logs réaligne toutes les pages modifiées à la valeur exacte du LSN de fin.',
      explanation: 'Permet une sauvegarde 100% en ligne (hot backup) sans bloquer les SELECT ni les INSERT/UPDATE/DELETE.',
      examTrap: 'Pendant une sauvegarde MEB, les applications peuvent continuer à insérer et mettre à jour sans ralentissement notable.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - How MySQL Enterprise Backup Works',
    },
  },
  {
    id: 'fc-mysql908-dom04-054',
    cardNumber: 54,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Rôle de l\'étape apply-log : Roll-Forward vs Roll-Back',
    difficulty: 'hard',
    tags: ['apply-log', 'Roll-Forward', 'Roll-Back', 'Redo Logs'],
    front: {
      question: 'Dans quel cas précis doit-on appliquer l\'option --uncommitted-rollback-skip lors de la phase apply-log de MEB ?',
      codeSnippet: `$ mysqlbackup --backup-dir=/backups/full --uncommitted-rollback-skip apply-log`,
      hint: 'Nécessaire lorsque l\'on prépare une sauvegarde complète qui va recevoir des sauvegardes incrémentales ultérieures.',
    },
    back: {
      answer: 'Usage critique de `--uncommitted-rollback-skip` :\n\n- **Phase `apply-log` standard** :\n  1. *Roll-Forward* : Rejoue tous les redo logs pour valider les écritures validées.\n  2. *Roll-Back* : Annule les transactions qui étaient encore en cours (non commitées) à la fin du backup.\n- **Quand vous avez des sauvegardes incrémentales à appliquer après** :\n  - Si vous annulez les transactions non commitées tout de suite (*Roll-Back*), vous cassez la continuité de l\'état de la base pour les incrémentales suivantes !\n  - En spécifiant `--uncommitted-rollback-skip`, MEB exécute le *Roll-Forward* mais **diffère le Roll-Back**.\n  - Le Roll-Back final ne sera exécuté qu\'après l\'application de la TOUTE DERNIÈRE sauvegarde incrémentale.',
      explanation: 'Règle d\'or : on ne fait le rollback des transactions inachevées que sur la dernière pièce du puzzle.',
      examTrap: 'Oublier d\'appliquer un rollback final laisserait des transactions fantômes ou des verrous internes dans les tablespaces.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Preparing Full and Incremental Backups',
    },
  },
  {
    id: 'fc-mysql908-dom04-055',
    cardNumber: 55,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Prérequis impératifs pour exécuter copy-back avec MEB',
    difficulty: 'medium',
    tags: ['copy-back', 'Prerequisites', 'Restoration', 'datadir'],
    front: {
      question: 'Quels sont les 3 prérequis stricts du système d\'exploitation et du serveur MySQL avant de lancer une commande mysqlbackup copy-back ?',
      hint: 'État du processus mysqld, contenu du datadir cible, et permissions d\'accès aux fichiers.',
    },
    back: {
      answer: 'Les 3 prérequis impératifs pour `copy-back` :\n\n1. **Arrêt complet du serveur MySQL (`mysqld`)** :\n   - Le démon `mysqld` ne doit pas être en cours d\'exécution (risque de corruption immédiate en écriture concurrente).\n2. **Répertoire `datadir` cible vide ou inexistant** :\n   - Par sécurité, `mysqlbackup` refuse d\'écraser un répertoire de données non vide. Le dossier doit être complètement vidé (`rm -rf /var/lib/mysql/*`) ou l\'option `--force` doit être ajoutée.\n3. **Droits et appartenance des fichiers (`chown mysql:mysql`)** :\n   - Après la copie, l\'utilisateur système exécutant `mysqld` (typiquement `mysql`) doit avoir les droits complets de lecture/écriture sur tous les fichiers et répertoires restaurés.',
      explanation: 'Un grand classique d\'échec de démarrage post-restauration est un mauvais propriétaire de fichier (fichiers appartenant à root).',
      examTrap: 'Ne jamais tenter de restaurer avec copy-back tant que mysqld est actif !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Restoring a Database with mysqlbackup',
    },
  },
  {
    id: 'fc-mysql908-dom04-056',
    cardNumber: 56,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegarde sous forme de fichier conteneurisé unique : Single-File Backup Image',
    difficulty: 'medium',
    tags: ['backup-to-image', 'backup-image', '.mbi', 'Packaging'],
    front: {
      question: 'Comment créer une sauvegarde physique MEB sous la forme d\'un seul fichier archive unique (.mbi) plutôt que d\'une arborescence de dossiers ?',
      codeSnippet: `$ mysqlbackup --user=backup_user --backup-image=/backups/full_2026.mbi backup-to-image`,
      hint: 'Commande backup-to-image associée à l\'option --backup-image.',
    },
    back: {
      answer: 'Fonctionnalité Single-File Backup Image (`.mbi`) :\n\n- La commande **`backup-to-image`** encapsule l\'intégralité des tablespaces, redo logs, fichiers de configuration et métadonnées dans une archive binaire unique (`.mbi` = MySQL Backup Image).\n- **Avantages majeurs** :\n  1. Facilite grandement le transfert via le réseau (SCP, Rsync, S3, OCI Object Storage).\n  2. Permet le streaming direct vers stdout ou un stockage distant sans créer de fichiers intermédiaires sur le disque local (`--backup-image=-`).\n  3. Supporte la compression et le chiffrement direct à la volée dans le conteneur.\n- Pour restaurer directement depuis une image : commande `copy-back-and-apply-log --backup-image=...`.',
      explanation: 'Évite de devoir gérer des millions de petits fichiers physiques sur le stockage de sauvegarde.',
      examTrap: 'L\'extension conventionnelle d\'une image MEB est .mbi (MySQL Backup Image).',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Single-File Backup (backup-to-image)',
    },
  },
  {
    id: 'fc-mysql908-dom04-057',
    cardNumber: 57,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Inspection et validation d\'un fichier image MEB : list-image et validate',
    difficulty: 'easy',
    tags: ['list-image', 'validate', 'MEB', 'Verification'],
    front: {
      question: 'Comment inspecter le contenu d\'un fichier image .mbi et vérifier son intégrité physique sans le restaurer sur disque ?',
      codeSnippet: `$ mysqlbackup --backup-image=/backups/full.mbi list-image
$ mysqlbackup --backup-image=/backups/full.mbi validate`,
      hint: 'Commandes list-image pour afficher les tables contenues et validate pour tester la cohérence des sommes de contrôle.',
    },
    back: {
      answer: 'Inspection et validation d\'images MEB :\n\n- **`list-image`** :\n  - Ouvre l\'archive `.mbi` et affiche la liste exhaustive des tablespaces, tables, fichiers de métadonnées et paramètres de configuration contenus dans l\'image sans rien extraire sur le disque.\n- **`validate`** :\n  - Vérifie les sommes de contrôle cryptographiques (checksums) de chaque page et bloc archivé pour confirmer qu\'aucune corruption de données n\'a eu lieu lors du stockage ou du transfert.\n- **`extract`** :\n  - Permet d\'extraire un tablespace ou un fichier individuel hors de l\'image `.mbi`.',
      explanation: 'Indispensable pour tester la viabilité d\'une sauvegarde avant de lancer une procédure de PRA.',
      examTrap: 'validate permet de certifier qu\'une archive transférée sur le réseau n\'a subi aucune perte de paquets.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Working with Backup Images',
    },
  },
  {
    id: 'fc-mysql908-dom04-058',
    cardNumber: 58,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegarde incrémentale avec MEB : --incremental et --incremental-base',
    difficulty: 'hard',
    tags: ['Incremental Backup', '--incremental', '--incremental-base', 'MEB'],
    front: {
      question: 'Quelle est la syntaxe exacte pour réaliser une sauvegarde incrémentale physique avec mysqlbackup et comment spécifier la sauvegarde de référence ?',
      codeSnippet: `$ mysqlbackup --backup-dir=/backups/inc1 \
  --incremental \
  --incremental-base=dir:/backups/full \
  backup`,
      hint: 'Option --incremental couplée à --incremental-base qui pointe vers le dossier ou l\'image précédente.',
    },
    back: {
      answer: 'Sauvegarde incrémentale physique avec MEB :\n\n- La commande **`--incremental`** indique à MEB de ne sauvegarder que les pages InnoDB modifiées depuis la sauvegarde précédente.\n- **Option `--incremental-base`** :\n  - `dir:/chemin/vers/backup_precedent` : Spécifie le répertoire contenant la sauvegarde complète (ou incrémentale précédente).\n  - `lsn:<LSN_NUMBER>` : Spécifie directement le Log Sequence Number exact à partir duquel copier les pages modifiées.\n- MEB lit le fichier `backup-my.cnf` du backup de référence pour identifier le LSN de fin (`to_lsn`) et ne copie que les pages dont le LSN est supérieur ou égal à cette valeur.',
      explanation: 'Réduit drastiquement le volume de données sauvegardées quotidiennement ainsi que l\'impact sur le sous-système de disques.',
      examTrap: 'Une sauvegarde incrémentale ne contient QUE les pages modifiées ; elle est inexploitable sans sa sauvegarde complète de base.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Making an Incremental Backup',
    },
  },
  {
    id: 'fc-mysql908-dom04-059',
    cardNumber: 59,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Le rôle central du Log Sequence Number (LSN) dans les sauvegardes incrémentales',
    difficulty: 'hard',
    tags: ['LSN', 'Log Sequence Number', 'InnoDB', 'Internals'],
    front: {
      question: 'Qu\'est-ce que le LSN (Log Sequence Number) dans InnoDB et comment MEB l\'utilise-t-il pour déterminer quelles pages doivent être sauvegardées ?',
      hint: 'Compteur continu d\'octets écrits dans le Redo Log ; chaque page InnoDB porte le LSN de sa dernière modification dans son en-tête.',
    },
    back: {
      answer: 'Rôle du LSN (Log Sequence Number) :\n\n- Le LSN est un entier non signé de 64 bits qui s\'incrémente de manière monotone à chaque octet écrit dans le journal de transactions (Redo Log) d\'InnoDB.\n- **En-tête de page InnoDB (Page Header)** :\n  - Chaque page de 16 Ko stocke dans ses premiers octets (`FIL_PAGE_LSN`) le LSN de la dernière transaction qui l\'a modifiée.\n- **Fonctionnement de MEB en incrémentale** :\n  1. MEB lit le `to_lsn` de la sauvegarde de référence (ex: `LSN 5000000`).\n  2. Il parcourt les tablespaces et compare le `FIL_PAGE_LSN` de chaque page.\n  3. Si `FIL_PAGE_LSN >= 5000000`, la page a été modifiée : elle est copiée dans l\'incrémentale.\n  4. Si `FIL_PAGE_LSN < 5000000`, la page est inchangée : elle est ignorée.',
      explanation: 'Mécanisme universel et infaillible d\'InnoDB garantissant la traçabilité temporelle des blocs.',
      examTrap: 'Le scan de toutes les pages d\'un gros fichier peut être long si beaucoup de pages sont inchangées (résolu par le Page Tracking).',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - InnoDB Log Sequence Number',
    },
  },
  {
    id: 'fc-mysql908-dom04-060',
    cardNumber: 60,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Enchaînement chronologique de apply-incremental-backup',
    difficulty: 'hard',
    tags: ['apply-incremental-backup', 'Preparation', 'Recovery Chain'],
    front: {
      question: 'Quelle est la procédure exacte pour restaurer une sauvegarde complète du dimanche et ses 2 sauvegardes incrémentales du lundi et mardi ?',
      codeSnippet: `$ mysqlbackup --backup-dir=/backups/full --uncommitted-rollback-skip apply-log
$ mysqlbackup --backup-dir=/backups/full --incremental-backup-dir=/backups/inc_lundi --uncommitted-rollback-skip apply-incremental-backup
$ mysqlbackup --backup-dir=/backups/full --incremental-backup-dir=/backups/inc_mardi apply-incremental-backup
$ mysqlbackup --backup-dir=/backups/full --datadir=/var/lib/mysql copy-back`,
      hint: 'Préparer la complète avec --uncommitted-rollback-skip, appliquer les incrémentales dans l\'ordre, et ne faire le rollback qu\'à la dernière étape.',
    },
    back: {
      answer: 'Séquence stricte de restauration incrémentale MEB :\n\n1. **Préparer la complète en suspendant le rollback** :\n   `mysqlbackup --backup-dir=/backups/full --uncommitted-rollback-skip apply-log`\n2. **Fusionner l\'incrémentale 1 (Lundi)** :\n   `mysqlbackup --backup-dir=/backups/full --incremental-backup-dir=/backups/inc_lundi --uncommitted-rollback-skip apply-incremental-backup`\n3. **Fusionner l\'incrémentale 2 (Mardi - Dernière incrémentale)** :\n   `mysqlbackup --backup-dir=/backups/full --incremental-backup-dir=/backups/inc_mardi apply-incremental-backup`\n   *(Ici, sans l\'option skip, le rollback final s\'exécute et stabilise l\'ensemble)*.\n4. **Recopier les fichiers finaux dans le datadir** :\n   `mysqlbackup --backup-dir=/backups/full --datadir=/var/lib/mysql copy-back`',
      explanation: 'Chaque incrémentale fusionne ses pages modifiées dans le dossier du backup complet initial.',
      examTrap: 'Inverser l\'ordre des incrémentales (ex: mardi avant lundi) détruit la cohérence des LSN et rend le backup corrompu.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Restoring an Incremental Backup',
    },
  },
  {
    id: 'fc-mysql908-dom04-061',
    cardNumber: 61,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegardes différentielles vs Sauvegardes incrémentales dans MEB',
    difficulty: 'medium',
    tags: ['Differential', 'Incremental', 'Strategy', 'Backup Architecture'],
    front: {
      question: 'Quelle est la différence fondamentale entre une sauvegarde incrémentale cumulative (différentielle) et une incrémentale différentielle stricte dans MEB ?',
      hint: 'La différentielle prend toujours comme base la sauvegarde complète du début de cycle, tandis que l\'incrémentale prend le backup de la veille.',
    },
    back: {
      answer: 'Différence Stratégique :\n\n- **Sauvegarde Incrémentale (Strict / Delta)** :\n  - Base de comparaison : la sauvegarde de la veille (qu\'elle soit complète ou incrémentale).\n  - *Avantage* : Plus rapide à exécuter chaque jour, volume minimal.\n  - *Inconvénient* : Chaîne de restauration fragile (si le fichier du mercredi est corrompu, impossible de restaurer le jeudi).\n- **Sauvegarde Différentielle (Cumulative)** :\n  - Base de comparaison : **toujours la sauvegarde complète du début de semaine** (`--incremental-base=dir:/backups/full`).\n  - *Avantage* : Restauration simplifiée au maximum (Restaurer la Complète + UNIQUEMENT la dernière Différentielle).\n  - *Inconvénient* : Le volume sauvegardé grossit chaque jour au fil de la semaine.',
      explanation: 'Sous MySQL Enterprise Backup, une sauvegarde différentielle est simplement une sauvegarde incrémentale pointant toujours vers le dossier du backup complet.',
      examTrap: 'MEB ne possède pas de commande "differential" dédiée : c\'est l\'option --incremental-base pointée sur la complète qui réalise une différentielle.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Incremental vs Differential Backups',
    },
  },
  {
    id: 'fc-mysql908-dom04-062',
    cardNumber: 62,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Optimisation majeure avec le Page Tracking d\'InnoDB (MySQL 8.0)',
    difficulty: 'hard',
    tags: ['innodb_page_tracking', 'Page Tracking', 'Performance', 'Optimization'],
    front: {
      question: 'Comment la fonctionnalité InnoDB Page Tracking de MySQL 8.0 révolutionne-t-elle la vitesse d\'exécution des sauvegardes incrémentales avec MEB ?',
      hint: 'Permet à MEB de lire une liste bitmap de pages modifiées au lieu de scanner séquentiellement tous les gigaoctets des tablespaces.',
    },
    back: {
      answer: 'Révolution du Page Tracking sous MySQL 8.0 :\n\n- **Sans Page Tracking** :\n  - Pour une base de 10 To, même si seulement 20 Mo de données ont changé dans la journée, MEB doit scanner et lire chaque bloc de 16 Ko des 10 To pour vérifier leur LSN en en-tête (I/O disque intensives de plusieurs heures).\n- **Avec Page Tracking (`page-tracking` actif)** :\n  - Un composant interne InnoDB consigne en mémoire et dans des fichiers de suivi compacts les numéros de page exacts modifiés au fur et à mesure des écritures.\n  - Lors du backup incrémentiel, MEB interroge le composant de suivi, obtient la liste exacte des blocs modifiés, et **accède directement par seek uniquement aux pages cibles**.\n  - La sauvegarde incrémentale de 10 To s\'exécute en quelques secondes ou minutes au lieu de plusieurs heures.',
      explanation: 'Nécessite l\'installation du composant ou plugin de page tracking dans MySQL Server.',
      examTrap: 'Le Page Tracking ne change rien à la taille des données copiées, mais réduit le temps d\'analyse de 99%.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Using Page Tracking with MySQL Enterprise Backup',
    },
  },
  {
    id: 'fc-mysql908-dom04-063',
    cardNumber: 63,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Compression native dans mysqlbackup : options et algorithmes',
    difficulty: 'medium',
    tags: ['Compression', '--compress', 'LZ4', 'ZLIB', 'MEB'],
    front: {
      question: 'Quelles sont les options pour activer la compression dans MEB et quels sont les algorithmes supportés ?',
      codeSnippet: `$ mysqlbackup --backup-image=/backups/backup.mbi \
  --compress \
  --compress-method=lz4 \
  --compress-level=3 \
  backup-to-image`,
      hint: 'Options --compress, --compress-method (lz4 ou zlib), et --compress-level.',
    },
    back: {
      answer: 'Compression dans MySQL Enterprise Backup :\n\n- **Options clés** :\n  - `--compress` : Active la compression des tablespaces et des blocs.\n  - `--compress-method=lz4` (ou `zlib`) :\n    - **`lz4`** : Recommandé pour la production : vitesse de compression extrêmement rapide avec un faible impact CPU, idéal pour ne pas saturer les cœurs du serveur MySQL.\n    - **`zlib`** : Offre un taux de compression plus élevé (taille finale plus petite) au prix d\'une consommation CPU plus importante.\n  - `--compress-level` : Niveau d\'intensité (ex: 1 à 9 pour zlib, 1 à 16 pour lz4).\n- Les fichiers sont décompressés automatiquement lors de l\'exécution de `copy-back-and-apply-log` ou `extract`.',
      explanation: 'Compresse également le flux réseau si la sauvegarde est dirigée vers un serveur distant ou un bucket cloud.',
      examTrap: 'La compression de tablespaces déjà compressés par InnoDB (ROW_FORMAT=COMPRESSED) n\'apporte aucun gain et consomme inutilement du CPU.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Compressing Backups',
    },
  },
  {
    id: 'fc-mysql908-dom04-064',
    cardNumber: 64,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Chiffrement des sauvegardes et compatibilité avec InnoDB TDE',
    difficulty: 'hard',
    tags: ['TDE', 'Encryption', 'Keyring', 'Security', 'MEB'],
    front: {
      question: 'Comment MEB gère-t-il la sauvegarde physique des tables InnoDB chiffrées avec Transparent Data Encryption (TDE) ?',
      hint: 'Les tablespaces chiffrés restent chiffrés lors de la copie physique, et le fichier de clés (keyring) doit impérativement être sauvegardé conjointement.',
    },
    back: {
      answer: 'Gestion de TDE avec MEB :\n\n1. **Chiffrement au repos préservé** :\n   - MEB copie les blocs des tablespaces chiffrés tels quels sans avoir besoin de les déchiffrer en mémoire, évitant toute fuite de données en clair.\n2. **Sauvegarde indispensable du Keyring (Master Key)** :\n   - Pour que la sauvegarde puisse un jour être restaurée et lue, les clés de déchiffrement contenues dans le composant de trousseau (`keyring_file`, `keyring_vault`, ou `component_keyring_file`) doivent impérativement être associées à la sauvegarde.\n   - MEB fournit l\'option **`--keyring-file-data`** ou le support des composants de chiffrement pour archiver les clés de chiffrement de manière sécurisée avec le backup.',
      explanation: 'Sans la clé maîtresse du keyring correspondant au LSN de la sauvegarde, la base restaurée sera 100% inaccessible et illisible.',
      examTrap: 'Une sauvegarde physique de tablespaces chiffrés sans sauvegarde du keyring est définitivement perdue : MySQL refusera d\'ouvrir les tablespaces au démarrage.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Backing Up Encrypted Tables with MEB',
    },
  },
  {
    id: 'fc-mysql908-dom04-065',
    cardNumber: 65,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegarde partielle de tables avec MEB : --include-tables et --exclude-tables',
    difficulty: 'medium',
    tags: ['Partial Backup', '--include-tables', 'MEB', 'Tablespaces'],
    front: {
      question: 'Quelles sont les règles et restrictions lors de la réalisation d\'une sauvegarde physique partielle de tables spécifiques avec MEB ?',
      codeSnippet: `$ mysqlbackup --backup-dir=/backups/partial \
  --include-tables="^hr\\." \
  backup`,
      hint: 'Expressions régulières pour cibler les tables ; chaque table doit être dans son propre fichier .ibd (innodb_file_per_table).',
    },
    back: {
      answer: 'Règles des sauvegardes partielles MEB :\n\n- **Options** :\n  - `--include-tables="regex"` : Inclut les tables correspondant à l\'expression régulière (ex: `^sales\\.orders`).\n  - `--exclude-tables="regex"` : Exclut les tables ciblées.\n- **Prérequis strict** :\n  - Les tables doivent impérativement être créées avec **`innodb_file_per_table=ON`** (chacune dans son propre fichier `.ibd`). Les tables résidant dans le tablespace système partagé (`ibdata1`) ne peuvent pas être séparées physiquement.\n- **À la restauration** :\n  - Ne peut pas être restaurée par simple `copy-back` direct écrasant le datadir, mais s\'importe via le mécanisme des **Transportable Tablespaces** (`ALTER TABLE ... IMPORT TABLESPACE`).',
      explanation: 'Permet d\'archiver des schémas historiques sans embarquer l\'ensemble des 50 bases du serveur.',
      examTrap: 'Toutes les tables liées par des contraintes FOREIGN KEY doivent être incluses ensemble dans la sélection regex.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Making a Partial Backup',
    },
  },
  {
    id: 'fc-mysql908-dom04-066',
    cardNumber: 66,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Verrouillage minimal dans MySQL 8.0 : LOCK INSTANCE FOR BACKUP',
    difficulty: 'hard',
    tags: ['LOCK INSTANCE FOR BACKUP', 'BACKUP_ADMIN', 'DDL', 'DML', 'Locks'],
    front: {
      question: 'Quel problème historique majeur posait le verrou FTWRL et comment l\'instruction MySQL 8.0 LOCK INSTANCE FOR BACKUP y remédie-t-elle ?',
      codeSnippet: `mysql> LOCK INSTANCE FOR BACKUP;
-- Opérations de sauvegarde des fichiers physiques
mysql> UNLOCK INSTANCE;`,
      hint: 'Permet les opérations DML (INSERT/UPDATE/DELETE) tout en bloquant uniquement les opérations DDL (ALTER/DROP/CREATE) et modifications de fichiers.',
    },
    back: {
      answer: 'Évolution majeure du verrouillage sous MySQL 8.0 :\n\n- **Ancien mécanisme (`FLUSH TABLES WITH READ LOCK` - FTWRL)** :\n  - Bloquait **absolument toutes les écritures (DML et DDL)** sur l\'ensemble du serveur. Tout `INSERT` ou `UPDATE` était immédiatement mis en attente, risquant d\'engorger les connexions applicatives et de provoquer des timeouts.\n- **Nouveau mécanisme (`LOCK INSTANCE FOR BACKUP`)** :\n  - **Autorise à 100% les requêtes DML** (`SELECT`, `INSERT`, `UPDATE`, `DELETE`). L\'application web ou mobile continue de fonctionner normalement sans interruption !\n  - **Bloque uniquement les opérations DDL** (`CREATE TABLE`, `ALTER TABLE`, `DROP TABLE`, `RENAME TABLE`, modifications de tablespaces).\n  - Empêche l\'incohérence physique liée à la création ou suppression de fichiers `.ibd` sur le disque pendant la copie.',
      explanation: 'Nécessite le nouveau privilège dynamique BACKUP_ADMIN.',
      examTrap: 'LOCK INSTANCE FOR BACKUP ne bloque pas les transactions de données, contrairement à FLUSH TABLES WITH READ LOCK.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - LOCK INSTANCE FOR BACKUP Statement',
    },
  },
  {
    id: 'fc-mysql908-dom04-067',
    cardNumber: 67,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegarde directe dans le Cloud (S3, OCI) avec MEB',
    difficulty: 'medium',
    tags: ['Cloud Storage', 'OCI', 'AWS S3', 'Object Storage', 'MEB'],
    front: {
      question: 'Comment MEB peut-il diffuser directement une sauvegarde physique vers un bucket cloud (Oracle Cloud Infrastructure OCI ou Amazon S3) sans stocker de fichier localement ?',
      codeSnippet: `$ mysqlbackup --cloud-service=oci \
  --cloud-config-file=/etc/oci.cfg \
  --cloud-bucket=mysql-backups \
  --backup-image=full_2026.mbi \
  backup-to-image`,
      hint: 'Options --cloud-service (oci, s3), --cloud-config-file, et streaming direct sans staging disque local.',
    },
    back: {
      answer: 'Sauvegarde Cloud directe avec MEB :\n\n- MEB intègre nativement des connecteurs pour les services de stockage d\'objets cloud :\n  - **Oracle Cloud Infrastructure (OCI Object Storage)** via `--cloud-service=oci`.\n  - **Amazon Web Services (AWS S3)** via `--cloud-service=s3`.\n- **Avantages architecturaux** :\n  1. **Zéro espace disque intermédiaire** : Les blocs InnoDB lus en mémoire sont compressés, chiffrés et diffusés en flux continu (streaming) par chunks HTTP multiparts directement vers le bucket.\n  2. **Restauration directe** : Possibilité d\'exécuter `copy-back-and-apply-log` en lisant directement le flux depuis le bucket cloud.',
      explanation: 'Évite de devoir provisionner des téraoctets de stockage local coûteux uniquement pour héberger les fichiers de backup temporaires.',
      examTrap: 'Le fichier de configuration cloud doit être protégé par des permissions strictes (chmod 600) car il contient les clés d\'accès API.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Backing Up to Cloud Storage',
    },
  },
  {
    id: 'fc-mysql908-dom04-068',
    cardNumber: 68,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Tables de suivi internes de MEB dans MySQL : backup_history et backup_progress',
    difficulty: 'easy',
    tags: ['backup_history', 'backup_progress', 'Audit', 'Monitoring'],
    front: {
      question: 'Dans quelles tables du schéma mysql sont enregistrés l\'historique d\'exécution et la progression en temps réel des sauvegardes MEB ?',
      codeSnippet: `mysql> SELECT backup_id, tool_name, start_time, end_time, backup_type, exit_state 
       FROM mysql.backup_history ORDER BY backup_id DESC LIMIT 5;`,
      hint: 'Tables mysql.backup_history pour les rapports passés et mysql.backup_progress pour le suivi live.',
    },
    back: {
      answer: 'Tables système de suivi de MEB :\n\n- **`mysql.backup_history`** :\n  - Consigne chaque sauvegarde terminée ou échouée avec : `backup_id`, date de début/fin, type (`full`, `incremental`), taille en octets, position binaire du LSN de fin (`binlog_pos`, `lsn_end`), et code de sortie (`exit_state = SUCCESS` ou `ERROR`).\n- **`mysql.backup_progress`** :\n  - Met à jour en continu l\'avancement en pourcentage, le débit d\'écriture (Mo/s), et le nombre de pages copiées pendant qu\'une opération de sauvegarde est en cours d\'exécution.\n- Permet aux consoles de monitoring (comme MySQL Enterprise Monitor - MEM) d\'alerter immédiatement en cas d\'échec de sauvegarde nocturne.',
      explanation: 'Ces tables sont créées automatiquement par MEB lors de sa première utilisation.',
      examTrap: 'Pour consulter ces tables, un compte de monitoring a seulement besoin du droit SELECT sur le schéma mysql.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - The backup_history and backup_progress Tables',
    },
  },
  {
    id: 'fc-mysql908-dom04-069',
    cardNumber: 69,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Gestion du tablespace système ibdata1 et des undo tablespaces dans MEB',
    difficulty: 'medium',
    tags: ['ibdata1', 'undo tablespaces', 'Tablespaces', 'Architecture'],
    front: {
      question: 'Pourquoi MEB copie-t-il obligatoirement ibdata1 et les tablespaces undo lors d\'une sauvegarde complète, et quel est leur rôle lors de la restauration ?',
      hint: 'ibdata1 contient le dictionnaire système et les undo logs contiennent les versions anciennes nécessaires à la cohérence MVCC.',
    },
    back: {
      answer: 'Rôle d\'ibdata1 et des Undo Tablespaces dans MEB :\n\n- **`ibdata1` (System Tablespace)** :\n  - Contient les structures de contrôle globales du moteur InnoDB, le doublewrite buffer, et les métadonnées de rollback segments.\n- **`undo_001`, `undo_002` (Undo Tablespaces)** :\n  - En MySQL 8.0, les undo logs sont obligatoirement séparés dans leurs propres tablespaces circulaires.\n  - Ils stockent les images avant modification (undo records) requises pour annuler les transactions non validées lors de la phase `apply-log`.\n- Sans la copie conforme d\'`ibdata1` et de tous les undo tablespaces actifs, le moteur InnoDB ne peut pas reconstruire l\'espace transactionnel au redémarrage.',
      explanation: 'En MySQL 8.0, le dictionnaire de données est lui aussi stocké dans un tablespace InnoDB interne (mysql.ibd).',
      examTrap: 'Ne supprimez jamais manuellement les fichiers undo_001 ou undo_002 d\'un répertoire restauré !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - System and Undo Tablespace Backup',
    },
  },
  {
    id: 'fc-mysql908-dom04-070',
    cardNumber: 70,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegarde du fichier de configuration my.cnf et des métadonnées (backup-my.cnf)',
    difficulty: 'easy',
    tags: ['backup-my.cnf', 'my.cnf', 'Configuration', 'MEB'],
    front: {
      question: 'Quel est le contenu et l\'utilité du fichier backup-my.cnf généré automatiquement par MEB à l\'intérieur du répertoire de sauvegarde ?',
      hint: 'Consigne les variables de démarrage InnoDB clés nécessaires à apply-log sans avoir besoin du my.cnf original du serveur.',
    },
    back: {
      answer: 'Rôle du fichier `backup-my.cnf` :\n\n- Lors de la phase de sauvegarde, MEB génère automatiquement un mini-fichier de configuration nommé **`backup-my.cnf`** dans le dossier de destination.\n- **Paramètres enregistrés** :\n  - `innodb_data_file_path` (chemins et tailles d\'`ibdata1`).\n  - `innodb_page_size` (taille de page : 16k, 8k, 4k, 32k ou 64k).\n  - `innodb_log_files_in_group` et `innodb_log_file_size`.\n  - `innodb_undo_directory` et nombre d\'undo tablespaces.\n- **Utilité** :\n  - Permet à la commande `apply-log` de s\'exécuter sur un autre serveur sans nécessiter l\'accès au fichier `/etc/my.cnf` de l\'hôte de production d\'origine.',
      explanation: 'Garantit l\'autonomie complète du dossier de sauvegarde pour les opérations hors-ligne.',
      examTrap: 'Si le serveur source utilise une taille de page personnalisée (ex: 8 Ko), apply-log utilisera automatiquement celle de backup-my.cnf.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - The backup-my.cnf File',
    },
  },

  // =========================================================================
  // SECTION 5 : RESTAURATION, POINT-IN-TIME RECOVERY (PITR) & BINLOG REPLAY (Cartes 71 à 85)
  // mysqlbinlog, positions, timestamps, GTID, sql_log_bin=0,
  // Transportable Tablespaces, innodb_force_recovery
  // =========================================================================
  {
    id: 'fc-mysql908-dom04-071',
    cardNumber: 71,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Principe fondamental du Point-in-Time Recovery (PITR)',
    difficulty: 'medium',
    tags: ['PITR', 'Binary Log', 'Disaster Recovery', 'Concepts'],
    front: {
      question: 'Qu\'est-ce que le Point-in-Time Recovery (PITR) et quelle est la formule mathématique des deux composants indispensables pour le réaliser ?',
      hint: 'Sauvegarde Complète (Point d\'ancrage) + Journaux Binaires (Binlogs) rejoués séquentiellement jusqu\'à l\'instant T désiré.',
    },
    back: {
      answer: 'Formule et Principe du Point-in-Time Recovery (PITR) :\n\n$$\\text{État cible à l\'instant } T = \\text{Dernière Sauvegarde Cohérente} + \\sum \\text{Événements Binlogs de } [T_{\\text{backup}}, T]$$\n\n- **Composants requis** :\n  1. **Une Sauvegarde Complète (Physique ou Logique)** : Restaurée en premier pour rétablir la base à la date/heure de la sauvegarde ($T_0$).\n  2. **La suite ininterrompue des Journaux Binaires (`binlog.00000X`)** : Permettant de rejouer (`replay`) toutes les transactions validées entre $T_0$ et l\'instant précis $T$ (ex: 1 seconde avant un `DROP TABLE` accidentel).\n- Sans les Binary Logs activés (`log_bin=ON`), le PITR est **strictement impossible** (perte de toutes les données créées depuis la dernière sauvegarde).',
      explanation: 'Le PITR est le mécanisme principal pour respecter un RPO (Recovery Point Objective) proche de zéro.',
      examTrap: 'Si un seul fichier binaire intermédiaire est corrompu ou manquant dans la séquence, la chaîne de relecture est rompue.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Point-in-Time (Incremental) Recovery',
    },
  },
  {
    id: 'fc-mysql908-dom04-072',
    cardNumber: 72,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Identification des coordonnées de départ du PITR (File, Position, GTID)',
    difficulty: 'medium',
    tags: ['Coordinates', 'Position', 'source-data', 'xtrabackup_binlog_info', 'PITR'],
    front: {
      question: 'Où trouve-t-on le fichier binlog et la position exacte de départ après la restauration d\'un dump mysqldump ou d\'une sauvegarde MEB ?',
      hint: 'Instruction CHANGE REPLICATION SOURCE TO dans le dump mysqldump, ou fichier backup-my.cnf / xtrabackup_binlog_info pour la sauvegarde physique.',
    },
    back: {
      answer: 'Localisation des coordonnées binaires initiales :\n\n- **Depuis un dump `mysqldump` (avec `--source-data=2`)** :\n  - En tête du fichier `.sql`, sous la forme d\'un commentaire SQL lisible :\n    ```sql\n    -- CHANGE MASTER TO MASTER_LOG_FILE=\'binlog.000042\', MASTER_LOG_POS=10737418;\n    ```\n- **Depuis une sauvegarde `MySQL Shell Dump`** :\n  - Dans le fichier de métadonnées `@.json`, champ `binlogFile` et `binlogPosition`, ainsi que `gtidExecuted`.\n- **Depuis une sauvegarde physique `MEB`** :\n  - Dans le fichier `backup-my.cnf` ou la table `mysql.backup_history`, champs `binlog_file` et `binlog_pos`.\n- Ce point précis marque le départ absolu de l\'extraction avec `mysqlbinlog`.',
      explanation: 'Rejouer les binlogs avant cette position provoquerait des erreurs d\'insertion en double de clés primaires.',
      examTrap: 'Ne jamais démarrer le rejeu binlog à la position 4 sans vérifier le fichier exact consigné dans les métadonnées de sauvegarde.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Determining Binary Log Coordinates',
    },
  },
  {
    id: 'fc-mysql908-dom04-073',
    cardNumber: 73,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Rejeu temporel avec mysqlbinlog : --start-datetime et --stop-datetime',
    difficulty: 'medium',
    tags: ['mysqlbinlog', '--start-datetime', '--stop-datetime', 'PITR'],
    front: {
      question: 'Comment extraire les transactions comprises entre deux dates précises à l\'aide de mysqlbinlog ?',
      codeSnippet: `$ mysqlbinlog --start-datetime="2026-09-18 10:00:00" \
  --stop-datetime="2026-09-18 14:32:05" \
  /var/lib/mysql/binlog.000012 | mysql -u root -p`,
      hint: 'Options --start-datetime et --stop-datetime au format standard YYYY-MM-DD HH:MM:SS.',
    },
    back: {
      answer: 'Filtrage temporel avec `mysqlbinlog` :\n\n- **Syntaxe** :\n  - `--start-datetime="YYYY-MM-DD HH:MM:SS"` : Ignore tous les événements enregistrés avant cette heure.\n  - `--stop-datetime="YYYY-MM-DD HH:MM:SS"` : Arrête l\'extraction dès qu\'un événement a un horodatage supérieur ou égal à cette valeur.\n- **Cas d\'usage classique** :\n  - Un stagiaire exécute un `DROP TABLE client` à 14:32:10.\n  - Vous configurez `--stop-datetime="2026-09-18 14:32:05"` pour rejouer toutes les requêtes légitimes jusqu\'à 5 secondes avant la catastrophe.\n- **Limite de précision** :\n  - La granularité est à la seconde près. Si plusieurs transactions ont eu lieu dans la même seconde, l\'approche par position (`--stop-position`) est requise.',
      explanation: 'Utile pour une récupération rapide quand on connaît l\'heure approximative d\'un incident.',
      examTrap: 'L\'heure spécifiée doit correspondre au fuseau horaire du serveur MySQL ayant généré les logs.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Point-in-Time Recovery Using Event Times',
    },
  },
  {
    id: 'fc-mysql908-dom04-074',
    cardNumber: 74,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Rejeu chirurgical par position avec mysqlbinlog : --start-position et --stop-position',
    difficulty: 'hard',
    tags: ['mysqlbinlog', '--stop-position', 'Surgical Recovery', 'PITR'],
    front: {
      question: 'Pourquoi la restauration basée sur les positions binaires (--stop-position) est-elle considérée comme la seule méthode infaillible pour le PITR ?',
      codeSnippet: `$ mysqlbinlog --start-position=1045 --stop-position=849200 binlog.000015 | mysql -u root -p`,
      hint: 'Les positions représentent les offsets exacts d\'octets de chaque transaction, éliminant toute ambiguïté temporelle.',
    },
    back: {
      answer: 'Précision absolue du filtrage par position :\n\n- Chaque événement dans un Binary Log commence et se termine à un **offset d\'octet physique unique et strictement croissant** (ex: `at 849200`).\n- **Pourquoi les horodatages ne suffisent pas en haute concurrence** :\n  - À un débit de 5 000 transactions/seconde, une même seconde contient des milliers d\'événements entremêlés.\n  - Utiliser `--stop-datetime` peut inclure la transaction fautive si elle s\'est produite dans la même seconde que des transactions légitimes.\n- **Méthode chirurgicale d\'excellence** :\n  1. Analyser le binlog en mode texte pour trouver la position exacte du début de l\'instruction destructrice (`# at 849200 DROP TABLE ...`).\n  2. Lancer `mysqlbinlog --stop-position=849200` : l\'opération s\'arrête au millième d\'octet juste avant l\'ordre fatal.',
      explanation: 'C\'est la méthode recommandée par Oracle pour toutes les récupérations critiques en production.',
      examTrap: 'La position spécifiée dans --stop-position n\'est PAS incluse dans la relecture.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Point-in-Time Recovery Using Event Positions',
    },
  },
  {
    id: 'fc-mysql908-dom04-075',
    cardNumber: 75,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Filtrage par base de données avec mysqlbinlog : le piège du format ROW vs STATEMENT',
    difficulty: 'hard',
    tags: ['mysqlbinlog', '--database', 'ROW Format', 'Exam Trap'],
    front: {
      question: 'Quel danger majeur guette l\'utilisation de l\'option --database (-d) de mysqlbinlog lorsque les logs sont enregistrés au format binlog_format=ROW ?',
      codeSnippet: `$ mysqlbinlog -d shop_db binlog.000001 | mysql -u root -p`,
      hint: 'En format ROW, certaines requêtes modifiant plusieurs bases ou tables associées peuvent être mal filtrées ou exclues.',
    },
    back: {
      answer: 'Le piège de `--database` avec le format ROW :\n\n- **En format STATEMENT** :\n  - Le filtrage se fait sur la base de données active par défaut au moment de la connexion (`USE db1;`). Une requête cross-database `UPDATE db2.table SET ...` exécutée alors qu\'on est connecté sur `db1` sera retenue par `-d db1`, causant des effets inattendus !\n- **En format ROW (Format par défaut de MySQL 8.0)** :\n  - Le log binaire enregistre les modifications ligne par ligne associées à la table réelle (`Table_map_event`).\n  - Bien que le filtrage sur les tables de `shop_db` fonctionne mieux en ROW, les transactions multi-bases ou les instructions DDL transverses peuvent être tronquées si l\'option `--database` élimine la moitié des événements d\'une transaction globale.\n- **Recommandation officielle d\'Oracle** :\n  - Éviter d\'utiliser `-d` avec `mysqlbinlog` en production si possible, ou rejouer sur une instance de secours isolée avant d\'exporter la base désirée.',
      explanation: 'La meilleure pratique pour restaurer une seule base est de restaurer l\'ensemble sur une instance sandbox puis d\'extraire la base.',
      examTrap: 'Question d\'examen fréquente : l\'option -d de mysqlbinlog ne garantit pas l\'isolation parfaite des événements multi-schémas.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - mysqlbinlog: database Option Caveats',
    },
  },
  {
    id: 'fc-mysql908-dom04-076',
    cardNumber: 76,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Désactivation de la journalisation binaire pendant le rejeu : sql_log_bin=0',
    difficulty: 'medium',
    tags: ['sql_log_bin=0', 'PITR', 'Replication Loop', 'Performance'],
    front: {
      question: 'Pourquoi devez-vous impérativement désactiver la journalisation binaire de session (SET sql_log_bin = 0) avant de réinjecter les binlogs lors d\'un PITR ?',
      codeSnippet: `mysql> SET sql_log_bin = 0;
mysql> SOURCE /tmp/replayed_events.sql;
mysql> SET sql_log_bin = 1;`,
      hint: 'Évite de réécrire les mêmes événements dans les nouveaux journaux binaires du serveur, ce qui saturerait le disque et casserait la réplication.',
    },
    back: {
      answer: 'Pourquoi appliquer `SET sql_log_bin = 0` lors de la restauration :\n\n1. **Éviter le dédoublement massif de binlogs** :\n   - Sans cela, chaque transaction lue et rejouée depuis les anciens binlogs serait à nouveau enregistrée dans les binlogs actifs du serveur, doublant inutilement la taille sur disque et générant des gigaoctets d\'I/O inutiles.\n2. **Éviter de polluer les réplicas (Boucles de réplication)** :\n   - Si le serveur restauré possède des réplicas connectés, leur envoyer ces millions d\'événements réinjectés provoquerait des conflits de clés primaires et corromprait la réplication.\n3. **Accélération massive des performances** :\n   - La désactivation des binlogs élimine la synchronisation fsync sur disque (`sync_binlog`), accélérant le rejeu de 3 à 5 fois.',
      explanation: 'sql_log_bin est une variable dynamique de session qui n\'impacte que la session courante du DBA.',
      examTrap: 'Ne jamais faire SET GLOBAL sql_log_bin=0 (qui désactiverait les logs pour tous les utilisateurs de l\'instance).',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - sql_log_bin System Variable',
    },
  },
  {
    id: 'fc-mysql908-dom04-077',
    cardNumber: 77,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Gestion des GTID lors du PITR avec mysqlbinlog : --skip-gtids',
    difficulty: 'hard',
    tags: ['GTID', '--skip-gtids', 'mysqlbinlog', 'PITR'],
    front: {
      question: 'Quel problème survient si vous tentez de rejouer des binlogs contenant des transactions GTID sur une instance où ces GTID sont déjà enregistrés dans gtid_executed ?',
      codeSnippet: `$ mysqlbinlog --skip-gtids binlog.000001 | mysql -u root -p`,
      hint: 'MySQL ignore silencieusement toute transaction dont le GTID figure déjà dans son ensemble gtid_executed.',
    },
    back: {
      answer: 'Problématique GTID et solution `--skip-gtids` :\n\n- **Le comportement natif de MySQL avec GTID** :\n  - Si un flux SQL contient des instructions `SET @@SESSION.GTID_NEXT = \'UUID:N\'`,\n  - Le moteur vérifie si ce GTID a déjà été exécuté sur l\'instance.\n  - Si le GTID existe déjà dans `@@GLOBAL.gtid_executed`, **MySQL ignore l\'événement et ne l\'exécute pas du tout** (déduplication automatique pour la réplication) !\n- **Conséquence dramatique en PITR** :\n  - Si vous avez restauré une base et tentez de rejouer des binlogs, les requêtes risquent d\'être purement et simplement sautées !\n- **Solution : `--skip-gtids`** :\n  - Demande à `mysqlbinlog` de supprimer les instructions `SET GTID_NEXT` du flux SQL extrait.\n  - Les requêtes sont alors exécutées comme de nouvelles transactions anonymes, générant de nouveaux GTID locaux sur le serveur cible.',
      explanation: 'Indispensable lors du rejeu de données sur une instance de test ou de développement.',
      examTrap: 'Sur un serveur où gtid_mode=ON, omettre --skip-gtids lors d\'un rejeu partiel peut conduire à une base de données vide sans afficher la moindre erreur !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - mysqlbinlog: --skip-gtids Option',
    },
  },
  {
    id: 'fc-mysql908-dom04-078',
    cardNumber: 78,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Rejeu des Journaux Binaires Chiffrés (Encrypted Binlogs) sous MySQL 8.0',
    difficulty: 'hard',
    tags: ['binlog_encryption', 'Encryption', 'Keyring', 'mysqlbinlog'],
    front: {
      question: 'Comment lire et extraire des événements avec mysqlbinlog lorsque la variable binlog_encryption=ON est activée sur le serveur ?',
      codeSnippet: `$ mysqlbinlog --read-from-remote-server \
  --host=127.0.0.1 --user=root -p \
  binlog.000020 > unencrypted_log.sql`,
      hint: 'mysqlbinlog ne peut pas déchiffrer les fichiers locaux sans le keyring ; il doit se connecter au serveur en tant que client de réplication.',
    },
    back: {
      answer: 'Lecture des binlogs chiffrés sous MySQL 8.0 :\n\n- Sous MySQL 8.0, avec `binlog_encryption=ON`, chaque fichier binaire est chiffré individuellement avec une clé de chiffrement de fichier (FDEK) chiffrée par la clé maîtresse du keyring.\n- Si vous essayez de lancer `$ mysqlbinlog /var/lib/mysql/binlog.000001` directement sur le disque, l\'utilitaire échoue car il ne possède pas le moteur de déchiffrement du keyring serveur.\n- **Procédure officielle d\'Oracle** :\n  - Utiliser l\'option **`--read-from-remote-server`** (ou `-R`) avec des identifiants de connexion (`--host`, `--user`, `--password`).\n  - Le serveur MySQL déchiffre les événements à la volée en mémoire via son keyring et les envoie au client `mysqlbinlog` via le protocole de réplication standard.',
      explanation: 'Garantit que même un pirate ayant volé les fichiers binaires sur le disque ne peut pas les lire sans accès réseau authentifié.',
      examTrap: 'Pour lire des binlogs chiffrés à distance, l\'utilisateur MySQL doit disposer des privilèges REPLICATION SLAVE ou REPLICATION CLIENT.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Reading Encrypted Binary Logs',
    },
  },
  {
    id: 'fc-mysql908-dom04-079',
    cardNumber: 79,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Analyse et audit d\'un binlog au format ROW : --base64-output=DECODE-ROWS et -v',
    difficulty: 'medium',
    tags: ['DECODE-ROWS', 'Verbose', 'Audit', 'Forensics', 'mysqlbinlog'],
    front: {
      question: 'Quelle combinaison d\'options de mysqlbinlog permet de traduire les événements de lignes binaires encodées en base64 en instructions pseudo-SQL lisibles par un humain ?',
      codeSnippet: `$ mysqlbinlog --base64-output=DECODE-ROWS -v -v binlog.000001 | grep -i "DROP TABLE"`,
      hint: 'Options --base64-output=DECODE-ROWS couplée à la verbosité -v ou -v -v.',
    },
    back: {
      answer: 'Décodage des événements ROW dans `mysqlbinlog` :\n\n- Par défaut, au format `binlog_format=ROW`, les données des lignes modifiées sont imprimées sous forme de gros blocs opaques `BINLOG \'aW5zZXJ0...\'` incompréhensibles à la lecture.\n- **Options de décodage indispensables pour le DBA** :\n  - **`-v` (Verbose)** : Décode les événements binaires et affiche sous forme de commentaires SQL les instructions réelles exécutées (`### INSERT INTO my_table`, `### WHERE @1=100 @2=\'John\'`).\n  - **`-v -v`** : Ajoute en plus les types de données SQL et les métadonnées de colonnes.\n  - **`--base64-output=DECODE-ROWS`** : Masque les blocs de base64 cryptiques pour ne laisser que le pseudo-SQL décodé et lisible.',
      explanation: 'Indispensable pour rechercher à quelle heure et par quelle transaction un incident applicatif s\'est produit.',
      examTrap: 'Un fichier généré avec --base64-output=DECODE-ROWS ne peut PAS être réinjecté dans mysql car les instructions ne sont que des commentaires !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - mysqlbinlog Row Event Display Options',
    },
  },
  {
    id: 'fc-mysql908-dom04-080',
    cardNumber: 80,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauter une transaction destructrice lors d\'un rejeu séquentiel',
    difficulty: 'hard',
    tags: ['Disaster Recovery', 'Skip Transaction', 'Position Math', 'PITR'],
    front: {
      question: 'Un administrateur a exécuté un "DROP DATABASE clients" à la position 150000 d\'un binlog. Comment rejouer l\'ensemble des transactions de la journée en sautant uniquement cette instruction fatale ?',
      hint: 'Exécuter mysqlbinlog en 2 étapes : d\'abord jusqu\'à la position précédant le DROP, puis de la position succédant immédiatement au DROP jusqu\'à la fin.',
    },
    back: {
      answer: 'Technique du saut de transaction destructrice en 2 passes :\n\n1. **Localiser les positions exactes d\'encadrement** :\n   - Supposons que l\'événement destructeur commence à l\'offset `150000` et se termine à l\'offset `150340`.\n2. **Passe 1 : Rejouer tout ce qui précède le DROP** :\n   ```bash\n   mysqlbinlog --stop-position=150000 binlog.000010 | mysql -u root -p\n   ```\n3. **Passe 2 : Rejouer tout ce qui succède au DROP jusqu\'au dernier log** :\n   ```bash\n   mysqlbinlog --start-position=150340 binlog.000010 | mysql -u root -p\n   mysqlbinlog binlog.000011 binlog.000012 | mysql -u root -p\n   ```\n- L\'instruction `DROP DATABASE` située entre `150000` et `150340` a été purement et simplement escamotée !',
      explanation: 'Permet de récupérer 100% de la production postérieure à l\'erreur humaine sans perdre le travail des autres utilisateurs.',
      examTrap: 'Ne vous trompez pas de position de départ pour la passe 2, sinon vous rejouerez le DROP ou sauterez la transaction suivante !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Skipping Unwanted Statements',
    },
  },
  {
    id: 'fc-mysql908-dom04-081',
    cardNumber: 81,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Restauration d\'une table unique par Transportable Tablespaces',
    difficulty: 'hard',
    tags: ['Transportable Tablespaces', 'FLUSH TABLES FOR EXPORT', 'CFG', 'IBD'],
    front: {
      question: 'Quelle est la séquence d\'instructions SQL pour exporter physiquement une table InnoDB individuelle d\'un serveur et l\'importer sur un autre sans toucher aux autres tables ?',
      codeSnippet: `-- Sur le serveur source :
mysql> FLUSH TABLES my_db.orders FOR EXPORT;
-- Copie des fichiers orders.ibd et orders.cfg vers le serveur cible
mysql> UNLOCK TABLES;

-- Sur le serveur cible :
mysql> ALTER TABLE my_db.orders DISCARD TABLESPACE;
-- Déplacement des fichiers .ibd et .cfg dans le dossier datadir cible
mysql> ALTER TABLE my_db.orders IMPORT TABLESPACE;`,
      hint: 'FLUSH TABLES ... FOR EXPORT, DISCARD TABLESPACE, copie physique des fichiers .ibd/.cfg, puis IMPORT TABLESPACE.',
    },
    back: {
      answer: 'Procédure des Transportable Tablespaces InnoDB :\n\n1. **Sur le serveur source** :\n   - `FLUSH TABLES orders FOR EXPORT;` : Force le flush de toutes les pages sales d\'`orders` en mémoire vers le disque, verrouille la table en lecture, et génère un fichier de métadonnées binaire `.cfg`.\n   - Copier `orders.ibd` et `orders.cfg` sur le serveur cible.\n   - `UNLOCK TABLES;` pour libérer le verrou sur la source.\n2. **Sur le serveur cible** :\n   - La structure de la table doit déjà exister (`CREATE TABLE orders (...)`).\n   - `ALTER TABLE orders DISCARD TABLESPACE;` (supprime le fichier `.ibd` vide cible).\n   - Copier les fichiers `orders.ibd` et `orders.cfg` dans `/var/lib/mysql/my_db/`.\n   - `ALTER TABLE orders IMPORT TABLESPACE;` (ajuste les Space ID internes et valide les index).',
      explanation: 'Permet de copier une table de 500 Go en quelques secondes sans dumper de SQL.',
      examTrap: 'Le fichier .cfg contient les métadonnées de schéma exactes et le dictionnaire interne nécessaires pour valider l\'importation.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Copying Tablespaces to Another Instance',
    },
  },
  {
    id: 'fc-mysql908-dom04-082',
    cardNumber: 82,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Prérequis et restrictions des Transportable Tablespaces sous MySQL 8.0',
    difficulty: 'medium',
    tags: ['Transportable Tablespaces', 'innodb_file_per_table', 'Restrictions'],
    front: {
      question: 'Quels sont les prérequis stricts pour qu\'une table InnoDB puisse faire l\'objet d\'un DISCARD/IMPORT TABLESPACE ?',
      hint: 'innodb_file_per_table doit être activé, la table ne doit pas être dans le tablespace système, et les structures de colonnes doivent concorder.',
    },
    back: {
      answer: 'Prérequis stricts pour `IMPORT TABLESPACE` :\n\n1. **`innodb_file_per_table = ON`** :\n   - La table doit résider dans son propre fichier `.ibd` individuel.\n2. **Compatibilité stricte des définitions DDL** :\n   - Les colonnes, types de données, index, et options de stockage (`ROW_FORMAT`) sur le serveur cible doivent correspondre exactement à ceux du serveur d\'origine.\n3. **Même taille de page InnoDB (`innodb_page_size`)** :\n   - Les deux serveurs doivent partager la même taille de bloc (par défaut 16 Ko).\n4. **Clés étrangères (`FOREIGN KEY`)** :\n   - Si la table possède des clés étrangères, il est nécessaire de désactiver temporairement `SET foreign_key_checks = 0;` pendant l\'importation.',
      explanation: 'Sous MySQL 8.0, les métadonnées SDI (Serialized Dictionary Information) intégrées au fichier .ibd facilitent la vérification.',
      examTrap: 'Une table stockée dans le system tablespace ibdata1 ne peut jamais être importée ou exportée de cette manière.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Transportable Tablespace Limitations',
    },
  },
  {
    id: 'fc-mysql908-dom04-083',
    cardNumber: 83,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Récupération d\'urgence après crash sévère : innodb_force_recovery',
    difficulty: 'hard',
    tags: ['innodb_force_recovery', 'Crash Recovery', 'Disaster Recovery', 'Corruption'],
    front: {
      question: 'À quoi sert la variable innodb_force_recovery et quel est l\'unique objectif du DBA lorsqu\'il démarre MySQL avec cette option activée ?',
      codeSnippet: `[mysqld]
innodb_force_recovery = 3`,
      hint: 'Démarrer une instance InnoDB corrompue en lecture seule afin de pouvoir dumper les données avant de reconstruire l\'instance.',
    },
    back: {
      answer: 'Objectif et comportement de `innodb_force_recovery` :\n\n- **Contexte** :\n  - Suite à une panne matérielle ou un arrêt violent, MySQL crash en boucle au démarrage lors de la phase de crash recovery automatique (assertion failure, corruption de page).\n- **Rôle de `innodb_force_recovery` (valeurs de 1 à 6)** :\n  - Bypasse certaines vérifications de cohérence interne et empêche les threads d\'arrière-plan (purge, insert buffer merge, rollbacks) de s\'exécuter.\n  - Permet à l\'instance de démarrer malgré des structures corrompues.\n- **L\'unique objectif du DBA** :\n  - **Extraire immédiatement les données accessibles avec `mysqldump` ou `util.dumpInstance()`** !\n  - Dès les données sauvegardées, l\'instance doit être détruite et reconstruite à neuf.',
      explanation: 'À partir de la valeur 4, l\'instance passe automatiquement en mode lecture seule (read-only) strict.',
      examTrap: 'Ne laissez JAMAIS un serveur en production tourner avec innodb_force_recovery > 0 pour traiter des utilisateurs !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Forcing InnoDB Recovery',
    },
  },
  {
    id: 'fc-mysql908-dom04-084',
    cardNumber: 84,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Signification des 6 niveaux progressifs de innodb_force_recovery',
    difficulty: 'hard',
    tags: ['innodb_force_recovery', 'Levels 1 to 6', 'InnoDB Internals', 'Exam Matrix'],
    front: {
      question: 'Quelles opérations internes InnoDB sont successivement désactivées par les niveaux 1, 2, 3, 4, 5 et 6 de innodb_force_recovery ?',
      hint: 'Commencer toujours à 1 et incrémenter pas à pas ; les niveaux supérieurs ignorent les redo logs, annulations et risquent d\'aggraver les pertes.',
    },
    back: {
      answer: 'Les 6 niveaux de `innodb_force_recovery` :\n\n- **1 (`SRV_FORCE_IGNORE_CORRUPT`)** : Ignore les pages corrompues trouvées lors du scan et continue l\'exécution.\n- **2 (`SRV_FORCE_NO_BACKGROUND`)** : Empêche l\'exécution du thread maître et des opérations de fusion du change buffer.\n- **3 (`SRV_FORCE_NO_TRX_UNDO`)** : Ne lance pas le rollback des transactions non validées après le crash recovery.\n- **4 (`SRV_FORCE_NO_IBUF_MERGE`)** : Désactive l\'insertion et le calcul des statistiques de buffer.\n- **5 (`SRV_FORCE_NO_UNDO_LOG_SCAN`)** : Ne consulte pas les undo logs au démarrage (traite les transactions inachevées comme validées).\n- **6 (`SRV_FORCE_NO_LOG_REDO`)** : Ne rejoue pas les redo logs (roll-forward désactivé). Les dernières transactions sont définitivement perdues.',
      explanation: 'Plus le chiffre est élevé, plus le risque d\'incohérence logique résiduelle est grand.',
      examTrap: 'Règle d\'examen : toujours commencer à 1 et tester. Ne jamais passer directement à 6 sous peine de perdre des données récupérables.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - innodb_force_recovery Values',
    },
  },
  {
    id: 'fc-mysql908-dom04-085',
    cardNumber: 85,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Restauration du dictionnaire de données interne sous MySQL 8.0 (mysql.ibd)',
    difficulty: 'hard',
    tags: ['Data Dictionary', 'mysql.ibd', 'Internals', 'Architecture'],
    front: {
      question: 'Où se trouvent stockées les métadonnées de schémas sous MySQL 8.0 et pourquoi la sauvegarde du fichier mysql.ibd est-elle indispensable ?',
      hint: 'Les fichiers .frm ont été supprimés sous MySQL 8.0 au profit d\'un dictionnaire de données transactionnel stocké dans mysql.ibd.',
    },
    back: {
      answer: 'Le dictionnaire de données sous MySQL 8.0 :\n\n- **Fin des fichiers `.frm`, `.par` et `.opt`** :\n  - Dans les versions antérieures à 8.0, chaque table avait un fichier de définition `.frm` distinct sur le système de fichiers.\n  - Sous MySQL 8.0, tous les métadonnées de tables, colonnes, contraintes, index et déclencheurs sont stockées dans des tables système internes hébergées dans un tablespace InnoDB dédié : **`mysql.ibd`**.\n- **Conséquence pour les sauvegardes physiques** :\n  - Vous ne pouvez plus reconstruire une instance en recopiant uniquement les fichiers `.ibd` de vos tables métier sans restaurer le tablespace système `mysql.ibd` correspondant.\n  - Le dictionnaire garantit des opérations DDL 100% transactionnelles et atomiques.',
      explanation: 'Permet d\'avoir des CREATE / ALTER TABLE sécurisés contre les crashes à mi-chemin.',
      examTrap: 'Ne cherchez pas de fichiers .frm dans le répertoire datadir d\'un serveur MySQL 8.0 : ils n\'existent plus.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - MySQL 8.0 Data Dictionary',
    },
  },

  // =========================================================================
  // SECTION 6 : STRATÉGIES AVANCÉES, RÉPLICATION, CLONE & AUTOMATISATION (Cartes 86 à 100)
  // Sauvegarde sur réplica, CLONE INSTANCE, auto.cnf, innochecksum,
  // ibd2sdi, binlog_expire_logs_seconds, RPO/RTO Runbooks
  // =========================================================================
  {
    id: 'fc-mysql908-dom04-086',
    cardNumber: 86,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegarde depuis un Réplica (Secondary/Replica Backup) : options et avantages',
    difficulty: 'medium',
    tags: ['Replica Backup', '--slave-info', '--dump-replica', 'mysqldump', 'MEB'],
    front: {
      question: 'Quels sont les avantages de réaliser les sauvegardes sur une instance réplica plutôt que sur la source primaire, et quelles options enregistrent les coordonnées binaires de la source ?',
      codeSnippet: `$ mysqldump --dump-replica=2 --single-transaction ...
$ mysqlbackup --slave-info ...`,
      hint: 'Soulage le primaire de la charge I/O et mémoire ; l\'option --dump-replica (ou --slave-info) capture les coordonnées du binaire de la SOURCE.',
    },
    back: {
      answer: 'Sauvegarde déportée sur un réplica :\n\n- **Avantages majeurs** :\n  1. **Décharge la source de production** de toute surconsommation CPU, I/O disque et saturation du buffer pool causée par la lecture séquentielle de téraoctets de données.\n  2. Permet d\'exécuter des sauvegardes intensives ou des vérifications d\'intégrité sans impacter le temps de réponse des utilisateurs réels.\n- **Capture des coordonnées de la source** :\n  - Si l\'on veut utiliser ce backup pour reconstruire un autre réplica ou la source elle-même, on a besoin des coordonnées binaire de la **source primaire**, pas de celles du réplica !\n  - **`mysqldump --dump-replica=2`** (remplace l\'ancienne option `--slave-info` sous 8.0) : Écrit l\'instruction `CHANGE REPLICATION SOURCE TO` avec les coordonnées de la source primaire correspondantes.',
      explanation: 'Assurez-vous que le réplica n\'a pas un retard de réplication (Seconds_Behind_Source) trop important au moment du dump.',
      examTrap: 'Arrêtez l\'applier SQL (STOP REPLICA SQL_THREAD) sur le réplica pendant un dump non transactionnel pour éviter l\'incohérence.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Backing Up a Replica',
    },
  },
  {
    id: 'fc-mysql908-dom04-087',
    cardNumber: 87,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Gestion des threads de réplication pendant la sauvegarde d\'un réplica',
    difficulty: 'medium',
    tags: ['STOP REPLICA SQL_THREAD', 'Replication State', 'Consistency'],
    front: {
      question: 'Pourquoi est-il parfois conseillé d\'arrêter le thread SQL (STOP REPLICA SQL_THREAD) tout en laissant actif le thread I/O (START REPLICA IO_THREAD) lors de la sauvegarde d\'un réplica ?',
      hint: 'Permet de figer l\'état des données locales pendant le dump tout en continuant à recevoir les binlogs de la source dans le relay log.',
    },
    back: {
      answer: 'Stratégie de gel de réplication :\n\n- En exécutant **`STOP REPLICA SQL_THREAD`** :\n  - Le moteur d\'application des requêtes locales s\'arrête. L\'état des tables sur le réplica devient totalement statique, éliminant tout risque de variation pendant les sauvegardes non cohérentes.\n- En maintenant **`START REPLICA IO_THREAD`** (ou `RECEIVER`) actif :\n  - Le réplica continue de télécharger sans interruption les transactions depuis la source primaire et les stocke dans ses journaux de relais (`relay logs`).\n  - Aucun risque d\'accumulation de logs ou de saturation du réseau à la reprise : le flux réseau continue normalement.\n- Une fois la sauvegarde terminée : `START REPLICA SQL_THREAD;` pour rattraper le retard accumulé.',
      explanation: 'Méthode classique d\'administration pour assurer une sauvegarde reposée sans isoler le serveur.',
      examTrap: 'N\'oubliez pas de redémarrer le SQL_THREAD après le backup, sinon le réplica dérivera indéfiniment.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Pausing Replication for Backup',
    },
  },
  {
    id: 'fc-mysql908-dom04-088',
    cardNumber: 88,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Reconstruction d\'un réplica à partir d\'une sauvegarde avec GTID : gtid_purged',
    difficulty: 'hard',
    tags: ['gtid_purged', 'Replica Provisioning', 'GTID Auto-Positioning'],
    front: {
      question: 'Comment provisionner un nouveau réplica à partir d\'une sauvegarde sans avoir à copier manuellement les vieux binlogs de la source ?',
      codeSnippet: `mysql> RESET MASTER;
mysql> SET @@GLOBAL.gtid_purged = '3E11FA47-71CA-11E1-9E33-C80AA9429562:1-140235';
mysql> CHANGE REPLICATION SOURCE TO SOURCE_HOST='master1', SOURCE_AUTO_POSITION=1;
mysql> START REPLICA;`,
      hint: 'Initialiser @@GLOBAL.gtid_purged sur le nouveau réplica avec l\'ensemble GTID de la sauvegarde, puis activer SOURCE_AUTO_POSITION=1.',
    },
    back: {
      answer: 'Provisionnement moderne de réplica par GTID :\n\n1. **Restauration des données** : Restaurer la sauvegarde physique ou logique sur le nouveau serveur réplica.\n2. **Vérification de l\'instance vierge** : `RESET MASTER;` pour vider l\'historique GTID local.\n3. **Initialisation de `gtid_purged`** :\n   - Injecter l\'ensemble exact des transactions déjà incluses dans la sauvegarde :\n     ```sql\n     SET @@GLOBAL.gtid_purged = \'UUID:1-N\';\n     ```\n4. **Activation de l\'auto-positionnement (`SOURCE_AUTO_POSITION = 1`)** :\n   - Lors du `START REPLICA`, le réplica contacte la source primaire et lui annonce : *"J\'ai déjà exécuté jusqu\'au GTID N, envoie-moi uniquement les transactions à partir de N+1"*\n   - La réplication s\'établit instantanément sans jamais avoir besoin de spécifier un nom de fichier binlog ni d\'offset numérique !',
      explanation: 'L\'un des bénéfices majeurs du mode GTID sous MySQL 8.0.',
      examTrap: 'gtid_purged ne peut être configuré que lorsque gtid_executed est entièrement vide.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Provisioning a Replica Using GTIDs',
    },
  },
  {
    id: 'fc-mysql908-dom04-089',
    cardNumber: 89,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Sauvegarde à froid (Cold Backup) : procédure et innodb_fast_shutdown=0',
    difficulty: 'medium',
    tags: ['Cold Backup', 'innodb_fast_shutdown=0', 'Offline Backup', 'OS Copy'],
    front: {
      question: 'Pourquoi devez-vous impérativement positionner innodb_fast_shutdown = 0 avant d\'arrêter MySQL pour effectuer une sauvegarde à froid (Cold Backup) au niveau fichier ?',
      codeSnippet: `mysql> SET GLOBAL innodb_fast_shutdown = 0;
mysql> SHUTDOWN;
# tar -czvf /backups/mysql_cold_backup.tar.gz /var/lib/mysql`,
      hint: 'Force un arrêt propre avec flush complet du buffer pool, purge des undo logs et fusion du change buffer (Clean Shutdown).',
    },
    back: {
      answer: 'Rôle de `innodb_fast_shutdown = 0` avant une sauvegarde à froid :\n\n- **Valeur par défaut (`innodb_fast_shutdown = 1`)** :\n  - MySQL fait un flush rapide mais saute la purge complète des undo logs et l\'insertion complète du change buffer.\n- **Valeur `innodb_fast_shutdown = 0` (Clean Shutdown)** :\n  - InnoDB effectue un nettoyage complet :\n    1. Écrit toutes les pages sales en mémoire sur le disque.\n    2. Vide entièrement le change buffer.\n    3. Purge les transactions supprimées dans les undo logs.\n    4. Enregistre un point de contrôle exact dans les en-têtes de redo logs.\n  - À l\'arrêt, tous les fichiers de données sont 100% cohérents et synchronisés entre eux.\n- Vous pouvez alors archiver le dossier `/var/lib/mysql` avec `tar` ou un instantané SAN/LVM en toute garantie.',
      explanation: 'Permet une restauration directe sans exécution de crash recovery au démarrage.',
      examTrap: 'innodb_fast_shutdown=2 fait un crash-like shutdown (ne jamais utiliser avant une sauvegarde à froid !).',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - innodb_fast_shutdown System Variable',
    },
  },
  {
    id: 'fc-mysql908-dom04-090',
    cardNumber: 90,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Le rôle crucial du fichier auto.cnf lors du clonage ou de la restauration de serveurs',
    difficulty: 'easy',
    tags: ['auto.cnf', 'server_uuid', 'Cloning', 'Replication Conflict'],
    front: {
      question: 'Pourquoi devez-vous obligatoirement supprimer le fichier auto.cnf dans le datadir lorsqu\'une sauvegarde physique est restaurée pour créer un nouveau serveur dans une topologie de réplication ?',
      hint: 'auto.cnf contient le server_uuid unique généré à l\'initialisation ; deux serveurs avec le même UUID ne peuvent pas répliquer ensemble.',
    },
    back: {
      answer: 'Rôle d\'`auto.cnf` et régénération de `server_uuid` :\n\n- Dans le répertoire `datadir` (`/var/lib/mysql`), le fichier `auto.cnf` contient l\'identifiant universel unique de l\'instance :\n  ```ini\n  [auto]\n  server_uuid=8a2e1d74-72a3-11ec-9e55-08002715e2e8\n  ```\n- **Conséquence d\'une copie conforme sans suppression** :\n  - Le serveur restauré aura le même `server_uuid` que le serveur source de production.\n  - Si vous tentez d\'en faire un réplica, la réplication échouera immédiatement car MySQL interdit à un serveur de recevoir des événements provenant d\'un serveur ayant le même `server_uuid` (détection de boucle infinie).\n- **Action requise** :\n  - Supprimer `/var/lib/mysql/auto.cnf` avant de démarrer le nouveau serveur. MySQL en générera un nouveau aléatoire lors du premier démarrage.',
      explanation: 'Règle numéro un lors de la duplication de VM ou de conteneurs MySQL.',
      examTrap: 'Ne modifiez pas manuellement le contenu d\'auto.cnf : supprimez-le simplement pour laisser MySQL le régénérer.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - The auto.cnf File and server_uuid',
    },
  },
  {
    id: 'fc-mysql908-dom04-091',
    cardNumber: 91,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Le plugin InnoDB Clone (MySQL 8.0.17+) : CLONE INSTANCE',
    difficulty: 'hard',
    tags: ['CLONE INSTANCE', 'InnoDB Clone', 'Physical Replication', 'MySQL 8.0.17+'],
    front: {
      question: 'Qu\'est-ce que le plugin InnoDB Clone introduit dans MySQL 8.0.17 et comment permet-il de cloner une instance entière sans aucun outil externe ?',
      codeSnippet: `mysql> CLONE INSTANCE FROM 'donor_user'@'donor_host':3306 
       IDENTIFIED BY 'Password123!' 
       DATA DIRECTORY = '/var/lib/mysql_replica';`,
      hint: 'Permet le clonage physique interne de données d\'un serveur donneur (Donor) vers un receveur (Recipient) directement via le port MySQL.',
    },
    back: {
      answer: 'Fonctionnement du plugin InnoDB Clone :\n\n- Introduit en MySQL 8.0.17, le composant `clone` permet de copier physiquement l\'intégralité des tablespaces, métadonnées et redo logs d\'un serveur **Donneur (Donor)** vers un serveur **Bénéficiaire (Recipient)**.\n- **Modes d\'exécution** :\n  1. **Clonage local (`CLONE LOCAL DATA DIRECTORY = \'...\'`)** : Copie physique locale à chaud sur un autre point de montage.\n  2. **Clonage réseau (`CLONE INSTANCE FROM ...`)** : Le donneur transmet ses blocs directement à travers le protocole réseau MySQL standard sans nécessiter SSH, MEB ou tar.\n- **Automatisation réplication** :\n  - Le bénéficiaire se configure automatiquement avec les métadonnées GTID du donneur et peut démarrer la réplication immédiatement.',
      explanation: 'C\'est la méthode native la plus rapide pour ajouter un nœud à un cluster InnoDB ReplicaSet ou Group Replication.',
      examTrap: 'Le clonage réseau remplace entièrement le contenu du datadir du serveur bénéficiaire et redémarre automatiquement l\'instance.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - The Clone Plugin',
    },
  },
  {
    id: 'fc-mysql908-dom04-092',
    cardNumber: 92,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Prérequis et privilèges du plugin InnoDB Clone',
    difficulty: 'hard',
    tags: ['BACKUP_ADMIN', 'CLONE_ADMIN', 'Clone Prerequisites', 'Security'],
    front: {
      question: 'Quels privilèges dynamiques et quelles conditions de version sont requis pour exécuter une commande CLONE INSTANCE entre deux serveurs ?',
      hint: 'Privilèges BACKUP_ADMIN sur le donneur, CLONE_ADMIN sur le receveur, et même version exacte de MySQL.',
    },
    back: {
      answer: 'Prérequis du plugin Clone :\n\n1. **Privilèges dynamiques dédiés** :\n   - Sur le Donneur (Donor) : **`BACKUP_ADMIN`**.\n   - Sur le Bénéficiaire (Recipient) : **`CLONE_ADMIN`** (permet de vider le datadir et redémarrer).\n2. **Plugin installé sur les deux serveurs** :\n   - `INSTALL PLUGIN clone SONAME \'mysql_clone.so\';`\n3. **Compatibilité stricte des versions** :\n   - Les deux serveurs doivent exécuter **exactement la même version mineure de MySQL** (ex: 8.0.32 vers 8.0.32). Le clonage échoue entre versions différentes.\n4. **Système d\'exploitation et architecture** :\n   - Même architecture matérielle et OS (ex: Linux x86_64 vers Linux x86_64).',
      explanation: 'Indispensable pour l\'approvisionnement automatique dans les architectures Cloud et conteneurs Kubernetes.',
      examTrap: 'Si le serveur donneur a des paramètres innodb_page_size différents du receveur, le clonage est rejeté.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Cloning Prerequisites',
    },
  },
  {
    id: 'fc-mysql908-dom04-093',
    cardNumber: 93,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Vérification hors-ligne de l\'intégrité des tablespaces : innochecksum',
    difficulty: 'medium',
    tags: ['innochecksum', 'Page Checksum', 'Corruption', 'Troubleshooting'],
    front: {
      question: 'Comment vérifier l\'intégrité physique d\'un fichier de tablespace .ibd sans démarrer le serveur MySQL ?',
      codeSnippet: `$ innochecksum -v /var/lib/mysql/sales/orders.ibd`,
      hint: 'Utilitaire officiel innochecksum livré avec MySQL pour valider les sommes de contrôle des pages InnoDB.',
    },
    back: {
      answer: 'Utilisation de l\'outil `innochecksum` :\n\n- `innochecksum` est un utilitaire en ligne de commande qui analyse un fichier tablespace InnoDB (`.ibd` ou `ibdata1`) à froid (serveur arrêté) :\n  1. Il lit chaque bloc de 16 Ko.\n  2. Il recalcule l\'algorithme de somme de contrôle (CRC-32C ou InnoDB checksum).\n  3. Il compare la valeur calculée avec la somme de contrôle stockée dans l\'en-tête et la queue de la page.\n  4. Il signale immédiatement toute page corrompue (mismatch checksum).\n- **Options utiles** :\n  - `-c` : Affiche le décompte des pages par type (index, données, undo, alloc).\n  - `-p <page_num>` : Examine une page spécifique en détail.\n  - `-w` : Réécrit une nouvelle somme de contrôle valide après une réparation manuelle.',
      explanation: 'Idéal pour valider un disque suspect après une coupure d\'alimentation ou une erreur SMART.',
      examTrap: 'N\'exécutez jamais innochecksum avec l\'option de réécriture sur un fichier en cours d\'utilisation par un démon mysqld actif.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - innochecksum — Offline InnoDB File Checksum Utility',
    },
  },
  {
    id: 'fc-mysql908-dom04-094',
    cardNumber: 94,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Maintenance et défragmentation en ligne : CHECK TABLE et OPTIMIZE TABLE',
    difficulty: 'medium',
    tags: ['CHECK TABLE', 'OPTIMIZE TABLE', 'Maintenance', 'InnoDB defragmentation'],
    front: {
      question: 'Que fait réellement l\'instruction OPTIMIZE TABLE sur une table InnoDB sous MySQL 8.0 et comment est-elle exécutée en interne ?',
      codeSnippet: `mysql> OPTIMIZE TABLE sales.orders;`,
      hint: 'Mappe en interne sur un ALTER TABLE ... FORCE qui reconstruit la table et ses index pour compacter l\'espace disque libéré.',
    },
    back: {
      answer: 'Fonctionnement d\'`OPTIMIZE TABLE` sur InnoDB :\n\n- **Mécanisme interne** :\n  - Contrairement à MyISAM où les index étaient simplement réordonnés, sur InnoDB, `OPTIMIZE TABLE` est mappé en interne sur :\n    ```sql\n    ALTER TABLE sales.orders ENGINE=InnoDB, FORCE;\n    ```\n  - Reconstruit physiquement la table en ligne (Online DDL) dans un nouveau fichier `.ibd` temporaire.\n  - Récupère l\'espace disque inutilisé (cluttered space) laissé par des opérations massives de `DELETE` ou de mises à jour de colonnes de longueur variable.\n  - Défragmente les pages de l\'index clustered B-Tree et compacte les index secondaires.\n- **Vérification d\'intégrité en ligne** :\n  - `CHECK TABLE orders;` vérifie la cohérence logique et physique des clés sans arrêter le serveur.',
      explanation: 'Nécessite un espace disque temporaire équivalent à la taille de la table pour opérer.',
      examTrap: 'OPTIMIZE TABLE renvoie un message informatif : "Table does not support optimize, doing recreate + analyze instead". Ce n\'est pas une erreur !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - OPTIMIZE TABLE Statement',
    },
  },
  {
    id: 'fc-mysql908-dom04-095',
    cardNumber: 95,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Purge et rotation des journaux binaires : binlog_expire_logs_seconds',
    difficulty: 'medium',
    tags: ['binlog_expire_logs_seconds', 'PURGE BINARY LOGS', 'Retention', 'Disk Space'],
    front: {
      question: 'Quelle variable système contrôle la rétention automatique des fichiers de journaux binaires sous MySQL 8.0 et quelle instruction permet de purger manuellement les anciens logs ?',
      codeSnippet: `mysql> SET GLOBAL binlog_expire_logs_seconds = 604800; -- 7 jours
mysql> PURGE BINARY LOGS TO 'binlog.000050';
mysql> PURGE BINARY LOGS BEFORE '2026-09-01 00:00:00';`,
      hint: 'binlog_expire_logs_seconds remplace l\'ancien expire_logs_days ; PURGE BINARY LOGS TO/BEFORE libère le disque en toute sécurité.',
    },
    back: {
      answer: 'Rétention et Purge des Binary Logs :\n\n- **Variable système recommandée (MySQL 8.0)** :\n  - **`binlog_expire_logs_seconds`** :\n    - Remplace l\'ancienne variable `expire_logs_days` (désormais dépréciée).\n    - Permet une granularité précise à la seconde près (ex: `259200` pour 3 jours, `604800` pour 7 jours).\n    - Purge automatiquement les fichiers plus anciens lors de chaque rotation de log binaire.\n- **Purge manuelle sécurisée** :\n  - Ne supprimez **jamais** un fichier binlog avec `rm` sous Linux (cela corrompt le fichier d\'index `binlog.index`) !\n  - Utiliser la commande SQL native :\n    - `PURGE BINARY LOGS TO \'binlog.000050\';` : Supprime tous les logs antérieurs à `binlog.000050`.\n    - `PURGE BINARY LOGS BEFORE \'YYYY-MM-DD HH:MM:SS\';` : Supprime selon l\'ancienneté.',
      explanation: 'Empêche la saturation du disque /var/lib/mysql qui provoquerait le crash immédiat de MySQL.',
      examTrap: 'Vérifiez toujours que vos réplicas ont fini de lire un binlog avant de le purger, sinon la réplication sera brisée.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Purging Binary Logs',
    },
  },
  {
    id: 'fc-mysql908-dom04-096',
    cardNumber: 96,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Gestion dynamique du Redo Log physique sous MySQL 8.0.30+ : innodb_redo_log_capacity',
    difficulty: 'hard',
    tags: ['innodb_redo_log_capacity', 'Redo Log', 'MySQL 8.0.30+', 'Architecture'],
    front: {
      question: 'Comment MySQL 8.0.30+ a-t-il modernisé la gestion des fichiers Redo Logs (ib_logfile0, ib_logfile1) avec innodb_redo_log_capacity ?',
      codeSnippet: `mysql> SET GLOBAL innodb_redo_log_capacity = 8589934592; -- 8 Go dynamique`,
      hint: 'Remplace innodb_log_files_in_group et innodb_log_file_size par une allocation dynamique gérée dans le dossier #innodb_redo.',
    },
    back: {
      answer: 'Modernisation du Redo Log (MySQL 8.0.30+) :\n\n- **Ancien modèle** :\n  - Fichiers statiques rigides `ib_logfile0`, `ib_logfile1` définis par `innodb_log_file_size` et `innodb_log_files_in_group`.\n  - Modifier la taille exigeait un arrêt propre complet du serveur et la suppression manuelle des fichiers.\n- **Nouveau modèle dynamique (`innodb_redo_log_capacity`)** :\n  - Stocké dans le sous-dossier `#innodb_redo/` du datadir avec des fichiers numérotés dynamiquement (`#ib_redo1`, `#ib_redo2`, etc.).\n  - **Modifiable à chaud sans redémarrage** : `SET GLOBAL innodb_redo_log_capacity = 4 * 1024 * 1024 * 1024;`\n  - InnoDB alloue ou détruit automatiquement des fichiers en arrière-plan pour atteindre la capacité cible demandée.\n  - Facilite grandement les sauvegardes physiques et l\'ajustement des performances de checkpointing.',
      explanation: 'Les anciennes variables sont devenues dépréciées à partir de MySQL 8.0.30.',
      examTrap: 'Ne modifiez plus innodb_log_file_size sur MySQL 8.0.30+, utilisez innodb_redo_log_capacity.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Redo Log Capacity and Architecture',
    },
  },
  {
    id: 'fc-mysql908-dom04-097',
    cardNumber: 97,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Extraction des métadonnées Serialized Dictionary Information (SDI) : ibd2sdi',
    difficulty: 'hard',
    tags: ['ibd2sdi', 'SDI', 'Serialized Dictionary Information', 'Recovery'],
    front: {
      question: 'Quel utilitaire permet d\'extraire la structure d\'une table (DDL) directement depuis son fichier .ibd orphelin sous MySQL 8.0 sans serveur démarré ?',
      codeSnippet: `$ ibd2sdi /var/lib/mysql/sales/orders.ibd --dump-file=orders_schema.json`,
      hint: 'Utilitaire officiel ibd2sdi extrayant les métadonnées SDI au format JSON embarquées dans chaque tablespace.',
    },
    back: {
      answer: 'Rôle de l\'outil `ibd2sdi` (MySQL 8.0) :\n\n- Sous MySQL 8.0, chaque fichier de tablespace individuel (`.ibd`) embarque une copie sérialisée de ses propres métadonnées de schéma appelée **SDI (Serialized Dictionary Information)**.\n- **Fonctionnement d\'`ibd2sdi`** :\n  - Analyse le fichier `.ibd` hors-ligne.\n  - Extrait l\'arborescence JSON complète décrivant la table : noms et types exacts des colonnes, index, contraintes, tablespaces et options de stockage.\n- **Cas d\'usage de sauvetage** :\n  - En cas de corruption totale du dictionnaire de données `mysql.ibd`, `ibd2sdi` permet de reconstituer les instructions `CREATE TABLE` d\'origine de n\'importe quel fichier `.ibd` rescapé pour recréer la structure et réimporter les données via `IMPORT TABLESPACE`.',
      explanation: 'Apporte une résilience sans précédent face aux corruptions matérielles.',
      examTrap: 'ibd2sdi ne fonctionne que sur les tablespaces InnoDB au format MySQL 8.0 (pas sur les vieux fichiers 5.7).',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - ibd2sdi — InnoDB SDI Extraction Utility',
    },
  },
  {
    id: 'fc-mysql908-dom04-098',
    cardNumber: 98,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Politique de test de restauration et Disaster Recovery Plan (DRP)',
    difficulty: 'medium',
    tags: ['Disaster Recovery', 'DRP', 'RTO', 'RPO', 'Testing'],
    front: {
      question: 'Quelle est la règle d\'or absolue de tout administrateur de bases de données concernant les sauvegardes et la validation du RTO/RPO ?',
      hint: '« Une sauvegarde n\'existe que si sa procédure de restauration a été testée et chronométrée avec succès avec des données réelles. »',
    },
    back: {
      answer: 'Règle d\'or du Disaster Recovery (DR) :\n\n- **« A backup is only as good as its tested restore »** :\n  - Réaliser des sauvegardes quotidiennes sans jamais tester leur restauration est l\'une des causes d\'échec les plus fréquentes en entreprise (fichiers tronqués, clés de chiffrement manquantes, permissions invalides, temps de restauration dépassant le RTO autorisé).\n- **Bonnes pratiques industrielles** :\n  1. **Automatisation de la restauration test** : Restaurer périodiquement (ex: hebdomadaire) la sauvegarde de production sur un serveur de test isolé.\n  2. **Mesure du RTO (Recovery Time Objective)** : Chronométrer le temps exact nécessaire pour remettre le service en marche.\n  3. **Mesure du RPO (Recovery Point Objective)** : Valider que le rejeu des binlogs comble le delta sans perte de transactions.\n  4. **Vérification d\'intégrité applicative** : Exécuter des requêtes de comptage et de sommes de contrôle pour valider les données.',
      explanation: 'Obligation de conformité dans la plupart des audits de sécurité et certifications de conformité.',
      examTrap: 'Avoir un fichier de dump de 50 Go ne garantit en rien qu\'il ne contient pas une erreur de syntaxe à 90% du fichier.',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Backup and Recovery Strategy',
    },
  },
  {
    id: 'fc-mysql908-dom04-099',
    cardNumber: 99,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Checklist opérationnelle d\'une procédure de restauration d\'urgence en 6 étapes',
    difficulty: 'medium',
    tags: ['Checklist', 'Runbook', 'Emergency', 'Incident Response'],
    front: {
      question: 'Quelles sont les 6 étapes séquentielles à suivre lors d\'une intervention d\'urgence de restauration totale (Total Disaster Recovery) ?',
      hint: 'Isoler, restaurer la sauvegarde de base, préparer/appliquer les logs, rejouer les binlogs jusqu\'à l\'incident, valider, puis rouvrir les accès.',
    },
    back: {
      answer: 'Checklist opérationnelle de Restauration d\'Urgence :\n\n1. **Isolation immédiate (Containment)** :\n   - Bloquer les connexions applicatives (`SET GLOBAL read_only = 1;` ou redirection firewall) pour éviter toute écriture supplémentaire sur un état corrompu.\n2. **Sauvegarde d\'audit des logs actifs** :\n   - Copier immédiatement les derniers binlogs encore présents sur le serveur (`FLUSH BINARY LOGS;` et copie des fichiers) avant toute manipulation.\n3. **Restauration de la dernière sauvegarde complète saine** :\n   - Restaurer l\'image physique MEB (`copy-back`) ou charger le dump MySQL Shell.\n4. **Application des sauvegardes incrémentales (le cas échéant)** :\n   - Fusionner les incrémentales dans l\'ordre chronologique strict.\n5. **Point-in-Time Recovery via rejeu des Binary Logs** :\n   - Exécuter `mysqlbinlog` avec `--stop-position` pour rejouer toutes les transactions légitimes jusqu\'à l\'instant précis précédant l\'incident (avec `sql_log_bin=0`).\n6. **Validation et Réouverture** :\n   - Tester la cohérence des tables (`CHECK TABLE`), vérifier les comptes, désactiver le mode maintenance, et rétablir le trafic.',
      explanation: 'Ce runbook standardisé élimine le stress et les erreurs humaines lors des crises de production.',
      examTrap: 'Ne jamais formater ou réinitialiser un serveur sans avoir d\'abord mis à l\'abri les derniers fichiers binlogs résiduels !',
      ruleRef: 'Oracle MySQL 8.0 Reference Manual - Disaster Recovery Steps',
    },
  },
  {
    id: 'fc-mysql908-dom04-100',
    cardNumber: 100,
    domainId: 'mysql-908-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Sauvegarde physique (MEB) et logique (mysqldump) & Restauration',
    subtopic: 'Grand Récapitulatif DOM-04 : Matrice d\'excellence pour l\'examen 1Z0-908',
    difficulty: 'hard',
    tags: ['Summary', '1Z0-908 Matrix', 'Exam Blueprint', 'Certification'],
    front: {
      question: 'Quels sont les points d\'attention ultimes à mémoriser pour réussir à 100% les questions sur le domaine DOM-04 (Backup & Recovery) du 1Z0-908 ?',
      hint: 'Distinction binaire physique/logique, options modernes 8.0, rôle du verrouillage BACKUP LOCK, et subtilités GTID.',
    },
    back: {
      answer: 'Matrice de Maîtrise Ultime 1Z0-908 - DOM-04 :\n\n- **Outils Logiques** :\n  - `mysqldump` : mono-thread, `--single-transaction` (InnoDB MVCC, pas de verrous DML), `--source-data=2` (coordonnées binlog).\n  - `MySQL Shell` : multi-threads par table (`bytesPerChunk`), compression `zstd`, `deferTableIndexes: \'all\'`, reprise sur panne (`resetProgress: false`).\n- **Outils Physiques (MEB)** :\n  - `mysqlbackup` : copie bloc de 16 Ko, suivi continu des redo logs, étapes `backup` -> `apply-log` -> `copy-back`.\n  - Verrouillage 8.0 : `LOCK INSTANCE FOR BACKUP` (bloque les DDL, autorise les DML) au lieu de `FTWRL`.\n  - Incrémentale : comparaison de `FIL_PAGE_LSN`, accéléré à 99% par `innodb_page_tracking`.\n- **PITR & Restauration** :\n  - Base Complète + Binlogs.\n  - Filtrage chirurgical : `mysqlbinlog --stop-position` (jamais par date si haute concurrence).\n  - Toujours désactiver `sql_log_bin=0` lors du rejeu.\n  - Avec GTID : utiliser `--skip-gtids` si réinjection sur un serveur non vierge.',
      explanation: 'La maîtrise de cette matrice garantit le score maximal sur toutes les questions théoriques et pratiques de sauvegarde.',
      examTrap: 'Attention aux faux synonymes : mysqldump n\'est pas physique, MEB n\'est pas logique, et mysqlpump n\'est pas le clone plugin.',
      ruleRef: 'Oracle MySQL 8.0 Database Administrator (1Z0-908) - Official Exam Guide',
    },
  },
];
