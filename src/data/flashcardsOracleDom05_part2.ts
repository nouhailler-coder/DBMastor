import { FlashcardItem } from '../types';

export const oracleDom05Part2Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 5 : SOUS-REQUÊTES SCALAIRES & CLAUSES SPÉCIFIQUES (51 à 62)
  // =========================================================================
  {
    id: 'fc-dom05-051',
    cardNumber: 51,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Définition d\'une expression de sous-requête scalaire',
    difficulty: 'easy',
    tags: ['Scalar Subquery', 'Définition', '1 ligne 1 colonne', 'NULL par défaut'],
    front: {
      question: 'Qu\'est-ce qu\'une sous-requête scalaire (scalar subquery) en SQL et quelle valeur produit-elle si aucune ligne ne correspond ?',
      hint: 'Une expression retournant une seule valeur atomique.',
    },
    back: {
      answer: 'Une sous-requête scalaire renvoie exactement UNE colonne et au maximum UNE ligne. Si aucune ligne ne correspond (0 ligne sélectionnée), elle s\'évalue automatiquement à la valeur NULL sans lever d\'erreur.',
      explanation: 'Parce qu\'elle renvoie une valeur unitaire, elle peut être utilisée presque partout où une constante littérale ou une expression de colonne est acceptée (SELECT, CASE, WHERE, ORDER BY).',
      codeSnippet: 'SELECT last_name,\n       (SELECT department_name\n        FROM departments d\n        WHERE d.department_id = e.department_id) AS dept_name\nFROM employees e;',
      examTrap: 'Si la sous-requête scalaire produit 2 lignes ou plus lors de l\'exécution, elle déclenche immédiatement ORA-01427.',
      ruleRef: 'Oracle SQL Reference - Scalar Subquery Expressions',
    },
  },
  {
    id: 'fc-dom05-052',
    cardNumber: 52,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requête scalaire dans une expression CASE',
    difficulty: 'medium',
    tags: ['CASE', 'Sous-requête scalaire', 'Condition dynamique', 'Syntaxe valide'],
    front: {
      question: 'Est-il valide d\'utiliser une sous-requête scalaire à l\'intérieur d\'une clause CASE ? Donnez un exemple concret.',
      hint: 'Peut-on comparer une colonne au résultat d\'une sous-requête dans une branche WHEN ?',
    },
    back: {
      answer: 'OUI, c\'est parfaitement valide. Une sous-requête scalaire peut être placée dans la clause WHEN, THEN ou ELSE d\'une expression CASE.',
      explanation: 'Oracle évalue la sous-requête scalaire comme une valeur unitaire pour tester la branche conditionnelle.',
      codeSnippet: 'SELECT employee_id, salary,\n  CASE\n    WHEN salary > (SELECT AVG(salary) FROM employees) THEN \'Au-dessus de la moyenne\'\n    ELSE \'En-dessous ou égal\'\n  END AS statut_salaire\nFROM employees;',
      examTrap: 'La sous-requête dans le CASE doit obligatoirement être scalaire (1 valeur max), sinon ORA-01427 ou ORA-00913 est levée.',
      ruleRef: 'Oracle SQL Reference - Subqueries in CASE Expressions',
    },
  },
  {
    id: 'fc-dom05-053',
    cardNumber: 53,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requête scalaire dans la clause ORDER BY',
    difficulty: 'medium',
    tags: ['ORDER BY', 'Sous-requête scalaire', 'Tri dynamique', 'Syntaxe'],
    front: {
      question: 'La requête suivante est-elle valide sous Oracle ? Comment trie-t-elle les employés ?',
      codeSnippet: 'SELECT employee_id, last_name, department_id\nFROM employees e\nORDER BY (\n  SELECT department_name\n  FROM departments d\n  WHERE d.department_id = e.department_id\n);',
      hint: 'Peut-on trier sur une colonne qui n\'appartient pas à la table interrogée sans faire de jointure explicite ?',
    },
    back: {
      answer: 'OUI, c\'est 100% valide. Elle trie les employés par ordre alphabétique du NOM de leur département (provenant de la table DEPARTMENTS), sans nécessiter de JOIN dans le FROM.',
      explanation: 'Une sous-requête scalaire corrélée dans ORDER BY associe à chaque ligne d\'employé le libellé de son département et utilise cette valeur pour déterminer la séquence de tri.',
      codeSnippet: '-- Valide et très utile quand on ne souhaite pas polluer le FROM ou le SELECT avec des jointures externes.',
      examTrap: 'Si un employé n\'a pas de département (department_id IS NULL), la sous-requête produit NULL et l\'employé est placé selon les règles de tri des NULLs (NULLS LAST par défaut en ASC).',
      ruleRef: 'Oracle SQL Reference - Subqueries in the ORDER BY Clause',
    },
  },
  {
    id: 'fc-dom05-054',
    cardNumber: 54,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requête dans la clause VALUES d\'un INSERT',
    difficulty: 'easy',
    tags: ['INSERT', 'VALUES', 'Sous-requête scalaire', 'DML'],
    front: {
      question: 'Peut-on utiliser une sous-requête à l\'intérieur de la clause VALUES d\'une instruction INSERT INTO ?',
      codeSnippet: 'INSERT INTO emp_bonus (emp_id, bonus_amount)\nVALUES (205, (SELECT salary * 0.15 FROM employees WHERE employee_id = 205));',
      hint: 'Une sous-requête scalaire peut-elle fournir la valeur d\'une colonne insérée ?',
    },
    back: {
      answer: 'OUI, une sous-requête scalaire entre parenthèses peut être utilisée dans la clause VALUES d\'un INSERT.',
      explanation: 'Elle permet de calculer dynamiquement une des valeurs insérées sans avoir à basculer obligatoirement vers la syntaxe INSERT INTO ... SELECT.',
      codeSnippet: '-- Valide : calcule 15% du salaire de l\'employé 205 pour son bonus.',
      examTrap: 'Si la sous-requête renvoie plus d\'une ligne, l\'erreur ORA-01427 empêchera l\'insertion de la ligne.',
      ruleRef: 'Oracle SQL Reference - Subqueries in INSERT VALUES',
    },
  },
  {
    id: 'fc-dom05-055',
    cardNumber: 55,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Clauses interdisant absolument les sous-requêtes',
    difficulty: 'hard',
    tags: ['Interdiction', 'CHECK constraint', 'GROUP BY', 'Règles SQL'],
    front: {
      question: 'Citez au moins trois contextes ou clauses SQL où l\'utilisation de sous-requêtes est strictement INTERDITE par Oracle.',
      hint: 'Pensez aux clauses de groupement, aux définitions de contraintes d\'intégrité, et aux clauses de partitionnement.',
    },
    back: {
      answer: '1. Dans la clause GROUP BY (erreur ORA-22818).\n2. Dans la condition d\'une contrainte CHECK de table (erreur ORA-02251).\n3. Dans les expressions de clés de partitionnement de table.\n4. Comme argument direct d\'une fonction d\'agrégation (ex: AVG((SELECT ...)) est illégal).',
      explanation: 'Les contraintes CHECK doivent être purement intra-ligne et déterministes sans dépendre de l\'état d\'autres tables ou lignes.',
      codeSnippet: '-- INTERDIT : ALTER TABLE emp ADD CONSTRAINT chk_sal CHECK (salary > (SELECT min_sal FROM jobs));\n-- Erreur : ORA-02251: subquery not allowed here',
      examTrap: '« Les sous-requêtes peuvent être utilisées dans n\'importe quelle clause SQL » est une affirmation fausse classique d\'examen.',
      ruleRef: 'Oracle Database Concepts - Subquery Restrictions',
    },
  },
  {
    id: 'fc-dom05-056',
    cardNumber: 56,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Jointures entre tables physiques et vues en ligne',
    difficulty: 'easy',
    tags: ['JOIN', 'Inline View', 'Combinaison', 'Agrégation préalable'],
    front: {
      question: 'Quel est l\'intérêt architectural majeur de joindre une table physique à une vue en ligne (Inline View) contenant un GROUP BY ?',
      codeSnippet: 'SELECT e.last_name, e.salary, d.avg_salary\nFROM employees e\nJOIN (\n  SELECT department_id, AVG(salary) AS avg_salary\n  FROM employees\n  GROUP BY department_id\n) d ON (e.department_id = d.department_id)\nWHERE e.salary > d.avg_salary;',
      hint: 'Comparaison des détails individuels avec des agrégats de groupe sans fonctions analytiques.',
    },
    back: {
      answer: 'Elle permet de comparer des lignes individuelles détaillées à des métriques agrégées de leur groupe en une seule passe, sans démultiplier le nombre de lignes retournées.',
      explanation: 'La vue en ligne produit exactement un enregistrement par département avec son salaire moyen, ce qui permet une équijointure 1:N propre et performante.',
      codeSnippet: '-- Affiche chaque employé dont le salaire dépasse la moyenne de son propre département.',
      examTrap: 'Sans la vue en ligne, tenter de faire cela dans la même requête avec un WHERE salary > AVG(salary) déclenche ORA-00934 (group function not allowed here).',
      ruleRef: 'Oracle SQL Reference - Joining Inline Views',
    },
  },
  {
    id: 'fc-dom05-057',
    cardNumber: 57,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'La clause LATERAL (Oracle 12c+)',
    difficulty: 'hard',
    tags: ['LATERAL', 'Clause FROM', 'Corrélation dans FROM', 'Oracle 12c'],
    front: {
      question: 'En SQL standard traditionnel, une vue en ligne dans le FROM peut-elle référencer les colonnes d\'une table déclarée avant elle dans le même FROM ? Quel mot-clé résout cela depuis Oracle 12c ?',
      hint: 'Le mot-clé commence par L.',
    },
    back: {
      answer: 'Traditionnellement NON (erreur ORA-00904 : identificateur non valide). Depuis Oracle 12c, le mot-clé LATERAL permet explicitement à une vue en ligne de référencer des tables situées à sa gauche dans le FROM.',
      explanation: 'LATERAL spécifie que la vue en ligne est corrélée et doit être réévaluée pour chaque ligne de la table parente.',
      codeSnippet: 'SELECT d.department_name, top_emp.last_name, top_emp.salary\nFROM departments d,\nLATERAL (\n  SELECT e.last_name, e.salary\n  FROM employees e\n  WHERE e.department_id = d.department_id\n  ORDER BY e.salary DESC\n  FETCH FIRST 1 ROW ONLY\n) top_emp;',
      examTrap: 'Sans le mot-clé LATERAL, la référence à « d.department_id » à l\'intérieur de la sous-requête dans le FROM échoue avec ORA-00904.',
      ruleRef: 'Oracle SQL Reference - LATERAL Inline Views',
    },
  },
  {
    id: 'fc-dom05-058',
    cardNumber: 58,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Opérateur CROSS APPLY',
    difficulty: 'hard',
    tags: ['CROSS APPLY', 'Jointure latérale', 'ANSI SQL', 'Oracle 12c'],
    front: {
      question: 'Qu\'est-ce que l\'opérateur CROSS APPLY en SQL Oracle et à quel type de jointure correspond-il ?',
      hint: 'Une jointure interne (INNER) avec une sous-requête corrélée à droite.',
    },
    back: {
      answer: 'CROSS APPLY est une jointure latérale interne : il évalue la sous-requête ou fonction table à droite pour chaque ligne de la table de gauche et ne renvoie la ligne de gauche que si la sous-requête produit au moins une ligne.',
      explanation: 'C\'est l\'équivalent moderne et standard d\'une jointure interne dépendante (INNER JOIN LATERAL). Si la table de droite ne produit 0 ligne, la ligne de gauche est rejetée.',
      codeSnippet: 'SELECT d.department_name, e.last_name, e.salary\nFROM departments d\nCROSS APPLY (\n  SELECT last_name, salary\n  FROM employees e\n  WHERE e.department_id = d.department_id\n  FETCH FIRST 2 ROWS ONLY\n) e;',
      examTrap: 'Si un département n\'a aucun employé, CROSS APPLY ne retourne pas ce département.',
      ruleRef: 'Oracle SQL Reference - CROSS APPLY Operator',
    },
  },
  {
    id: 'fc-dom05-059',
    cardNumber: 59,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Opérateur OUTER APPLY',
    difficulty: 'hard',
    tags: ['OUTER APPLY', 'Jointure latérale externe', 'Préservation de lignes', 'NULL'],
    front: {
      question: 'Quelle est la différence fondamentale entre CROSS APPLY et OUTER APPLY ?',
      hint: 'Pensez au comportement d\'un LEFT OUTER JOIN face aux résultats vides.',
    },
    back: {
      answer: 'OUTER APPLY préserve toutes les lignes de la table de gauche même si la sous-requête de droite ne retourne aucune ligne (les colonnes de droite sont alors remplies de NULLs), à l\'image d\'un LEFT OUTER JOIN.',
      explanation: 'À l\'inverse, CROSS APPLY rejette la ligne de gauche si la sous-requête de droite est vide.',
      codeSnippet: 'SELECT d.department_name, e.last_name\nFROM departments d\nOUTER APPLY (\n  SELECT last_name\n  FROM employees e\n  WHERE e.department_id = d.department_id\n  ORDER BY salary DESC\n  FETCH FIRST 1 ROW ONLY\n) e;',
      examTrap: 'Pour afficher TOUS les départements avec leur employé le mieux payé (y compris les départements sans personnel), utilisez impérativement OUTER APPLY.',
      ruleRef: 'Oracle SQL Reference - OUTER APPLY Operator',
    },
  },
  {
    id: 'fc-dom05-060',
    cardNumber: 60,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Différence entre vue permanente (CREATE VIEW) et Inline View',
    difficulty: 'easy',
    tags: ['CREATE VIEW', 'Inline View', 'Métadonnées', 'Durabilité'],
    front: {
      question: 'Quelles sont les deux différences majeures entre une vue permanente créée par « CREATE VIEW » et une vue en ligne (Inline View) ?',
      hint: 'Persistance dans le dictionnaire de données et réutilisabilité.',
    },
    back: {
      answer: '1. Une vue permanente est un objet stocké dans le dictionnaire de données (USER_VIEWS) et réutilisable par n\'importe quelle requête.\n2. Une vue en ligne existe UNIQUEMENT pendant la durée d\'exécution de la requête SQL qui la définit et ne stocke aucune métadonnée.',
      explanation: 'Sur le plan de l\'exécution et de l\'optimiseur de requêtes (CBO), les deux sont traitées de manière quasiment identique via le mécanisme de View Merging.',
      codeSnippet: '-- Vue permanente :\nCREATE VIEW v_dept_sal AS SELECT department_id, AVG(salary) sal FROM employees GROUP BY department_id;\n-- Vue en ligne :\nSELECT * FROM (SELECT department_id, AVG(salary) sal FROM employees GROUP BY department_id);',
      examTrap: 'Une vue en ligne ne peut pas être référencée en dehors de la requête où elle est écrite.',
      ruleRef: 'Oracle Database Concepts - Views vs Inline Views',
    },
  },
  {
    id: 'fc-dom05-061',
    cardNumber: 61,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Mise en cache des sous-requêtes scalaires (Scalar Subquery Caching)',
    difficulty: 'hard',
    tags: ['CBO', 'Cache', 'Performance', 'Mécanisme interne'],
    front: {
      question: 'Comment l\'optimiseur Oracle (CBO) optimise-t-il l\'exécution des sous-requêtes scalaires répétitives dans une clause SELECT ?',
      hint: 'Pensez à une table de hachage en mémoire pour stocker les résultats récents.',
    },
    back: {
      answer: 'Oracle implémente le « Scalar Subquery Caching » : il conserve en mémoire cache les paramètres d\'entrée récents et leurs résultats associés afin d\'éviter de réexécuter la sous-requête pour des valeurs d\'entrée identiques.',
      explanation: 'Si 100 employés appartiennent tous au département 50, la sous-requête de recherche du nom de département ne sera physiquement exécutée qu\'une seule fois pour la valeur 50, puis servie depuis le cache pour les 99 autres.',
      codeSnippet: 'SELECT last_name, (SELECT department_name FROM departments d WHERE d.department_id = e.department_id)\nFROM employees e;',
      examTrap: 'Si le nombre de valeurs distinctes en entrée est gigantesque, le cache sature et les performances chutent, rendant un JOIN explicite préférable.',
      ruleRef: 'Oracle Performance Tuning Guide - Scalar Subquery Caching',
    },
  },
  {
    id: 'fc-dom05-062',
    cardNumber: 62,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requêtes scalaires et fonctions SQL imbriquées',
    difficulty: 'medium',
    tags: ['Fonctions', 'UPPER', 'NVL', 'Sous-requête'],
    front: {
      question: 'Peut-on passer une sous-requête scalaire comme argument d\'une fonction mono-ligne (ex: UPPER, NVL, ROUND) ?',
      codeSnippet: 'SELECT employee_id,\n       NVL((SELECT commission_pct FROM bonus b WHERE b.emp_id = e.employee_id), 0) AS comm\nFROM employees e;',
      hint: 'Une sous-requête scalaire équivaut à une valeur unitaire.',
    },
    back: {
      answer: 'OUI, absolument. Toute fonction mono-ligne acceptant une expression scalaire peut recevoir une sous-requête scalaire entre parenthèses comme argument.',
      explanation: 'Le moteur évalue la sous-requête, puis transmet sa valeur retournée (ou NULL si 0 ligne) à la fonction enveloppante.',
      codeSnippet: 'SELECT ROUND((SELECT AVG(salary) FROM employees), 2) AS salaire_moyen FROM dual;',
      examTrap: 'La sous-requête doit être rigoureusement entourée de ses propres parenthèses à l\'intérieur des parenthèses de la fonction.',
      ruleRef: 'Oracle SQL Reference - Scalar Expressions as Arguments',
    },
  },

  // =========================================================================
  // SECTION 6 : SOUS-REQUÊTES CORRÉLÉES & DML (UPDATE / DELETE) (63 à 75)
  // =========================================================================
  {
    id: 'fc-dom05-063',
    cardNumber: 63,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Définition d\'une sous-requête corrélée',
    difficulty: 'easy',
    tags: ['Correlated Subquery', 'Définition', 'Évaluation ligne à ligne', 'Dépendance'],
    front: {
      question: 'Qu\'est-ce qu\'une sous-requête corrélée (correlated subquery) et comment se déroule son cycle d\'évaluation par rapport à la requête parente ?',
      hint: 'La sous-requête peut-elle s\'exécuter de façon autonome sans la requête externe ?',
    },
    back: {
      answer: 'C\'est une sous-requête qui référence une ou plusieurs colonnes de la table de la requête externe parente. Elle ne peut PAS s\'exécuter seule de façon autonome.',
      explanation: 'Oracle extrait une ligne candidate de la requête externe, injecte ses valeurs dans la sous-requête, exécute la sous-requête pour évaluer la condition, puis répète ce processus pour chaque ligne candidate de la table externe.',
      codeSnippet: 'SELECT e.last_name, e.salary, e.department_id\nFROM employees e\nWHERE e.salary > (\n  SELECT AVG(sub.salary)\n  FROM employees sub\n  WHERE sub.department_id = e.department_id -- Référence corrélée !\n);',
      examTrap: 'Une sous-requête corrélée est conceptuellement évaluée une fois par ligne candidate (contrairement à une sous-requête simple évaluée une seule fois au début).',
      ruleRef: 'Oracle Database Concepts - Processing Correlated Subqueries',
    },
  },
  {
    id: 'fc-dom05-064',
    cardNumber: 64,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Portée et visibilité des alias de table',
    difficulty: 'medium',
    tags: ['Scope', 'Visibilité', 'Alias', 'Erreur de portée'],
    front: {
      question: 'Dans une sous-requête corrélée, la requête interne peut-elle voir les alias de la requête externe ? La requête externe peut-elle voir les alias de la requête interne ?',
      hint: 'La portée est-elle unidirectionnelle ?',
    },
    back: {
      answer: 'La requête interne PEUT voir les alias de la requête externe (portée descendante). La requête externe NE PEUT PAS voir les alias définis uniquement à l\'intérieur de la sous-requête (portée restreinte).',
      explanation: 'Tenter d\'utiliser un alias de la sous-requête interne dans la clause SELECT ou WHERE externe provoque l\'erreur ORA-00904 : identificateur non valide.',
      codeSnippet: '-- Invalide ORA-00904 :\nSELECT e.last_name, sub.avg_sal  -- sub est inconnu ici !\nFROM employees e\nWHERE e.salary > (SELECT AVG(sub.salary) AS avg_sal FROM employees sub WHERE sub.dept_id = e.dept_id);',
      examTrap: 'Les alias internes sont totalement masqués et invisibles pour la requête parente externe.',
      ruleRef: 'Oracle SQL Reference - Scope of Identifiers in Subqueries',
    },
  },
  {
    id: 'fc-dom05-065',
    cardNumber: 65,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Le cas d\'école du salaire supérieur à la moyenne de son département',
    difficulty: 'medium',
    tags: ['Cas d\'école', 'Corrélation', 'AVG départemental', 'WHERE'],
    front: {
      question: 'Expliquez étape par étape le fonctionnement de cette requête d\'examen classique :',
      codeSnippet: 'SELECT last_name, salary, department_id\nFROM employees outer\nWHERE salary > (\n  SELECT AVG(salary)\n  FROM employees inner\n  WHERE inner.department_id = outer.department_id\n);',
      hint: 'Comment le calcul de la moyenne s\'adapte-t-il à chaque employé ?',
    },
    back: {
      answer: 'Pour chaque employé candidat examiné dans la table "outer", Oracle calcule le salaire moyen des employés travaillant dans le MÊME département (inner.department_id = outer.department_id). Si le salaire du candidat dépasse cette moyenne spécifique, la ligne est affichée.',
      explanation: 'Cela permet un calcul dynamique par groupe sans utiliser de GROUP BY au niveau de la requête principale.',
      codeSnippet: '-- Si l\'employé appartient au département 60, la moyenne est calculée sur le département 60 uniquement.',
      examTrap: 'Si department_id est NULL pour un employé, la condition de corrélation inner.dept = NULL donne UNKNOWN, la sous-requête ne trouve rien et l\'employé est écarté.',
      ruleRef: 'Oracle University - SQL Fundamentals Correlated Subqueries',
    },
  },
  {
    id: 'fc-dom05-066',
    cardNumber: 66,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requête corrélée dans l\'instruction UPDATE',
    difficulty: 'hard',
    tags: ['UPDATE', 'DML', 'Corrélation', 'Mise à jour en masse'],
    front: {
      question: 'Comment fonctionne une instruction UPDATE utilisant une sous-requête corrélée dans sa clause SET ?',
      codeSnippet: 'UPDATE employees e\nSET e.department_name = (\n  SELECT d.department_name\n  FROM departments d\n  WHERE d.department_id = e.department_id\n);',
      hint: 'Pour chaque ligne mise à jour, d\'où provient la nouvelle valeur ?',
    },
    back: {
      answer: 'Pour chaque ligne de la table EMPLOYEES parcourue par le UPDATE, Oracle exécute la sous-requête corrélée pour rechercher le nom du département correspondant à son department_id et assigne cette valeur au champ e.department_name.',
      explanation: 'C\'est la méthode standard en SQL Oracle pour synchroniser des données dénormalisées entre tables liées.',
      codeSnippet: '-- Chaque employé reçoit le nom exact de son département respectif.',
      examTrap: 'ATTENTION PIÈGE MORTEL : si un employé a department_id valant NULL ou un identifiant inexistant dans DEPARTMENTS, la sous-requête renvoie NULL, et la colonne est écrasée par NULL !',
      ruleRef: 'Oracle SQL Reference - Correlated UPDATE Statements',
    },
  },
  {
    id: 'fc-dom05-067',
    cardNumber: 67,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sécuriser un UPDATE corrélé avec WHERE EXISTS',
    difficulty: 'hard',
    tags: ['UPDATE', 'WHERE EXISTS', 'Éviter NULL', 'Bonne pratique d\'examen'],
    front: {
      question: 'Pourquoi devez-vous presque TOUJOURS coupler une clause SET corrélée avec une clause WHERE EXISTS dans un UPDATE ?',
      codeSnippet: 'UPDATE employees e\nSET e.department_name = (\n  SELECT d.department_name FROM departments d WHERE d.department_id = e.department_id\n)\nWHERE EXISTS (\n  SELECT 1 FROM departments d WHERE d.department_id = e.department_id\n);',
      hint: 'Que se passe-t-il pour les lignes qui n\'ont pas de correspondance dans la table cible ?',
    },
    back: {
      answer: 'Sans la clause WHERE EXISTS, l\'UPDATE met à jour TOUTES les lignes de la table. Si une ligne n\'a pas de correspondance dans la sous-requête, sa colonne est impitoyablement écrasée par NULL.',
      explanation: 'La clause WHERE EXISTS garantit que seules les lignes ayant une contrepartie réelle dans la table de référence sont modifiées ; les autres conservent leur valeur intacte.',
      codeSnippet: '-- Sans WHERE EXISTS, un employé avec department_id NULL voit sa colonne department_name mise à NULL même si elle contenait une valeur auparavant !',
      examTrap: 'Une question d\'examen très classique montre un UPDATE qui a accidentellement mis des colonnes à NULL : la cause est l\'omission du WHERE EXISTS.',
      ruleRef: 'Oracle Database Administration Guide - Safe Correlated Updates',
    },
  },
  {
    id: 'fc-dom05-068',
    cardNumber: 68,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Instruction DELETE avec sous-requête corrélée',
    difficulty: 'medium',
    tags: ['DELETE', 'Corrélation', 'Suppression ciblée', 'DML'],
    front: {
      question: 'Écrivez une requête DELETE supprimant tous les départements de la table DEPARTMENTS qui ne possèdent aucun employé associé.',
      hint: 'Utilisez DELETE FROM departments d WHERE NOT EXISTS (...)',
    },
    back: {
      answer: 'DELETE FROM departments d\nWHERE NOT EXISTS (\n  SELECT 1\n  FROM employees e\n  WHERE e.department_id = d.department_id\n);',
      explanation: 'Oracle teste chaque département un par un. Si la sous-requête ne trouve aucun employé rattaché à ce département, NOT EXISTS est VRAI et le département est supprimé.',
      codeSnippet: '-- Supprime les départements orphelins en toute sécurité.',
      examTrap: 'Ne tentez pas d\'écrire « DELETE d FROM departments d » : en syntaxe SQL Oracle, le nom de table ou alias ne se répète pas après le mot-clé DELETE.',
      ruleRef: 'Oracle SQL Reference - Correlated DELETE Statements',
    },
  },
  {
    id: 'fc-dom05-069',
    cardNumber: 69,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Dédoublonnage de lignes avec ROWID et sous-requête corrélée',
    difficulty: 'hard',
    tags: ['ROWID', 'Dédoublonnage', 'DELETE', 'Requête d\'expert'],
    front: {
      question: 'Expliquez la logique de cette requête célèbre pour supprimer les doublons d\'une table en ne conservant que l\'occurrence ayant le plus petit ROWID :',
      codeSnippet: 'DELETE FROM duplicate_table a\nWHERE a.rowid > (\n  SELECT MIN(b.rowid)\n  FROM duplicate_table b\n  WHERE b.id = a.id AND b.code = a.code\n);',
      hint: 'ROWID est l\'adresse physique unique d\'une ligne dans Oracle.',
    },
    back: {
      answer: 'La sous-requête corrélée trouve le plus petit ROWID (MIN(rowid)) pour chaque groupe de valeurs dupliquées (id, code). Si le rowid de la ligne courante "a" est strictement supérieur à ce minimum, cela signifie qu\'il s\'agit d\'un doublon ultérieur, et la ligne est supprimée.',
      explanation: 'Cette technique permet de conserver exactement une ligne par groupe de doublons de façon hautement performante.',
      codeSnippet: '-- La première ligne insérée (MIN(rowid)) est conservée, les répliques sont supprimées.',
      examTrap: 'Cette question apparaît sous différentes variantes à l\'examen 1Z0-071 pour tester la maîtrise conjointe des sous-requêtes corrélées et du pseudo-colonne ROWID.',
      ruleRef: 'Oracle Database SQL Language Reference - ROWID and Deduplication',
    },
  },
  {
    id: 'fc-dom05-070',
    cardNumber: 70,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Imbrication de sous-requêtes corrélées sur plusieurs niveaux',
    difficulty: 'hard',
    tags: ['Multi-niveaux', 'Profondeur', 'Visibilité des ancêtres', 'Règle de portée'],
    front: {
      question: 'Une sous-requête imbriquée au 3ème niveau de profondeur peut-elle référencer un alias défini au 1er niveau (la requête principale racine) ?',
      hint: 'Les alias de requête sont-ils hérités dans toute l\'arborescence descendante ?',
    },
    back: {
      answer: 'OUI ! En SQL Oracle, la visibilité des alias est descendante dans toute la hiérarchie : une sous-requête à n\'importe quel niveau de profondeur peut référencer les colonnes de n\'importe laquelle de ses requêtes ancêtres.',
      explanation: 'Cependant, pour préserver la lisibilité et les performances de l\'optimiseur, il est déconseillé de dépasser deux niveaux de corrélation.',
      codeSnippet: 'SELECT * FROM t1\nWHERE col1 IN (\n  SELECT col2 FROM t2\n  WHERE col2 IN (\n    SELECT col3 FROM t3\n    WHERE t3.x = t1.x -- Référence à la table racine t1 valide !\n  )\n);',
      examTrap: 'La visibilité est descendante mais JAMAIS ascendante (t1 ne peut pas voir les colonnes de t3).',
      ruleRef: 'Oracle SQL Reference - Scope Rules for Multilevel Subqueries',
    },
  },
  {
    id: 'fc-dom05-071',
    cardNumber: 71,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requête corrélée dans la clause HAVING',
    difficulty: 'hard',
    tags: ['HAVING', 'Corrélation', 'Agrégats de groupe', 'Cas avancé'],
    front: {
      question: 'Une sous-requête placée dans la clause HAVING peut-elle être corrélée avec la requête principale ? Donnez les règles.',
      hint: 'Quelles colonnes de la requête principale peuvent être référencées dans HAVING ?',
    },
    back: {
      answer: 'OUI, mais la corrélation ne peut référencer que les colonnes qui font partie de la clause GROUP BY de la requête principale ou des expressions agrégées.',
      explanation: 'Puisque la clause HAVING s\'exécute au niveau des groupes consolidés, elle ne peut pas corréler des colonnes individuelles qui n\'ont pas été groupées.',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees e1\nGROUP BY department_id\nHAVING AVG(salary) > (\n  SELECT AVG(salary) FROM employees e2\n  WHERE e2.job_id = (SELECT job_id FROM employees WHERE employee_id = 100)\n);',
      examTrap: 'Référencer une colonne non groupée de la requête externe dans le HAVING de la sous-requête lève ORA-00979 (not a GROUP BY expression).',
      ruleRef: 'Oracle SQL Reference - Correlated Subqueries in HAVING',
    },
  },
  {
    id: 'fc-dom05-072',
    cardNumber: 72,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requête corrélée vs Auto-jointure (Self Join)',
    difficulty: 'medium',
    tags: ['Comparaison', 'Self Join', 'Équivalence logique', 'Choix de conception'],
    front: {
      question: 'Quelle est l\'équivalence entre une sous-requête corrélée pour comparer des employés et une auto-jointure (self-join) ?',
      codeSnippet: '-- Option 1 : Sous-requête corrélée\nSELECT e.last_name, e.salary FROM employees e WHERE e.salary > (SELECT AVG(salary) FROM employees WHERE department_id = e.department_id);',
      hint: 'Comment réécrire cela avec une Inline View jointe ?',
    },
    back: {
      answer: 'On peut remplacer la sous-requête corrélée par une auto-jointure avec une vue en ligne pré-agrégée (JOIN (SELECT department_id, AVG(salary) ... GROUP BY department_id) d ON e.department_id = d.department_id).',
      explanation: 'Souvent, l\'optimiseur Oracle (CBO) effectue en interne une transformation appelée « Subquery Unnesting » pour transformer la sous-requête corrélée en jointure afin de paralléliser le traitement.',
      codeSnippet: '-- Subquery unnesting permet au CBO d\'utiliser un Hash Join performant au lieu d\'une boucle itérative.',
      examTrap: 'Sur le plan des résultats, les deux approches retournent les mêmes données si les NULLs sont gérés pareillement.',
      ruleRef: 'Oracle Performance Tuning Guide - Subquery Unnesting',
    },
  },
  {
    id: 'fc-dom05-073',
    cardNumber: 73,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requête corrélée dans la projection SELECT',
    difficulty: 'medium',
    tags: ['SELECT', 'Corrélation scalaire', 'Projection', 'Alternative au JOIN'],
    front: {
      question: 'Quel est l\'inconvénient potentiel sur les performances de multiplier les sous-requêtes corrélées dans la clause SELECT plutôt que de faire des LEFT JOINs ?',
      codeSnippet: 'SELECT e.last_name,\n  (SELECT d.department_name FROM departments d WHERE d.department_id = e.department_id) AS dept,\n  (SELECT j.job_title FROM jobs j WHERE j.job_id = e.job_id) AS job\nFROM employees e;',
      hint: 'Combien de fois les sous-requêtes sont-elles exécutées ?',
    },
    back: {
      answer: 'Chaque sous-requête corrélée dans le SELECT doit conceptuellement être évaluée pour chaque ligne de la table principale. Si le volume est important et la mise en cache insuffisante, cela engendre un surcoût d\'appels répétés par rapport à un Hash Join global.',
      explanation: 'Pour un petit nombre de lignes, c\'est très lisible et rapide (grâce au scalar caching), mais pour des millions de lignes, les jointures classiques sont plus scalables.',
      codeSnippet: '-- Valide et pratique pour des lookups ponctuels.',
      examTrap: 'Assurez-vous toujours que chaque sous-requête dans le SELECT ne renvoie jamais plus d\'une ligne, sinon ORA-01427 fait planter toute la requête.',
      ruleRef: 'Oracle SQL Reference - Correlated Scalar Subqueries in Select List',
    },
  },
  {
    id: 'fc-dom05-074',
    cardNumber: 74,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Règle de résolution des noms de colonnes non qualifiés',
    difficulty: 'hard',
    tags: ['Résolution des noms', 'Homonymie', 'Piège classique', 'Qualification obligatoire'],
    front: {
      question: 'Dans la requête ci-dessous, la colonne "department_id" dans la sous-requête n\'est pas qualifiée. De quelle table Oracle extrait-il cette colonne ?',
      codeSnippet: 'SELECT last_name\nFROM employees\nWHERE department_id IN (\n  SELECT department_id\n  FROM departments\n);',
      hint: 'La résolution cherche-t-elle d\'abord dans la table locale la plus proche ou dans la table externe ?',
    },
    back: {
      answer: 'Oracle résout TOUJOURS les colonnes non qualifiées en cherchant D\'ABORD dans les tables de la requête locale (ici DEPARTMENTS). Si elle n\'y existe pas, il cherche dans les requêtes parentes externes.',
      explanation: 'Si la colonne existe dans la table de la sous-requête, elle est résolue localement. Si elle n\'existe PAS dans la sous-requête mais existe dans la table externe, la requête devient subitement une sous-requête corrélée sans que vous l\'ayez explicitement voulu !',
      codeSnippet: '-- PIÈGE DANGEREUX :\n-- Si une table cible ne contient pas la colonne mais la table externe oui, la requête ne lève pas d\'erreur et corréle silencieusement avec le parent !',
      examTrap: 'Bonne pratique impérative pour l\'examen et la production : TOUJOURS préfixer toutes les colonnes par l\'alias de leur table dans les sous-requêtes.',
      ruleRef: 'Oracle SQL Reference - Identifier Resolution in Subqueries',
    },
  },
  {
    id: 'fc-dom05-075',
    cardNumber: 75,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Sous-requêtes corrélées et prédicat LIKE',
    difficulty: 'medium',
    tags: ['LIKE', 'Corrélation', 'Concaténation', 'Pattern matching'],
    front: {
      question: 'Peut-on utiliser une sous-requête corrélée avec l\'opérateur de recherche textuelle LIKE ?',
      codeSnippet: 'SELECT c.customer_name\nFROM customers c\nWHERE EXISTS (\n  SELECT 1 FROM blacklist b\n  WHERE c.customer_name LIKE \'%\' || b.keyword || \'%\'\n);',
      hint: 'La condition de corrélation doit-elle obligatoirement être une égalité (=) ?',
    },
    back: {
      answer: 'OUI ! La corrélation peut reposer sur n\'importe quelle condition booléenne légale en SQL (LIKE, BETWEEN, >, <, fonctions regex, etc.), et pas uniquement sur une égalité (=).',
      explanation: 'Toute expression reliant la table externe à la sous-requête constitue une corrélation valide.',
      codeSnippet: '-- Valide : sélectionne les clients dont le nom contient l\'un des mots-clés de la liste noire.',
      examTrap: 'Ne croyez pas que les sous-requêtes corrélées sont restreintes aux comparaisons PK/FK par égalité.',
      ruleRef: 'Oracle SQL Reference - Non-Equi Correlated Conditions',
    },
  },

  // =========================================================================
  // SECTION 7 : OPÉRATEURS EXISTS ET NOT EXISTS VS IN / NOT IN (76 à 86)
  // =========================================================================
  {
    id: 'fc-dom05-076',
    cardNumber: 76,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Fonctionnement de l\'opérateur EXISTS',
    difficulty: 'easy',
    tags: ['EXISTS', 'Test booléen', 'Présence de lignes', 'Performance'],
    front: {
      question: 'Comment fonctionne l\'opérateur EXISTS et que vérifie-t-il exactement ?',
      hint: 'S\'intéresse-t-il aux valeurs des colonnes ou au nombre de lignes ?',
    },
    back: {
      answer: 'EXISTS teste uniquement l\'EXISTENCE d\'au moins une ligne répondant aux critères de la sous-requête. Il s\'évalue à TRUE dès qu\'au moins une ligne est trouvée, et à FALSE si la sous-requête ne retourne aucune ligne.',
      explanation: 'L\'opérateur EXISTS ne renvoie aucune donnée : c\'est un opérateur purement booléen qui n\'évalue jamais à UNKNOWN (seulement TRUE ou FALSE).',
      codeSnippet: 'SELECT department_id, department_name\nFROM departments d\nWHERE EXISTS (\n  SELECT 1\n  FROM employees e\n  WHERE e.department_id = d.department_id\n);',
      examTrap: 'Contrairement aux autres opérateurs de comparaison, EXISTS n\'est précédé d\'aucun nom de colonne à sa gauche.',
      ruleRef: 'Oracle SQL Reference - The EXISTS Condition',
    },
  },
  {
    id: 'fc-dom05-077',
    cardNumber: 77,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'SELECT 1 vs SELECT * vs SELECT NULL dans EXISTS',
    difficulty: 'medium',
    tags: ['SELECT 1', 'SELECT *', 'SELECT NULL', 'Optimisation d\'examen'],
    front: {
      question: 'Y a-t-il une différence de résultat ou de performance entre « WHERE EXISTS (SELECT 1 ...) », « WHERE EXISTS (SELECT * ...) » et « WHERE EXISTS (SELECT NULL ...) » ?',
      hint: 'Le moteur SQL Oracle projette-t-il réellement les colonnes spécifiées dans EXISTS ?',
    },
    back: {
      answer: 'AUCUNE DIFFÉRENCE. Les trois syntaxes sont strictement équivalentes en termes de résultat et de plan d\'exécution.',
      explanation: 'Puisque EXISTS ne s\'intéresse qu\'à l\'existence de la ligne et non à son contenu, l\'optimiseur Oracle ignore complètement la clause SELECT de la sous-requête. SELECT NULL renvoie une ligne contenant NULL, ce qui compte comme une ligne existante (TRUE) !',
      codeSnippet: '-- Ces 3 requêtes font exactement la même chose :\nWHERE EXISTS (SELECT 1 FROM employees ...)\nWHERE EXISTS (SELECT * FROM employees ...)\nWHERE EXISTS (SELECT NULL FROM employees ...)',
      examTrap: 'Piège d\'examen classique : croire que « SELECT NULL » dans un EXISTS s\'évalue à faux ou à null. C\'est FAUX : une ligne contenant NULL est une ligne valide, donc EXISTS renvoie TRUE !',
      ruleRef: 'Oracle Database Concepts - EXISTS Optimization',
    },
  },
  {
    id: 'fc-dom05-078',
    cardNumber: 78,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Mécanisme de court-circuit (Short-Circuit) d\'EXISTS',
    difficulty: 'medium',
    tags: ['Short-Circuit', 'Court-circuit', 'Performance', 'Arrêt immédiat'],
    front: {
      question: 'Pourquoi l\'opérateur EXISTS est-il particulièrement efficace lorsqu\'une table contient des millions d\'enregistrements correspondants ?',
      hint: 'Oracle a-t-il besoin de compter toutes les lignes qui correspondent ?',
    },
    back: {
      answer: 'Grâce au mécanisme de court-circuit (short-circuit) : dès qu\'Oracle trouve la TOUTE PREMIÈRE ligne qui satisfait la condition de la sous-requête, il arrête immédiatement la recherche et renvoie TRUE, sans scanner le reste de la table.',
      explanation: 'À l\'inverse, une fonction comme COUNT(*) devrait scanner toutes les lignes correspondantes pour calculer le total avant de tester si COUNT(*) > 0.',
      codeSnippet: '-- Très rapide : stoppe à la première occurrence trouvée !\nWHERE EXISTS (SELECT 1 FROM big_table WHERE big_table.id = parent.id)',
      examTrap: 'Ne remplacez jamais « WHERE EXISTS (...) » par « WHERE (SELECT COUNT(*) ...) > 0 » : c\'est un anti-pattern de performance majeur sanctionné à l\'examen.',
      ruleRef: 'Oracle Performance Tuning Guide - EXISTS Short-Circuit Evaluation',
    },
  },
  {
    id: 'fc-dom05-079',
    cardNumber: 79,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'L\'opérateur NOT EXISTS',
    difficulty: 'easy',
    tags: ['NOT EXISTS', 'Absence de ligne', 'Antijoin', 'Sémantique'],
    front: {
      question: 'Que retourne la condition NOT EXISTS et quand s\'évalue-t-elle à TRUE ?',
      hint: 'Quand aucune ligne ne valide la sous-requête.',
    },
    back: {
      answer: 'NOT EXISTS s\'évalue à TRUE si et seulement si la sous-requête interne ne retourne AUCUNE ligne (ensemble résultant vide). Si au moins une ligne correspond, NOT EXISTS s\'évalue à FALSE.',
      explanation: 'C\'est l\'outil de prédilection en SQL pour détecter l\'absence de dépendance ou d\'historique (clients sans commande, départements sans employé, articles jamais vendus).',
      codeSnippet: 'SELECT d.department_name\nFROM departments d\nWHERE NOT EXISTS (\n  SELECT 1 FROM employees e WHERE e.department_id = d.department_id\n);',
      examTrap: 'NOT EXISTS renvoie toujours soit TRUE soit FALSE. Il ne renvoie JAMAIS UNKNOWN.',
      ruleRef: 'Oracle SQL Reference - The NOT EXISTS Condition',
    },
  },
  {
    id: 'fc-dom05-080',
    cardNumber: 80,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Comparaison directe : NOT IN vs NOT EXISTS face aux NULLs',
    difficulty: 'hard',
    tags: ['NOT IN vs NOT EXISTS', 'Comparatif', 'NULL safety', 'Question favorite d\'examen'],
    front: {
      question: 'Soit la table ORDERS avec des valeurs CLIENT_ID pouvant être NULL. Pourquoi la requête A ne retourne rien alors que la requête B retourne la liste exacte des clients sans commande ?',
      codeSnippet: '-- Requête A :\nSELECT client_id FROM clients WHERE client_id NOT IN (SELECT client_id FROM orders);\n\n-- Requête B :\nSELECT c.client_id FROM clients c WHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.client_id = c.client_id);',
      hint: 'Rappelez-vous la décomposition logique de NOT IN face à NULL.',
    },
    back: {
      answer: 'Dans la requête A, la présence d\'au moins un CLIENT_ID valant NULL dans ORDERS fait évaluer le NOT IN à UNKNOWN pour chaque client, vidant complètement le résultat. Dans la requête B, la corrélation o.client_id = c.client_id ignore naturellement les NULLs et NOT EXISTS vérifie fidèlement l\'absence de commande.',
      explanation: 'NOT EXISTS est « NULL-safe » car il teste la présence de lignes dans un ensemble, tandis que NOT IN effectue une égalité booléenne sur les valeurs.',
      codeSnippet: '-- Règle d\'or absolue :\n-- Si la colonne externe/interne est nullable, préférez TOUJOURS NOT EXISTS à NOT IN.',
      examTrap: 'C\'est la question conceptuelle la plus rentable de l\'examen 1Z0-071.',
      ruleRef: 'Oracle SQL Reference - Comparison of NOT IN and NOT EXISTS',
    },
  },
  {
    id: 'fc-dom05-081',
    cardNumber: 81,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'EXISTS sans sous-requête corrélée',
    difficulty: 'medium',
    tags: ['EXISTS non corrélé', 'Condition globale', 'Tout ou rien', 'Drapeau'],
    front: {
      question: 'Que produit une requête utilisant EXISTS avec une sous-requête NON corrélée ?',
      codeSnippet: 'SELECT last_name, salary\nFROM employees\nWHERE EXISTS (\n  SELECT 1 FROM departments WHERE department_id = 10\n);',
      hint: 'La condition dépend-elle de chaque employé individuel ?',
    },
    back: {
      answer: 'C\'est un comportement de type « tout ou rien » : si la sous-requête renvoie au moins une ligne, EXISTS vaut TRUE et TOUTES les lignes de la table externe sont retournées. Si la sous-requête est vide, AUCUNE ligne n\'est retournée.',
      explanation: 'Puisqu\'il n\'y a aucune corrélation, la sous-requête est évaluée une seule fois. Ici, le département 10 existant, tous les employés sont affichés.',
      codeSnippet: '-- Si le département 10 existe => Affiche tous les employés.\n-- Si le département 10 n\'existe pas => Affiche 0 ligne.',
      examTrap: 'À l\'examen, si la sous-requête d\'un EXISTS n\'a pas d\'alias la reliant au parent, méfiez-vous : elle filtre globalement la requête entière comme un commutateur on/off.',
      ruleRef: 'Oracle Database Concepts - Uncorrelated EXISTS Behavior',
    },
  },
  {
    id: 'fc-dom05-082',
    cardNumber: 82,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Division relationnelle avec double NOT EXISTS',
    difficulty: 'hard',
    tags: ['Double NOT EXISTS', 'Division relationnelle', 'Pour tous les éléments', 'Cas expert'],
    front: {
      question: 'Comment exprime-t-on en SQL la question : « Trouver les étudiants qui sont inscrits à TOUS les cours disponibles » sans fonction d\'agrégation COUNT ?',
      hint: 'Trouver les étudiants pour lesquels il N\'EXISTE AUCUN cours auquel il N\'EXISTE PAS d\'inscription.',
    },
    back: {
      answer: 'On utilise une double négation avec deux clauses NOT EXISTS imbriquées (division relationnelle).',
      explanation: 'En logique : « Être inscrit à tous les cours » équivaut à « Il n\'existe aucun cours auquel cet étudiant n\'est pas inscrit ».',
      codeSnippet: 'SELECT s.student_name\nFROM students s\nWHERE NOT EXISTS (\n  SELECT c.course_id FROM courses c\n  WHERE NOT EXISTS (\n    SELECT 1 FROM enrollments e\n    WHERE e.student_id = s.student_id AND e.course_id = c.course_id\n  )\n);',
      examTrap: 'Une alternative classique plus intuitive utilise COUNT(DISTINCT course_id) = (SELECT COUNT(*) FROM courses).',
      ruleRef: 'C.J. Date - Relational Database Writings: The Relational Division',
    },
  },
  {
    id: 'fc-dom05-083',
    cardNumber: 83,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Transformer un LEFT JOIN ... WHERE IS NULL en NOT EXISTS',
    difficulty: 'medium',
    tags: ['LEFT JOIN IS NULL', 'NOT EXISTS', 'Équivalence d\'antijointure', 'Refactorisation'],
    front: {
      question: 'Donnez la requête équivalente à ce LEFT JOIN filtré en utilisant l\'opérateur NOT EXISTS :',
      codeSnippet: 'SELECT d.department_id, d.department_name\nFROM departments d\nLEFT JOIN employees e ON (d.department_id = e.department_id)\nWHERE e.employee_id IS NULL;',
      hint: 'Écrivez une condition corrélée dans NOT EXISTS.',
    },
    back: {
      answer: 'SELECT d.department_id, d.department_name\nFROM departments d\nWHERE NOT EXISTS (\n  SELECT 1 FROM employees e WHERE e.department_id = d.department_id\n);',
      explanation: 'Ces deux requêtes sont sémantiquement et fonctionnellement identiques : elles extraient toutes les deux les départements sans aucun employé (antijointure).',
      codeSnippet: '-- Les deux requêtes produisent exactement le même jeu de données.',
      examTrap: 'L\'optimiseur Oracle les réécrit d\'ailleurs souvent sous le même plan d\'exécution interne (Hash Join Anti).',
      ruleRef: 'Oracle SQL Reference - Antijoin Implementations',
    },
  },
  {
    id: 'fc-dom05-084',
    cardNumber: 84,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Transformation en sous-requête IN vs EXISTS',
    difficulty: 'medium',
    tags: ['IN vs EXISTS', 'Subquery Unnesting', 'Semi-jointure', 'CBO'],
    front: {
      question: 'Dans quel cas l\'écriture avec « WHERE col IN (SELECT col ...) » et l\'écriture avec « WHERE EXISTS (SELECT 1 ... WHERE col = outer.col) » produisent-elles exactement le même résultat ?',
      hint: 'Y a-t-il une divergence si les colonnes ne contiennent aucun NULL ?',
    },
    back: {
      answer: 'Si la colonne de jointure n\'est pas nullable (contrainte NOT NULL garantie) ou si aucune valeur NULL n\'est manipulée, IN et EXISTS sont strictement équivalents en résultat.',
      explanation: 'Dans les versions modernes d\'Oracle, le CBO convertit généralement les deux formes en une « Semi-jointure » (Semi-Join Hash ou Nested Loops) identique.',
      codeSnippet: '-- Équivalence si department_id est non nul :\nWHERE department_id IN (SELECT department_id FROM departments WHERE location_id = 1700)\nWHERE EXISTS (SELECT 1 FROM departments d WHERE d.department_id = e.department_id AND d.location_id = 1700)',
      examTrap: 'Dès que des NULLs interviennent dans les données avec une négation (NOT IN vs NOT EXISTS), l\'équivalence est brisée !',
      ruleRef: 'Oracle Database SQL Tuning - Semi-Join Transformations',
    },
  },
  {
    id: 'fc-dom05-085',
    cardNumber: 85,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Synthèse des 4 méthodes d\'exclusion (Antijoin)',
    difficulty: 'hard',
    tags: ['Synthèse', '4 méthodes', 'Antijoin', 'Comparatif global'],
    front: {
      question: 'Quelles sont les 4 méthodes SQL distinctes pour trouver les enregistrements de la table A absents de la table B ?',
      hint: '1) Sous-requête avec négation d\'appartenance\n2) Test booléen d\'existence\n3) Jointure externe\n4) Opérateur d\'ensemble',
    },
    back: {
      answer: '1. NOT IN (avec filtre IS NOT NULL pour se prémunir des pièges).\n2. NOT EXISTS (la méthode la plus sûre et universelle).\n3. LEFT OUTER JOIN avec prédicat « WHERE B.pk IS NULL ».\n4. L\'opérateur d\'ensemble MINUS (SELECT pk FROM A MINUS SELECT fk FROM B).',
      explanation: 'Chaque méthode a ses avantages : NOT EXISTS est le plus robuste, LEFT JOIN permet de projeter d\'autres colonnes, MINUS est très concis pour comparer des clés primaires.',
      codeSnippet: '-- Méthode 4 (MINUS) :\nSELECT department_id FROM departments\nMINUS\nSELECT department_id FROM employees WHERE department_id IS NOT NULL;',
      examTrap: 'L\'examen 1Z0-071 présente souvent ces 4 options et demande d\'identifier celle qui ne fonctionne pas ou celles qui sont équivalentes.',
      ruleRef: 'Oracle Certification Guide - 4 Ways to Perform Antijoins',
    },
  },
  {
    id: 'fc-dom05-086',
    cardNumber: 86,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'EXISTS et les fonctions d\'agrégation dans la sous-requête',
    difficulty: 'medium',
    tags: ['EXISTS', 'COUNT(*)', 'Agrégation inutile', 'Anti-pattern'],
    front: {
      question: 'Pourquoi la condition « WHERE EXISTS (SELECT COUNT(*) FROM employees WHERE department_id = d.department_id) » est-elle un piège et s\'évalue-t-elle TOUJOURS à TRUE ?',
      hint: 'Combien de lignes COUNT(*) sans GROUP BY renvoie-t-il, même s\'il n\'y a aucun employé ?',
    },
    back: {
      answer: 'Parce que COUNT(*) sans GROUP BY renvoie TOUJOURS exactement UNE ligne (la valeur 0 si aucun employé n\'existe). Pour EXISTS, une ligne contenant le chiffre 0 est UNE LIGNE EXISTANTE, donc EXISTS renvoie TOUJOURS TRUE !',
      explanation: 'La sous-requête ne produit jamais un ensemble vide de lignes, ce qui rend la condition WHERE EXISTS constamment vraie pour tous les départements sans exception.',
      codeSnippet: '-- PIÈGE MORTEL D\'EXAMEN :\nSELECT COUNT(*) FROM employees WHERE 1=2; -- Renvoie 1 ligne (valeur 0) !\n-- Par conséquent : WHERE EXISTS (SELECT COUNT(*) ...) est TOUJOURS VRAI !',
      examTrap: 'Ne mettez JAMAIS de fonction d\'agrégation sans GROUP BY à l\'intérieur d\'un EXISTS !',
      ruleRef: 'Oracle SQL Reference - Aggregate Functions in EXISTS Conditions',
    },
  },

  // =========================================================================
  // SECTION 8 : CLAUSE WITH (SUBQUERY FACTORING / CTE) (87 à 93)
  // =========================================================================
  {
    id: 'fc-dom05-087',
    cardNumber: 87,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Définition de la clause WITH (Subquery Factoring / CTE)',
    difficulty: 'easy',
    tags: ['Clause WITH', 'CTE', 'Subquery Factoring', 'Lisibilité'],
    front: {
      question: 'Qu\'est-ce que la clause WITH (également appelée Subquery Factoring ou Common Table Expression - CTE) en SQL Oracle et où se place-t-elle ?',
      hint: 'Elle se place au tout début de la requête SELECT.',
    },
    back: {
      answer: 'La clause WITH permet de définir un ou plusieurs blocs de sous-requêtes nommés avant le SELECT principal. Elle se place tout au début de l\'instruction SQL (avant le premier SELECT).',
      explanation: 'Ces blocs nommés agissent comme des tables temporaires en mémoire, réutilisables à volonté dans la suite de la requête.',
      codeSnippet: 'WITH dept_costs AS (\n  SELECT department_id, SUM(salary) AS total_sal\n  FROM employees\n  GROUP BY department_id\n)\nSELECT department_id, total_sal\nFROM dept_costs\nWHERE total_sal > 50000;',
      examTrap: 'Le mot-clé WITH ne peut apparaître qu\'UNE SEULE FOIS au début de la requête, même si on déclare plusieurs sous-requêtes.',
      ruleRef: 'Oracle SQL Reference - The WITH Clause (Subquery Factoring)',
    },
  },
  {
    id: 'fc-dom05-088',
    cardNumber: 88,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Réutilisation multiple d\'une même CTE',
    difficulty: 'medium',
    tags: ['Réutilisation', 'Matérialisation', 'Performance', 'Avantage CTE'],
    front: {
      question: 'Quel est le double avantage (lisibilité et performance) de la clause WITH lorsqu\'une sous-requête doit être consultée plusieurs fois dans une même requête ?',
      hint: 'Oracle doit-il ré-exécuter la requête à chaque fois ?',
    },
    back: {
      answer: '1. Lisibilité : elle évite de dupliquer le code SQL complexe à plusieurs endroits.\n2. Performance : l\'optimiseur Oracle peut matérialiser le résultat dans une table temporaire globale en mémoire (ou TEMP) pour ne l\'exécuter qu\'une seule fois et réutiliser le résultat stocké.',
      explanation: 'L\'optimiseur choisit automatiquement entre « INLINE » (substitution directe) et « MATERIALIZE » (calcul unique stocké).',
      codeSnippet: 'WITH avg_sal AS (\n  SELECT AVG(salary) AS val FROM employees\n)\nSELECT last_name, salary\nFROM employees, avg_sal\nWHERE salary > avg_sal.val;',
      examTrap: 'Avec des sous-requêtes dans le FROM traditionnelles, le code devait être recopié textuellement à chaque utilisation.',
      ruleRef: 'Oracle SQL Performance Tuning - WITH Clause Materialization',
    },
  },
  {
    id: 'fc-dom05-089',
    cardNumber: 89,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Déclaration de plusieurs CTEs consécutives',
    difficulty: 'easy',
    tags: ['Multiples CTEs', 'Virgule', 'Syntaxe', 'Pas de WITH répété'],
    front: {
      question: 'Quelle est la syntaxe exacte pour déclarer plusieurs sous-requêtes dans une seule clause WITH ? Doit-on répéter le mot-clé WITH ?',
      codeSnippet: 'WITH cte1 AS (SELECT ...), cte2 AS (SELECT ...)\nSELECT ...',
      hint: 'Séparez les blocs par une virgule sans répéter le mot WITH.',
    },
    back: {
      answer: 'On sépare simplement chaque nom de sous-requête et sa définition par une VIRGULE (,). Il ne faut SURTOUT PAS répéter le mot-clé WITH.',
      explanation: 'Répéter le mot WITH pour la deuxième CTE déclenche immédiatement l\'erreur ORA-00928 : missing SELECT keyword.',
      codeSnippet: 'WITH\n  dept_avg AS (\n    SELECT department_id, AVG(salary) AS avg_s FROM employees GROUP BY department_id\n  ),\n  company_avg AS (\n    SELECT AVG(salary) AS comp_s FROM employees\n  )\nSELECT d.department_id, d.avg_s, c.comp_s\nFROM dept_avg d CROSS JOIN company_avg c\nWHERE d.avg_s > c.comp_s;',
      examTrap: 'Piège d\'examen : une proposition de réponse réécrivant « WITH cte1 AS (...), WITH cte2 AS (...) » est syntaxiquement FAUSSE.',
      ruleRef: 'Oracle SQL Reference - Declaring Multiple Query Names in WITH',
    },
  },
  {
    id: 'fc-dom05-090',
    cardNumber: 90,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Règle d\'ordre de dépendance entre les CTEs',
    difficulty: 'medium',
    tags: ['Ordre de dépendance', 'Visibilité séquentielle', 'ORA-00942', 'Règle stricte'],
    front: {
      question: 'Une CTE peut-elle référencer une autre CTE déclarée dans la même clause WITH ? Existe-t-il une règle d\'ordonnancement obligatoire ?',
      hint: 'Une CTE peut-elle référencer une CTE qui est définie APRÈS elle ?',
    },
    back: {
      answer: 'OUI, une CTE peut référencer une autre CTE, mais UNIQUEMENT si celle-ci a été déclarée AVANT elle (visibilité séquentielle de haut en bas).',
      explanation: 'Tenter de référencer une CTE définie plus bas dans le script provoque l\'erreur ORA-00942 : table or view does not exist car le nom n\'a pas encore été analysé.',
      codeSnippet: 'WITH\n  cte_a AS (SELECT department_id, COUNT(*) AS cnt FROM employees GROUP BY department_id),\n  cte_b AS (SELECT * FROM cte_a WHERE cnt > 5) -- Valide : cte_a est déclarée avant !\nSELECT * FROM cte_b;',
      examTrap: 'L\'ordre de déclaration dans la clause WITH est strictement contraignant.',
      ruleRef: 'Oracle Database SQL Language Reference - CTE Reference Order',
    },
  },
  {
    id: 'fc-dom05-091',
    cardNumber: 91,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Nommage explicite des colonnes dans la signature de la CTE',
    difficulty: 'medium',
    tags: ['Signature de colonnes', 'Alias explicite', 'Syntaxe propre', 'Oracle 11g+'],
    front: {
      question: 'Quelle syntaxe élégante permet de nommer les colonnes d\'une CTE directement dans sa signature sans devoir utiliser « AS alias » à l\'intérieur du SELECT ?',
      codeSnippet: 'WITH dept_summary (id, nbr_emp, masse_sal) AS (\n  SELECT department_id, COUNT(*), SUM(salary)\n  FROM employees\n  GROUP BY department_id\n)\nSELECT id, nbr_emp, masse_sal FROM dept_summary;',
      hint: 'Nommer les colonnes entre parenthèses juste après le nom de la CTE.',
    },
    back: {
      answer: 'La liste explicite des noms de colonnes entre parenthèses immédiatement après le nom de la CTE : « WITH nom_cte (col1, col2, ...) AS (...) ».',
      explanation: 'Cette syntaxe positionnelle assigne les noms col1, col2 aux colonnes projetées dans l\'ordre exact du SELECT, rendant les alias internes facultatifs.',
      codeSnippet: '-- Valide et très propre pour renommer des colonnes calculées en une ligne.',
      examTrap: 'Le nombre de colonnes spécifié dans la signature doit correspondre EXACTEMENT au nombre d\'expressions projetées par le SELECT interne, sinon ORA-32038 est levée.',
      ruleRef: 'Oracle SQL Reference - Column Aliasing in Subquery Factoring',
    },
  },
  {
    id: 'fc-dom05-092',
    cardNumber: 92,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Clause WITH récursive (Recursive CTE)',
    difficulty: 'hard',
    tags: ['Recursive CTE', 'UNION ALL', 'Hiérarchie', 'Oracle 11gR2+'],
    front: {
      question: 'Comment est structurée une CTE récursive sous Oracle (utilisée pour parcourir des structures en arbre comme alternative à CONNECT BY) ?',
      hint: 'Membre ancre + UNION ALL + membre récursif.',
    },
    back: {
      answer: 'Elle est composée de deux parties reliées par UNION ALL :\n1. Le membre ancré (anchor member) : la requête de base qui sélectionne les nœuds racines.\n2. Le membre récursif (recursive member) : la requête qui référence le nom de la CTE elle-même pour joindre les enfants aux parents.',
      explanation: 'Oracle répète l\'évaluation du membre récursif jusqu\'à ce qu\'aucune nouvelle ligne ne soit produite.',
      codeSnippet: 'WITH emp_hierarchy (emp_id, mgr_id, lvl) AS (\n  -- Ancre :\n  SELECT employee_id, manager_id, 1 FROM employees WHERE manager_id IS NULL\n  UNION ALL\n  -- Récursif :\n  SELECT e.employee_id, e.manager_id, h.lvl + 1\n  FROM employees e JOIN emp_hierarchy h ON (e.manager_id = h.emp_id)\n)\nSELECT * FROM emp_hierarchy;',
      examTrap: 'L\'opérateur d\'ensemble entre l\'ancre et la partie récursive DOIT impérativement être « UNION ALL » (et non UNION, INTERSECT ou MINUS).',
      ruleRef: 'Oracle Database SQL Language Reference - Recursive Subquery Factoring',
    },
  },
  {
    id: 'fc-dom05-093',
    cardNumber: 93,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Portée de vie d\'une CTE définie par WITH',
    difficulty: 'easy',
    tags: ['Portée de vie', 'Durabilité', 'Instruction unique', 'Pas de persistance'],
    front: {
      question: 'Quelle est la durée de vie d\'une CTE créée par une clause WITH ? Peut-elle être interrogée par la requête suivante dans une session SQL ?',
      hint: 'Persiste-t-elle après le point-virgule final ?',
    },
    back: {
      answer: 'Sa durée de vie est strictement limitée à la SEULE instruction SQL dans laquelle elle est déclarée. Elle est détruite dès la fin de l\'exécution de la requête.',
      explanation: 'Elle ne peut pas être interrogée par une instruction ultérieure. Si vous avez besoin de persistance multi-requêtes, il faut créer une vue permanente (CREATE VIEW) ou une table temporaire globale (GTT).',
      codeSnippet: '-- Requête 1 :\nWITH my_cte AS (SELECT * FROM employees) SELECT COUNT(*) FROM my_cte;\n\n-- Requête 2 (immédiatement après) :\nSELECT * FROM my_cte; -- ORA-00942: table or view does not exist !',
      examTrap: 'Ne confondez pas une CTE avec une table temporaire : une CTE n\'a aucune persistance au-delà du point-virgule de son instruction.',
      ruleRef: 'Oracle SQL Reference - Scope of Subquery Factoring',
    },
  },

  // =========================================================================
  // SECTION 9 : OPÉRATEURS D'ENSEMBLE (SET OPERATORS) & RÈGLES D'OR (94 à 100)
  // =========================================================================
  {
    id: 'fc-dom05-094',
    cardNumber: 94,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Les 4 opérateurs d\'ensemble en SQL Oracle',
    difficulty: 'easy',
    tags: ['UNION', 'UNION ALL', 'INTERSECT', 'MINUS', 'Les 4 opérateurs'],
    front: {
      question: 'Quels sont les 4 opérateurs d\'ensemble (Set Operators) disponibles en SQL Oracle et quel est leur rôle respectif ?',
      hint: 'Union avec dédoublonnage, union brute, intersection, différence.',
    },
    back: {
      answer: '1. UNION : fusionne deux ensembles et élimine automatiquement les doublons.\n2. UNION ALL : fusionne deux ensembles en conservant TOUS les doublons.\n3. INTERSECT : ne retourne que les lignes communes présentes dans les DEUX ensembles (sans doublons).\n4. MINUS : retourne les lignes du premier ensemble qui sont ABSENTES du second ensemble (sans doublons).',
      explanation: 'Ces opérateurs combinent les flux verticaux de plusieurs requêtes SELECT en un seul jeu de résultats.',
      codeSnippet: 'SELECT employee_id FROM employees\nUNION\nSELECT employee_id FROM job_history;',
      examTrap: 'L\'opérateur de différence s\'appelle « MINUS » en Oracle (contrairement au standard ANSI pur qui utilise « EXCEPT »).',
      ruleRef: 'Oracle SQL Reference - The Set Operators',
    },
  },
  {
    id: 'fc-dom05-095',
    cardNumber: 95,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Règle d\'or #1 : Nombre de colonnes et compatibilité des types',
    difficulty: 'medium',
    tags: ['Règle d\'or', 'Nombre de colonnes', 'Types compatibles', 'Erreurs ORA'],
    front: {
      question: 'Quelles sont les deux conditions strictes que doivent respecter les listes de sélection (SELECT) de deux requêtes combinées par un opérateur d\'ensemble ?',
      hint: 'Nombre de colonnes et types de données associés.',
    },
    back: {
      answer: '1. Même NOMBRE de colonnes dans chaque SELECT (sinon ORA-01789: query block has incorrect number of result columns).\n2. Les types de données des colonnes correspondantes doivent appartenir à la même famille compatible par position (sinon ORA-01790: expression must have same datatype as corresponding expression).',
      explanation: 'Les noms des colonnes n\'ont aucune importance : la correspondance se fait STRICTEMENT PAR POSITION.',
      codeSnippet: '-- Valide : 2 colonnes NUMBER et VARCHAR2 des deux côtés\nSELECT employee_id, last_name FROM employees\nUNION\nSELECT manager_id, department_name FROM departments;',
      examTrap: 'Les noms de colonnes peuvent être totalement différents ! Les libellés d\'en-tête affichés seront TOUJOURS ceux de la PREMIÈRE requête SELECT.',
      ruleRef: 'Oracle SQL Reference - Guidelines for Set Operators',
    },
  },
  {
    id: 'fc-dom05-096',
    cardNumber: 96,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Règle d\'or #2 : Emplacement et syntaxe de la clause ORDER BY',
    difficulty: 'medium',
    tags: ['ORDER BY', 'Règle d\'or', 'Fin de requête', 'ORA-00933'],
    front: {
      question: 'Où doit impérativement se situer la clause ORDER BY dans une requête contenant des opérateurs d\'ensemble, et sur quels noms de colonnes peut-elle trier ?',
      codeSnippet: 'SELECT employee_id AS emp_code, salary FROM employees\nUNION\nSELECT id AS identifiant, remuneration FROM archive_emp\nORDER BY ... ;',
      hint: 'Peut-on mettre un ORDER BY dans la première sous-requête ? Quel nom de colonne est reconnu ?',
    },
    back: {
      answer: 'La clause ORDER BY peut UNIQUEMENT figurer à la TOUTE FIN de la dernière requête SELECT. Elle ne peut référencer que les noms de colonnes ou alias définis dans le PREMIER SELECT (ou des numéros de colonnes par position 1, 2, ...).',
      explanation: 'Mettre un ORDER BY dans l\'un des SELECT intermédiaires provoque ORA-00933: SQL command not properly ended.',
      codeSnippet: '-- Valide à la fin :\nSELECT employee_id AS emp_code, salary FROM employees\nUNION ALL\nSELECT id, remuneration FROM archive_emp\nORDER BY emp_code DESC; -- Ou : ORDER BY 1 DESC;',
      examTrap: 'Tenter de trier par « ORDER BY identifiant » (nom de la seconde requête) échoue avec ORA-00904 : invalid identifier.',
      ruleRef: 'Oracle SQL Reference - The ORDER BY Clause in Set Operations',
    },
  },
  {
    id: 'fc-dom05-097',
    cardNumber: 97,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Différence essentielle : UNION vs UNION ALL',
    difficulty: 'easy',
    tags: ['UNION vs UNION ALL', 'Dédoublonnage', 'Tri implicite', 'Performance'],
    front: {
      question: 'Quelles sont les deux différences majeures entre UNION et UNION ALL en termes de résultat et de coût de calcul ?',
      hint: 'Traitement des doublons et opération de tri en mémoire.',
    },
    back: {
      answer: '1. Résultat : UNION élimine automatiquement les doublons pour ne retourner que des lignes distinctes. UNION ALL conserve TOUTES les lignes, y compris les doublons identiques.\n2. Performance : UNION effectue un tri en mémoire (ou hash unique) pour dédoublonner, ce qui est coûteux. UNION ALL concatène directement les flux sans aucun tri et s\'exécute beaucoup plus rapidement.',
      explanation: 'Si vous savez qu\'il n\'y a pas de doublons ou si vous souhaitez les conserver, UNION ALL est toujours préférable pour les performances.',
      codeSnippet: 'SELECT department_id FROM employees -- 107 lignes\nUNION ALL\nSELECT department_id FROM employees; -- 107 lignes\n-- UNION ALL renvoie 214 lignes.\n-- UNION renvoie seulement ~12 lignes distinctes.',
      examTrap: 'Par défaut, UNION trie le résultat par ordre croissant des colonnes de gauche à droite. UNION ALL ne trie rien.',
      ruleRef: 'Oracle Performance Tuning Guide - UNION vs UNION ALL',
    },
  },
  {
    id: 'fc-dom05-098',
    cardNumber: 98,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'L\'opérateur INTERSECT',
    difficulty: 'medium',
    tags: ['INTERSECT', 'Intersection', 'Lignes communes', 'Dédoublonnage'],
    front: {
      question: 'Que retourne l\'opérateur INTERSECT et comment gère-t-il les doublons et les valeurs NULL ?',
      codeSnippet: 'SELECT job_id FROM employees\nINTERSECT\nSELECT job_id FROM job_history;',
      hint: 'Seules les lignes présentes dans les deux ensembles sont conservées.',
    },
    back: {
      answer: 'INTERSECT retourne uniquement les lignes distinctes présentes SIMULTANÉMENT dans les deux requêtes. Il élimine tous les doublons et trie implicitement le résultat.',
      explanation: 'Deux lignes contenant NULL dans les mêmes colonnes sont considérées comme identiques par les opérateurs d\'ensemble (contrairement aux jointures ordinaires où NULL = NULL donne UNKNOWN).',
      codeSnippet: '-- Affiche la liste unique des postes (job_id) qui existent à la fois dans EMPLOYEES et dans JOB_HISTORY.',
      examTrap: 'Si une ligne apparaît 3 fois dans la table A et 2 fois dans la table B, INTERSECT ne la renvoie qu\'UNE SEULE FOIS.',
      ruleRef: 'Oracle SQL Reference - The INTERSECT Operator',
    },
  },
  {
    id: 'fc-dom05-099',
    cardNumber: 99,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'L\'opérateur MINUS et sensibilité à l\'ordre des opérandes',
    difficulty: 'medium',
    tags: ['MINUS', 'Différence', 'Ordre non commutatif', 'Piège classique'],
    front: {
      question: 'L\'opérateur MINUS est-il commutatif ? La requête « A MINUS B » produit-elle toujours le même résultat que « B MINUS A » ?',
      hint: 'En mathématiques, 5 - 3 est-il égal à 3 - 5 ?',
    },
    back: {
      answer: 'NON, MINUS n\'est PAS commutatif ! « A MINUS B » retourne les lignes de A absentes de B. « B MINUS A » retourne les lignes de B absentes de A.',
      explanation: 'L\'ordre des requêtes est capital : la première requête détermine le réservoir de lignes candidates initiales, et la seconde agit comme un filtre d\'exclusion.',
      codeSnippet: '-- Affiche les employés qui n\'ont jamais changé de poste dans le passé :\nSELECT employee_id FROM employees\nMINUS\nSELECT employee_id FROM job_history;\n\n-- La requête inverse afficherait les anciens employés du job_history qui ne sont plus dans l\'entreprise !',
      examTrap: 'UNION et INTERSECT sont commutatifs (A UNION B = B UNION A), mais MINUS ne l\'est JAMAIS.',
      ruleRef: 'Oracle SQL Reference - The MINUS Operator Non-Commutativity',
    },
  },
  {
    id: 'fc-dom05-100',
    cardNumber: 100,
    domainId: 'oracle-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sous-requêtes simples, corrélées & Opérateurs d\'ensemble',
    subtopic: 'Priorité d\'évaluation et types de colonnes interdits dans les opérateurs d\'ensemble',
    difficulty: 'hard',
    tags: ['Priorité', 'De gauche à droite', 'Parenthèses', 'Colonnes LOB interdites'],
    front: {
      question: 'Dans une chaîne d\'opérateurs d\'ensemble sans parenthèses (ex: SELECT1 UNION SELECT2 INTERSECT SELECT3), quel est l\'ordre de priorité ? Quels types de données de colonnes sont strictement interdits ?',
      hint: 'Tous les opérateurs d\'ensemble ont-ils la même priorité en Oracle ?',
    },
    back: {
      answer: '1. Ordre d\'évaluation : Tous les opérateurs d\'ensemble ont la MÊME PRIORITÉ. Ils sont évalués strictement de GAUCHE À DROITE (de haut en bas), sauf si des parenthèses forcent un ordre différent.\n2. Types interdits : Les types LOB (BLOB, CLOB, NCLOB), LONG, BFILE et les collections (VARRAY, Nested Tables) sont INTERDITS dans les opérations avec dédoublonnage (UNION, INTERSECT, MINUS).',
      explanation: 'Pour exécuter l\'INTERSECT avant le UNION, il faut impérativement encadrer le bloc concerné par des parenthèses (...).',
      codeSnippet: '-- Évaluation de haut en bas :\nSELECT 1 FROM dual\nUNION\nSELECT 2 FROM dual\nINTERSECT\nSELECT 2 FROM dual;\n-- Calcule d\'abord (1 UNION 2) = {1, 2}, puis {1, 2} INTERSECT {2} = {2}.',
      examTrap: 'Ne supposez pas qu\'INTERSECT a une priorité plus élevée que UNION (contrairement à la multiplication vs addition en arithmétique) : en SQL Oracle, ils ont la même priorité !',
      ruleRef: 'Oracle SQL Reference - Set Operators Precedence and Datatype Restrictions',
    },
  },
];
