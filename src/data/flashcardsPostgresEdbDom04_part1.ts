import { FlashcardItem } from '../types';

export const postgresEdbDom04Part1Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 1 : ARCHITECTURE MVCC & CYCLE DE VIE DES TUPLES (Cartes 1 à 10)
  // xmin, xmax, t_infomask, dead tuples, UPDATE = DELETE + INSERT, ctid
  // =========================================================================
  {
    id: 'fc-pgedb-dom04-001',
    cardNumber: 1,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Principe fondamental de MVCC (Multi-Version Concurrency Control)',
    difficulty: 'easy',
    tags: ['MVCC', 'Architecture', 'Concurrency', 'Snapshots'],
    front: {
      question: 'Quel est le principe fondamental du modèle MVCC (Multi-Version Concurrency Control) dans PostgreSQL ?',
      hint: 'Pensez à la relation entre lecteurs et rédacteurs.',
    },
    back: {
      answer: 'Le principe fondamental de MVCC est : **"Les lecteurs ne bloquent jamais les rédacteurs, et les rédacteurs ne bloquent jamais les lecteurs"** (Readers never block writers, and writers never block readers).\n\nLorsqu\'une ligne est modifiée (`UPDATE`) ou supprimée (`DELETE`), PostgreSQL ne modifie pas la donnée en place sur le disque. Il crée une nouvelle version du tuple (row version) ou marque l\'ancienne comme expirée. Chaque transaction voit un instantané cohérent (**Snapshot**) de la base.',
      explanation: 'Cette architecture élimine le besoin de verrous partagés en lecture sur les lignes, garantissant un débit transactionnel élevé en environnement concurrent.',
      examTrap: 'MVCC évite les verrous entre lectures et écritures, mais deux transactions qui tentent d\'écrire (`UPDATE`/`DELETE`) sur la même ligne se bloquent mutuellement par verrou exclusif de ligne.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 13: Concurrency Control',
    },
  },
  {
    id: 'fc-pgedb-dom04-002',
    cardNumber: 2,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Champs d\'en-tête de tuple : `xmin` et `xmax`',
    difficulty: 'medium',
    tags: ['MVCC', 'xmin', 'xmax', 'Tuple Header', 'Internals'],
    front: {
      question: 'À quoi servent les pseudo-colonnes d\'en-tête de tuple `xmin` et `xmax` dans PostgreSQL ?',
      codeSnippet: 'SELECT xmin, xmax, ctid, id, nom FROM clients LIMIT 3;',
      hint: 'Identifiants de transactions de création et de suppression/remplacement.',
    },
    back: {
      answer: 'Chaque tuple PostgreSQL possède un en-tête (HeapTupleHeader) contenant :\n- `xmin` : L\'identifiant de la transaction (XID) qui a inséré cette version du tuple.\n- `xmax` : L\'identifiant de la transaction (XID) qui a supprimé ou remplacé cette version de tuple (vaut `0` si le tuple est actif et non supprimé/verrouillé).\n\nEn comparant ces XID avec l\'instantané (`Snapshot`) de la transaction courante, PostgreSQL détermine si ce tuple est visible ou non.',
      explanation: '`xmax` peut également stocker temporairement l\'identifiant d\'une transaction ayant posé un verrou explicite (`SELECT ... FOR UPDATE / FOR SHARE`).',
      examTrap: '`xmin` et `xmax` sont des colonnes système cachées. Elles ne sont pas affichées par un `SELECT *`, il faut les nommer explicitement.',
      ruleRef: 'PostgreSQL 16 Documentation - Section 5.5: System Columns',
    },
  },
  {
    id: 'fc-pgedb-dom04-003',
    cardNumber: 3,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Mécanisme d\'un `UPDATE` sous MVCC',
    difficulty: 'medium',
    tags: ['MVCC', 'UPDATE', 'Dead Tuples', 'HOT'],
    front: {
      question: 'Comment un `UPDATE` est-il exécuté physiquement sous le moteur MVCC de PostgreSQL ?',
      codeSnippet: 'UPDATE comptes SET solde = solde + 100 WHERE id = 42;',
      hint: 'Combinaison de deux opérations fondamentales.',
    },
    back: {
      answer: 'Physiquement, un `UPDATE` correspond à une suppression logique suivie d\'une insertion :\n1. Le tuple existant reste intact sur le disque, mais son champ `xmax` est mis à jour avec le XID de la transaction courante (il devient un candidat "mort" après commit).\n2. Un **nouveau tuple complet** est inséré dans un bloc de données avec son `xmin` égal au XID courant et son `xmax = 0`.\n3. L\'ancien tuple pointe vers le nouveau tuple via son pointeur d\'en-tête `t_ctid`.',
      explanation: 'Un volume important d\'`UPDATE` génère donc autant de tuples obsolètes qu\'un volume équivalent de `DELETE`.',
      examTrap: 'L\'`UPDATE` n\'écrase JAMAIS l\'ancien tuple sur le disque, sauf optimisation HOT (Heap-Only Tuple) qui évite seulement la mise à jour d\'index.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 65: Database Physical Storage',
    },
  },
  {
    id: 'fc-pgedb-dom04-004',
    cardNumber: 4,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Définition d\'un "Dead Tuple" (Tuple mort)',
    difficulty: 'easy',
    tags: ['MVCC', 'Dead Tuples', 'Garbage Collection', 'VACUUM'],
    front: {
      question: 'Qu\'est-ce qu\'un "Dead Tuple" (tuple mort) dans une table PostgreSQL ?',
      hint: 'Tuple qui n\'est plus visible par aucune transaction active ou future.',
    },
    back: {
      answer: 'Un **Dead Tuple** est une version de ligne qui n\'est plus visible par **aucune** transaction courante ou future :\n- Soit la ligne a été supprimée (`DELETE`) et la transaction a été validée (`COMMIT`).\n- Soit la ligne a été modifiée (`UPDATE`) et remplacée par une nouvelle version validée.\n- Soit la transaction ayant inséré le tuple a été annulée (`ROLLBACK`).\n\nCe tuple mort occupe toujours de l\'espace disque dans le fichier de table et dans les index associés jusqu\'à son nettoyage par le processus de **VACUUM**.',
      explanation: 'Si une transaction ancienne reste ouverte (long-running transaction), elle empêche la conversion des tuples obsolètes en tuples morts récupérables.',
      examTrap: 'Un tuple modifié par un `ROLLBACK` n\'est pas effacé immédiatement du disque : il devient un tuple mort dès le rollback et attend le passage du VACUUM.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming',
    },
  },
  {
    id: 'fc-pgedb-dom04-005',
    cardNumber: 5,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Identifiant physique `ctid` (Page et Offset)',
    difficulty: 'medium',
    tags: ['ctid', 'Storage', 'Tuple Header', 'Page'],
    front: {
      question: 'Que représente la pseudo-colonne `ctid` d\'un tuple et comment se lit sa valeur `(0, 4)` ?',
      codeSnippet: 'SELECT ctid, id FROM articles LIMIT 1;\n-- Résultat : ctid = \'(0, 4)\'',
      hint: 'Numéro de page de 8 Ko et indice de pointeur dans la page.',
    },
    back: {
      answer: 'Le `ctid` (Current Tuple ID) représente l\'**emplacement physique exact** du tuple au sein du fichier de table :\n- Le premier nombre (`0`) est le numéro de page/bloc de 8 Ko dans le fichier (0-indexé).\n- Le second nombre (`4`) est l\'indice du pointeur d\'élément (ItemPointer/Line Pointer) dans cette page.\n\n`(0, 4)` indique que le tuple est référencé par le 4e pointeur de la première page.',
      explanation: 'Contrairement à une clé primaire, la valeur de `ctid` n\'est pas immuable : elle change lors d\'un `UPDATE` classique ou lors d\'un `VACUUM FULL`.',
      examTrap: 'Ne jamais utiliser `ctid` comme clé primaire applicative car sa valeur change dès qu\'un tuple est déplacé ou mis à jour.',
      ruleRef: 'PostgreSQL 16 Documentation - System Columns - ctid',
    },
  },
  {
    id: 'fc-pgedb-dom04-006',
    cardNumber: 6,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Optimisation HOT (Heap-Only Tuples)',
    difficulty: 'hard',
    tags: ['HOT', 'Performance', 'UPDATE', 'Index Bloat'],
    front: {
      question: 'Qu\'est-ce que l\'optimisation HOT (Heap-Only Tuples) et à quelles deux conditions strictes peut-elle se déclencher lors d\'un `UPDATE` ?',
      hint: 'Une question de colonnes indexées et d\'espace disponible dans le bloc.',
    },
    back: {
      answer: 'L\'optimisation **HOT** permet à un `UPDATE` d\'insérer le nouveau tuple dans le tas (Heap) sans créer de nouvelles entrées dans les index, réduisant drastiquement le bloat des index et l\'I/O.\n\nLes 2 conditions obligatoires sont :\n1. **Aucune colonne indexée** n\'est modifiée par l\'`UPDATE`.\n2. La **même page physique (bloc de 8 Ko)** que l\'ancien tuple dispose d\'assez d\'espace libre pour accueillir la nouvelle version de la ligne.',
      explanation: 'L\'index continue de pointer vers l\'ancien tuple (racine), et un chaînage interne dans la page redirige le parcours vers le nouveau tuple HOT.',
      examTrap: 'Si le paramètre `fillfactor` d\'une table est à 100% (défaut), HOT ne peut fonctionner que si de l\'espace a déjà été libéré dans la page.',
      ruleRef: 'PostgreSQL 16 Documentation - Readme HOT (Heap-Only Tuples)',
    },
  },
  {
    id: 'fc-pgedb-dom04-007',
    cardNumber: 7,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Paramètre de stockage `fillfactor` et HOT',
    difficulty: 'medium',
    tags: ['fillfactor', 'Storage Parameters', 'HOT', 'Performance'],
    front: {
      question: 'Pourquoi et comment configure-t-on le paramètre de stockage `fillfactor` pour favoriser les mises à jour HOT ?',
      codeSnippet: 'ALTER TABLE historique_sessions SET (fillfactor = 80);',
      hint: 'Pourcentage de remplissage initial d\'une page de données.',
    },
    back: {
      answer: 'Le `fillfactor` (entre 10 et 100, valeur par défaut = 100 pour les tables) indique le pourcentage maximal de remplissage d\'une page de 8 Ko lors d\'un `INSERT`.\n\nEn réduisant le `fillfactor` (ex: `80`), PostgreSQL réserve 20% d\'espace vide dans chaque page. Cet espace libre permet aux futurs `UPDATE` de stocker le nouveau tuple dans la même page, maximisant le taux de réussite des mises à jour HOT.',
      explanation: 'Après modification du `fillfactor`, les nouvelles pages respectent la règle, mais pour réorganiser les pages existantes, un `VACUUM FULL` ou `CLUSTER` est nécessaire.',
      examTrap: 'Réduire le `fillfactor` augmente la taille physique globale de la table sur disque pour les tables en insertion pure (`INSERT-only`). Il n\'est utile que pour les tables sujettes à de fréquents `UPDATE`.',
      ruleRef: 'PostgreSQL 16 Documentation - Storage Parameters - fillfactor',
    },
  },
  {
    id: 'fc-pgedb-dom04-008',
    cardNumber: 8,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Bits d\'information d\'en-tête : `t_infomask` et validation de Commit',
    difficulty: 'hard',
    tags: ['t_infomask', 'Internals', 'Commit Log', 'pg_xact'],
    front: {
      question: 'Pourquoi PostgreSQL utilise-t-il les flags d\'état dans `t_infomask` (ex: `HEAP_XMIN_COMMITTED`) plutôt que de consulter systématiquement le journal des transactions (`pg_xact`) ?',
      hint: 'Optimisation de performance pour éviter la contention sur le statut des transactions.',
    },
    back: {
      answer: 'Pour déterminer la visibilité d\'un tuple, PostgreSQL devrait vérifier dans `pg_xact` (anciennement `pg_clog`) si la transaction `xmin` a été commitée. Cette lecture répétée créerait un goulet d\'étranglement mémoire/disque massif.\n\nPour éviter cela, la première transaction qui lit le tuple consulte `pg_xact` puis positionne un drapeau (hint bit) directement dans l\'en-tête du tuple (`t_infomask`), tel que `HEAP_XMIN_COMMITTED` ou `HEAP_XMIN_ABORTED`. Les transactions suivantes lisent ce drapeau immédiatement sans consulter `pg_xact`.',
      explanation: 'Cette écriture de hint bits peut générer des écritures de pages disque "sales" (dirty buffers) même lors de simples requêtes `SELECT` exécutées pour la première fois.',
      examTrap: 'Un simple `SELECT` peut provoquer l\'écriture de pages sur disque en arrière-plan à cause de la mise à jour des hint bits dans `t_infomask`.',
      ruleRef: 'PostgreSQL Source Code - src/include/access/htup_details.h',
    },
  },
  {
    id: 'fc-pgedb-dom04-009',
    cardNumber: 9,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Colonnes `cmin` et `cmax` (Command ID)',
    difficulty: 'medium',
    tags: ['cmin', 'cmax', 'Command ID', 'Internals'],
    front: {
      question: 'À quoi servent les pseudo-colonnes `cmin` et `cmax` dans l\'en-tête d\'un tuple ?',
      hint: 'Visibilité des modifications à l\'intérieur d\'une même transaction multi-instructions.',
    },
    back: {
      answer: '`cmin` et `cmax` représentent l\'identifiant de commande séquentiel (**Command ID**) au sein d\'une **même transaction** :\n- `cmin` : Numéro d\'ordre de la commande SQL de la transaction ayant créé le tuple.\n- `cmax` : Numéro d\'ordre de la commande SQL ayant détruit ou mis à jour le tuple.\n\nIls permettent à une transaction de voir ses propres modifications successives (par exemple, si une instruction 1 insère une ligne, l\'instruction 2 de la même transaction peut la voir et la modifier).',
      explanation: 'Dans l\'en-tête physique, `cmin` et `cmax` partagent le même espace mémoire sous forme d\'un champ union (`t_cid`), car un tuple n\'est que très rarement créé et détruit au sein de la même commande.',
      examTrap: '`cmin`/`cmax` ne sont pertinents qu\'au sein de la transaction locale. Pour les transactions externes, seuls `xmin`/`xmax` déterminent la visibilité.',
      ruleRef: 'PostgreSQL 16 Documentation - System Columns - cmin/cmax',
    },
  },
  {
    id: 'fc-pgedb-dom04-010',
    cardNumber: 10,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Conséquence du non-nettoyage des Dead Tuples',
    difficulty: 'easy',
    tags: ['Bloat', 'Dead Tuples', 'Performance Degradation', 'Disaster'],
    front: {
      question: 'Quelles sont les trois conséquences majeures pour une base de données PostgreSQL si le processus de VACUUM ne nettoie jamais les dead tuples ?',
      hint: 'Espace disque, mémoire partagée, et risque d\'arrêt total.',
    },
    back: {
      answer: 'Les trois conséquences majeures sont :\n1. **Fragmentation et gonflement (Table & Index Bloat)** : Consommation exponentielle d\'espace disque inutile.\n2. **Dégradation sévère des performances de lecture** : Les scans séquentiels et les parcours d\'index doivent charger en mémoire (`shared_buffers`) des millions de pages contenant des données mortes non visibles.\n3. **Blocage fatal par Wraparound des XID** : La base de données s\'arrête et refuse toute nouvelle écriture (`ERROR: database is not accepting commands`) lorsque la limite des 2 milliards de transactions approche.',
      explanation: 'Le VACUUM n\'est pas une tâche d\'optimisation optionnelle, mais le mécanisme vital de survie du moteur PostgreSQL.',
      examTrap: 'Même si aucun utilisateur ne se plaint de lenteur, l\'absence de VACUUM conduira inévitablement à un arrêt forcé de l\'instance pour prévenir la corruption des données par wraparound.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - Table Bloat',
    },
  },

  // =========================================================================
  // SECTION 2 : BLOAT, FSM (FREE SPACE MAP) & VISIBILITY MAP (VM) (Cartes 11 à 20)
  // =========================================================================
  {
    id: 'fc-pgedb-dom04-011',
    cardNumber: 11,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Concept de "Bloat" (Gonflement)',
    difficulty: 'easy',
    tags: ['Bloat', 'Storage', 'Dead Tuples', 'Concepts'],
    front: {
      question: 'Comment définit-on précisément le "Bloat" (gonflement) d\'une table ou d\'un index dans PostgreSQL ?',
      hint: 'Écart entre l\'espace alloué sur disque et l\'espace réellement nécessaire pour les données vivantes.',
    },
    back: {
      answer: 'Le **Bloat** correspond à l\'espace disque alloué et occupé par les fichiers de données (table heap ou index) qui n\'héberge que des **dead tuples** ou de l\'espace inutilisable.\n\nIl représente le gaspillage d\'espace physique par rapport à la taille minimale requise pour stocker uniquement les lignes actives (live tuples). Un bloat de 70% signifie que sur 10 Go de table, seuls 3 Go contiennent des données actives et 7 Go sont du vide ou des lignes obsolètes.',
      explanation: 'Le bloat dégrade le cache `shared_buffers` en polluant la RAM avec des blocs ne contenant presque aucune donnée utile.',
      examTrap: 'Supprimer 90% des lignes d\'une table via `DELETE` ne réduit PAS la taille du fichier sur le disque : cela crée 90% de bloat.',
      ruleRef: 'EDB Postgres Guide - Understanding Table and Index Bloat',
    },
  },
  {
    id: 'fc-pgedb-dom04-012',
    cardNumber: 12,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Fichier FSM (Free Space Map) : Extension `_fsm`',
    difficulty: 'medium',
    tags: ['FSM', 'Free Space Map', 'Storage', 'Internals'],
    front: {
      question: 'Quel est le rôle du fichier Free Space Map (`_fsm`) associé à chaque table et index ?',
      codeSnippet: '# Dans le répertoire $PGDATA/base/<db_oid>/\n16384        # Fichier principal (Heap)\n16384_fsm    # Free Space Map\n16384_vm     # Visibility Map',
      hint: 'Où PostgreSQL cherche-t-il de la place lors d\'un INSERT ?',
    },
    back: {
      answer: 'Le fichier **Free Space Map (`_fsm`)** maintient une arborescence binaire compacte indiquant la quantité d\'**espace libre disponible dans chaque page de 8 Ko** de la table.\n\nLors d\'une opération `INSERT` ou `UPDATE`, PostgreSQL consulte le `_fsm` pour trouver rapidement une page existante capable d\'accueillir le nouveau tuple, évitant ainsi d\'agrandir le fichier sur disque si des trous récupérés par VACUUM sont disponibles.',
      explanation: 'Le `_fsm` est mis à jour et consolidé par le processus `VACUUM` lorsqu\'il libère des dead tuples dans les pages.',
      examTrap: 'Si le `_fsm` est corrompu ou manquant, PostgreSQL n\'arrête pas l\'instance : il alloue de nouvelles pages en fin de fichier et le `_fsm` peut être reconstruit au prochain VACUUM.',
      ruleRef: 'PostgreSQL 16 Documentation - Free Space Map',
    },
  },
  {
    id: 'fc-pgedb-dom04-013',
    cardNumber: 13,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Fichier VM (Visibility Map) : Extension `_vm`',
    difficulty: 'hard',
    tags: ['Visibility Map', 'VM', 'Index-Only Scan', 'Freeze'],
    front: {
      question: 'Quelles sont les deux informations fondamentales (stockées sous forme de 2 bits par page) contenues dans la Visibility Map (`_vm`) ?',
      hint: 'Visibilité universelle pour toutes les transactions et statut de gel (freeze).',
    },
    back: {
      answer: 'La **Visibility Map (`_vm`)** stocke **2 bits** pour chaque page de données du tas (Heap) :\n1. **Bit `all-visible`** : Indique que tous les tuples de la page sont visibles par toutes les transactions actuelles et futures (aucun tuple mort, aucun tuple en cours de transaction). Permet les **Index-Only Scans** sans visiter la table !\n2. **Bit `all-frozen`** : Indique que tous les tuples de la page sont gelés (frozen). Le VACUUM anti-wraparound peut complètement **ignorer** ces pages, accélérant massivement le freeze.',
      explanation: 'Introduit en PG 8.4 pour l\'Index-Only Scan et étendu en PG 9.6 pour le flag `all-frozen`.',
      examTrap: 'Une page n\'a son bit `all-visible` armé QUE lors du passage de VACUUM. Des transactions `INSERT` pures ne l\'activent pas automatiquement.',
      ruleRef: 'PostgreSQL 16 Documentation - Visibility Map',
    },
  },
  {
    id: 'fc-pgedb-dom04-014',
    cardNumber: 14,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Accélération des "Index-Only Scans" grâce à la Visibility Map',
    difficulty: 'medium',
    tags: ['Index-Only Scan', 'Visibility Map', 'Performance', 'EXPLAIN'],
    front: {
      question: 'Pourquoi un "Index-Only Scan" doit-il parfois visiter les pages du tas (Heap Fetches), et quel est l\'impact du VACUUM sur ce comportement ?',
      codeSnippet: 'EXPLAIN (ANALYZE, BUFFERS) SELECT email FROM clients WHERE email LIKE \'a%\';\n-- Sortie : Heap Fetches: 1250',
      hint: 'Les index normaux ne contiennent pas les drapeaux xmin/xmax de visibilité MVCC.',
    },
    back: {
      answer: 'Les index PostgreSQL ne contiennent pas les informations de transaction `xmin`/`xmax`. Pour savoir si une ligne trouvée dans l\'index est visible, le moteur consulte la **Visibility Map** :\n- Si le bit `all-visible` de la page correspondante est à **1**, PostgreSQL sait avec certitude que la ligne est visible : il lit la valeur dans l\'index et **ne touche pas au tas** (Heap Fetches = 0).\n- Si le bit est à **0**, il doit charger la page de table depuis la mémoire ou le disque pour vérifier la visibilité du tuple (**Heap Fetch**).\n\nLe passage de `VACUUM` active ce bit et fait chuter les Heap Fetches à zéro.',
      explanation: 'Si vous observez un nombre élevé de "Heap Fetches" dans un plan d\'exécution, lancer un `VACUUM` sur la table restaure les performances optimales de l\'Index-Only Scan.',
      examTrap: 'Un Index-Only Scan n\'est pas garanti d\'être à zéro I/O sur la table si la table n\'a pas été récemment aspirée par VACUUM.',
      ruleRef: 'PostgreSQL 16 Documentation - Index-Only Scans',
    },
  },
  {
    id: 'fc-pgedb-dom04-015',
    cardNumber: 15,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Extension `pgstattuple` pour mesurer le bloat exact',
    difficulty: 'medium',
    tags: ['pgstattuple', 'Bloat', 'Monitoring', 'Extensions'],
    front: {
      question: 'Quelle fonction de l\'extension officielle `pgstattuple` permet de mesurer le pourcentage exact de dead tuples et d\'espace libre dans une table ?',
      codeSnippet: 'CREATE EXTENSION pgstattuple;\nSELECT * FROM pgstattuple(\'commandes\');',
      hint: 'Retourne table_len, tuple_percent, dead_tuple_percent, free_percent.',
    },
    back: {
      answer: 'La fonction **`pgstattuple(regclass)`** lit physiquement la table et retourne les métriques précises :\n- `table_len` : Taille totale de la table en octets.\n- `tuple_count` / `tuple_percent` : Nombre et proportion de lignes vivantes.\n- `dead_tuple_count` / `dead_tuple_percent` : Nombre et pourcentage exact de dead tuples (bloat de lignes mortes).\n- `free_space` / `free_percent` : Espace vide réutilisable dans les blocs.\n\nPour les index, on utilise la fonction compagne **`pgstatindex(\'nom_index\')`**.',
      explanation: 'Attention : `pgstattuple` effectue un scan séquentiel complet de la table. Pour les très grandes tables en production, préférez `pgstattuple_approx()` qui se base sur la Visibility Map.',
      examTrap: 'Sur une table de plusieurs téraoctets, appeler `pgstattuple()` sature les disques car il lit chaque page une à une.',
      ruleRef: 'PostgreSQL 16 Documentation - Appendix F: pgstattuple',
    },
  },
  {
    id: 'fc-pgedb-dom04-016',
    cardNumber: 16,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Fonction `pgstattuple_approx` (Mesure rapide et légère)',
    difficulty: 'medium',
    tags: ['pgstattuple', 'pgstattuple_approx', 'Performance', 'Bloat'],
    front: {
      question: 'Pourquoi préfère-t-on `pgstattuple_approx()` à `pgstattuple()` sur les tables volumineuses en production ?',
      hint: 'Utilisation de la Visibility Map pour éviter de scanner les blocs entièrement sains.',
    },
    back: {
      answer: '`pgstattuple_approx()` est considérablement plus rapide car elle **ignore les blocs marqués `all-visible` dans la Visibility Map**.\n\nPuisqu\'elle sait que ces pages ne contiennent aucun dead tuple, elle n\'a besoin de lire physiquement sur disque que les pages non entièrement visibles, réduisant l\'I/O de 90 à 99% sur une table régulièrement maintenue tout en fournissant une estimation très fidèle du bloat.',
      explanation: 'Retourne notamment `approx_tuple_count`, `approx_tuple_percent`, `dead_tuple_count` et `approx_free_percent`.',
      examTrap: 'Si la Visibility Map n\'est pas à jour (pas de VACUUM récent), `pgstattuple_approx()` devra lire davantage de blocs et perdra une partie de son avantage.',
      ruleRef: 'PostgreSQL 16 Documentation - pgstattuple - pgstattuple_approx',
    },
  },
  {
    id: 'fc-pgedb-dom04-017',
    cardNumber: 17,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Bloat des Index B-tree',
    difficulty: 'hard',
    tags: ['Index Bloat', 'B-Tree', 'Maintenance', 'REINDEX'],
    front: {
      question: 'Pourquoi les index B-tree sont-ils souvent beaucoup plus sujets au bloat irrécupérable que les tables sous PostgreSQL ?',
      hint: 'Structure d\'arbre ordonné et division de pages (Page Splits).',
    },
    back: {
      answer: 'Dans un index B-tree, les clés doivent rester strictement ordonnées. Lorsqu\'un `UPDATE` ou `DELETE` crée des dead tuples dans un index, ces emplacements ne peuvent être réutilisés **que par de futures insertions dont la valeur se situe dans la même plage de clés** de la page d\'index.\n\nDe plus, des insertions aléatoires provoquent des éclatements de pages (**Page Splits**) laissant les pages à 50% vides. Même si le VACUUM nettoie les pointeurs morts, une page d\'index n\'est presque jamais rendue au système d\'exploitation et reste souvent semi-remplie.',
      explanation: 'Un index B-tree sur une table avec beaucoup de churn (écritures/suppressions) peut atteindre 70 à 80% de bloat structurel.',
      examTrap: 'Le VACUUM standard ne fusionne pas les pages d\'index fragmentées et ne réduit pas la taille du fichier d\'index. Seul un `REINDEX` compacte un index B-tree.',
      ruleRef: 'PostgreSQL 16 Documentation - B-Tree Index Maintenance',
    },
  },
  {
    id: 'fc-pgedb-dom04-018',
    cardNumber: 18,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Déduplication B-tree (PostgreSQL 13+)',
    difficulty: 'medium',
    tags: ['B-Tree', 'Deduplication', 'Index Bloat', 'PostgreSQL 13+'],
    front: {
      question: 'Comment le mécanisme de déduplication B-tree (introduit en PG 13) combat-il nativement le bloat des index ?',
      codeSnippet: 'CREATE INDEX idx_statut ON commandes(statut) WITH (deduplicate_items = on);',
      hint: 'Clés en double stockées avec une liste de ctid compacte.',
    },
    back: {
      answer: 'Sur les colonnes à faible cardinalité (ex: statuts, booléens, clés étrangères répétées), les index B-tree contenaient historiquement la valeur de la clé dupliquée pour chaque tuple.\n\nDepuis PG 13, la déduplication regroupe les doublons : **la clé n\'est stockée qu\'une seule fois dans la page d\'index**, suivie d\'un tableau compact de pointeurs `ctid` (Posting List). Cela réduit la taille de l\'index de 30% à 70% et diminue considérablement les page splits et le bloat.',
      explanation: 'Ce comportement est actif par défaut (`deduplicate_items = on`) lors de la création d\'index B-tree ordinaires.',
      examTrap: 'La déduplication est automatiquement désactivée pour les index `UNIQUE` ou sur des types de colonnes sans opérateur d\'égalité binaire stricte.',
      ruleRef: 'PostgreSQL 16 Documentation - B-Tree Deduplication',
    },
  },
  {
    id: 'fc-pgedb-dom04-019',
    cardNumber: 19,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Blocage du nettoyage de bloat par transactions longues',
    difficulty: 'hard',
    tags: ['Bloat', 'Long Running Transactions', 'Vacuum Blocking', 'Troubleshooting'],
    front: {
      question: 'Pourquoi une simple requête `SELECT` en cours depuis 6 heures peut-elle causer un bloat catastrophique sur l\'ensemble du cluster PostgreSQL ?',
      hint: 'L\'horizon de visibilité de l\'instantané (Snapshot).',
    },
    back: {
      answer: 'Une transaction ancienne (même en simple lecture `SELECT`) fige un **Snapshot** avec un horizon de transaction bas (`xmin` ancien).\n\nPour respecter l\'isolation MVCC, le VACUUM a l\'interdiction formelle de supprimer tout dead tuple dont le `xmax` est supérieur ou égal au `xmin` de la transaction la plus ancienne en cours (`oldestxmin`).\n\nPar conséquent, **aucun dead tuple créé après le début de cette requête ne peut être nettoyé dans aucune table de la base**, provoquant une accumulation massive de bloat incontrôlable.',
      explanation: 'Cela s\'applique également aux sessions en état `idle in transaction` et aux slots de réplication logique orphelins ou inactifs.',
      examTrap: 'Même si la transaction longue n\'accède qu\'à la table A, elle bloque le VACUUM sur TOUTES les tables de la base de données !',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - Preventing Wraparound',
    },
  },
  {
    id: 'fc-pgedb-dom04-020',
    cardNumber: 20,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Paramètre `old_snapshot_threshold`',
    difficulty: 'hard',
    tags: ['old_snapshot_threshold', 'Snapshot Too Old', 'Bloat Prevention', 'Configuration'],
    front: {
      question: 'Quel est le rôle du paramètre `old_snapshot_threshold` et quelle erreur une requête longue recevra-t-elle s\'il est dépassé ?',
      codeSnippet: '# postgresql.conf\nold_snapshot_threshold = 4h',
      hint: 'Erreur Snapshot Too Old (SQLSTATE 55000).',
    },
    back: {
      answer: 'Le paramètre `old_snapshot_threshold` (désactivé à `-1` par défaut) définit la durée maximale pendant laquelle un instantané (snapshot) peut lire des versions de lignes mortes.\n\nS\'il est configuré (ex: `4h`), VACUUM est autorisé à détruire les dead tuples anciens sans attendre les transactions longues. Si une requête vieille de plus de 4 heures tente de lire une page où des dead tuples ont été nettoyés, elle est immédiatement annulée avec l\'erreur :\n`ERROR: snapshot too old` (SQLSTATE `55000`).',
      explanation: 'Ce mécanisme protège les clusters critiques contre le bloat massif causé par des requêtes de reporting oubliées.',
      examTrap: 'Une fois configuré dans `postgresql.conf`, `old_snapshot_threshold` nécessite un redémarrage complet du cluster (`postmaster restart`).',
      ruleRef: 'PostgreSQL 16 Documentation - Resource Consumption - old_snapshot_threshold',
    },
  },

  // =========================================================================
  // SECTION 3 : LE VACUUM STANDARD (LAZY VACUUM) EN PROFONDEUR (Cartes 21 à 30)
  // =========================================================================
  {
    id: 'fc-pgedb-dom04-021',
    cardNumber: 21,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Les 4 missions fondamentales de la commande `VACUUM`',
    difficulty: 'easy',
    tags: ['VACUUM', 'Maintenance', 'Architecture', 'Overview'],
    front: {
      question: 'Quelles sont les 4 tâches essentielles accomplies par la commande standard `VACUUM` ?',
      codeSnippet: 'VACUUM clients;',
      hint: 'Nettoyage des dead tuples, réutilisation d\'espace, gel des XID, et mise à jour des cartes.',
    },
    back: {
      answer: 'Les 4 tâches fondamentales de `VACUUM` sont :\n1. **Récupérer l\'espace disque interne** : Identifier les dead tuples dans le tas (Heap) et supprimer leurs références dans tous les index associés.\n2. **Rendre l\'espace réutilisable** : Marquer l\'espace libéré dans le `_fsm` pour que les futurs `INSERT`/`UPDATE` puissent l\'utiliser sans agrandir le fichier.\n3. **Geler les transactions (Freeze)** : Remplacer les XID anciens par `FrozenTransactionId` pour empêcher le Wraparound des 32 bits.\n4. **Mettre à jour la Visibility Map (`_vm`)** : Marquer les pages `all-visible` (pour booster les Index-Only Scans) et `all-frozen`.',
      explanation: 'Si l\'option `ANALYZE` est ajoutée (`VACUUM ANALYZE`), il calcule également les statistiques du planificateur.',
      examTrap: 'Le VACUUM standard ne rend PAS (ou très rarement) l\'espace disque au système d\'exploitation. Il le garde dans le fichier de table.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands - VACUUM',
    },
  },
  {
    id: 'fc-pgedb-dom04-022',
    cardNumber: 22,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Niveau de verrouillage d\'un `VACUUM` standard',
    difficulty: 'easy',
    tags: ['VACUUM', 'Locking', 'Concurrency', 'ShareUpdateExclusiveLock'],
    front: {
      question: 'Quel niveau de verrouillage SQL est posé par un `VACUUM` standard (sans FULL) et quelles opérations concurrentes restent autorisées ?',
      hint: 'ShareUpdateExclusiveLock. Les SELECT, INSERT, UPDATE, DELETE sont-ils bloqués ?',
    },
    back: {
      answer: 'Le `VACUUM` standard pose un verrou **`ShareUpdateExclusiveLock`** (mode 4).\n\nCe verrou est très permissif :\n- Les requêtes en lecture (`SELECT`) s\'exécutent normalement sans aucun blocage.\n- Les écritures concurrentes (`INSERT`, `UPDATE`, `DELETE`) s\'exécutent également normalement en parallèle.\n\nIl ne bloque que les opérations DDL concurrentes (`ALTER TABLE`, `DROP TABLE`), `VACUUM FULL`, `CLUSTER` ou un autre `VACUUM` sur la même table.',
      explanation: 'C\'est pourquoi le VACUUM standard peut (et doit) tourner en continu en pleine production 24h/24.',
      examTrap: 'Le VACUUM standard ne bloque pas les écritures, mais il consomme des I/O disque et CPU qui peuvent ralentir globalement le système si son coût n\'est pas bridé.',
      ruleRef: 'PostgreSQL 16 Documentation - Explicit Locking - Table-level Locks',
    },
  },
  {
    id: 'fc-pgedb-dom04-023',
    cardNumber: 23,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Troncature des blocs de fin de fichier (Truncate at EOF)',
    difficulty: 'medium',
    tags: ['VACUUM', 'Truncate', 'Disk Space', 'Operating System'],
    front: {
      question: 'Dans quelle seule condition très spécifique un `VACUUM` standard restitue-t-il réellement de l\'espace disque physique au système d\'exploitation ?',
      hint: 'Pages entièrement vides situées à la toute fin du fichier.',
    },
    back: {
      answer: 'Un `VACUUM` standard ne restitue de l\'espace à l\'OS que s\'il y a une série de **pages entièrement vides à la toute fin physique du fichier de table** (End Of File - EOF).\n\nDans ce cas précis, `VACUUM` pose brièvement un verrou `AccessExclusiveLock` de quelques millisecondes pour tronquer le fichier (`ftruncate`) et rendre ces blocs terminaux au système d\'exploitation.',
      explanation: 'Si une seule ligne active subsiste dans la dernière page du fichier (ex: page 100 000), aucune page précédente (pages 1 à 99 999) ne pourra être tronquée, même si elles sont toutes 100% vides.',
      examTrap: 'Si une transaction concurrente bloque l\'acquisition du verrou exclusif pour tronquer la fin de table, `VACUUM` abandonne silencieusement la troncature sans erreur pour ne pas pénaliser la production.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming',
    },
  },
  {
    id: 'fc-pgedb-dom04-024',
    cardNumber: 24,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Phases d\'exécution d\'un VACUUM standard',
    difficulty: 'hard',
    tags: ['VACUUM', 'Phases', 'Internals', 'pg_stat_progress_vacuum'],
    front: {
      question: 'Quelles sont les grandes phases successives franchies par l\'algorithme de VACUUM lors du traitement d\'une table ?',
      hint: 'Scan du tas, scan des index, vacuuming du tas, et troncature finale.',
    },
    back: {
      answer: 'Le VACUUM s\'exécute selon les phases principales suivantes :\n1. **`scanning heap`** : Parcours séquentiel des blocs de la table pour collecter les identifiants de pointeurs des dead tuples dans un tableau en mémoire (`maintenance_work_mem`).\n2. **`vacuuming indexes`** : Parcours de chaque index de la table pour supprimer tous les pointeurs vers ces dead tuples.\n3. **`vacuuming heap`** : Retour sur le tas pour marquer les emplacements des dead tuples comme utilisables dans les pages et mettre à jour le `_fsm` et la `_vm`.\n4. **`truncating heap`** (optionnel) : Troncation des pages vides terminales en fin de fichier.',
      explanation: 'Ces phases sont observables en temps réel via la vue système `pg_stat_progress_vacuum`.',
      examTrap: 'Si le nombre de dead tuples dépasse la mémoire allouée par `maintenance_work_mem`, VACUUM doit répéter les cycles "scan heap -> vacuum indexes -> vacuum heap" plusieurs fois, augmentant drastiquement les I/O.',
      ruleRef: 'PostgreSQL 16 Documentation - Progress Reporting - VACUUM Progress',
    },
  },
  {
    id: 'fc-pgedb-dom04-025',
    cardNumber: 25,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Rôle de `maintenance_work_mem` pour le VACUUM',
    difficulty: 'medium',
    tags: ['maintenance_work_mem', 'Memory', 'VACUUM Performance', 'Tuning'],
    front: {
      question: 'Comment le paramètre `maintenance_work_mem` influence-t-il directement l\'efficacité et la vitesse du processus de `VACUUM` ?',
      codeSnippet: 'SET maintenance_work_mem = \'1GB\';\nVACUUM VERBOSE grosse_table;',
      hint: 'Taille du tableau en mémoire contenant la liste des dead tuples (TIDs).',
    },
    back: {
      answer: '`maintenance_work_mem` détermine la taille du tampon mémoire utilisé par `VACUUM` pour stocker la liste des pointeurs de dead tuples (`TID`) collectés lors du scan du tas :\n- Avec une valeur trop faible (ex: défaut de 64 Mo), le tampon se remplit vite (environ 10 millions de TIDs par Go de RAM). VACUUM doit interrompre son scan du tas pour aller nettoyer les index, puis reprendre le scan du tas, scannant les index plusieurs fois.\n- Avec une valeur généreuse (ex: 1 Go ou 2 Go), `VACUUM` peut stocker tous les TIDs en une seule passe et ne scanner chaque index qu\'**une seule fois**.',
      explanation: 'Il existe une limite technique interne : au-delà de 1 Go (PostgreSQL 16 ou antérieur), un worker de vacuum ne stocke pas plus de dead tuples dans son tableau structuré (1 Go suffit pour stocker environ 178 millions de TIDs).',
      examTrap: 'Chaque worker autovacuum actif peut consommer jusqu\'à `autovacuum_work_mem` (ou `maintenance_work_mem`), attention à ne pas surdimensionner globalement la RAM.',
      ruleRef: 'PostgreSQL 16 Documentation - Resource Consumption - Memory',
    },
  },
  {
    id: 'fc-pgedb-dom04-026',
    cardNumber: 26,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Paramètre `autovacuum_work_mem`',
    difficulty: 'medium',
    tags: ['autovacuum_work_mem', 'Memory', 'Autovacuum', 'Tuning'],
    front: {
      question: 'Pourquoi est-il fortement recommandé de configurer `autovacuum_work_mem` séparément de `maintenance_work_mem` ?',
      codeSnippet: '# postgresql.conf\nmaintenance_work_mem = 2GB\nautovacuum_work_mem = 512MB',
      hint: 'Éviter l\'épuisement de la mémoire quand plusieurs workers autovacuum s\'exécutent en parallèle.',
    },
    back: {
      answer: 'Si `autovacuum_work_mem` est laissé à sa valeur par défaut (`-1`), chaque worker autovacuum hérite de la valeur de `maintenance_work_mem`.\n\nSi un DBA augmente `maintenance_work_mem` à 4 Go pour accélérer des créations manuelles d\'index (`CREATE INDEX`), et que `autovacuum_max_workers` est à 5, les workers automatiques pourraient consommer jusqu\'à 5 x 4 Go = **20 Go de RAM**, risquant de provoquer un OOM-Killer (Out-Of-Memory) du système d\'exploitation !\n\nConfigurer un `autovacuum_work_mem` dédié (ex: 512 Mo) isole et sécurise la consommation des démons en tâche de fond.',
      explanation: 'Cette bonne pratique évite les effets de bord entre opérations manuelles de maintenance lourdes et automatismes.',
      examTrap: 'Si `autovacuum_work_mem = -1`, il prend silencieusement la valeur de `maintenance_work_mem`.',
      ruleRef: 'PostgreSQL 16 Documentation - Resource Consumption - Memory',
    },
  },
  {
    id: 'fc-pgedb-dom04-027',
    cardNumber: 27,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Syntaxe moderne `VACUUM (...)` et options clés',
    difficulty: 'easy',
    tags: ['VACUUM', 'SQL Syntax', 'Options', 'PostgreSQL 16'],
    front: {
      question: 'Quelle est la syntaxe moderne recommandée de la commande `VACUUM` et que font les options `VERBOSE`, `FREEZE` et `INDEX_CLEANUP` ?',
      codeSnippet: 'VACUUM (VERBOSE, INDEX_CLEANUP AUTO, PARALLEL 2) ma_table;',
      hint: 'Syntaxe parenthésée de type fonction avec arguments booléens ou paramètres.',
    },
    back: {
      answer: 'La syntaxe moderne utilise des parenthèses `VACUUM (options) table;` :\n- **`VERBOSE`** : Affiche un rapport d\'exécution détaillé par page, nombre de tuples supprimés, espace libéré et temps par phase.\n- **`FREEZE`** : Force un gel agressif immédiat des tuples avec un âge de transaction nul (équivalent historique de `VACUUM FREEZE`).\n- **`INDEX_CLEANUP AUTO / ON / OFF`** : Permet de désactiver ou différer le nettoyage des index (très utile pour libérer d\'urgence de l\'espace tas ou sous forte pression de freeze sans subir la latence de scan des index).',
      explanation: 'L\'option `INDEX_CLEANUP FALSE` permet un vacuum d\'urgence hyper rapide sur le tas uniquement.',
      examTrap: 'Désactiver `INDEX_CLEANUP` laisse des pointeurs morts dans les index, ce qui accroît temporairement le bloat de ces index.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands - VACUUM',
    },
  },
  {
    id: 'fc-pgedb-dom04-028',
    cardNumber: 28,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Parallélisme du VACUUM manuel (`PARALLEL`)',
    difficulty: 'medium',
    tags: ['VACUUM', 'Parallelism', 'Performance', 'Indexes'],
    front: {
      question: 'Comment fonctionne l\'option `PARALLEL` de la commande `VACUUM` (introduite en PostgreSQL 13) et que parallélise-t-elle exactement ?',
      codeSnippet: 'VACUUM (PARALLEL 4) grosse_table;',
      hint: 'Elle ne parallélise pas le scan du tas mais le nettoyage simultané des index.',
    },
    back: {
      answer: 'L\'option `PARALLEL N` permet à `VACUUM` de lancer des **workers de requêtes parallèles** pour nettoyer simultanément plusieurs index de la table :\n- Si une table possède 4 index volumineux, `PARALLEL 4` assigne chaque index à un worker en parallèle au lieu de les traiter séquentiellement un par un.\n- Le scan initial du tas reste mono-threadé, mais la phase la plus longue (`vacuuming indexes`) est considérablement accélérée.',
      explanation: 'Le nombre effectif de workers est borné par le nombre d\'index éligibles et le paramètre `max_parallel_maintenance_workers`.',
      examTrap: '`VACUUM (PARALLEL)` ne s\'applique pas à `autovacuum`. Le parallélisme ne peut être invoqué que lors d\'un appel `VACUUM` manuel.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands - VACUUM - PARALLEL',
    },
  },
  {
    id: 'fc-pgedb-dom04-029',
    cardNumber: 29,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Contrôle du coût d\'I/O du VACUUM : Cost-based Vacuum Delay',
    difficulty: 'hard',
    tags: ['Cost Delay', 'I/O Throttling', 'vacuum_cost_delay', 'Performance'],
    front: {
      question: 'Quel est le mécanisme qui empêche un `VACUUM` de saturer les disques au détriment des requêtes de production ?',
      codeSnippet: '# postgresql.conf\nvacuum_cost_delay = 2ms\nvacuum_cost_limit = 200\nvacuum_cost_page_hit = 1\nvacuum_cost_page_miss = 2\nvacuum_cost_page_dirty = 20',
      hint: 'Accumulation de points de coût et micro-pauses.',
    },
    back: {
      answer: 'C\'est le mécanisme de **Cost-based Vacuum Delay** (temporisation basée sur le coût) :\nÀ chaque bloc traité, VACUUM incrémente un compteur de coût :\n- Bloc déjà en RAM (`vacuum_cost_page_hit`) : coût faible (1).\n- Bloc lu depuis le disque (`vacuum_cost_page_miss`) : coût moyen (2).\n- Bloc modifié/écrit car contenant des dead tuples (`vacuum_cost_page_dirty`) : coût élevé (20).\n\nDès que la somme atteint `vacuum_cost_limit` (ex: 200), le worker s\'endort pendant `vacuum_cost_delay` (ex: 2 ms) avant de reprendre.',
      explanation: 'Cela lisse les pics d\'I/O et garantit que les lectures/écritures des utilisateurs restent prioritaires et fluides.',
      examTrap: 'Si `vacuum_cost_delay` est trop élevé ou `limit` trop basse, le VACUUM tourne trop lentement et n\'arrive plus à suivre le rythme des UPDATE/DELETE, générant du bloat.',
      ruleRef: 'PostgreSQL 16 Documentation - Cost-based Vacuum Delay',
    },
  },
  {
    id: 'fc-pgedb-dom04-030',
    cardNumber: 30,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Amélioration de `vacuum_cost_page_miss` dans PostgreSQL 16',
    difficulty: 'hard',
    tags: ['PostgreSQL 16', 'vacuum_cost_page_miss', 'I/O', 'Evolution'],
    front: {
      question: 'Quelle modification majeure a été apportée à la valeur par défaut de `vacuum_cost_page_miss` dans PostgreSQL 16 ?',
      hint: 'Passage d\'un ratio hérité des disques mécaniques HDD à une réalité moderne SSD.',
    },
    back: {
      answer: 'Dans PostgreSQL 16, la valeur par défaut de **`vacuum_cost_page_miss` a été abaissée de 10 à 2** (avec un `vacuum_cost_limit` par défaut ajusté de 200 à 200).\n\nHistoriquement, lire une page sur un disque dur mécanique était 10 fois plus pénalisant qu\'une lecture en RAM. Avec la généralisation des SSD/NVMe et des architectures cloud modernes, ce coût a été ramené à 2, ce qui permet à `VACUUM` et `autovacuum` de nettoyer les tables beaucoup plus rapidement par défaut sans saturation injustifiée.',
      explanation: 'Cette modification réduit l\'accumulation de retard d\'autovacuum sur les bases à fort trafic.',
      examTrap: 'Sur un serveur PG 16 récemment migré depuis PG 15 sans revoir les fichiers de configuration personnalisés, vérifiez que d\'anciennes valeurs de coût figées n\'annulent pas ce gain natif.',
      ruleRef: 'PostgreSQL 16 Release Notes - Cost-based Vacuum Delay Defaults',
    },
  },

  // =========================================================================
  // SECTION 4 : VACUUM FULL & ALTERNATIVES (pg_repack) (Cartes 31 à 40)
  // =========================================================================
  {
    id: 'fc-pgedb-dom04-031',
    cardNumber: 31,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Fonctionnement interne de `VACUUM FULL`',
    difficulty: 'easy',
    tags: ['VACUUM FULL', 'Compaction', 'Disk Space', 'Locks'],
    front: {
      question: 'Comment `VACUUM FULL` parvient-il à rendre physiquement l\'intégralité de l\'espace disque libéré au système d\'exploitation ?',
      codeSnippet: 'VACUUM FULL commandes;',
      hint: 'Il ne nettoie pas sur place : il réécrit un tout nouveau fichier de table.',
    },
    back: {
      answer: '`VACUUM FULL` ne modifie pas les blocs de la table existante sur place. Il procède ainsi :\n1. Il alloue un **nouveau fichier de données physique tout neuf** sur le disque.\n2. Il copie uniquement les **lignes actives (live tuples)** de l\'ancienne table vers le nouveau fichier de façon 100% compacte et contiguë.\n3. Il reconstruit de zéro tous les index associés.\n4. Il supprime l\'ancien fichier physique volumineux et le remplace par le nouveau, **restituant immédiatement l\'espace libéré au système d\'exploitation**.',
      explanation: 'Tous les `ctid` de toutes les lignes sont réécrits de manière séquentielle compacte.',
      examTrap: '`VACUUM FULL` nécessite temporairement jusqu\'au double de la taille de la table en espace disque disponible pour créer la nouvelle copie !',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands - VACUUM FULL',
    },
  },
  {
    id: 'fc-pgedb-dom04-032',
    cardNumber: 32,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Verrouillage de `VACUUM FULL` : `AccessExclusiveLock`',
    difficulty: 'easy',
    tags: ['VACUUM FULL', 'Locking', 'AccessExclusiveLock', 'Production Risk'],
    front: {
      question: 'Quel verrou est requis par `VACUUM FULL` et quel est son impact sur les applications utilisatrices ?',
      hint: 'Le verrou le plus restrictif de PostgreSQL (mode 8).',
    },
    back: {
      answer: '`VACUUM FULL` exige un verrou **`AccessExclusiveLock`** (mode maximal 8) sur la table pendant toute la durée de son exécution.\n\nCe verrou bloque **absolument toutes les opérations concurrentes**, y compris les simples lectures (`SELECT`). Toute tentative d\'accès à la table par l\'application sera mise en attente (ou échouera par timeout), rendant le service indisponible pour cette table jusqu\'à la fin de la réécriture.',
      explanation: 'Sur une table de plusieurs centaines de Go, cette opération peut prendre plusieurs heures.',
      examTrap: 'Exécuter un `VACUUM FULL` sur une table de production en pleine journée est une erreur d\'exploitation grave qui entraîne une indisponibilité applicative.',
      ruleRef: 'PostgreSQL 16 Documentation - Explicit Locking',
    },
  },
  {
    id: 'fc-pgedb-dom04-033',
    cardNumber: 33,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Exigence d\'espace disque libre pour `VACUUM FULL`',
    difficulty: 'medium',
    tags: ['VACUUM FULL', 'Disk Space', 'Risk', 'Capacity Planning'],
    front: {
      question: 'Pourquoi lancer `VACUUM FULL` en urgence lorsque le disque de la base de données est plein à 98% conduit-il presque systématiquement à l\'échec ?',
      hint: 'Création d\'un second fichier complet en parallèle avant suppression de l\'ancien.',
    },
    back: {
      answer: 'Parce que `VACUUM FULL` a besoin de créer la nouvelle table compactée **à côté** de l\'ancienne table existante avant de détruire cette dernière.\n\nSi une table fait 300 Go (dont 100 Go de données vivantes), vous devez disposer d\'au moins 100 Go d\'espace disque libre immédiat pour écrire la nouvelle table et ses index. Si le disque est saturé à 98%, l\'opération échouera avec `ERROR: could not extend file: No space left on device` et effectuera un rollback complet.',
      explanation: 'En situation de disque plein, d\'autres techniques d\'urgence (suppression de logs WAL anciens archivés, drop de tables temporaires, ou `pg_repack` sur un tablespace séparé) doivent être employées.',
      examTrap: 'Après un crash pour disque plein lors d\'un `VACUUM FULL`, le rollback supprime les fichiers temporaires créés, mais l\'espace n\'a pas été libéré d\'un seul octet.',
      ruleRef: 'PostgreSQL Troubleshooting - Disk Full Emergency Procedures',
    },
  },
  {
    id: 'fc-pgedb-dom04-034',
    cardNumber: 34,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'L\'outil communautaire `pg_repack` (Alternative en ligne)',
    difficulty: 'medium',
    tags: ['pg_repack', 'Zero Downtime', 'Bloat', 'Extensions'],
    front: {
      question: 'Pourquoi l\'extension communautaire `pg_repack` est-elle largement préférée à `VACUUM FULL` dans les environnements haute disponibilité ?',
      codeSnippet: '$ pg_repack -d ma_base --table commandes',
      hint: 'Réorganisation physique sans verrou exclusif prolongé.',
    },
    back: {
      answer: '`pg_repack` réorganise et compacte les tables et index pour restituer l\'espace disque à l\'OS **en ligne sans verrouiller les lectures ni les écritures** (Online Table Reorganization) :\n1. Il crée une table de réplique temporaire et y copie les données.\n2. Il capture les modifications concurrentes (`INSERT`, `UPDATE`, `DELETE`) via des triggers et une table de log dédiée.\n3. Il applique le delta de log sur la nouvelle table.\n4. Il ne pose un `AccessExclusiveLock` que pendant **quelques millisecondes** pour permuter les noms des tables dans le catalogue système.',
      explanation: 'C\'est l\'outil standard de l\'industrie pour débloater les tables sans interrompre les SLAs applicatifs.',
      examTrap: '`pg_repack` nécessite que la table possède une clé primaire (`PRIMARY KEY`) ou un index unique `NOT NULL` pour pouvoir rejouer les modifications du journal de bord.',
      ruleRef: 'pg_repack Documentation - Reorganize tables in PostgreSQL databases with minimal locks',
    },
  },
  {
    id: 'fc-pgedb-dom04-035',
    cardNumber: 35,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Commande `CLUSTER` vs `VACUUM FULL`',
    difficulty: 'medium',
    tags: ['CLUSTER', 'VACUUM FULL', 'Index Ordering', 'Locks'],
    front: {
      question: 'Quelle est la différence fondamentale entre `VACUUM FULL` et la commande SQL `CLUSTER` ?',
      codeSnippet: 'CLUSTER clients USING idx_clients_nom;\nCLUSTER;',
      hint: 'Toutes deux réécrivent la table, mais CLUSTER réordonne physiquement les lignes.',
    },
    back: {
      answer: 'Les deux commandes réécrivent entièrement la table sous verrou `AccessExclusiveLock` et rendent l\'espace à l\'OS, mais :\n- **`VACUUM FULL`** copie les lignes dans l\'ordre où il les trouve dans le tas, sans réordonnancement particulier.\n- **`CLUSTER nom_table USING nom_index`** réordonne physiquement les lignes sur le disque en fonction de l\'ordre des clés de l\'index spécifié.\n\nCela optimise les lectures par plage (`Range Scans`) futures sur cette colonne, car les pages disque consécutives contiendront des valeurs adjacentes.',
      explanation: 'PostgreSQL ne maintient pas ce tri lors des insertions futures : le clustering se dégrade avec le temps et doit être relancé périodiquement.',
      examTrap: '`CLUSTER` pose le même verrou exclusif bloquant que `VACUUM FULL`.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands - CLUSTER',
    },
  },
  {
    id: 'fc-pgedb-dom04-036',
    cardNumber: 36,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Déplacement de table vers un autre Tablespace lors du débloat',
    difficulty: 'medium',
    tags: ['TABLESPACE', 'Disk Space', 'ALTER TABLE', 'Bloat'],
    front: {
      question: 'Comment la commande `ALTER TABLE ... SET TABLESPACE` peut-elle servir à débloater une table lorsque le disque principal est saturé ?',
      codeSnippet: 'ALTER TABLE logs_audit SET TABLESPACE disque_rapide_nvme;',
      hint: 'Réécriture complète de la table sur un nouveau point de montage disposant d\'espace.',
    },
    back: {
      answer: 'Déplacer une table vers un autre tablespace réécrit physiquement la table et ses index sur le nouveau système de fichiers :\n- Seules les lignes vivantes sont copiées, éliminant tout le bloat à 100% comme un `VACUUM FULL`.\n- Cela résout le problème de manque d\'espace disque sur le volume d\'origine, car la nouvelle copie est écrite sur le nouveau volume (disposant de stockage libre).\n- Une fois la copie terminée, l\'ancien fichier sur le volume d\'origine est supprimé, libérant l\'espace.',
      explanation: 'Cette opération requiert également un verrou `AccessExclusiveLock` pendant le transfert.',
      examTrap: 'Les index de la table restent sur leur tablespace d\'origine sauf si vous exécutez également `ALTER INDEX ... SET TABLESPACE`.',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands - ALTER TABLE',
    },
  },
  {
    id: 'fc-pgedb-dom04-037',
    cardNumber: 37,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Option `truncate` du VACUUM (`vacuum_truncate`)',
    difficulty: 'hard',
    tags: ['vacuum_truncate', 'Storage Parameters', 'Locks', 'Deadlock'],
    front: {
      question: 'Pourquoi désactiverait-on la troncature automatique de fin de fichier d\'une table via `ALTER TABLE ... SET (vacuum_truncate = false)` ?',
      codeSnippet: 'ALTER TABLE transactions_critiques SET (vacuum_truncate = false);',
      hint: 'Éviter les blocages et les deadlocks liés à la brève tentative d\'AccessExclusiveLock.',
    },
    back: {
      answer: 'À la fin d\'un `VACUUM`, pour tronquer les blocs vides à la fin du fichier, PostgreSQL doit tenter d\'obtenir un verrou `AccessExclusiveLock`.\n\nSur des tables à très haute concurrence transactionnelle, cette tentative d\'élévation de verrou peut provoquer des contentions, faire échouer des transactions avec des erreurs de verrouillage ou créer des files d\'attente de blocages applicatifs.\n\nDésactiver la troncature (`vacuum_truncate = false`) force `VACUUM` à garder la taille physique du fichier telle quelle, garantissant qu\'aucun verrou exclusif ne sera jamais sollicité.',
      explanation: 'L\'espace des blocs terminaux reste utilisable pour les futures insertions grâce au `_fsm`.',
      examTrap: 'Désactiver `vacuum_truncate` empêchera définitivement le fichier de rétrécir lors d\'un VACUUM standard, même s\'il est vide à la fin.',
      ruleRef: 'PostgreSQL 16 Documentation - Storage Parameters - vacuum_truncate',
    },
  },
  {
    id: 'fc-pgedb-dom04-038',
    cardNumber: 38,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Surveillance de la réécriture de table dans `pg_stat_activity`',
    difficulty: 'medium',
    tags: ['pg_stat_activity', 'Monitoring', 'VACUUM FULL', 'Wait Events'],
    front: {
      question: 'Comment identifier si une opération `VACUUM FULL` ou `CLUSTER` est en cours et quel type d\'événement d\'attente (`wait_event`) génère-t-elle typiquement ?',
      codeSnippet: 'SELECT pid, query, state, wait_event_type, wait_event\nFROM pg_stat_activity\nWHERE query ILIKE \'%VACUUM FULL%\';',
      hint: 'Événements de lecture/écriture I/O de données dans pg_stat_activity.',
    },
    back: {
      answer: 'Dans `pg_stat_activity`, la session exécutant `VACUUM FULL` apparaît à l\'état `active` avec le texte exact de la requête.\n\nSes événements d\'attente typiques sont :\n- `wait_event_type = \'IO\'` avec `wait_event = \'DataFileRead\'` ou `\'DataFileWrite\'` lors de la copie des blocs de données.\n- `wait_event_type = \'Lock\'` avec `wait_event = \'relation\'` si elle attend que des transactions concurrentes se terminent pour acquérir son `AccessExclusiveLock` initial.',
      explanation: 'Depuis PostgreSQL 12, les progrès de `VACUUM FULL` sont également suivis dans la vue `pg_stat_progress_cluster`.',
      examTrap: 'Si `wait_event = \'relation\'`, l\'opération n\'a même pas encore commencé à copier les données : elle est bloquée en file d\'attente de verrou.',
      ruleRef: 'PostgreSQL 16 Documentation - Monitoring - pg_stat_activity',
    },
  },
  {
    id: 'fc-pgedb-dom04-039',
    cardNumber: 39,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Impact de `VACUUM FULL` sur la réplication en streaming',
    difficulty: 'hard',
    tags: ['Replication', 'WAL', 'VACUUM FULL', 'Standby Lag'],
    front: {
      question: 'Quel impact majeur l\'exécution d\'un `VACUUM FULL` sur une table de 100 Go a-t-elle sur les serveurs réplicas (standbys) ?',
      hint: 'Génération massive de WAL et retard de réplication (Replication Lag).',
    },
    back: {
      answer: 'Puisque `VACUUM FULL` réécrit intégralement la table et reconstruit tous ses index, il génère un **volume colossal de journaux de transactions (WAL)** équivalent à la taille de la nouvelle table (plus de 100 Go de WAL émis d\'un coup).\n\nConséquences sur les réplicas :\n1. **Saturation de la bande passante réseau** de réplication.\n2. **Important retard de réplication (Replication Lag)** le temps que les standbys rejouent ces gigaoctets de WAL.\n3. Risque de requêtes annulées sur les standbys avec l\'erreur `canceling statement due to conflict with recovery` à cause du verrou exclusif rejoué.',
      explanation: 'Pour éviter ce lag massif, `pg_repack` ou des fenêtres de maintenance planifiées sont obligatoires.',
      examTrap: 'Sur le standby, l\'`AccessExclusiveLock` est rejoué dès le début de la réécriture, coupant l\'accès en lecture aux utilisateurs du réplica.',
      ruleRef: 'PostgreSQL 16 Documentation - High Availability, Load Balancing, and Replication',
    },
  },
  {
    id: 'fc-pgedb-dom04-040',
    cardNumber: 40,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Pourquoi `TRUNCATE` est-il instantané comparé à `DELETE` + `VACUUM FULL` ?',
    difficulty: 'easy',
    tags: ['TRUNCATE', 'Performance', 'DDL', 'Storage'],
    front: {
      question: 'Pourquoi `TRUNCATE table;` prend-il quelques millisecondes pour vider 500 millions de lignes, alors qu\'un `DELETE` suivi d\'un `VACUUM FULL` prendrait des heures ?',
      hint: 'Déréférencement du fichier physique au niveau de l\'inode/OS plutôt que scan ligne par ligne.',
    },
    back: {
      answer: '`DELETE` est une opération DML qui doit enregistrer chaque ligne supprimée, mettre à jour `xmax` et écrire des Mo/Go de WAL pour chaque ligne.\n\nÀ l\'inverse, **`TRUNCATE` est une opération DDL** :\n- Il alloue immédiatement un nouveau fichier physique vide (nouveau `relfilenode`) dans le catalogue.\n- Il supprime l\'ancien fichier physique de plusieurs centaines de Go sur le système de fichiers en une seule opération OS (`unlink`).\n- Aucun parcours de ligne ni aucun scan d\'index n\'est nécessaire.',
      explanation: '`TRUNCATE` requiert un verrou `AccessExclusiveLock` et réinitialise également les index associés.',
      examTrap: '`TRUNCATE` ne déclenche pas les triggers `FOR EACH ROW` de suppression (seuls les triggers `FOR EACH STATEMENT` de truncate sont exécutés).',
      ruleRef: 'PostgreSQL 16 Documentation - SQL Commands - TRUNCATE',
    },
  },

  // =========================================================================
  // SECTION 5 : TRANSACTION ID (XID) WRAPAROUND & PROCESSUS DE FREEZE (Cartes 41 à 50)
  // =========================================================================
  {
    id: 'fc-pgedb-dom04-041',
    cardNumber: 41,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Le problème du Transaction ID (XID) Wraparound (32 bits)',
    difficulty: 'hard',
    tags: ['Wraparound', 'XID', 'Freeze', 'Core Architecture'],
    front: {
      question: 'Qu\'est-ce que le problème du "Transaction ID Wraparound" et pourquoi survient-il mathématiquement à 2^31 (~2 milliards) de transactions ?',
      hint: 'Compteur XID circulaire signé sur 32 bits.',
    },
    back: {
      answer: 'Les identifiants de transaction (XID) de PostgreSQL sont stockés sur **32 bits**, soit 4 294 967 296 valeurs possibles. L\'arithmétique modulaire compare les XID de manière circulaire :\n- Tout XID situé dans les 2^31 (~2,1 milliards) valeurs précédentes est considéré comme **"dans le passé"** (visible).\n- Tout XID situé dans les 2^31 valeurs suivantes est considéré comme **"dans le futur"** (non visible).\n\nSi le compteur boucle après 2,1 milliards de transactions sans maintenance, les transactions historiques les plus anciennes apparaîtront soudainement comme situées "dans le futur", rendant **toutes les données de la base invisibles (perte logique totale)**.',
      explanation: 'C\'est l\'un des risques de corruption logique les plus critiques dans PostgreSQL, neutralisé par le mécanisme de FREEZE.',
      examTrap: 'Le compteur ne s\'arrête pas à 4 milliards : le bug de visibilité se produit dès 2 milliards de transactions à cause de la comparaison circulaire.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - Preventing Transaction ID Wraparound Failures',
    },
  },
  {
    id: 'fc-pgedb-dom04-042',
    cardNumber: 42,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Concept de "Frozen XID" (`FrozenTransactionId` = 2)',
    difficulty: 'medium',
    tags: ['Frozen XID', 'Freeze', 'MVCC', 'Internals'],
    front: {
      question: 'Qu\'est-ce qu\'un tuple "gelé" (Frozen Tuple) et quelle valeur magique spéciale prend son statut ?',
      hint: 'XID spécial 2 (FrozenTransactionId) ou bit HEAP_XMIN_FROZEN dans t_infomask.',
    },
    back: {
      answer: 'Lorsqu\'un tuple est "gelé" par `VACUUM`, PostgreSQL lui applique un statut spécial qui signifie :\n**"Ce tuple a été validé dans un passé infiniment lointain, il est définitivement visible par toutes les transactions présentes et futures, quel que soit leur XID."**\n\nHistoriquement, cela remplaçait `xmin` par la valeur magique `2` (`FrozenTransactionId`). Depuis PostgreSQL 9.4+, cela positionne un bit d\'en-tête direct dans `t_infomask` (`HEAP_XMIN_FROZEN`), préservant le `xmin` d\'origine tout en le marquant universellement visible.',
      explanation: 'Une fois gelé, le tuple n\'est plus jamais sujet aux comparaisons arithmétiques circulaires de XID.',
      examTrap: 'Geler un tuple ne le rend pas en lecture seule : il peut toujours être modifié (`UPDATE`) ou supprimé (`DELETE`) par une transaction.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - Freezing',
    },
  },
  {
    id: 'fc-pgedb-dom04-043',
    cardNumber: 43,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Paramètre `vacuum_freeze_min_age`',
    difficulty: 'medium',
    tags: ['vacuum_freeze_min_age', 'Freeze', 'Tuning', 'Configuration'],
    front: {
      question: 'À quoi sert le paramètre de configuration `vacuum_freeze_min_age` (défaut : 50 millions de transactions) ?',
      hint: 'Âge minimum en transactions avant qu\'un tuple vivant ne soit éligible au gel.',
    },
    back: {
      answer: '`vacuum_freeze_min_age` définit l\'**âge minimum** (en nombre de transactions écoulées) qu\'un tuple doit avoir atteint pour qu\'un `VACUUM` prenne la peine de le geler.\n\nSi un tuple a été inséré il y a seulement 10 000 transactions, `VACUUM` ne le gèle pas immédiatement pour éviter de réécrire la page inutilement si ce tuple risque d\'être modifié ou supprimé rapidement.\n\nLa valeur par défaut est de 50 000 000 de transactions.',
      explanation: 'Plus la valeur est basse, plus le gel est anticipé, mais plus le volume d\'écritures disque (dirty pages) augmente.',
      examTrap: 'Une valeur de 0 force le gel immédiat de chaque tuple valide rencontré lors du VACUUM.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - vacuum_freeze_min_age',
    },
  },
  {
    id: 'fc-pgedb-dom04-044',
    cardNumber: 44,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Paramètre `vacuum_freeze_table_age` (Freeze agressif)',
    difficulty: 'hard',
    tags: ['vacuum_freeze_table_age', 'Aggressive Vacuum', 'Visibility Map', 'Freeze'],
    front: {
      question: 'Quel comportement de scan déclenche `vacuum_freeze_table_age` (défaut : 150 millions de transactions) lorsqu\'une table atteint cet âge ?',
      hint: 'Passage d\'un scan partiel basé sur la Visibility Map à un scan complet agressif.',
    },
    back: {
      answer: 'Habituellement, `VACUUM` saute les pages marquées `all-frozen` dans la Visibility Map pour économiser des I/O.\n\nCependant, si le `relfrozenxid` de la table est plus vieux que **`vacuum_freeze_table_age`** transactions, `VACUUM` déclenche un **scan agressif (Aggressive Vacuum)** :\nIl ignore la Visibility Map et **lit 100% des pages de la table** pour geler tous les tuples restants et permettre d\'avancer le pointeur `relfrozenxid` de la table dans `pg_class`.',
      explanation: 'Cela garantit que l\'âge global de la table ne continue pas à augmenter indéfiniment.',
      examTrap: 'Pendant un scan agressif, toutes les pages non gelées sont lues et écrites, ce qui peut générer un pic d\'I/O important.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - vacuum_freeze_table_age',
    },
  },
  {
    id: 'fc-pgedb-dom04-045',
    cardNumber: 45,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Paramètre `autovacuum_freeze_max_age` (Anti-wraparound d\'urgence)',
    difficulty: 'hard',
    tags: ['autovacuum_freeze_max_age', 'Anti-wraparound', 'Safety', 'Critical'],
    front: {
      question: 'Que se passe-t-il lorsque l\'âge des transactions d\'une table atteint le seuil critique de `autovacuum_freeze_max_age` (défaut : 200 millions) ?',
      hint: 'Déclenchement obligatoire et inarrêtable d\'un autovacuum anti-wraparound.',
    },
    back: {
      answer: 'Lorsque l\'âge d\'une table atteint `autovacuum_freeze_max_age` :\n1. **Autovacuum se déclenche obligatoirement en mode anti-wraparound**, même si l\'autovacuum est désactivé globalement (`autovacuum = off`) !\n2. Ce worker spécial **ne peut pas être interrompu** ou annulé.\n3. Il s\'exécute avec la plus haute priorité pour geler les tuples et repousser le danger de wraparound.',
      explanation: 'C\'est l\'ultime filet de sécurité automatique du moteur PostgreSQL.',
      examTrap: 'Ne jamais tenter de tuer le processus worker autovacuum anti-wraparound avec `kill -9` : le postmaster redémarrerait immédiatement le cluster et relancerait le worker !',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - autovacuum_freeze_max_age',
    },
  },
  {
    id: 'fc-pgedb-dom04-046',
    cardNumber: 46,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Seuil d\'arrêt d\'urgence : "Database is not accepting commands"',
    difficulty: 'hard',
    tags: ['Emergency', 'Database Shutdown', 'Safety Limit', 'Disaster Recovery'],
    front: {
      question: 'À quel seuil exact avant les 2 milliards de transactions PostgreSQL coupe-t-il les connexions en écriture pour se protéger ?',
      hint: 'Il reste exactement 10 millions ou 1 million de transactions avant le wraparound.',
    },
    back: {
      answer: 'PostgreSQL applique deux seuils d\'alerte et d\'arrêt stricts :\n- **À 40 millions de transactions** avant wraparound : Avertissements quotidiens dans les logs système (`WARNING: database "x" must be vacuumed within Y transactions`).\n- **À 10 millions de transactions** avant wraparound : Les avertissements deviennent critiques et s\'affichent à chaque transaction.\n- **À 1 million de transactions restant** : PostgreSQL **refuse toute nouvelle commande** et s\'arrête avec l\'erreur fatale :\n`ERROR: database is not accepting commands to avoid wraparound data loss in database "..."`.',
      explanation: 'À ce stade, l\'instance doit être démarrée en mode mono-utilisateur (`postgres --single`) pour exécuter un `VACUUM FREEZE` manuel.',
      examTrap: 'Seul un superutilisateur ou le mode mono-utilisateur peut alors se connecter pour lancer la maintenance de sauvetage.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - Preventing Transaction ID Wraparound Failures',
    },
  },
  {
    id: 'fc-pgedb-dom04-047',
    cardNumber: 47,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Surveillance de l\'âge des XID par base de données (`pg_database`)',
    difficulty: 'medium',
    tags: ['pg_database', 'Monitoring', 'age()', 'XID Wraparound'],
    front: {
      question: 'Quelle requête SQL permet de surveiller l\'âge de transaction de chaque base de données par rapport au risque de wraparound ?',
      codeSnippet: 'SELECT datname, age(datfrozenxid) FROM pg_database ORDER BY 2 DESC;',
      hint: 'Fonction age(datfrozenxid) sur le catalogue pg_database.',
    },
    back: {
      answer: 'La fonction **`age(datfrozenxid)`** sur `pg_database` retourne le nombre exact de transactions écoulées depuis le plus ancien XID non gelé dans chaque base de données :\n```sql\nSELECT datname, \n       age(datfrozenxid) AS xid_age,\n       2000000000 - age(datfrozenxid) AS tx_remaining\nFROM pg_database\nORDER BY age(datfrozenxid) DESC;\n```\nSi `xid_age` dépasse 200 millions, la base entre en zone de vigilance autovacuum. Si elle approche 1,5 à 2 milliards, la situation est critique.',
      explanation: 'Cette métrique doit impérativement faire l\'objet d\'une alerte de supervision (Prometheus/Grafana, Nagios, Zabbix).',
      examTrap: 'Ne pas surveiller `datminmxid` (MultiXact ID) est une erreur courante : les MultiXact possèdent aussi leur propre wraparound indépendant !',
      ruleRef: 'PostgreSQL 16 Documentation - System Catalogs - pg_database',
    },
  },
  {
    id: 'fc-pgedb-dom04-048',
    cardNumber: 48,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Surveillance de l\'âge des tables dans `pg_class`',
    difficulty: 'medium',
    tags: ['pg_class', 'relfrozenxid', 'Monitoring', 'SQL Query'],
    front: {
      question: 'Comment trouver les 5 tables les plus âgées d\'une base de données susceptibles de déclencher un autovacuum anti-wraparound ?',
      hint: 'Interrogation de pg_class avec age(relfrozenxid) pour les tables ordinaires (relkind = \'r\').',
    },
    back: {
      answer: 'En interrogeant le catalogue `pg_class` avec la fonction `age(relfrozenxid)` :\n```sql\nSELECT relname, \n       relnamespace::regnamespace AS schema,\n       age(relfrozenxid) AS table_xid_age,\n       pg_size_pretty(pg_total_relation_size(oid)) AS taille\nFROM pg_class\nWHERE relkind IN (\'r\', \'t\') -- tables ordinaires et tables TOAST\nORDER BY age(relfrozenxid) DESC\nLIMIT 5;\n```',
      explanation: 'Inclure les tables TOAST (`relkind = \'t\'`) est crucial car une table TOAST volumineuse peut bloquer l\'avancée du XID d\'une base entière.',
      examTrap: 'Oublier les tables TOAST dans la surveillance : une table parent peut avoir un âge bas alors que sa table TOAST associée frôle le wraparound !',
      ruleRef: 'PostgreSQL 16 Documentation - System Catalogs - pg_class',
    },
  },
  {
    id: 'fc-pgedb-dom04-049',
    cardNumber: 49,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Wraparound des MultiXact IDs (mxid)',
    difficulty: 'hard',
    tags: ['MultiXact', 'mxid', 'SELECT FOR SHARE', 'Wraparound'],
    front: {
      question: 'Qu\'est-ce qu\'un MultiXact ID et pourquoi possède-t-il son propre mécanisme de wraparound (`autovacuum_multixact_freeze_max_age`) ?',
      hint: 'Verrous partagés concurrents posés par plusieurs transactions sur une même ligne.',
    },
    back: {
      answer: 'Le champ `xmax` d\'un tuple ne peut stocker qu\'un seul identifiant de transaction. Si plusieurs transactions posent un verrou partagé concurrent sur la même ligne (ex: `SELECT ... FOR SHARE`), PostgreSQL crée un **MultiXact ID (mxid)** qui pointe vers un tableau de XIDs dans le sous-répertoire `$PGDATA/pg_multixact`.\n\nComme les XIDs, les mxids sont stockés sur 32 bits et sont soumis au wraparound circulaire à 2^31. Le paramètre **`autovacuum_multixact_freeze_max_age`** (défaut : 400 millions) force un freeze des MultiXacts anciens.',
      explanation: 'Observé via `datminmxid` dans `pg_database` et `relminmxid` dans `pg_class`.',
      examTrap: 'Des applications abusant de `SELECT FOR SHARE` ou de clés étrangères concurrentes peuvent déclencher des arrêts d\'urgence pour mxid wraparound sans que le compteur XID normal ne soit élevé.',
      ruleRef: 'PostgreSQL 16 Documentation - Routine Vacuuming - MultiXact Wraparound',
    },
  },
  {
    id: 'fc-pgedb-dom04-050',
    cardNumber: 50,
    domainId: 'pg-edb-dom-04',
    domainCode: 'DOM-04',
    domainTitle: 'Maintenance, MVCC, VACUUM, ANALYZE & Réindexation',
    subtopic: 'Dépannage d\'urgence : Le mode mono-utilisateur (`postgres --single`)',
    difficulty: 'hard',
    tags: ['Single User Mode', 'Disaster Recovery', 'Wraparound', 'CLI'],
    front: {
      question: 'Si le cluster est totalement bloqué par sécurité anti-wraparound (`database is not accepting commands`), quelle commande système exacte permet au DBA de réparer la situation ?',
      hint: 'Arrêt de l\'instance puis démarrage direct du binaire postgres avec le flag --single.',
    },
    back: {
      answer: 'La procédure de sauvetage officielle consiste à :\n1. Arrêter le service PostgreSQL (`pg_ctl stop` ou `systemctl stop postgresql`).\n2. Démarrer l\'exécutable `postgres` directement en **mode mono-utilisateur** sur la base bloquée :\n   ```bash\n   postgres --single -D /var/lib/postgresql/16/data nom_base\n   ```\n3. Dans l\'interpréteur interactif mono-utilisateur, lancer un vacuum agressif complet :\n   ```sql\n   backend> VACUUM (FREEZE, VERBOSE);\n   ```\n4. Quitter (Ctrl+D) et redémarrer le service PostgreSQL normalement.',
      explanation: 'En mode mono-utilisateur, la vérification de sécurité anti-wraparound est suspendue pour permettre au superutilisateur d\'exécuter le freeze salvateur.',
      examTrap: 'Ne jamais lancer le mode mono-utilisateur tant que le serveur PostgreSQL normal tourne sur le même répertoire de données `$PGDATA` sous peine de corruption irréversible.',
      ruleRef: 'PostgreSQL 16 Documentation - Server Applications - postgres (Single-User Mode)',
    },
  },
];
