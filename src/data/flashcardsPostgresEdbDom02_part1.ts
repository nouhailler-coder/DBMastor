import { FlashcardItem } from '../types';

export const postgresEdbDom02Part1Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 1 : MÉTHODES D'INSTALLATION, PAQUETAGES OS & DÉPÔTS EDB / PGDG (Cartes 1 à 10)
  // Paquets PGDG, EDB Repositories, compilation sources, utilisateur postgres, systemd
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-001',
    cardNumber: 1,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Dépôts officiels PGDG (PostgreSQL Global Development Group)',
    difficulty: 'easy',
    tags: ['Installation', 'PGDG', 'APT', 'YUM', 'Linux'],
    front: {
      question: 'Qu\'est-ce que le dépôt PGDG et pourquoi est-il fortement recommandé de l\'utiliser plutôt que les dépôts natifs des distributions Linux ?',
      hint: 'Les dépôts natifs des distributions comme RHEL ou Ubuntu fournissent souvent des versions obsolètes avec un support limité.',
    },
    back: {
      answer: 'Le dépôt PGDG (PostgreSQL Global Development Group) est le référentiel officiel maintenu directement par la communauté PostgreSQL pour les distributions Linux (Debian/Ubuntu via apt.postgresql.org et RHEL/Rocky/Alma/Fedora via yum.postgresql.org).\nIl est recommandé car il garantit l\'accès aux versions majeures les plus récentes (ex: PG 16), à toutes les mises à jour mineures de sécurité dès leur sortie, ainsi qu\'à un écosystème complet d\'extensions (PostGIS, pg_stat_statements, pgvector, repmgr).',
      explanation: 'Les dépôts standards des OS figent généralement une version de PostgreSQL pour toute la durée de vie de la distribution (ex: PG 12 sur Ubuntu 20.04), empêchant de bénéficier des optimisations et correctifs de PostgreSQL 16.',
      examTrap: 'Ne confondez pas le paquet de base fourni par la distribution (ex: paquet postgresql sous Debian qui pointe vers une version figée) avec les paquets versionnés officiels PGDG (ex: postgresql-16).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 17: Installation from Binary Packages',
    },
  },
  {
    id: 'fc-pgedb-dom02-002',
    cardNumber: 2,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Dépôts EnterpriseDB (EDB Repositories) & EPAS',
    difficulty: 'medium',
    tags: ['EDB', 'EPAS', 'Repositories', 'Installation', 'Enterprise'],
    front: {
      question: 'Comment configure-t-on l\'accès aux dépôts logiciels EnterpriseDB (EDB) pour installer EDB Postgres Advanced Server (EPAS) ou les outils d\'entreprise EDB ?',
      codeSnippet: '$ curl -1sLf "https://downloads.enterprisedb.com/.../setup.rpm.sh" | sudo -E bash',
      hint: 'Un script de bootstrap ou un paquet rpm/deb de configuration de dépôt est requis, associé à un token ou abonnement EDB.',
    },
    back: {
      answer: 'Pour installer EDB Postgres Advanced Server (EPAS) ou les outils EDB (EDB Postgres Enterprise Manager, Barman, Failover Manager) :\n1. Obtenir un token d\'accès ou compte sur EnterpriseDB Downloads.\n2. Exécuter le script de configuration officiel d\'EDB (edb-repo setup) ou installer le paquet de release `edb-repo`.\n3. Le gestionnaire de paquets de l\'OS (dnf/yum ou apt) intègre alors les dépôts sécurisés EDB pour télécharger les binaires certifiés.',
      explanation: 'EDB fournit des versions certifiées enrichies de PostgreSQL intégrant des fonctionnalités spécifiques d\'entreprise, notamment la compatibilité native avec la syntaxe et le code PL/SQL Oracle, ainsi que des contrôles d\'audit stricts (EDBAudit).',
      examTrap: 'Sur l\'examen EDB Associate, retenez qu\'EPAS nécessite ses propres dépôts ou installateurs graphiques/silencieux d\'EDB, distincts des dépôts communautaires PGDG.',
      ruleRef: 'EDB Postgres Advanced Server Installation Guide - Using the EDB Repositories',
    },
  },
  {
    id: 'fc-pgedb-dom02-003',
    cardNumber: 3,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Typologie des paquets logiciels PostgreSQL (RHEL vs Debian)',
    difficulty: 'medium',
    tags: ['Packages', 'Contrib', 'Devel', 'Client', 'Server'],
    front: {
      question: 'Quels sont les paquets essentiels à installer sur un serveur de base de données PostgreSQL 16 sous Linux RHEL/Rocky (famille RPM) ?',
      hint: 'Il existe une séparation entre le moteur serveur, les outils clients, les extensions additionnelles et les fichiers d\'en-tête de développement.',
    },
    back: {
      answer: 'Sous RHEL/Rocky/AlmaLinux (famille RPM PGDG), les paquets clés sont :\n1. `postgresql16-server` : Contient le démon serveur postgres, initdb et les fichiers de service systemd.\n2. `postgresql16` : Contient les utilitaires clients (psql, pg_dump, pg_restore, pg_basebackup).\n3. `postgresql16-contrib` : Contient les extensions communautaires officielles (pg_stat_statements, pgcrypto, uuid-ossp, hstore).\n4. `postgresql16-libs` : Bibliothèques partagées dynamiques (libpq).\n5. `postgresql16-devel` : En-têtes C pour compiler des extensions tierces.',
      explanation: 'Sur Debian/Ubuntu, les conventions de nommage équivalentes sont `postgresql-16`, `postgresql-client-16`, `postgresql-contrib` et `postgresql-server-dev-16`.',
      examTrap: 'Installer uniquement `postgresql16` n\'installe que les outils clients ! Pour héberger une base de données opérationnelle, le paquet `postgresql16-server` est impératif.',
      ruleRef: 'PostgreSQL 16 Community Packaging Guidelines & EDB Certified Associate Syllabus',
    },
  },
  {
    id: 'fc-pgedb-dom02-004',
    cardNumber: 4,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Compte système OS et groupe `postgres`',
    difficulty: 'easy',
    tags: ['Security', 'OS Account', 'User', 'Permissions'],
    front: {
      question: 'Pourquoi l\'installation de PostgreSQL crée-t-elle systématiquement un utilisateur et un groupe système dédié nommé `postgres` sur le système d\'exploitation ?',
      hint: 'PostgreSQL applique le principe de moindre privilège et refuse formellement de tourner avec les privilèges root.',
    },
    back: {
      answer: 'L\'utilisateur système `postgres` est créé pour :\n1. Exécuter les processus PostgreSQL avec des privilèges restreints (moindre privilège).\n2. Isoler les fichiers de base de données (propriété exclusive des répertoires $PGDATA avec permissions 0700/0750).\n3. Empêcher l\'exécution en tant que superutilisateur système `root`. PostgreSQL refuse catégoriquement de démarrer sous le compte root pour des raisons évidentes de sécurité.',
      explanation: 'Par défaut sur les distributions Linux durcies, le compte système `postgres` a son mot de passe désactivé ou est configuré avec un shell restreint ; les administrateurs y accèdent via `sudo -u postgres -i` ou `su - postgres`.',
      examTrap: 'Si vous tentez d\'exécuter `postgres -D ...` ou `pg_ctl start` directement en tant que `root`, la commande échoue immédiatement avec le message d\'erreur : "Execution of PostgreSQL by a user with administrative rights is not permitted".',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3: Starting the Database Server',
    },
  },
  {
    id: 'fc-pgedb-dom02-005',
    cardNumber: 5,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Variables d\'environnement système (PATH, PGDATA, LD_LIBRARY_PATH)',
    difficulty: 'medium',
    tags: ['Environment Variables', 'PATH', 'PGDATA', 'Bash'],
    front: {
      question: 'Quelles sont les variables d\'environnement Linux fondamentales à configurer dans le profil du compte `postgres` (~/.bash_profile ou ~/.bashrc) ?',
      codeSnippet: 'export PATH=/usr/pgsql-16/bin:$PATH\nexport PGDATA=/var/lib/pgsql/16/data\nexport PGDATABASE=postgres',
      hint: 'Elles évitent de saisir le chemin absolu des binaires et permettent aux commandes comme pg_ctl d\'identifier le cluster par défaut.',
    },
    back: {
      answer: 'Les variables d\'environnement essentielles sont :\n1. `PATH` : Doit inclure le répertoire des binaires PostgreSQL (ex: `/usr/pgsql-16/bin` sous RHEL ou `/usr/lib/postgresql/16/bin` sous Debian) pour pouvoir appeler `psql`, `pg_ctl`, `initdb`, `pg_dump` sans chemin absolu.\n2. `PGDATA` : Spécifie l\'emplacement par défaut du cluster de données (évite de devoir passer `-D /path/to/data` à chaque commande `pg_ctl` ou `initdb`).\n3. `LD_LIBRARY_PATH` : (si installation hors chemins standards) Pour localiser les bibliothèques partagées `libpq.so`.',
      explanation: 'D\'autres variables clientes très utiles incluent `PGPORT` (ex: 5432), `PGUSER` (ex: postgres) et `PGDATABASE` (base par défaut lors de l\'exécution de `psql`).',
      examTrap: 'Si `PGDATA` n\'est pas positionné dans l\'environnement, `pg_ctl start` ou `pg_ctl status` renverra une erreur demandant explicitement de spécifier l\'option `-D`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 34: libpq - Environment Variables',
    },
  },
  {
    id: 'fc-pgedb-dom02-006',
    cardNumber: 6,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Gestion du service PostgreSQL avec systemd (systemctl)',
    difficulty: 'easy',
    tags: ['systemd', 'systemctl', 'Service', 'Service Management'],
    front: {
      question: 'Quelles sont les commandes `systemctl` usuelles pour démarrer, arrêter, recharger et activer PostgreSQL 16 au démarrage de l\'OS ?',
      hint: 'Le nom de l\'unité systemd est généralement postgresql-16 (RHEL) ou postgresql (Debian/Ubuntu).',
    },
    back: {
      answer: 'Sous RHEL/Rocky Linux (nom d\'unité `postgresql-16`) :\n- Démarrage : `sudo systemctl start postgresql-16`\n- Arrêt : `sudo systemctl stop postgresql-16`\n- Rechargement de configuration (SIGHUP) : `sudo systemctl reload postgresql-16`\n- Activation au boot : `sudo systemctl enable postgresql-16`\n- État en direct : `sudo systemctl status postgresql-16`',
      explanation: 'L\'utilisation de systemd est la méthode de gestion recommandée en production pour garantir le redémarrage automatique en cas de panne de l\'OS et la supervision des cgroups et limites système (ulimit/LimitNOFILE).',
      examTrap: 'Sur Debian/Ubuntu, le nom du service global est simplement `postgresql` ou `postgresql@16-main.service` pour cibler le cluster spécifique `main`.',
      ruleRef: 'EDB Postgres Certified Associate Study Guide - Chapter: Service Administration',
    },
  },
  {
    id: 'fc-pgedb-dom02-007',
    cardNumber: 7,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Divergence d\'arborescence de fichiers : Debian/Ubuntu vs RHEL/CentOS',
    difficulty: 'hard',
    tags: ['Filesystem', 'Debian', 'RHEL', 'Directories', 'Architecture'],
    front: {
      question: 'Où sont stockés les fichiers de données ($PGDATA) et les fichiers de configuration (postgresql.conf) sous RHEL/Rocky par rapport à Debian/Ubuntu ?',
      hint: 'Debian sépare traditionnellement la configuration dans /etc/ et les données dans /var/lib/, tandis que RHEL regroupe tout dans le même répertoire.',
    },
    back: {
      answer: '1. RHEL / Rocky / CentOS (PGDG standard) :\n- Données ($PGDATA) : `/var/lib/pgsql/16/data`\n- Fichiers de configuration : Directement à l\'intérieur de `$PGDATA` (`/var/lib/pgsql/16/data/postgresql.conf` et `pg_hba.conf`).\n\n2. Debian / Ubuntu (postgresql-common) :\n- Données ($PGDATA) : `/var/lib/postgresql/16/main`\n- Fichiers de configuration : Séparés dans `/etc/postgresql/16/main/` (`postgresql.conf` et `pg_hba.conf` se trouvent dans `/etc/`).',
      explanation: 'Sous Debian, le script wrapper `postgresql-common` gère une abstraction multi-clusters et configure la variable `data_directory = \'/var/lib/postgresql/16/main\'` à l\'intérieur du fichier `/etc/postgresql/16/main/postgresql.conf`.',
      examTrap: 'Sur Debian/Ubuntu, modifier `postgresql.conf` à l\'intérieur de `/var/lib/postgresql/16/main/` n\'aura aucun effet car le serveur lit celui présent dans `/etc/postgresql/16/main/` !',
      ruleRef: 'Debian PostgreSQL packaging documentation & PostgreSQL 16 Docs Chapter 19.2',
    },
  },
  {
    id: 'fc-pgedb-dom02-008',
    cardNumber: 8,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Outils de gestion multi-clusters Debian/Ubuntu (pg_createcluster)',
    difficulty: 'medium',
    tags: ['pg_createcluster', 'pg_lsclusters', 'Debian', 'Ubuntu', 'Multi-Cluster'],
    front: {
      question: 'Quelles sont les commandes fournies par le paquet `postgresql-common` sous Debian/Ubuntu pour gérer plusieurs clusters sur un même serveur ?',
      hint: 'Ce sont des scripts wrappers préfixés par pg_*.',
    },
    back: {
      answer: 'Les utilitaires spécifiques Debian/Ubuntu sont :\n1. `pg_lsclusters` : Liste tous les clusters locaux avec leur version, nom, port, statut, propriétaire et chemins de données/logs.\n2. `pg_createcluster <version> <nom>` : Initialise un nouveau cluster et ses configurations (ex: `pg_createcluster 16 analytics`).\n3. `pg_dropcluster <version> <nom>` : Supprime un cluster et ses données de façon permanente.\n4. `pg_ctlcluster <version> <nom> <action>` : Démarre, arrête ou recharge un cluster précis (ex: `pg_ctlcluster 16 main start`).',
      explanation: 'Ces outils automatisent l\'allocation automatique des ports TCP pour éviter les collisions (ex: 5432 pour le 1er cluster, 5433 pour le suivant) et créent l\'arborescence standardisée dans `/etc` et `/var/lib`.',
      examTrap: 'Ces commandes sont exclusives à Debian et Ubuntu. Elles n\'existent pas nativement sous RHEL/Rocky Linux (sauf si vous installez manuellement le portage de postgresql-common).',
      ruleRef: 'Manpage pg_createcluster(8), pg_lsclusters(1)',
    },
  },
  {
    id: 'fc-pgedb-dom02-009',
    cardNumber: 9,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Compilation depuis les sources (./configure, make)',
    difficulty: 'hard',
    tags: ['Compilation', 'Sources', 'configure', 'make', 'Installation'],
    front: {
      question: 'Quelles sont les trois étapes canoniques pour compiler et installer PostgreSQL à partir du code source, et quelle option de `./configure` définit le répertoire d\'installation ?',
      codeSnippet: '$ ./configure --prefix=/opt/pgsql-16 --with-openssl\n$ make\n$ sudo make install',
      hint: 'L\'option standard GNU définit le préfixe de base de l\'arborescence des fichiers installés.',
    },
    back: {
      answer: 'Les trois étapes sont :\n1. Configuration : `./configure --prefix=<chemin>` (vérifie les dépendances système, les bibliothèques C, et génère les Makefiles).\n2. Compilation : `make` (ou `make -j $(nproc)` pour accélérer la compilation en parallèle).\n3. Installation : `sudo make install` (copie les binaires compilés, bibliothèques et documentations dans le répertoire cible).\n\nL\'option `--prefix` (ex: `--prefix=/opt/pgsql-16`) définit la racine d\'installation.',
      explanation: 'Pour installer également les extensions du répertoire contrib, on exécute `make -C contrib` suivi de `make -C contrib install`. On peut aussi tester la conformité avec `make check`.',
      examTrap: 'Par défaut, si `--prefix` n\'est pas spécifié, `./configure` installe PostgreSQL dans `/usr/local/pgsql`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 16: Installation from Source Code',
    },
  },
  {
    id: 'fc-pgedb-dom02-010',
    cardNumber: 10,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Mise à jour mineure vs Mise à jour majeure',
    difficulty: 'easy',
    tags: ['Upgrade', 'Minor Version', 'Major Version', 'Compatibility'],
    front: {
      question: 'Quelle est la différence fondamentale entre une mise à jour mineure (ex: 16.1 vers 16.2) et une mise à jour majeure (ex: 15.x vers 16.x) en termes de compatibilité physique des données ?',
      hint: 'Les versions mineures garantissent une compatibilité binaire à 100% au niveau du format disque.',
    },
    back: {
      answer: '1. Mise à jour mineure (ex: 16.1 -> 16.2) :\n- Compatibilité binaire descendante garantie à 100% pour les fichiers disques et catalogues.\n- Ne nécessite AUCUNE migration de données : il suffit de remplacer les exécutables/paquets et de redémarrer le service.\n\n2. Mise à jour majeure (ex: 15.x -> 16.x) :\n- Le format de stockage interne, le catalogue système et les structures binaires changent.\n- Nécessite obligatoirement une migration physique via `pg_upgrade` (rapide avec l\'option `--link`) ou logique via `pg_dumpall | psql`.',
      explanation: 'Depuis la version 10, la politique de versioning de PostgreSQL utilise deux chiffres : le premier chiffre est la version majeure (14, 15, 16), le second est la version mineure de correctifs (16.1, 16.2).',
      examTrap: 'Ne tentez jamais de démarrer des binaires PostgreSQL 16 directement sur un répertoire de données ($PGDATA) initialisé en PostgreSQL 15 : le serveur refusera immédiatement de démarrer en signalant une incompatibilité de version du catalogue.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.6: Upgrading a PostgreSQL Cluster',
    },
  },

  // =========================================================================
  // SECTION 2 : INITIALISATION D'UN CLUSTER AVEC `initdb` (Cartes 11 à 20)
  // Options initdb, encodage UTF-8, locales, collations ICU, data checksums, waldir
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-011',
    cardNumber: 11,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Rôle et syntaxe fondamentale de la commande `initdb`',
    difficulty: 'easy',
    tags: ['initdb', 'PGDATA', 'Cluster Creation', 'Template Databases'],
    front: {
      question: 'Que réalise concrètement la commande `initdb` et quelles sont les bases de données créées automatiquement par défaut ?',
      codeSnippet: '$ initdb -D /var/lib/pgsql/16/data',
      hint: 'Elle initialise le catalogue système et crée trois bases de données de départ.',
    },
    back: {
      answer: 'La commande `initdb` initialise un nouveau cluster de bases de données PostgreSQL (une instance physique). Elle :\n1. Crée l\'arborescence de répertoires sous `$PGDATA`.\n2. Initialise les catalogues système (`pg_catalog`) et le sous-système de transactions.\n3. Crée les 3 bases de données par défaut :\n   - `template0` : Modèle de référence vierge, jamais modifié, réservé au système.\n   - `template1` : Modèle de travail cloné lors de chaque `CREATE DATABASE`.\n   - `postgres` : Base de données par défaut pour l\'administration, les utilisateurs et les utilitaires.',
      explanation: 'Un "cluster" dans la terminologie PostgreSQL ne désigne pas un ensemble de machines en réseau, mais une collection de bases de données gérées par une unique instance PostgreSQL sur une même machine.',
      examTrap: 'Ne confondez pas un cluster PostgreSQL (instance serveur locale avec ses bases) avec un cluster de haute disponibilité ou de basculement.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: initdb',
    },
  },
  {
    id: 'fc-pgedb-dom02-012',
    cardNumber: 12,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Sélection de l\'encodage des caractères (`-E` / `--encoding`)',
    difficulty: 'medium',
    tags: ['initdb', 'Encoding', 'UTF-8', 'Character Set', 'SQL_ASCII'],
    front: {
      question: 'Pourquoi est-il primordial de spécifier l\'encodage UTF8 lors de l\'exécution d\'`initdb` avec l\'option `-E` ou `--encoding` ?',
      codeSnippet: '$ initdb -D /data/pg16 -E UTF8',
      hint: 'Si rien n\'est précisé, l\'encodage est hérité des variables de locale de l\'environnement OS, ce qui peut poser des problèmes irréversibles.',
    },
    back: {
      answer: 'Spécifier `-E UTF8` (ou `--encoding=UTF8`) garantit que le cluster et ses bases modèles (`template0`, `template1`) utiliseront le jeu de caractères universel Unicode UTF-8.\nSi l\'encodage n\'est pas spécifié, `initdb` utilise l\'encodage déduit de la locale de l\'OS. Si l\'OS est configuré en `C` ou `POSIX`, le cluster est créé en `SQL_ASCII`.\nOr, l\'encodage d\'un cluster et de `template1` ne peut pas être modifié après coup !',
      explanation: 'En `SQL_ASCII`, PostgreSQL stocke les octets bruts sans aucune validation de caractères ni conversion de jeu, ce qui expose à des corruptions d\'affichage et empêche le tri multilingue correct.',
      examTrap: 'Une base créée avec `CREATE DATABASE` ne peut pas avoir un encodage différent de `template1` à moins de cloner `template0` avec un encodage compatible.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 24.3: Character Set Support',
    },
  },
  {
    id: 'fc-pgedb-dom02-013',
    cardNumber: 13,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Paramètres de localisation et de collation (`--locale`, `LC_COLLATE`)',
    difficulty: 'medium',
    tags: ['initdb', 'Locale', 'LC_COLLATE', 'LC_CTYPE', 'Sorting'],
    front: {
      question: 'Quel est l\'impact du choix de la locale (`--locale`, `--lc-collate`, `--lc-ctype`) lors de l\'exécution d\'`initdb` sur les performances et le comportement des requêtes ?',
      hint: 'Cela influence directement l\'ordre de tri des index B-tree et la possibilité d\'optimiser les clauses LIKE.',
    },
    back: {
      answer: 'Le paramètre de locale détermine :\n1. `LC_COLLATE` : L\'ordre alphabétique de tri des chaînes de caractères (ex: prise en compte des accents, majuscules/minuscules) utilisé par les clauses `ORDER BY` et les index B-tree standard.\n2. `LC_CTYPE` : La classification des caractères (qu\'est-ce qu\'une lettre, un chiffre, une majuscule pour `UPPER()` et `LOWER()`).\n\nImpact majeur sur les performances :\nSous une locale linguistique (ex: `fr_FR.UTF-8` ou `en_US.UTF-8`), un index B-tree standard ne peut PAS être utilisé pour accélérer une recherche par préfixe `WHERE nom LIKE \'DUPONT%\'` sans utiliser un opérateur d\'index spécial (`varchar_pattern_ops`). Avec `--locale=C`, le tri est binaire pur et les index standards fonctionnent avec `LIKE`.',
      explanation: 'La locale `C` (ou `POSIX`) offre les meilleures performances brutes de tri et de création d\'index, mais ne respecte pas les règles d\'ordre alphabétique spécifiques aux langues (ex: é après e).',
      examTrap: 'Une modification de la bibliothèque libc de l\'OS peut modifier l\'ordre de tri d\'une locale glibc, entraînant une corruption silencieuse des index B-tree existants.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 24.1: Locale Support',
    },
  },
  {
    id: 'fc-pgedb-dom02-014',
    cardNumber: 14,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Fournisseur de collation ICU (`--locale-provider=icu`) sous PG 15/16',
    difficulty: 'hard',
    tags: ['initdb', 'ICU', 'Locale Provider', 'PostgreSQL 16', 'Corruptions'],
    front: {
      question: 'Quelle nouveauté majeure PostgreSQL 15/16 apporte l\'option `--locale-provider=icu` lors de l\'exécution d\'`initdb` ?',
      codeSnippet: '$ initdb -D /data/pg16 --locale-provider=icu --icu-locale=fr-FR -E UTF8',
      hint: 'Elle permet de s\'affranchir des variations imprévisibles de la bibliothèque glibc du système d\'exploitation hôte.',
    },
    back: {
      answer: 'Sous PostgreSQL 16, l\'option `--locale-provider=icu` permet d\'utiliser la bibliothèque universelle ICU (International Components for Unicode) comme moteur de collation par défaut du cluster à la place de la bibliothèque C standard (`libc`).\nAvantages majeurs :\n1. Indépendance totale par rapport à la glibc de l\'OS hôte.\n2. Cohérence de tri identique quel que soit le système d\'exploitation (Linux, macOS, Windows).\n3. Évite les corruptions d\'index B-tree lors des montées de version d\'OS (mises à niveau de glibc).\n4. Détection et avertissement automatique en cas de changement de version de collation ICU (`ALTER COLLATION ... REFRESH VERSION`).',
      explanation: 'Jusqu\'à PostgreSQL 14, ICU était disponible au niveau table ou colonne, mais le cluster devait être initialisé avec libc. Depuis PG 15 et consolidé en PG 16, ICU peut être le fournisseur global du cluster.',
      examTrap: 'Si le serveur hôte n\'a pas la bibliothèque `libicu` installée et compilée avec PostgreSQL, `initdb` refusera l\'option `--locale-provider=icu`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 24.2: Collation Support - ICU Provider',
    },
  },
  {
    id: 'fc-pgedb-dom02-015',
    cardNumber: 15,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Sommes de contrôle des blocs de données (`-k` / `--data-checksums`)',
    difficulty: 'medium',
    tags: ['initdb', 'Checksums', 'Data Integrity', 'Corruption', 'Bit Rot'],
    front: {
      question: 'À quoi sert l\'option `-k` (ou `--data-checksums`) de la commande `initdb` et pourquoi est-elle indispensable en production ?',
      codeSnippet: '$ initdb -D /var/lib/pgsql/16/data -k',
      hint: 'Elle calcule et vérifie une empreinte de chaque page de 8 Ko écrite sur disque pour détecter le "bit rot" ou les pannes de stockage.',
    },
    back: {
      answer: 'L\'option `-k` active le calcul de sommes de contrôle (checksums) sur chaque page de données (bloc de 8 Ko) lors de son écriture sur disque.\nÀ chaque relecture d\'une page dans `shared_buffers`, PostgreSQL recalcule la somme de contrôle et la compare à celle stockée dans l\'en-tête de page.\nSi une différence est constatée (due à une défaillance de contrôleur disque, bit rot, panne matérielle ou corruption de cache), PostgreSQL lève immédiatement une erreur critique et empêche la propagation silencieuse de données invalides.',
      explanation: 'Le surcoût CPU est minime (généralement inférieur à 2-3% sur les processeurs modernes disposant d\'instructions vectorielles) alors que la garantie d\'intégrité est inestimable pour une base de production.',
      examTrap: 'Depuis PostgreSQL 12, les checksums peuvent être activés après coup à froid via l\'utilitaire `pg_checksums -e -D $PGDATA`, mais activer l\'option dès `initdb -k` est la bonne pratique absolue.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: initdb (-k) & pg_checksums',
    },
  },
  {
    id: 'fc-pgedb-dom02-016',
    cardNumber: 16,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Séparation physique du journal de transactions (`-X` / `--waldir`)',
    difficulty: 'hard',
    tags: ['initdb', 'WAL', 'SSD', 'Disk I/O', 'Performance'],
    front: {
      question: 'Quelle option d\'`initdb` permet de déporter le répertoire des journaux de transactions (WAL) sur un volume de stockage ou disque physique séparé ?',
      codeSnippet: '$ initdb -D /data/pgdata -X /wal/pg_wal -k',
      hint: 'Historiquement nommée --xlogdir, elle crée un lien symbolique transparent entre $PGDATA/pg_wal et le chemin cible.',
    },
    back: {
      answer: 'L\'option est `-X` ou `--waldir=<chemin>`.\nElle permet de placer le répertoire des WAL (historiquement nommé `pg_xlog`, aujourd\'hui `pg_wal`) sur un disque physique dédié (par exemple un SSD NVMe à très faible latence en RAID 1).\nTechniquement, `initdb` crée un lien symbolique (symlink) dans `$PGDATA/pg_wal` pointant vers le répertoire distant spécifié.',
      explanation: 'Isoler les écritures séquentielles synchrones des WAL sur un contrôleur I/O séparé des écritures aléatoires des fichiers de données (`base/`) élimine les contentions disque et améliore considérablement le débit des transactions (TPS).',
      examTrap: 'Ne déplacez pas manuellement le dossier `pg_wal` sans arrêter proprement le serveur et sans recréer le lien symbolique exact avec les bonnes permissions d\'accès.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: initdb (--waldir)',
    },
  },
  {
    id: 'fc-pgedb-dom02-017',
    cardNumber: 17,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Définition du superutilisateur initial (`-U`, `-A`, `-W`)',
    difficulty: 'medium',
    tags: ['initdb', 'Superuser', 'Authentication', 'Security', 'Password'],
    front: {
      question: 'Quelles sont les options d\'`initdb` pour définir un nom de superutilisateur spécifique, exiger la saisie d\'un mot de passe et définir la méthode d\'authentification par défaut ?',
      codeSnippet: '$ initdb -D /data -U admin_edb -A scram-sha-256 -W',
      hint: 'Trois commutateurs permettent de contrôler le nom du rôle (-U), la méthode pg_hba (-A) et la prompt de mot de passe (-W).',
    },
    back: {
      answer: 'Les options sont :\n1. `-U <nom>` (ou `--username`) : Définit le nom du superutilisateur initial (par défaut, c\'est le nom du compte système exécutant `initdb`, souvent `postgres`).\n2. `-A <méthode>` (ou `--auth` ou `--auth-local` / `--auth-host`) : Définit la méthode d\'authentification par défaut inscrite dans le fichier `pg_hba.conf` initial (recommandation moderne : `scram-sha-256`).\n3. `-W` (ou `--pwprompt`) : Demande interactivement à l\'administrateur de saisir un mot de passe fort pour le superutilisateur initial lors de l\'initialisation.',
      explanation: 'Si `-W` n\'est pas spécifié et que la méthode est positionnée sur `trust`, n\'importe quel utilisateur local pourra se connecter sans mot de passe, ce qui présente un risque de sécurité majeur.',
      examTrap: 'Si vous ne spécifiez pas `-A`, `initdb` configure généralement `peer` pour les sockets locaux Unix et `scram-sha-256` pour TCP/IP sous les versions récentes.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: initdb',
    },
  },
  {
    id: 'fc-pgedb-dom02-018',
    cardNumber: 18,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Permissions obligatoires du système de fichiers sur `$PGDATA`',
    difficulty: 'medium',
    tags: ['Permissions', 'Security', 'chmod', '0700', '0750'],
    front: {
      question: 'Quelles sont les permissions Unix strictes exigées par PostgreSQL sur le répertoire `$PGDATA` et que se passe-t-il si les permissions sont trop permissives ?',
      codeSnippet: '$ chmod 0777 /var/lib/pgsql/16/data\n$ pg_ctl start -D /var/lib/pgsql/16/data',
      hint: 'Le serveur refuse formellement de démarrer si d\'autres utilisateurs système ont des droits de lecture ou d\'écriture.',
    },
    back: {
      answer: 'PostgreSQL exige que le répertoire `$PGDATA` appartienne à l\'utilisateur système exécutant le serveur et soit verrouillé avec les droits octaux stricts :\n- `0700` (`drwx------`) : Accès exclusif de lecture, écriture et exécution par le propriétaire uniquement.\n- Ou `0750` (`drwxr-x---`) : Autorisé depuis PG 11 si l\'administrateur souhaite accorder un accès en lecture au groupe système (par exemple pour un compte dédié aux sauvegardes physiques).\n\nSi les permissions sont trop permissives (ex: `0755` ou `0777`), PostgreSQL refuse catégoriquement de démarrer avec l\'erreur fatale : "data directory has invalid permissions (must be 0700 or 0750)".',
      explanation: 'Ce verrouillage de sécurité strict protège les données d\'une lecture ou modification non autorisée par d\'autres comptes système non privilégiés partageant la machine.',
      examTrap: 'Une question classique d\'examen présente un serveur qui refuse de démarrer après une intervention administrateur ayant exécuté un `chmod 777` : la solution est de restaurer immédiatement `chmod 700 $PGDATA`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3: Starting the Database Server',
    },
  },
  {
    id: 'fc-pgedb-dom02-019',
    cardNumber: 19,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Initialisation dans un répertoire existant non vide',
    difficulty: 'easy',
    tags: ['initdb', 'Errors', 'Troubleshooting', 'Directory not empty'],
    front: {
      question: 'Que se produit-il si vous tentez d\'exécuter `initdb -D /chemin/vers/data` alors que le répertoire cible existe déjà et contient des fichiers (ex: lost+found) ?',
      hint: 'initdb est strict et refuse de s\'exécuter pour éviter tout risque d\'écrasement accidentel.',
    },
    back: {
      answer: 'La commande `initdb` échoue immédiatement avec une erreur fatale :\n"initdb: error: directory \'/chemin/vers/data\' exists but is not empty".\n`initdb` refuse formellement d\'initialiser un cluster dans un dossier contenant des fichiers afin d\'éviter d\'écraser des données existantes.',
      explanation: 'Cas classique : lors du montage d\'une nouvelle partition disque formatée en ext4, un répertoire caché `lost+found` est créé à la racine du point de montage. Il est recommandé de créer un sous-dossier (ex: `/data/pg16/data`) appartenant à l\'utilisateur `postgres` et strictement vide avant d\'appeler `initdb`.',
      examTrap: 'Il n\'existe pas d\'option `--force` sous initdb. Le répertoire doit obligatoirement être vidé ou inexistant.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: initdb',
    },
  },
  {
    id: 'fc-pgedb-dom02-020',
    cardNumber: 20,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Vérification post-initialisation avec `pg_controldata`',
    difficulty: 'medium',
    tags: ['pg_controldata', 'initdb', 'Control File', 'Verification'],
    front: {
      question: 'Quelle commande autonome permet d\'inspecter l\'état et la configuration interne d\'un cluster fraîchement initialisé sans démarrer le serveur PostgreSQL ?',
      codeSnippet: '$ pg_controldata /var/lib/pgsql/16/data',
      hint: 'Cet outil lit directement le fichier binaire global/pg_control.',
    },
    back: {
      answer: 'L\'outil est `pg_controldata` (syntaxe : `pg_controldata [option] [DATADIR]`).\nIl lit et formate le fichier binaire `$PGDATA/global/pg_control` pour afficher immédiatement :\n- L\'état du cluster (Database cluster state : shut down, in production, etc.).\n- La version du catalogue système (Catalog version number) et l\'identifiant unique du cluster (Database system identifier).\n- Les paramètres fixés à l\'initialisation : taille de bloc (8192 o), taille de bloc WAL, activation des checksums (Data page checksum version).\n- L\'encodage et les locales configurées.',
      explanation: '`pg_controldata` est un outil de diagnostic inestimable pour les DBA car il fonctionne même lorsque le serveur est arrêté ou incapable de démarrer.',
      examTrap: '`pg_controldata` ne nécessite aucun mot de passe ni connexion réseau : il requiert uniquement les droits de lecture OS sur le fichier `$PGDATA/global/pg_control`.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_controldata',
    },
  },

  // =========================================================================
  // SECTION 3 : ANATOMIE ET STRUCTURE DÉTAILLÉE DU RÉPERTOIRE `$PGDATA` (Cartes 21 à 30)
  // Arborescence, base/, global/, pg_wal/, pg_control, pg_tblspc/, relfilenode, 1GB segment
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-021',
    cardNumber: 21,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Fichiers de configuration situés à la racine de `$PGDATA`',
    difficulty: 'easy',
    tags: ['PGDATA', 'postgresql.conf', 'pg_hba.conf', 'pg_ident.conf', 'Configuration'],
    front: {
      question: 'Quels sont les trois fichiers de configuration textuels fondamentaux situés à la racine d\'un cluster PostgreSQL standard ($PGDATA) ?',
      hint: 'L\'un règle le moteur, le second contrôle l\'accès client et le troisième gère la correspondance d\'identités.',
    },
    back: {
      answer: 'Les trois fichiers de configuration racine sont :\n1. `postgresql.conf` : Paramètres de configuration du moteur (mémoire, connexions, processus, logs, timeouts, réplication).\n2. `pg_hba.conf` : Fichier de sécurité de contrôle d\'accès par hôte (Host-Based Authentication) régissant qui peut se connecter, depuis quelle IP, vers quelle base et avec quelle méthode d\'authentification.\n3. `pg_ident.conf` : Fichier de mappage d\'identités entre les comptes du système d\'exploitation / LDAP / Kerberos et les rôles de base de données.',
      explanation: 'À ces trois fichiers s\'ajoute `postgresql.auto.conf`, généré automatiquement par le serveur lors de l\'utilisation de commandes SQL `ALTER SYSTEM`.',
      examTrap: 'Sur Debian/Ubuntu, ces fichiers sont déportés dans `/etc/postgresql/<version>/<cluster>/`, mais sur toutes les installations standards et RHEL, ils résident directement dans `$PGDATA`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters',
    },
  },
  {
    id: 'fc-pgedb-dom02-022',
    cardNumber: 22,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Fichier spécial `postgresql.auto.conf`',
    difficulty: 'medium',
    tags: ['postgresql.auto.conf', 'ALTER SYSTEM', 'Override', 'Configuration'],
    front: {
      question: 'Quel est le rôle du fichier `postgresql.auto.conf` et pourquoi ne faut-il JAMAIS le modifier manuellement avec un éditeur de texte ?',
      codeSnippet: '# Do not edit this file manually!\n# It will be overwritten by the ALTER SYSTEM command.\nwork_mem = \'64MB\'',
      hint: 'Il est géré exclusivement par une commande SQL dédiée exécutée par le superutilisateur.',
    },
    back: {
      answer: 'Le fichier `postgresql.auto.conf` est créé et mis à jour automatiquement par le serveur lorsque le superutilisateur exécute des commandes SQL `ALTER SYSTEM SET ...` ou `ALTER SYSTEM RESET ...`.\nIl est inclus implicitement à la toute fin de `postgresql.conf` et écrase donc toute valeur préalablement définie dans ce dernier.\n\nIl ne faut pas le modifier manuellement car le serveur peut l\'écraser à tout moment lors d\'un prochain `ALTER SYSTEM`, et une erreur de syntaxe manuelle peut empêcher le serveur de redémarrer.',
      explanation: 'Pour annuler un paramètre défini dans `postgresql.auto.conf`, on doit utiliser la commande SQL `ALTER SYSTEM RESET <paramètre>;` suivie d\'un rechargement de configuration.',
      examTrap: 'Même si vous modifiez un paramètre dans `postgresql.conf`, si celui-ci est présent dans `postgresql.auto.conf`, la valeur de `postgresql.auto.conf` primera toujours !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.5: Alter System',
    },
  },
  {
    id: 'fc-pgedb-dom02-023',
    cardNumber: 23,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Organisation du sous-répertoire `$PGDATA/base/`',
    difficulty: 'medium',
    tags: ['PGDATA', 'base', 'OID', 'relfilenode', 'Storage'],
    front: {
      question: 'Comment les données des tables et des bases de données sont-elles organisées physiquement dans le sous-répertoire `$PGDATA/base/` ?',
      codeSnippet: '$ ls -l $PGDATA/base\ndrwx------ 16384\ndrwx------ 16385',
      hint: 'Chaque base de données possède son propre sous-dossier nommé par son identifiant OID.',
    },
    back: {
      answer: 'Le sous-répertoire `$PGDATA/base/` contient un sous-dossier par base de données appartenant au tablespace par défaut (`pg_default`).\nChaque sous-dossier porte comme nom l\'OID (Object Identifier) de la base de données :\n- Vous pouvez lier le nom de la base à son dossier via la requête : `SELECT oid, datname FROM pg_database;`.\n\nÀ l\'intérieur de chaque sous-dossier de base, les tables, index, toasts et séquences sont stockés sous forme de fichiers individuels nommés d\'après leur `relfilenode` (consultable via `SELECT relname, relfilenode FROM pg_class;`).',
      explanation: 'Cette structure stricte permet d\'isoler physiquement les fichiers de données de chaque base de données.',
      examTrap: 'Attention : le numéro `relfilenode` d\'une table correspond initialement à son `oid`, mais il change après une opération de réécriture comme `VACUUM FULL`, `CLUSTER` ou `TRUNCATE`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 73: Database Physical Storage',
    },
  },
  {
    id: 'fc-pgedb-dom02-024',
    cardNumber: 24,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Segmentation des fichiers de table (Segments de 1 Go)',
    difficulty: 'hard',
    tags: ['Storage', 'Segmentation', '1GB', 'relfilenode', 'File System'],
    front: {
      question: 'Pourquoi une table volumineuse de 50 Go n\'apparaît-elle pas sous la forme d\'un seul fichier dans `$PGDATA/base/<db_oid>/` mais sous plusieurs fichiers numérotés ?',
      codeSnippet: '$ ls -l $PGDATA/base/16384/24589*\n-rw------- 1 postgres postgres 1073741824 24589\n-rw------- 1 postgres postgres 1073741824 24589.1\n-rw------- 1 postgres postgres  524288000 24589.2',
      hint: 'PostgreSQL découpe historiquement les relations en segments de taille fixe de 1 Go.',
    },
    back: {
      answer: 'PostgreSQL segmente automatiquement toute relation (table ou index) en fichiers individuels d\'une taille maximale de 1 Go (1 073 741 824 octets, soit 131 072 blocs de 8 Ko).\nLorsque le premier fichier (`<relfilenode>`) atteint 1 Go, PostgreSQL crée automatiquement le segment suivant nommé `<relfilenode>.1`, puis `<relfilenode>.2`, etc.\n\nRaisons :\n1. Évite les limitations historiques de taille de fichier sur certains systèmes de fichiers (limites à 2 Go sous 32-bit).\n2. Facilite la gestion des copies, du cache OS et des sauvegardes.',
      explanation: 'Cette segmentation est totalement transparente pour le moteur SQL et l\'utilisateur : lors d\'un SELECT ou d\'un balayage, le serveur lit séquentiellement l\'ensemble des segments physiques.',
      examTrap: 'Cette limite de 1 Go (`RELSEG_SIZE`) est compilée en dur dans le moteur. Elle ne peut être modifiée qu\'en recompilant PostgreSQL avec l\'option `--with-segsize`.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 73.1: File Layout',
    },
  },
  {
    id: 'fc-pgedb-dom02-025',
    cardNumber: 25,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Fichiers spéciaux d\'une relation : `_fsm` et `_vm`',
    difficulty: 'hard',
    tags: ['Storage', 'FSM', 'VM', 'Free Space Map', 'Visibility Map'],
    front: {
      question: 'À côté du fichier principal d\'une table, que représentent les fichiers annexes portant les suffixes `_fsm` et `_vm` (ex: `24589_fsm`, `24589_vm`) ?',
      codeSnippet: '-rw------- 1 postgres postgres 1073741824 24589\n-rw------- 1 postgres postgres     262144 24589_fsm\n-rw------- 1 postgres postgres      32768 24589_vm',
      hint: 'L\'un gère l\'espace libre disponible pour les nouvelles insertions, l\'autre accélère les Index-Only Scans et le VACUUM.',
    },
    back: {
      answer: '1. `_fsm` (Free Space Map) :\n- Carte de l\'espace libre de la table.\n- Arbre binaire qui enregistre la quantité d\'espace disponible dans chaque page de données pour permettre à une commande `INSERT` ou `UPDATE` de trouver rapidement une page existante avec de la place disponible sans balayer toute la table.\n\n2. `_vm` (Visibility Map) :\n- Carte de visibilité de la table.\n- Deux bits par page de données : le 1er bit indique si tous les tuples de la page sont visibles par toutes les transactions actives (permet les Index-Only Scans sans visiter la table heap), le 2nd bit indique si la page est entièrement "gelée" (all-frozen pour le vacuum anti-wraparound).',
      explanation: 'Ces structures sont créées et maintenues en mémoire partagée puis synchronisées sur disque par les opérations du moteur et par le processus Autovacuum.',
      examTrap: 'Supprimer manuellement un fichier `_fsm` ou `_vm` ne détruit pas les données de la table mais ralentit drastiquement le serveur le temps que VACUUM les reconstruise.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 73.2: Free Space Map & Chapter 73.3: Visibility Map',
    },
  },
  {
    id: 'fc-pgedb-dom02-026',
    cardNumber: 26,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Contenu du sous-répertoire `$PGDATA/global/`',
    difficulty: 'medium',
    tags: ['PGDATA', 'global', 'pg_control', 'Shared Catalogs', 'Roles'],
    front: {
      question: 'Quelles données critiques sont stockées dans le sous-répertoire `$PGDATA/global/` ?',
      hint: 'Il contient les tables du catalogue partagées par toutes les bases du cluster, ainsi que le fichier de contrôle.',
    },
    back: {
      answer: 'Le sous-répertoire `$PGDATA/global/` stocke :\n1. Les tables du catalogue système partagées par l\'ensemble du cluster (global relations) :\n   - `pg_database` (liste des bases de données du cluster).\n   - `pg_authid` et `pg_auth_members` (utilisateurs, rôles et mots de passe).\n   - `pg_tablespace` (définitions des tablespaces).\n   - `pg_shdescription`, `pg_shseclabel`.\n2. Le fichier de contrôle binaire fondamental de l\'instance : `pg_control`.',
      explanation: 'Contrairement aux relations standard qui sont isolées dans le dossier `base/<db_oid>/`, les tables de `global/` s\'appliquent transversalement à tout le cluster PostgreSQL.',
      examTrap: 'Une corruption dans le répertoire `global/` (et particulièrement de `pg_control`) peut paralyser l\'intégralité du cluster et de toutes les bases de données hébergées.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 73: Database Physical Storage',
    },
  },
  {
    id: 'fc-pgedb-dom02-027',
    cardNumber: 27,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Rôle fondamental du fichier binaire `global/pg_control`',
    difficulty: 'hard',
    tags: ['pg_control', 'Checkpoint', 'Recovery', 'LSN', 'Crash'],
    front: {
      question: 'Pourquoi le fichier `global/pg_control` est-il qualifié de fichier le plus critique de l\'instance PostgreSQL ?',
      hint: 'Il indique au serveur où commencer le rejeu des WAL lors du démarrage et quel est l\'état actuel du cluster.',
    },
    back: {
      answer: 'Le fichier `global/pg_control` conserve l\'état d\'exécution global du cluster. Il enregistre en temps réel :\n1. L\'état du cluster (`Database cluster state` : in production, shut down, in crash recovery).\n2. La position exacte (LSN) du dernier Checkpoint valide.\n3. L\'adresse REDO : position précise dans les WAL à partir de laquelle le rejeu doit débuter en cas de crash recovery.\n4. L\'identifiant de la Timeline active et les informations d\'historique.\n5. L\'identifiant système unique du cluster (System Identifier).\n6. Les paramètres d\'initialisation matériels (block_size, wal_block_size, checksums).',
      explanation: 'Si `pg_control` est supprimé ou corrompu, le serveur ne peut plus démarrer car il ignore quel est le dernier point de cohérence. Seul l\'outil d\'urgence de dernier recours `pg_resetwal` peut synthétiser un `pg_control` factice, avec un risque de perte de cohérence.',
      examTrap: 'Ne tentez jamais d\'éditer `pg_control` avec un éditeur de texte standard : c\'est un fichier binaire strict de structure C interne (`ControlFileData`).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 28.1: Reliability & WAL',
    },
  },
  {
    id: 'fc-pgedb-dom02-028',
    cardNumber: 28,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Organisation du sous-répertoire `$PGDATA/pg_wal/` et `archive_status/`',
    difficulty: 'medium',
    tags: ['pg_wal', 'archive_status', 'WAL', 'Segments', 'Archiving'],
    front: {
      question: 'Quelle est la structure des fichiers dans `$PGDATA/pg_wal/` et à quoi sert le sous-dossier `archive_status/` ?',
      codeSnippet: '$ ls $PGDATA/pg_wal\n000000010000000000000001  000000010000000000000002  archive_status',
      hint: 'Le sous-dossier contient de petits fichiers indicateurs .ready et .done utilisés par le processus d\'archivage.',
    },
    back: {
      answer: 'Dans `$PGDATA/pg_wal/` :\n1. Les fichiers WAL de 16 Mo portent un nom hexadécimal à 24 caractères représentant la Timeline (8 caractères), le numéro logique de segment (8 caractères) et l\'offset (8 caractères).\n2. Le sous-dossier `archive_status/` contient des fichiers vides de statut utilisés par le processus `archiver` :\n   - `<segment>.ready` : Indique que le segment WAL est plein et attend d\'être copié par l\'`archive_command`.\n   - `<segment>.done` : Indique que l\'archivage a réussi avec succès ; le fichier WAL peut désormais être recyclé ou supprimé lors du prochain checkpoint.',
      explanation: 'Si l\'`archive_command` échoue, les fichiers `.ready` s\'accumulent dans `archive_status/`, ce qui empêche PostgreSQL de supprimer les segments correspondants et sature le disque.',
      examTrap: 'Avant PostgreSQL 10, ce répertoire s\'appelait `pg_xlog`. Il a été renommé `pg_wal` pour éviter que des administrateurs système ignorants ne vident le dossier en croyant qu\'il s\'agissait de simples fichiers logs ordinaires !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26.3: Continuous Archiving',
    },
  },
  {
    id: 'fc-pgedb-dom02-029',
    cardNumber: 29,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Rôle du sous-répertoire `$PGDATA/pg_tblspc/`',
    difficulty: 'medium',
    tags: ['Tablespace', 'pg_tblspc', 'Symlink', 'Storage', 'Location'],
    front: {
      question: 'Comment PostgreSQL stocke-t-il les tablespaces créés par l\'administrateur dans le répertoire `$PGDATA/pg_tblspc/` ?',
      codeSnippet: 'CREATE TABLESPACE fast_ssd LOCATION \'/mnt/nvme/pg_fast\';',
      hint: 'Il crée un lien symbolique vers le point de montage ou répertoire externe.',
    },
    back: {
      answer: 'Dans `$PGDATA/pg_tblspc/`, PostgreSQL ne stocke pas directement les données du tablespace, mais crée un lien symbolique (symlink sous Linux/Unix) ou une jonction NTFS (sous Windows) nommé d\'après l\'OID du tablespace.\nCe lien pointe vers le chemin absolu spécifié dans la clause `LOCATION` (ex: `/mnt/nvme/pg_fast`).\nÀ l\'intérieur de ce dossier externe, PostgreSQL crée une arborescence versionnée (`PG_16_<version_id>/`) contenant les sous-dossiers par base de données.',
      explanation: 'Ce mécanisme permet de répartir les tables et index les plus sollicités sur des disques ultra-rapides tout en conservant une référence logique centralisée sous `$PGDATA/pg_tblspc/`.',
      examTrap: 'Lors d\'une sauvegarde physique avec `pg_basebackup`, il faut utiliser l\'option `--tablespace-mapping` ou `--format=tar` pour gérer correctement la relocalisation des répertoires cibles des tablespaces.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 23.6: Tablespaces',
    },
  },
  {
    id: 'fc-pgedb-dom02-030',
    cardNumber: 30,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Sous-répertoires internes : `pg_xact/`, `pg_multixact/`, `pg_stat_tmp/`',
    difficulty: 'hard',
    tags: ['pg_xact', 'Commit Status', 'pg_clog', 'Transactions', 'Multixact'],
    front: {
      question: 'Quelle est la fonction du sous-répertoire `$PGDATA/pg_xact/` (anciennement `pg_clog`) et que stocke-t-il ?',
      hint: 'Il stocke deux bits par transaction pour indiquer si celle-ci a été commitée, abortée ou est encore en cours.',
    },
    back: {
      answer: 'Le sous-répertoire `$PGDATA/pg_xact/` (renommé ainsi en PG 10, historiquement `pg_clog`) stocke le statut de validation de chaque transaction (Commit Log).\nPour chaque Transaction ID (XID), PostgreSQL maintient 2 bits d\'état :\n- `00` : En cours (in progress)\n- `01` : Validée (committed)\n- `10` : Annulée / Rollback (aborted)\n- `11` : Sous-transaction validée.\n\nCe journal est consulté lors des vérifications de visibilité MVCC pour déterminer si les tuples créés par une transaction sont visibles par d\'autres transactions.',
      explanation: 'Ces fichiers sont tronqués et purgés périodiquement par le processus `VACUUM` au fur et à mesure que les transactions anciennes sont gelées (freezing).',
      examTrap: 'Ne jamais toucher ni supprimer les fichiers dans `pg_xact/` ! Leur perte corrompt instantanément la visibilité MVCC de toutes les tables du cluster.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 73: Physical Storage',
    },
  },

  // =========================================================================
  // SECTION 4 : GESTION DU SERVEUR AVEC L'UTILITAIRE `pg_ctl` (Cartes 31 à 40)
  // Commandes pg_ctl, modes stop (smart, fast, immediate), reload, promote, status
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-031',
    cardNumber: 31,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Rôle et syntaxe générale de l\'utilitaire `pg_ctl`',
    difficulty: 'easy',
    tags: ['pg_ctl', 'Control', 'Service', 'Postmaster', 'Start/Stop'],
    front: {
      question: 'Qu\'est-ce que l\'outil `pg_ctl` et quelles sont ses sous-commandes d\'action principales ?',
      codeSnippet: '$ pg_ctl -D /var/lib/pgsql/16/data <action>',
      hint: 'C\'est l\'utilitaire standard pour initialiser, démarrer, arrêter, redémarrer, recharger et promouvoir une instance.',
    },
    back: {
      answer: '`pg_ctl` est l\'utilitaire officiel d\'administration en ligne de commande pour contrôler le serveur PostgreSQL (exécutable `postgres`).\nSes principales actions sont :\n1. `init[db]` : Initialise un nouveau cluster de bases de données.\n2. `start` : Lance le serveur en arrière-plan.\n3. `stop` : Arrête le serveur selon un mode choisi (`smart`, `fast`, `immediate`).\n4. `restart` : Arrête puis redémarre le serveur.\n5. `reload` : Envoie un signal SIGHUP pour recharger les fichiers de configuration sans redémarrer.\n6. `status` : Indique si le serveur tourne sur le `$PGDATA` spécifié.\n7. `promote` : Proclame un serveur Standby en lecture-écriture (Primaire).',
      explanation: 'Bien que les administrateurs utilisent souvent `systemctl` en production, `pg_ctl` reste la commande de référence sous-jacente interrogée lors des certifications PostgreSQL/EDB.',
      examTrap: '`pg_ctl` doit obligatoirement être exécuté par le propriétaire du cluster (`postgres`) et jamais en tant que `root`.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_ctl',
    },
  },
  {
    id: 'fc-pgedb-dom02-032',
    cardNumber: 32,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Modes d\'arrêt de `pg_ctl stop -m <mode>` : Smart, Fast, Immediate',
    difficulty: 'medium',
    tags: ['pg_ctl', 'Stop Modes', 'Fast', 'Smart', 'Immediate', 'Shutdown'],
    front: {
      question: 'Quels sont les trois modes d\'arrêt proposés par `pg_ctl stop -m` et quel est le mode par défaut depuis PostgreSQL 9.5+ ?',
      codeSnippet: '$ pg_ctl stop -D $PGDATA -m fast',
      hint: 'L\'un attend les déconnexions, le second force la déconnexion et fait un checkpoint propre, le troisième coupe tout sans checkpoint.',
    },
    back: {
      answer: 'Les trois modes d\'arrêt sont :\n1. `smart` (SIGTERM sur postmaster) : Arrêt courtois. Refuse les nouvelles connexions, mais attend patiemment que toutes les sessions clientes existantes se déconnectent et que les sauvegardes en ligne se terminent. Peut bloquer indéfiniment si des clients restent connectés.\n2. `fast` (SIGINT sur backends - MODE PAR DÉFAUT) : Arrêt rapide recommandé. Interrompt immédiatement toutes les requêtes actives, déconnecte de force tous les clients (rollback des transactions en cours) et effectue un Checkpoint d\'arrêt propre (Shutdown Checkpoint).\n3. `immediate` (SIGQUIT sur backends) : Arrêt brutal d\'urgence (équivalent crash / coupure de courant). Tue tous les processus sans effectuer de checkpoint. Nécessite obligatoirement un cycle complet de Crash Recovery au prochain démarrage.',
      explanation: 'Le mode `fast` est le standard de l\'industrie et le mode utilisé par défaut par les scripts systemd de PostgreSQL.',
      examTrap: 'N\'utilisez JAMAIS le mode `immediate` pour une maintenance planifiée ! Bien qu\'il n\'y ait pas de corruption de données garantie grâce aux WAL, le prochain démarrage sera beaucoup plus long en raison du crash recovery.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_ctl (Stop Modes)',
    },
  },
  {
    id: 'fc-pgedb-dom02-033',
    cardNumber: 33,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Différence entre `pg_ctl reload` et `pg_ctl restart`',
    difficulty: 'easy',
    tags: ['pg_ctl', 'reload', 'restart', 'SIGHUP', 'Availability'],
    front: {
      question: 'Quelle est la différence fondamentale entre `pg_ctl reload` et `pg_ctl restart` en matière de disponibilité pour les applications clientes ?',
      hint: 'Le premier ne déconnecte personne et ne provoque aucune interruption de service.',
    },
    back: {
      answer: '1. `pg_ctl reload` :\n- Envoie le signal `SIGHUP` au processus maître Postmaster.\n- Relit et applique à chaud les modifications de `postgresql.conf` et `pg_hba.conf` (pour les paramètres de contexte SIGHUP ou USER).\n- Zéro interruption : les connexions clientes restent actives et connectées.\n\n2. `pg_ctl restart` :\n- Arrête complètement l\'instance (déconnexion de tous les clients, libération de la mémoire partagée) puis la relance.\n- Provoque une indisponibilité temporaire de la base de données.',
      explanation: 'En production, on privilégie toujours `pg_ctl reload` (ou `SELECT pg_reload_conf();`). On ne fait un `restart` que si un paramètre de contexte `postmaster` (comme `shared_buffers` ou `max_connections`) doit être appliqué.',
      examTrap: 'Une tentative de modification de `shared_buffers` suivie d\'un simple `pg_ctl reload` ne sera PAS prise en compte et restera à son ancienne valeur.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters',
    },
  },
  {
    id: 'fc-pgedb-dom02-034',
    cardNumber: 34,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Option `-w` (wait) vs `-W` (no-wait) dans `pg_ctl`',
    difficulty: 'medium',
    tags: ['pg_ctl', 'wait', 'Scripting', 'Automation', 'DevOps'],
    front: {
      question: 'Dans les scripts d\'automatisation ou de démarrage, pourquoi utilise-t-on l\'option `-w` (wait) avec `pg_ctl start` ou `pg_ctl stop` ?',
      codeSnippet: '$ pg_ctl start -D $PGDATA -l /var/log/pg.log -w -t 60',
      hint: 'Elle force la commande à bloquer tant que le serveur n\'est pas réellement prêt à accepter des connexions.',
    },
    back: {
      answer: 'L\'option `-w` (ou `--wait`) force `pg_ctl` à attendre la fin effective de l\'opération avant de rendre la main avec un code de retour (exit code) :\n- Pour `start -w` : Attend que le serveur ait terminé son initialisation/recovery et soit prêt à accepter les connexions clientes (teste la connexion sur le socket/port).\n- Pour `stop -w` : Attend que tous les processus enfants soient terminés et que le fichier `postmaster.pid` soit supprimé.\n\nL\'option `-t <secondes>` spécifie le timeout d\'attente (par défaut 60 secondes).',
      explanation: 'À l\'inverse, `-W` (no-wait) rend la main immédiatement sans vérifier si le serveur a réussi à démarrer ou s\'il a planté lors de l\'initialisation de la mémoire partagée.',
      examTrap: 'Dans les scripts de déploiement CI/CD, omettre `-w` conduit souvent à des échecs des étapes suivantes qui tentent d\'exécuter des commandes `psql` alors que le serveur est encore en phase de recovery.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_ctl',
    },
  },
  {
    id: 'fc-pgedb-dom02-035',
    cardNumber: 35,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Redirection des traces avec l\'option `-l` / `--log` de `pg_ctl start`',
    difficulty: 'easy',
    tags: ['pg_ctl', 'Logging', 'Logfile', 'Console Output', 'Troubleshooting'],
    front: {
      question: 'À quoi sert l\'option `-l <fichier>` lors de l\'exécution de `pg_ctl start` ?',
      codeSnippet: '$ pg_ctl start -D /data -l /var/log/pgsql/server.log',
      hint: 'Elle capture la sortie standard (stdout) et les erreurs (stderr) du processus serveur.',
    },
    back: {
      answer: 'L\'option `-l` (ou `--log=<fichier>`) redirige la sortie console du serveur (`stdout` et `stderr`) vers un fichier journal spécifié.\nC\'est particulièrement indispensable si le collecteur de journaux interne (`logging_collector = on`) n\'est pas encore actif ou en cas d\'échec de démarrage très précoce (ex: mémoire partagée impossible à allouer, permission refusée sur $PGDATA, port réseau déjà utilisé).',
      explanation: 'Si le serveur plante avant même d\'avoir pu initialiser son moteur de logging interne, la cause du crash ne figurera que dans le fichier désigné par `-l`.',
      examTrap: 'Si vous lancez `pg_ctl start` sans `-l` et en tâche de fond dans un terminal interactif, les messages d\'erreurs risquent d\'être perdus ou polluants pour votre terminal.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_ctl',
    },
  },
  {
    id: 'fc-pgedb-dom02-036',
    cardNumber: 36,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Promotion d\'un réplica avec `pg_ctl promote`',
    difficulty: 'medium',
    tags: ['pg_ctl', 'promote', 'Standby', 'High Availability', 'Failover'],
    front: {
      question: 'Que réalise la commande `pg_ctl promote` lorsqu\'elle est exécutée sur une instance PostgreSQL fonctionnant en réplica Standby (lecture seule) ?',
      codeSnippet: '$ pg_ctl promote -D /var/lib/pgsql/16/standby',
      hint: 'Elle termine le mode de récupération continue et transforme l\'instance en serveur primaire ouvert en lecture-écriture.',
    },
    back: {
      answer: 'La commande `pg_ctl promote` ordonne à un serveur secondaire (Standby en streaming replication) de :\n1. Sortir du mode de récupération continue (recovery).\n2. Rejouer les derniers WAL résiduels jusqu\'au point de basculement.\n3. Incrémenter l\'identifiant de Timeline (ex: Timeline 1 -> Timeline 2) et écrire un fichier d\'historique de timeline (`.history`).\n4. Ouvrir la base de données en mode lecture-écriture (Read/Write), devenant ainsi le nouveau serveur Primaire du cluster.',
      explanation: 'Elle équivaut à exécuter la fonction SQL `SELECT pg_promote();` depuis une session connectée au réplica.',
      examTrap: 'Une fois promu, le serveur ne peut plus redevenir un Standby sans être resynchronisé via `pg_rewind` ou reconstruit avec `pg_basebackup`.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_ctl promote',
    },
  },
  {
    id: 'fc-pgedb-dom02-037',
    cardNumber: 37,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Vérification de l\'état avec `pg_ctl status`',
    difficulty: 'easy',
    tags: ['pg_ctl', 'status', 'Monitoring', 'postmaster.pid', 'Exit Codes'],
    front: {
      question: 'Comment fonctionne la commande `pg_ctl status` pour vérifier si le serveur PostgreSQL est opérationnel sur un répertoire de données ?',
      codeSnippet: '$ pg_ctl status -D /var/lib/pgsql/16/data',
      hint: 'Elle vérifie la présence et le contenu du fichier verrou postmaster.pid.',
    },
    back: {
      answer: '`pg_ctl status` inspecte le répertoire spécifié par `-D` ou la variable `PGDATA` :\n1. Il vérifie l\'existence du fichier `postmaster.pid`.\n2. Si le fichier existe, il lit le PID inscrit et vérifie auprès du système d\'exploitation si ce processus existe toujours et s\'il correspond bien au binaire postgres.\n3. Codes de retour Unix (exit codes) :\n   - Code 0 : Le serveur est actif et fonctionne (PID affiché).\n   - Code 3 : Le serveur n\'est pas actif (aucun processus correspondant).\n   - Code 4 : Le répertoire spécifié n\'existe pas ou est inaccessible.',
      explanation: 'Ces codes de retour normalisés permettent aux outils de supervision (Nagios, Zabbix) ou gestionnaires de haute disponibilité (Pacemaker, Keepalived) de tester la santé du serveur.',
      examTrap: '`pg_ctl status` teste l\'existence du processus Postmaster, mais ne garantit pas que le serveur est capable de traiter des requêtes SQL (ex: si le serveur est bloqué en pause de recovery).',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_ctl',
    },
  },
  {
    id: 'fc-pgedb-dom02-038',
    cardNumber: 38,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Passage d\'options au binaire postgres via `pg_ctl -o`',
    difficulty: 'medium',
    tags: ['pg_ctl', 'Options', 'Command Line', 'Debugging', 'Port'],
    front: {
      question: 'Comment peut-on passer des paramètres de configuration temporaires au moteur `postgres` lors d\'un démarrage avec `pg_ctl` ?',
      codeSnippet: '$ pg_ctl start -D /data -o "-p 5433 -c work_mem=128MB"',
      hint: 'L\'option -o permet de transmettre une chaîne de commutateurs directement à l\'exécutable sous-jacent.',
    },
    back: {
      answer: 'L\'option `-o "<options>"` permet de transmettre des arguments de ligne de commande directement au binaire `postgres` sous-jacent.\nExemples d\'utilisation courante :\n- Démarrer temporairement sur un port TCP distinct : `pg_ctl start -D /data -o "-p 5433"`\n- Surcharger un paramètre sans modifier postgresql.conf : `pg_ctl start -D /data -o "-c shared_buffers=2GB"`\n- Activer le mode debug détaillé : `pg_ctl start -D /data -o "-d 2"`',
      explanation: 'Les options passées via `-o` prennent le dessus sur les valeurs inscrites dans `postgresql.conf` et `postgresql.auto.conf` pour la session d\'exécution courante.',
      examTrap: 'Toutes les options dans `-o` doivent être encadrées par des guillemets pour ne pas être interprétées par le shell comme des arguments de `pg_ctl`.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_ctl & postgres command line',
    },
  },
  {
    id: 'fc-pgedb-dom02-039',
    cardNumber: 39,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Mode mono-utilisateur de secours (`postgres --single`)',
    difficulty: 'hard',
    tags: ['Single-User', 'Troubleshooting', 'Disaster Recovery', 'Maintenance', 'Emergency'],
    front: {
      question: 'Dans quel scénario d\'extrême urgence utilise-t-on le mode mono-utilisateur (`postgres --single`) et quelles en sont les particularités ?',
      codeSnippet: '$ postgres --single -D /var/lib/pgsql/16/data postgres',
      hint: 'Il permet d\'ouvrir une invite SQL directement sur le catalogue système lorsque l\'instance refuse de démarrer normalement.',
    },
    back: {
      answer: 'Le mode mono-utilisateur (`postgres --single -D $PGDATA <nom_base>`) est un mode de secours ultime utilisé lorsque le serveur refuse de démarrer normalement (ex: catalogues système corrompus, paramètre invalide bloquant, table système endommagée).\nParticularités :\n1. Aucun processus Postmaster, aucun démon d\'arrière-plan (pas de bgwriter, pas d\'autovacuum, pas de checkpointer).\n2. Aucun socket réseau ni port TCP n\'est ouvert : une seule session locale connectée directement en mode console.\n3. Chaque instruction SQL se termine par un retour à la ligne (Enter) suivi de Ctrl+D pour exécuter.\n4. Permet d\'exécuter des `REINDEX`, `VACUUM` ou des modifications manuelles sur des tables système critiques.',
      explanation: 'Ce mode ne doit être utilisé qu\'en cas de sinistre absolu par un DBA expérimenté, car les sécurités usuelles de concurrence sont désactivées.',
      examTrap: 'Le serveur normal doit impérativement être arrêté avant de lancer `postgres --single`, sous peine de corrompre immédiatement le cluster par double accès concurrent.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.3.3: Single-User Mode',
    },
  },
  {
    id: 'fc-pgedb-dom02-040',
    cardNumber: 40,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Gestion des signaux système POSIX (SIGTERM, SIGINT, SIGHUP, SIGQUIT)',
    difficulty: 'hard',
    tags: ['Signals', 'POSIX', 'kill', 'SIGHUP', 'SIGTERM', 'SIGINT'],
    front: {
      question: 'Quels sont les effets des principaux signaux Unix envoyés au PID du processus maître Postmaster (`kill -<SIGNAL> <PID>`) ?',
      hint: 'Chaque signal correspond exactement à un mode d\'arrêt ou à un rechargement.',
    },
    back: {
      answer: 'Les signaux envoyés au Postmaster produisent les actions suivantes :\n- `SIGHUP` : Équivaut à `pg_ctl reload`. Relit tous les fichiers de configuration sans interruption.\n- `SIGINT` : Équivaut à `pg_ctl stop -m fast`. Déconnecte immédiatement les sessions clientes et fait un shutdown checkpoint.\n- `SIGTERM` : Équivaut à `pg_ctl stop -m smart`. Attend la déconnexion volontaire des clients.\n- `SIGQUIT` : Équivaut à `pg_ctl stop -m immediate`. Tue brutalement les backends sans checkpoint.\n- `SIGKILL` (`kill -9`) : À PROSCRIRE ABSOLUMENT ! Tue le postmaster sans laisser les backends libérer la mémoire partagée et les verrous IPC.',
      explanation: '`pg_ctl` ne fait rien d\'autre en interne que de repérer le PID dans `postmaster.pid` et d\'envoyer le signal POSIX correspondant au processus maître.',
      examTrap: 'N\'envoyez jamais `SIGKILL` au Postmaster en pensant "accélérer" l\'arrêt : cela laisse des processus backends orphelins et corrompt les segments IPC partagés.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.5: Shutting Down the Server',
    },
  },

  // =========================================================================
  // SECTION 5 : HIÉRARCHIE, SYNTAXE ET INCLUSIONS DE `postgresql.conf` (Cartes 41 à 50)
  // Format des directives, include, include_dir, include_if_exists, pg_file_settings
  // =========================================================================
  {
    id: 'fc-pgedb-dom02-041',
    cardNumber: 41,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Syntaxe générale et types de valeurs dans `postgresql.conf`',
    difficulty: 'easy',
    tags: ['postgresql.conf', 'Syntax', 'Units', 'Configuration', 'Types'],
    front: {
      question: 'Quelles sont les règles syntaxiques de base pour définir des paramètres et leurs unités dans `postgresql.conf` ?',
      codeSnippet: 'shared_buffers = 4GB\nwork_mem = \'64MB\'\ncheckpoint_timeout = 15min\nlisten_addresses = \'*\'',
      hint: 'Le fichier utilise des paires nom = valeur ; les unités de mémoire et de temps sont supportées nativement.',
    },
    back: {
      answer: 'Règles syntaxiques :\n1. Format : `nom_parametre = valeur` (le signe `=` et les espaces autour sont optionnels).\n2. Insensibilité à la casse pour les noms de paramètres (`shared_buffers` ou `SHARED_BUFFERS`).\n3. Les chaînes contenant des caractères spéciaux ou des espaces doivent être encadrées de guillemets simples (ex: `listen_addresses = \'*\'`).\n4. Unités de mémoire reconnues : `kB`, `MB`, `GB`, `TB` (sensibles à la casse pour kB vs KB).\n5. Unités de temps reconnues : `ms`, `s`, `min`, `h`, `d`.\n6. Booléens acceptés : `on` / `off`, `true` / `false`, `yes` / `no`, `1` / `0`.\n7. Commentaires : Débutent par un dièse `#`.',
      explanation: 'Si aucune unité n\'est précisée lors de la saisie d\'un nombre, PostgreSQL utilise l\'unité par défaut du paramètre (ex: blocs de 8 Ko pour shared_buffers, millisecondes pour deadlock_timeout).',
      examTrap: 'Ne confondez pas les minuscules et majuscules pour les kilo-octets : `kB` (1024 octets) est l\'unité standard de PostgreSQL.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.1: Parameter Names and Values',
    },
  },
  {
    id: 'fc-pgedb-dom02-042',
    cardNumber: 42,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Règle de préséance en cas de doublon dans `postgresql.conf`',
    difficulty: 'easy',
    tags: ['postgresql.conf', 'Precedence', 'Overrides', 'Configuration'],
    front: {
      question: 'Si un même paramètre est défini à deux reprises avec des valeurs différentes au sein de `postgresql.conf`, laquelle est retenue par le serveur ?',
      codeSnippet: 'work_mem = 32MB\n# ... 50 lignes plus bas ...\nwork_mem = 64MB',
      hint: 'Le fichier de configuration est lu séquentiellement de haut en bas.',
    },
    back: {
      answer: 'C\'est la DERNIÈRE valeur rencontrée dans le fichier qui l\'emporte et est appliquée par le serveur.\nDans l\'exemple ci-dessus, `work_mem` sera valorisé à `64MB`.',
      explanation: 'Cette règle de préséance séquentielle est exploitée par les administrateurs pour placer des fichiers d\'inclusion personnalisés à la toute fin de `postgresql.conf` afin d\'écraser proprement les réglages par défaut situés plus haut.',
      examTrap: 'Attention aux fichiers volumineux où un paramètre oublié en fin de fichier annule silencieusement une modification faite en début de fichier.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters',
    },
  },
  {
    id: 'fc-pgedb-dom02-043',
    cardNumber: 43,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Directive d\'inclusion de fichier : `include`',
    difficulty: 'medium',
    tags: ['include', 'postgresql.conf', 'Modular Configuration', 'Architecture'],
    front: {
      question: 'Comment utilise-t-on la directive `include` dans `postgresql.conf` et quel est le comportement si le fichier cible est introuvable ?',
      codeSnippet: 'include = \'tuning.conf\'',
      hint: 'Le fichier cible est obligatoire ; son absence provoque une erreur fatale.',
    },
    back: {
      answer: 'La directive `include = \'chemin/vers/fichier.conf\'` permet de modulariser la configuration en important le contenu d\'un autre fichier de configuration textuel.\n- Si le chemin est relatif, il est résolu par rapport au répertoire contenant `postgresql.conf`.\n- Si le fichier spécifié n\'existe pas ou n\'est pas lisible, PostgreSQL lève une erreur FATALE et REFUSE de démarrer ou de recharger la configuration.',
      explanation: 'Cette directive est très utilisée pour séparer les réglages matériels (RAM, CPU), les réglages de réplication et les réglages de logging dans des fichiers dédiés.',
      examTrap: 'Ne confondez pas avec `include_if_exists` : `include` est strict et échoue si le fichier cible n\'existe pas.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.4: Managing Configuration File Contents',
    },
  },
  {
    id: 'fc-pgedb-dom02-044',
    cardNumber: 44,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Directive `include_if_exists`',
    difficulty: 'medium',
    tags: ['include_if_exists', 'postgresql.conf', 'Modular Configuration', 'Overrides'],
    front: {
      question: 'Dans quelle situation privilégie-t-on la directive `include_if_exists` plutôt que `include` ?',
      codeSnippet: 'include_if_exists = \'local_overrides.conf\'',
      hint: 'Elle permet d\'inclure un fichier facultatif sans risquer de bloquer le serveur s\'il n\'est pas présent.',
    },
    back: {
      answer: 'On utilise `include_if_exists = \'fichier.conf\'` lorsque le fichier de configuration cible est optionnel.\n- Si le fichier existe, son contenu est lu et appliqué.\n- Si le fichier est absent, PostgreSQL ignore simplement la directive sans émettre d\'erreur ni interrompre le démarrage du serveur.',
      explanation: 'C\'est idéal pour les environnements de déploiement automatisé (Ansible, Puppet) où un modèle standard `postgresql.conf` inclut un fichier de surcharge locale propre à chaque machine (`local_overrides.conf`) qui n\'existe que sur certains serveurs spécifiques.',
      examTrap: 'Si le fichier existe mais contient des erreurs de syntaxe, le serveur lèvera une erreur de parsing même avec `include_if_exists` !',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.4: Managing Configuration File Contents',
    },
  },
  {
    id: 'fc-pgedb-dom02-045',
    cardNumber: 45,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Directive de répertoire d\'inclusion : `include_dir`',
    difficulty: 'medium',
    tags: ['include_dir', 'conf.d', 'postgresql.conf', 'Automation'],
    front: {
      question: 'Comment fonctionne la directive `include_dir` et selon quel ordre les fichiers présents dans le répertoire cible sont-ils traités ?',
      codeSnippet: 'include_dir = \'conf.d\'',
      hint: 'Elle lit tous les fichiers terminés par .conf selon l\'ordre alphabétique standard.',
    },
    back: {
      answer: 'La directive `include_dir = \'nom_repertoire\'` (ex: `conf.d`) inclut automatiquement tous les fichiers se terminant par l\'extension `.conf` situés dans ce répertoire.\nOrdre d\'exécution :\nLes fichiers sont traités dans l\'ordre alphabétique de leurs noms de fichiers (selon les règles de la locale C).\nLes fichiers dont le nom commence par un point (`.`) ou ne se terminent pas par `.conf` sont ignorés.',
      explanation: 'Bonne pratique d\'administration : préfixer les fichiers avec des numéros à deux chiffres pour contrôler rigoureusement l\'ordre de lecture (ex: `01-memory.conf`, `02-logging.conf`, `99-overrides.conf`).',
      examTrap: 'Si deux fichiers dans `conf.d` définissent le même paramètre, c\'est le fichier lu en dernier selon l\'ordre alphabétique qui écrasera les valeurs précédentes.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.4: Managing Configuration File Contents',
    },
  },
  {
    id: 'fc-pgedb-dom02-046',
    cardNumber: 46,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Vue système d\'audit des fichiers : `pg_file_settings`',
    difficulty: 'hard',
    tags: ['pg_file_settings', 'Audit', 'Syntax Errors', 'Troubleshooting', 'SIGHUP'],
    front: {
      question: 'À quoi sert la vue système `pg_file_settings` et pourquoi les DBA doivent-ils l\'interroger systématiquement avant et après un rechargement ?',
      codeSnippet: 'SELECT sourcefile, sourceline, name, setting, applied, error\nFROM pg_file_settings\nWHERE error IS NOT NULL OR applied = false;',
      hint: 'Elle affiche ligne par ligne le parsing des fichiers de configuration et les éventuelles erreurs de syntaxe.',
    },
    back: {
      answer: 'La vue système `pg_file_settings` résume le contenu de tous les fichiers de configuration analysés par le serveur (`postgresql.conf`, fichiers inclus, `postgresql.auto.conf`).\nElle fournit pour chaque directive :\n- `sourcefile` et `sourceline` : Le fichier exact et le numéro de ligne où le paramètre est écrit.\n- `name` et `setting` : Le nom et la valeur textuelle renseignée.\n- `applied` (booléen) : Indique si la ligne a été réellement appliquée (vaut `false` si la ligne est écrasée par une ligne ultérieure ou si une erreur est survenue).\n- `error` (texte) : Message d\'erreur exact si la syntaxe est invalide ou si l\'unité est inconnue.',
      explanation: 'Interroger cette vue après avoir modifié un fichier permet de détecter immédiatement une faute de frappe avant qu\'elle ne provoque un échec au prochain redémarrage du serveur.',
      examTrap: 'Une erreur visible dans `pg_file_settings` n\'interrompt pas les connexions existantes lors d\'un `pg_reload_conf()`, mais le paramètre défaillant conservera son ancienne valeur.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 54: System Views - pg_file_settings',
    },
  },
  {
    id: 'fc-pgedb-dom02-047',
    cardNumber: 47,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Délocalisation de `postgresql.conf` en dehors de `$PGDATA`',
    difficulty: 'hard',
    tags: ['config_file', 'Debian', 'HBA', 'Architectures', 'Separation'],
    front: {
      question: 'Comment peut-on démarrer une instance PostgreSQL avec un fichier `postgresql.conf` stocké en dehors du répertoire physique `$PGDATA` ?',
      codeSnippet: '$ postgres -D /var/lib/pgsql/data --config-file=/etc/postgresql/custom.conf',
      hint: 'On utilise l\'option de ligne de commande --config-file ou la directive interne config_file.',
    },
    back: {
      answer: 'On peut déporter la configuration en spécifiant l\'option `--config-file=<chemin_absolu>` (ou `-c config_file=...`) lors du lancement du binaire `postgres` ou via `pg_ctl -o`.\nÀ l\'intérieur de ce `postgresql.conf` externe, deux paramètres obligatoires doivent pointer vers les autres répertoires :\n1. `data_directory = \'/chemin/vers/PGDATA\'` : Spécifie où se trouvent les fichiers de données physiques.\n2. `hba_file = \'/etc/.../pg_hba.conf\'` : Spécifie l\'emplacement du fichier de contrôle d\'accès.',
      explanation: 'C\'est l\'architecture standard adoptée par Debian et Ubuntu, où les configurations résident dans `/etc/postgresql/` et les données dans `/var/lib/postgresql/`.',
      examTrap: 'Si `data_directory` est mal configuré à l\'intérieur du `postgresql.conf` déporté, le serveur refusera de démarrer.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.2: File Locations',
    },
  },
  {
    id: 'fc-pgedb-dom02-048',
    cardNumber: 48,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Paramètres personnalisés et préfixes d\'extensions (Custom Options)',
    difficulty: 'medium',
    tags: ['Custom Options', 'Extensions', 'pg_stat_statements', 'Prefix'],
    front: {
      question: 'Quelle est la convention obligatoire pour déclarer des paramètres de configuration relatifs à des extensions tierces dans `postgresql.conf` ?',
      codeSnippet: 'pg_stat_statements.max = 10000\npg_stat_statements.track = all\nauto_explain.log_min_duration = 500',
      hint: 'Les paramètres d\'extensions doivent être préfixés par le nom du module suivi d\'un point.',
    },
    back: {
      answer: 'Les paramètres de configuration pour les extensions ou modules externes doivent impérativement comporter un préfixe personnalisé au format :\n`<nom_extension>.<nom_parametre> = <valeur>`.\nPar exemple : `pg_stat_statements.max = 10000` ou `auto_explain.log_min_duration = 1000`.\n\nPour que ces extensions soient chargées en mémoire et que leurs paramètres soient reconnus dès le démarrage, l\'extension doit souvent être déclarée dans le paramètre `shared_preload_libraries`.',
      explanation: 'PostgreSQL permet de définir n\'importe quelle variable préfixée arbitraire (ex: `myapp.tenant_id = 42`), ce qui est très pratique pour passer des variables globales de contexte applicatif.',
      examTrap: 'Si vous définissez un paramètre sans point qui n\'est pas un paramètre natif du cœur de PostgreSQL, le serveur rejettera la directive comme une erreur inconnue.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1.6: Customized Options',
    },
  },
  {
    id: 'fc-pgedb-dom02-049',
    cardNumber: 49,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Restauration depuis `postgresql.conf.sample`',
    difficulty: 'easy',
    tags: ['postgresql.conf.sample', 'Disaster Recovery', 'Defaults', 'Template'],
    front: {
      question: 'Où trouve-t-on le fichier modèle d\'origine `postgresql.conf.sample` et à quoi sert-il en cas de corruption du fichier de configuration ?',
      hint: 'Il est installé dans le répertoire des partages système (share) ou laissé à l\'initialisation.',
    },
    back: {
      answer: '`postgresql.conf.sample` est le modèle officiel fourni par PostgreSQL lors de l\'installation (situé dans `$PGDATA` ou dans `$PREFIX/share/postgresql/`).\nIl contient l\'intégralité des centaines de paramètres du moteur avec leur valeur par défaut commentée par un dièse `#` et une brève explication.\nEn cas de corruption irrémédiable de votre `postgresql.conf`, vous pouvez le restaurer en copiant `postgresql.conf.sample` vers `$PGDATA/postgresql.conf`.',
      explanation: 'Dans ce fichier modèle, toute ligne commentée (ex: `#shared_buffers = 128MB`) représente la valeur par défaut compilée en dur dans le moteur si le paramètre n\'est pas défini explicitement.',
      examTrap: 'Décommenter une ligne sans en modifier la valeur applique la valeur explicite, ce qui peut empêcher de futurs ajustements automatiques lors de montées de version.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 19.1: Setting Parameters',
    },
  },
  {
    id: 'fc-pgedb-dom02-050',
    cardNumber: 50,
    domainId: 'pg-edb-dom-02',
    domainCode: 'DOM-02',
    domainTitle: 'Installation, Initialisation (initdb) & postgresql.conf',
    subtopic: 'Vérification syntaxique à froid avec `postgres -C`',
    difficulty: 'hard',
    tags: ['postgres -C', 'Syntax Check', 'CLI', 'Verification', 'Troubleshooting'],
    front: {
      question: 'Quelle commande en ligne de commande permet de vérifier la valeur interprétée d\'un paramètre de configuration sans démarrer le serveur ?',
      codeSnippet: '$ postgres -D /var/lib/pgsql/16/data -C shared_buffers',
      hint: 'L\'option -C interroge le fichier de configuration et affiche la valeur calculée sans lancer le serveur complet.',
    },
    back: {
      answer: 'La commande `postgres -D <PGDATA> -C <nom_parametre>` lit l\'ensemble des fichiers de configuration (`postgresql.conf`, inclusions, `postgresql.auto.conf`) et affiche la valeur finale calculée du paramètre spécifié sans démarrer l\'instance.\nExemple : `$ postgres -D $PGDATA -C shared_buffers` renvoie `1048576` (en blocs de 8 Ko, soit 8 Go).\nSi le fichier contient une erreur de syntaxe fatale, la commande échoue immédiatement en affichant l\'erreur.',
      explanation: 'C\'est une commande extrêmement utile dans les scripts de déploiement pour valider l\'intégrité de la configuration avant de tenter un redémarrage du service.',
      examTrap: '`postgres -C` affiche la valeur en unités internes brutes du moteur (souvent en blocs ou millisecondes) plutôt qu\'avec l\'unité humaine saisie dans le fichier.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: postgres',
    },
  },
];
