import { FlashcardItem } from '../types';

export const oracleDom03Part1Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 1 : FONCTIONS D'AGRÉGATION & COMPORTEMENT FACE À NULL (1 à 15)
  // =========================================================================
  {
    id: 'fc-dom03-001',
    cardNumber: 1,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Agrégats & Valeurs NULL',
    difficulty: 'easy',
    tags: ['Agrégats', 'NULL', 'Comportement', 'Oracle 19c'],
    front: {
      question: 'Quel est le comportement par défaut de toutes les fonctions d\'agrégation standards (SUM, AVG, MIN, MAX, STDDEV, VARIANCE) vis-à-vis des valeurs NULL ?',
      hint: 'Pensez aux lignes qui contiennent NULL dans la colonne agrégée.',
    },
    back: {
      answer: 'Toutes les fonctions d\'agrégation (à la seule exception de COUNT(*)) IGNORENT totalement les valeurs NULL.',
      explanation: 'Lors du calcul de la somme, de la moyenne ou de l\'écart-type, les lignes où la colonne évaluée vaut NULL sont purement et simplement exclues du calcul ainsi que du dénominateur (nombre d\'éléments pris en compte).',
      codeSnippet: '-- Si les salaires sont : 1000, 2000, NULL\n-- AVG calculera (1000 + 2000) / 2 = 1500 (et NON / 3)\nSELECT AVG(salary) FROM employees;',
      examTrap: 'Piège d\'examen classique : AVG(col) ne divise PAS par le nombre total de lignes de la table, mais uniquement par le nombre de lignes où col IS NOT NULL.',
      ruleRef: 'Oracle SQL Reference - Aggregate Functions and NULL Values',
    },
  },
  {
    id: 'fc-dom03-002',
    cardNumber: 2,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Agrégats & Valeurs NULL',
    difficulty: 'medium',
    tags: ['AVG', 'NVL', 'NULL', 'Calcul pondéré'],
    front: {
      question: 'Comment forcer la fonction AVG à inclure les valeurs NULL dans la moyenne comme valant 0 (pour que le dénominateur représente le nombre total de lignes) ?',
      hint: 'Combinez la fonction d\'agrégation avec une fonction de substitution scalaire.',
    },
    back: {
      answer: 'En enveloppant la colonne dans la fonction NVL : AVG(NVL(commission_pct, 0)).',
      explanation: 'La fonction NVL(commission_pct, 0) remplace chaque NULL par 0 avant que la fonction d\'agrégation AVG n\'intervienne. Ainsi, chaque ligne possède une valeur numérique non nulle, et le dénominateur inclut toutes les lignes de la table.',
      codeSnippet: '-- Moyenne réelle des bénéficiaires uniquement :\nSELECT AVG(commission_pct) FROM employees;\n\n-- Moyenne sur l\'ensemble des salariés (y compris sans commission) :\nSELECT AVG(NVL(commission_pct, 0)) FROM employees;',
      examTrap: 'À l\'examen, si une question demande la moyenne globale de l\'entreprise pour un taux de commission où seuls 20% ont une commission, AVG(commission_pct) est FAUX ; seule la forme avec NVL est correcte.',
      ruleRef: 'SQL Aggregate Functions - NVL with AVG/SUM',
    },
  },
  {
    id: 'fc-dom03-003',
    cardNumber: 3,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Agrégats sur ensemble vide ou tout-NULL',
    difficulty: 'hard',
    tags: ['Ensemble vide', 'NULL', 'COUNT', 'SUM', 'AVG'],
    front: {
      question: 'Que renvoient respectivement SUM(salary), AVG(salary), MAX(salary) et COUNT(*) lorsqu\'ils sont exécutés sur une table vide ou sur un ensemble sans aucune ligne correspondante ?',
      codeSnippet: 'SELECT SUM(salary), AVG(salary), MAX(salary), COUNT(*)\nFROM employees\nWHERE 1 = 2;',
      hint: 'Attention, il y a une différence fondamentale entre COUNT et les autres agrégats !',
    },
    back: {
      answer: 'SUM, AVG et MAX renvoient NULL, tandis que COUNT(*) renvoie 0.',
      explanation: 'Lorsqu\'une requête d\'agrégation globale (sans clause GROUP BY) ne filtre aucune ligne, COUNT(*) et COUNT(col) renvoient toujours la valeur 0. En revanche, toutes les autres fonctions d\'agrégation (SUM, AVG, MIN, MAX, STDDEV, VARIANCE) renvoient NULL (et non 0 !).',
      codeSnippet: 'SELECT \n  SUM(salary) AS total,    -- Renvoie NULL\n  AVG(salary) AS moyenne,  -- Renvoie NULL\n  MAX(salary) AS maximum,  -- Renvoie NULL\n  COUNT(*)    AS nbr_lignes -- Renvoie 0\nFROM employees WHERE department_id = 99999; -- Aucun département 99999',
      examTrap: 'Piège d\'examen mortel : beaucoup de candidats pensent que SUM(salary) renvoie 0 sur un ensemble vide. C\'est faux, SUM renvoie NULL ! Pour obtenir 0, il faut écrire NVL(SUM(salary), 0).',
      ruleRef: 'Aggregate Functions - Behavior on Empty Result Sets',
    },
  },
  {
    id: 'fc-dom03-004',
    cardNumber: 4,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Types de données pour les agrégats',
    difficulty: 'medium',
    tags: ['Types', 'MIN', 'MAX', 'DATE', 'VARCHAR2'],
    front: {
      question: 'Les fonctions MIN et MAX peuvent-elles être utilisées sur des colonnes de type VARCHAR2 et DATE, ou uniquement sur des types NUMBER ?',
      hint: 'Pensez à l\'ordre alphabétique ou chronologique.',
    },
    back: {
      answer: 'MIN et MAX s\'appliquent à TOUS les types de données : NUMBER, VARCHAR2, CHAR et DATE/TIMESTAMP.',
      explanation: 'Pour les chaînes (VARCHAR2/CHAR), MIN renvoie la première valeur selon le classement binaire/collation (ordre alphabétique) et MAX la dernière. Pour les DATE, MIN renvoie la date la plus ancienne et MAX la date la plus récente. Seules SUM, AVG, STDDEV et VARIANCE requièrent un type numérique.',
      codeSnippet: 'SELECT \n  MIN(last_name)  AS premier_nom,    -- Ordre alphabétique\n  MAX(hire_date)  AS dernier_embauche, -- Date la plus récente\n  MIN(salary)     AS plus_petit_salaire -- Valeur numérique minimale\nFROM employees;',
      examTrap: 'Une question demandant si MIN(hire_date) ou MAX(last_name) provoque une erreur de compilation ORA est un piège classique : cette syntaxe est parfaitement légale.',
      ruleRef: 'SQL Language Reference - MIN and MAX Function Data Types',
    },
  },
  {
    id: 'fc-dom03-005',
    cardNumber: 5,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonctions statistiques',
    difficulty: 'medium',
    tags: ['STDDEV', 'VARIANCE', 'Statistiques', 'Types'],
    front: {
      question: 'Quelles sont les fonctions d\'agrégation statistiques de dispersion fournies par Oracle et quel type de données exigent-elles ?',
      hint: 'Pensez à l\'écart-type et à la variance.',
    },
    back: {
      answer: 'VARIANCE et STDDEV (Standard Deviation / Écart-type). Elles n\'acceptent que des types NUMBER.',
      explanation: 'VARIANCE(expr) calcule la variance de l\'échantillon et STDDEV(expr) calcule l\'écart-type de l\'échantillon. Comme pour AVG et SUM, elles ignorent les valeurs NULL et nécessitent obligatoirement des données numériques ou convertibles en NUMBER.',
      codeSnippet: 'SELECT \n  ROUND(STDDEV(salary), 2) AS ecart_type,\n  ROUND(VARIANCE(salary), 2) AS variance\nFROM employees\nWHERE department_id = 50;',
      examTrap: 'Si le groupe ne contient qu\'une seule ligne non-NULL, VARIANCE et STDDEV renvoient 0 (et non NULL ou une erreur).',
      ruleRef: 'SQL Aggregate Functions - STDDEV, VARIANCE',
    },
  },
  {
    id: 'fc-dom03-006',
    cardNumber: 6,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonction MEDIAN',
    difficulty: 'medium',
    tags: ['MEDIAN', 'Moyenne vs Médiane', 'Agrégats'],
    front: {
      question: 'Quelle est la différence fondamentale entre les fonctions AVG et MEDIAN sous Oracle SQL ?',
      hint: 'Pensez à la sensibilité aux valeurs extrêmes et au calcul de la valeur centrale.',
    },
    back: {
      answer: 'AVG calcule la moyenne arithmétique (somme / nombre), tandis que MEDIAN renvoie la valeur du milieu (50e percentile) qui partage la distribution en deux moitiés égales.',
      explanation: 'MEDIAN ordonne les valeurs non-NULL : si le nombre d\'éléments est impair, il renvoie la valeur centrale exacte ; s\'il est pair, il renvoie la moyenne des deux valeurs centrales. MEDIAN accepte les types NUMBER et DATE/TIMESTAMP.',
      codeSnippet: 'SELECT \n  AVG(salary)    AS salaire_moyen,\n  MEDIAN(salary) AS salaire_median\nFROM employees;',
      examTrap: 'Contrairement à AVG qui ne fonctionne que sur les NUMBER, MEDIAN fonctionne également sur les colonnes de type DATE et TIMESTAMP !',
      ruleRef: 'Oracle SQL Reference - MEDIAN Function',
    },
  },
  {
    id: 'fc-dom03-007',
    cardNumber: 7,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'DISTINCT vs ALL dans les agrégats',
    difficulty: 'easy',
    tags: ['DISTINCT', 'ALL', 'SUM', 'AVG'],
    front: {
      question: 'Quel est le mot-clé par défaut appliqué aux arguments des fonctions d\'agrégation si ni DISTINCT ni ALL n\'est spécifié ?',
      codeSnippet: 'SELECT SUM(salary), SUM(ALL salary) FROM employees;',
      hint: 'Prend-on en compte les doublons par défaut ?',
    },
    back: {
      answer: 'Le mot-clé par défaut est ALL (tous les doublons sont conservés).',
      explanation: 'SUM(salary) est strictement équivalent à SUM(ALL salary). Toutes les occurrences non-NULL sont additionnées. Pour ne comptabiliser qu\'une seule fois chaque valeur distincte, il faut spécifier explicitement le mot-clé DISTINCT.',
      codeSnippet: '-- Calcule la somme de chaque valeur unique de salaire :\nSELECT SUM(DISTINCT salary) FROM employees;\n\n-- Calcule la somme de tous les salaires avec doublons :\nSELECT SUM(ALL salary), SUM(salary) FROM employees;',
      examTrap: 'SUM(DISTINCT salary) produira un résultat beaucoup plus faible que SUM(salary) dès lors que plusieurs employés ont le même salaire. L\'examen teste régulièrement cette distinction.',
      ruleRef: 'Aggregate Functions Syntax - ALL vs DISTINCT',
    },
  },
  {
    id: 'fc-dom03-008',
    cardNumber: 8,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'DISTINCT dans AVG',
    difficulty: 'medium',
    tags: ['AVG', 'DISTINCT', 'Calcul'],
    front: {
      question: 'Si une table contient les valeurs de salaires (1000, 1000, 1000, 4000), que renvoient respectivement AVG(salary) et AVG(DISTINCT salary) ?',
      hint: 'Calculez la somme et le diviseur pour chaque cas.',
    },
    back: {
      answer: 'AVG(salary) renvoie 1750, alors que AVG(DISTINCT salary) renvoie 2500.',
      explanation: 'Pour AVG(salary) : (1000 + 1000 + 1000 + 4000) / 4 = 7000 / 4 = 1750. Pour AVG(DISTINCT salary), les doublons 1000 sont éliminés, ne laissant que (1000, 4000) : (1000 + 4000) / 2 = 5000 / 2 = 2500.',
      codeSnippet: 'SELECT \n  AVG(salary) AS moyenne_globale,          -- 1750\n  AVG(DISTINCT salary) AS moyenne_distincte -- 2500\nFROM (SELECT 1000 AS salary FROM dual UNION ALL\n      SELECT 1000 FROM dual UNION ALL\n      SELECT 1000 FROM dual UNION ALL\n      SELECT 4000 FROM dual);',
      examTrap: 'Ne confondez pas DISTINCT dans le SELECT : SELECT DISTINCT AVG(salary) ne change rien au résultat mathématique de la moyenne, alors que AVG(DISTINCT salary) change le calcul intérieur !',
      ruleRef: 'Oracle SQL Reference - Aggregate Arguments',
    },
  },
  {
    id: 'fc-dom03-009',
    cardNumber: 9,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'DISTINCT avec MIN et MAX',
    difficulty: 'easy',
    tags: ['MIN', 'MAX', 'DISTINCT', 'Optimisation'],
    front: {
      question: 'L\'utilisation du mot-clé DISTINCT dans MIN(DISTINCT col) ou MAX(DISTINCT col) a-t-elle un impact sur la valeur finale retournée par rapport à MIN(col) ou MAX(col) ?',
      hint: 'Le plus petit élément d\'un ensemble sans doublons est-il différent du plus petit élément avec doublons ?',
    },
    back: {
      answer: 'Non, le résultat retourné est strictement identique. La syntaxe est valide mais redondante.',
      explanation: 'Que l\'on déduplique ou non les valeurs avant de chercher le minimum ou le maximum, la plus petite valeur ou la plus grande valeur reste exactement la même.',
      codeSnippet: 'SELECT \n  MIN(salary), MIN(DISTINCT salary),\n  MAX(salary), MAX(DISTINCT salary)\nFROM employees;\n-- Les colonnes deux à deux produisent exactement les mêmes résultats.',
      examTrap: 'Bien que mathématiquement inutile, la syntaxe MIN(DISTINCT col) est parfaitement valide sous Oracle et ne lève aucune erreur ! L\'examen pose parfois la question pour tester si vous pensez que c\'est une erreur de syntaxe.',
      ruleRef: 'SQL Reference - MIN, MAX with DISTINCT',
    },
  },
  {
    id: 'fc-dom03-010',
    cardNumber: 10,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Agrégats dans les expressions arithmétiques',
    difficulty: 'medium',
    tags: ['Arithmétique', 'Expressions', 'SUM', 'COUNT'],
    front: {
      question: 'Est-il légal de combiner des fonctions d\'agrégation dans des expressions arithmétiques au sein de la clause SELECT (ex: SUM(salary) / COUNT(*)) ?',
      hint: 'Une fonction d\'agrégation produit une valeur scalaire une fois le groupe formé.',
    },
    back: {
      answer: 'Oui, c\'est totalement valide et très courant pour construire des métriques personnalisées.',
      explanation: 'Chaque fonction d\'agrégation retourne une valeur scalaire pour le groupe. Vous pouvez donc additionner, soustraire, multiplier ou diviser ces résultats, ou les imbriquer dans des fonctions scalaires comme ROUND(SUM(salary) / COUNT(*), 2).',
      codeSnippet: 'SELECT \n  department_id,\n  SUM(salary) / COUNT(*) AS calc_avg,\n  AVG(salary) AS direct_avg,\n  ROUND((SUM(salary) / SUM(SUM(salary)) OVER()) * 100, 2) AS pct_share\nFROM employees\nGROUP BY department_id;',
      examTrap: 'Attention : SUM(salary / COUNT(*)) est en revanche STRICTEMENT INTERDIT car on ne peut pas mettre une fonction de groupe à l\'intérieur de l\'argument d\'une fonction de groupe de cette façon sans enfreindre les règles d\'imbrication.',
      ruleRef: 'SQL Expressions - Combining Aggregate Results',
    },
  },
  {
    id: 'fc-dom03-011',
    cardNumber: 11,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonctions scalaires dans les agrégats',
    difficulty: 'medium',
    tags: ['Imbrication', 'Scalaire', 'UPPER', 'LENGTH', 'SUM'],
    front: {
      question: 'Peut-on passer le résultat d\'une fonction scalaire (ex: LENGTH, UPPER, ROUND, NVL) en argument d\'une fonction d\'agrégation ?',
      codeSnippet: 'SELECT AVG(LENGTH(last_name)), SUM(ROUND(salary, -3)) FROM employees;',
      hint: 'L\'évaluation scalaire a lieu sur chaque ligne avant l\'agrégation.',
    },
    back: {
      answer: 'Oui, c\'est parfaitement légal et fréquemment utilisé.',
      explanation: 'Oracle évalue d\'abord la fonction scalaire sur chaque ligne individuelle, puis la fonction d\'agrégation opère sur l\'ensemble des résultats scalaires ainsi calculés.',
      codeSnippet: 'SELECT \n  AVG(LENGTH(last_name)) AS longueur_moyenne_nom,\n  MAX(ROUND(salary, -2))  AS salaire_arrondi_max\nFROM employees;',
      examTrap: 'Ne confondez pas : "Fonction scalaire à l\'intérieur d\'un agrégat" (ex: AVG(NVL(col,0))) qui est TOUJOURS autorisée, et "Fonction d\'agrégation dans la clause WHERE" qui est STRICTEMENT INTERDITE.',
      ruleRef: 'SQL Language Reference - Expressions in Arguments of Group Functions',
    },
  },
  {
    id: 'fc-dom03-012',
    cardNumber: 12,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Erreur ORA-00934',
    difficulty: 'hard',
    tags: ['ORA-00934', 'WHERE', 'Erreur', 'Ordre d\'exécution'],
    front: {
      question: 'Quel message d\'erreur Oracle génère-t-il si une fonction d\'agrégation est placée directement dans la clause WHERE d\'une requête ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nWHERE AVG(salary) > 5000\nGROUP BY department_id;',
      hint: 'La clause WHERE s\'exécute avant que les groupes ne soient formés.',
    },
    back: {
      answer: 'ORA-00934: group function is not allowed here (fonction de groupe non autorisée ici).',
      explanation: 'Dans le cycle de vie logique d\'une requête SQL, la clause WHERE filtre les lignes individuelles lors de la lecture des blocs de données, AVANT toute constitution de groupe. Les agrégats n\'existent pas encore à ce stade, d\'où l\'erreur.',
      codeSnippet: '-- INCORRECT (ORA-00934) :\nSELECT department_id FROM employees WHERE AVG(salary) > 5000 GROUP BY department_id;\n\n-- CORRECT (avec HAVING) :\nSELECT department_id, AVG(salary) FROM employees GROUP BY department_id HAVING AVG(salary) > 5000;',
      examTrap: 'C\'est l\'un des pièges les plus fréquents de l\'examen 1Z0-071 : repérez immédiatement toute fonction de groupe (SUM, COUNT, etc.) dans une clause WHERE, sauf si elle se trouve à l\'intérieur d\'une sous-requête !',
      ruleRef: 'Oracle Error Messages - ORA-00934',
    },
  },
  {
    id: 'fc-dom03-013',
    cardNumber: 13,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Sous-requête dans WHERE avec agrégat',
    difficulty: 'medium',
    tags: ['WHERE', 'Sous-requête', 'AVG', 'Exceptions'],
    front: {
      question: 'Une fonction de groupe peut-elle apparaître dans la clause WHERE si elle est encapsulée dans une sous-requête ?',
      codeSnippet: 'SELECT last_name, salary \nFROM employees \nWHERE salary > (SELECT AVG(salary) FROM employees);',
      hint: 'La sous-requête s\'exécute-t-elle de manière autonome ?',
    },
    back: {
      answer: 'Oui ! C\'est parfaitement légal et c\'est la manière standard de comparer une valeur individuelle à un agrégat global.',
      explanation: 'La sous-requête (SELECT AVG(salary) FROM employees) s\'exécute et retourne une constante scalaire (ex: 6461). La clause WHERE compare ensuite simplement la colonne individuelle salary à ce résultat scalaire.',
      codeSnippet: 'SELECT employee_id, last_name, salary\nFROM employees\nWHERE salary > (SELECT AVG(salary) FROM employees);',
      examTrap: 'Ne confondez pas : WHERE salary > AVG(salary) qui provoque ORA-00934, et WHERE salary > (SELECT AVG(salary) FROM employees) qui est 100% valide.',
      ruleRef: 'Subqueries with Aggregate Functions in WHERE Clause',
    },
  },
  {
    id: 'fc-dom03-014',
    cardNumber: 14,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Agrégat sans clause GROUP BY',
    difficulty: 'easy',
    tags: ['Table entière', 'Agrégat global', 'Groupe unique'],
    front: {
      question: 'Que constitue l\'ensemble des lignes d\'une table pour une fonction d\'agrégation lorsqu\'AUCUNE clause GROUP BY n\'est spécifiée ?',
      codeSnippet: 'SELECT COUNT(*), AVG(salary), MAX(salary) FROM employees;',
      hint: 'Combien de lignes la requête retourne-t-elle ?',
    },
    back: {
      answer: 'La table entière (ou l\'ensemble filtré par le WHERE) est considérée comme un SEUL et UNIQUE groupe.',
      explanation: 'La requête retourne exactement UNE SEULE ligne de résultat représentant les agrégats de toutes les lignes satisfaisant la condition de filtre.',
      codeSnippet: 'SELECT COUNT(*), MAX(salary), MIN(salary) \nFROM employees \nWHERE department_id = 10;\n-- Retourne exactement 1 ligne de résultat.',
      examTrap: 'Si la condition WHERE ne sélectionne aucune ligne, la requête renvoie tout de même UNE ligne contenant 0 pour COUNT(*) et NULL pour les autres agrégats.',
      ruleRef: 'Single-Group Aggregate Queries',
    },
  },
  {
    id: 'fc-dom03-015',
    cardNumber: 15,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Erreur ORA-00937',
    difficulty: 'medium',
    tags: ['ORA-00937', 'Mélange colonne agrégat', 'GROUP BY manquant'],
    front: {
      question: 'Quelle erreur Oracle se produit si vous tentez de sélectionner une colonne individuelle aux côtés d\'une fonction d\'agrégation sans clause GROUP BY ?',
      codeSnippet: 'SELECT department_id, AVG(salary) FROM employees;',
      hint: 'Une colonne veut N lignes, l\'agrégat n\'en produit qu\'une seule.',
    },
    back: {
      answer: 'ORA-00937: not a single-group group function (la fonction de groupe ne s\'applique pas à un groupe unique).',
      explanation: 'department_id souhaite retourner 107 lignes (une par employé), tandis que AVG(salary) sans GROUP BY ne calcule qu\'une seule ligne pour toute la table. Oracle ne peut pas juxtaposer des flux de cardinalité différente sans instruction de regroupement.',
      codeSnippet: '-- ERREUR ORA-00937 :\nSELECT department_id, AVG(salary) FROM employees;\n\n-- CORRECTION :\nSELECT department_id, AVG(salary) FROM employees GROUP BY department_id;',
      examTrap: 'Chaque fois qu\'une colonne non agrégée apparaît dans le SELECT sans clause GROUP BY, c\'est systématiquement ORA-00937 à l\'examen.',
      ruleRef: 'Oracle Error Messages - ORA-00937',
    },
  },

  // =========================================================================
  // SECTION 2 : LA FONCTION COUNT EN DÉTAIL (16 à 25)
  // =========================================================================
  {
    id: 'fc-dom03-016',
    cardNumber: 16,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT(*) vs COUNT(colonne)',
    difficulty: 'easy',
    tags: ['COUNT', 'NULL', 'Différence fondamentale'],
    front: {
      question: 'Quelle est la différence fondamentale de comptage entre COUNT(*) et COUNT(commission_pct) ?',
      hint: 'Que fait chacune face aux valeurs NULL ?',
    },
    back: {
      answer: 'COUNT(*) compte toutes les lignes sans exception (y compris les lignes avec NULLs et doublons), tandis que COUNT(commission_pct) ne compte que les lignes où commission_pct IS NOT NULL.',
      explanation: 'COUNT(*) évalue l\'existence même de la ligne. COUNT(expression) évalue l\'expression sur chaque ligne et n\'incrémente le compteur que si l\'expression s\'évalue à une valeur non nulle.',
      codeSnippet: 'SELECT \n  COUNT(*)                 AS total_employes,     -- 107\n  COUNT(commission_pct)    AS avec_commission,    -- 35 (ignore les NULLs)\n  COUNT(department_id)     AS avec_departement    -- 106 (1 employé sans dept)\nFROM employees;',
      examTrap: 'Si une table a 10 lignes et que la colonne col vaut NULL sur 4 lignes, COUNT(*) renvoie 10, alors que COUNT(col) renvoie 6. Ce piège tombe à chaque session d\'examen.',
      ruleRef: 'SQL Language Reference - COUNT(*), COUNT(expr)',
    },
  },
  {
    id: 'fc-dom03-017',
    cardNumber: 17,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT(1) vs COUNT(*)',
    difficulty: 'medium',
    tags: ['COUNT(1)', 'COUNT(*)', 'Performance', 'Optimiseur'],
    front: {
      question: 'Existe-t-il une différence de performance ou de résultat sous le moteur d\'optimisation moderne d\'Oracle entre COUNT(*), COUNT(1) et COUNT(\'x\') ?',
      hint: 'Une constante peut-elle valoir NULL ?',
    },
    back: {
      answer: 'Non, aucune différence ! Le résultat est rigoureusement identique et l\'optimiseur CBO d\'Oracle les traite de façon strictement équivalente.',
      explanation: 'Dans COUNT(1) ou COUNT(\'x\'), l\'expression constante ne vaut jamais NULL pour aucune ligne. L\'optimiseur réécrit en interne COUNT(1) en COUNT(*) et choisit le plan d\'exécution le plus rapide (scan d\'index le plus étroit possible ou Bitmap index scan).',
      codeSnippet: 'SELECT COUNT(*), COUNT(1), COUNT(\'X\'), COUNT(0) FROM employees;\n-- Tous renvoient exactement le même nombre (107) avec le même plan d\'exécution.',
      examTrap: 'Certains faux mythes affirment que COUNT(1) est plus rapide que COUNT(*). À l\'examen Oracle, ils sont considérés comme sémantiquement et techniquement équivalents.',
      ruleRef: 'Oracle Cost-Based Optimizer Guide - Aggregate Optimization',
    },
  },
  {
    id: 'fc-dom03-018',
    cardNumber: 18,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT(DISTINCT expr)',
    difficulty: 'medium',
    tags: ['COUNT', 'DISTINCT', 'Doublons', 'NULL'],
    front: {
      question: 'Que fait COUNT(DISTINCT department_id) et comment traite-t-il les doublons et les valeurs NULL ?',
      codeSnippet: 'SELECT COUNT(DISTINCT department_id) FROM employees;',
      hint: 'Combien de départements uniques sont comptabilisés ?',
    },
    back: {
      answer: 'Il compte le nombre de valeurs distinctes non-NULL de department_id. Les doublons sont éliminés et les valeurs NULL sont ignorées.',
      explanation: 'Si 100 employés sont répartis sur 11 départements différents et que l\'un d\'eux n\'a pas de département (NULL), COUNT(DISTINCT department_id) retournera 11 (et non 12 !).',
      codeSnippet: 'SELECT \n  COUNT(*)                        AS total_lignes,        -- 107\n  COUNT(department_id)           AS depts_non_nulls,     -- 106\n  COUNT(DISTINCT department_id)  AS nb_depts_uniques     -- 11\nFROM employees;',
      examTrap: 'DISTINCT n\'inclut JAMAIS NULL dans le décompte de COUNT(DISTINCT col), car la fonction COUNT(expr) ignore par définition les NULLs avant même de vérifier l\'unicité.',
      ruleRef: 'SQL Reference - COUNT with DISTINCT',
    },
  },
  {
    id: 'fc-dom03-019',
    cardNumber: 19,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT(DISTINCT a, b) - Syntaxe Oracle',
    difficulty: 'hard',
    tags: ['COUNT', 'DISTINCT', 'Multi-colonnes', 'Erreur syntaxe'],
    front: {
      question: 'La syntaxe COUNT(DISTINCT col1, col2) avec plusieurs colonnes séparées par une virgule est-elle valide sous Oracle SQL standard ?',
      codeSnippet: 'SELECT COUNT(DISTINCT department_id, job_id) FROM employees;',
      hint: 'Combien d\'arguments la fonction COUNT accepte-t-elle ?',
    },
    back: {
      answer: 'Non ! COUNT n\'accepte qu\'un SEUL argument. Cette écriture produit ORA-00909: invalid number of arguments.',
      explanation: 'Pour compter le nombre de combinaisons distinctes de deux colonnes sans sous-requête, il faut soit concaténer les colonnes (ex: COUNT(DISTINCT department_id || \'-\' || job_id)), soit utiliser une sous-requête avec COUNT(*).',
      codeSnippet: '-- ERREUR (ORA-00909) :\nSELECT COUNT(DISTINCT department_id, job_id) FROM employees;\n\n-- SOLUTIONS VALIDES :\nSELECT COUNT(DISTINCT department_id || \'#\' || job_id) FROM employees;\n-- Ou bien :\nSELECT COUNT(*) FROM (SELECT DISTINCT department_id, job_id FROM employees);',
      examTrap: 'Bien que certains SGBD (comme MySQL) autorisent COUNT(DISTINCT a, b), sous Oracle c\'est une erreur de syntaxe stricte (ORA-00909). Très fréquent à l\'examen 1Z0-071.',
      ruleRef: 'Oracle SQL Reference - COUNT Syntax Arguments',
    },
  },
  {
    id: 'fc-dom03-020',
    cardNumber: 20,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT conditionnel avec CASE',
    difficulty: 'medium',
    tags: ['COUNT', 'CASE', 'Agrégation conditionnelle'],
    front: {
      question: 'Comment compter uniquement les employés ayant un salaire > 10000 en utilisant la combinaison de COUNT et CASE ?',
      hint: 'Que doit retourner la branche ELSE du CASE pour que COUNT ne l\'incrémente pas ?',
    },
    back: {
      answer: 'COUNT(CASE WHEN salary > 10000 THEN 1 END) ou COUNT(CASE WHEN salary > 10000 THEN salary END).',
      explanation: 'Dans une expression CASE, si la clause ELSE est omise, elle retourne implicitement NULL. Comme COUNT(expression) ignore les valeurs NULL, seules les lignes satisfaisant la condition WHEN sont comptabilisées !',
      codeSnippet: 'SELECT \n  COUNT(*) AS total_staff,\n  COUNT(CASE WHEN salary > 10000 THEN 1 END) AS hauts_salaires,\n  COUNT(CASE WHEN salary <= 10000 THEN \'OK\' END) AS salaires_normaux\nFROM employees;',
      examTrap: 'Si vous écrivez COUNT(CASE WHEN salary > 10000 THEN 1 ELSE 0 END), COUNT comptera TOUTES les lignes car 0 n\'est PAS NULL ! Il faut soit omettre ELSE, soit utiliser SUM au lieu de COUNT.',
      ruleRef: 'Conditional Aggregation Techniques in SQL',
    },
  },
  {
    id: 'fc-dom03-021',
    cardNumber: 21,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT avec littéral NULL',
    difficulty: 'hard',
    tags: ['COUNT', 'NULL littéral', 'Cas limite'],
    front: {
      question: 'Quel résultat retourne la requête suivante exécutée sur la table employees comportant 107 lignes ?',
      codeSnippet: 'SELECT COUNT(NULL) FROM employees;',
      hint: 'COUNT(expr) incrémente-t-il lorsque expr vaut NULL ?',
    },
    back: {
      answer: 'Elle retourne 0.',
      explanation: 'L\'expression fournie en argument est le littéral NULL. Pour chaque ligne de la table, l\'argument vaut NULL, donc le compteur de COUNT n\'est jamais incrémenté et renvoie 0.',
      codeSnippet: 'SELECT COUNT(NULL) FROM employees;\n-- Résultat : 0 (sur 107 lignes existantes)',
      examTrap: 'Ne confondez pas avec COUNT(*), COUNT(1) ou COUNT(0) qui renvoient tous 107. COUNT(NULL) renvoie toujours 0.',
      ruleRef: 'SQL Aggregate Evaluation of Literal NULLs',
    },
  },
  {
    id: 'fc-dom03-022',
    cardNumber: 22,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT et clause WHERE fausse',
    difficulty: 'easy',
    tags: ['COUNT', 'WHERE faux', 'Valeur de retour'],
    front: {
      question: 'Quel est le résultat de : SELECT COUNT(employee_id) FROM employees WHERE 1 = 0 ?',
      hint: 'La table contient-elle des lignes satisfaisant 1 = 0 ? Que renvoie COUNT ?',
    },
    back: {
      answer: '0 (une seule ligne contenant la valeur numérique 0).',
      explanation: 'COUNT(expr) sans clause GROUP BY renvoie toujours au moins une ligne de résultat avec la valeur 0 lorsque aucune ligne ne correspond aux filtres.',
      codeSnippet: 'SELECT COUNT(employee_id) FROM employees WHERE 1 = 0;\n-- Résultat : 0 (1 ligne)',
      examTrap: 'L\'examen propose souvent "No rows selected" ou "NULL". C\'est faux : la requête renvoie bien 1 ligne contenant la valeur 0.',
      ruleRef: 'COUNT on Zero Rows Result Set',
    },
  },
  {
    id: 'fc-dom03-023',
    cardNumber: 23,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT dans ORDER BY',
    difficulty: 'medium',
    tags: ['ORDER BY', 'COUNT', 'Tri par agrégat'],
    front: {
      question: 'Peut-on trier les groupes par COUNT(*) ou par une fonction d\'agrégation qui ne figure PAS dans la clause SELECT ?',
      codeSnippet: 'SELECT department_id\nFROM employees\nGROUP BY department_id\nORDER BY COUNT(*) DESC;',
      hint: 'La clause ORDER BY s\'exécute après la constitution et l\'agrégation des groupes.',
    },
    back: {
      answer: 'Oui, c\'est parfaitement valide sous Oracle SQL.',
      explanation: 'Puisque la clause ORDER BY s\'exécute après les clauses GROUP BY et HAVING, les agrégats de chaque groupe sont déjà calculés en mémoire. On peut donc trier sur COUNT(*), AVG(salary) ou n\'importe quel agrégat valide sans obligation de l\'afficher dans le SELECT.',
      codeSnippet: 'SELECT department_id\nFROM employees\nGROUP BY department_id\nORDER BY COUNT(*) DESC;\n-- Affiche uniquement les identifiants de départements, triés du plus grand effectif au plus petit.',
      examTrap: 'Attention : l\'inverse (trier par une colonne individuelle non agrégée qui n\'est pas dans le GROUP BY) est en revanche STRICTEMENT INTERDIT (ORA-00979).',
      ruleRef: 'SQL Order of Execution - ORDER BY with Aggregate Expressions',
    },
  },
  {
    id: 'fc-dom03-024',
    cardNumber: 24,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT avec jointure externe',
    difficulty: 'hard',
    tags: ['COUNT', 'OUTER JOIN', 'Piège classique'],
    front: {
      question: 'Dans une jointure LEFT OUTER JOIN entre départements et employés, pourquoi faut-il utiliser COUNT(e.employee_id) plutôt que COUNT(*) pour compter les employés par département ?',
      hint: 'Que produit COUNT(*) sur un département sans aucun employé ?',
    },
    back: {
      answer: 'COUNT(*) renverrait 1 (car la ligne du département existe avec des colonnes employé à NULL), alors que COUNT(e.employee_id) renvoie correctement 0.',
      explanation: 'Dans un LEFT JOIN, un département orphelin génère une ligne où toutes les colonnes de la table employees valent NULL. COUNT(*) compte la ligne et donne 1 ! COUNT(e.employee_id) évalue la colonne de l\'employé (qui est NULL) et renvoie donc judicieusement 0.',
      codeSnippet: 'SELECT \n  d.department_name,\n  COUNT(*)             AS faux_total,    -- Donne 1 pour les depts vides !\n  COUNT(e.employee_id) AS vrai_total     -- Donne 0 pour les depts vides !\nFROM departments d\nLEFT OUTER JOIN employees e ON d.department_id = e.department_id\nGROUP BY d.department_name;',
      examTrap: 'C\'est l\'un des pièges les plus redoutables de l\'examen 1Z0-071 sur les rapports d\'agrégation et jointures externes.',
      ruleRef: 'Outer Joins with Aggregate Functions Best Practices',
    },
  },
  {
    id: 'fc-dom03-025',
    cardNumber: 25,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'COUNT et expressions booléennes',
    difficulty: 'medium',
    tags: ['COUNT', 'Booléen', 'Syntaxe invalide'],
    front: {
      question: 'La syntaxe suivante est-elle autorisée sous Oracle SQL ? SELECT COUNT(salary > 5000) FROM employees;',
      hint: 'Le type de données BOOLEAN existe-t-il dans le moteur SQL Oracle natif ?',
    },
    back: {
      answer: 'Non, c\'est une erreur de syntaxe (ORA-00907 ou ORA-00920). Le moteur SQL d\'Oracle ne possède pas de type BOOLEAN dans les requêtes standards.',
      explanation: 'Contrairement à PostgreSQL ou MySQL où une condition renvoie un booléen (1 ou 0), Oracle SQL exige une expression complète avec CASE ou DECODE pour effectuer un comptage sous condition.',
      codeSnippet: '-- ERREUR sous Oracle SQL :\nSELECT COUNT(salary > 5000) FROM employees;\n\n-- SYNTAXE VALIDE :\nSELECT COUNT(CASE WHEN salary > 5000 THEN 1 END) FROM employees;',
      examTrap: 'Ne vous laissez pas tromper par la syntaxe issue d\'autres moteurs SQL. Sous Oracle, l\'argument de COUNT ne peut pas être un prédicat de comparaison direct.',
      ruleRef: 'Oracle SQL Data Types - No Native Boolean in SQL Queries',
    },
  },

  // =========================================================================
  // SECTION 3 : LISTAGG & AGRÉGATS SPÉCIAUX (26 à 35)
  // =========================================================================
  {
    id: 'fc-dom03-026',
    cardNumber: 26,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonction LISTAGG',
    difficulty: 'medium',
    tags: ['LISTAGG', 'WITHIN GROUP', 'Concaténation', 'Syntaxe'],
    front: {
      question: 'Quelle est la clause syntaxique OBLIGATOIRE qui doit accompagner la fonction d\'agrégation LISTAGG sous Oracle ?',
      hint: 'Comment spécifier l\'ordre de concaténation des chaînes au sein de chaque groupe ?',
    },
    back: {
      answer: 'La clause WITHIN GROUP (ORDER BY ...).',
      explanation: 'LISTAGG concatène les valeurs d\'une colonne en une seule chaîne de caractères délimitée. La clause WITHIN GROUP (ORDER BY colonne [ASC|DESC]) est obligatoire pour garantir un ordre d\'assemblage déterministe.',
      codeSnippet: 'SELECT \n  department_id,\n  LISTAGG(last_name, \', \') WITHIN GROUP (ORDER BY last_name) AS liste_salaries\nFROM employees\nGROUP BY department_id;',
      examTrap: 'Omettre la clause WITHIN GROUP (ORDER BY ...) provoque immédiatement l\'erreur ORA-30497 ou ORA-00923 lors de la compilation de la requête.',
      ruleRef: 'Oracle SQL Reference - LISTAGG Aggregate Function',
    },
  },
  {
    id: 'fc-dom03-027',
    cardNumber: 27,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'LISTAGG et valeurs NULL',
    difficulty: 'easy',
    tags: ['LISTAGG', 'NULL', 'Comportement'],
    front: {
      question: 'Comment la fonction LISTAGG gère-t-elle les valeurs NULL présentes dans la colonne à concaténer ?',
      hint: 'Ajoute-t-elle des séparateurs vides ou ignore-t-elle simplement les NULLs ?',
    },
    back: {
      answer: 'LISTAGG ignore totalement les valeurs NULL et n\'ajoute aucun séparateur superflu pour elles.',
      explanation: 'Comme les autres fonctions d\'agrégation standards, toute valeur NULL rencontrée dans la colonne passée en premier argument est ignorée lors de la formation de la chaîne de résultat.',
      codeSnippet: '-- Si les noms sont \'King\', NULL, \'Kochhar\'\n-- Le résultat sera \'King, Kochhar\' et non \'King, , Kochhar\'\nSELECT LISTAGG(commission_pct, \'; \') WITHIN GROUP (ORDER BY commission_pct)\nFROM employees\nWHERE department_id = 80;',
      examTrap: 'À l\'examen, si une question suggère que LISTAGG insère deux délimiteurs consécutifs ", ," en présence de NULL, c\'est FAUX.',
      ruleRef: 'LISTAGG Null Handling Specifications',
    },
  },
  {
    id: 'fc-dom03-028',
    cardNumber: 28,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'LISTAGG ON OVERFLOW TRUNCATE (Oracle 12c+ / 19c)',
    difficulty: 'hard',
    tags: ['LISTAGG', '19c', 'ON OVERFLOW', 'ORA-01489'],
    front: {
      question: 'Quelle clause permet d\'éviter l\'erreur ORA-01489: result of string concatenation is too long lorsque LISTAGG dépasse la limite de 4000 octets (VARCHAR2) ?',
      hint: 'Introduite en 12c R2 et testée en 1Z0-071.',
    },
    back: {
      answer: 'La clause ON OVERFLOW TRUNCATE [\'texte_remplacement\'] [WITH | WITHOUT COUNT].',
      explanation: 'Par défaut, si la chaîne résultante dépasse 4000 octets (ou 32767 avec MAX_STRING_SIZE=EXTENDED), Oracle lève ORA-01489. La clause ON OVERFLOW TRUNCATE tronque élégamment le résultat et ajoute un indicateur (ex: \'...\') ainsi que le décompte des valeurs omises.',
      codeSnippet: 'SELECT \n  department_id,\n  LISTAGG(last_name, \', \' ON OVERFLOW TRUNCATE \'...\' WITH COUNT)\n    WITHIN GROUP (ORDER BY last_name) AS noms\nFROM employees\nGROUP BY department_id;',
      examTrap: 'La clause ON OVERFLOW se place AVANT la clause WITHIN GROUP, et non après ! L\'examen teste fréquemment cet ordre syntaxique précis.',
      ruleRef: 'Oracle 19c SQL Reference - LISTAGG ON OVERFLOW Clause',
    },
  },
  {
    id: 'fc-dom03-029',
    cardNumber: 29,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'LISTAGG DISTINCT (Oracle 19c)',
    difficulty: 'medium',
    tags: ['LISTAGG', 'DISTINCT', 'Oracle 19c', 'Nouveauté'],
    front: {
      question: 'Depuis Oracle 19c, peut-on utiliser le mot-clé DISTINCT directement à l\'intérieur de LISTAGG pour éliminer les doublons de la liste concaténée ?',
      codeSnippet: 'SELECT department_id, LISTAGG(DISTINCT job_id, \', \') WITHIN GROUP (ORDER BY job_id) FROM employees GROUP BY department_id;',
      hint: 'Cette fonctionnalité a été nativement introduite dans Oracle Database 19c.',
    },
    back: {
      answer: 'Oui, depuis Oracle 19c, LISTAGG(DISTINCT expr, separator) est pleinement supporté.',
      explanation: 'Avant Oracle 19c, tenter d\'écrire LISTAGG(DISTINCT ...) déclenchait ORA-30498. En 19c (la version cible du 1Z0-071), cette syntaxe est officielle et évite d\'avoir à dédupliquer en sous-requête.',
      codeSnippet: 'SELECT \n  department_id,\n  LISTAGG(DISTINCT job_id, \'; \') WITHIN GROUP (ORDER BY job_id) AS métiers_uniques\nFROM employees\nGROUP BY department_id;',
      examTrap: 'Si l\'examen spécifie un contexte Oracle 19c, LISTAGG(DISTINCT ...) est VALIDE.',
      ruleRef: 'Oracle 19c New Features - LISTAGG DISTINCT Support',
    },
  },
  {
    id: 'fc-dom03-030',
    cardNumber: 30,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'LISTAGG en tant que fonction analytique',
    difficulty: 'hard',
    tags: ['LISTAGG', 'Analytique', 'OVER', 'WITHIN GROUP'],
    front: {
      question: 'LISTAGG peut-elle être utilisée sans clause GROUP BY en tant que fonction de fenêtrage (analytique) ?',
      codeSnippet: 'SELECT last_name, LISTAGG(last_name, \',\') WITHIN GROUP (ORDER BY hire_date) OVER (PARTITION BY department_id) FROM employees;',
      hint: 'Remarquez la présence conjointe de WITHIN GROUP et de OVER().',
    },
    back: {
      answer: 'Oui ! LISTAGG supporte à la fois le mode agrégat (avec GROUP BY) et le mode analytique (avec la clause OVER).',
      explanation: 'Dans sa forme analytique, LISTAGG conserve les lignes de détail tout en affichant pour chaque ligne la concaténation de toutes les valeurs de la partition spécifiée dans la clause OVER (PARTITION BY ...).',
      codeSnippet: 'SELECT \n  employee_id,\n  department_id,\n  last_name,\n  LISTAGG(last_name, \'; \') WITHIN GROUP (ORDER BY hire_date) \n    OVER (PARTITION BY department_id) AS collègues_dept\nFROM employees;',
      examTrap: 'Même en mode analytique avec la clause OVER, la clause WITHIN GROUP (ORDER BY ...) reste STRICTEMENT OBLIGATOIRE.',
      ruleRef: 'Analytic Functions - LISTAGG Syntax and Semantics',
    },
  },
  {
    id: 'fc-dom03-031',
    cardNumber: 31,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Séparateur par défaut dans LISTAGG',
    difficulty: 'medium',
    tags: ['LISTAGG', 'Séparateur', 'Optionnel'],
    front: {
      question: 'Le second argument (séparateur) de la fonction LISTAGG est-il obligatoire ou optionnel sous Oracle SQL ?',
      codeSnippet: 'SELECT LISTAGG(last_name) WITHIN GROUP (ORDER BY last_name) FROM employees;',
      hint: 'Que se passe-t-il si vous ne fournissez aucun séparateur ?',
    },
    back: {
      answer: 'Le séparateur est OPTIONNEL. Si omis, les valeurs sont concaténées directement sans aucun séparateur intermédiaire.',
      explanation: 'LISTAGG(expr) concatène les chaînes bout à bout (équivalent à une chaîne vide \'\' comme délimiteur). Pour insérer un espace ou une virgule, il faut le préciser en second argument.',
      codeSnippet: 'SELECT LISTAGG(last_name) WITHIN GROUP (ORDER BY last_name) AS chaine_continue\nFROM employees WHERE department_id = 90;\n-- Résultat : \'De HaanKingKochhar\'',
      examTrap: 'Ne croyez pas que la virgule est le séparateur par défaut : sans second argument, aucun caractère de séparation n\'est inséré.',
      ruleRef: 'Oracle SQL Reference - LISTAGG Parameters',
    },
  },
  {
    id: 'fc-dom03-032',
    cardNumber: 32,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonctions CORR, COVAR_POP, COVAR_SAMP',
    difficulty: 'hard',
    tags: ['Statistiques avancées', 'CORR', 'Agrégats'],
    front: {
      question: 'Que calcule la fonction d\'agrégation binaire CORR(expr1, expr2) sous Oracle ?',
      hint: 'Statistique mesurant la relation linéaire entre deux variables numériques.',
    },
    back: {
      answer: 'Elle calcule le coefficient de corrélation linéaire de Pearson entre deux séries numériques.',
      explanation: 'CORR prend deux expressions numériques en arguments et retourne un nombre entre -1 et +1. Les paires contenant au moins une valeur NULL sont ignorées.',
      codeSnippet: 'SELECT ROUND(CORR(salary, (SYSDATE - hire_date)), 4) AS correlation_anciennete_salaire\nFROM employees;',
      examTrap: 'CORR exige impérativement 2 arguments numériques. Si une table ne compte aucune paire valide, elle retourne NULL.',
      ruleRef: 'SQL Aggregate Functions - CORR, COVAR_POP',
    },
  },
  {
    id: 'fc-dom03-033',
    cardNumber: 33,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Agrégats définis par l\'utilisateur',
    difficulty: 'hard',
    tags: ['ODCIAggregate', 'Personnalisé', 'Concept'],
    front: {
      question: 'Oracle permet-il de créer ses propres fonctions d\'agrégation personnalisées en PL/SQL ?',
      hint: 'Pensez à l\'interface système standard ODCIAggregate.',
    },
    back: {
      answer: 'Oui, via l\'interface d\'extensibilité ODCIAggregate (implémentant Iterate, Merge, Terminate).',
      explanation: 'Oracle permet de définir des types objets PL/SQL conformes à l\'interface ODCIAggregate pour créer des fonctions d\'agrégation sur-mesure (ex: produit géométrique, calcul de médiane complexe).',
      codeSnippet: '-- Création d\'une fonction d\'agrégat personnalisée :\nCREATE OR REPLACE FUNCTION SecondHighest(input NUMBER)\nRETURN NUMBER PARALLEL_ENABLE AGGREGATE USING SecondHighestImpl;',
      examTrap: 'Il suffit de savoir pour l\'examen 1Z0-071 qu\'Oracle supporte les fonctions d\'agrégation personnalisées (User-Defined Aggregate Functions).',
      ruleRef: 'Oracle Data Cartridge Developer\'s Guide - User-Defined Aggregate Functions',
    },
  },
  {
    id: 'fc-dom03-034',
    cardNumber: 34,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'STATS_MODE',
    difficulty: 'medium',
    tags: ['STATS_MODE', 'Statistiques', 'Mode'],
    front: {
      question: 'Quelle fonction d\'agrégation Oracle permet de trouver le "mode" statistique (la valeur la plus fréquente) d\'un ensemble de valeurs ?',
      hint: 'Fonction de la famille STATS_*',
    },
    back: {
      answer: 'La fonction STATS_MODE(expr).',
      explanation: 'STATS_MODE prend un ensemble de valeurs en entrée et retourne la valeur qui apparaît le plus grand nombre de fois. Contrairement à MEDIAN qui calcule le milieu, STATS_MODE identifie la valeur la plus fréquente.',
      codeSnippet: 'SELECT \n  STATS_MODE(department_id) AS dept_plus_frequent,\n  STATS_MODE(salary)        AS salaire_le_plus_courant\nFROM employees;',
      examTrap: 'Si plusieurs valeurs ont la même fréquence maximale (distribution multimodale), Oracle sélectionne l\'une de ces valeurs de manière non-spécifiée.',
      ruleRef: 'Oracle SQL Reference - STATS_MODE Function',
    },
  },
  {
    id: 'fc-dom03-035',
    cardNumber: 35,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'REGR_SLOPE & Famille régression linéaire',
    difficulty: 'hard',
    tags: ['Régression', 'REGR_*', 'Agrégats'],
    front: {
      question: 'À quelle famille appartiennent les fonctions REGR_SLOPE, REGR_INTERCEPT et REGR_R2 ?',
      hint: 'Analyse statistique de deux variables.',
    },
    back: {
      answer: 'Aux fonctions d\'agrégation de régression linéaire binaire d\'Oracle.',
      explanation: 'Ces fonctions calculent la pente (slope), l\'ordonnée à l\'origine (intercept) et le coefficient de détermination ($R^2$) de la droite des moindres carrés entre deux variables numériques dépendantes et indépendantes.',
      codeSnippet: 'SELECT \n  ROUND(REGR_SLOPE(salary, commission_pct), 2) AS pente,\n  ROUND(REGR_INTERCEPT(salary, commission_pct), 2) AS ordonnee_origine\nFROM employees;',
      examTrap: 'Dans toutes les fonctions REGR_*(Y, X), le premier paramètre est la variable dépendante (Y) et le second paramètre est la variable indépendante (X). L\'ordre compte !',
      ruleRef: 'SQL Aggregate Functions - Linear Regression Functions',
    },
  },

  // =========================================================================
  // SECTION 4 : LA RÈGLE D'OR DE GROUP BY & RÈGLES SYNTAXIQUES (36 à 50)
  // =========================================================================
  {
    id: 'fc-dom03-036',
    cardNumber: 36,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Règle d\'or GROUP BY',
    difficulty: 'easy',
    tags: ['GROUP BY', 'Règle d\'or', 'ORA-00979', 'Examen'],
    front: {
      question: 'Énoncez la règle d\'or fondamentale régissant les colonnes de la clause SELECT lorsqu\'une clause GROUP BY est présente.',
      hint: 'Que doit-on faire de toute colonne qui n\'est pas dans une fonction d\'agrégation ?',
    },
    back: {
      answer: 'Toute colonne ou expression présente dans la clause SELECT qui n\'est PAS englobée dans une fonction d\'agrégation DOIT obligatoirement figurer dans la clause GROUP BY.',
      explanation: 'Si une colonne figure dans la liste de sélection sans être agrégée et sans être listée dans GROUP BY, Oracle lève l\'erreur ORA-00979: not a GROUP BY expression.',
      codeSnippet: '-- ERREUR (last_name n\'est ni agrégé ni groupé) :\nSELECT department_id, last_name, AVG(salary)\nFROM employees\nGROUP BY department_id; -- ORA-00979\n\n-- VALIDE :\nSELECT department_id, last_name, AVG(salary)\nFROM employees\nGROUP BY department_id, last_name;',
      examTrap: 'L\'erreur ORA-00979 est de loin l\'erreur la plus fréquemment testée à l\'examen 1Z0-071. Repérez systématiquement les intrus dans le SELECT !',
      ruleRef: 'Oracle SQL Reference - The GROUP BY Clause Rules',
    },
  },
  {
    id: 'fc-dom03-037',
    cardNumber: 37,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Règle réciproque de GROUP BY',
    difficulty: 'easy',
    tags: ['GROUP BY', 'Réciproque', 'SELECT', 'Validité'],
    front: {
      question: 'Une colonne présente dans la clause GROUP BY doit-elle OBLIGATOIREMENT figurer dans la liste de sélection (clause SELECT) ?',
      codeSnippet: 'SELECT AVG(salary)\nFROM employees\nGROUP BY department_id;',
      hint: 'Peut-on regrouper selon un critère sans pour autant afficher ce critère ?',
    },
    back: {
      answer: 'NON ! La règle réciproque n\'existe pas : une colonne PEUT figurer dans GROUP BY sans être mentionnée dans le SELECT.',
      explanation: 'Il est parfaitement légal d\'agréger des données selon une dimension (ex: department_id) et de ne restituer que les métriques agrégées (ex: AVG(salary), COUNT(*)) sans afficher la clé de partitionnement.',
      codeSnippet: 'SELECT AVG(salary), COUNT(*)\nFROM employees\nGROUP BY department_id;\n-- Parfaitement légal : renvoie une ligne par département contenant moyenne et effectif.',
      examTrap: 'À l\'examen, une question affirme souvent qu\'une requête est invalide car "department_id figure dans GROUP BY mais pas dans le SELECT". C\'est 100% FAUX !',
      ruleRef: 'GROUP BY Clause Flexibility Rules',
    },
  },
  {
    id: 'fc-dom03-038',
    cardNumber: 38,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Alias de colonne dans GROUP BY',
    difficulty: 'hard',
    tags: ['Alias', 'GROUP BY', 'ORA-00904', 'Cycle d\'exécution'],
    front: {
      question: 'Peut-on utiliser un alias défini dans la clause SELECT au sein de la clause GROUP BY sous Oracle SQL ?',
      codeSnippet: 'SELECT department_id AS dept, AVG(salary)\nFROM employees\nGROUP BY dept;',
      hint: 'Quel est l\'ordre d\'exécution logique des clauses SQL sous Oracle ?',
    },
    back: {
      answer: 'NON, c\'est STRICTEMENT INTERDIT sous Oracle SQL et déclenche ORA-00904: "DEPT": invalid identifier.',
      explanation: 'Dans l\'ordre d\'exécution des requêtes Oracle (FROM -> WHERE -> GROUP BY -> HAVING -> SELECT -> ORDER BY), la clause GROUP BY est exécutée bien AVANT la clause SELECT. Au moment du GROUP BY, l\'alias "dept" n\'existe pas encore.',
      codeSnippet: '-- ERREUR (ORA-00904) :\nSELECT department_id AS dept, AVG(salary) FROM employees GROUP BY dept;\n\n-- VALIDE :\nSELECT department_id AS dept, AVG(salary) FROM employees GROUP BY department_id;',
      examTrap: 'Certains SGBD (MySQL/PostgreSQL) acceptent les alias dans le GROUP BY. Sous Oracle SQL 19c, c\'est une cause d\'échec immédiate. Seul ORDER BY peut utiliser les alias du SELECT !',
      ruleRef: 'SQL Order of Execution & Alias Scope Restrictions',
    },
  },
  {
    id: 'fc-dom03-039',
    cardNumber: 39,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Numéros de position dans GROUP BY',
    difficulty: 'hard',
    tags: ['Numéros de position', 'GROUP BY', 'ORA-00979'],
    front: {
      question: 'Peut-on utiliser des numéros de position de colonne (ex: GROUP BY 1, 2) au lieu des noms de colonnes dans la clause GROUP BY sous Oracle SQL ?',
      codeSnippet: 'SELECT department_id, job_id, COUNT(*)\nFROM employees\nGROUP BY 1, 2;',
      hint: 'Cette syntaxe est-elle autorisée dans ORDER BY ? Est-elle autorisée dans GROUP BY ?',
    },
    back: {
      answer: 'NON ! Sous Oracle SQL, les numéros de position (ordinal column positions) sont STRICTEMENT INTERDITS dans la clause GROUP BY.',
      explanation: 'Alors que la clause ORDER BY autorise les numéros de position (ex: ORDER BY 1, 2), la clause GROUP BY exige impérativement le nom explicite des colonnes ou des expressions. GROUP BY 1 grouperait sur la constante littérale 1 !',
      codeSnippet: '-- ERREUR ORA-00979 (ou groupement sur constante ne résolvant pas les colonnes du SELECT) :\nSELECT department_id, COUNT(*) FROM employees GROUP BY 1;\n\n-- VALIDE :\nSELECT department_id, COUNT(*) FROM employees GROUP BY department_id ORDER BY 1;',
      examTrap: 'Ne confondez jamais ORDER BY (qui accepte les numéros de position et les alias) et GROUP BY (qui refuse formellement les numéros et les alias).',
      ruleRef: 'Oracle SQL Syntax - Prohibited Expressions in GROUP BY',
    },
  },
  {
    id: 'fc-dom03-040',
    cardNumber: 40,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Expressions complexes dans GROUP BY',
    difficulty: 'medium',
    tags: ['Expressions', 'GROUP BY', 'TO_CHAR', 'EXTRACT'],
    front: {
      question: 'Si la clause SELECT contient TO_CHAR(hire_date, \'YYYY\'), que doit contenir la clause GROUP BY ?',
      codeSnippet: 'SELECT TO_CHAR(hire_date, \'YYYY\'), COUNT(*)\nFROM employees\nGROUP BY ???;',
      hint: 'Faut-il mettre hire_date ou l\'expression exacte ?',
    },
    back: {
      answer: 'Elle doit contenir exactement la même expression : GROUP BY TO_CHAR(hire_date, \'YYYY\').',
      explanation: 'Si vous écrivez GROUP BY hire_date, Oracle groupe par chaque timestamp/seconde distincte de date d\'embauche, ce qui ne correspond pas au regroupement par année souhaité dans le SELECT et produit des résultats morcelés.',
      codeSnippet: '-- BONNE SYNTAXE :\nSELECT TO_CHAR(hire_date, \'YYYY\') AS annee, COUNT(*)\nFROM employees\nGROUP BY TO_CHAR(hire_date, \'YYYY\');\n\n-- Si vous écrivez GROUP BY hire_date, la requête compile mais produit 1 ligne par date complète !',
      examTrap: 'Toute expression calculée non agrégée dans le SELECT doit être répétée à l\'identique dans la clause GROUP BY.',
      ruleRef: 'GROUP BY on Complex Scalar Expressions',
    },
  },
  {
    id: 'fc-dom03-041',
    cardNumber: 41,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Gestion des valeurs NULL dans GROUP BY',
    difficulty: 'medium',
    tags: ['NULL', 'GROUP BY', 'Groupe unique', 'Comportement'],
    front: {
      question: 'Comment la clause GROUP BY traite-t-elle les lignes contenant des valeurs NULL dans la colonne de regroupement ?',
      codeSnippet: 'SELECT department_id, COUNT(*)\nFROM employees\nGROUP BY department_id;',
      hint: 'Crée-t-elle plusieurs groupes pour chaque NULL ou un seul groupe ?',
    },
    back: {
      answer: 'Toutes les lignes ayant une valeur NULL pour la colonne de regroupement sont réunies dans UN SEUL ET UNIQUE groupe distinct.',
      explanation: 'Même si deux NULL ne sont pas égaux au sens de l\'opérateur = (NULL = NULL évalue à UNKNOWN), la clause GROUP BY traite les valeurs NULL comme identiques aux fins de regroupement.',
      codeSnippet: 'SELECT department_id, COUNT(*)\nFROM employees\nGROUP BY department_id;\n-- S\'il y a 3 employés sans département, il y aura UNE seule ligne avec department_id = NULL et COUNT(*) = 3.',
      examTrap: 'Une question affirmant que "les lignes ayant NULL sont exclues du résultat par le GROUP BY" est fausse. Elles apparaissent toujours sous la forme d\'un groupe NULL.',
      ruleRef: 'Oracle SQL Reference - Grouping of NULL Values',
    },
  },
  {
    id: 'fc-dom03-042',
    cardNumber: 42,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Groupement multi-colonnes',
    difficulty: 'easy',
    tags: ['Multi-colonnes', 'GROUP BY', 'Combinaison'],
    front: {
      question: 'Dans une requête avec GROUP BY department_id, job_id, comment les groupes de lignes sont-ils constitués ?',
      codeSnippet: 'SELECT department_id, job_id, SUM(salary)\nFROM employees\nGROUP BY department_id, job_id;',
      hint: 'Pensez aux couples uniques (clé composite).',
    },
    back: {
      answer: 'Un groupe distinct est créé pour chaque combinaison unique de (department_id, job_id).',
      explanation: 'Deux employés ne seront placés dans le même groupe que s\'ils appartiennent AU MÊME département ET exercent LE MÊME métier. La hiérarchie visuelle ou l\'ordre d\'écriture dans GROUP BY ne modifie pas le nombre de groupes générés.',
      codeSnippet: 'SELECT department_id, job_id, COUNT(*)\nFROM employees\nGROUP BY department_id, job_id;\n-- Exemple de groupe : Dept 60 / IT_PROG (5 personnes)',
      examTrap: 'Inverser GROUP BY job_id, department_id produit exactement les mêmes agrégats et le même nombre de groupes ! Seul un ORDER BY explicite garantit l\'ordre de présentation final.',
      ruleRef: 'Grouping by Multiple Columns',
    },
  },
  {
    id: 'fc-dom03-043',
    cardNumber: 43,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'GROUP BY sans fonction d\'agrégation',
    difficulty: 'medium',
    tags: ['GROUP BY', 'DISTINCT', 'Sans agrégat', 'Équivalence'],
    front: {
      question: 'La clause GROUP BY peut-elle être employée dans une requête qui ne comporte AUCUNE fonction d\'agrégation dans le SELECT ? Que produit-elle ?',
      codeSnippet: 'SELECT department_id, job_id\nFROM employees\nGROUP BY department_id, job_id;',
      hint: 'Quel autre mot-clé du SELECT produit un effet similaire ?',
    },
    back: {
      answer: 'Oui, c\'est parfaitement valide. Elle retourne la liste des combinaisons distinctes de ces colonnes (comportement équivalent à SELECT DISTINCT).',
      explanation: 'Aucune règle SQL n\'impose la présence d\'une fonction d\'agrégation lorsqu\'une clause GROUP BY est spécifiée. La requête regroupe simplement les lignes et restitue les clés de groupe uniques.',
      codeSnippet: '-- Ces deux requêtes renvoient exactement le même ensemble de lignes :\nSELECT department_id, job_id FROM employees GROUP BY department_id, job_id;\nSELECT DISTINCT department_id, job_id FROM employees;',
      examTrap: 'Certains candidats pensent à tort qu\'une clause GROUP BY sans COUNT ou SUM est invalide. C\'est 100% syntaxiquement légal.',
      ruleRef: 'SQL Language Reference - GROUP BY without Aggregates',
    },
  },
  {
    id: 'fc-dom03-044',
    cardNumber: 44,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Clause WHERE et GROUP BY ensemble',
    difficulty: 'easy',
    tags: ['WHERE', 'GROUP BY', 'Ordre', 'Filtrage préalable'],
    front: {
      question: 'Quel est l\'ordre syntaxique et logique relatif entre les clauses WHERE et GROUP BY ?',
      codeSnippet: 'SELECT department_id, AVG(salary)\nFROM employees\nWHERE salary > 3000\nGROUP BY department_id;',
      hint: 'Qui filtre les lignes avant que les groupes ne soient formés ?',
    },
    back: {
      answer: 'La clause WHERE doit précéder GROUP BY syntaxiquement et s\'exécute AVANT le regroupement logiquement.',
      explanation: 'La clause WHERE élimine les lignes non désirées dès la lecture de la table. Les groupes ne sont ensuite constitués qu\'à partir des seules lignes survivantes.',
      codeSnippet: 'SELECT department_id, COUNT(*), AVG(salary)\nFROM employees\nWHERE hire_date >= DATE \'2015-01-01\' -- Filtre lignes individuelles\nGROUP BY department_id;              -- Constitue les groupes',
      examTrap: 'Placer WHERE après GROUP BY (ex: GROUP BY ... WHERE ...) déclenche une erreur immédiate de syntaxe ORA-00933: SQL command not properly ended.',
      ruleRef: 'SQL Statement Processing Order - WHERE before GROUP BY',
    },
  },
  {
    id: 'fc-dom03-045',
    cardNumber: 45,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Clause ORDER BY après GROUP BY',
    difficulty: 'easy',
    tags: ['ORDER BY', 'GROUP BY', 'Ordre final', 'Tri'],
    front: {
      question: 'La clause GROUP BY garantit-elle à elle seule que les lignes de sortie seront triées dans l\'ordre des colonnes de regroupement ?',
      hint: 'Pensez aux algorithmes de hachage (Hash Group By).',
    },
    back: {
      answer: 'NON ! La clause GROUP BY ne garantit JAMAIS l\'ordre de tri des résultats.',
      explanation: 'Dans les versions modernes d\'Oracle, l\'optimiseur utilise fréquemment l\'opérateur "HASH GROUP BY", qui ne trie pas les données. La seule et unique manière de garantir un ordre de tri est d\'ajouter explicitement une clause ORDER BY.',
      codeSnippet: 'SELECT department_id, COUNT(*)\nFROM employees\nGROUP BY department_id\nORDER BY department_id ASC; -- Seul garant d\'un tri ordonné',
      examTrap: 'Ancien piège : historiquement (Oracle 9i), Oracle utilisait SORT GROUP BY, donnant l\'illusion d\'un tri. En 19c, l\'absence de ORDER BY implique un ordre indéterminé.',
      ruleRef: 'Hash Group By vs Sort Group By in Oracle Optimizer',
    },
  },
  {
    id: 'fc-dom03-046',
    cardNumber: 46,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Constantes littérales dans le SELECT avec GROUP BY',
    difficulty: 'medium',
    tags: ['Littéral', 'Constante', 'SELECT', 'GROUP BY'],
    front: {
      question: 'Peut-on inclure une constante littérale (ex: \'Tech Dept\', 100, SYSDATE) dans la clause SELECT sans la mentionner dans GROUP BY ?',
      codeSnippet: 'SELECT department_id, \'Active\' AS status, SYSDATE AS run_date, AVG(salary)\nFROM employees\nGROUP BY department_id;',
      hint: 'Une constante varie-t-elle d\'une ligne à l\'autre dans un même groupe ?',
    },
    back: {
      answer: 'Oui, c\'est totalement valide et ne déclenche aucune erreur.',
      explanation: 'Les constantes littérales et les fonctions d\'environnement sans argument dépendant des lignes (comme SYSDATE, USER, CURRENT_DATE) sont invariantes au sein du groupe et n\'ont pas besoin d\'être spécifiées dans le GROUP BY.',
      codeSnippet: 'SELECT \n  department_id,\n  \'Siège Social\' AS site,\n  AVG(salary)    AS salaire_moyen\nFROM employees\nGROUP BY department_id;',
      examTrap: 'Ne pensez pas que chaque élément du SELECT doit être dans GROUP BY : les constantes et fonctions sans colonnes font exception.',
      ruleRef: 'SQL Rules on Invariant Literals with GROUP BY',
    },
  },
  {
    id: 'fc-dom03-047',
    cardNumber: 47,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Colonnes calculées et expressions de concaténation',
    difficulty: 'medium',
    tags: ['Concaténation', 'Expressions', 'ORA-00979'],
    front: {
      question: 'Pourquoi la requête suivante échoue-t-elle et comment la corriger ?',
      codeSnippet: 'SELECT first_name || \' \' || last_name, COUNT(*)\nFROM employees\nGROUP BY last_name;',
      hint: 'Quelle colonne manque dans la clause de regroupement ?',
    },
    back: {
      answer: 'Elle échoue avec ORA-00979 car first_name figure dans l\'expression du SELECT mais n\'est pas présent dans la clause GROUP BY.',
      explanation: 'Toute l\'expression de concaténation first_name || \' \' || last_name (ou les deux colonnes individuellement) doit figurer dans le GROUP BY afin qu\'Oracle puisse regrouper de manière cohérente.',
      codeSnippet: '-- CORRECTION OPTION 1 (expression exacte) :\nSELECT first_name || \' \' || last_name, COUNT(*)\nFROM employees\nGROUP BY first_name || \' \' || last_name;\n\n-- CORRECTION OPTION 2 (deux colonnes distinctes) :\nSELECT first_name || \' \' || last_name, COUNT(*)\nFROM employees\nGROUP BY first_name, last_name;',
      examTrap: 'Grouper par last_name uniquement ne suffit pas dès lors que first_name est référencé dans l\'expression non agrégée du SELECT.',
      ruleRef: 'Grouping Expressions and Column Dependencies',
    },
  },
  {
    id: 'fc-dom03-048',
    cardNumber: 48,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'CASE dans la clause GROUP BY',
    difficulty: 'hard',
    tags: ['CASE', 'GROUP BY', 'Groupement par tranche'],
    front: {
      question: 'Peut-on utiliser une expression conditionnelle CASE dans la clause GROUP BY pour regrouper des salariés par tranches de salaire ?',
      codeSnippet: 'SELECT \n  CASE WHEN salary >= 10000 THEN \'Haut\' ELSE \'Standard\' END AS categorie,\n  COUNT(*)\nFROM employees\nGROUP BY ???;',
      hint: 'Peut-on répéter l\'expression CASE dans GROUP BY ?',
    },
    back: {
      answer: 'Oui ! On peut parfaitement placer l\'expression CASE complète au sein de la clause GROUP BY.',
      explanation: 'Oracle évalue le CASE pour chaque ligne et assigne la ligne au groupe correspondant à la valeur retournée. Cela permet de construire des tranches d\'agrégation dynamiques.',
      codeSnippet: 'SELECT \n  CASE WHEN salary >= 10000 THEN \'Haut\' ELSE \'Standard\' END AS categorie,\n  COUNT(*),\n  ROUND(AVG(salary), 2) AS moyenne\nFROM employees\nGROUP BY CASE WHEN salary >= 10000 THEN \'Haut\' ELSE \'Standard\' END;',
      examTrap: 'Rappel : vous NE POUVEZ PAS écrire GROUP BY categorie (avec l\'alias) sous Oracle. Il faut répéter tout le bloc CASE dans la clause GROUP BY.',
      ruleRef: 'GROUP BY with Conditional CASE Expressions',
    },
  },
  {
    id: 'fc-dom03-049',
    cardNumber: 49,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Fonctions de groupe dans GROUP BY',
    difficulty: 'hard',
    tags: ['GROUP BY', 'Fonction de groupe', 'ORA-00934'],
    front: {
      question: 'Peut-on mettre une fonction d\'agrégation à l\'intérieur de la clause GROUP BY (ex: GROUP BY AVG(salary)) ?',
      codeSnippet: 'SELECT department_id\nFROM employees\nGROUP BY AVG(salary);',
      hint: 'Une fonction de groupe opère-t-elle avant ou après la définition des groupes ?',
    },
    back: {
      answer: 'NON, c\'est STRICTEMENT INTERDIT et produit ORA-00934: group function is not allowed here.',
      explanation: 'Le rôle de GROUP BY est de définir la granularité selon laquelle les fonctions d\'agrégation vont s\'exécuter. Une fonction d\'agrégation ne peut donc pas servir de critère pour créer son propre groupe.',
      codeSnippet: '-- ERREUR ABSOLUE (ORA-00934) :\nSELECT department_id FROM employees GROUP BY AVG(salary);',
      examTrap: 'Cette question est souvent posée sous forme de choix multiple parmi 4 requêtes syntaxiquement incorrectes.',
      ruleRef: 'Oracle Error Messages - Disallowed Group Functions in GROUP BY',
    },
  },
  {
    id: 'fc-dom03-050',
    cardNumber: 50,
    domainId: 'oracle-dom-03',
    domainCode: 'DOM-03',
    domainTitle: 'Agrégats, GROUP BY & Clause HAVING',
    subtopic: 'Types de données interdits dans GROUP BY',
    difficulty: 'hard',
    tags: ['LOB', 'BLOB', 'CLOB', 'LONG', 'Interdictions GROUP BY'],
    front: {
      question: 'Quels types de données de colonnes sont STRICTEMENT INTERDITS au sein de la clause GROUP BY sous Oracle ?',
      hint: 'Pensez aux types volumineux pour lesquels la comparaison d\'égalité n\'est pas directement indexable.',
    },
    back: {
      answer: 'Les types de grands objets LOB (BLOB, CLOB, NCLOB), le type obsolète LONG, ainsi que les types ROWID / UROWID.',
      explanation: 'Tenter de grouper sur une colonne de type CLOB ou BLOB déclenche l\'erreur ORA-00932: inconsistent datatypes: expected - got CLOB. Pour regrouper sur du texte CLOB, il faut préalablement en extraire une chaîne VARCHAR2 (ex: DBMS_LOB.SUBSTR).',
      codeSnippet: '-- Si resume est CLOB :\n-- ERREUR : SELECT resume FROM candidates GROUP BY resume; -- ORA-00932\n\n-- SOLUTION :\nSELECT DBMS_LOB.SUBSTR(resume, 4000, 1), COUNT(*)\nFROM candidates\nGROUP BY DBMS_LOB.SUBSTR(resume, 4000, 1);',
      examTrap: 'À l\'examen 1Z0-071, souvenez-vous que les colonnes LOB (CLOB/BLOB) et LONG ne peuvent JAMAIS apparaître dans GROUP BY, ni dans DISTINCT, ni dans ORDER BY sans fonction de conversion !',
      ruleRef: 'Data Types Restrictions in GROUP BY and DISTINCT Clauses',
    },
  },
];
