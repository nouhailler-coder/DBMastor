import { FlashcardItem } from '../types';

export const postgresEdbDom05Part1Flashcards: FlashcardItem[] = [
  // =========================================================================
  // SECTION 1 : SAUVEGARDE LOGIQUE AVEC PG_DUMP (Cartes 1 à 10)
  // Formats (-Fp, -Fc, -Fd, -Ft), options d'extraction, cohérence snapshot
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-001',
    cardNumber: 1,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Principe fondamental et impact de `pg_dump`',
    difficulty: 'easy',
    tags: ['pg_dump', 'Backup', 'Logical', 'Locking', 'MVCC'],
    front: {
      question: 'Quel est le principe de fonctionnement de l\'utilitaire `pg_dump` et quel est son impact sur les opérations concurrentes du serveur ?',
      hint: 'Pensez au niveau de verrouillage et au modèle MVCC.',
    },
    back: {
      answer: '`pg_dump` est un utilitaire client de sauvegarde logique qui extrait une base de données PostgreSQL sous forme de script SQL ou d\'archive compressée.\n\nSon fonctionnement repose sur le modèle MVCC : `pg_dump` démarre une transaction avec le niveau d\'isolation **`REPEATABLE READ`** (ou utilise un snapshot transactionnel exporté). Il pose uniquement des verrous légers partagés **`AccessShareLock`** sur les tables lues.',
      explanation: 'Grâce à `AccessShareLock`, `pg_dump` ne bloque AUCUNE opération de lecture (`SELECT`) ni d\'écriture (`INSERT`, `UPDATE`, `DELETE`). Seules les opérations DDL exclusives bloquantes (`DROP TABLE`, `ALTER TABLE`, `VACUUM FULL`) sont incompatibles avec lui.',
      examTrap: '`pg_dump` ne bloque PAS les écritures concurrentes. De plus, il ne sauvegarde qu\'UNE SEULE base de données à la fois et n\'inclut pas les objets globaux du cluster (rôles, tablespaces).',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Backup and Restore (pg_dump)',
    },
  },
  {
    id: 'fc-pgedb-dom05-002',
    cardNumber: 2,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Les 4 formats de sortie de `pg_dump` (-F)',
    difficulty: 'easy',
    tags: ['pg_dump', 'Formats', 'CLI', 'Syntax'],
    front: {
      question: 'Quels sont les 4 formats de sortie supportés par `pg_dump` via l\'option `-F` (`--format`) ?',
      codeSnippet: 'pg_dump -F <format> -d production -f backup_file',
      hint: 'Plain text, Custom, Directory, Tar.',
    },
    back: {
      answer: 'Les 4 formats supportés sont :\n\n1. **`p` ou `plain`** (Défaut) : Script SQL en texte brut.\n2. **`c` ou `custom`** : Archive binaire propriétaire compressée avec table des matières interne (TOC).\n3. **`d` ou `directory`** : Répertoire contenant un fichier `toc.dat` et un fichier compressé par table/blob.\n4. **`t` ou `tar`** : Archive tar standard non compressée (compatible avec `pg_restore`).',
      explanation: 'Les formats `c` et `d` sont les plus puissants en production car ils permettent la sélection fine des objets, le réordonnancement, la compression et la restauration parallèle avec `pg_restore`.',
      examTrap: 'Le format `p` (plain text) se restaure exclusivement avec `psql`, alors que les formats `c`, `d` et `t` se restaurent impérativement avec `pg_restore`.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_dump',
    },
  },
  {
    id: 'fc-pgedb-dom05-003',
    cardNumber: 3,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Avantages du format Custom (`-Fc`)',
    difficulty: 'medium',
    tags: ['pg_dump', 'Custom Format', 'pg_restore', 'TOC'],
    front: {
      question: 'Pourquoi le format Custom (`-Fc`) est-il recommandé pour les sauvegardes logiques en production plutôt que le format texte brut (`-Fp`) ?',
      hint: 'Pensez à la compression, à la flexibilité de restauration et à la table des matières.',
    },
    back: {
      answer: 'Le format Custom (`-Fc`) offre des avantages décisifs :\n\n1. **Compression intégrée** : Réduit considérablement l\'espace disque et les I/O de sauvegarde.\n2. **Table des matières interne (TOC)** : Enregistre les dépendances entre objets pour restaurer dans l\'ordre optimal.\n3. **Restauration sélective** : Permet à `pg_restore` d\'extraire une seule table, un seul schéma, ou uniquement les schémas/données sans tout relire.\n4. **Indépendance vis-à-vis des erreurs de script** : Évite les échecs de parsing SQL complexes.',
      explanation: 'Avec `-Fc`, on peut inspecter le contenu de la sauvegarde sans restaurer avec `pg_restore -l` et filtrer les éléments à restaurer avec `pg_restore -L`.',
      examTrap: 'Le format `custom` (`-Fc`) ne supporte PAS la sauvegarde multi-processus parallèle à l\'export (`pg_dump -j`). Seul le format `directory` (`-Fd`) supporte `-j` à l\'export.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump options: -F custom',
    },
  },
  {
    id: 'fc-pgedb-dom05-004',
    cardNumber: 4,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Sauvegarde parallèle avec le format Directory (`-Fd -j`)',
    difficulty: 'medium',
    tags: ['pg_dump', 'Directory Format', 'Parallelism', 'Performance'],
    front: {
      question: 'Quelle est la condition obligatoire pour exécuter `pg_dump` en parallèle avec plusieurs workers (`-j` / `--jobs`), et quelle commande permet de l\'exécuter ?',
      hint: 'Quel format est capable d\'écrire dans plusieurs fichiers simultanément ?',
    },
    back: {
      answer: 'Pour utiliser le parallélisme à la sauvegarde (`-j` ou `--jobs`), le format de sortie doit obligatoirement être le **format Répertoire (`-Fd` ou `--format=directory`)**.\n\nCommande d\'exemple :\n```bash\npg_dump -Fd -j 4 -d finance_db -f /backup/finance_dir\n```',
      explanation: 'Le format Directory crée un répertoire contenant une table des matières `toc.dat` et écrit chaque table et grand objet (Large Object) dans des fichiers individuels séparés, permettant à plusieurs processus ouvriers de dumper plusieurs tables simultanément.',
      examTrap: 'Tenter de combiner `-j` avec `-Fp`, `-Fc` ou `-Ft` provoquera une erreur fatale : `pg_dump: error: parallel backup only supported by the directory format`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump: --jobs=num_jobs',
    },
  },
  {
    id: 'fc-pgedb-dom05-005',
    cardNumber: 5,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Filtrage fin des objets : schémas et tables (`-n`, `-N`, `-t`, `-T`)',
    difficulty: 'easy',
    tags: ['pg_dump', 'Filtering', 'Schema', 'Tables', 'CLI'],
    front: {
      question: 'Quelles sont les options de `pg_dump` pour inclure ou exclure des schémas et des tables spécifiques ?',
      codeSnippet: 'pg_dump -n hr -T "hr.temp_*" -d appdb > hr_clean.sql',
      hint: 'Pensez aux minuscules pour inclure et aux majuscules pour exclure.',
    },
    back: {
      answer: 'Les options de filtrage sont :\n\n- **`-n <schema>`** (`--schema`) : Sauvegarde uniquement le schéma spécifié.\n- **`-N <schema>`** (`--exclude-schema`) : Exclut le schéma spécifié.\n- **`-t <table>`** (`--table`) : Sauvegarde uniquement la table spécifiée.\n- **`-T <table>`** (`--exclude-table`) : Exclut la table spécifiée.\n\nToutes ces options supportent les motifs génériques (wildcards comme `*`).',
      explanation: 'Ces options peuvent être répétées plusieurs fois dans la même commande pour combiner inclusions et exclusions complexes (ex: `-t "orders*" -T "orders_archive*"`).',
      examTrap: 'Si vous utilisez des jokers shell comme `*`, vous devez impérativement entourer le nom de guillemets pour éviter que votre shell Linux ne développe le motif avant son passage à `pg_dump`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump filtering options',
    },
  },
  {
    id: 'fc-pgedb-dom05-006',
    cardNumber: 6,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Séparation de la structure et des données (`-s` vs `-a`)',
    difficulty: 'easy',
    tags: ['pg_dump', 'Schema-only', 'Data-only', 'CLI'],
    front: {
      question: 'Quelle est la différence entre les options `-s` (`--schema-only`) et `-a` (`--data-only`) dans `pg_dump` ?',
      codeSnippet: '# Commande 1:\npg_dump -s -d appdb > ddl.sql\n# Commande 2:\npg_dump -a -d appdb > data.sql',
      hint: 'L\'un n\'extrait que les définitions DDL, l\'autre uniquement les données des lignes.',
    },
    back: {
      answer: '- **`-s` (`--schema-only`)** : N\'extrait que les définitions des objets (DDL : tables, types, vues, index, contraintes, fonctions, triggers, séquences). Aucune ligne de données n\'est exportée.\n- **`-a` (`--data-only`)** : N\'extrait que les données des tables (commandes `COPY` ou `INSERT`). Aucune instruction `CREATE` ou DDL n\'est exportée.',
      explanation: 'Ces options sont très utiles pour dupliquer une structure vide en environnement de test (`-s`) ou pour migrer des données vers une structure préexistante (`-a`).',
      examTrap: 'Avec `-a`, si les tables cibles comportent des clés étrangères ou des triggers actifs, la réinsertion peut échouer à cause des contraintes si l\'ordre d\'insertion n\'est pas respecté ou si les triggers ne sont pas désactivés.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump: --schema-only & --data-only',
    },
  },
  {
    id: 'fc-pgedb-dom05-007',
    cardNumber: 7,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Options de nettoyage et recréation (`-c`, `-C`, `--if-exists`)',
    difficulty: 'medium',
    tags: ['pg_dump', 'Options', 'DDL', 'Clean', 'Create'],
    front: {
      question: 'Que font respectivement les options `-c` (`--clean`), `-C` (`--create`) et `--if-exists` lors d\'une sauvegarde ou restauration logique ?',
      codeSnippet: 'pg_dump -c -C --if-exists -d inventory > full_inventory.sql',
      hint: 'Supprimer les objets avant de les recréer, et créer la base de données elle-même.',
    },
    back: {
      answer: '- **`-c` (`--clean`)** : Génère des instructions `DROP` avant les instructions `CREATE` pour chaque objet sauvegardé.\n- **`-C` (`--create`)** : Ajoute au début la commande `CREATE DATABASE` et la connexion à cette base (`\\connect`), permettant de restaurer sans créer la base cible manuellement au préalable.\n- **`--if-exists`** : Utilisé conjointement avec `-c`, il ajoute la clause conditionnelle `DROP ... IF EXISTS`, évitant les erreurs d\'avertissement si les objets n\'existent pas encore dans la base cible.',
      explanation: 'Sans `-C`, le script suppose que la base cible existe déjà et qu\'on s\'y connecte lors de la restauration.',
      examTrap: 'Si vous appliquez `-c` sans `--if-exists` sur une base vierge, le script SQL renverra des messages d\'erreur de type "ERROR: table ... does not exist" lors de la phase des `DROP`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump options: -c, -C, --if-exists',
    },
  },
  {
    id: 'fc-pgedb-dom05-008',
    cardNumber: 8,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Gestion des propriétaires et privilèges (`-O` et `-x`)',
    difficulty: 'medium',
    tags: ['pg_dump', 'Security', 'Owner', 'Privileges', 'Portability'],
    front: {
      question: 'Pourquoi et quand doit-on utiliser les options `-O` (`--no-owner`) et `-x` (`--no-privileges` / `--no-acl`) avec `pg_dump` ?',
      hint: 'Pensez au transfert d\'une base vers un environnement où les rôles d\'origine n\'existent pas.',
    },
    back: {
      answer: '- **`-O` (`--no-owner`)** : Empêche la génération des commandes `ALTER TABLE ... OWNER TO <user>` dans le script. Les objets créés à la restauration appartiendront alors automatiquement à l\'utilisateur exécutant la restauration.\n- **`-x` (`--no-privileges` / `--no-acl`)** : Empêche la génération des commandes `GRANT` et `REVOKE` relatives aux privilèges d\'accès.',
      explanation: 'Ces options sont essentielles lorsqu\'on restaure une base de données sur un serveur distinct (ex: passage de Production à Développement/Test) où les rôles et permissions d\'origine n\'ont pas été créés.',
      examTrap: 'Sans `-O`, si le rôle propriétaire de la table sur le serveur source n\'existe pas sur le serveur cible, la restauration renverra des erreurs "ERROR: role ... does not exist".',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump: --no-owner and --no-privileges',
    },
  },
  {
    id: 'fc-pgedb-dom05-009',
    cardNumber: 9,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Sauvegarde des Large Objects (BLOBs) et option `--blobs`',
    difficulty: 'hard',
    tags: ['pg_dump', 'Large Objects', 'BLOB', 'pg_largeobject'],
    front: {
      question: 'Comment les Large Objects (type `OID` stocké dans `pg_largeobject`) sont-ils gérés par `pg_dump`, et quel est le rôle de l\'option `-b` (`--blobs`) ?',
      hint: 'Attention au comportement par défaut de PostgreSQL 16 selon que la sauvegarde est totale ou filtrée.',
    },
    back: {
      answer: 'Les Large Objects (BLOBs historiques créés via l\'API `lo_create`/`lo_import`) sont stockés dans la table système `pg_largeobject`.\n\n- **Comportement par défaut** : `pg_dump` inclut automatiquement les Large Objects si aucune table ou schéma spécifique n\'est filtré.\n- **Option `-b` (`--blobs`)** : Force l\'inclusion des Large Objects dans la sauvegarde (utile si on spécifie des tables et qu\'on veut aussi les blobs).\n- **Option `--no-blobs` (`-B`)** : Exclut explicitement tous les Large Objects de la sauvegarde pour économiser de la place.',
      explanation: 'Contrairement aux colonnes de type `BYTEA` qui sont des données normales stockées dans les pages de table (ou en TOAST) et toujours extraites avec la table, les Large Objects nécessitent des métadonnées séparées.',
      examTrap: 'Si vous utilisez `-t <table>` pour sauvegarder une table spécifique, les Large Objects ne sont PAS inclus par défaut. Vous devez ajouter `-b` si vos lignes font référence à des OIDs de Large Objects.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump: Large Objects handling',
    },
  },
  {
    id: 'fc-pgedb-dom05-010',
    cardNumber: 10,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Cohérence au Snapshot et option `--snapshot`',
    difficulty: 'hard',
    tags: ['pg_dump', 'Snapshots', 'REPEATABLE READ', 'Consistency'],
    front: {
      question: 'Comment `pg_dump` garantit-il la cohérence exacte des données sauvegardées, et à quoi sert l\'option `--snapshot` ?',
      hint: 'Niveau d\'isolation transactionnelle et synchronisation inter-processus.',
    },
    back: {
      answer: '`pg_dump` garantit la cohérence en exécutant l\'extraction au sein d\'une transaction au niveau **`REPEATABLE READ`**. Il voit ainsi l\'état figé de la base au moment où le snapshot a été capturé, insensible aux commits ultérieurs.\n\nL\'option **`--snapshot=<nom_snapshot>`** permet d\'utiliser un snapshot synchronisé préexistant (exporté par une autre session via la fonction `pg_export_snapshot()`).',
      explanation: 'L\'utilisation d\'un snapshot synchronisé est le mécanisme fondamental qui permet à `pg_dump -Fd -j <n>` de faire dumper plusieurs tables par des workers différents tout en garantissant qu\'ils voient tous exactement le même instantané.',
      examTrap: 'Si le paramètre `old_snapshot_threshold` est configuré et que le dump prend plus de temps que ce seuil, `pg_dump` peut échouer avec l\'erreur "snapshot too old".',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dump: --snapshot and pg_export_snapshot()',
    },
  },

  // =========================================================================
  // SECTION 2 : RESTAURATION LOGIQUE AVEC PG_RESTORE & PSQL (Cartes 11 à 20)
  // Options de restauration, listes TOC, parallélisme, gestion d'erreurs
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-011',
    cardNumber: 11,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Sélection de l\'utilitaire de restauration : `psql` vs `pg_restore`',
    difficulty: 'easy',
    tags: ['pg_restore', 'psql', 'Restore', 'Formats'],
    front: {
      question: 'Quel outil en ligne de commande devez-vous utiliser pour restaurer respectivement un fichier `.sql` en texte brut et un fichier `.dump` au format custom (`-Fc`) ?',
      hint: 'Un interpréteur de commandes SQL vs un utilitaire dédié aux archives binaires.',
    },
    back: {
      answer: '1. **Fichier texte brut `.sql` (`-Fp`)** : Se restaure impérativement avec **`psql`** :\n```bash\npsql -U postgres -d mabase -f backup.sql\n```\n2. **Fichier Custom (`-Fc`), Directory (`-Fd`) ou Tar (`-Ft`)** : Se restaure impérativement avec **`pg_restore`** :\n```bash\npg_restore -U postgres -d mabase backup.dump\n```',
      explanation: '`pg_restore` ne peut pas lire de script SQL texte brut. À l\'inverse, `psql` ne peut pas interpréter directement les blocs compressés d\'une archive custom.',
      examTrap: 'Exécuter `pg_restore backup.sql` sur un script texte brut échouera immédiatement avec l\'erreur "input file appears to be a text format dump. Please use psql".',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Backup and Restore (pg_restore)',
    },
  },
  {
    id: 'fc-pgedb-dom05-012',
    cardNumber: 12,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Restauration parallèle avec `pg_restore -j`',
    difficulty: 'medium',
    tags: ['pg_restore', 'Parallelism', 'Performance', 'Jobs'],
    front: {
      question: 'Quels formats d\'archives permettent une restauration parallèle avec `pg_restore -j <n>` et comment fonctionne l\'optimisation interne ?',
      codeSnippet: 'pg_restore -j 8 -d reporting_db -F c /backup/reporting.dump',
      hint: 'Custom et Directory supportent -j à la restauration, mais pas Tar.',
    },
    back: {
      answer: '`pg_restore -j <num_jobs>` fonctionne avec les formats **Custom (`-Fc`)** et **Directory (`-Fd`)**.\n\nFonctionnement interne :\n1. Le processus maître lit la table des matières (TOC) et établit l\'arbre des dépendances.\n2. Il alloue plusieurs connexions clientes simultanées au serveur.\n3. Il charge les tables en parallèle, puis crée les index et contraintes de clés étrangères en parallèle après le chargement des données.',
      explanation: 'La création des index en parallèle après l\'insertion des données accélère considérablement la restauration par rapport à une reconstruction séquentielle.',
      examTrap: 'Le format `tar` (`-Ft`) ne supporte PAS la restauration parallèle car il est impossible d\'effectuer des accès aléatoires concurrents rapides dans une archive tar.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: --jobs=num_jobs',
    },
  },
  {
    id: 'fc-pgedb-dom05-013',
    cardNumber: 13,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Inspection de la table des matières avec `pg_restore -l`',
    difficulty: 'easy',
    tags: ['pg_restore', 'TOC', 'Inspection', 'Metadata'],
    front: {
      question: 'Comment afficher le contenu détaillé et la table des matières (TOC) d\'une archive de sauvegarde custom sans restaurer aucune donnée ?',
      hint: 'Option `-l` ou `--list`.',
    },
    back: {
      answer: 'En utilisant l\'option **`-l` (`--list`)** de `pg_restore` :\n```bash\npg_restore -l production.dump > list.txt\n```\nCette commande produit un fichier texte énumérant chaque élément archivé avec son ID interne, son type d\'objet (TABLE, INDEX, SEQUENCE, CONSTRAINT), son schéma, son nom et son propriétaire.',
      explanation: 'Cette liste permet d\'auditer précisément ce que contient la sauvegarde avant toute opération et sert de base à la restauration sélective.',
      examTrap: '`pg_restore -l` ne se connecte pas à la base de données et ne modifie rien sur le serveur. Il lit uniquement les métadonnées internes du fichier d\'archive.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: -l, --list',
    },
  },
  {
    id: 'fc-pgedb-dom05-014',
    cardNumber: 14,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Restauration sélective fine avec `pg_restore -L`',
    difficulty: 'medium',
    tags: ['pg_restore', 'Selective Restore', 'TOC', 'Use-List'],
    front: {
      question: 'Quelle est la procédure exacte pour restaurer uniquement un sous-ensemble d\'objets choisis à partir d\'une archive custom de `pg_dump` ?',
      codeSnippet: '# Étape 1:\npg_restore -l backup.dump > toc.txt\n# Étape 2:\n# ... éditer toc.txt ...\n# Étape 3:\npg_restore -L toc.txt -d testdb backup.dump',
      hint: 'Utiliser la liste TOC filtrée avec l\'option `-L`.',
    },
    back: {
      answer: 'La procédure standard est :\n\n1. Extraire la table des matières avec `pg_restore -l backup.dump > my_list.txt`.\n2. Éditer le fichier texte `my_list.txt` : supprimer ou commenter (avec un point-virgule `;`) les lignes des objets qu\'on ne souhaite pas restaurer, ou modifier l\'ordre d\'apparition.\n3. Lancer la restauration en passant cette liste filtrée avec l\'option **`-L` (`--use-list`)** :\n```bash\npg_restore -L my_list.txt -d mabase backup.dump\n```',
      explanation: '`pg_restore` ne restaurera strictement que les éléments listés dans le fichier passé à `-L`, dans l\'ordre où ils y figurent.',
      examTrap: 'Ne confondez pas `-l` (minuscule : générer la liste) et `-L` (majuscule : appliquer une liste pour filtrer la restauration).',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: -L, --use-list',
    },
  },
  {
    id: 'fc-pgedb-dom05-015',
    cardNumber: 15,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Option `-C` (`--create`) et base de connexion technique dans `pg_restore`',
    difficulty: 'medium',
    tags: ['pg_restore', 'Create Database', 'Connection', 'CLI'],
    front: {
      question: 'Lors de l\'utilisation de `pg_restore -C` (`--create`) pour recréer une base de données, à quelle base devez-vous spécifier la connexion avec `-d` ?',
      codeSnippet: 'pg_restore -C -d <quelle_base?> backup.dump',
      hint: 'On ne peut pas se connecter à une base qui n\'a pas encore été créée.',
    },
    back: {
      answer: 'Vous devez spécifier le nom d\'une base technique d\'accueil préexistante, généralement **`postgres`** ou **`template1`** :\n```bash\npg_restore -C -d postgres backup.dump\n```\n`pg_restore` se connecte d\'abord à la base `postgres`, émet la commande `CREATE DATABASE <nom_initial>`, puis bascule automatiquement sur la nouvelle base pour y injecter les schémas et données.',
      explanation: 'L\'archive enregistre le nom d\'origine de la base dans son en-tête pour savoir quelle base créer.',
      examTrap: 'Si vous mettez le nom de la base à restaurer (qui n\'existe pas encore) comme cible de `-d`, la commande échouera immédiatement à l\'établissement de la connexion avant même de pouvoir exécuter le `CREATE DATABASE`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: -C, --create',
    },
  },
  {
    id: 'fc-pgedb-dom05-016',
    cardNumber: 16,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Nettoyage avant restauration : `pg_restore --clean`',
    difficulty: 'medium',
    tags: ['pg_restore', 'Clean', 'Drop', 'If-Exists'],
    front: {
      question: 'Comment fonctionne l\'option `-c` (`--clean`) dans `pg_restore` et quelle précaution est nécessaire ?',
      hint: 'Suppression préalable des objets cibles avant réinsertion.',
    },
    back: {
      answer: 'L\'option **`-c` (`--clean`)** demande à `pg_restore` d\'émettre des instructions `DROP` sur chaque objet de base de données avant de le recréer.\n\nPrécaution indispensable :\nSi des objets existent déjà et sont liés par des dépendances complexes (ex: clés étrangères), l\'ordre des `DROP` peut échouer sans l\'utilisation de `--if-exists`. De plus, elle effacera irrémédiablement les données existantes dans les tables cibles.',
      explanation: 'Contrairement à `pg_dump -c` qui insère les `DROP` dans le fichier dump généré, `pg_restore -c` peut appliquer cette suppression dynamique même si le dump n\'avait pas été créé avec `-c`.',
      examTrap: 'Si les tables existantes contiennent des données plus récentes, `pg_restore -c` les supprimera sans confirmation préalable.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: -c, --clean',
    },
  },
  {
    id: 'fc-pgedb-dom05-017',
    cardNumber: 17,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Comportement d\'erreur : option `--exit-on-error` (`-e`)',
    difficulty: 'easy',
    tags: ['pg_restore', 'Error Handling', 'CLI', 'exit-on-error'],
    front: {
      question: 'Quel est le comportement par défaut de `pg_restore` face à une erreur SQL, et que fait l\'option `-e` (`--exit-on-error`) ?',
      hint: 'Ignorer et continuer vs arrêt immédiat.',
    },
    back: {
      answer: '- **Comportement par défaut** : `pg_restore` ignore les erreurs individuelles (affiche un message d\'erreur sur `stderr`) et continue le traitement de tous les autres objets jusqu\'au bout.\n- **Option `-e` (`--exit-on-error`)** : Interrompt immédiatement l\'exécution dès la première instruction SQL qui provoque une erreur, évitant la propagation d\'erreurs en cascade.',
      explanation: 'Par défaut, à la fin de l\'exécution, `pg_restore` affiche un message de synthèse comme : `WARNING: errors ignored on restore: 12`.',
      examTrap: 'Sans `--exit-on-error`, si la création d\'une table échoue (ex: type manquant), `pg_restore` tentera quand même d\'insérer les données et de créer les index pour cette table inexistante, polluant les logs.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: -e, --exit-on-error',
    },
  },
  {
    id: 'fc-pgedb-dom05-018',
    cardNumber: 18,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Restauration transactionnelle avec `--single-transaction` (`-1`)',
    difficulty: 'medium',
    tags: ['pg_restore', 'Transaction', 'Atomicity', 'Rollback'],
    front: {
      question: 'À quoi sert l\'option `--single-transaction` (`-1`) dans `pg_restore` et quelle est sa restriction majeure ?',
      hint: 'Tout valider ou tout annuler en cas de problème, mais incompatible avec une autre option de performance.',
    },
    back: {
      answer: 'L\'option **`--single-transaction` (`-1`)** encapsule toute la restauration dans une seule transaction (`BEGIN ... COMMIT`). Si la moindre erreur survient, l\'ensemble des modifications est automatiquement annulé (`ROLLBACK`), laissant la base dans son état initial intact.\n\n**Restriction majeure** :\nElle est **strictement incompatible avec le parallélisme (`-j`)** car chaque worker parallèle nécessite sa propre connexion et transaction indépendante.',
      explanation: 'Cette option garantit l\'atomicité complète de la restauration, idéale pour les mises à jour logiques critiques où un état partiel serait inacceptable.',
      examTrap: 'Associer `-j 4` et `--single-transaction` déclenche une erreur immédiate de `pg_restore`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: --single-transaction',
    },
  },
  {
    id: 'fc-pgedb-dom05-019',
    cardNumber: 19,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Restauration partielle par sections : `--section`',
    difficulty: 'hard',
    tags: ['pg_restore', 'Sections', 'pre-data', 'data', 'post-data'],
    front: {
      question: 'Quelles sont les 3 sections d\'une archive PostgreSQL que l\'on peut restaurer séparément via l\'option `--section` de `pg_restore` ?',
      codeSnippet: 'pg_restore --section=post-data -d mabase backup.dump',
      hint: 'Avant les données, les données, et après les données.',
    },
    back: {
      answer: 'Les 3 sections sont :\n\n1. **`pre-data`** : Définition des types, séquences, tables sans index ni contraintes (DDL de base).\n2. **`data`** : Données brutes des tables (`COPY`) et Large Objects.\n3. **`post-data`** : Création des index, contraintes de clés primaires/étrangères, règles et triggers.',
      explanation: 'Cette séparation permet une optimisation majeure lors de migrations lourdes : on charge la structure `pre-data`, on importe les données en masse (`data`), puis on construit les index et contraintes (`post-data`) en bloc, ce qui est 5 à 10 fois plus rapide que d\'insérer dans des tables déjà indexées.',
      examTrap: 'Si vous restaurez `data` sans avoir préalablement restauré ou créé la section `pre-data`, le chargement échouera car les tables cibles n\'existeront pas.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore: --section=(pre-data|data|post-data)',
    },
  },
  {
    id: 'fc-pgedb-dom05-020',
    cardNumber: 20,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Remplacement de base cible sans changer le dump',
    difficulty: 'easy',
    tags: ['pg_restore', 'Database Rename', 'Target Database', 'CLI'],
    front: {
      question: 'Comment restaurer une archive custom générée depuis la base `ventes_prod` vers une nouvelle base nommée `ventes_dev` ?',
      hint: 'Spécifier simplement le nouveau nom avec `-d` sans utiliser `-C`.',
    },
    back: {
      answer: 'Il suffit de créer la base cible `ventes_dev` au préalable et de passer son nom avec l\'option **`-d`** (sans utiliser l\'option `-C`) :\n```bash\ncreatedb -U postgres ventes_dev\npg_restore -U postgres -d ventes_dev /backup/ventes_prod.dump\n```',
      explanation: 'Quand `-C` n\'est pas spécifié, `pg_restore` se connecte directement à la base fournie par `-d` et y applique tous les objets de l\'archive sans se soucier du nom d\'origine.',
      examTrap: 'Si l\'archive a été créée avec `pg_dump -C` et que vous lancez `pg_restore -C -d ventes_dev`, `pg_restore` recréera la base `ventes_prod` originale et non `ventes_dev`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_restore usage guidelines',
    },
  },

  // =========================================================================
  // SECTION 3 : SAUVEGARDE GLOBALE DE CLUSTER AVEC PG_DUMPALL (Cartes 21 à 30)
  // Objets globaux (rôles, tablespaces), format texte brut, limitations
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-021',
    cardNumber: 21,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Périmètre et rôle de `pg_dumpall`',
    difficulty: 'easy',
    tags: ['pg_dumpall', 'Cluster', 'Global Objects', 'Backup'],
    front: {
      question: 'Quel est le périmètre couvert par l\'utilitaire `pg_dumpall` par rapport à `pg_dump` ?',
      hint: 'L\'un est limité à une seule base, l\'autre englobe la totalité du cluster PostgreSQL.',
    },
    back: {
      answer: '`pg_dumpall` sauvegarde **l\'intégralité d\'un cluster PostgreSQL** en une seule opération :\n\n1. **Toutes les bases de données** utilisateur et système du cluster.\n2. **Tous les objets globaux** communs à l\'ensemble du cluster (qui ne sont pas stockés dans une base particulière) : rôles/utilisateurs (`pg_authid`), tablespaces (`pg_tablespace`), et configurations de paramètres au niveau rôle ou base.',
      explanation: '`pg_dump` ne peut pas sauvegarder les rôles ni les tablespaces car ces objets sont globaux au cluster et non rattachés à une base individuelle.',
      examTrap: 'Une sauvegarde effectuée uniquement avec `pg_dump` ne contient AUCUN utilisateur ni mot de passe. Si vous devez reconstruire un serveur à blanc, vos données seront restaurées mais aucun utilisateur applicatif ne pourra se connecter.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Backup and Restore (pg_dumpall)',
    },
  },
  {
    id: 'fc-pgedb-dom05-022',
    cardNumber: 22,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Format exclusif de sortie de `pg_dumpall`',
    difficulty: 'easy',
    tags: ['pg_dumpall', 'Format', 'Plain Text', 'Limitations'],
    front: {
      question: 'Quels formats de sortie sont supportés par `pg_dumpall` ? Peut-on utiliser `-Fc` (custom) avec `pg_dumpall` ?',
      hint: 'Il n\'y a qu\'un seul format possible.',
    },
    back: {
      answer: '`pg_dumpall` génère **exclusivement un script SQL en texte brut (`plain text`)**.\n\nIl ne supporte **AUCUN autre format** (pas de format custom `-Fc`, ni directory `-Fd`, ni tar `-Ft`).',
      explanation: 'En interne, `pg_dumpall` extrait les objets globaux sous forme de commandes SQL, puis appelle séquentiellement `pg_dump -Fp` sur chacune des bases du cluster en les concaténant dans le même flux.',
      examTrap: 'Tenter de passer `-Fc` ou `-Fd` à `pg_dumpall` provoquera une erreur d\'option inconnue ou invalide. Par conséquent, `pg_restore` ne peut JAMAIS être utilisé pour restaurer un dump produit par `pg_dumpall`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dumpall reference',
    },
  },
  {
    id: 'fc-pgedb-dom05-023',
    cardNumber: 23,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Extraction ciblée des objets globaux (`-g` / `--globals-only`)',
    difficulty: 'medium',
    tags: ['pg_dumpall', 'Globals', 'Roles', 'Tablespaces', 'CLI'],
    front: {
      question: 'Quelle commande permet de sauvegarder UNIQUEMENT les objets globaux (rôles, mots de passe chiffrés, tablespaces) sans exporter aucune table ni base de données ?',
      codeSnippet: 'pg_dumpall -g -f globals.sql',
      hint: 'Option `-g` ou `--globals-only`.',
    },
    back: {
      answer: 'La commande utilise l\'option **`-g` (`--globals-only`)** :\n```bash\npg_dumpall -U postgres -g -f /backup/cluster_globals.sql\n```\nElle extrait tous les rôles (avec leurs hachages SCRAM/MD5 et leurs options `SUPERUSER`, `LOGIN`, `CREATEDB`), les appartenances aux groupes, les définitions des tablespaces et les privilèges globaux.',
      explanation: 'C\'est la commande standard à intégrer dans les scripts de sauvegarde nocturne pour accompagner les `pg_dump -Fc` individuels de chaque base.',
      examTrap: 'Le fichier produit par `-g` contient les définitions des tablespaces avec leurs chemins physiques absolus sur le système de fichiers. Si vous restaurez sur un serveur ayant une arborescence différente, il faudra adapter ces chemins.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dumpall: -g, --globals-only',
    },
  },
  {
    id: 'fc-pgedb-dom05-024',
    cardNumber: 24,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Extraction ciblée des rôles (`-r` / `--roles-only`)',
    difficulty: 'easy',
    tags: ['pg_dumpall', 'Roles', 'Security', 'Authentication'],
    front: {
      question: 'Quelle est la différence entre l\'option `-g` (`--globals-only`) et l\'option `-r` (`--roles-only`) dans `pg_dumpall` ?',
      hint: 'L\'un inclut les tablespaces, l\'autre se limite strictement aux comptes utilisateurs et privilèges.',
    },
    back: {
      answer: '- **`-r` (`--roles-only`)** : N\'extrait STRICTEMENT QUE les rôles, utilisateurs, groupes et leurs appartenances (`CREATE ROLE`, `ALTER ROLE`, `GRANT <role> TO <user>`).\n- **`-g` (`--globals-only`)** : Extrait les rôles ET les définitions de tablespaces (`CREATE TABLESPACE`).',
      explanation: '`-r` est idéal pour synchroniser les comptes utilisateurs entre deux clusters sans risquer d\'importer des définitions de tablespaces inexistants sur la machine cible.',
      examTrap: 'Ni `-g` ni `-r` ne sauvegardent les privilèges d\'accès aux tables individuelles (`GRANT SELECT ON ...`). Ces privilèges spécifiques sont stockés dans chaque base et sont sauvegardés par `pg_dump`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dumpall: -r, --roles-only',
    },
  },
  {
    id: 'fc-pgedb-dom05-025',
    cardNumber: 25,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Extraction ciblée des tablespaces (`-t` / `--tablespaces-only`)',
    difficulty: 'easy',
    tags: ['pg_dumpall', 'Tablespaces', 'Storage', 'CLI'],
    front: {
      question: 'À quoi sert l\'option `-t` (`--tablespaces-only`) de `pg_dumpall` ?',
      codeSnippet: 'pg_dumpall -t -f tablespaces.sql',
      hint: 'Elle ne sauvegarde que les instructions DDL de création d\'emplacements de stockage.',
    },
    back: {
      answer: 'L\'option **`-t` (`--tablespaces-only`)** extrait exclusivement les instructions `CREATE TABLESPACE` du cluster avec leurs chemins de répertoires associés et leurs propriétaires.',
      explanation: 'Cette option est très ciblée pour recréer la structure de stockage d\'un cluster avant d\'y restaurer les données.',
      examTrap: 'Attention à la confusion d\'options : dans `pg_dump`, `-t` signifie `--table` (sauvegarder une table spécifique). Dans `pg_dumpall`, `-t` signifie `--tablespaces-only` !',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dumpall: -t, --tablespaces-only',
    },
  },
  {
    id: 'fc-pgedb-dom05-026',
    cardNumber: 26,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Procédure de restauration d\'un fichier généré par `pg_dumpall`',
    difficulty: 'easy',
    tags: ['pg_dumpall', 'Restore', 'psql', 'Superuser'],
    front: {
      question: 'Comment restaure-t-on une sauvegarde complète générée par `pg_dumpall` ? Quel rôle utilisateur est impératif ?',
      codeSnippet: '# Génération:\npg_dumpall -f full_cluster.sql\n# Restauration:\n???',
      hint: 'Utiliser `psql` avec un compte superutilisateur connecté à la base `postgres`.',
    },
    back: {
      answer: 'La restauration s\'effectue impérativement avec **`psql`** et requiert les privilèges **`SUPERUSER`** (compte `postgres`) :\n```bash\npsql -U postgres -d postgres -f full_cluster.sql\n```\nLe privilège superutilisateur est indispensable car le script exécute des commandes globales (`CREATE ROLE`, `CREATE TABLESPACE`, `CREATE DATABASE`) impossibles pour un rôle ordinaire.',
      explanation: 'Le script contient des commandes `\\connect <dbname>` pour basculer automatiquement d\'une base à l\'autre lors de la restauration des objets de chaque base.',
      examTrap: 'Ne jamais tenter d\'utiliser `pg_restore` sur un fichier `pg_dumpall` : cela échouera avec une erreur de format immédiate.',
      ruleRef: 'PostgreSQL 16 Documentation - Restoring the dump (pg_dumpall)',
    },
  },
  {
    id: 'fc-pgedb-dom05-027',
    cardNumber: 27,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Limitation critique de cohérence transactionnelle de `pg_dumpall`',
    difficulty: 'hard',
    tags: ['pg_dumpall', 'Consistency', 'MVCC', 'Snapshot', 'Limitation'],
    front: {
      question: 'Quelle est la limitation majeure de `pg_dumpall` concernant la cohérence des données entre les différentes bases du cluster ?',
      hint: 'Pensez au cycle de vie des transactions et aux snapshots entre différentes bases de données.',
    },
    back: {
      answer: '`pg_dumpall` **ne garantit PAS un instantané cohérent entre les différentes bases de données** du cluster.\n\nChaque base de données est sauvegardée de manière séquentielle dans sa propre transaction indépendante. Par conséquent, des modifications committées dans la base B pendant que `pg_dumpall` sauvegarde la base A seront visibles lors du dump de B, créant un décalage temporel inter-bases.',
      explanation: 'Dans PostgreSQL, une transaction ne peut pas englober plusieurs bases de données physiques distinctes. La cohérence transactionnelle MVCC n\'est donc garantie qu\'au niveau interne de chaque base individuellement.',
      examTrap: 'Si votre application possède deux bases interdépendantes (ex: `auth_db` et `erp_db`), `pg_dumpall` ne garantit pas la cohérence référentielle croisée entre elles.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Backup and Restore Notes',
    },
  },
  {
    id: 'fc-pgedb-dom05-028',
    cardNumber: 28,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Absence de parallélisme dans `pg_dumpall`',
    difficulty: 'medium',
    tags: ['pg_dumpall', 'Performance', 'Parallelism', 'Limitations'],
    front: {
      question: 'Peut-on exécuter `pg_dumpall` avec plusieurs processus ouvriers en parallèle (`-j`) pour accélérer la sauvegarde d\'un gros cluster ?',
      hint: 'Le format de sortie est uniquement texte brut...',
    },
    back: {
      answer: '**NON**, `pg_dumpall` ne supporte pas l\'option de parallélisme `-j` (`--jobs`). La sauvegarde de toutes les bases est strictement séquentielle.\n\nSur un cluster volumineux hébergeant des dizaines de bases de données, `pg_dumpall` peut être extrêmement lent et monopolisera un seul cœur processeur.',
      explanation: 'Pour paralléliser la sauvegarde logique d\'un cluster complet, la bonne pratique industrielle consiste à sauvegarder les objets globaux avec `pg_dumpall -g`, puis à orchestrer des scripts exécutant `pg_dump -Fd -j <n>` en parallèle sur chaque base.',
      examTrap: 'L\'option `-j` n\'existe tout simplement pas dans la ligne de commande `pg_dumpall`.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_dumpall limitations',
    },
  },
  {
    id: 'fc-pgedb-dom05-029',
    cardNumber: 29,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Architecture recommandée de sauvegarde logique de production',
    difficulty: 'medium',
    tags: ['Best Practices', 'pg_dump', 'pg_dumpall', 'Architecture'],
    front: {
      question: 'Quelle est la stratégie recommandée par PostgreSQL et EnterpriseDB pour combiner `pg_dumpall` et `pg_dump` dans un plan de sauvegarde logique ?',
      hint: 'Sauvegarder les métadonnées globales d\'un côté, et les bases au format custom/directory de l\'autre.',
    },
    back: {
      answer: 'La stratégie recommandée est la suivante :\n\n1. **Objets globaux** : Exécuter `pg_dumpall -g -f globals.sql` pour figer les rôles, droits et tablespaces.\n2. **Bases de données individuelles** : Boucler sur la liste des bases et exécuter pour chacune :\n```bash\npg_dump -Fc -f /backup/${DB}_$(date +%F).dump ${DB}\n```\n(ou `-Fd -j 4` pour les très grosses bases).',
      explanation: 'Cette architecture offre la flexibilité maximale : restauration possible d\'une seule table sans rejouer tout le cluster, compression optimale, parallélisme et conservation des rôles globaux.',
      examTrap: 'Se reposer uniquement sur un fichier `pg_dumpall.sql` unique de plusieurs centaines de gigaoctets rend toute restauration d\'urgence d\'une petite table extrêmement longue et complexe.',
      ruleRef: 'EDB PostgreSQL Associate Study Guide - Backup Strategy Patterns',
    },
  },
  {
    id: 'fc-pgedb-dom05-030',
    cardNumber: 30,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Sensibilité aux mots de passe dans les scripts de dump',
    difficulty: 'medium',
    tags: ['Security', 'Passwords', 'pg_dumpall', 'pgpass'],
    front: {
      question: 'Comment automatiser l\'authentification de `pg_dumpall` et `pg_dump` dans des scripts cron sans exposer le mot de passe en clair dans la ligne de commande ?',
      codeSnippet: '# À PROSCRIRE : pg_dump -W ou mot de passe dans l\'historique',
      hint: 'Fichier de configuration sécurisé `.pgpass` sous Linux.',
    },
    back: {
      answer: 'On utilise le fichier de mot de passe sécurisé **`~/.pgpass`** (ou `%APPDATA%\\postgresql\\pgpass.conf` sous Windows) avec les permissions strictes **`chmod 600`** (lecture/écriture pour l\'utilisateur propriétaire uniquement).\n\nFormat d\'une ligne dans `~/.pgpass` :\n```text\nhostname:port:database:username:password\nlocalhost:5432:*:postgres:MonMotDePasseSecret\n```',
      explanation: 'Alternativement, on peut définir la variable d\'environnement `PGPASSWORD` dans l\'environnement sécurisé du script, mais `.pgpass` est la méthode standard et sécurisée recommandée.',
      examTrap: 'Si les permissions du fichier `.pgpass` sont plus ouvertes que `0600` (ex: `0644`), PostgreSQL refusera purement et simplement de le lire par mesure de sécurité.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 34: The Password File (.pgpass)',
    },
  },

  // =========================================================================
  // SECTION 4 : SAUVEGARDE PHYSIQUE À CHAUD AVEC PG_BASEBACKUP (Cartes 31 à 40)
  // Connexion de réplication, -X stream vs fetch, format tar vs plain, -R
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-031',
    cardNumber: 31,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Principe et nature de `pg_basebackup`',
    difficulty: 'easy',
    tags: ['pg_basebackup', 'Physical Backup', 'Hot Backup', 'Cluster'],
    front: {
      question: 'Qu\'est-ce que `pg_basebackup` et quelle est sa différence fondamentale avec `pg_dump` ?',
      hint: 'Sauvegarde physique fichier/bloc par bloc vs extraction logique de données SQL.',
    },
    back: {
      answer: '`pg_basebackup` est un outil de **sauvegarde physique à chaud** de l\'intégralité du cluster PostgreSQL.\n\nDifférences fondamentales :\n- **Niveau** : Il effectue une copie binaire bit à bit de l\'arborescence `$PGDATA` (tables, index, catalogues système, fichiers de configuration) au lieu de générer des requêtes SQL.\n- **Périmètre** : Il sauvegarde obligatoirement l\'ensemble du cluster (toutes les bases, rôles et tablespaces simultanément).\n- **Usage** : C\'est la base indispensable pour la restauration PITR (Point-in-Time Recovery) et le provisionnement de serveurs réplicas (Standby nodes).',
      explanation: 'Une sauvegarde physique est beaucoup plus rapide à restaurer qu\'un dump logique volumineux car il n\'y a aucune réindexation ni parsing SQL à exécuter.',
      examTrap: '`pg_basebackup` ne permet pas de sauvegarder une seule table ou une seule base de données : c\'est tout le cluster ou rien.',
      ruleRef: 'PostgreSQL 16 Documentation - Reference: pg_basebackup',
    },
  },
  {
    id: 'fc-pgedb-dom05-032',
    cardNumber: 32,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Protocole de réplication et prérequis d\'authentification',
    difficulty: 'medium',
    tags: ['pg_basebackup', 'Replication Protocol', 'pg_hba.conf', 'Privileges'],
    front: {
      question: 'Quel protocole réseau `pg_basebackup` utilise-t-il pour se connecter au serveur et quelles sont les deux conditions de sécurité requises sur PostgreSQL ?',
      hint: 'Protocole de réplication streaming, attribut de rôle et ligne spécifique dans pg_hba.conf.',
    },
    back: {
      answer: '`pg_basebackup` utilise le **protocole de réplication en streaming** de PostgreSQL (via la bibliothèque libpq standard).\n\nLes 2 conditions obligatoires sont :\n1. **Attribut de rôle** : Le rôle de connexion doit posséder l\'attribut **`REPLICATION`** (ou être `SUPERUSER`).\n2. **Règle `pg_hba.conf`** : Le fichier `pg_hba.conf` doit autoriser les connexions sur la pseudo-base **`replication`** pour cet hôte et cet utilisateur :\n```text\nhost  replication  repuser  192.168.1.0/24  scram-sha-256\n```',
      explanation: 'Sans cette ligne `replication` dans `pg_hba.conf`, la connexion sera rejetée même si l\'utilisateur a le droit de se connecter à la base `postgres`.',
      examTrap: 'Le mot-clé `replication` dans `pg_hba.conf` ne désigne pas une vraie base de données physique, mais un canal spécial du protocole PostgreSQL.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Continuous Archiving and Standby',
    },
  },
  {
    id: 'fc-pgedb-dom05-033',
    cardNumber: 33,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Formats de sortie : `-Fp` (Plain) vs `-Ft` (Tar)',
    difficulty: 'easy',
    tags: ['pg_basebackup', 'Formats', 'Plain', 'Tar', 'CLI'],
    front: {
      question: 'Quels sont les deux formats de sortie supportés par `pg_basebackup` via l\'option `-F` (`--format`) ?',
      codeSnippet: 'pg_basebackup -h srv1 -U repuser -D /backup/base -F <format>',
      hint: 'Répertoire standard identique à PGDATA vs fichiers archives tar.',
    },
    back: {
      answer: 'Les 2 formats supportés sont :\n\n1. **`-Fp` ou `--format=plain`** (Défaut) : Écrit une arborescence de fichiers identique à `$PGDATA` dans le répertoire cible. Immédiatement utilisable pour démarrer une instance ou un standby.\n2. **`-Ft` ou `--format=tar`** : Écrit le contenu sous forme de fichiers archives `.tar`. Il génère `base.tar` pour `$PGDATA`, et un fichier tar distinct pour chaque tablespace additionnel.',
      explanation: 'Le format `plain` est idéal pour initialiser directement un serveur réplica, tandis que le format `tar` est privilégié pour l\'archivage à froid sur bande ou stockage objet distant.',
      examTrap: 'Si le cluster contient des tablespaces externes et que vous utilisez `-Fp`, le répertoire cible doit être sur la même machine ou utiliser `-T` pour remapper les chemins.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_basebackup: -F, --format',
    },
  },
  {
    id: 'fc-pgedb-dom05-034',
    cardNumber: 34,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Gestion indispensable des WAL : `-X stream` vs `-X fetch`',
    difficulty: 'hard',
    tags: ['pg_basebackup', 'WAL', 'Streaming', 'Consistency', 'max_wal_senders'],
    front: {
      question: 'Quelle est la différence fondamentale entre `-X stream` et `-X fetch` dans `pg_basebackup`, et pourquoi `-X stream` est-il le standard de production ?',
      codeSnippet: 'pg_basebackup -D /backup/base -X stream',
      hint: 'Flux concurrent de WAL via un second walsender vs récupération finale à la fin du backup.',
    },
    back: {
      answer: '- **`-X stream` (`--wal-method=stream`)** [Défaut] : Ouvre une **deuxième connexion de réplication simultanée** qui streame en continu les fichiers WAL générés pendant toute la durée de la copie des fichiers de données.\n- **`-X fetch` (`--wal-method=fetch`)** : Attend la fin complète de la copie des données pour rapatrier en bloc tous les fichiers WAL nécessaires.',
      explanation: '`-X stream` est impératif sur les bases actives : si l\'activité est intense, les WAL générés au début de la sauvegarde risquent d\'être recyclés ou purgés avant la fin du backup avec `-X fetch`, provoquant l\'échec de la sauvegarde.',
      examTrap: 'Puisque `-X stream` ouvre 2 connexions de réplication simultanées (une pour les données, une pour les WAL), le paramètre `max_wal_senders` sur le serveur maître doit être configuré à au moins **2** (ou plus).',
      ruleRef: 'PostgreSQL 16 Documentation - pg_basebackup: --wal-method',
    },
  },
  {
    id: 'fc-pgedb-dom05-035',
    cardNumber: 35,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Option `-R` (`--write-recovery-conf`) pour créer un Standby',
    difficulty: 'medium',
    tags: ['pg_basebackup', 'Standby', 'Replication', 'standby.signal', 'Postgres 16'],
    front: {
      question: 'Que fait précisément l\'option `-R` (`--write-recovery-conf`) dans `pg_basebackup` (dans les versions modernes de PostgreSQL dont PostgreSQL 16) ?',
      codeSnippet: 'pg_basebackup -h master_pg -U repuser -D /var/lib/postgresql/16/data -Fp -Xs -R',
      hint: 'Création du fichier signal et injection des paramètres de connexion dans postgresql.auto.conf.',
    },
    back: {
      answer: 'L\'option **`-R`** configure automatiquement la cible pour qu\'elle démarre immédiatement en tant que **serveur secondaire réplica (Standby)** :\n\n1. Elle crée le fichier vide **`standby.signal`** à la racine de `$PGDATA`.\n2. Elle injecte la chaîne de connexion du primaire (`primary_conninfo = \'host=... user=... port=...\'`) dans le fichier **`postgresql.auto.conf`**.',
      explanation: 'Historiquement (avant PostgreSQL 12), cette option créait un fichier `recovery.conf`. Depuis PostgreSQL 12+, elle configure `standby.signal` et `postgresql.auto.conf`.',
      examTrap: 'À l\'examen, toute proposition mentionnant la génération d\'un fichier `recovery.conf` dans PostgreSQL 16 est un piège obsolète !',
      ruleRef: 'PostgreSQL 16 Documentation - pg_basebackup: -R, --write-recovery-conf',
    },
  },
  {
    id: 'fc-pgedb-dom05-036',
    cardNumber: 36,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Remapping des tablespaces avec `-T` (`--tablespace-mapping`)',
    difficulty: 'medium',
    tags: ['pg_basebackup', 'Tablespaces', 'Tablespace-Mapping', 'Storage'],
    front: {
      question: 'Comment sauvegarder avec `pg_basebackup` un cluster possédant des tablespaces externes situés sur des disques spécifiques, en redirigeant leurs répertoires vers un autre emplacement ?',
      codeSnippet: 'pg_basebackup -D /data/backup -Fp -T /data/ts_indexes=/backup/ts_indexes',
      hint: 'Syntaxe `-T ancien_chemin=nouveau_chemin`.',
    },
    back: {
      answer: 'On utilise l\'option **`-T` (`--tablespace-mapping=olddir=newdir`)** :\n```bash\npg_basebackup -h srv1 -U repuser -D /backup/pgdata -Fp \\\n  -T /mnt/ssd/fast_ts=/backup/mapped_ts\n```\nCette option demande à `pg_basebackup` de remplacer le lien symbolique interne du tablespace pour qu\'il pointe vers le nouvel emplacement spécifié.',
      explanation: 'Cette option peut être spécifiée plusieurs fois s\'il y a plusieurs tablespaces. Elle est indispensable lors de la duplication locale ou vers un serveur ayant une topologie de disques différente.',
      examTrap: 'Si vous utilisez le format plain (`-Fp`) sans `-T` sur la même machine physique que le primaire, la sauvegarde échouera car elle essaiera d\'écrire les fichiers dans les répertoires existants du primaire en production.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_basebackup: -T, --tablespace-mapping',
    },
  },
  {
    id: 'fc-pgedb-dom05-037',
    cardNumber: 37,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Limitation de la bande passante avec `-r` (`--max-rate`)',
    difficulty: 'easy',
    tags: ['pg_basebackup', 'Throttling', 'Bandwidth', 'I/O'],
    front: {
      question: 'Comment brider la bande passante consommée par `pg_basebackup` pour éviter de saturer la carte réseau ou les disques du serveur primaire en production ?',
      codeSnippet: 'pg_basebackup -D /backup/base -r 50M',
      hint: 'Option `-r` ou `--max-rate` avec suffixe k ou M.',
    },
    back: {
      answer: 'En utilisant l\'option **`-r` (`--max-rate=<rate>`)** :\n```bash\npg_basebackup -h prod_db -U repuser -D /backup/data -X stream -r 50M\n```\nElle limite le débit de transfert à la valeur indiquée par seconde (ex: `50M` pour 50 mégaoctets/seconde, `500k` pour 500 kilooctets/seconde).',
      explanation: 'Le bridage s\'applique au processus d\'envoi sur le serveur primaire, garantissant que les I/O de sauvegarde n\'empiètent pas sur les transactions clientes en cours.',
      examTrap: 'Le débit spécifié est compté en octets par seconde (bytes/sec) et non en bits par seconde.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_basebackup: -r, --max-rate',
    },
  },
  {
    id: 'fc-pgedb-dom05-038',
    cardNumber: 38,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Compression moderne dans PostgreSQL 16 (`-Z` / `--compress`)',
    difficulty: 'medium',
    tags: ['pg_basebackup', 'Compression', 'lz4', 'zstd', 'Postgres 16'],
    front: {
      question: 'Quels algorithmes de compression modernes sont supportés par `pg_basebackup` dans PostgreSQL 16 via l\'option `-Z` (`--compress`) et quel est l\'avantage de `server-zstd` ?',
      codeSnippet: 'pg_basebackup -D /backup/base -Ft -Z server-zstd:level=5',
      hint: 'gzip, lz4, zstd avec compression côté client ou côté serveur.',
    },
    back: {
      answer: 'PostgreSQL 16 supporte les algorithmes modernes **gzip**, **lz4** et **zstd**.\n\nOn peut choisir l\'emplacement de la compression :\n- **`client-zstd`** : Le serveur envoie les données brutes, le client `pg_basebackup` compresse (soulage le CPU du serveur).\n- **`server-zstd`** : Le serveur compresse les données avant de les émettre sur le réseau (réduit considérablement la bande passante réseau consommée).',
      explanation: '`zstd` offre un ratio de compression supérieur à `gzip` tout en étant nettement plus rapide, particulièrement efficace pour sauvegarder sur des liens réseau distants (WAN / Cloud).',
      examTrap: 'La compression côté serveur (`server-zstd`/`server-lz4`) nécessite que PostgreSQL ait été compilé avec le support de ces bibliothèques sur le serveur.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_basebackup: -Z, --compress',
    },
  },
  {
    id: 'fc-pgedb-dom05-039',
    cardNumber: 39,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Comportement du Checkpoint initial : `-c fast` vs `-c spread`',
    difficulty: 'medium',
    tags: ['pg_basebackup', 'Checkpoint', 'Performance', 'I/O Spike'],
    front: {
      question: 'À quoi sert l\'option `-c` (`--checkpoint`) dans `pg_basebackup` et quelle est la différence entre `fast` et `spread` ?',
      codeSnippet: 'pg_basebackup -D /backup/base -c fast\nvs\npg_basebackup -D /backup/base -c spread',
      hint: 'L\'un démarre immédiatement au prix d\'un pic d\'écritures disque, l\'autre s\'étale doucement.',
    },
    back: {
      answer: 'Avant de copier les fichiers, `pg_basebackup` doit déclencher un Checkpoint sur le primaire pour garantir la cohérence initiale du point de départ.\n\n- **`-c fast`** : Force un checkpoint immédiat et agressif. La sauvegarde démarre très vite, mais cela peut causer un pic sévère d\'I/O disque perturbant les transactions applicatives.\n- **`-c spread`** (Défaut) : Émet un checkpoint étalé dans le temps (respectant `checkpoint_completion_target`). La sauvegarde attend que le checkpoint se termine en douceur.',
      explanation: 'En environnement de production sensible aux latences, `-c spread` est recommandé pour éviter les micro-gels d\'application causés par le vidage brutal des `shared_buffers`.',
      examTrap: 'Avec `-c spread`, `pg_basebackup` peut sembler "bloqué" pendant plusieurs minutes sans afficher de progrès : il attend simplement la fin du checkpoint étalé.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_basebackup: -c, --checkpoint',
    },
  },
  {
    id: 'fc-pgedb-dom05-040',
    cardNumber: 40,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Suivi de la progression en direct (`-P` et `pg_stat_progress_basebackup`)',
    difficulty: 'easy',
    tags: ['pg_basebackup', 'Monitoring', 'Progress', 'CLI'],
    front: {
      question: 'Comment suivre l\'avancement en temps réel d\'un `pg_basebackup` côté client et côté serveur ?',
      hint: 'Option `-P` côté client et vue système `pg_stat_progress_*` côté serveur.',
    },
    back: {
      answer: '1. **Côté client** : Utiliser l\'option **`-P` (`--progress`)** combinée avec `-v` (`--verbose`). Elle affiche une barre de progression avec le pourcentage, les gigaoctets transférés et le temps estimé restant.\n2. **Côté serveur** : Interroger la vue dynamique **`pg_stat_progress_basebackup`** :\n```sql\nSELECT pid, phase, backup_total, backup_streamed, tablespaces_total\nFROM pg_stat_progress_basebackup;\n```',
      explanation: 'Pour calculer la taille totale estimée, le serveur fait un parcours rapide des métadonnées avant le début du streaming.',
      examTrap: 'L\'estimation de progression peut varier si l\'activité d\'écriture est intense sur le cluster pendant la sauvegarde physique.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 28: pg_stat_progress_basebackup',
    },
  },

  // =========================================================================
  // SECTION 5 : SAUVEGARDE PHYSIQUE BAS NIVEAU & BACKUP_LABEL (Cartes 41 à 50)
  // pg_backup_start/stop, fichier backup_label, tablespace_map, LVM snapshot
  // =========================================================================
  {
    id: 'fc-pgedb-dom05-041',
    cardNumber: 41,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'API SQL de sauvegarde physique de bas niveau (Low-Level API)',
    difficulty: 'medium',
    tags: ['Low-Level Backup', 'pg_backup_start', 'pg_backup_stop', 'SQL API'],
    front: {
      question: 'Quelles sont les fonctions SQL modernes de bas niveau utilisées pour encadrer une sauvegarde physique par copie de fichiers ou snapshot de stockage (ex: snapshot SAN/LVM/EBS) ?',
      hint: 'Fonctions start et stop introduites pour remplacer les anciennes commandes exclusives.',
    },
    back: {
      answer: 'Les fonctions SQL sont **`pg_backup_start("label")`** et **`pg_backup_stop()`**.\n\nSéquence standard :\n```sql\n-- 1. Signaler le début de la sauvegarde\nSELECT pg_backup_start(\'Sauvegarde_LVM_hebdo\', false);\n\n-- 2. Copier les fichiers au niveau OS ou faire le snapshot stockage (tar, rsync, lvm)\n\n-- 3. Signaler la fin de la sauvegarde\nSELECT pg_backup_stop();\n```',
      explanation: 'Ces fonctions modernes de sauvegarde non exclusive permettent à plusieurs outils de sauvegarde d\'opérer simultanément sans conflit.',
      examTrap: 'Les anciennes fonctions `pg_start_backup()` et `pg_stop_backup()` ont été supprimées dans PostgreSQL 15+. Dans PostgreSQL 16, seules `pg_backup_start` et `pg_backup_stop` existent.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Low-Level API',
    },
  },
  {
    id: 'fc-pgedb-dom05-042',
    cardNumber: 42,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Mécanisme interne de `pg_backup_start()`',
    difficulty: 'hard',
    tags: ['pg_backup_start', 'Internals', 'Checkpoint', 'Full Page Writes'],
    front: {
      question: 'Que se passe-t-il exactement sous le capot lorsque vous exécutez `SELECT pg_backup_start("backup_tag");` sur PostgreSQL ?',
      hint: 'Pensez au checkpoint initial, au basculement en mode backup et à la protection contre les écritures partielles.',
    },
    back: {
      answer: '`pg_backup_start()` effectue 3 actions fondamentales :\n\n1. **Déclenche un Checkpoint** immédiat (ou étalé) et force l\'écriture de tous les tampons modifiés sur le disque.\n2. **Enregistre le LSN de départ (Start LSN)** dans le flux WAL.\n3. **Active le mode `full_page_writes` forcé** dans tout le moteur : toute modification de bloc de données provoquera l\'écriture de la page entière de 8 Ko dans les WAL lors du premier accès, garantissant la réparabilité des blocs corrompus par une copie OS concurrente (*torn pages*).',
      explanation: 'Sans `full_page_writes`, si une copie physique lit un bloc de 8 Ko pendant que PostgreSQL en écrit la moitié, la copie contiendrait une page déchirée irrécupérable.',
      examTrap: '`pg_backup_start` ne fige pas les écritures de la base de données : les transactions continuent normalement d\'insérer et modifier des données.',
      ruleRef: 'PostgreSQL 16 Documentation - Low-Level Backup: pg_backup_start details',
    },
  },
  {
    id: 'fc-pgedb-dom05-043',
    cardNumber: 43,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Le fichier critique `backup_label`',
    difficulty: 'hard',
    tags: ['backup_label', 'PITR', 'Recovery', 'Internals'],
    front: {
      question: 'Qu\'est-ce que le fichier `backup_label`, quelles informations cruciales contient-il et quel est son rôle lors d\'une restauration ?',
      codeSnippet: 'START WAL LOCATION: 0/1A000028 (file 00000001000000000000001A)\nCHECKPOINT LOCATION: 0/1A000060\nSTART TIME: 2026-09-15 14:00:00 UTC\nLABEL: Sauvegarde_LVM_hebdo',
      hint: 'Il indique à PostgreSQL où commence l\'état cohérent dans les WAL.',
    },
    back: {
      answer: 'Le fichier **`backup_label`** est généré par `pg_backup_stop()` (ou intégré par `pg_basebackup`). Il est placé à la racine de `$PGDATA` de la sauvegarde.\n\nInformations contenues :\n- **START WAL LOCATION** : Le point LSN exact où commence la sauvegarde.\n- **CHECKPOINT LOCATION** : Le point du checkpoint initial associé.\n- **START TIME & LABEL** : L\'heure et le libellé de la sauvegarde.\n\n**Rôle lors de la restauration** : Au démarrage, PostgreSQL lit ce fichier pour savoir à partir de quel segment WAL précis il doit obligatoirement rejouer les logs pour atteindre un état cohérent (*Consistent State*).',
      explanation: 'Dès que le serveur a atteint l\'état cohérent lors du replay des WAL, il supprime (ou renomme en `.old`) le fichier `backup_label`.',
      examTrap: 'Si le fichier `backup_label` est manquant dans une sauvegarde physique, PostgreSQL ne saura pas qu\'il s\'agit d\'un backup à chaud : il croira à un crash brutal et tentera de démarrer sans rejouer les WAL nécessaires, corrompant les données.',
      ruleRef: 'PostgreSQL 16 Documentation - Continuous Archiving: backup_label file',
    },
  },
  {
    id: 'fc-pgedb-dom05-044',
    cardNumber: 44,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Piège d\'administration mortel : le fichier `backup_label` sur le primaire',
    difficulty: 'hard',
    tags: ['backup_label', 'Fatal Trap', 'Crash', 'Admin Disaster'],
    front: {
      question: 'Pourquoi ne faut-il JAMAIS créer ou laisser un fichier nommé `backup_label` dans le `$PGDATA` d\'un serveur primaire en cours de fonctionnement ?',
      hint: 'Que se passerait-il si le serveur venait à redémarrer ou crasher ?',
    },
    back: {
      answer: 'Si un fichier `backup_label` est présent dans le `$PGDATA` du primaire actif et que le serveur subit un crash ou un redémarrage impromptu :\n\nPostgreSQL lira ce fichier au boot, pensera qu\'il démarre à partir d\'une ancienne sauvegarde restaurée, cherchera à rejouer des WAL archivés depuis le `START WAL LOCATION` ancien, et **refusera catégoriquement de démarrer en mode lecture/écriture normal** s\'il ne trouve pas les fichiers WAL dans l\'archive !',
      explanation: 'C\'est précisément pour cette raison que `pg_backup_start()` moderne n\'écrit plus de fichier `backup_label` sur le disque du primaire. C\'est `pg_backup_stop()` qui renvoie le contenu du `backup_label` en résultat SQL, et c\'est le script de sauvegarde qui doit l\'écrire dans le dossier de destination de la copie.',
      examTrap: 'Ne copiez jamais un fichier `backup_label` provenant d\'un backup vers votre instance de production principale.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Low-Level Backup Caveats',
    },
  },
  {
    id: 'fc-pgedb-dom05-045',
    cardNumber: 45,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Le fichier `tablespace_map`',
    difficulty: 'medium',
    tags: ['tablespace_map', 'Tablespaces', 'Low-Level Backup', 'Symlinks'],
    front: {
      question: 'Quel est le rôle du fichier `tablespace_map` généré lors d\'une sauvegarde physique de bas niveau ?',
      hint: 'Faire correspondre les liens symboliques de `pg_tblspc` aux chemins réels.',
    },
    back: {
      answer: 'Le fichier **`tablespace_map`** enregistre les correspondances entre les identifiants OID des tablespaces (liens symboliques dans `$PGDATA/pg_tblspc/`) et leurs répertoires cibles absolus sur le système de fichiers.\n\nExemple de contenu :\n```text\n16390 /mnt/disque_rapide/data_ts\n16391 /mnt/disque_sec/archive_ts\n```',
      explanation: 'Lors d\'une restauration, ce fichier permet à PostgreSQL de recréer correctement les liens symboliques dans `pg_tblspc` si les données ont été copiées séparément.',
      examTrap: 'Tout comme `backup_label`, ce fichier est renvoyé par `pg_backup_stop()` et ne doit être injecté que dans l\'archive de sauvegarde, jamais sur le serveur primaire actif.',
      ruleRef: 'PostgreSQL 16 Documentation - Low-Level Backup: tablespace_map',
    },
  },
  {
    id: 'fc-pgedb-dom05-046',
    cardNumber: 46,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Annulation d\'une sauvegarde physique avec `pg_backup_abort()`',
    difficulty: 'medium',
    tags: ['pg_backup_abort', 'Error Recovery', 'Low-Level API', 'Maintenance'],
    front: {
      question: 'Que devez-vous faire si votre script de sauvegarde physique échoue au milieu de la copie de fichiers (ex: disque plein) après avoir exécuté `pg_backup_start()` ?',
      hint: 'Appeler une fonction d\'abandon pour libérer l\'état interne de sauvegarde.',
    },
    back: {
      answer: 'Vous devez exécuter la fonction SQL **`SELECT pg_backup_abort();`** dans la même session (ou sur le serveur).\n\nCette fonction désactive le mode de sauvegarde à chaud, libère les verrous de session et permet au moteur de revenir à son état nominal.',
      explanation: 'Si la session client qui a initié `pg_backup_start` est fermée ou coupée par le réseau, PostgreSQL annule automatiquement la sauvegarde.',
      examTrap: 'Ne laissez jamais une sauvegarde active sans appeler soit `pg_backup_stop()` soit `pg_backup_abort()`, car cela maintient le forçage de `full_page_writes` et peut consommer plus de WAL.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_backup_abort() reference',
    },
  },
  {
    id: 'fc-pgedb-dom05-047',
    cardNumber: 47,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Prérequis des snapshots de stockage (LVM, SAN, EBS Cloud)',
    difficulty: 'hard',
    tags: ['Storage Snapshot', 'LVM', 'SAN', 'EBS', 'Crash-Consistency'],
    front: {
      question: 'Pourquoi un simple snapshot de baie de stockage SAN, LVM ou AWS EBS pris sans précaution ne constitue-t-il pas une sauvegarde en ligne fiable sans `pg_backup_start` ?',
      hint: 'Crash-consistency vs cohérence applicative avec archivage WAL.',
    },
    back: {
      answer: 'Un snapshot bloc de stockage pur sans avertir PostgreSQL est au mieux **crash-consistent** :\n1. Si la base est sur plusieurs disques ou tablespaces différents, le snapshot n\'est pas atomique entre les disques sans freeze de stockage.\n2. Sans `pg_backup_start`, aucun fichier `backup_label` n\'est généré, empêchant de connaître le point exact de cohérence WAL.\n3. Pour être garanti récupérable sans dépendre uniquement d\'un crash-recovery risqué, le snapshot doit être encadré par `pg_backup_start()` et `pg_backup_stop()`, et les WAL produits pendant le snapshot doivent être archivés.',
      explanation: 'Avec `pg_backup_start`, le checkpoint est fait et le LSN de départ est gravé dans la mémoire de PostgreSQL.',
      examTrap: 'Croire qu\'un snapshot EBS ou VM pris "à chaud" sans aucune commande PostgreSQL est suffisant pour garantir un RPO=0 ou une restauration Point-In-Time.',
      ruleRef: 'EDB Postgres Backup Guide - Storage Snapshots vs Database Backups',
    },
  },
  {
    id: 'fc-pgedb-dom05-048',
    cardNumber: 48,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Suppression des sauvegardes exclusives depuis PostgreSQL 15',
    difficulty: 'medium',
    tags: ['PostgreSQL 15', 'PostgreSQL 16', 'Exclusive Backup', 'Evolution'],
    front: {
      question: 'Pourquoi l\'ancienne fonction `pg_start_backup(\'label\', true)` a-t-elle été définitivement supprimée dans PostgreSQL 15 et 16 ?',
      hint: 'Risques liés au fichier physique écrit sur le primaire en cas de crash.',
    },
    back: {
      answer: 'L\'ancien mode exclusif écrivait directement le fichier `backup_label` dans `$PGDATA` sur le serveur primaire actif. Si le serveur crashait pendant la sauvegarde, il ne pouvait plus redémarrer sans intervention manuelle risquée.\n\nDans PostgreSQL 15 et 16, **seules les sauvegardes non exclusives sont autorisées** (`pg_backup_start`), garantissant la résilience totale du primaire.',
      explanation: 'Désormais, l\'état de sauvegarde est rattaché à la connexion de session cliente.',
      examTrap: 'Sur l\'examen EDB ASSOC 16, toute question ou réponse utilisant la syntaxe `pg_start_backup(...)` ou `pg_stop_backup(...)` fait référence à une syntaxe dépréciée et supprimée.',
      ruleRef: 'PostgreSQL 16 Documentation - Release Notes: Backup API Changes',
    },
  },
  {
    id: 'fc-pgedb-dom05-049',
    cardNumber: 49,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Obligation absolue d\'archivage des WAL pendant la sauvegarde',
    difficulty: 'hard',
    tags: ['WAL Archiving', 'Physical Backup', 'Consistency', 'WAL segments'],
    front: {
      question: 'Que renvoie `pg_backup_stop()` et pourquoi la sauvegarde est-elle incomplète tant que le dernier fichier WAL n\'a pas été archivé ?',
      codeSnippet: 'SELECT * FROM pg_backup_stop(true);',
      hint: 'LSN de début et LSN de fin : tous les WAL intermédiaires sont obligatoires.',
    },
    back: {
      answer: '`pg_backup_stop()` renvoie 3 colonnes : `lsn` (le stop LSN), `labelfile` (le contenu du fichier `backup_label`), et `spcmapfile` (le contenu de `tablespace_map`).\n\n**Règle absolue** : La sauvegarde physique n\'est intègre et restaurable QUE si **TOUS les segments WAL générés entre le `START WAL LOCATION` et le `STOP WAL LOCATION` sont conservés** dans la sauvegarde ou dans l\'archive WAL.',
      explanation: 'Si ne serait-ce qu\'un seul fichier WAL de cet intervalle manque à l\'appel lors de la restauration, le moteur s\'arrêtera avec une erreur et la base sera corrompue et inutilisable.',
      examTrap: 'Le paramètre booléen de `pg_backup_stop(wait_for_archive => true)` attend que le dernier fichier WAL soit confirmé archivé avant de rendre la main.',
      ruleRef: 'PostgreSQL 16 Documentation - pg_backup_stop() parameters',
    },
  },
  {
    id: 'fc-pgedb-dom05-050',
    cardNumber: 50,
    domainId: 'pg-edb-dom-05',
    domainCode: 'DOM-05',
    domainTitle: 'Sauvegardes physiques, logiques (pg_dump) & Réplication streaming',
    subtopic: 'Contrainte de session pour `pg_backup_start`',
    difficulty: 'medium',
    tags: ['Session', 'Connection', 'pg_backup_start', 'Troubleshooting'],
    front: {
      question: 'Pourquoi `pg_backup_start()` et `pg_backup_stop()` doivent-ils être impérativement exécutés dans la MÊME session cliente PostgreSQL ?',
      hint: 'L\'état de sauvegarde non exclusive est stocké en mémoire de session.',
    },
    back: {
      answer: 'Puisque le mode de sauvegarde moderne est non exclusif, l\'état actif de la sauvegarde est lié à la **session de connexion cliente en cours**.\n\nSi vous exécutez `pg_backup_start()` dans une session psql puis que vous quittez (fermeture de session), PostgreSQL considère que la sauvegarde a été abandonnée et appelle automatiquement `pg_backup_abort()`. Si vous ouvrez une nouvelle session pour appeler `pg_backup_stop()`, elle renverra une erreur indiquant qu\'aucune sauvegarde n\'est en cours pour cette session.',
      explanation: 'Dans les scripts shell automatisés, on garde un descripteur de fichier ouvert ou un process psql persistant pour exécuter start, copier les fichiers, puis exécuter stop dans la même connexion.',
      examTrap: 'Exécuter `psql -c "SELECT pg_backup_start(\'...\');"` en ligne de commande One-Shot ferme la session immédiatement à la fin de la commande, annulant instantanément la sauvegarde.',
      ruleRef: 'PostgreSQL 16 Documentation - Chapter 26: Non-Exclusive Low-Level Backup',
    },
  },
];
